PM09 / LA-007 / DIAGNOSTICO DE FUENTE
No modifica fuente.js. Extrae contexto para reproducir antes de corregir.
bytes_fuente=5238267

PATRON 'function\\s+Resultados\\w*\\s*\\(': 1 coincidencias
--- coincidencia 1 pos=4680237 ---
 font-semibold mb-1" }, "\u26A0 Revisa estas l\xEDneas del texto pegado"), /* @__PURE__ */ import_react4.default.createElement("div", { className: "text-[11.5px] mb-2" }, "En estas los n\xFAmeros no cuadraban entre s\xED. No he inventado nada: compru\xE9balas contra el albar\xE1n."), avisosPegado.map((a22, i33) => /* @__PURE__ */ import_react4.default.createElement("div", { key: i33, className: "text-[11.5px]" }, "\xB7 ", a22))), desviaciones.length > 0 && /* @__PURE__ */ import_react4.default.createElement(Card, { className: "mb-3", style: { background: C2.amberSoft, border: "none" } }, /* @__PURE__ */ import_react4.default.createElement("div", { className: "text-[12px] font-semibold mb-1" }, "\u26A0 El precio ha cambiado respecto a la \xFAltima compra"), /* @__PURE__ */ import_react4.default.createElement("div", { className: "text-[11.5px] mb-2" }, "Compru\xE9balo antes de confirmar: una subida que pasa desapercibida se lleva el margen sin que se note."), desviaciones.map((d2) => /* @__PURE__ */ import_react4.default.createElement("div", { key: d2.productoId, className: "text-[11.5px] mono" }, d2.nombre, ": \u20AC", fmt(d2.antes), " \u2192 \u20AC", fmt(d2.ahora), " ", /* @__PURE__ */ import_react4.default.createElement("b", { style: { color: d2.variacion > 0 ? C2.red : C2.accent } }, "(", d2.variacion > 0 ? "+" : "", d2.variacion, "%)")))), error && /* @__PURE__ */ import_react4.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C2.red } }, error), !bloqueado && /* @__PURE__ */ import_react4.default.createElement("div", { className: "flex gap-2 flex-wrap" }, /* @__PURE__ */ import_react4.default.createElement(Btn, { onClick: darEntrada, disabled: procesandoEntrada }, /* @__PURE__ */ import_react4.default.createElement(CircleCheck, { size: 14 }), " ", procesandoEntrada ? "Dando entrada\u2026" : "Dar entrada al almac\xE9n"), /* @__PURE__ */ import_react4.default.createElement(Btn, { variant: "ghost", onClick: guardarBorrador }, "Guardar como borrador"), /* @__PURE__ */ import_react4.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setModo("lista");
    setAlb(null);
    setFotoRevisionIA("");
  } }, "Cancelar")));
}
function Resultados({ movimientos, productos, productoPorId, gastosGenerales, addGasto, deleteGasto, empleados = [] }) {
  const [desde, setDesde] = (0, import_react4.useState)(primerDiaMes(/* @__PURE__ */ new Date()));
  const [hasta, setHasta] = (0, import_react4.useState)(ultimoDiaMes(/* @__PURE__ */ new Date()));
  const [showGasto, setShowGasto] = (0, import_react4.useState)(false);
  const [gastoForm, setGastoForm] = (0, import_react4.useState)({ concepto: "", importe: "", categoria: "" });
  const [gastoError, setGastoError] = (0, import_react4.useState)("");
  function irMesActual() {
    setDesde(primerDiaMes(/* @__PURE__ */ new Date()));
    setHasta(ultimoDiaMes(/* @__PURE__ */ new Date()));
  }
  function irMesAnterior() {
    const d2 = /* @__PURE__ */ new Date();
    d2.setMonth(d2.getMonth() - 1);
    setDesde(primerDiaMes(d2));
    setHasta(ultimoDiaMes(d2));
  }
  const salidasPeriodo = (0, import_react4.useMemo)(
    () => movimientos.filter((m22) => esSalida(m22) && m22.motivo && m22.fecha >= desde && m22.fecha <= hasta),
    [movimientos, desde, hasta]
  );
  const ventas = salidasPeriodo.filter((m22) => esVenta(m22));
  const autoconsumo = salidasPeriodo.filter((m22) => m22.motivo === "Autoconsumo");
  const roturas = salidasPeriodo.filter((m22) => m22.motivo === "Rotura");
  const mermas = salidasPeriodo.filter((m22) => esMerma(m22) && m22.motivo !== "Autoconsumo" && m22.motivo !== "Rotura");
  const aceite = salidasPeriodo.filter((m22) => m22.motivo === "Cambio de aceite" || m22.motivo === "Relleno de aceite");
  const ingresos = ventas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0), 0);
  const costeVentas = ventas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.costoUnitario) || 0), 0);
  const margenBruto = ingresos - costeVentas;
  const margenPct = ingresos > 0 ? margenBruto / ingresos * 100 : null;
  const valorMermas = mermas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.costoUnitario) || 0), 0);
  const valorAutoconsumo = autoconsumo.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.costoUnitario) || 0), 0);
  const valorRoturas = roturas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.costoUnitario) || 0), 0);
  const valorAceite = aceite.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.costoUnitario) || 0), 0);
  const mermasPorProducto = (0, import_react4.useMemo)(() => {
    const mapa = {};
    mermas.forEach((m22) => {
      mapa[m22.productoId] = (mapa[m22.productoId] || 0) + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.costoUnitario) || 0);
    });
    return Object.entries(mapa).map(([id, valor]) => ({ producto: productoPorId(id), valor })).filter((x3) => x3.producto && x3.valor > 0).sort((a22, b2) => b2.valor - a22.valor).slice(0, 5);
  }, [mermas, productoPorId]);
  const dias = Math.max(1, Math.round((new Date(hasta) - new Date(desde)) / 864e5) + 1);
  const gastosFijos = gastosGenerales.filter((g2) => !g2.fecha);
  const gastosPuntuales = gastosGenerales.filter((g2) => g2.fecha);
  const gastosGeneralesMensual = gastosFijos.reduce((a22, g2) => a22 + (Number(g2.importe) || 0), 0);
  const gastosPuntualesPeriodo = gastosPuntuales.filter((g2) => g2.fecha >= desde && g2.fecha <= hasta).reduce((a22, g2) => a22 + (Number(g2.importe) || 0), 0);
  const desgloseGastosPorCategoria = (() => {
    const mapa = {};
    const suma = (cat, importe) => {
      const clave = cat && cat.trim() ? cat : "Sin categor\xEDa";
      mapa[clave] = (mapa[clave] || 0) + importe;
    };
    gastosFijos.forEach((g2) => suma(g2.categoria, (Number(g2.importe) || 0) * (dias / 30)));
    gastosPuntuales.filter((g2) => g2.fecha >= desde && g2.fecha <= hasta).forEach((g2) => suma(g2.categoria, Number(g2.importe) || 0));
    return Object.entries(mapa).map(([categoria, importe]) => ({ categoria, importe })).sort((a22, b2) => b2.importe - a22.importe);
  })();
  const costePersonalMensual = empleados.filter((e2) => e2.activo !== false).reduce((a22, e2) => {
    const bruto = Number(e2.salarioBrutoMensual) || 0;
    const coste = e2.costeEmpresaMensual !== "" && e2.costeEmpresaMensual != null ? Number(e2.costeEmpresaMensual) : bruto * 1.32;
    return a22 + coste;
  }, 0);
  const personalEsEstimado = empleados.some((e2) => e2.activo !== false && (e2.costeEmpresaMensual === "" || e2.costeEmpresaMensual == null) && Number(e2.salarioBrutoMensual) > 0);
  const gastosTotalMensual = gastosGeneralesMensual + costePersonalMensual;
  const gastosPeriodo = gastosTotalMensual * (dias / 30) + gastosPuntualesPeriodo;
  const costeInventarioSinVenta = valorMermas + valorAutoconsumo + valorRoturas + valorAceite;
  const resultadoNeto = margenBruto - gastosPeriodo - costeInventarioSinVenta;
  const huboLegacy = movimientos.some((m22) => m22.tipo === "salida" && !m22.motivo);
  const margenTeorico = (0, import_react4.useMemo)(() => {
    const conPrecio = productos.filter((p22) => Number(p22.precioVenta) > 0);
    if (!conPrecio.length) return null;
    const total = conPrecio.reduce((a22, p22) => a22 + (margenDe(p22) || 0), 0);
   
--- fin coincidencia ---

PATRON 'function\\s+esVenta\\s*\\(': 1 coincidencias
--- coincidencia 1 pos=3982901 ---

    return { producto: p22, puntuacion: coincidencias / palabras.length };
  }).filter((c22) => c22.puntuacion > 0).sort((a22, b2) => b2.puntuacion - a22.puntuacion);
  return candidatos.slice(0, maximo);
}
function precioNeto(p22) {
  const pvp = Number(p22 && p22.precioVenta) || 0;
  return pvp / (1 + ivaDe(p22) / 100);
}
function margenDe(p22) {
  const neto = precioNeto(p22);
  if (neto <= 0) return null;
  return (neto - (Number(p22.costo) || 0)) / neto * 100;
}
var uid = () => Date.now().toString(36) + Math.random().toString(36).slice(2, 8);
function valorCF(item) {
  if (!item) return null;
  const candidatos = [item.conteo, item.cf, item.c3, item.c2, item.c1, item.cantidadContada];
  for (const v22 of candidatos) {
    if (v22 !== "" && v22 !== null && v22 !== void 0 && !isNaN(Number(v22))) return Number(v22);
  }
  return null;
}
var fmt = (n2) => (Number(n2) || 0).toLocaleString("es-ES", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
async function hashPin(pin) {
  if (!pin) return "";
  const datos = new TextEncoder().encode("chocoloyos-pin-v1-" + pin);
  const buffer = await crypto.subtle.digest("SHA-256", datos);
  return Array.from(new Uint8Array(buffer)).map((b2) => b2.toString(16).padStart(2, "0")).join("");
}
if (typeof window !== "undefined") {
  window.conectarNube = async function() {
    if (!window.NUBE_URL || !window.NUBE_CLAVE) return false;
    try {
      window.__nubeCliente = createClient(window.NUBE_URL, window.NUBE_CLAVE);
      window.__nubeActiva = true;
      return true;
    } catch (e2) {
      console.warn("Sin nube, se trabaja en local:", e2 && e2.message);
      window.__nubeActiva = false;
      return false;
    }
  };
  window.getSupabaseClient = async function() {
    return window.__nubeCliente;
  };
}
function esMovimientoNuevo(m22) {
  return m22 != null && m22.operationId !== void 0 && m22.operationId !== null;
}
function esSalida(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return Number(m22.cantidad) < 0;
  return m22.tipo === "salida";
}
function esEntrada(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return Number(m22.cantidad) > 0;
  return m22.tipo === "entrada";
}
function esVenta(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return m22.tipo === "VENTA" || m22.tipo === "VENTA_ENCARGO";
  return m22.tipo === "salida" && m22.motivo === "Venta";
}
var MOTIVOS_MERMA_ANTIGUOS = /* @__PURE__ */ new Set(["Merma", "Autoconsumo", "Rotura"]);
var MOTIVOS_MERMA = [
  "Merma",
  "Autoconsumo",
  "Rotura",
  "Producci\xF3n",
  "Caducidad",
  "Deterioro",
  "Robo o p\xE9rdida",
  "Error de registro",
  "Devoluci\xF3n no registrada",
  "Consumo interno",
  "Degustaci\xF3n",
  "Producto defectuoso"
];
function esMerma(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return m22.tipo === "MERMA";
  return m22.tipo === "salida" && MOTIVOS_MERMA_ANTIGUOS.has(m22.motivo);
}
function cantidadConSigno(m22) {
  if (!m22) return 0;
  if (esMovimientoNuevo(m22)) return Number(m22.cantidad) || 0;
  const base = Number(m22.cantidad) || 0;
  return m22.tipo === "salida" ? -base : base;
}
var TIPOS_NO_MERCANCIA = ["utillaje", "limpieza"];
var esNoMercancia = (p22) => TIPOS_NO_MERCANCIA.includes(p22?.tipo || "materia_prima");
var aISO = (d2) => {
  const y22 = d2.getFullYear();
  const m22 = String(d2.getMonth() + 1).padStart(2, "0");
  const dd = String(d2.getDate()).padStart(2, "0");
  return `${y22}-${m22}-${dd}`;
};
var todayISO = () => aISO(/* @__PURE__ */ new Date());
var primerDiaMes = (fecha) => {
  const d2 = new Date(fecha);
  return aISO(new Date(d2.getFullYear(), d2.getMonth(), 1));
};
var ultimoDiaMes = (fecha) => {
  const d2 = new Date(fecha);
  return aISO(new Date(d2.getFullYear(), d2.getMonth() + 1, 0));
};
function horasDeEmpleadoEnMes(fichajes, empleadoId, mes) {
  const delMes = fichajes.filter((f22) => f22.empleadoId === empleadoId && (f22.fecha || "").slice(0, 7) === mes).sort((a22, b2) => (a22.timestamp || "").localeCompare(b2.timestamp || ""));
  let total = 0;
  let entrada = null;
  delMes.forEach((f22) => {
    if (f22.tipo === "entrada") entrada = f22;
    else if (f22.tipo === "salida" && entrada) {
      const ini = /* @__PURE__ */ new Date(`${entrada.fecha}T${entrada.hora}:00`);
      const fin = /* @__PURE__ */ new Date(`${f22.fecha}T${f22.hora}:00`);
      const horas = (fin - ini) / 36e5;
      if (horas > 0 && horas < 24) total += horas;
      entrada = null;
    }
  });
  return total;
}
function costePorHoraEquipoEnMes(nominas, fichajes, mes) {
  const nominasDelMes = nominas.filter((n2) => n2.mes === mes);
  const costeTotalMes = nominasDelMes.reduce((a22, n2) => a22 + (Number(n2.costeTotalEmpresa) || 0), 0);
  const horasTotalesMes = nominasDelMes.reduce((a22, n2) => a22 + horasDeEmpleadoEnMes(fichajes, n2.empleadoId, mes), 0);
  return horasTotalesMes > 0 ? costeTotalMes / horasTotalesMes : null;
}
async function loadKey(key, fallback, retries = 2) {
  for (let attempt = 0; attempt <= retries; attempt++) {
    try {
      const res = await window.storage.get(key, false);
      if (res && res.value) return JSON.parse(res.value);
      return fallback;
    } catch (e2) {
      if (attempt === retries) {
        console.error("No se pudo cargar", key, "tras varios intentos:", e2);
        return fallback;
      }
      await new Promise((r2) => setTimeout(r2, 350));
    }
  }
  return fallback;
}
async function saveKey(key, value) {
  try {
    await window.storage.set(key, JSON.stringify(value), false);
  } catch (e2) {
    console.error("Error guardando", key, e2);
    if (typeof window !== "undefined" && window.dispatchEvent) {
      window.dispatchEvent(new CustomEvent("fallo-guardado", { detail: { key, mensaje: e2 && e2.message } }));
    }
  }
}
async function sincronizarStockPm07({ setProductos, setMovimientos, localActivoId = null }) {
  if (typeof window === "undefined" || !window.__nubeActiva || typeof window.getSupabaseClient !== "function") return { ok: false, offline: true };
  const supabase = await window.getSupabaseClient();
  let qStock = supabase.from("stock_estado").select("empresa_id,local_id,producto_id,almacen,piso,total,minimo,bajo_minimo,fraccionable,precision_cantidad,local_operable");
  let qMov = supabase.from("movimientos_stock").select("id,operation_id,tipo,empresa_id,local_id,producto_id,delta_almacen,delta_piso,delta_total,cantidad,movimiento_original_id,datos,actor_user_id,created_at").order("created_at", { ascending: false }).limit(2e3);
  const [rStock, rMov] = await Promise.all([qStock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : null;
      const st2 = exacto || activo;
      if (!st2) return prod;
      return { ...prod, stock: Number(st2.total) || 0, stockPisoVenta: Number(st2.piso) || 0, stockMinimo: Number(st2.minimo) || 0, _pm07BajoMinimo: !!st2.bajo_minimo, _pm07Servidor: true, _pm07LocalOperable: st2.local_operable
--- fin coincidencia ---

PATRON 'function\\s+esSalida\\s*\\(': 1 coincidencias
--- coincidencia 1 pos=3982603 ---
arParaComparar(descripcion).split(" ").filter((w2) => w2.length >= 3);
  if (palabras.length === 0) return [];
  const candidatos = productos.map((p22) => {
    const nombreNorm = normalizarParaComparar(p22.nombre);
    const coincidencias = palabras.filter((w2) => nombreNorm.includes(w2)).length;
    return { producto: p22, puntuacion: coincidencias / palabras.length };
  }).filter((c22) => c22.puntuacion > 0).sort((a22, b2) => b2.puntuacion - a22.puntuacion);
  return candidatos.slice(0, maximo);
}
function precioNeto(p22) {
  const pvp = Number(p22 && p22.precioVenta) || 0;
  return pvp / (1 + ivaDe(p22) / 100);
}
function margenDe(p22) {
  const neto = precioNeto(p22);
  if (neto <= 0) return null;
  return (neto - (Number(p22.costo) || 0)) / neto * 100;
}
var uid = () => Date.now().toString(36) + Math.random().toString(36).slice(2, 8);
function valorCF(item) {
  if (!item) return null;
  const candidatos = [item.conteo, item.cf, item.c3, item.c2, item.c1, item.cantidadContada];
  for (const v22 of candidatos) {
    if (v22 !== "" && v22 !== null && v22 !== void 0 && !isNaN(Number(v22))) return Number(v22);
  }
  return null;
}
var fmt = (n2) => (Number(n2) || 0).toLocaleString("es-ES", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
async function hashPin(pin) {
  if (!pin) return "";
  const datos = new TextEncoder().encode("chocoloyos-pin-v1-" + pin);
  const buffer = await crypto.subtle.digest("SHA-256", datos);
  return Array.from(new Uint8Array(buffer)).map((b2) => b2.toString(16).padStart(2, "0")).join("");
}
if (typeof window !== "undefined") {
  window.conectarNube = async function() {
    if (!window.NUBE_URL || !window.NUBE_CLAVE) return false;
    try {
      window.__nubeCliente = createClient(window.NUBE_URL, window.NUBE_CLAVE);
      window.__nubeActiva = true;
      return true;
    } catch (e2) {
      console.warn("Sin nube, se trabaja en local:", e2 && e2.message);
      window.__nubeActiva = false;
      return false;
    }
  };
  window.getSupabaseClient = async function() {
    return window.__nubeCliente;
  };
}
function esMovimientoNuevo(m22) {
  return m22 != null && m22.operationId !== void 0 && m22.operationId !== null;
}
function esSalida(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return Number(m22.cantidad) < 0;
  return m22.tipo === "salida";
}
function esEntrada(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return Number(m22.cantidad) > 0;
  return m22.tipo === "entrada";
}
function esVenta(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return m22.tipo === "VENTA" || m22.tipo === "VENTA_ENCARGO";
  return m22.tipo === "salida" && m22.motivo === "Venta";
}
var MOTIVOS_MERMA_ANTIGUOS = /* @__PURE__ */ new Set(["Merma", "Autoconsumo", "Rotura"]);
var MOTIVOS_MERMA = [
  "Merma",
  "Autoconsumo",
  "Rotura",
  "Producci\xF3n",
  "Caducidad",
  "Deterioro",
  "Robo o p\xE9rdida",
  "Error de registro",
  "Devoluci\xF3n no registrada",
  "Consumo interno",
  "Degustaci\xF3n",
  "Producto defectuoso"
];
function esMerma(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return m22.tipo === "MERMA";
  return m22.tipo === "salida" && MOTIVOS_MERMA_ANTIGUOS.has(m22.motivo);
}
function cantidadConSigno(m22) {
  if (!m22) return 0;
  if (esMovimientoNuevo(m22)) return Number(m22.cantidad) || 0;
  const base = Number(m22.cantidad) || 0;
  return m22.tipo === "salida" ? -base : base;
}
var TIPOS_NO_MERCANCIA = ["utillaje", "limpieza"];
var esNoMercancia = (p22) => TIPOS_NO_MERCANCIA.includes(p22?.tipo || "materia_prima");
var aISO = (d2) => {
  const y22 = d2.getFullYear();
  const m22 = String(d2.getMonth() + 1).padStart(2, "0");
  const dd = String(d2.getDate()).padStart(2, "0");
  return `${y22}-${m22}-${dd}`;
};
var todayISO = () => aISO(/* @__PURE__ */ new Date());
var primerDiaMes = (fecha) => {
  const d2 = new Date(fecha);
  return aISO(new Date(d2.getFullYear(), d2.getMonth(), 1));
};
var ultimoDiaMes = (fecha) => {
  const d2 = new Date(fecha);
  return aISO(new Date(d2.getFullYear(), d2.getMonth() + 1, 0));
};
function horasDeEmpleadoEnMes(fichajes, empleadoId, mes) {
  const delMes = fichajes.filter((f22) => f22.empleadoId === empleadoId && (f22.fecha || "").slice(0, 7) === mes).sort((a22, b2) => (a22.timestamp || "").localeCompare(b2.timestamp || ""));
  let total = 0;
  let entrada = null;
  delMes.forEach((f22) => {
    if (f22.tipo === "entrada") entrada = f22;
    else if (f22.tipo === "salida" && entrada) {
      const ini = /* @__PURE__ */ new Date(`${entrada.fecha}T${entrada.hora}:00`);
      const fin = /* @__PURE__ */ new Date(`${f22.fecha}T${f22.hora}:00`);
      const horas = (fin - ini) / 36e5;
      if (horas > 0 && horas < 24) total += horas;
      entrada = null;
    }
  });
  return total;
}
function costePorHoraEquipoEnMes(nominas, fichajes, mes) {
  const nominasDelMes = nominas.filter((n2) => n2.mes === mes);
  const costeTotalMes = nominasDelMes.reduce((a22, n2) => a22 + (Number(n2.costeTotalEmpresa) || 0), 0);
  const horasTotalesMes = nominasDelMes.reduce((a22, n2) => a22 + horasDeEmpleadoEnMes(fichajes, n2.empleadoId, mes), 0);
  return horasTotalesMes > 0 ? costeTotalMes / horasTotalesMes : null;
}
async function loadKey(key, fallback, retries = 2) {
  for (let attempt = 0; attempt <= retries; attempt++) {
    try {
      const res = await window.storage.get(key, false);
      if (res && res.value) return JSON.parse(res.value);
      return fallback;
    } catch (e2) {
      if (attempt === retries) {
        console.error("No se pudo cargar", key, "tras varios intentos:", e2);
        return fallback;
      }
      await new Promise((r2) => setTimeout(r2, 350));
    }
  }
  return fallback;
}
async function saveKey(key, value) {
  try {
    await window.storage.set(key, JSON.stringify(value), false);
  } catch (e2) {
    console.error("Error guardando", key, e2);
    if (typeof window !== "undefined" && window.dispatchEvent) {
      window.dispatchEvent(new CustomEvent("fallo-guardado", { detail: { key, mensaje: e2 && e2.message } }));
    }
  }
}
async function sincronizarStockPm07({ setProductos, setMovimientos, localActivoId = null }) {
  if (typeof window === "undefined" || !window.__nubeActiva || typeof window.getSupabaseClient !== "function") return { ok: false, offline: true };
  const supabase = await window.getSupabaseClient();
  let qStock = supabase.from("stock_estado").select("empresa_id,local_id,producto_id,almacen,piso,total,minimo,bajo_minimo,fraccionable,precision_cantidad,local_operable");
  let qMov = supabase.from("movimientos_stock").select("id,operation_id,tipo,empresa_id,local_id,producto_id,delta_almacen,delta_piso,delta_total,cantidad,movimiento_original_id,datos,actor_user_id,created_at").order("created_at", { ascending: false }).limit(2e3);
  const [rStock, rMov] = await Promise.all([qStock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : n
--- fin coincidencia ---

PATRON 'function\\s+sincronizarStockPm07\\s*\\(': 1 coincidencias
--- coincidencia 1 pos=3986502 ---
eturn aISO(new Date(d2.getFullYear(), d2.getMonth(), 1));
};
var ultimoDiaMes = (fecha) => {
  const d2 = new Date(fecha);
  return aISO(new Date(d2.getFullYear(), d2.getMonth() + 1, 0));
};
function horasDeEmpleadoEnMes(fichajes, empleadoId, mes) {
  const delMes = fichajes.filter((f22) => f22.empleadoId === empleadoId && (f22.fecha || "").slice(0, 7) === mes).sort((a22, b2) => (a22.timestamp || "").localeCompare(b2.timestamp || ""));
  let total = 0;
  let entrada = null;
  delMes.forEach((f22) => {
    if (f22.tipo === "entrada") entrada = f22;
    else if (f22.tipo === "salida" && entrada) {
      const ini = /* @__PURE__ */ new Date(`${entrada.fecha}T${entrada.hora}:00`);
      const fin = /* @__PURE__ */ new Date(`${f22.fecha}T${f22.hora}:00`);
      const horas = (fin - ini) / 36e5;
      if (horas > 0 && horas < 24) total += horas;
      entrada = null;
    }
  });
  return total;
}
function costePorHoraEquipoEnMes(nominas, fichajes, mes) {
  const nominasDelMes = nominas.filter((n2) => n2.mes === mes);
  const costeTotalMes = nominasDelMes.reduce((a22, n2) => a22 + (Number(n2.costeTotalEmpresa) || 0), 0);
  const horasTotalesMes = nominasDelMes.reduce((a22, n2) => a22 + horasDeEmpleadoEnMes(fichajes, n2.empleadoId, mes), 0);
  return horasTotalesMes > 0 ? costeTotalMes / horasTotalesMes : null;
}
async function loadKey(key, fallback, retries = 2) {
  for (let attempt = 0; attempt <= retries; attempt++) {
    try {
      const res = await window.storage.get(key, false);
      if (res && res.value) return JSON.parse(res.value);
      return fallback;
    } catch (e2) {
      if (attempt === retries) {
        console.error("No se pudo cargar", key, "tras varios intentos:", e2);
        return fallback;
      }
      await new Promise((r2) => setTimeout(r2, 350));
    }
  }
  return fallback;
}
async function saveKey(key, value) {
  try {
    await window.storage.set(key, JSON.stringify(value), false);
  } catch (e2) {
    console.error("Error guardando", key, e2);
    if (typeof window !== "undefined" && window.dispatchEvent) {
      window.dispatchEvent(new CustomEvent("fallo-guardado", { detail: { key, mensaje: e2 && e2.message } }));
    }
  }
}
async function sincronizarStockPm07({ setProductos, setMovimientos, localActivoId = null }) {
  if (typeof window === "undefined" || !window.__nubeActiva || typeof window.getSupabaseClient !== "function") return { ok: false, offline: true };
  const supabase = await window.getSupabaseClient();
  let qStock = supabase.from("stock_estado").select("empresa_id,local_id,producto_id,almacen,piso,total,minimo,bajo_minimo,fraccionable,precision_cantidad,local_operable");
  let qMov = supabase.from("movimientos_stock").select("id,operation_id,tipo,empresa_id,local_id,producto_id,delta_almacen,delta_piso,delta_total,cantidad,movimiento_original_id,datos,actor_user_id,created_at").order("created_at", { ascending: false }).limit(2e3);
  const [rStock, rMov] = await Promise.all([qStock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : null;
      const st2 = exacto || activo;
      if (!st2) return prod;
      return { ...prod, stock: Number(st2.total) || 0, stockPisoVenta: Number(st2.piso) || 0, stockMinimo: Number(st2.minimo) || 0, _pm07BajoMinimo: !!st2.bajo_minimo, _pm07Servidor: true, _pm07LocalOperable: st2.local_operable !== false };
    }));
  }
  if (typeof setMovimientos === "function") {
    const server = movs.map((m4) => {
      const d2 = m4.datos && typeof m4.datos === "object" ? m4.datos : {};
      const delta = Number(m4.delta_total) || 0;
      const creado = String(m4.created_at || "");
      return {
        id: `pm07-${m4.id}`,
        operationId: m4.operation_id,
        ventaId: d2.ventaId || (m4.tipo === "VENTA" ? m4.operation_id : d2.anulaVentaId || null),
        anulaVentaId: d2.anulaVentaId || null,
        productoId: m4.producto_id,
        localId: m4.local_id,
        empresaId: m4.empresa_id,
        cantidad: delta,
        cantidadFisica: Number(m4.cantidad) || Math.abs(delta),
        tipo: m4.tipo,
        motivo: d2.motivo || m4.tipo,
        referencia: d2.referencia || "PM-07",
        costoUnitario: Number(d2.costoUnitario) || 0,
        ingresoUnitario: Number(d2.ingresoUnitario) || 0,
        ivaVentaAplicado: Number(d2.ivaVentaAplicado) || 0,
        medioPago: d2.medioPago || null,
        detallePago: d2.detallePago || null,
        fecha: creado ? creado.slice(0, 10) : todayISO(),
        hora: creado ? creado.slice(11, 16) : (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
        afectaStockTotal: delta !== 0,
        afectaStockPisoVenta: Number(m4.delta_piso) !== 0,
        movimientoOriginalId: m4.movimiento_original_id || null,
        _pm07Servidor: true
      };
    });
    setMovimientos((prev) => [...server, ...(prev || []).filter((m4) => !m4._pm07Servidor)]);
  }
  return { ok: true, stocks: stocks.length, movimientos: movs.length };
}
function redondearDineroPM08(valor) {
  const n2 = Number(valor);
  return Number.isFinite(n2) ? Math.round((n2 + Number.EPSILON) * 100) / 100 : NaN;
}
function modoSincronizadoPM08() {
  return typeof window !== "undefined" && !!window.NUBE_URL && !window.__modoPruebasLocal;
}
function horaActualPM08() {
  return (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5);
}
function clavePendientePM08(tipo, empresaId, localId, extra = "") {
  return `almacen:pm08:${tipo}:${empresaId || "sin-empresa"}:${localId || "todos"}${extra ? `:${extra}` : ""}`;
}
function leerPendientePM08(clave) {
  try {
    return JSON.parse(localStorage.getItem(clave) || "null");
  } catch {
    return null;
  }
}
function guardarPendientePM08(clave, valor) {
  try {
    localStorage.setItem(clave, JSON.stringify(valor));
    return true;
  } catch {
    return false;
  }
}
function limpiarPendientePM08(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
function prepararPendientePM08(clave, prefijo, payload) {
  const existente = leerPendientePM08(clave);
  if (existente) {
    if (JSON.stringify(existente.payload) !== JSON.stringify(payload)) {
      return { ok: false, pendiente: existente, error: "Hay una operaci\xF3n anterior pendiente en este local. Reint\xE9ntala con sus mismos datos antes de iniciar otra." };
    }
    return { ok: true, pendiente: existente, recuperada: true };
  }
  const pendiente = { operationId: `${prefijo}-${Date.now()}-${uid()}`, payload, creadoEn: (/* @__PURE__ */ new Date()).toISOString() };
  if (!guardarPendientePM08(clave, pendiente)) {
    return { ok: false, error: "No se ha podido conservar el borrador de la operaci\xF3n en este dispositivo." };
  }
  return { ok: true, pendiente, recuperada: false };
}
var operacionesPM08EnCurso = {};
function esErrorTransitorioPM08(error) {
  const codigo = String(error?.code || "").toLowerCase();
  const mensaje = String(error?.message || error || "").toLowerCase();
  return !codigo && /failed to fetch|f
--- fin coincidencia ---

PATRON 'function\\s+anularVenta\\s*\\(': 1 coincidencias
--- coincidencia 1 pos=4237184 ---
oId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
    if (arqueoDelDia && typeof window !== "undefined" && window.__nubeActiva) {
      fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          titulo: "Caja cerrada, ahora desactualizada",
          cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`,
          localId: lineas[0]?.localId || null,
          url: "/"
        })
      }).catch(() => {
      });
    }
    return { ok: true, lineasAnuladas: aplicadas, arqueoAfectado: !!arqueoDelDia };
  }
  async function anularVenta(ventaId, movimientosActuales, motivo = "") {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para gestionar ventas del TPV." };
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return anularVentaLocal(ventaId, movimientosActuales, motivo);
    const lineasOriginales = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineasOriginales.length === 0) return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    try {
      const supabase = await window.getSupabaseClient();
      const operationId = `rev-${ventaId}`;
      const r2 = await supabase.rpc("revertir_venta_stock_carrito", { p_operation_id: operationId, p_venta_operation_id: ventaId, p_motivo: motivo || "" });
      if (r2.error) throw r2.error;
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      const fechaVenta = lineasOriginales[0]?.fecha;
      const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
      if (arqueoDelDia) {
        fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ titulo: "Caja cerrada, ahora desactualizada", cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`, localId: r2.data?.localId || lineasOriginales[0]?.localId || null, url: "/" })
        }).catch(() => {
        });
      }
      return { ok: true, lineasAnuladas: Number(r2.data?.lineasAnuladas) || 0, arqueoAfectado: !!arqueoDelDia, modo: "pm07-servidor", sincronizada: true, replayed: !!r2.data?.replayed };
    } catch (error) {
      const msg = String(error?.message || "");
      if (msg.includes("venta_stock_ya_revertida")) return { ok: false, error: "La venta ya estaba anulada.", yaExistia: true };
      return { ok: false, error: "No se pudo confirmar la anulaci\xF3n con el servidor. No se ha modificado el stock local." };
    }
  }
  return { venderCarrito, venderLocal, anularVenta, venderLineas };
}
function crearLogicaTraspasos({ productos, setProductos, movimientos, setMovimientos, setTraspasos, registrarAuditoria, localActivoId, locales = [] }) {
  function productoEsDelLocalActivoTraspaso(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  async function traspasarStock(productoId, cantidad, direccion) {
    const cant = Number(cantidad) || 0;
    if (cant <= 0) return { ok: false, error: "Indica una cantidad mayor que cero." };
    const prod = productos.find((p22) => p22.id === productoId);
    if (!prod) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoTraspaso(prod)) return { ok: false, error: "Ese producto no pertenece al local activo." };
    const enPiso = Number(prod.stockPisoVenta) || 0;
    const total = Number(prod.stock) || 0;
    const enAlmacen = Math.max(0, total - enPiso);
    if (direccion === "a_piso" && cant > enAlmacen) {
      return { ok: false, error: `Solo hay ${fmt(enAlmacen)} ${prod.unidad} en el almac\xE9n.` };
    }
    if (direccion === "a_almacen" && cant > enPiso) {
      return { ok: false, error: `Solo hay ${fmt(enPiso)} ${prod.unidad} en el piso de venta.` };
    }
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (hayConexion) {
      const empresaId = prod.empresaId;
      if (!empresaId) return { ok: false, error: "No se pudo determinar la empresa del producto." };
      const operationId = uid();
      try {
        const supabase = await window.getSupabaseClient();
        const r3 = await supabase.rpc("trasladar_stock_interno", { p_operation_id: operationId, p_empresa_id: empresaId, p_local_id: localActivoId, p_producto_id: productoId, p_origen: direccion === "a_piso" ? "almacen" : "piso", p_destino: direccion === "a_piso" ? "piso" : "almacen", p_cantidad: cant, p_datos: { direccion, referencia: "L&A Suite" } });
        if (r3.error) throw r3.error;
        await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
        setTraspasos((s22) => [{ id: operationId, operationId, localId: localActivoId || prod.localId || null, productoId, nombre: prod.nombre, cantidad: cant, direccion, fecha: todayISO(), hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5), _pm07Servidor: true }, ...s22]);
        return { ok: true, operationId, replayed: !!r3.data?.replayed };
      } catch (error) {
        const msg = 
--- fin coincidencia ---

PATRON 'REVERSO': 42 coincidencias
--- coincidencia 1 pos=3992748 ---
;
  } catch {
    return false;
  }
}
function limpiarPendientePM08(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
function prepararPendientePM08(clave, prefijo, payload) {
  const existente = leerPendientePM08(clave);
  if (existente) {
    if (JSON.stringify(existente.payload) !== JSON.stringify(payload)) {
      return { ok: false, pendiente: existente, error: "Hay una operaci\xF3n anterior pendiente en este local. Reint\xE9ntala con sus mismos datos antes de iniciar otra." };
    }
    return { ok: true, pendiente: existente, recuperada: true };
  }
  const pendiente = { operationId: `${prefijo}-${Date.now()}-${uid()}`, payload, creadoEn: (/* @__PURE__ */ new Date()).toISOString() };
  if (!guardarPendientePM08(clave, pendiente)) {
    return { ok: false, error: "No se ha podido conservar el borrador de la operaci\xF3n en este dispositivo." };
  }
  return { ok: true, pendiente, recuperada: false };
}
var operacionesPM08EnCurso = {};
function esErrorTransitorioPM08(error) {
  const codigo = String(error?.code || "").toLowerCase();
  const mensaje = String(error?.message || error || "").toLowerCase();
  return !codigo && /failed to fetch|fetch failed|network|conexi[oó]n|timeout|load failed|offline/.test(mensaje);
}
function mensajeErrorPM08(error, fallback = "No se ha podido confirmar la operaci\xF3n.") {
  const mensaje = String(error?.message || error || "");
  const mensajes = [
    ["operation_id_conflict", "El identificador de reintento ya existe con datos distintos. La operaci\xF3n no se ha repetido."],
    ["contexto_no_autorizado", "La empresa o el local no pertenecen al \xE1mbito autorizado de esta cuenta."],
    ["venta_fuera_de_contexto", "La venta pertenece a otra empresa o a otro local."],
    ["local_inactivo", "El local est\xE1 cerrado o inactivo y no admite nuevas operaciones."],
    ["periodo_caja_cerrado", "Ese d\xEDa ya tiene un arqueo activo. Anula el arqueo de forma trazable antes de modificar la caja."],
    ["arqueo_ya_existe", "Ya existe un arqueo activo para ese d\xEDa y local."],
    ["arqueo_ya_anulado", "El arqueo ya estaba anulado."],
    ["movimiento_caja_ya_revertido", "Ese movimiento de caja ya tiene un reverso."],
    ["devolucion_supera_cantidad_pendiente", "La cantidad supera lo que queda pendiente de devolver en la venta."],
    ["reembolso_supera_importe_pendiente", "El reembolso supera el importe permitido por la cantidad devuelta."],
    ["venta_ya_anulada", "La venta est\xE1 anulada y ya no admite devoluciones."],
    ["venta_con_devoluciones", "La venta ya tiene devoluciones; no puede anularse por completo."],
    ["venta_no_encontrada", "No se ha encontrado la venta original."],
    ["producto_no_pertenece_a_venta", "El producto no pertenece a la venta seleccionada."],
    ["importe_venta_no_disponible", "La venta no conserva un importe verificable para autorizar el reembolso."],
    ["stock_insuficiente", "No hay stock suficiente para completar la devoluci\xF3n al proveedor."],
    ["cantidad_invalida", "La cantidad debe ser mayor que cero y respetar la unidad del producto."],
    ["unidad_indivisible", "Este producto solo admite cantidades enteras."],
    ["precision_cantidad_excedida", "La cantidad tiene m\xE1s decimales de los permitidos para este producto."],
    ["importe_negativo", "El importe no puede ser negativo."],
    ["importe_cero", "El importe debe ser mayor que cero."],
    ["importe_invalido", "Escribe un importe v\xE1lido."],
    ["importe_fuera_rango", "El importe est\xE1 fuera del rango permitido."],
    ["importe_precision_invalida", "El importe solo puede tener dos decimales."],
    ["medio_reembolso_invalido", "Elige un medio de reintegro coherente con el importe."],
    ["motivo_requerido", "Escribe el motivo de la operaci\xF3n."],
    ["caja_no_autorizada", "Tu rol no tiene permiso para operar la caja."],
    ["arqueo_no_autorizado", "Tu rol no tiene permiso para realizar arqueos."],
    ["devolucion_no_autorizada", "Tu rol no tiene permiso para registrar devoluciones."],
    ["devolucion_proveedor_no_autorizada", "Tu rol no tiene permiso para devolver mercanc\xEDa a proveedores."],
    ["reverso_caja_no_autorizado", "Solo Propietario o Encargado pueden revertir movimientos de caja."],
    ["anulacion_arqueo_no_autorizada", "Solo Propietario o Encargado pueden anular un arqueo."]
  ];
  const encontrado = mensajes.find(([codigo]) => mensaje.includes(codigo));
  return encontrado ? encontrado[1] : mensaje ? `${fallback} ${mensaje}` : fallback;
}
function normalizarMovimientoCajaPM08(fila) {
  if (!fila) return null;
  const creado = String(fila.created_at || fila.createdAt || "");
  return {
    id: fila.operation_id || fila.operationId || fila.id,
    operationId: fila.operation_id || fila.operationId || fila.id,
    empresaId: fila.empresa_id || fila.empresaId,
    localId: fila.local_id || fila.localId,
    fecha: fila.fecha || (creado ? creado.slice(0, 10) : todayISO()),
    hora: creado ? creado.slice(11, 16) : fila.hora || horaActualPM08(),
    tipo: String(fila.tipo || "").toUpperCase(),
    importe: Number(fila.importe) || 0,
    efectoEfectivo: Number(fila.efecto_efectivo ?? fila.efectoEfectivo) || 0,
    medioPago: fila.medio_pago || fila.medioPago || "EFECTIVO",
    concepto: fila.concepto || fila.motivo || "",
    motivo: fila.concepto || fila.motivo || "",
    origen: fila.origen_tipo || fila.origen || "MANUAL",
    referenciaId: fila.origen_id || fila.referenciaId || null,
    refOperationId: fila.ref_operation_id || fila.refOperationId || null,
    actorUserId: fila.actor_user_id || fila.actorUserId || null,
    createdAt: creado || null,
    _pm08Servidor: !!(fila.operation_id || fila.actor_user_id)
  };
}
function normalizarArqueoPM08(fila) {
  if (!fila) return null;
  const creado = String(fila.created_at || fila.createdAt || "");
  return {
    id: fila.operation_id || fila.operationId || fila.id,
    operationId: fila.operation_id || fila.operationId || fila.id,
    empresaId: fila.empresa_id || fila.empresaId,
    localId: fila.local_id || fila.localId,
    fecha: fila.fecha || todayISO(),
    hora: creado ? creado.slice(11, 16) : fila.hora || horaActualPM08(),
    alcance: fila.alcance || "DIA",
    efectivoBase: Number(fila.efectivo_base ?? fila.efectivoBase) || 0,
    efectivoEsperado: Number(fila.efectivo_esperado ?? fila.efectivoEsperado) || 0,
    efectivoContado: Number(fila.efectivo_contado ?? fila.efectivoContado ?? fila.efectivoReal) || 0,
    efectivoReal: Number(fila.efectivo_contado ?? fila.efectivoContado ?? fila.efectivoReal) || 0,
    diferencia: Number(fila.diferencia) || 0,
    notas: fila.notas || "",
    estado: fila.estado || "ACTIVO",
    anuladoPorOperationId: fila.anulado_por_operation_id || fila.anuladoPorOperationId || null,
    anuladoMotivo: fila.anulado_motivo || fila.anuladoMotivo || null,
    actorUserId: fila.actor_user_id || fila.actorUserId || null,
    createdAt: creado || null,
    _pm08Servidor: !!(fila.operation_id || fila.actor_user_id)
  };
}
function normalizarDevolucionPM08(fila, tipo) {
  if (!fila) return null;
  const payload = fila.payload && typeof fila.payload === "object" ? fila.payload : {};
  const datos = payload.datos && typeof payload.datos === "object" ? payload.datos : {};
  return {
    id: fila.operation_id || fila.operationId || fila.id,
    operationId: fila.operation_id || fila.operationId || f
--- fin coincidencia ---

--- coincidencia 2 pos=3994706 ---
tes de modificar la caja."],
    ["arqueo_ya_existe", "Ya existe un arqueo activo para ese d\xEDa y local."],
    ["arqueo_ya_anulado", "El arqueo ya estaba anulado."],
    ["movimiento_caja_ya_revertido", "Ese movimiento de caja ya tiene un reverso."],
    ["devolucion_supera_cantidad_pendiente", "La cantidad supera lo que queda pendiente de devolver en la venta."],
    ["reembolso_supera_importe_pendiente", "El reembolso supera el importe permitido por la cantidad devuelta."],
    ["venta_ya_anulada", "La venta est\xE1 anulada y ya no admite devoluciones."],
    ["venta_con_devoluciones", "La venta ya tiene devoluciones; no puede anularse por completo."],
    ["venta_no_encontrada", "No se ha encontrado la venta original."],
    ["producto_no_pertenece_a_venta", "El producto no pertenece a la venta seleccionada."],
    ["importe_venta_no_disponible", "La venta no conserva un importe verificable para autorizar el reembolso."],
    ["stock_insuficiente", "No hay stock suficiente para completar la devoluci\xF3n al proveedor."],
    ["cantidad_invalida", "La cantidad debe ser mayor que cero y respetar la unidad del producto."],
    ["unidad_indivisible", "Este producto solo admite cantidades enteras."],
    ["precision_cantidad_excedida", "La cantidad tiene m\xE1s decimales de los permitidos para este producto."],
    ["importe_negativo", "El importe no puede ser negativo."],
    ["importe_cero", "El importe debe ser mayor que cero."],
    ["importe_invalido", "Escribe un importe v\xE1lido."],
    ["importe_fuera_rango", "El importe est\xE1 fuera del rango permitido."],
    ["importe_precision_invalida", "El importe solo puede tener dos decimales."],
    ["medio_reembolso_invalido", "Elige un medio de reintegro coherente con el importe."],
    ["motivo_requerido", "Escribe el motivo de la operaci\xF3n."],
    ["caja_no_autorizada", "Tu rol no tiene permiso para operar la caja."],
    ["arqueo_no_autorizado", "Tu rol no tiene permiso para realizar arqueos."],
    ["devolucion_no_autorizada", "Tu rol no tiene permiso para registrar devoluciones."],
    ["devolucion_proveedor_no_autorizada", "Tu rol no tiene permiso para devolver mercanc\xEDa a proveedores."],
    ["reverso_caja_no_autorizado", "Solo Propietario o Encargado pueden revertir movimientos de caja."],
    ["anulacion_arqueo_no_autorizada", "Solo Propietario o Encargado pueden anular un arqueo."]
  ];
  const encontrado = mensajes.find(([codigo]) => mensaje.includes(codigo));
  return encontrado ? encontrado[1] : mensaje ? `${fallback} ${mensaje}` : fallback;
}
function normalizarMovimientoCajaPM08(fila) {
  if (!fila) return null;
  const creado = String(fila.created_at || fila.createdAt || "");
  return {
    id: fila.operation_id || fila.operationId || fila.id,
    operationId: fila.operation_id || fila.operationId || fila.id,
    empresaId: fila.empresa_id || fila.empresaId,
    localId: fila.local_id || fila.localId,
    fecha: fila.fecha || (creado ? creado.slice(0, 10) : todayISO()),
    hora: creado ? creado.slice(11, 16) : fila.hora || horaActualPM08(),
    tipo: String(fila.tipo || "").toUpperCase(),
    importe: Number(fila.importe) || 0,
    efectoEfectivo: Number(fila.efecto_efectivo ?? fila.efectoEfectivo) || 0,
    medioPago: fila.medio_pago || fila.medioPago || "EFECTIVO",
    concepto: fila.concepto || fila.motivo || "",
    motivo: fila.concepto || fila.motivo || "",
    origen: fila.origen_tipo || fila.origen || "MANUAL",
    referenciaId: fila.origen_id || fila.referenciaId || null,
    refOperationId: fila.ref_operation_id || fila.refOperationId || null,
    actorUserId: fila.actor_user_id || fila.actorUserId || null,
    createdAt: creado || null,
    _pm08Servidor: !!(fila.operation_id || fila.actor_user_id)
  };
}
function normalizarArqueoPM08(fila) {
  if (!fila) return null;
  const creado = String(fila.created_at || fila.createdAt || "");
  return {
    id: fila.operation_id || fila.operationId || fila.id,
    operationId: fila.operation_id || fila.operationId || fila.id,
    empresaId: fila.empresa_id || fila.empresaId,
    localId: fila.local_id || fila.localId,
    fecha: fila.fecha || todayISO(),
    hora: creado ? creado.slice(11, 16) : fila.hora || horaActualPM08(),
    alcance: fila.alcance || "DIA",
    efectivoBase: Number(fila.efectivo_base ?? fila.efectivoBase) || 0,
    efectivoEsperado: Number(fila.efectivo_esperado ?? fila.efectivoEsperado) || 0,
    efectivoContado: Number(fila.efectivo_contado ?? fila.efectivoContado ?? fila.efectivoReal) || 0,
    efectivoReal: Number(fila.efectivo_contado ?? fila.efectivoContado ?? fila.efectivoReal) || 0,
    diferencia: Number(fila.diferencia) || 0,
    notas: fila.notas || "",
    estado: fila.estado || "ACTIVO",
    anuladoPorOperationId: fila.anulado_por_operation_id || fila.anuladoPorOperationId || null,
    anuladoMotivo: fila.anulado_motivo || fila.anuladoMotivo || null,
    actorUserId: fila.actor_user_id || fila.actorUserId || null,
    createdAt: creado || null,
    _pm08Servidor: !!(fila.operation_id || fila.actor_user_id)
  };
}
function normalizarDevolucionPM08(fila, tipo) {
  if (!fila) return null;
  const payload = fila.payload && typeof fila.payload === "object" ? fila.payload : {};
  const datos = payload.datos && typeof payload.datos === "object" ? payload.datos : {};
  return {
    id: fila.operation_id || fila.operationId || fila.id,
    operationId: fila.operation_id || fila.operationId || fila.id,
    empresaId: fila.empresa_id || fila.empresaId,
    localId: fila.local_id || fila.localId,
    tipo,
    fecha: fila.fecha || todayISO(),
    productoId: fila.producto_id || fila.productoId,
    productoNombre: datos.productoNombre || fila.producto_nombre || fila.productoNombre || fila.producto_id || fila.productoId,
    cantidad: Number(fila.cantidad) || 0,
    motivo: fila.motivo || "",
    reembolso: Number(fila.reembolso) || 0,
    medioReembolso: fila.medio_reembolso || fila.medioReembolso || "SIN_REEMBOLSO",
    ventaId: fila.venta_operation_id || fila.ventaId || null,
    proveedorId: fila.proveedor_id || fila.proveedorId || null,
    proveedorNombre: fila.proveedor_nombre || fila.proveedorNombre || datos.proveedorNombre || "",
    actorUserId: fila.actor_user_id || fila.actorUserId || null,
    createdAt: fila.created_at || fila.createdAt || null,
    _pm08Servidor: !!(fila.operation_id || fila.actor_user_id)
  };
}
async function sincronizarCajaPm08({ setArqueos, setMovimientosCaja, setDevoluciones }) {
  if (!modoSincronizadoPM08() || !window.__nubeActiva || !window.__nubeCliente) return { ok: false, offline: true };
  const supabase = window.__nubeCliente;
  const [rCaja, rArqueos, rCliente, rProveedor] = await Promise.all([
    supabase.from("caja_operaciones").select("operation_id,tipo,empresa_id,local_id,fecha,importe,efecto_efectivo,medio_pago,concepto,origen_tipo,origen_id,ref_operation_id,actor_user_id,created_at").order("created_at", { ascending: false }).limit(2e3),
    supabase.from("arqueos_caja").select("operation_id,empresa_id,local_id,fecha,alcance,efectivo_base,efectivo_esperado,efectivo_contado,diferencia,notas,estado,anulado_por_operation_id,anulado_motivo,actor_user_id,created_at").order("created_at", { ascending: false }).limit(1e3),
    supabase.from("devoluciones_venta").select("operation_id,venta_operation_id,empresa_id,local_id,producto_id,cantidad,reembolso,medio_reembolso,motivo,
--- fin coincidencia ---

--- coincidencia 3 pos=4135434 ---
.lineas || []).forEach((ln2) => {
    const baseLinea = (Number(ln2.importe) || 0) + (Number(ln2.canon) || 0);
    const tipo = Number(ln2.ivaPct) || 0;
    const cuota = baseLinea * (tipo / 100);
    base += baseLinea;
    iva += cuota;
    if (!porTipo[tipo]) porTipo[tipo] = { base: 0, cuota: 0 };
    porTipo[tipo].base += baseLinea;
    porTipo[tipo].cuota += cuota;
  });
  const cargos = Number(a22?.cargos) || 0;
  if (cargos) {
    const tipo = Number(a22?.cargosIva) || 0;
    const cuota = cargos * (tipo / 100);
    base += cargos;
    iva += cuota;
    if (!porTipo[tipo]) porTipo[tipo] = { base: 0, cuota: 0 };
    porTipo[tipo].base += cargos;
    porTipo[tipo].cuota += cuota;
  }
  return { base: redondearDineroPM06(base), iva: redondearDineroPM06(iva), total: redondearDineroPM06(base + iva), porTipo };
}
function calcularTotalesFacturaDirecta(f22) {
  const base = redondearDineroPM06(f22?.importeBase);
  const tipo = Number(f22?.ivaPct) || 0;
  const iva = redondearDineroPM06(base * (tipo / 100));
  return { base, iva, total: redondearDineroPM06(base + iva), porTipo: { [tipo]: { base, cuota: iva } } };
}
function normalizarPagoPM06(p22) {
  if (!p22) return null;
  return {
    id: p22.id,
    operationId: p22.operationId || p22.operation_id,
    facturaId: p22.facturaId || p22.factura_id,
    origenFactura: p22.origenFactura || p22.origen_factura,
    empresaId: p22.empresaId || p22.empresa_id,
    localId: p22.localId || p22.local_id,
    importe: Number(p22.importe) || 0,
    fecha: p22.fecha || todayISO(),
    estado: p22.estado || "CONFIRMADO",
    reviertePagoId: p22.reviertePagoId || p22.revierte_pago_id || null,
    medioPago: p22.medioPago || p22.medio_pago || "",
    datos: p22.datos || {},
    createdAt: p22.createdAt || p22.created_at || null
  };
}
function calcularSaldoFacturaPM06(pagos, facturaId, origenFactura, total, empresaId, localId, legacyPagada) {
  const relacionados = (pagos || []).filter((p22) => p22 && p22.facturaId === facturaId && p22.origenFactura === origenFactura && (!empresaId || p22.empresaId === empresaId) && (!localId || p22.localId === localId));
  let pagado = relacionados.reduce((a22, p22) => a22 + (p22.estado === "REVERSO" ? -Math.abs(Number(p22.importe) || 0) : p22.estado === "CONFIRMADO" ? Math.abs(Number(p22.importe) || 0) : 0), 0);
  const total2 = redondearDineroPM06(total);
  if (!relacionados.length && legacyPagada) pagado = total2;
  pagado = Math.max(0, redondearDineroPM06(pagado));
  const pendiente = Math.max(0, redondearDineroPM06(total2 - pagado));
  return { total: total2, pagado, pendiente, pagada: pendiente <= 1e-3 && total2 > 0 };
}
function modoSincronizadoPM06() {
  return typeof window !== "undefined" && !!window.NUBE_URL && !window.__modoPruebasLocal;
}
function claveOperacionPM06(tipo, origen, facturaId) {
  return `almacen:pm06:${tipo}:${origen}:${facturaId}`;
}
function leerPendientePM06(clave) {
  try {
    return JSON.parse(localStorage.getItem(clave) || "null");
  } catch {
    return null;
  }
}
function guardarPendientePM06(clave, valor) {
  try {
    localStorage.setItem(clave, JSON.stringify(valor));
  } catch {
  }
}
function limpiarPendientePM06(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
var pagosPM06EnCurso = {};
async function registrarPagoPM06({ factura, origenFactura, importe, pagosFacturas, setPagosFacturas }) {
  const empresaId = factura?.empresaId || null;
  const localId = factura?.localId || null;
  const facturaId = factura?.id || null;
  const importe2 = redondearDineroPM06(importe);
  const total = redondearDineroPM06(factura?.total ?? factura?.importeTotal ?? 0);
  const saldo = calcularSaldoFacturaPM06(pagosFacturas, facturaId, origenFactura, total, empresaId, localId, factura?.pagada);
  if (!facturaId || !empresaId || !localId) return { ok: false, error: "La factura no tiene empresa/local propios. No se ha registrado el pago." };
  if (!(importe2 > 0)) return { ok: false, error: "El importe del pago debe ser mayor que cero." };
  if (importe2 > saldo.pendiente + 1e-3) return { ok: false, error: "El pago supera el saldo pendiente de la factura." };
  const clave = claveOperacionPM06("pago", origenFactura, facturaId);
  if (pagosPM06EnCurso[clave]) return pagosPM06EnCurso[clave];
  const tarea = (async () => {
    let pendiente = leerPendientePM06(clave);
    if (pendiente && redondearDineroPM06(pendiente.importe) !== importe2) {
      return { ok: false, error: "Hay un pago anterior pendiente de confirmar. Recupera la conexi\xF3n antes de registrar otro importe." };
    }
    if (!pendiente) {
      pendiente = { id: uid(), operationId: `pago-${facturaId}-${Date.now()}-${uid()}`, importe: importe2, empresaId, localId, facturaId, origenFactura };
      guardarPendientePM06(clave, pendiente);
    }
    if (modoSincronizadoPM06()) {
      if (!window.__nubeActiva || !window.__nubeCliente) {
        return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. El pago NO se ha confirmado." };
      }
      const r2 = await window.__nubeCliente.rpc("registrar_pago_factura", {
        p_id: pendiente.id,
        p_operation_id: pendiente.operationId,
        p_factura_id: facturaId,
        p_origen_factura: origenFactura,
        p_empresa_id: empresaId,
        p_local_id: localId,
        p_importe: importe2,
        p_fecha: todayISO(),
        p_medio_pago: "",
        p_datos: { origen: "L&A Suite PM-06" }
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el pago: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed, pendiente: redondearDineroPM06(r2.data?.pendiente) };
    }
    const pago = normalizarPagoPM06({ ...pendiente, fecha: todayISO(), estado: "CONFIRMADO" });
    setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago.operationId) ? s22 : [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
async function revertirUltimoPagoPM06({ factura, origenFactura, pagosFacturas, setPagosFacturas, motivo = "Correcci\xF3n de pago" }) {
  const empresaId = factura?.empresaId || null;
  const localId = factura?.localId || null;
  const facturaId = factura?.id || null;
  const relacionados = (pagosFacturas || []).filter((p22) => p22 && p22.facturaId === facturaId && p22.origenFactura === origenFactura && p22.empresaId === empresaId && p22.localId === localId);
  const reversados = new Set(relacionados.filter((p22) => p22.estado === "REVERSO" && p22.reviertePagoId).map((p22) => p22.reviertePagoId));
  const confirmados = relacionados.filter((p22) => p22.estado === "CONFIRMADO" && !reversados.has(p22.id));
  const original = confirmados[confirmados.length - 1];
  if (!original) return { ok: false, error: "No hay un pago trazable que se pueda revertir." };
  const clave = claveOperacionPM06("reverso", origenFactura, original.id);
  if (pagosPM06EnCurso[clave]) return pagosPM06EnCurso[clave];
  const tarea = (as
--- fin coincidencia ---

--- coincidencia 4 pos=4140157 ---
alId, facturaId, origenFactura };
      guardarPendientePM06(clave, pendiente);
    }
    if (modoSincronizadoPM06()) {
      if (!window.__nubeActiva || !window.__nubeCliente) {
        return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. El pago NO se ha confirmado." };
      }
      const r2 = await window.__nubeCliente.rpc("registrar_pago_factura", {
        p_id: pendiente.id,
        p_operation_id: pendiente.operationId,
        p_factura_id: facturaId,
        p_origen_factura: origenFactura,
        p_empresa_id: empresaId,
        p_local_id: localId,
        p_importe: importe2,
        p_fecha: todayISO(),
        p_medio_pago: "",
        p_datos: { origen: "L&A Suite PM-06" }
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el pago: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed, pendiente: redondearDineroPM06(r2.data?.pendiente) };
    }
    const pago = normalizarPagoPM06({ ...pendiente, fecha: todayISO(), estado: "CONFIRMADO" });
    setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago.operationId) ? s22 : [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
async function revertirUltimoPagoPM06({ factura, origenFactura, pagosFacturas, setPagosFacturas, motivo = "Correcci\xF3n de pago" }) {
  const empresaId = factura?.empresaId || null;
  const localId = factura?.localId || null;
  const facturaId = factura?.id || null;
  const relacionados = (pagosFacturas || []).filter((p22) => p22 && p22.facturaId === facturaId && p22.origenFactura === origenFactura && p22.empresaId === empresaId && p22.localId === localId);
  const reversados = new Set(relacionados.filter((p22) => p22.estado === "REVERSO" && p22.reviertePagoId).map((p22) => p22.reviertePagoId));
  const confirmados = relacionados.filter((p22) => p22.estado === "CONFIRMADO" && !reversados.has(p22.id));
  const original = confirmados[confirmados.length - 1];
  if (!original) return { ok: false, error: "No hay un pago trazable que se pueda revertir." };
  const clave = claveOperacionPM06("reverso", origenFactura, original.id);
  if (pagosPM06EnCurso[clave]) return pagosPM06EnCurso[clave];
  const tarea = (async () => {
    let pendiente = leerPendientePM06(clave);
    if (!pendiente) {
      pendiente = { id: uid(), operationId: `reverso-${original.id}-${Date.now()}-${uid()}`, pagoId: original.id };
      guardarPendientePM06(clave, pendiente);
    }
    if (modoSincronizadoPM06()) {
      if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
      const r2 = await window.__nubeCliente.rpc("revertir_pago_factura", {
        p_id: pendiente.id,
        p_operation_id: pendiente.operationId,
        p_pago_id: original.id,
        p_motivo: motivo
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el reverso: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed };
    }
    const pago = normalizarPagoPM06({ id: pendiente.id, operationId: pendiente.operationId, facturaId, origenFactura, empresaId, localId, importe: original.importe, fecha: todayISO(), estado: "REVERSO", reviertePagoId: original.id, datos: { motivo } });
    setPagosFacturas((s22) => [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
function crearLogicaGastos({ setGastosGenerales, localActivoId, empresaId }) {
  function addGasto(data) {
    const empresaDoc = data?.empresaId || empresaId || null;
    const localDoc = data?.localId || localActivoId || null;
    if (!empresaDoc || !localDoc) return { ok: false, error: "Selecciona un local antes de registrar el gasto." };
    const nuevo = { id: uid(), ...data, empresaId: empresaDoc, localId: localDoc, estado: data?.estado || "ACTIVO" };
    setGastosGenerales((s22) => [...s22, nuevo]);
    return { ok: true, gasto: nuevo };
  }
  function deleteGasto(id, localIdEsperado = localActivoId) {
    let eliminado = false;
    setGastosGenerales((s22) => {
      const actual = s22.find((g2) => g2.id === id);
      if (!actual || actual.empresaId !== empresaId || localIdEsperado && actual.localId !== localIdEsperado) return s22;
      if (actual.facturaDirectaId) return s22;
      eliminado = true;
      return s22.filter((g2) => g2.id !== id);
    });
    return eliminado;
  }
  return { addGasto, deleteGasto };
}
function crearLogicaCaja({ arqueos, setArqueos, movimientosCaja, setMovimientosCaja, localActivoId, empresaId }) {
  function leerBorradorArqueo(fecha) {
    return leerPendientePM08(clavePendientePM08("arqueo", empresaId, localActivoId, fecha))?.payload || null;
  }
  async function addArqueo(data) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de guardar el arqueo." };
    const fecha = data?.fecha || todayISO();
    const efectivoBase = redondearDineroPM08(data?.efectivoBase ?? data?.efectivoEsperado ?? 0);
    const efectivoContado = redondearDineroPM08(data?.efectivoContado ?? data?.efectivoReal);
    if (!Number.isFinite(efectivoBase) || efectivoBase < 0) return { ok: false, error: "El efectivo base no es v\xE1lido." };
    if (!Number.isFinite(efectivoContado) || efectivoContado < 0) return { ok: false, error: "El efectivo contado debe ser cero o un importe positivo." };
    if ((arqueos || []).some((a22) => a22.localId === localActivoId && a22.fecha === fecha && a22.estado !== "ANULADO")) {
      return { ok: false, error: "Ya existe un arqueo activo para ese d\xEDa y local." };
    }
    const payload = {
      empresaId,
      localId: localActivoId,
      fecha,
      efectivoBase,
      efectivoContado,
      notas: String(data?.notas || "").trim(),
      snapshot: data?.snapshot || {}
    };
    const clave = clavePendientePM08("arqueo", empresaId, localActivoId, fecha);
    const preparado = prepararPendientePM08(clave, "pm08-arqueo", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) {
          return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. El arqueo NO se ha confirmado; el borrador queda listo para reintentar." };
        }
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.
--- fin coincidencia ---

--- coincidencia 5 pos=4140520 ---
c("registrar_pago_factura", {
        p_id: pendiente.id,
        p_operation_id: pendiente.operationId,
        p_factura_id: facturaId,
        p_origen_factura: origenFactura,
        p_empresa_id: empresaId,
        p_local_id: localId,
        p_importe: importe2,
        p_fecha: todayISO(),
        p_medio_pago: "",
        p_datos: { origen: "L&A Suite PM-06" }
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el pago: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed, pendiente: redondearDineroPM06(r2.data?.pendiente) };
    }
    const pago = normalizarPagoPM06({ ...pendiente, fecha: todayISO(), estado: "CONFIRMADO" });
    setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago.operationId) ? s22 : [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
async function revertirUltimoPagoPM06({ factura, origenFactura, pagosFacturas, setPagosFacturas, motivo = "Correcci\xF3n de pago" }) {
  const empresaId = factura?.empresaId || null;
  const localId = factura?.localId || null;
  const facturaId = factura?.id || null;
  const relacionados = (pagosFacturas || []).filter((p22) => p22 && p22.facturaId === facturaId && p22.origenFactura === origenFactura && p22.empresaId === empresaId && p22.localId === localId);
  const reversados = new Set(relacionados.filter((p22) => p22.estado === "REVERSO" && p22.reviertePagoId).map((p22) => p22.reviertePagoId));
  const confirmados = relacionados.filter((p22) => p22.estado === "CONFIRMADO" && !reversados.has(p22.id));
  const original = confirmados[confirmados.length - 1];
  if (!original) return { ok: false, error: "No hay un pago trazable que se pueda revertir." };
  const clave = claveOperacionPM06("reverso", origenFactura, original.id);
  if (pagosPM06EnCurso[clave]) return pagosPM06EnCurso[clave];
  const tarea = (async () => {
    let pendiente = leerPendientePM06(clave);
    if (!pendiente) {
      pendiente = { id: uid(), operationId: `reverso-${original.id}-${Date.now()}-${uid()}`, pagoId: original.id };
      guardarPendientePM06(clave, pendiente);
    }
    if (modoSincronizadoPM06()) {
      if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
      const r2 = await window.__nubeCliente.rpc("revertir_pago_factura", {
        p_id: pendiente.id,
        p_operation_id: pendiente.operationId,
        p_pago_id: original.id,
        p_motivo: motivo
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el reverso: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed };
    }
    const pago = normalizarPagoPM06({ id: pendiente.id, operationId: pendiente.operationId, facturaId, origenFactura, empresaId, localId, importe: original.importe, fecha: todayISO(), estado: "REVERSO", reviertePagoId: original.id, datos: { motivo } });
    setPagosFacturas((s22) => [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
function crearLogicaGastos({ setGastosGenerales, localActivoId, empresaId }) {
  function addGasto(data) {
    const empresaDoc = data?.empresaId || empresaId || null;
    const localDoc = data?.localId || localActivoId || null;
    if (!empresaDoc || !localDoc) return { ok: false, error: "Selecciona un local antes de registrar el gasto." };
    const nuevo = { id: uid(), ...data, empresaId: empresaDoc, localId: localDoc, estado: data?.estado || "ACTIVO" };
    setGastosGenerales((s22) => [...s22, nuevo]);
    return { ok: true, gasto: nuevo };
  }
  function deleteGasto(id, localIdEsperado = localActivoId) {
    let eliminado = false;
    setGastosGenerales((s22) => {
      const actual = s22.find((g2) => g2.id === id);
      if (!actual || actual.empresaId !== empresaId || localIdEsperado && actual.localId !== localIdEsperado) return s22;
      if (actual.facturaDirectaId) return s22;
      eliminado = true;
      return s22.filter((g2) => g2.id !== id);
    });
    return eliminado;
  }
  return { addGasto, deleteGasto };
}
function crearLogicaCaja({ arqueos, setArqueos, movimientosCaja, setMovimientosCaja, localActivoId, empresaId }) {
  function leerBorradorArqueo(fecha) {
    return leerPendientePM08(clavePendientePM08("arqueo", empresaId, localActivoId, fecha))?.payload || null;
  }
  async function addArqueo(data) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de guardar el arqueo." };
    const fecha = data?.fecha || todayISO();
    const efectivoBase = redondearDineroPM08(data?.efectivoBase ?? data?.efectivoEsperado ?? 0);
    const efectivoContado = redondearDineroPM08(data?.efectivoContado ?? data?.efectivoReal);
    if (!Number.isFinite(efectivoBase) || efectivoBase < 0) return { ok: false, error: "El efectivo base no es v\xE1lido." };
    if (!Number.isFinite(efectivoContado) || efectivoContado < 0) return { ok: false, error: "El efectivo contado debe ser cero o un importe positivo." };
    if ((arqueos || []).some((a22) => a22.localId === localActivoId && a22.fecha === fecha && a22.estado !== "ANULADO")) {
      return { ok: false, error: "Ya existe un arqueo activo para ese d\xEDa y local." };
    }
    const payload = {
      empresaId,
      localId: localActivoId,
      fecha,
      efectivoBase,
      efectivoContado,
      notas: String(data?.notas || "").trim(),
      snapshot: data?.snapshot || {}
    };
    const clave = clavePendientePM08("arqueo", empresaId, localActivoId, fecha);
    const preparado = prepararPendientePM08(clave, "pm08-arqueo", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) {
          return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. El arqueo NO se ha confirmado; el borrador queda listo para reintentar." };
        }
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("registrar_arqueo_caja", {
              p_operation_id: preparado.pendiente.operationId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_fecha: fecha,
              p_efectivo_base: efectivoBase,
              p_efectivo_contado: efectivoContado,
              p_notas: payload.notas,
              p_snapshot:
--- fin coincidencia ---

--- coincidencia 6 pos=4140766 ---
   p_importe: importe2,
        p_fecha: todayISO(),
        p_medio_pago: "",
        p_datos: { origen: "L&A Suite PM-06" }
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el pago: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed, pendiente: redondearDineroPM06(r2.data?.pendiente) };
    }
    const pago = normalizarPagoPM06({ ...pendiente, fecha: todayISO(), estado: "CONFIRMADO" });
    setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago.operationId) ? s22 : [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
async function revertirUltimoPagoPM06({ factura, origenFactura, pagosFacturas, setPagosFacturas, motivo = "Correcci\xF3n de pago" }) {
  const empresaId = factura?.empresaId || null;
  const localId = factura?.localId || null;
  const facturaId = factura?.id || null;
  const relacionados = (pagosFacturas || []).filter((p22) => p22 && p22.facturaId === facturaId && p22.origenFactura === origenFactura && p22.empresaId === empresaId && p22.localId === localId);
  const reversados = new Set(relacionados.filter((p22) => p22.estado === "REVERSO" && p22.reviertePagoId).map((p22) => p22.reviertePagoId));
  const confirmados = relacionados.filter((p22) => p22.estado === "CONFIRMADO" && !reversados.has(p22.id));
  const original = confirmados[confirmados.length - 1];
  if (!original) return { ok: false, error: "No hay un pago trazable que se pueda revertir." };
  const clave = claveOperacionPM06("reverso", origenFactura, original.id);
  if (pagosPM06EnCurso[clave]) return pagosPM06EnCurso[clave];
  const tarea = (async () => {
    let pendiente = leerPendientePM06(clave);
    if (!pendiente) {
      pendiente = { id: uid(), operationId: `reverso-${original.id}-${Date.now()}-${uid()}`, pagoId: original.id };
      guardarPendientePM06(clave, pendiente);
    }
    if (modoSincronizadoPM06()) {
      if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
      const r2 = await window.__nubeCliente.rpc("revertir_pago_factura", {
        p_id: pendiente.id,
        p_operation_id: pendiente.operationId,
        p_pago_id: original.id,
        p_motivo: motivo
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el reverso: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed };
    }
    const pago = normalizarPagoPM06({ id: pendiente.id, operationId: pendiente.operationId, facturaId, origenFactura, empresaId, localId, importe: original.importe, fecha: todayISO(), estado: "REVERSO", reviertePagoId: original.id, datos: { motivo } });
    setPagosFacturas((s22) => [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
function crearLogicaGastos({ setGastosGenerales, localActivoId, empresaId }) {
  function addGasto(data) {
    const empresaDoc = data?.empresaId || empresaId || null;
    const localDoc = data?.localId || localActivoId || null;
    if (!empresaDoc || !localDoc) return { ok: false, error: "Selecciona un local antes de registrar el gasto." };
    const nuevo = { id: uid(), ...data, empresaId: empresaDoc, localId: localDoc, estado: data?.estado || "ACTIVO" };
    setGastosGenerales((s22) => [...s22, nuevo]);
    return { ok: true, gasto: nuevo };
  }
  function deleteGasto(id, localIdEsperado = localActivoId) {
    let eliminado = false;
    setGastosGenerales((s22) => {
      const actual = s22.find((g2) => g2.id === id);
      if (!actual || actual.empresaId !== empresaId || localIdEsperado && actual.localId !== localIdEsperado) return s22;
      if (actual.facturaDirectaId) return s22;
      eliminado = true;
      return s22.filter((g2) => g2.id !== id);
    });
    return eliminado;
  }
  return { addGasto, deleteGasto };
}
function crearLogicaCaja({ arqueos, setArqueos, movimientosCaja, setMovimientosCaja, localActivoId, empresaId }) {
  function leerBorradorArqueo(fecha) {
    return leerPendientePM08(clavePendientePM08("arqueo", empresaId, localActivoId, fecha))?.payload || null;
  }
  async function addArqueo(data) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de guardar el arqueo." };
    const fecha = data?.fecha || todayISO();
    const efectivoBase = redondearDineroPM08(data?.efectivoBase ?? data?.efectivoEsperado ?? 0);
    const efectivoContado = redondearDineroPM08(data?.efectivoContado ?? data?.efectivoReal);
    if (!Number.isFinite(efectivoBase) || efectivoBase < 0) return { ok: false, error: "El efectivo base no es v\xE1lido." };
    if (!Number.isFinite(efectivoContado) || efectivoContado < 0) return { ok: false, error: "El efectivo contado debe ser cero o un importe positivo." };
    if ((arqueos || []).some((a22) => a22.localId === localActivoId && a22.fecha === fecha && a22.estado !== "ANULADO")) {
      return { ok: false, error: "Ya existe un arqueo activo para ese d\xEDa y local." };
    }
    const payload = {
      empresaId,
      localId: localActivoId,
      fecha,
      efectivoBase,
      efectivoContado,
      notas: String(data?.notas || "").trim(),
      snapshot: data?.snapshot || {}
    };
    const clave = clavePendientePM08("arqueo", empresaId, localActivoId, fecha);
    const preparado = prepararPendientePM08(clave, "pm08-arqueo", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) {
          return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. El arqueo NO se ha confirmado; el borrador queda listo para reintentar." };
        }
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("registrar_arqueo_caja", {
              p_operation_id: preparado.pendiente.operationId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_fecha: fecha,
              p_efectivo_base: efectivoBase,
              p_efectivo_contado: efectivoContado,
              p_notas: payload.notas,
              p_snapshot: payload.snapshot
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r
--- fin coincidencia ---

--- coincidencia 7 pos=4141045 ---
zaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed, pendiente: redondearDineroPM06(r2.data?.pendiente) };
    }
    const pago = normalizarPagoPM06({ ...pendiente, fecha: todayISO(), estado: "CONFIRMADO" });
    setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago.operationId) ? s22 : [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
async function revertirUltimoPagoPM06({ factura, origenFactura, pagosFacturas, setPagosFacturas, motivo = "Correcci\xF3n de pago" }) {
  const empresaId = factura?.empresaId || null;
  const localId = factura?.localId || null;
  const facturaId = factura?.id || null;
  const relacionados = (pagosFacturas || []).filter((p22) => p22 && p22.facturaId === facturaId && p22.origenFactura === origenFactura && p22.empresaId === empresaId && p22.localId === localId);
  const reversados = new Set(relacionados.filter((p22) => p22.estado === "REVERSO" && p22.reviertePagoId).map((p22) => p22.reviertePagoId));
  const confirmados = relacionados.filter((p22) => p22.estado === "CONFIRMADO" && !reversados.has(p22.id));
  const original = confirmados[confirmados.length - 1];
  if (!original) return { ok: false, error: "No hay un pago trazable que se pueda revertir." };
  const clave = claveOperacionPM06("reverso", origenFactura, original.id);
  if (pagosPM06EnCurso[clave]) return pagosPM06EnCurso[clave];
  const tarea = (async () => {
    let pendiente = leerPendientePM06(clave);
    if (!pendiente) {
      pendiente = { id: uid(), operationId: `reverso-${original.id}-${Date.now()}-${uid()}`, pagoId: original.id };
      guardarPendientePM06(clave, pendiente);
    }
    if (modoSincronizadoPM06()) {
      if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
      const r2 = await window.__nubeCliente.rpc("revertir_pago_factura", {
        p_id: pendiente.id,
        p_operation_id: pendiente.operationId,
        p_pago_id: original.id,
        p_motivo: motivo
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el reverso: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed };
    }
    const pago = normalizarPagoPM06({ id: pendiente.id, operationId: pendiente.operationId, facturaId, origenFactura, empresaId, localId, importe: original.importe, fecha: todayISO(), estado: "REVERSO", reviertePagoId: original.id, datos: { motivo } });
    setPagosFacturas((s22) => [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
function crearLogicaGastos({ setGastosGenerales, localActivoId, empresaId }) {
  function addGasto(data) {
    const empresaDoc = data?.empresaId || empresaId || null;
    const localDoc = data?.localId || localActivoId || null;
    if (!empresaDoc || !localDoc) return { ok: false, error: "Selecciona un local antes de registrar el gasto." };
    const nuevo = { id: uid(), ...data, empresaId: empresaDoc, localId: localDoc, estado: data?.estado || "ACTIVO" };
    setGastosGenerales((s22) => [...s22, nuevo]);
    return { ok: true, gasto: nuevo };
  }
  function deleteGasto(id, localIdEsperado = localActivoId) {
    let eliminado = false;
    setGastosGenerales((s22) => {
      const actual = s22.find((g2) => g2.id === id);
      if (!actual || actual.empresaId !== empresaId || localIdEsperado && actual.localId !== localIdEsperado) return s22;
      if (actual.facturaDirectaId) return s22;
      eliminado = true;
      return s22.filter((g2) => g2.id !== id);
    });
    return eliminado;
  }
  return { addGasto, deleteGasto };
}
function crearLogicaCaja({ arqueos, setArqueos, movimientosCaja, setMovimientosCaja, localActivoId, empresaId }) {
  function leerBorradorArqueo(fecha) {
    return leerPendientePM08(clavePendientePM08("arqueo", empresaId, localActivoId, fecha))?.payload || null;
  }
  async function addArqueo(data) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de guardar el arqueo." };
    const fecha = data?.fecha || todayISO();
    const efectivoBase = redondearDineroPM08(data?.efectivoBase ?? data?.efectivoEsperado ?? 0);
    const efectivoContado = redondearDineroPM08(data?.efectivoContado ?? data?.efectivoReal);
    if (!Number.isFinite(efectivoBase) || efectivoBase < 0) return { ok: false, error: "El efectivo base no es v\xE1lido." };
    if (!Number.isFinite(efectivoContado) || efectivoContado < 0) return { ok: false, error: "El efectivo contado debe ser cero o un importe positivo." };
    if ((arqueos || []).some((a22) => a22.localId === localActivoId && a22.fecha === fecha && a22.estado !== "ANULADO")) {
      return { ok: false, error: "Ya existe un arqueo activo para ese d\xEDa y local." };
    }
    const payload = {
      empresaId,
      localId: localActivoId,
      fecha,
      efectivoBase,
      efectivoContado,
      notas: String(data?.notas || "").trim(),
      snapshot: data?.snapshot || {}
    };
    const clave = clavePendientePM08("arqueo", empresaId, localActivoId, fecha);
    const preparado = prepararPendientePM08(clave, "pm08-arqueo", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) {
          return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. El arqueo NO se ha confirmado; el borrador queda listo para reintentar." };
        }
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("registrar_arqueo_caja", {
              p_operation_id: preparado.pendiente.operationId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_fecha: fecha,
              p_efectivo_base: efectivoBase,
              p_efectivo_contado: efectivoContado,
              p_notas: payload.notas,
              p_snapshot: payload.snapshot
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 el arqueo. Conservamos el mismo identificador para reintentarlo sin duplicar." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM0
--- fin coincidencia ---

--- coincidencia 8 pos=4141389 ---
nte) };
    }
    const pago = normalizarPagoPM06({ ...pendiente, fecha: todayISO(), estado: "CONFIRMADO" });
    setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago.operationId) ? s22 : [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
async function revertirUltimoPagoPM06({ factura, origenFactura, pagosFacturas, setPagosFacturas, motivo = "Correcci\xF3n de pago" }) {
  const empresaId = factura?.empresaId || null;
  const localId = factura?.localId || null;
  const facturaId = factura?.id || null;
  const relacionados = (pagosFacturas || []).filter((p22) => p22 && p22.facturaId === facturaId && p22.origenFactura === origenFactura && p22.empresaId === empresaId && p22.localId === localId);
  const reversados = new Set(relacionados.filter((p22) => p22.estado === "REVERSO" && p22.reviertePagoId).map((p22) => p22.reviertePagoId));
  const confirmados = relacionados.filter((p22) => p22.estado === "CONFIRMADO" && !reversados.has(p22.id));
  const original = confirmados[confirmados.length - 1];
  if (!original) return { ok: false, error: "No hay un pago trazable que se pueda revertir." };
  const clave = claveOperacionPM06("reverso", origenFactura, original.id);
  if (pagosPM06EnCurso[clave]) return pagosPM06EnCurso[clave];
  const tarea = (async () => {
    let pendiente = leerPendientePM06(clave);
    if (!pendiente) {
      pendiente = { id: uid(), operationId: `reverso-${original.id}-${Date.now()}-${uid()}`, pagoId: original.id };
      guardarPendientePM06(clave, pendiente);
    }
    if (modoSincronizadoPM06()) {
      if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
      const r2 = await window.__nubeCliente.rpc("revertir_pago_factura", {
        p_id: pendiente.id,
        p_operation_id: pendiente.operationId,
        p_pago_id: original.id,
        p_motivo: motivo
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el reverso: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed };
    }
    const pago = normalizarPagoPM06({ id: pendiente.id, operationId: pendiente.operationId, facturaId, origenFactura, empresaId, localId, importe: original.importe, fecha: todayISO(), estado: "REVERSO", reviertePagoId: original.id, datos: { motivo } });
    setPagosFacturas((s22) => [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
function crearLogicaGastos({ setGastosGenerales, localActivoId, empresaId }) {
  function addGasto(data) {
    const empresaDoc = data?.empresaId || empresaId || null;
    const localDoc = data?.localId || localActivoId || null;
    if (!empresaDoc || !localDoc) return { ok: false, error: "Selecciona un local antes de registrar el gasto." };
    const nuevo = { id: uid(), ...data, empresaId: empresaDoc, localId: localDoc, estado: data?.estado || "ACTIVO" };
    setGastosGenerales((s22) => [...s22, nuevo]);
    return { ok: true, gasto: nuevo };
  }
  function deleteGasto(id, localIdEsperado = localActivoId) {
    let eliminado = false;
    setGastosGenerales((s22) => {
      const actual = s22.find((g2) => g2.id === id);
      if (!actual || actual.empresaId !== empresaId || localIdEsperado && actual.localId !== localIdEsperado) return s22;
      if (actual.facturaDirectaId) return s22;
      eliminado = true;
      return s22.filter((g2) => g2.id !== id);
    });
    return eliminado;
  }
  return { addGasto, deleteGasto };
}
function crearLogicaCaja({ arqueos, setArqueos, movimientosCaja, setMovimientosCaja, localActivoId, empresaId }) {
  function leerBorradorArqueo(fecha) {
    return leerPendientePM08(clavePendientePM08("arqueo", empresaId, localActivoId, fecha))?.payload || null;
  }
  async function addArqueo(data) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de guardar el arqueo." };
    const fecha = data?.fecha || todayISO();
    const efectivoBase = redondearDineroPM08(data?.efectivoBase ?? data?.efectivoEsperado ?? 0);
    const efectivoContado = redondearDineroPM08(data?.efectivoContado ?? data?.efectivoReal);
    if (!Number.isFinite(efectivoBase) || efectivoBase < 0) return { ok: false, error: "El efectivo base no es v\xE1lido." };
    if (!Number.isFinite(efectivoContado) || efectivoContado < 0) return { ok: false, error: "El efectivo contado debe ser cero o un importe positivo." };
    if ((arqueos || []).some((a22) => a22.localId === localActivoId && a22.fecha === fecha && a22.estado !== "ANULADO")) {
      return { ok: false, error: "Ya existe un arqueo activo para ese d\xEDa y local." };
    }
    const payload = {
      empresaId,
      localId: localActivoId,
      fecha,
      efectivoBase,
      efectivoContado,
      notas: String(data?.notas || "").trim(),
      snapshot: data?.snapshot || {}
    };
    const clave = clavePendientePM08("arqueo", empresaId, localActivoId, fecha);
    const preparado = prepararPendientePM08(clave, "pm08-arqueo", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) {
          return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. El arqueo NO se ha confirmado; el borrador queda listo para reintentar." };
        }
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("registrar_arqueo_caja", {
              p_operation_id: preparado.pendiente.operationId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_fecha: fecha,
              p_efectivo_base: efectivoBase,
              p_efectivo_contado: efectivoContado,
              p_notas: payload.notas,
              p_snapshot: payload.snapshot
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 el arqueo. Conservamos el mismo identificador para reintentarlo sin duplicar." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido guardar el arqueo.") };
          }
          const arqueo2 = normalizarArqueoPM08(r2.data?.arqueo);
          if (!arqueo2) throw new Error("respuesta_arqueo_incompleta");
          setArqueos((prev) => [arqueo2, ...(prev || []).filter((a22) => a22.operationId !== arqueo2.operationId && a22.id !== arqueo2.id)]);

--- fin coincidencia ---

--- coincidencia 9 pos=4141940 ---
 de pago" }) {
  const empresaId = factura?.empresaId || null;
  const localId = factura?.localId || null;
  const facturaId = factura?.id || null;
  const relacionados = (pagosFacturas || []).filter((p22) => p22 && p22.facturaId === facturaId && p22.origenFactura === origenFactura && p22.empresaId === empresaId && p22.localId === localId);
  const reversados = new Set(relacionados.filter((p22) => p22.estado === "REVERSO" && p22.reviertePagoId).map((p22) => p22.reviertePagoId));
  const confirmados = relacionados.filter((p22) => p22.estado === "CONFIRMADO" && !reversados.has(p22.id));
  const original = confirmados[confirmados.length - 1];
  if (!original) return { ok: false, error: "No hay un pago trazable que se pueda revertir." };
  const clave = claveOperacionPM06("reverso", origenFactura, original.id);
  if (pagosPM06EnCurso[clave]) return pagosPM06EnCurso[clave];
  const tarea = (async () => {
    let pendiente = leerPendientePM06(clave);
    if (!pendiente) {
      pendiente = { id: uid(), operationId: `reverso-${original.id}-${Date.now()}-${uid()}`, pagoId: original.id };
      guardarPendientePM06(clave, pendiente);
    }
    if (modoSincronizadoPM06()) {
      if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
      const r2 = await window.__nubeCliente.rpc("revertir_pago_factura", {
        p_id: pendiente.id,
        p_operation_id: pendiente.operationId,
        p_pago_id: original.id,
        p_motivo: motivo
      });
      if (r2.error) return { ok: false, pendiente: true, error: "No se ha podido confirmar el reverso: " + (r2.error.message || "error de sincronizaci\xF3n") };
      const pago2 = normalizarPagoPM06(r2.data?.pago);
      if (pago2) setPagosFacturas((s22) => s22.some((x3) => x3.operationId === pago2.operationId) ? s22 : [...s22, pago2]);
      limpiarPendientePM06(clave);
      return { ok: true, pago: pago2, replayed: !!r2.data?.replayed };
    }
    const pago = normalizarPagoPM06({ id: pendiente.id, operationId: pendiente.operationId, facturaId, origenFactura, empresaId, localId, importe: original.importe, fecha: todayISO(), estado: "REVERSO", reviertePagoId: original.id, datos: { motivo } });
    setPagosFacturas((s22) => [...s22, pago]);
    limpiarPendientePM06(clave);
    return { ok: true, pago, replayed: false };
  })();
  pagosPM06EnCurso[clave] = tarea;
  try {
    return await tarea;
  } finally {
    delete pagosPM06EnCurso[clave];
  }
}
function crearLogicaGastos({ setGastosGenerales, localActivoId, empresaId }) {
  function addGasto(data) {
    const empresaDoc = data?.empresaId || empresaId || null;
    const localDoc = data?.localId || localActivoId || null;
    if (!empresaDoc || !localDoc) return { ok: false, error: "Selecciona un local antes de registrar el gasto." };
    const nuevo = { id: uid(), ...data, empresaId: empresaDoc, localId: localDoc, estado: data?.estado || "ACTIVO" };
    setGastosGenerales((s22) => [...s22, nuevo]);
    return { ok: true, gasto: nuevo };
  }
  function deleteGasto(id, localIdEsperado = localActivoId) {
    let eliminado = false;
    setGastosGenerales((s22) => {
      const actual = s22.find((g2) => g2.id === id);
      if (!actual || actual.empresaId !== empresaId || localIdEsperado && actual.localId !== localIdEsperado) return s22;
      if (actual.facturaDirectaId) return s22;
      eliminado = true;
      return s22.filter((g2) => g2.id !== id);
    });
    return eliminado;
  }
  return { addGasto, deleteGasto };
}
function crearLogicaCaja({ arqueos, setArqueos, movimientosCaja, setMovimientosCaja, localActivoId, empresaId }) {
  function leerBorradorArqueo(fecha) {
    return leerPendientePM08(clavePendientePM08("arqueo", empresaId, localActivoId, fecha))?.payload || null;
  }
  async function addArqueo(data) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de guardar el arqueo." };
    const fecha = data?.fecha || todayISO();
    const efectivoBase = redondearDineroPM08(data?.efectivoBase ?? data?.efectivoEsperado ?? 0);
    const efectivoContado = redondearDineroPM08(data?.efectivoContado ?? data?.efectivoReal);
    if (!Number.isFinite(efectivoBase) || efectivoBase < 0) return { ok: false, error: "El efectivo base no es v\xE1lido." };
    if (!Number.isFinite(efectivoContado) || efectivoContado < 0) return { ok: false, error: "El efectivo contado debe ser cero o un importe positivo." };
    if ((arqueos || []).some((a22) => a22.localId === localActivoId && a22.fecha === fecha && a22.estado !== "ANULADO")) {
      return { ok: false, error: "Ya existe un arqueo activo para ese d\xEDa y local." };
    }
    const payload = {
      empresaId,
      localId: localActivoId,
      fecha,
      efectivoBase,
      efectivoContado,
      notas: String(data?.notas || "").trim(),
      snapshot: data?.snapshot || {}
    };
    const clave = clavePendientePM08("arqueo", empresaId, localActivoId, fecha);
    const preparado = prepararPendientePM08(clave, "pm08-arqueo", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) {
          return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. El arqueo NO se ha confirmado; el borrador queda listo para reintentar." };
        }
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("registrar_arqueo_caja", {
              p_operation_id: preparado.pendiente.operationId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_fecha: fecha,
              p_efectivo_base: efectivoBase,
              p_efectivo_contado: efectivoContado,
              p_notas: payload.notas,
              p_snapshot: payload.snapshot
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 el arqueo. Conservamos el mismo identificador para reintentarlo sin duplicar." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido guardar el arqueo.") };
          }
          const arqueo2 = normalizarArqueoPM08(r2.data?.arqueo);
          if (!arqueo2) throw new Error("respuesta_arqueo_incompleta");
          setArqueos((prev) => [arqueo2, ...(prev || []).filter((a22) => a22.operationId !== arqueo2.operationId && a22.id !== arqueo2.id)]);
          limpiarPendientePM08(clave);
          try {
            await sincronizarCajaPm08({ setArqueos, setMovimientosCaja });
          } catch {
          }
          return { ok: true, arqueo: arqueo2, replayed: !!r2.data?.replayed };
        } catch (e2) {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 el arqueo. Conservamos el mismo identificador para reintentarlo sin duplicar." };
        }
      }
      const efectos = (movimientosCaja || []).filter((m22) => m22.localId === localAc
--- fin coincidencia ---

--- coincidencia 10 pos=4158455 ---
ientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId && m22.id !== movimiento2.id)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 el movimiento. Reintenta: se utilizar\xE1 el mismo identificador y no se duplicar\xE1." };
        }
      }
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha,
        hora: horaActualPM08(),
        tipo: tipoCanonico,
        importe: imp,
        efectoEfectivo: tipoCanonico === "ENTRADA" ? imp : -imp,
        medioPago: "EFECTIVO",
        concepto: conceptoLimpio,
        origen: "MANUAL",
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function eliminarMovimientoCaja(id, motivo) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona el local del movimiento." };
    const original = (movimientosCaja || []).find((m22) => (m22.operationId === id || m22.id === id) && m22.localId === localActivoId);
    if (!original || !["ENTRADA", "RETIRADA", "entrada", "salida"].includes(original.tipo)) return { ok: false, error: "Solo se pueden revertir entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant 
--- fin coincidencia ---

--- coincidencia 11 pos=4158632 ---
ave);
          if (!r2.data?.replayed) registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 el movimiento. Reintenta: se utilizar\xE1 el mismo identificador y no se duplicar\xE1." };
        }
      }
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha,
        hora: horaActualPM08(),
        tipo: tipoCanonico,
        importe: imp,
        efectoEfectivo: tipoCanonico === "ENTRADA" ? imp : -imp,
        medioPago: "EFECTIVO",
        concepto: conceptoLimpio,
        origen: "MANUAL",
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function eliminarMovimientoCaja(id, motivo) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona el local del movimiento." };
    const original = (movimientosCaja || []).find((m22) => (m22.operationId === id || m22.id === id) && m22.localId === localActivoId);
    if (!original || !["ENTRADA", "RETIRADA", "entrada", "salida"].includes(original.tipo)) return { ok: false, error: "Solo se pueden revertir entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no pu
--- fin coincidencia ---

--- coincidencia 12 pos=4158683 ---
ditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 el movimiento. Reintenta: se utilizar\xE1 el mismo identificador y no se duplicar\xE1." };
        }
      }
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha,
        hora: horaActualPM08(),
        tipo: tipoCanonico,
        importe: imp,
        efectoEfectivo: tipoCanonico === "ENTRADA" ? imp : -imp,
        medioPago: "EFECTIVO",
        concepto: conceptoLimpio,
        origen: "MANUAL",
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function eliminarMovimientoCaja(id, motivo) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona el local del movimiento." };
    const original = (movimientosCaja || []).find((m22) => (m22.operationId === id || m22.id === id) && m22.localId === localActivoId);
    if (!original || !["ENTRADA", "RETIRADA", "entrada", "salida"].includes(original.tipo)) return { ok: false, error: "Solo se pueden revertir entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return
--- fin coincidencia ---

--- coincidencia 13 pos=4158777 ---
     return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 el movimiento. Reintenta: se utilizar\xE1 el mismo identificador y no se duplicar\xE1." };
        }
      }
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha,
        hora: horaActualPM08(),
        tipo: tipoCanonico,
        importe: imp,
        efectoEfectivo: tipoCanonico === "ENTRADA" ? imp : -imp,
        medioPago: "EFECTIVO",
        concepto: conceptoLimpio,
        origen: "MANUAL",
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function eliminarMovimientoCaja(id, motivo) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona el local del movimiento." };
    const original = (movimientosCaja || []).find((m22) => (m22.operationId === id || m22.id === id) && m22.localId === localActivoId);
    if (!original || !["ENTRADA", "RETIRADA", "entrada", "salida"].includes(original.tipo)) return { ok: false, error: "Solo se pueden revertir entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    if (reembolsoNum === 0 &&
--- fin coincidencia ---

--- coincidencia 14 pos=4158959 ---
servidor recibi\xF3 el movimiento. Reintenta: se utilizar\xE1 el mismo identificador y no se duplicar\xE1." };
        }
      }
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha,
        hora: horaActualPM08(),
        tipo: tipoCanonico,
        importe: imp,
        efectoEfectivo: tipoCanonico === "ENTRADA" ? imp : -imp,
        medioPago: "EFECTIVO",
        concepto: conceptoLimpio,
        origen: "MANUAL",
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function eliminarMovimientoCaja(id, motivo) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona el local del movimiento." };
    const original = (movimientosCaja || []).find((m22) => (m22.operationId === id || m22.id === id) && m22.localId === localActivoId);
    if (!original || !["ENTRADA", "RETIRADA", "entrada", "salida"].includes(original.tipo)) return { ok: false, error: "Solo se pueden revertir entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    if (reembolsoNum === 0 && medio !== "SIN_REEMBOLSO") return { ok: false, error: "Para 0 \u20AC elige \u201CSin reembolso / cambio\u201D." };
    if (reembolsoNum > 0 && !["EFECTIVO", "TARJETA", "TRANSFERENCI
--- fin coincidencia ---

--- coincidencia 15 pos=4159027 ---
mo identificador y no se duplicar\xE1." };
        }
      }
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha,
        hora: horaActualPM08(),
        tipo: tipoCanonico,
        importe: imp,
        efectoEfectivo: tipoCanonico === "ENTRADA" ? imp : -imp,
        medioPago: "EFECTIVO",
        concepto: conceptoLimpio,
        origen: "MANUAL",
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function eliminarMovimientoCaja(id, motivo) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona el local del movimiento." };
    const original = (movimientosCaja || []).find((m22) => (m22.operationId === id || m22.id === id) && m22.localId === localActivoId);
    if (!original || !["ENTRADA", "RETIRADA", "entrada", "salida"].includes(original.tipo)) return { ok: false, error: "Solo se pueden revertir entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    if (reembolsoNum === 0 && medio !== "SIN_REEMBOLSO") return { ok: false, error: "Para 0 \u20AC elige \u201CSin reembolso / cambio\u201D." };
    if (reembolsoNum > 0 && !["EFECTIVO", "TARJETA", "TRANSFERENCIA", "OTRO"].includes(medio)) return { ok: false, error: "Elige el me
--- fin coincidencia ---

--- coincidencia 16 pos=4159363 ---
ctivo: tipoCanonico === "ENTRADA" ? imp : -imp,
        medioPago: "EFECTIVO",
        concepto: conceptoLimpio,
        origen: "MANUAL",
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function eliminarMovimientoCaja(id, motivo) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona el local del movimiento." };
    const original = (movimientosCaja || []).find((m22) => (m22.operationId === id || m22.id === id) && m22.localId === localActivoId);
    if (!original || !["ENTRADA", "RETIRADA", "entrada", "salida"].includes(original.tipo)) return { ok: false, error: "Solo se pueden revertir entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    if (reembolsoNum === 0 && medio !== "SIN_REEMBOLSO") return { ok: false, error: "Para 0 \u20AC elige \u201CSin reembolso / cambio\u201D." };
    if (reembolsoNum > 0 && !["EFECTIVO", "TARJETA", "TRANSFERENCIA", "OTRO"].includes(medio)) return { ok: false, error: "Elige el medio de reintegro." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const lineaVenta = (movimientos || []
--- fin coincidencia ---

--- coincidencia 17 pos=4159670 ---
!== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("MOVIMIENTO_CAJA", `${tipoCanonico} de ${money(imp)} \xB7 ${conceptoLimpio}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function eliminarMovimientoCaja(id, motivo) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona el local del movimiento." };
    const original = (movimientosCaja || []).find((m22) => (m22.operationId === id || m22.id === id) && m22.localId === localActivoId);
    if (!original || !["ENTRADA", "RETIRADA", "entrada", "salida"].includes(original.tipo)) return { ok: false, error: "Solo se pueden revertir entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    if (reembolsoNum === 0 && medio !== "SIN_REEMBOLSO") return { ok: false, error: "Para 0 \u20AC elige \u201CSin reembolso / cambio\u201D." };
    if (reembolsoNum > 0 && !["EFECTIVO", "TARJETA", "TRANSFERENCIA", "OTRO"].includes(medio)) return { ok: false, error: "Elige el medio de reintegro." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const lineaVenta = (movimientos || []).find((m22) => esVenta(m22) && (m22.operationId || m22.ventaId || m22.id) === ventaId && m22.productoId === productoId && (!m22.localId || m22.localId === localActivoId));
    if (!lineaVenta) return { ok: false, error: "El producto no pertenece a la venta seleccionada." };
    if ((movimientos || []).som
--- fin coincidencia ---

--- coincidencia 18 pos=4159973 ---
it tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function eliminarMovimientoCaja(id, motivo) {
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona el local del movimiento." };
    const original = (movimientosCaja || []).find((m22) => (m22.operationId === id || m22.id === id) && m22.localId === localActivoId);
    if (!original || !["ENTRADA", "RETIRADA", "entrada", "salida"].includes(original.tipo)) return { ok: false, error: "Solo se pueden revertir entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    if (reembolsoNum === 0 && medio !== "SIN_REEMBOLSO") return { ok: false, error: "Para 0 \u20AC elige \u201CSin reembolso / cambio\u201D." };
    if (reembolsoNum > 0 && !["EFECTIVO", "TARJETA", "TRANSFERENCIA", "OTRO"].includes(medio)) return { ok: false, error: "Elige el medio de reintegro." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const lineaVenta = (movimientos || []).find((m22) => esVenta(m22) && (m22.operationId || m22.ventaId || m22.id) === ventaId && m22.productoId === productoId && (!m22.localId || m22.localId === localActivoId));
    if (!lineaVenta) return { ok: false, error: "El producto no pertenece a la venta seleccionada." };
    if ((movimientos || []).some((m22) => m22.tipo === "REVERSO" && (m22.anulaVentaId === ventaId || m22.documentoOrigenId === ventaId))) return { ok: false, error: "La venta est\xE1 anulada y ya no admite devoluciones." };
    const cantidadOriginal = Math.abs(Number(lineaVenta.cantidadFisica ?? lineaVenta.cantidad) || 0);
    cons
--- fin coincidencia ---

--- coincidencia 19 pos=4160493 ---
entradas o retiradas manuales." };
    const motivoLimpio = String(motivo || "").trim();
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo del reverso." };
    if ((movimientosCaja || []).some((m22) => m22.refOperationId === (original.operationId || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    if (reembolsoNum === 0 && medio !== "SIN_REEMBOLSO") return { ok: false, error: "Para 0 \u20AC elige \u201CSin reembolso / cambio\u201D." };
    if (reembolsoNum > 0 && !["EFECTIVO", "TARJETA", "TRANSFERENCIA", "OTRO"].includes(medio)) return { ok: false, error: "Elige el medio de reintegro." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const lineaVenta = (movimientos || []).find((m22) => esVenta(m22) && (m22.operationId || m22.ventaId || m22.id) === ventaId && m22.productoId === productoId && (!m22.localId || m22.localId === localActivoId));
    if (!lineaVenta) return { ok: false, error: "El producto no pertenece a la venta seleccionada." };
    if ((movimientos || []).some((m22) => m22.tipo === "REVERSO" && (m22.anulaVentaId === ventaId || m22.documentoOrigenId === ventaId))) return { ok: false, error: "La venta est\xE1 anulada y ya no admite devoluciones." };
    const cantidadOriginal = Math.abs(Number(lineaVenta.cantidadFisica ?? lineaVenta.cantidad) || 0);
    const anteriores = (devoluciones || []).filter((d2) => d2.tipo === "cliente" && d2.ventaId === ventaId && d2.productoId === productoId);
    const cantidadDevuelta = anteriores.reduce((acc, d2) => acc + (Number(d2.cantidad) || 0), 0);
    const reembolsado = anteriores.reduce((acc, d2) => acc + (Number(d2.reembolso) || 0), 0);
    if (cantidadDevuelta + cant > cantidadOriginal + 1e-6) return { ok: false, error: "La cantidad supera lo que queda pendiente de devolver en la venta." };
    const brutoUnitario = Math.abs(Nu
--- fin coincidencia ---

--- coincidencia 20 pos=4160756 ---
Id || original.id))) return { ok: false, error: "Ese movimiento ya tiene un reverso." };
    const clave = clavePendientePM08("reverso-caja", empresaId, localActivoId, original.operationId || original.id);
    const fechaReverso = leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = { movimientoOperationId: original.operationId || original.id, motivo: motivoLimpio, fecha: fechaReverso };
    const preparado = prepararPendientePM08(clave, "pm08-reverso-caja", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n. El reverso NO se ha confirmado." };
        try {
          const r2 = await Promise.race([
            window.__nubeCliente.rpc("revertir_movimiento_caja", { p_operation_id: preparado.pendiente.operationId, p_movimiento_operation_id: original.operationId || original.id, p_motivo: motivoLimpio, p_fecha: fechaReverso }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r2.error) {
            if (esErrorTransitorioPM08(r2.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r2.error, "No se ha podido revertir el movimiento.") };
          }
          const movimiento2 = normalizarMovimientoCajaPM08(r2.data?.movimiento);
          setMovimientosCaja((prev) => [movimiento2, ...(prev || []).filter((m22) => m22.operationId !== movimiento2.operationId)]);
          limpiarPendientePM08(clave);
          if (!r2.data?.replayed) registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
          return { ok: true, movimiento: movimiento2, replayed: !!r2.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar el reverso. Reintenta con el mismo borrador." };
        }
      }
      const tipoOriginal = String(original.tipo).toUpperCase() === "ENTRADA" ? "ENTRADA" : "RETIRADA";
      const movimiento = normalizarMovimientoCajaPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        fecha: fechaReverso,
        tipo: tipoOriginal === "ENTRADA" ? "REVERSO_ENTRADA" : "REVERSO_RETIRADA",
        importe: original.importe,
        efectoEfectivo: -(Number(original.efectoEfectivo) || (tipoOriginal === "ENTRADA" ? Number(original.importe) : -Number(original.importe))),
        medioPago: "EFECTIVO",
        concepto: `Reverso: ${motivoLimpio}`,
        origen: "REVERSO_CAJA",
        referenciaId: original.operationId || original.id,
        refOperationId: original.operationId || original.id,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      setMovimientosCaja((prev) => [movimiento, ...(prev || []).filter((m22) => m22.operationId !== movimiento.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria?.("REVERSO_CAJA", `${motivoLimpio} \xB7 ${original.operationId || original.id}`);
      return { ok: true, movimiento, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  return { registrarMovimientoCaja, eliminarMovimientoCaja, leerBorradorMovimientoCaja };
}
function crearLogicaDevoluciones({ productos, setProductos, movimientos, setMovimientos, devoluciones, setDevoluciones, setMovimientosCaja, setArqueos, registrarAuditoria, localActivoId, empresaId }) {
  function productoEsDelLocalActivoDevolucion(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return !prod.localId || prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function leerBorradorDevolucion(tipo) {
    return leerPendientePM08(clavePendientePM08(`devolucion-${tipo}`, empresaId, localActivoId))?.payload || null;
  }
  async function registrarDevolucionCliente({ productoId, cantidad, motivo, reembolso, medioReembolso, ventaId, fecha }) {
    const cant = Number(cantidad);
    const reembolsoNum = redondearDineroPM08(reembolso === "" || reembolso == null ? 0 : reembolso);
    const medio = String(medioReembolso || (reembolsoNum > 0 ? "EFECTIVO" : "SIN_REEMBOLSO")).trim().toUpperCase();
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!ventaId) return { ok: false, error: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    if (reembolsoNum === 0 && medio !== "SIN_REEMBOLSO") return { ok: false, error: "Para 0 \u20AC elige \u201CSin reembolso / cambio\u201D." };
    if (reembolsoNum > 0 && !["EFECTIVO", "TARJETA", "TRANSFERENCIA", "OTRO"].includes(medio)) return { ok: false, error: "Elige el medio de reintegro." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const lineaVenta = (movimientos || []).find((m22) => esVenta(m22) && (m22.operationId || m22.ventaId || m22.id) === ventaId && m22.productoId === productoId && (!m22.localId || m22.localId === localActivoId));
    if (!lineaVenta) return { ok: false, error: "El producto no pertenece a la venta seleccionada." };
    if ((movimientos || []).some((m22) => m22.tipo === "REVERSO" && (m22.anulaVentaId === ventaId || m22.documentoOrigenId === ventaId))) return { ok: false, error: "La venta est\xE1 anulada y ya no admite devoluciones." };
    const cantidadOriginal = Math.abs(Number(lineaVenta.cantidadFisica ?? lineaVenta.cantidad) || 0);
    const anteriores = (devoluciones || []).filter((d2) => d2.tipo === "cliente" && d2.ventaId === ventaId && d2.productoId === productoId);
    const cantidadDevuelta = anteriores.reduce((acc, d2) => acc + (Number(d2.cantidad) || 0), 0);
    const reembolsado = anteriores.reduce((acc, d2) => acc + (Number(d2.reembolso) || 0), 0);
    if (cantidadDevuelta + cant > cantidadOriginal + 1e-6) return { ok: false, error: "La cantidad supera lo que queda pendiente de devolver en la venta." };
    const brutoUnitario = Math.abs(Number(lineaVenta.ingresoUnitario) || 0) * (1 + (Number(lineaVenta.ivaVentaAplicado) || 0) / 100);
    if (reembolsoNum > 0 && !(brutoUnitario > 0)) return { ok: false, error: "La venta no conserva un importe verificable para autorizar el reembolso." };
    if (ree
--- fin coincidencia ---

PATRON 'DEVOLUCION_CLIENTE': 2 coincidencias
--- coincidencia 1 pos=4169846 ---
eal,
              p_datos: { productoNombre: producto.nombre, origen: "L&A Suite PM-08" }
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r3.error) {
            if (esErrorTransitorioPM08(r3.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. Conservamos el mismo identificador para evitar duplicar stock o reembolso." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r3.error, "No se ha podido registrar la devoluci\xF3n.") };
          }
          const registro2 = normalizarDevolucionPM08(r3.data?.devolucion, "cliente");
          const movimientoCaja2 = normalizarMovimientoCajaPM08(r3.data?.movimientoCaja);
          if (!registro2) throw new Error("respuesta_devolucion_incompleta");
          setDevoluciones((s22) => [registro2, ...(s22 || []).filter((d2) => d2.operationId !== registro2.operationId && d2.id !== registro2.id)]);
          if (movimientoCaja2) setMovimientosCaja((s22) => [movimientoCaja2, ...(s22 || []).filter((m22) => m22.operationId !== movimientoCaja2.operationId)]);
          limpiarPendientePM08(clave);
          try {
            await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
            await sincronizarCajaPm08({ setArqueos, setMovimientosCaja, setDevoluciones });
          } catch {
          }
          if (!r3.data?.replayed) registrarAuditoria("Devoluci\xF3n de cliente", `${producto.nombre} \xD7${cant}${reembolsoNum > 0 ? ` \xB7 reembolso \u20AC${fmt(reembolsoNum)} (${medio})` : " \xB7 sin reembolso"}`);
          return { ok: true, registro: registro2, movimientoCaja: movimientoCaja2, replayed: !!r3.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. No repitas con otros datos: usa el mismo borrador para evitar duplicar stock o reembolso." };
        }
      }
      const r2 = aplicarMovimientoStock({
        productoId,
        cantidad: cant,
        tipo: "DEVOLUCION_CLIENTE",
        operationId: preparado.pendiente.operationId,
        movimientoId: `pm08-local-stock-${uid()}`,
        origen: "devolucionCliente",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        motivo: `Devoluci\xF3n de cliente \xB7 ${motivoLimpio}`,
        permitirDeficit: false,
        camposExtra: { reembolso: reembolsoNum, medioReembolso: medio, ingresoUnitario: -(Number(lineaVenta.ingresoUnitario) || 0), ivaVentaAplicado: Number(lineaVenta.ivaVentaAplicado) || 0 }
      });
      if (!r2.ok) {
        limpiarPendientePM08(clave);
        return { ok: false, error: r2.error || "No se ha podido aplicar al stock." };
      }
      const registro = normalizarDevolucionPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        tipo: "cliente",
        fecha: fechaReal,
        productoId,
        productoNombre: producto.nombre,
        cantidad: cant,
        motivo: motivoLimpio,
        reembolso: reembolsoNum,
        medioReembolso: medio,
        ventaId,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      }, "cliente");
      let movimientoCaja = null;
      if (reembolsoNum > 0) {
        movimientoCaja = normalizarMovimientoCajaPM08({
          operationId: preparado.pendiente.operationId,
          empresaId,
          localId: localActivoId,
          fecha: fechaReal,
          tipo: "REEMBOLSO",
          importe: reembolsoNum,
          efectoEfectivo: medio === "EFECTIVO" ? -reembolsoNum : 0,
          medioPago: medio,
          concepto: `Reembolso devoluci\xF3n \xB7 ${producto.nombre}`,
          origen: "DEVOLUCION_CLIENTE",
          referenciaId: preparado.pendiente.operationId,
          createdAt: (/* @__PURE__ */ new Date()).toISOString()
        });
        setMovimientosCaja((s22) => [movimientoCaja, ...(s22 || []).filter((m22) => m22.operationId !== movimientoCaja.operationId)]);
      }
      setDevoluciones((s22) => [registro, ...(s22 || []).filter((d2) => d2.operationId !== registro.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria("Devoluci\xF3n de cliente", `${producto.nombre} \xD7${cant}${reembolsoNum > 0 ? ` \xB7 reembolso \u20AC${fmt(reembolsoNum)} (${medio})` : " \xB7 sin reembolso"}`);
      return { ok: true, registro, movimientoCaja, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function registrarDevolucionProveedor({ productoId, cantidad, motivo, proveedorId, proveedorNombre, fecha }) {
    const cant = Number(cantidad);
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const clave = clavePendientePM08("devolucion-proveedor", empresaId, localActivoId);
    const fechaReal = fecha || leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = {
      empresaId,
      localId: localActivoId,
      productoId,
      productoNombre: producto.nombre,
      cantidad: cant,
      proveedorId: proveedorId || null,
      proveedorNombre: proveedorNombre || "",
      motivo: motivoLimpio,
      fecha: fechaReal
    };
    const preparado = prepararPendientePM08(clave, "pm08-dev-proveedor", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. No se ha descontado stock; el borrador queda listo para reintentar." };
        try {
          const r3 = await Promise.race([
            window.__nubeCliente.rpc("registrar_devolucion_proveedor", {
              p_operation_id: preparado.pendiente.operationId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_producto_id: productoId,
              p_cantidad: cant,
              p_proveedor_id: proveedorId || null,
              p_proveedor_nombre: proveedorNombre || "",
              p_motivo: motivoLimpio,
              p_fecha: fechaReal,
              p_datos: { productoNombre: producto.nombre, origen: "L&A Suite PM-08" }
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r3.error) 
--- fin coincidencia ---

--- coincidencia 2 pos=4171555 ---
\xB7 sin reembolso"}`);
          return { ok: true, registro: registro2, movimientoCaja: movimientoCaja2, replayed: !!r3.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. No repitas con otros datos: usa el mismo borrador para evitar duplicar stock o reembolso." };
        }
      }
      const r2 = aplicarMovimientoStock({
        productoId,
        cantidad: cant,
        tipo: "DEVOLUCION_CLIENTE",
        operationId: preparado.pendiente.operationId,
        movimientoId: `pm08-local-stock-${uid()}`,
        origen: "devolucionCliente",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        motivo: `Devoluci\xF3n de cliente \xB7 ${motivoLimpio}`,
        permitirDeficit: false,
        camposExtra: { reembolso: reembolsoNum, medioReembolso: medio, ingresoUnitario: -(Number(lineaVenta.ingresoUnitario) || 0), ivaVentaAplicado: Number(lineaVenta.ivaVentaAplicado) || 0 }
      });
      if (!r2.ok) {
        limpiarPendientePM08(clave);
        return { ok: false, error: r2.error || "No se ha podido aplicar al stock." };
      }
      const registro = normalizarDevolucionPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        tipo: "cliente",
        fecha: fechaReal,
        productoId,
        productoNombre: producto.nombre,
        cantidad: cant,
        motivo: motivoLimpio,
        reembolso: reembolsoNum,
        medioReembolso: medio,
        ventaId,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      }, "cliente");
      let movimientoCaja = null;
      if (reembolsoNum > 0) {
        movimientoCaja = normalizarMovimientoCajaPM08({
          operationId: preparado.pendiente.operationId,
          empresaId,
          localId: localActivoId,
          fecha: fechaReal,
          tipo: "REEMBOLSO",
          importe: reembolsoNum,
          efectoEfectivo: medio === "EFECTIVO" ? -reembolsoNum : 0,
          medioPago: medio,
          concepto: `Reembolso devoluci\xF3n \xB7 ${producto.nombre}`,
          origen: "DEVOLUCION_CLIENTE",
          referenciaId: preparado.pendiente.operationId,
          createdAt: (/* @__PURE__ */ new Date()).toISOString()
        });
        setMovimientosCaja((s22) => [movimientoCaja, ...(s22 || []).filter((m22) => m22.operationId !== movimientoCaja.operationId)]);
      }
      setDevoluciones((s22) => [registro, ...(s22 || []).filter((d2) => d2.operationId !== registro.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria("Devoluci\xF3n de cliente", `${producto.nombre} \xD7${cant}${reembolsoNum > 0 ? ` \xB7 reembolso \u20AC${fmt(reembolsoNum)} (${medio})` : " \xB7 sin reembolso"}`);
      return { ok: true, registro, movimientoCaja, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function registrarDevolucionProveedor({ productoId, cantidad, motivo, proveedorId, proveedorNombre, fecha }) {
    const cant = Number(cantidad);
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const clave = clavePendientePM08("devolucion-proveedor", empresaId, localActivoId);
    const fechaReal = fecha || leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = {
      empresaId,
      localId: localActivoId,
      productoId,
      productoNombre: producto.nombre,
      cantidad: cant,
      proveedorId: proveedorId || null,
      proveedorNombre: proveedorNombre || "",
      motivo: motivoLimpio,
      fecha: fechaReal
    };
    const preparado = prepararPendientePM08(clave, "pm08-dev-proveedor", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. No se ha descontado stock; el borrador queda listo para reintentar." };
        try {
          const r3 = await Promise.race([
            window.__nubeCliente.rpc("registrar_devolucion_proveedor", {
              p_operation_id: preparado.pendiente.operationId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_producto_id: productoId,
              p_cantidad: cant,
              p_proveedor_id: proveedorId || null,
              p_proveedor_nombre: proveedorNombre || "",
              p_motivo: motivoLimpio,
              p_fecha: fechaReal,
              p_datos: { productoNombre: producto.nombre, origen: "L&A Suite PM-08" }
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r3.error) {
            if (esErrorTransitorioPM08(r3.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. Reintenta con el mismo borrador para no duplicar la salida de stock." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r3.error, "No se ha podido registrar la devoluci\xF3n al proveedor.") };
          }
          const registro2 = normalizarDevolucionPM08(r3.data?.devolucion, "proveedor");
          if (!registro2) throw new Error("respuesta_devolucion_incompleta");
          setDevoluciones((s22) => [registro2, ...(s22 || []).filter((d2) => d2.operationId !== registro2.operationId && d2.id !== registro2.id)]);
          limpiarPendientePM08(clave);
          try {
            await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
            await sincronizarCajaPm08({ setArqueos, setMovimientosCaja, setDevoluciones });
          } catch {
          }
          if (!r3.data?.replayed) registrarAuditoria("Devoluci\xF3n a proveedor", `${producto.nombre} \xD7${cant}${proveedorNombre ? ` \xB7 ${proveedorNombre}` : ""}`);
          return { ok: true, registro: registro2, replayed: !!r3.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. Reintenta con el mismo borrador para no duplicar la salida de stock." };
        }
      }
      if ((Number(producto.stock) || 0) + 1e-6 < cant) {
        limpiarPendientePM08(clave);
        return { ok: false, error: "No hay stock suficiente para devolver esa cantidad al proveedor." };
      }
      const r2 = aplic
--- fin coincidencia ---

PATRON 'ingresoUnitario': 34 coincidencias
--- coincidencia 1 pos=3988988 ---
);
  let qStock = supabase.from("stock_estado").select("empresa_id,local_id,producto_id,almacen,piso,total,minimo,bajo_minimo,fraccionable,precision_cantidad,local_operable");
  let qMov = supabase.from("movimientos_stock").select("id,operation_id,tipo,empresa_id,local_id,producto_id,delta_almacen,delta_piso,delta_total,cantidad,movimiento_original_id,datos,actor_user_id,created_at").order("created_at", { ascending: false }).limit(2e3);
  const [rStock, rMov] = await Promise.all([qStock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : null;
      const st2 = exacto || activo;
      if (!st2) return prod;
      return { ...prod, stock: Number(st2.total) || 0, stockPisoVenta: Number(st2.piso) || 0, stockMinimo: Number(st2.minimo) || 0, _pm07BajoMinimo: !!st2.bajo_minimo, _pm07Servidor: true, _pm07LocalOperable: st2.local_operable !== false };
    }));
  }
  if (typeof setMovimientos === "function") {
    const server = movs.map((m4) => {
      const d2 = m4.datos && typeof m4.datos === "object" ? m4.datos : {};
      const delta = Number(m4.delta_total) || 0;
      const creado = String(m4.created_at || "");
      return {
        id: `pm07-${m4.id}`,
        operationId: m4.operation_id,
        ventaId: d2.ventaId || (m4.tipo === "VENTA" ? m4.operation_id : d2.anulaVentaId || null),
        anulaVentaId: d2.anulaVentaId || null,
        productoId: m4.producto_id,
        localId: m4.local_id,
        empresaId: m4.empresa_id,
        cantidad: delta,
        cantidadFisica: Number(m4.cantidad) || Math.abs(delta),
        tipo: m4.tipo,
        motivo: d2.motivo || m4.tipo,
        referencia: d2.referencia || "PM-07",
        costoUnitario: Number(d2.costoUnitario) || 0,
        ingresoUnitario: Number(d2.ingresoUnitario) || 0,
        ivaVentaAplicado: Number(d2.ivaVentaAplicado) || 0,
        medioPago: d2.medioPago || null,
        detallePago: d2.detallePago || null,
        fecha: creado ? creado.slice(0, 10) : todayISO(),
        hora: creado ? creado.slice(11, 16) : (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
        afectaStockTotal: delta !== 0,
        afectaStockPisoVenta: Number(m4.delta_piso) !== 0,
        movimientoOriginalId: m4.movimiento_original_id || null,
        _pm07Servidor: true
      };
    });
    setMovimientos((prev) => [...server, ...(prev || []).filter((m4) => !m4._pm07Servidor)]);
  }
  return { ok: true, stocks: stocks.length, movimientos: movs.length };
}
function redondearDineroPM08(valor) {
  const n2 = Number(valor);
  return Number.isFinite(n2) ? Math.round((n2 + Number.EPSILON) * 100) / 100 : NaN;
}
function modoSincronizadoPM08() {
  return typeof window !== "undefined" && !!window.NUBE_URL && !window.__modoPruebasLocal;
}
function horaActualPM08() {
  return (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5);
}
function clavePendientePM08(tipo, empresaId, localId, extra = "") {
  return `almacen:pm08:${tipo}:${empresaId || "sin-empresa"}:${localId || "todos"}${extra ? `:${extra}` : ""}`;
}
function leerPendientePM08(clave) {
  try {
    return JSON.parse(localStorage.getItem(clave) || "null");
  } catch {
    return null;
  }
}
function guardarPendientePM08(clave, valor) {
  try {
    localStorage.setItem(clave, JSON.stringify(valor));
    return true;
  } catch {
    return false;
  }
}
function limpiarPendientePM08(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
function prepararPendientePM08(clave, prefijo, payload) {
  const existente = leerPendientePM08(clave);
  if (existente) {
    if (JSON.stringify(existente.payload) !== JSON.stringify(payload)) {
      return { ok: false, pendiente: existente, error: "Hay una operaci\xF3n anterior pendiente en este local. Reint\xE9ntala con sus mismos datos antes de iniciar otra." };
    }
    return { ok: true, pendiente: existente, recuperada: true };
  }
  const pendiente = { operationId: `${prefijo}-${Date.now()}-${uid()}`, payload, creadoEn: (/* @__PURE__ */ new Date()).toISOString() };
  if (!guardarPendientePM08(clave, pendiente)) {
    return { ok: false, error: "No se ha podido conservar el borrador de la operaci\xF3n en este dispositivo." };
  }
  return { ok: true, pendiente, recuperada: false };
}
var operacionesPM08EnCurso = {};
function esErrorTransitorioPM08(error) {
  const codigo = String(error?.code || "").toLowerCase();
  const mensaje = String(error?.message || error || "").toLowerCase();
  return !codigo && /failed to fetch|fetch failed|network|conexi[oó]n|timeout|load failed|offline/.test(mensaje);
}
function mensajeErrorPM08(error, fallback = "No se ha podido confirmar la operaci\xF3n.") {
  const mensaje = String(error?.message || error || "");
  const mensajes = [
    ["operation_id_conflict", "El identificador de reintento ya existe con datos distintos. La operaci\xF3n no se ha repetido."],
    ["contexto_no_autorizado", "La empresa o el local no pertenecen al \xE1mbito autorizado de esta cuenta."],
    ["venta_fuera_de_contexto", "La venta pertenece a otra empresa o a otro local."],
    ["local_inactivo", "El local est\xE1 cerrado o inactivo y no admite nuevas operaciones."],
    ["periodo_caja_cerrado", "Ese d\xEDa ya tiene un arqueo activo. Anula el arqueo de forma trazable antes de modificar la caja."],
    ["arqueo_ya_existe", "Ya existe un arqueo activo para ese d\xEDa y local."],
    ["arqueo_ya_anulado", "El arqueo ya estaba anulado."],
    ["movimiento_caja_ya_revertido", "Ese movimiento de caja ya tiene un reverso."],
    ["devolucion_supera_cantidad_pendiente", "La cantidad supera lo que queda pendiente de devolver en la venta."],
    ["reembolso_supera_importe_pendiente", "El reembolso supera el importe permitido por la cantidad devuelta."],
    ["venta_ya_anulada", "La venta est\xE1 anulada y ya no admite devoluciones."],
    ["venta_con_devoluciones", "La venta ya tiene devoluciones; no puede anularse por completo."],
    ["venta_no_encontrada", "No se ha encontrado la venta original."],
    ["producto_no_pertenece_a_venta", "El producto no pertenece a la venta seleccionada."],
    ["importe_venta_no_disponible", "La venta no conserva un importe verificable para autorizar el reembolso."],
    ["stock_insuficiente", "No hay stock suficiente para completar la devoluci\xF3n al proveedor."],
    ["cantidad_invalida", "La cantidad debe ser mayor que cero y respetar la unidad del producto."],
    ["unidad_indivisible", "Este producto solo admite cantidades enteras."],
    ["precision_cantidad_excedida", "La cantidad tiene m\xE1s decimales de los permitidos para este producto."],
    ["importe_negativo", "El importe no puede ser negativo."],
    ["importe_cero", "El importe debe ser mayor que cero."],
    ["importe_invalido", "Escribe un importe v\xE1lido."],
    ["importe_fuera_rango", "El importe est\xE1 fuera del rango permitido."],
    ["importe_precision_invalida", "El importe solo puede tener dos decimales."],
    ["medio_reembols
--- fin coincidencia ---

--- coincidencia 2 pos=3989015 ---
from("stock_estado").select("empresa_id,local_id,producto_id,almacen,piso,total,minimo,bajo_minimo,fraccionable,precision_cantidad,local_operable");
  let qMov = supabase.from("movimientos_stock").select("id,operation_id,tipo,empresa_id,local_id,producto_id,delta_almacen,delta_piso,delta_total,cantidad,movimiento_original_id,datos,actor_user_id,created_at").order("created_at", { ascending: false }).limit(2e3);
  const [rStock, rMov] = await Promise.all([qStock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : null;
      const st2 = exacto || activo;
      if (!st2) return prod;
      return { ...prod, stock: Number(st2.total) || 0, stockPisoVenta: Number(st2.piso) || 0, stockMinimo: Number(st2.minimo) || 0, _pm07BajoMinimo: !!st2.bajo_minimo, _pm07Servidor: true, _pm07LocalOperable: st2.local_operable !== false };
    }));
  }
  if (typeof setMovimientos === "function") {
    const server = movs.map((m4) => {
      const d2 = m4.datos && typeof m4.datos === "object" ? m4.datos : {};
      const delta = Number(m4.delta_total) || 0;
      const creado = String(m4.created_at || "");
      return {
        id: `pm07-${m4.id}`,
        operationId: m4.operation_id,
        ventaId: d2.ventaId || (m4.tipo === "VENTA" ? m4.operation_id : d2.anulaVentaId || null),
        anulaVentaId: d2.anulaVentaId || null,
        productoId: m4.producto_id,
        localId: m4.local_id,
        empresaId: m4.empresa_id,
        cantidad: delta,
        cantidadFisica: Number(m4.cantidad) || Math.abs(delta),
        tipo: m4.tipo,
        motivo: d2.motivo || m4.tipo,
        referencia: d2.referencia || "PM-07",
        costoUnitario: Number(d2.costoUnitario) || 0,
        ingresoUnitario: Number(d2.ingresoUnitario) || 0,
        ivaVentaAplicado: Number(d2.ivaVentaAplicado) || 0,
        medioPago: d2.medioPago || null,
        detallePago: d2.detallePago || null,
        fecha: creado ? creado.slice(0, 10) : todayISO(),
        hora: creado ? creado.slice(11, 16) : (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
        afectaStockTotal: delta !== 0,
        afectaStockPisoVenta: Number(m4.delta_piso) !== 0,
        movimientoOriginalId: m4.movimiento_original_id || null,
        _pm07Servidor: true
      };
    });
    setMovimientos((prev) => [...server, ...(prev || []).filter((m4) => !m4._pm07Servidor)]);
  }
  return { ok: true, stocks: stocks.length, movimientos: movs.length };
}
function redondearDineroPM08(valor) {
  const n2 = Number(valor);
  return Number.isFinite(n2) ? Math.round((n2 + Number.EPSILON) * 100) / 100 : NaN;
}
function modoSincronizadoPM08() {
  return typeof window !== "undefined" && !!window.NUBE_URL && !window.__modoPruebasLocal;
}
function horaActualPM08() {
  return (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5);
}
function clavePendientePM08(tipo, empresaId, localId, extra = "") {
  return `almacen:pm08:${tipo}:${empresaId || "sin-empresa"}:${localId || "todos"}${extra ? `:${extra}` : ""}`;
}
function leerPendientePM08(clave) {
  try {
    return JSON.parse(localStorage.getItem(clave) || "null");
  } catch {
    return null;
  }
}
function guardarPendientePM08(clave, valor) {
  try {
    localStorage.setItem(clave, JSON.stringify(valor));
    return true;
  } catch {
    return false;
  }
}
function limpiarPendientePM08(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
function prepararPendientePM08(clave, prefijo, payload) {
  const existente = leerPendientePM08(clave);
  if (existente) {
    if (JSON.stringify(existente.payload) !== JSON.stringify(payload)) {
      return { ok: false, pendiente: existente, error: "Hay una operaci\xF3n anterior pendiente en este local. Reint\xE9ntala con sus mismos datos antes de iniciar otra." };
    }
    return { ok: true, pendiente: existente, recuperada: true };
  }
  const pendiente = { operationId: `${prefijo}-${Date.now()}-${uid()}`, payload, creadoEn: (/* @__PURE__ */ new Date()).toISOString() };
  if (!guardarPendientePM08(clave, pendiente)) {
    return { ok: false, error: "No se ha podido conservar el borrador de la operaci\xF3n en este dispositivo." };
  }
  return { ok: true, pendiente, recuperada: false };
}
var operacionesPM08EnCurso = {};
function esErrorTransitorioPM08(error) {
  const codigo = String(error?.code || "").toLowerCase();
  const mensaje = String(error?.message || error || "").toLowerCase();
  return !codigo && /failed to fetch|fetch failed|network|conexi[oó]n|timeout|load failed|offline/.test(mensaje);
}
function mensajeErrorPM08(error, fallback = "No se ha podido confirmar la operaci\xF3n.") {
  const mensaje = String(error?.message || error || "");
  const mensajes = [
    ["operation_id_conflict", "El identificador de reintento ya existe con datos distintos. La operaci\xF3n no se ha repetido."],
    ["contexto_no_autorizado", "La empresa o el local no pertenecen al \xE1mbito autorizado de esta cuenta."],
    ["venta_fuera_de_contexto", "La venta pertenece a otra empresa o a otro local."],
    ["local_inactivo", "El local est\xE1 cerrado o inactivo y no admite nuevas operaciones."],
    ["periodo_caja_cerrado", "Ese d\xEDa ya tiene un arqueo activo. Anula el arqueo de forma trazable antes de modificar la caja."],
    ["arqueo_ya_existe", "Ya existe un arqueo activo para ese d\xEDa y local."],
    ["arqueo_ya_anulado", "El arqueo ya estaba anulado."],
    ["movimiento_caja_ya_revertido", "Ese movimiento de caja ya tiene un reverso."],
    ["devolucion_supera_cantidad_pendiente", "La cantidad supera lo que queda pendiente de devolver en la venta."],
    ["reembolso_supera_importe_pendiente", "El reembolso supera el importe permitido por la cantidad devuelta."],
    ["venta_ya_anulada", "La venta est\xE1 anulada y ya no admite devoluciones."],
    ["venta_con_devoluciones", "La venta ya tiene devoluciones; no puede anularse por completo."],
    ["venta_no_encontrada", "No se ha encontrado la venta original."],
    ["producto_no_pertenece_a_venta", "El producto no pertenece a la venta seleccionada."],
    ["importe_venta_no_disponible", "La venta no conserva un importe verificable para autorizar el reembolso."],
    ["stock_insuficiente", "No hay stock suficiente para completar la devoluci\xF3n al proveedor."],
    ["cantidad_invalida", "La cantidad debe ser mayor que cero y respetar la unidad del producto."],
    ["unidad_indivisible", "Este producto solo admite cantidades enteras."],
    ["precision_cantidad_excedida", "La cantidad tiene m\xE1s decimales de los permitidos para este producto."],
    ["importe_negativo", "El importe no puede ser negativo."],
    ["importe_cero", "El importe debe ser mayor que cero."],
    ["importe_invalido", "Escribe un importe v\xE1lido."],
    ["importe_fuera_rango", "El importe est\xE1 fuera del rango permitido."],
    ["importe_precision_invalida", "El importe solo puede tener dos decimales."],
    ["medio_reembolso_invalido", "Elige un medi
--- fin coincidencia ---

--- coincidencia 3 pos=4056175 ---
nidades = consumo[p22.id] || 0;
      const costo = Number(p22.costo) || 0;
      const valor = baseConsumo ? unidades * costo : (Number(p22.stock) || 0) * costo;
      return { id: p22.id, valor, unidades, rotacion: (Number(p22.stock) || 0) > 0 ? unidades / (Number(p22.stock) || 1) : null };
    });
    const total = conValor.reduce((a22, x3) => a22 + x3.valor, 0);
    const mapa = {};
    const detalle = {};
    if (total <= 0) {
      clasificables.forEach((p22) => {
        mapa[p22.id] = "C";
        detalle[p22.id] = { valor: 0, unidades: 0, rotacion: null };
      });
      return { mapa, detalle, baseConsumo };
    }
    const ordenados = [...conValor].sort((a22, b2) => b2.valor - a22.valor);
    let acumulado = 0;
    ordenados.forEach((x3) => {
      acumulado += x3.valor;
      const pct = acumulado / total * 100;
      mapa[x3.id] = pct <= 80 ? "A" : pct <= 95 ? "B" : "C";
      detalle[x3.id] = { valor: x3.valor, unidades: x3.unidades, rotacion: x3.rotacion };
    });
    return { mapa, detalle, baseConsumo };
  }, [productosDelLocalActivo, movimientosDelLocalActivo]);
  const clasificacionABCDelLocalActivo = analisisABCDelLocalActivo.mapa;
  const pedidosPendientes = (0, import_react4.useMemo)(() => pedidos2.filter((p22) => p22.estado !== "Recibido"), [pedidos2]);
  const margenPromedio = (0, import_react4.useMemo)(() => {
    const conPrecio = productos.filter((p22) => Number(p22.precioVenta) > 0);
    if (!conPrecio.length) return 0;
    const total = conPrecio.reduce((acc, p22) => acc + (margenDe(p22) || 0), 0);
    return total / conPrecio.length;
  }, [productos]);
  const margenPorProducto = (0, import_react4.useMemo)(() => {
    const hace90 = /* @__PURE__ */ new Date();
    hace90.setDate(hace90.getDate() - 90);
    return productos.filter((p22) => Number(p22.precioVenta) > 0).map((p22) => {
      const ventas = movimientos.filter(
        (m22) => m22.productoId === p22.id && esVenta(m22) && new Date(m22.fecha) >= hace90
      );
      const unidadesVendidas = ventas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0), 0);
      const ingresos = ventas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0), 0);
      const costes = ventas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.costoUnitario) || 0), 0);
      return { ...p22, unidadesVendidas, ingresos, beneficio: ingresos - costes, margenPct: margenDe(p22) };
    }).filter((p22) => p22.unidadesVendidas > 0).sort((a22, b2) => b2.beneficio - a22.beneficio);
  }, [productos, movimientos]);
  const rotacionPorProducto = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    return productos.map((p22) => {
      const salidas = movimientos.filter((m22) => m22.productoId === p22.id && esSalida(m22));
      const cantSalidas = salidas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0), 0);
      if (salidas.length === 0) {
        return { ...p22, cantSalidas: 0, consumoDiario: null, dias: null, rotacion: null };
      }
      const primera = salidas.reduce((min, m22) => new Date(m22.fecha) < min ? new Date(m22.fecha) : min, new Date(salidas[0].fecha));
      const diasTranscurridos = Math.max(1, Math.round((hoy - primera) / 864e5) + 1);
      const consumoDiario = cantSalidas / diasTranscurridos;
      const dias = consumoDiario > 0 ? (Number(p22.stock) || 0) / consumoDiario : null;
      const rotacion = dias ? 365 / dias : null;
      return { ...p22, cantSalidas, consumoDiario, dias, rotacion, diasConDatos: diasTranscurridos };
    });
  }, [productos, movimientos]);
  const patronesDesviacionConteo = (0, import_react4.useMemo)(() => {
    const porProducto = {};
    movimientos.filter((m22) => {
      if (!m22.productoId) return false;
      if (esMovimientoNuevo(m22)) return m22.tipo === "INVENTARIO" && m22.origen !== "reconciliacionStock";
      return m22.tipo === "ajuste";
    }).forEach((m22) => {
      if (!porProducto[m22.productoId]) porProducto[m22.productoId] = [];
      porProducto[m22.productoId].push(m22);
    });
    const resultado = [];
    Object.entries(porProducto).forEach(([productoId, ajustes]) => {
      if (ajustes.length < 3) return;
      const negativos = ajustes.filter((a22) => Number(a22.cantidad) < 0).length;
      const positivos = ajustes.filter((a22) => Number(a22.cantidad) > 0).length;
      const total = ajustes.length;
      const proporcionMayor = Math.max(negativos, positivos) / total;
      if (proporcionMayor < 0.75) return;
      const prod = productos.find((p22) => p22.id === productoId);
      const acumulado = ajustes.reduce((a22, m22) => a22 + Number(m22.cantidad), 0);
      const ultimaFecha = ajustes.reduce((max, m22) => m22.fecha > max ? m22.fecha : max, ajustes[0].fecha);
      resultado.push({
        productoId,
        nombre: prod ? prod.nombre : "Producto eliminado",
        unidad: prod ? prod.unidad : "",
        direccion: negativos > positivos ? "falta" : "sobra",
        numAjustes: total,
        acumulado,
        ultimaFecha
      });
    });
    return resultado.sort((a22, b2) => Math.abs(b2.acumulado) - Math.abs(a22.acumulado));
  }, [movimientos, productos]);
  function diasHastaProximaEntrega(leadTime, diasReparto) {
    if (!diasReparto || diasReparto.length === 0) return leadTime;
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    for (let n2 = leadTime; n2 <= leadTime + 14; n2++) {
      const fecha = new Date(hoy);
      fecha.setDate(fecha.getDate() + n2);
      if (diasReparto.includes(fecha.getDay())) return n2;
    }
    return leadTime;
  }
  const productosEnPedidoPendiente = (0, import_react4.useMemo)(() => {
    const ids = /* @__PURE__ */ new Set();
    pedidosPendientes.forEach((pe2) => (pe2.items || []).forEach((it2) => ids.add(it2.productoId)));
    return ids;
  }, [pedidosPendientes]);
  const DIAS_SEGURIDAD_PEDIDO = 7;
  const sugerenciasPedido = (0, import_react4.useMemo)(() => {
    return rotacionPorProducto.filter((p22) => !esNoMercancia(p22) && p22.tipo !== "elaborado" && p22.proveedorId && p22.dias !== null).map((p22) => {
      const prov = proveedores.find((pv) => pv.id === p22.proveedorId);
      const leadTime = prov ? Number(prov.leadTime) || 0 : 0;
      const diasHastaEntrega = diasHastaProximaEntrega(leadTime, prov ? prov.diasReparto : []);
      const cantidadCubrir = p22.consumoDiario * (diasHastaEntrega + DIAS_SEGURIDAD_PEDIDO);
      const cantidadSugerida = Math.max(0, Math.ceil(cantidadCubrir - (Number(p22.stock) || 0)));
      const cajasSugeridas = p22.udsPorCaja > 1 ? Math.ceil(cantidadSugerida / p22.udsPorCaja) : null;
      return { ...p22, proveedorNombre: prov ? prov.nombre : "\u2014", leadTime, diasHastaEntrega, margen: p22.dias - diasHastaEntrega, cantidadSugerida, cajasSugeridas };
    }).filter((p22) => p22.leadTime > 0 && p22.margen <= 1 && !productosEnPedidoPendiente.has(p22.id)).sort((a22, b2) => a22.margen - b2.margen);
  }, [rotacionPorProducto, proveedores, productosEnPedidoPendiente]);
  const sugerenciasPedidoDelLocalActivo = (0, import_react4.useMemo)(() => {
    if (!localActivoId) return sugerenciasPedido;
    return sugerenciasPedido.filter((p22) => p22.localId === localActivoId);
  }, [sugerenciasPedido, localActivoId]);
  const lotesCaducidad = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date(
--- fin coincidencia ---

--- coincidencia 4 pos=4073063 ---
: empleados.filter((e2) => e2.localId === localActivoId), [empleados, localActivoId]);
  const idsEmpleadosDelLocalActivo = (0, import_react4.useMemo)(() => new Set(empleadosDelLocalActivo.map((e2) => e2.id)), [empleadosDelLocalActivo]);
  const fichajesDelLocalActivo = (0, import_react4.useMemo)(() => !localActivoId ? fichajes : fichajes.filter((f22) => f22.localId === localActivoId), [fichajes, localActivoId]);
  const turnosDelLocalActivo = (0, import_react4.useMemo)(() => !localActivoId ? turnos : turnos.filter((t22) => t22.localId === localActivoId), [turnos, localActivoId]);
  const nominasDelLocalActivo = (0, import_react4.useMemo)(() => !localActivoId ? nominas : nominas.filter((n2) => n2.localId === localActivoId), [nominas, localActivoId]);
  const documentosPersonalProntoDelLocalActivo = (0, import_react4.useMemo)(() => documentosPersonalPronto.filter((d2) => idsEmpleadosDelLocalActivo.has(d2.empleadoId)), [documentosPersonalPronto, idsEmpleadosDelLocalActivo]);
  const encargosPendientes = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    return encargos.filter((e2) => e2.estado !== "Entregado" && e2.estado !== "Cancelado").map((e2) => {
      const f22 = new Date(e2.fechaEntrega);
      const dias = isNaN(f22) ? null : Math.round((f22 - hoy) / 864e5);
      const total = (e2.lineas || []).reduce((a22, ln2) => a22 + (Number(ln2.cantidad) || 0) * (Number(ln2.precioUnitario) || 0), 0);
      return { ...e2, dias, total, cliente: clientes.find((c22) => c22.id === e2.clienteId) };
    }).sort((a22, b2) => (a22.dias ?? 9999) - (b2.dias ?? 9999));
  }, [encargos, clientes]);
  const encargosUrgentes = (0, import_react4.useMemo)(() => encargosPendientes.filter((e2) => e2.dias !== null && e2.dias <= 2), [encargosPendientes]);
  const analisisClientes = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    return clientes.map((c22) => {
      const susVentas = movimientos.filter((m22) => m22.clienteId === c22.id && esVenta(m22));
      const facturado = susVentas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0), 0);
      const fechas = susVentas.map((m22) => m22.fecha).sort();
      const ultima = fechas[fechas.length - 1] || null;
      const diasSinComprar = ultima ? Math.round((hoy - new Date(ultima)) / 864e5) : null;
      const nEncargos = encargos.filter((e2) => e2.clienteId === c22.id && e2.estado === "Entregado").length;
      return { ...c22, facturado, ultima, diasSinComprar, nEncargos };
    }).sort((a22, b2) => b2.facturado - a22.facturado);
  }, [clientes, movimientos, encargos]);
  const clientesDormidos = (0, import_react4.useMemo)(
    () => analisisClientes.filter((c22) => c22.nEncargos > 0 && c22.diasSinComprar !== null && c22.diasSinComprar > 60),
    [analisisClientes]
  );
  const ventaCruzada = (0, import_react4.useMemo)(() => {
    const entregados = encargos.filter((e2) => e2.estado === "Entregado");
    const pares = {};
    entregados.forEach((e2) => {
      const ids = [...new Set((e2.lineas || []).map((l22) => l22.productoId).filter(Boolean))];
      for (let i33 = 0; i33 < ids.length; i33++) {
        for (let j2 = i33 + 1; j2 < ids.length; j2++) {
          const key = [ids[i33], ids[j2]].sort().join("|");
          pares[key] = (pares[key] || 0) + 1;
        }
      }
    });
    return {
      base: entregados.length,
      filas: Object.entries(pares).map(([key, veces]) => {
        const [a22, b2] = key.split("|");
        return { a: productos.find((p22) => p22.id === a22), b: productos.find((p22) => p22.id === b2), veces };
      }).filter((x3) => x3.a && x3.b).sort((x3, y22) => y22.veces - x3.veces).slice(0, 8)
    };
  }, [encargos, productos]);
  const appccPendientesHoy = alertasAppcc.pendientes;
  const fichajesAbiertos = (0, import_react4.useMemo)(() => {
    const hoy = todayISO();
    const porEmpleadoFecha = {};
    fichajes.forEach((f22) => {
      const key = `${f22.empleadoId}-${f22.fecha}`;
      if (!porEmpleadoFecha[key]) porEmpleadoFecha[key] = [];
      porEmpleadoFecha[key].push(f22);
    });
    const abiertos = [];
    Object.entries(porEmpleadoFecha).forEach(([key, lista]) => {
      const fecha = lista[0].fecha;
      if (fecha === hoy) return;
      const entradas = lista.filter((f22) => f22.tipo === "entrada").length;
      const salidas = lista.filter((f22) => f22.tipo === "salida").length;
      if (entradas > salidas) {
        const empleadoId = lista[0].empleadoId;
        const emp = empleados.find((e2) => e2.id === empleadoId);
        abiertos.push({ empleadoId, fecha, nombre: emp ? emp.nombre : "\u2014" });
      }
    });
    return abiertos;
  }, [fichajes, empleados]);
  const fichajesAbiertosDelLocalActivo = (0, import_react4.useMemo)(() => fichajesAbiertos.filter((f22) => idsEmpleadosDelLocalActivo.has(f22.empleadoId)), [fichajesAbiertos, idsEmpleadosDelLocalActivo]);
  const facturasPorPagar = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    function calcularVencimiento(fechaFactura, diasPago) {
      if (diasPago === null || !fechaFactura) return { vencimiento: null, dias: null };
      const v22 = new Date(fechaFactura);
      v22.setDate(v22.getDate() + diasPago);
      return { vencimiento: aISO(v22), dias: Math.round((v22 - hoy) / 864e5) };
    }
    const deAlbaranes = albaranes.filter((a22) => a22.estado === "confirmado" && a22.esFactura !== false).map((a22) => {
      const prov = a22.proveedorSnapshot || proveedorPorId(a22.proveedorId);
      const total = calcularTotalesFacturaAlbaran(a22).total;
      const fechaFactura = a22.fechaFactura || a22.fecha;
      const diasPago = prov && prov.diasPago != null ? Number(prov.diasPago) || null : null;
      const saldo = calcularSaldoFacturaPM06(pagosFacturas, a22.id, "albaran", total, a22.empresaId, a22.localId, a22.pagada);
      const { vencimiento, dias } = calcularVencimiento(fechaFactura, diasPago);
      return {
        id: a22.id,
        origen: "albaran",
        concepto: `Mercanc\xEDa \xB7 ${prov ? prov.nombre : "\u2014"}`,
        proveedor: prov,
        numeroFactura: a22.numeroFactura || a22.numero || "s/n",
        fechaFactura,
        vencimiento,
        dias,
        total,
        empresaId: a22.empresaId || null,
        localId: a22.localId || null,
        pagado: saldo.pagado,
        pendiente: saldo.pendiente,
        pagada: saldo.pagada
      };
    });
    const deDirectas = facturasDirectas.filter((f22) => f22.estado !== "ANULADA").map((f22) => {
      const prov = f22.proveedorSnapshot || proveedorPorId(f22.proveedorId);
      const diasPago = prov && prov.diasPago != null ? Number(prov.diasPago) || null : null;
      const saldo = calcularSaldoFacturaPM06(pagosFacturas, f22.id, "directa", f22.importeTotal, f22.empresaId, f22.localId, f22.pagada);
      const auto = calcularVencimiento(f22.fecha, diasPago);
      const vencimiento = f22.vencimiento || auto.vencimiento;
      const dias = vencimiento ? Math.round((new Date(vencimiento) - hoy) / 864e5) : null;
      return {
        id: f22.id,
        origen: "directa",
        concepto: f22.concepto || "Factura directa",
        proveedor: prov,
        numeroFactura: f22.numeroFactura || "s/n",
        fechaFactura: f22.fecha,

--- fin coincidencia ---

--- coincidencia 5 pos=4080712 ---
 pagada: saldo.pagada
      };
    });
    return [...deAlbaranes, ...deDirectas].sort((x3, y22) => {
      if (x3.pagada !== y22.pagada) return x3.pagada ? 1 : -1;
      if (x3.dias === null) return 1;
      if (y22.dias === null) return -1;
      return x3.dias - y22.dias;
    });
  }, [albaranes, proveedores, facturasDirectas, pagosFacturas]);
  const facturasPorPagarDelLocalActivo = (0, import_react4.useMemo)(() => {
    if (!localActivoId) return facturasPorPagar;
    const idsAlbaranes = new Set(albaranesDelLocalActivo.map((a22) => a22.id));
    const idsDirectas = new Set(facturasDirectasDelLocalActivo.map((f22) => f22.id));
    return facturasPorPagar.filter((f22) => f22.origen === "albaran" ? idsAlbaranes.has(f22.id) : idsDirectas.has(f22.id));
  }, [facturasPorPagar, albaranesDelLocalActivo, facturasDirectasDelLocalActivo, localActivoId]);
  const pendientesPagoDelLocalActivo = (0, import_react4.useMemo)(() => facturasPorPagarDelLocalActivo.filter((f22) => !f22.pagada), [facturasPorPagarDelLocalActivo]);
  const totalPendientePagoDelLocalActivo = (0, import_react4.useMemo)(() => pendientesPagoDelLocalActivo.reduce((a22, f22) => a22 + (f22.pendiente ?? f22.total), 0), [pendientesPagoDelLocalActivo]);
  const vencenProntoDelLocalActivo = (0, import_react4.useMemo)(() => pendientesPagoDelLocalActivo.filter((f22) => f22.dias !== null && f22.dias <= 7), [pendientesPagoDelLocalActivo]);
  const pendientesPago = (0, import_react4.useMemo)(() => facturasPorPagar.filter((f22) => !f22.pagada), [facturasPorPagar]);
  const vencenPronto = (0, import_react4.useMemo)(() => pendientesPago.filter((f22) => f22.dias !== null && f22.dias <= 7), [pendientesPago]);
  const totalPendientePago = (0, import_react4.useMemo)(() => pendientesPago.reduce((a22, f22) => a22 + (f22.pendiente ?? f22.total), 0), [pendientesPago]);
  const promedioDiarioVentas = (0, import_react4.useMemo)(() => {
    const hace30 = /* @__PURE__ */ new Date();
    hace30.setDate(hace30.getDate() - 30);
    const ventas = movimientos.filter((m22) => esVenta(m22) && new Date(m22.fecha) >= hace30);
    const total = ventas.reduce(
      (a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0) * (1 + (Number(m22.ivaVentaAplicado) || 0) / 100),
      0
    );
    return total / 30;
  }, [movimientos]);
  const proyeccionTesoreria = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    const dias = [];
    for (let i33 = 0; i33 < 30; i33++) {
      const d2 = new Date(hoy);
      d2.setDate(d2.getDate() + i33);
      const fechaISO = aISO(d2);
      const pagosDia = pendientesPago.filter((f22) => f22.vencimiento === fechaISO).reduce((a22, f22) => a22 + (f22.pendiente ?? f22.total), 0);
      const encargosDia = encargos.filter((e2) => e2.estado !== "Entregado" && e2.estado !== "Cancelado" && e2.fechaEntrega === fechaISO).reduce((a22, e2) => {
        const total = (e2.lineas || []).reduce((x3, l22) => x3 + (Number(l22.cantidad) || 0) * (Number(l22.precioUnitario) || 0), 0);
        return a22 + Math.max(0, total - (Number(e2.se\u00F1al) || 0));
      }, 0);
      const ingresosDia = promedioDiarioVentas + encargosDia;
      dias.push({ fecha: fechaISO, ingresosDia, pagosDia, netoDia: ingresosDia - pagosDia });
    }
    return dias;
  }, [pendientesPago, encargos, promedioDiarioVentas]);
  const ingresosPorMes = (0, import_react4.useMemo)(() => {
    const mapa = {};
    movimientos.forEach((m22) => {
      if (!esVenta(m22)) return;
      const key = m22.fecha.slice(0, 7);
      const importe = Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0);
      mapa[key] = (mapa[key] || 0) + importe;
    });
    return mapa;
  }, [movimientos]);
  const gastoPorProveedor = (0, import_react4.useMemo)(() => {
    const map = {};
    pedidos2.forEach((pe2) => {
      const total = pe2.items.reduce((a22, it2) => a22 + it2.cantidad * it2.costoUnitario, 0);
      map[pe2.proveedorId] = (map[pe2.proveedorId] || 0) + total;
    });
    return Object.entries(map).map(([proveedorId, total]) => ({ proveedor: proveedorPorId(proveedorId), total })).sort((a22, b2) => b2.total - a22.total);
  }, [pedidos2, proveedores]);
  const productosSinMovimiento = (0, import_react4.useMemo)(
    () => productos.filter((p22) => !esUtillaje(p22) && !movimientos.some((m22) => m22.productoId === p22.id && esSalida(m22))),
    [productos, movimientos]
  );
  const rotacionPorProductoDelLocalActivo = (0, import_react4.useMemo)(() => rotacionPorProducto.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [rotacionPorProducto, idsProductosDelLocalActivo]);
  const margenPorProductoDelLocalActivo = (0, import_react4.useMemo)(() => margenPorProducto.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [margenPorProducto, idsProductosDelLocalActivo]);
  const productosSinMovimientoDelLocalActivo = (0, import_react4.useMemo)(() => productosSinMovimiento.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [productosSinMovimiento, idsProductosDelLocalActivo]);
  const patronesDesviacionConteoDelLocalActivo = (0, import_react4.useMemo)(() => patronesDesviacionConteo.filter((x3) => idsProductosDelLocalActivo.has(x3.productoId)), [patronesDesviacionConteo, idsProductosDelLocalActivo]);
  const gastoPorProveedorDelLocalActivo = (0, import_react4.useMemo)(() => {
    const map = {};
    pedidosDelLocalActivo.forEach((pe2) => {
      const total = (pe2.items || []).reduce((a22, it2) => a22 + (Number(it2.cantidad) || 0) * (Number(it2.costoUnitario) || 0), 0);
      map[pe2.proveedorId] = (map[pe2.proveedorId] || 0) + total;
    });
    return Object.entries(map).map(([proveedorId, total]) => ({ proveedor: proveedorPorId(proveedorId), total })).sort((a22, b2) => b2.total - a22.total);
  }, [pedidosDelLocalActivo, proveedores]);
  if (!ready) {
    return /* @__PURE__ */ import_react4.default.createElement("div", { style: { background: C2.bg, color: C2.ink }, className: "w-full h-full min-h-[600px] flex items-center justify-center font-sans" }, /* @__PURE__ */ import_react4.default.createElement(LoaderCircle, { className: "animate-spin", size: 22 }), /* @__PURE__ */ import_react4.default.createElement("span", { className: "ml-2 text-sm" }, "Cargando almac\xE9n\u2026"));
  }
  const localPorProductoInforme = new Map(productos.map((p22) => [p22.id, p22.localId || null]));
  const idsLocalesEmpresaInforme = new Set(locales.filter((l22) => l22 && l22.empresaId === empresaDelLocalActivo?.id).map((l22) => l22.id));
  const localEsDeEmpresaInforme = (id) => !!id && idsLocalesEmpresaInforme.has(id);
  const inferirLocalLineasInforme = (lineas) => {
    const ids = [...new Set((lineas || []).map((ln2) => localPorProductoInforme.get(ln2 && ln2.productoId)).filter(Boolean))];
    return ids.length === 1 ? ids[0] : null;
  };
  const productosInforme = localInformeId ? productos.filter((p22) => p22.localId === localInformeId) : productos.filter((p22) => localEsDeEmpresaInforme(p22.localId));
  const movimientosInforme = localInformeId ? movimientos.filter((m22) => (m22.localId || localPorProductoInforme.get(m22.productoId) || null) === localInformeId) : movimientos.filter((m22) => localEsDeEmpresaInforme(m22.localId || localPorProductoInforme.get(m22.productoId) || null));
  const albaranesInforme = localInformeId ? albaranes.filter((a22) => (a22.localId || infe
--- fin coincidencia ---

--- coincidencia 6 pos=4082170 ---
seMemo)(() => facturasPorPagar.filter((f22) => !f22.pagada), [facturasPorPagar]);
  const vencenPronto = (0, import_react4.useMemo)(() => pendientesPago.filter((f22) => f22.dias !== null && f22.dias <= 7), [pendientesPago]);
  const totalPendientePago = (0, import_react4.useMemo)(() => pendientesPago.reduce((a22, f22) => a22 + (f22.pendiente ?? f22.total), 0), [pendientesPago]);
  const promedioDiarioVentas = (0, import_react4.useMemo)(() => {
    const hace30 = /* @__PURE__ */ new Date();
    hace30.setDate(hace30.getDate() - 30);
    const ventas = movimientos.filter((m22) => esVenta(m22) && new Date(m22.fecha) >= hace30);
    const total = ventas.reduce(
      (a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0) * (1 + (Number(m22.ivaVentaAplicado) || 0) / 100),
      0
    );
    return total / 30;
  }, [movimientos]);
  const proyeccionTesoreria = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    const dias = [];
    for (let i33 = 0; i33 < 30; i33++) {
      const d2 = new Date(hoy);
      d2.setDate(d2.getDate() + i33);
      const fechaISO = aISO(d2);
      const pagosDia = pendientesPago.filter((f22) => f22.vencimiento === fechaISO).reduce((a22, f22) => a22 + (f22.pendiente ?? f22.total), 0);
      const encargosDia = encargos.filter((e2) => e2.estado !== "Entregado" && e2.estado !== "Cancelado" && e2.fechaEntrega === fechaISO).reduce((a22, e2) => {
        const total = (e2.lineas || []).reduce((x3, l22) => x3 + (Number(l22.cantidad) || 0) * (Number(l22.precioUnitario) || 0), 0);
        return a22 + Math.max(0, total - (Number(e2.se\u00F1al) || 0));
      }, 0);
      const ingresosDia = promedioDiarioVentas + encargosDia;
      dias.push({ fecha: fechaISO, ingresosDia, pagosDia, netoDia: ingresosDia - pagosDia });
    }
    return dias;
  }, [pendientesPago, encargos, promedioDiarioVentas]);
  const ingresosPorMes = (0, import_react4.useMemo)(() => {
    const mapa = {};
    movimientos.forEach((m22) => {
      if (!esVenta(m22)) return;
      const key = m22.fecha.slice(0, 7);
      const importe = Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0);
      mapa[key] = (mapa[key] || 0) + importe;
    });
    return mapa;
  }, [movimientos]);
  const gastoPorProveedor = (0, import_react4.useMemo)(() => {
    const map = {};
    pedidos2.forEach((pe2) => {
      const total = pe2.items.reduce((a22, it2) => a22 + it2.cantidad * it2.costoUnitario, 0);
      map[pe2.proveedorId] = (map[pe2.proveedorId] || 0) + total;
    });
    return Object.entries(map).map(([proveedorId, total]) => ({ proveedor: proveedorPorId(proveedorId), total })).sort((a22, b2) => b2.total - a22.total);
  }, [pedidos2, proveedores]);
  const productosSinMovimiento = (0, import_react4.useMemo)(
    () => productos.filter((p22) => !esUtillaje(p22) && !movimientos.some((m22) => m22.productoId === p22.id && esSalida(m22))),
    [productos, movimientos]
  );
  const rotacionPorProductoDelLocalActivo = (0, import_react4.useMemo)(() => rotacionPorProducto.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [rotacionPorProducto, idsProductosDelLocalActivo]);
  const margenPorProductoDelLocalActivo = (0, import_react4.useMemo)(() => margenPorProducto.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [margenPorProducto, idsProductosDelLocalActivo]);
  const productosSinMovimientoDelLocalActivo = (0, import_react4.useMemo)(() => productosSinMovimiento.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [productosSinMovimiento, idsProductosDelLocalActivo]);
  const patronesDesviacionConteoDelLocalActivo = (0, import_react4.useMemo)(() => patronesDesviacionConteo.filter((x3) => idsProductosDelLocalActivo.has(x3.productoId)), [patronesDesviacionConteo, idsProductosDelLocalActivo]);
  const gastoPorProveedorDelLocalActivo = (0, import_react4.useMemo)(() => {
    const map = {};
    pedidosDelLocalActivo.forEach((pe2) => {
      const total = (pe2.items || []).reduce((a22, it2) => a22 + (Number(it2.cantidad) || 0) * (Number(it2.costoUnitario) || 0), 0);
      map[pe2.proveedorId] = (map[pe2.proveedorId] || 0) + total;
    });
    return Object.entries(map).map(([proveedorId, total]) => ({ proveedor: proveedorPorId(proveedorId), total })).sort((a22, b2) => b2.total - a22.total);
  }, [pedidosDelLocalActivo, proveedores]);
  if (!ready) {
    return /* @__PURE__ */ import_react4.default.createElement("div", { style: { background: C2.bg, color: C2.ink }, className: "w-full h-full min-h-[600px] flex items-center justify-center font-sans" }, /* @__PURE__ */ import_react4.default.createElement(LoaderCircle, { className: "animate-spin", size: 22 }), /* @__PURE__ */ import_react4.default.createElement("span", { className: "ml-2 text-sm" }, "Cargando almac\xE9n\u2026"));
  }
  const localPorProductoInforme = new Map(productos.map((p22) => [p22.id, p22.localId || null]));
  const idsLocalesEmpresaInforme = new Set(locales.filter((l22) => l22 && l22.empresaId === empresaDelLocalActivo?.id).map((l22) => l22.id));
  const localEsDeEmpresaInforme = (id) => !!id && idsLocalesEmpresaInforme.has(id);
  const inferirLocalLineasInforme = (lineas) => {
    const ids = [...new Set((lineas || []).map((ln2) => localPorProductoInforme.get(ln2 && ln2.productoId)).filter(Boolean))];
    return ids.length === 1 ? ids[0] : null;
  };
  const productosInforme = localInformeId ? productos.filter((p22) => p22.localId === localInformeId) : productos.filter((p22) => localEsDeEmpresaInforme(p22.localId));
  const movimientosInforme = localInformeId ? movimientos.filter((m22) => (m22.localId || localPorProductoInforme.get(m22.productoId) || null) === localInformeId) : movimientos.filter((m22) => localEsDeEmpresaInforme(m22.localId || localPorProductoInforme.get(m22.productoId) || null));
  const albaranesInforme = localInformeId ? albaranes.filter((a22) => (a22.localId || inferirLocalLineasInforme(a22.lineas)) === localInformeId) : albaranes.filter((a22) => localEsDeEmpresaInforme(a22.localId || inferirLocalLineasInforme(a22.lineas)));
  const facturasDirectasInforme = localInformeId ? facturasDirectas.filter((f22) => f22.localId === localInformeId) : facturasDirectas.filter((f22) => localEsDeEmpresaInforme(f22.localId));
  const gastosGeneralesInforme = (localInformeId ? gastosGenerales.filter((g2) => g2.localId === localInformeId) : gastosGenerales.filter((g2) => localEsDeEmpresaInforme(g2.localId))).filter((g2) => g2.estado !== "ANULADO");
  const empleadosInforme = localInformeId ? empleados.filter((e2) => e2.localId === localInformeId) : empleados.filter((e2) => localEsDeEmpresaInforme(e2.localId));
  const idsEmpleadosInforme = new Set(empleadosInforme.map((e2) => e2.id));
  const fichajesAbiertosInforme = fichajesAbiertos.filter((f22) => idsEmpleadosInforme.has(f22.empleadoId));
  const documentosPersonalProntoInforme = documentosPersonalPronto.filter((d2) => idsEmpleadosInforme.has(d2.empleadoId));
  const pedidosInforme = localInformeId ? pedidos2.filter((pe2) => (pe2.localId || inferirLocalLineasInforme(pe2.items)) === localInformeId) : pedidos2.filter((pe2) => localEsDeEmpresaInforme(pe2.localId || inferirLocalLineasInforme(pe2.items)));
  const encargosInforme = localInformeId ? encargos.filter((e2) => (e2.localId || inferirLocalLineasInforme(e2.lineas)) === localInformeId) : encargos.filter((e
--- fin coincidencia ---

--- coincidencia 7 pos=4165716 ---
ror: "Elige la venta original." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!Number.isFinite(reembolsoNum) || reembolsoNum < 0) return { ok: false, error: "El reembolso no puede ser negativo." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    if (reembolsoNum === 0 && medio !== "SIN_REEMBOLSO") return { ok: false, error: "Para 0 \u20AC elige \u201CSin reembolso / cambio\u201D." };
    if (reembolsoNum > 0 && !["EFECTIVO", "TARJETA", "TRANSFERENCIA", "OTRO"].includes(medio)) return { ok: false, error: "Elige el medio de reintegro." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const lineaVenta = (movimientos || []).find((m22) => esVenta(m22) && (m22.operationId || m22.ventaId || m22.id) === ventaId && m22.productoId === productoId && (!m22.localId || m22.localId === localActivoId));
    if (!lineaVenta) return { ok: false, error: "El producto no pertenece a la venta seleccionada." };
    if ((movimientos || []).some((m22) => m22.tipo === "REVERSO" && (m22.anulaVentaId === ventaId || m22.documentoOrigenId === ventaId))) return { ok: false, error: "La venta est\xE1 anulada y ya no admite devoluciones." };
    const cantidadOriginal = Math.abs(Number(lineaVenta.cantidadFisica ?? lineaVenta.cantidad) || 0);
    const anteriores = (devoluciones || []).filter((d2) => d2.tipo === "cliente" && d2.ventaId === ventaId && d2.productoId === productoId);
    const cantidadDevuelta = anteriores.reduce((acc, d2) => acc + (Number(d2.cantidad) || 0), 0);
    const reembolsado = anteriores.reduce((acc, d2) => acc + (Number(d2.reembolso) || 0), 0);
    if (cantidadDevuelta + cant > cantidadOriginal + 1e-6) return { ok: false, error: "La cantidad supera lo que queda pendiente de devolver en la venta." };
    const brutoUnitario = Math.abs(Number(lineaVenta.ingresoUnitario) || 0) * (1 + (Number(lineaVenta.ivaVentaAplicado) || 0) / 100);
    if (reembolsoNum > 0 && !(brutoUnitario > 0)) return { ok: false, error: "La venta no conserva un importe verificable para autorizar el reembolso." };
    if (reembolsado + reembolsoNum > redondearDineroPM08((cantidadDevuelta + cant) * brutoUnitario) + 1e-6) return { ok: false, error: "El reembolso supera el importe permitido por la cantidad devuelta." };
    const clave = clavePendientePM08("devolucion-cliente", empresaId, localActivoId);
    const fechaReal = fecha || leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = {
      ventaId,
      empresaId,
      localId: localActivoId,
      productoId,
      productoNombre: producto.nombre,
      cantidad: cant,
      reembolso: reembolsoNum,
      medioReembolso: medio,
      motivo: motivoLimpio,
      fecha: fechaReal
    };
    const preparado = prepararPendientePM08(clave, "pm08-dev-cliente", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. No se ha modificado ni stock ni caja; el borrador queda listo para reintentar." };
        try {
          const r3 = await Promise.race([
            window.__nubeCliente.rpc("registrar_devolucion_venta", {
              p_operation_id: preparado.pendiente.operationId,
              p_venta_operation_id: ventaId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_producto_id: productoId,
              p_cantidad: cant,
              p_reembolso: reembolsoNum,
              p_medio_reembolso: medio,
              p_motivo: motivoLimpio,
              p_fecha: fechaReal,
              p_datos: { productoNombre: producto.nombre, origen: "L&A Suite PM-08" }
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r3.error) {
            if (esErrorTransitorioPM08(r3.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. Conservamos el mismo identificador para evitar duplicar stock o reembolso." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r3.error, "No se ha podido registrar la devoluci\xF3n.") };
          }
          const registro2 = normalizarDevolucionPM08(r3.data?.devolucion, "cliente");
          const movimientoCaja2 = normalizarMovimientoCajaPM08(r3.data?.movimientoCaja);
          if (!registro2) throw new Error("respuesta_devolucion_incompleta");
          setDevoluciones((s22) => [registro2, ...(s22 || []).filter((d2) => d2.operationId !== registro2.operationId && d2.id !== registro2.id)]);
          if (movimientoCaja2) setMovimientosCaja((s22) => [movimientoCaja2, ...(s22 || []).filter((m22) => m22.operationId !== movimientoCaja2.operationId)]);
          limpiarPendientePM08(clave);
          try {
            await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
            await sincronizarCajaPm08({ setArqueos, setMovimientosCaja, setDevoluciones });
          } catch {
          }
          if (!r3.data?.replayed) registrarAuditoria("Devoluci\xF3n de cliente", `${producto.nombre} \xD7${cant}${reembolsoNum > 0 ? ` \xB7 reembolso \u20AC${fmt(reembolsoNum)} (${medio})` : " \xB7 sin reembolso"}`);
          return { ok: true, registro: registro2, movimientoCaja: movimientoCaja2, replayed: !!r3.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. No repitas con otros datos: usa el mismo borrador para evitar duplicar stock o reembolso." };
        }
      }
      const r2 = aplicarMovimientoStock({
        productoId,
        cantidad: cant,
        tipo: "DEVOLUCION_CLIENTE",
        operationId: preparado.pendiente.operationId,
        movimientoId: `pm08-local-stock-${uid()}`,
        origen: "devolucionCliente",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        motivo: `Devoluci\xF3n de cliente \xB7 ${motivoLimpio}`,
        permitirDeficit: false,
        camposExtra: { reembolso: reembolsoNum, medioReembolso: medio, ingresoUnitario: -(Number(lineaVenta.ingresoUnitario) || 0), ivaVentaAplicado: Number(lineaVenta.ivaVentaAplicado) || 0 }
      });
      if (!r2.ok) {
        limpiarPendientePM08(clave);
        return { ok: false, error: r2.error || "No se ha podido aplicar al stock." };
      }
      const registro = normalizarDevolucionPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        tipo: "cliente",
        fecha: fechaReal,
        productoId,
        productoNombre: producto.nombre,
        cantidad: cant,
        motivo: motivoLimpio,
        reembolso: reembolsoNum,
        medio
--- fin coincidencia ---

--- coincidencia 8 pos=4170281 ---
3n. Conservamos el mismo identificador para evitar duplicar stock o reembolso." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r3.error, "No se ha podido registrar la devoluci\xF3n.") };
          }
          const registro2 = normalizarDevolucionPM08(r3.data?.devolucion, "cliente");
          const movimientoCaja2 = normalizarMovimientoCajaPM08(r3.data?.movimientoCaja);
          if (!registro2) throw new Error("respuesta_devolucion_incompleta");
          setDevoluciones((s22) => [registro2, ...(s22 || []).filter((d2) => d2.operationId !== registro2.operationId && d2.id !== registro2.id)]);
          if (movimientoCaja2) setMovimientosCaja((s22) => [movimientoCaja2, ...(s22 || []).filter((m22) => m22.operationId !== movimientoCaja2.operationId)]);
          limpiarPendientePM08(clave);
          try {
            await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
            await sincronizarCajaPm08({ setArqueos, setMovimientosCaja, setDevoluciones });
          } catch {
          }
          if (!r3.data?.replayed) registrarAuditoria("Devoluci\xF3n de cliente", `${producto.nombre} \xD7${cant}${reembolsoNum > 0 ? ` \xB7 reembolso \u20AC${fmt(reembolsoNum)} (${medio})` : " \xB7 sin reembolso"}`);
          return { ok: true, registro: registro2, movimientoCaja: movimientoCaja2, replayed: !!r3.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. No repitas con otros datos: usa el mismo borrador para evitar duplicar stock o reembolso." };
        }
      }
      const r2 = aplicarMovimientoStock({
        productoId,
        cantidad: cant,
        tipo: "DEVOLUCION_CLIENTE",
        operationId: preparado.pendiente.operationId,
        movimientoId: `pm08-local-stock-${uid()}`,
        origen: "devolucionCliente",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        motivo: `Devoluci\xF3n de cliente \xB7 ${motivoLimpio}`,
        permitirDeficit: false,
        camposExtra: { reembolso: reembolsoNum, medioReembolso: medio, ingresoUnitario: -(Number(lineaVenta.ingresoUnitario) || 0), ivaVentaAplicado: Number(lineaVenta.ivaVentaAplicado) || 0 }
      });
      if (!r2.ok) {
        limpiarPendientePM08(clave);
        return { ok: false, error: r2.error || "No se ha podido aplicar al stock." };
      }
      const registro = normalizarDevolucionPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        tipo: "cliente",
        fecha: fechaReal,
        productoId,
        productoNombre: producto.nombre,
        cantidad: cant,
        motivo: motivoLimpio,
        reembolso: reembolsoNum,
        medioReembolso: medio,
        ventaId,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      }, "cliente");
      let movimientoCaja = null;
      if (reembolsoNum > 0) {
        movimientoCaja = normalizarMovimientoCajaPM08({
          operationId: preparado.pendiente.operationId,
          empresaId,
          localId: localActivoId,
          fecha: fechaReal,
          tipo: "REEMBOLSO",
          importe: reembolsoNum,
          efectoEfectivo: medio === "EFECTIVO" ? -reembolsoNum : 0,
          medioPago: medio,
          concepto: `Reembolso devoluci\xF3n \xB7 ${producto.nombre}`,
          origen: "DEVOLUCION_CLIENTE",
          referenciaId: preparado.pendiente.operationId,
          createdAt: (/* @__PURE__ */ new Date()).toISOString()
        });
        setMovimientosCaja((s22) => [movimientoCaja, ...(s22 || []).filter((m22) => m22.operationId !== movimientoCaja.operationId)]);
      }
      setDevoluciones((s22) => [registro, ...(s22 || []).filter((d2) => d2.operationId !== registro.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria("Devoluci\xF3n de cliente", `${producto.nombre} \xD7${cant}${reembolsoNum > 0 ? ` \xB7 reembolso \u20AC${fmt(reembolsoNum)} (${medio})` : " \xB7 sin reembolso"}`);
      return { ok: true, registro, movimientoCaja, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function registrarDevolucionProveedor({ productoId, cantidad, motivo, proveedorId, proveedorNombre, fecha }) {
    const cant = Number(cantidad);
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const clave = clavePendientePM08("devolucion-proveedor", empresaId, localActivoId);
    const fechaReal = fecha || leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = {
      empresaId,
      localId: localActivoId,
      productoId,
      productoNombre: producto.nombre,
      cantidad: cant,
      proveedorId: proveedorId || null,
      proveedorNombre: proveedorNombre || "",
      motivo: motivoLimpio,
      fecha: fechaReal
    };
    const preparado = prepararPendientePM08(clave, "pm08-dev-proveedor", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. No se ha descontado stock; el borrador queda listo para reintentar." };
        try {
          const r3 = await Promise.race([
            window.__nubeCliente.rpc("registrar_devolucion_proveedor", {
              p_operation_id: preparado.pendiente.operationId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_producto_id: productoId,
              p_cantidad: cant,
              p_proveedor_id: proveedorId || null,
              p_proveedor_nombre: proveedorNombre || "",
              p_motivo: motivoLimpio,
              p_fecha: fechaReal,
              p_datos: { productoNombre: producto.nombre, origen: "L&A Suite PM-08" }
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r3.error) {
            if (esErrorTransitorioPM08(r3.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. Reintenta con el mismo borrador para no duplicar la salida de stock." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r3.error, "No se ha podido registrar la devoluci\xF3n al proveedor.") };
          }
          const
--- fin coincidencia ---

--- coincidencia 9 pos=4170318 ---
r para evitar duplicar stock o reembolso." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r3.error, "No se ha podido registrar la devoluci\xF3n.") };
          }
          const registro2 = normalizarDevolucionPM08(r3.data?.devolucion, "cliente");
          const movimientoCaja2 = normalizarMovimientoCajaPM08(r3.data?.movimientoCaja);
          if (!registro2) throw new Error("respuesta_devolucion_incompleta");
          setDevoluciones((s22) => [registro2, ...(s22 || []).filter((d2) => d2.operationId !== registro2.operationId && d2.id !== registro2.id)]);
          if (movimientoCaja2) setMovimientosCaja((s22) => [movimientoCaja2, ...(s22 || []).filter((m22) => m22.operationId !== movimientoCaja2.operationId)]);
          limpiarPendientePM08(clave);
          try {
            await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
            await sincronizarCajaPm08({ setArqueos, setMovimientosCaja, setDevoluciones });
          } catch {
          }
          if (!r3.data?.replayed) registrarAuditoria("Devoluci\xF3n de cliente", `${producto.nombre} \xD7${cant}${reembolsoNum > 0 ? ` \xB7 reembolso \u20AC${fmt(reembolsoNum)} (${medio})` : " \xB7 sin reembolso"}`);
          return { ok: true, registro: registro2, movimientoCaja: movimientoCaja2, replayed: !!r3.data?.replayed };
        } catch {
          return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. No repitas con otros datos: usa el mismo borrador para evitar duplicar stock o reembolso." };
        }
      }
      const r2 = aplicarMovimientoStock({
        productoId,
        cantidad: cant,
        tipo: "DEVOLUCION_CLIENTE",
        operationId: preparado.pendiente.operationId,
        movimientoId: `pm08-local-stock-${uid()}`,
        origen: "devolucionCliente",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        motivo: `Devoluci\xF3n de cliente \xB7 ${motivoLimpio}`,
        permitirDeficit: false,
        camposExtra: { reembolso: reembolsoNum, medioReembolso: medio, ingresoUnitario: -(Number(lineaVenta.ingresoUnitario) || 0), ivaVentaAplicado: Number(lineaVenta.ivaVentaAplicado) || 0 }
      });
      if (!r2.ok) {
        limpiarPendientePM08(clave);
        return { ok: false, error: r2.error || "No se ha podido aplicar al stock." };
      }
      const registro = normalizarDevolucionPM08({
        operationId: preparado.pendiente.operationId,
        empresaId,
        localId: localActivoId,
        tipo: "cliente",
        fecha: fechaReal,
        productoId,
        productoNombre: producto.nombre,
        cantidad: cant,
        motivo: motivoLimpio,
        reembolso: reembolsoNum,
        medioReembolso: medio,
        ventaId,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      }, "cliente");
      let movimientoCaja = null;
      if (reembolsoNum > 0) {
        movimientoCaja = normalizarMovimientoCajaPM08({
          operationId: preparado.pendiente.operationId,
          empresaId,
          localId: localActivoId,
          fecha: fechaReal,
          tipo: "REEMBOLSO",
          importe: reembolsoNum,
          efectoEfectivo: medio === "EFECTIVO" ? -reembolsoNum : 0,
          medioPago: medio,
          concepto: `Reembolso devoluci\xF3n \xB7 ${producto.nombre}`,
          origen: "DEVOLUCION_CLIENTE",
          referenciaId: preparado.pendiente.operationId,
          createdAt: (/* @__PURE__ */ new Date()).toISOString()
        });
        setMovimientosCaja((s22) => [movimientoCaja, ...(s22 || []).filter((m22) => m22.operationId !== movimientoCaja.operationId)]);
      }
      setDevoluciones((s22) => [registro, ...(s22 || []).filter((d2) => d2.operationId !== registro.operationId)]);
      limpiarPendientePM08(clave);
      registrarAuditoria("Devoluci\xF3n de cliente", `${producto.nombre} \xD7${cant}${reembolsoNum > 0 ? ` \xB7 reembolso \u20AC${fmt(reembolsoNum)} (${medio})` : " \xB7 sin reembolso"}`);
      return { ok: true, registro, movimientoCaja, local: true };
    })();
    operacionesPM08EnCurso[clave] = tarea;
    try {
      return await tarea;
    } finally {
      delete operacionesPM08EnCurso[clave];
    }
  }
  async function registrarDevolucionProveedor({ productoId, cantidad, motivo, proveedorId, proveedorNombre, fecha }) {
    const cant = Number(cantidad);
    const motivoLimpio = String(motivo || "").trim();
    if (!empresaId || !localActivoId) return { ok: false, error: "Selecciona un local concreto antes de registrar la devoluci\xF3n." };
    if (!productoId) return { ok: false, error: "Elige un producto." };
    if (!Number.isFinite(cant) || cant <= 0) return { ok: false, error: "Pon una cantidad mayor que cero." };
    if (!motivoLimpio) return { ok: false, error: "Escribe el motivo de la devoluci\xF3n." };
    const producto = productos.find((p22) => p22.id === productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoDevolucion(producto)) return { ok: false, error: "El producto no pertenece al local activo." };
    const clave = clavePendientePM08("devolucion-proveedor", empresaId, localActivoId);
    const fechaReal = fecha || leerPendientePM08(clave)?.payload?.fecha || todayISO();
    const payload = {
      empresaId,
      localId: localActivoId,
      productoId,
      productoNombre: producto.nombre,
      cantidad: cant,
      proveedorId: proveedorId || null,
      proveedorNombre: proveedorNombre || "",
      motivo: motivoLimpio,
      fecha: fechaReal
    };
    const preparado = prepararPendientePM08(clave, "pm08-dev-proveedor", payload);
    if (!preparado.ok) return preparado;
    if (operacionesPM08EnCurso[clave]) return operacionesPM08EnCurso[clave];
    const tarea = (async () => {
      if (modoSincronizadoPM08()) {
        if (!window.__nubeActiva || !window.__nubeCliente) return { ok: false, pendiente: true, error: "Sin conexi\xF3n con la cuenta sincronizada. No se ha descontado stock; el borrador queda listo para reintentar." };
        try {
          const r3 = await Promise.race([
            window.__nubeCliente.rpc("registrar_devolucion_proveedor", {
              p_operation_id: preparado.pendiente.operationId,
              p_empresa_id: empresaId,
              p_local_id: localActivoId,
              p_producto_id: productoId,
              p_cantidad: cant,
              p_proveedor_id: proveedorId || null,
              p_proveedor_nombre: proveedorNombre || "",
              p_motivo: motivoLimpio,
              p_fecha: fechaReal,
              p_datos: { productoNombre: producto.nombre, origen: "L&A Suite PM-08" }
            }),
            new Promise((_22, reject) => setTimeout(() => reject(new Error("timeout_pm08")), Number(window.ESPERA_NUBE_MS) || 6e3))
          ]);
          if (r3.error) {
            if (esErrorTransitorioPM08(r3.error)) return { ok: false, pendiente: true, error: "No se pudo confirmar si el servidor recibi\xF3 la devoluci\xF3n. Reintenta con el mismo borrador para no duplicar la salida de stock." };
            limpiarPendientePM08(clave);
            return { ok: false, error: mensajeErrorPM08(r3.error, "No se ha podido registrar la devoluci\xF3n al proveedor.") };
          }
          const registro2 = normalizarDevolucionPM08
--- fin coincidencia ---

--- coincidencia 10 pos=4180586 ---
setClientes((s22) => [...s22, nuevo]);
    return nuevo;
  }
  function updateCliente(id, data) {
    setClientes((s22) => s22.map((c22) => c22.id === id && c22.empresaId === empresaId ? { ...c22, ...data, empresaId: c22.empresaId } : c22));
  }
  function deleteCliente(id) {
    const c22 = clientes.find((x3) => x3.id === id);
    registrarAuditoria("Eliminar cliente", c22 ? c22.nombre : id);
    setClientes((s22) => s22.filter((c222) => c222.id !== id));
  }
  function anonimizarCliente(id) {
    const c22 = clientes.find((x3) => x3.id === id);
    registrarAuditoria("Anonimizar cliente", c22 ? c22.nombre : id);
    setClientes(
      (s22) => s22.map(
        (cli) => cli.id === id ? { ...cli, nombre: "Cliente anonimizado", telefono: "", email: "", notas: "", anonimizado: true } : cli
      )
    );
  }
  return { addCliente, updateCliente, deleteCliente, anonimizarCliente };
}
function crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria }) {
  const idsConocidos = new Set(movimientos.map((m22) => m22.id));
  const porId = new Map(movimientos.map((m22) => [m22.id, m22]));
  const snapshotLocal = /* @__PURE__ */ new Map();
  function productoActual(productoId) {
    if (snapshotLocal.has(productoId)) return snapshotLocal.get(productoId);
    return productos.find((p22) => p22.id === productoId);
  }
  function calcularStockTeorico(productoId, campo = "stock") {
    return movimientos.filter((m22) => m22.productoId === productoId).filter((m22) => m22.origen !== "reconciliacionStock").filter((m22) => {
      if (esMovimientoNuevo(m22)) {
        return !!m22[campo === "stockPisoVenta" ? "afectaStockPisoVenta" : "afectaStockTotal"];
      }
      return campo !== "stockPisoVenta";
    }).reduce((suma, m22) => suma + cantidadConSigno(m22), 0);
  }
  function aplicarMovimientoStock({
    productoId,
    cantidad,
    tipo,
    operationId,
    movimientoId,
    origen,
    documentoOrigenId,
    usuario,
    dispositivo,
    motivo,
    permitirDeficit = false,
    afectaStockTotal = true,
    afectaStockPisoVenta = false,
    revierteMovimientoId = null,
    camposExtra = null
    // campos económicos que el motor no conoce (ingresoUnitario,
    // medioPago...) — se escriben en el mismo momento de crear
    // el movimiento, nunca como un parche después.
  }) {
    const id = movimientoId || uid();
    if (idsConocidos.has(id)) {
      const existente = porId.get(id);
      if (existente && Number(existente.cantidad) !== Number(cantidad)) {
        return { ok: false, error: `El movimiento ${id} ya existe con una cantidad distinta (${existente.cantidad} \u2260 ${cantidad}). No se sobrescribe.` };
      }
      return { ok: true, movimiento: existente, yaExistia: true };
    }
    const producto = productoActual(productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    const cantidadNum = Number(cantidad) || 0;
    const resultadoCampos = {};
    let deficitGenerado = 0;
    if (afectaStockTotal) {
      const stockActual = Number(producto.stock) || 0;
      const deficitActual = Number(producto.deficitPendiente) || 0;
      const teoricoAntes = stockActual - deficitActual;
      const teoricoDespues = teoricoAntes + cantidadNum;
      const deficitNuevo = Math.max(0, -teoricoDespues);
      if (deficitNuevo > deficitActual && !permitirDeficit) {
        return { ok: false, error: `Stock insuficiente: hay ${Math.max(0, teoricoAntes)}, se piden ${-cantidadNum}.` };
      }
      resultadoCampos.stock = Math.max(0, teoricoDespues);
      resultadoCampos.deficitPendiente = deficitNuevo;
      deficitGenerado = Math.max(0, deficitNuevo - deficitActual);
      const minimo = Number(producto.stockMinimo) || 0;
      if (minimo > 0 && teoricoAntes >= minimo && teoricoDespues < minimo && typeof window !== "undefined" && window.__nubeActiva) {
        fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            titulo: "Stock bajo",
            cuerpo: `${producto.nombre} baj\xF3 del m\xEDnimo (quedan ${fmt(Math.max(0, teoricoDespues))}).`,
            localId: producto.localId || null,
            url: "/"
          })
        }).catch(() => {
        });
      }
    }
    if (afectaStockPisoVenta) {
      const pisoActual = Number(producto.stockPisoVenta) || 0;
      resultadoCampos.stockPisoVenta = Math.max(0, pisoActual + cantidadNum);
    }
    const movimiento = {
      id,
      operationId: operationId || id,
      productoId,
      localId: producto.localId || null,
      cantidad: cantidadNum,
      tipo,
      motivo: motivo || "",
      fecha: todayISO(),
      origen: origen || "",
      documentoOrigenId: documentoOrigenId || null,
      usuario: usuario || "",
      dispositivo: dispositivo || "",
      stockAnterior: Number(producto.stock) || 0,
      stockPosterior: resultadoCampos.stock !== void 0 ? resultadoCampos.stock : Number(producto.stock) || 0,
      afectaStockTotal,
      afectaStockPisoVenta,
      revierteMovimientoId,
      ...camposExtra || {}
    };
    setProductos((s22) => s22.map((p22) => p22.id === productoId ? { ...p22, ...resultadoCampos } : p22));
    snapshotLocal.set(productoId, { ...producto, ...resultadoCampos });
    setMovimientos((s22) => [movimiento, ...s22]);
    idsConocidos.add(id);
    porId.set(id, movimiento);
    if (deficitGenerado > 0 && registrarAuditoria) {
      registrarAuditoria("deficit_stock_detectado", `${producto.nombre}: ${deficitGenerado} sin cobertura (${tipo}, operaci\xF3n ${movimiento.operationId})`);
    }
    return { ok: true, movimiento, yaExistia: false };
  }
  return { aplicarMovimientoStock, calcularStockTeorico };
}
function crearLogicaReconciliacion({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria, localActivoId }) {
  const { aplicarMovimientoStock, calcularStockTeorico } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  function diagnosticarStock() {
    const porProducto = /* @__PURE__ */ new Map();
    for (const m22 of movimientos) {
      if (!porProducto.has(m22.productoId)) porProducto.set(m22.productoId, []);
      porProducto.get(m22.productoId).push(m22);
    }
    return productos.map((p22) => {
      const movimientosDelProducto = porProducto.get(p22.id) || [];
      const stockCache = Number(p22.stock) || 0;
      const deficitCache = Number(p22.deficitPendiente) || 0;
      const teoricoCache = stockCache - deficitCache;
      const teoricoHistorico = movimientosDelProducto.filter((m22) => m22.origen !== "reconciliacionStock").filter((m22) => esMovimientoNuevo(m22) ? !!m22.afectaStockTotal : true).reduce((suma, m22) => suma + cantidadConSigno(m22), 0);
      const teoricoReal = p22._pm07Servidor ? teoricoCache : teoricoHistorico;
      const diferencia = Number((teoricoReal - teoricoCache).toFixed(4));
      const coincide = Math.abs(diferencia) < 0.01;
      const deficitReal = Math.max(0, -teoricoReal);
      const tieneDeficit = deficitReal >= 0.01;
      let estado;
      if (coincide && !tieneDeficit) estado = "OK";
      else if (coincide && tieneDeficit) estado = "DEFICIT";
      else if (!coincide && !tieneDeficit) estado = "DISCREPANCIA";
      else estado = "DEFICIT_Y_DISCREPANCIA";
   
--- fin coincidencia ---

--- coincidencia 11 pos=4195126 ---
2.map((p22) => p22.id === id ? { ...p22, ...restoDatos } : p22));
    }
    if (anterior && stockNuevoForm !== void 0) {
      const stockActual = Number(anterior.stock) || 0;
      const deficitActual = Number(anterior.deficitPendiente) || 0;
      const stockTeoricoAntes = stockActual - deficitActual;
      const valorNuevo = Number(stockNuevoForm) || 0;
      const dif = valorNuevo - stockTeoricoAntes;
      if (dif !== 0) {
        const { aplicarMovimientoStock: aplicarMovimientoStock2 } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
        aplicarMovimientoStock2({
          productoId: id,
          cantidad: dif,
          tipo: "INVENTARIO",
          movimientoId: uid(),
          origen: "updateProducto",
          afectaStockTotal: true,
          permitirDeficit: true,
          motivo: "Correcci\xF3n manual de stock"
        });
      }
    }
  }
  function deleteProducto(id) {
    const p22 = productos.find((x3) => x3.id === id);
    if (!productoEsDelLocalActivo(p22)) return false;
    registrarAuditoria("Eliminar producto (borrado l\xF3gico)", p22 ? p22.nombre : id);
    setProductos((s22) => s22.map((pr) => pr.id === id ? { ...pr, activo: false, eliminadoEn: todayISO() } : pr));
  }
  function reactivarProducto(id) {
    const p22 = productos.find((x3) => x3.id === id);
    if (!productoEsDelLocalActivo(p22)) return false;
    registrarAuditoria("Reactivar producto", p22 ? p22.nombre : id);
    setProductos((s22) => s22.map((pr) => pr.id === id ? { ...pr, activo: true, eliminadoEn: null } : pr));
  }
  function registrarSalida(productoId, cantidad, opciones = {}) {
    if (almacenCongelado) return false;
    const cant = Number(cantidad);
    if (!cant || cant <= 0) return false;
    const { motivo = "Venta", precioVentaUnitario = null, referencia = "", medioPago = "Efectivo" } = opciones;
    const prod = productos.find((p22) => p22.id === productoId);
    if (!prod || !productoEsDelLocalActivo(prod)) return false;
    const esVenta2 = motivo === "Venta";
    const esMermaMotivo = MOTIVOS_MERMA.includes(motivo);
    const tipo = esVenta2 ? "VENTA" : esMermaMotivo ? "MERMA" : "OTRO";
    const ingresoUnitario = esVenta2 ? precioVentaUnitario !== null && precioVentaUnitario !== "" ? Number(precioVentaUnitario) || 0 : precioNeto(prod) : 0;
    const r2 = aplicarMovimientoStock({
      productoId,
      cantidad: -cant,
      tipo,
      movimientoId: uid(),
      origen: "registrarSalida",
      afectaStockTotal: true,
      permitirDeficit: true,
      motivo: referencia || motivo,
      camposExtra: {
        costoUnitario: Number(prod.costo) || 0,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago: esVenta2 ? medioPago : null
      }
    });
    return r2.ok;
  }
  return { addProducto, updateProducto, deleteProducto, reactivarProducto, registrarSalida, ajustarProductoPorOtro };
}
function crearLogicaPedidos({ pedidos: pedidos2, setPedidos, productos, setProductos, setMovimientos, almacenCongelado, procesarRecepcion, localActivoId }) {
  function pedidoEsDelLocalActivo(pedido) {
    if (!pedido) return false;
    if (!localActivoId) return true;
    return pedido.localId === localActivoId;
  }
  function crearPedido({ proveedorId, fechaEsperada, items }) {
    const pedido = {
      id: uid(),
      localId: localActivoId || null,
      proveedorId,
      fecha: todayISO(),
      fechaEsperada,
      estado: "Pendiente",
      items: items.map((it2) => ({ ...it2, cantidadRecibida: 0 }))
    };
    setPedidos((s22) => [pedido, ...s22]);
    return pedido;
  }
  function actualizarPedido(pedidoId, { proveedorId, fechaEsperada, items }) {
    const actual = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(actual)) return false;
    setPedidos(
      (s22) => s22.map(
        (pe2) => pe2.id === pedidoId ? { ...pe2, proveedorId, fechaEsperada, items: items.map((it2) => ({ ...it2, cantidadRecibida: 0 })) } : pe2
      )
    );
    return true;
  }
  function eliminarPedido(pedidoId) {
    const actual = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(actual)) return false;
    setPedidos((s22) => s22.filter((pe2) => pe2.id !== pedidoId));
    return true;
  }
  function cerrarPedido(pedidoId) {
    const actual = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(actual)) return false;
    setPedidos((s22) => s22.map((pe2) => pe2.id === pedidoId ? { ...pe2, estado: "Recibido", cerradoManualmente: true } : pe2));
    return true;
  }
  function recibirPedido(pedidoId, lineas) {
    if (almacenCongelado) return false;
    const pedido = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(pedido)) return false;
    const lineasConDatos = lineas.filter((ln2) => Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || localActivoId && (!prod.localId || prod.localId !== localActivoId)) return null;
      return {
        productoId: ln2.productoId,
        cantidad: Number(ln2.cantidad),
        precioBruto: Number(ln2.precioBruto) || 0,
        importe: (Number(ln2.precioBruto) || 0) * Number(ln2.cantidad),
        ivaPct: Number(ln2.ivaPct) || (prod ? prod.ivaCompra : 10) || 10,
        udsPorCaja: Number(ln2.udsPorCaja) || (prod ? prod.udsPorCaja : 1) || 1,
        descripcion: prod ? prod.nombre : "",
        unidad: prod ? prod.unidad : "unidad"
      };
    }).filter(Boolean);
    if (!lineasConDatos.length) return false;
    const { lineasResueltas, avisos } = procesarRecepcion({
      lineas: lineasConDatos,
      proveedorId: pedido.proveedorId,
      fecha: todayISO(),
      documentoTipo: "pedido",
      documentoId: pedido.id,
      documentoNumero: pedido.id.slice(-6)
    });
    setPedidos(
      (s22) => s22.map((pe2) => {
        if (pe2.id !== pedidoId) return pe2;
        const items = pe2.items.map((it2) => {
          const suyas = lineasResueltas.filter((ln2) => ln2.productoId === it2.productoId);
          if (!suyas.length) return it2;
          const recibidas = suyas.reduce((a22, ln2) => a22 + (Number(ln2.unidadesEntradas) || 0), 0);
          return { ...it2, cantidadRecibida: (Number(it2.cantidadRecibida) || 0) + recibidas };
        });
        const completo = items.every((it2) => (Number(it2.cantidadRecibida) || 0) >= (Number(it2.cantidad) || 0));
        const algo = items.some((it2) => (Number(it2.cantidadRecibida) || 0) > 0);
        return { ...pe2, items, estado: completo ? "Recibido" : algo ? "Parcial" : "Pendiente" };
      })
    );
    return { ok: true, avisos };
  }
  return { crearPedido, actualizarPedido, eliminarPedido, recibirPedido, cerrarPedido };
}
function crearLogicaFichasCosto({ productos, setFichasCosto, localActivoId }) {
  const fichaEsDelLocalActivo = (f22) => !!f22 && (!localActivoId || f22.localId === localActivoId);
  const productoEsDelLocalActivoFicha = (p22) => !!p22 && (!localActivoId || p22.localId === localActivoId);
  function addFichaCosto(data) {
    setFichasCosto((s22) => [...s22, { id: uid(), ...data, localId: localActivoId || data.localId || null }]);
  }
  function updateFichaCosto(id, data) {
    setFichasCosto((s22) => s22.map((f22) => f22.id === id && fichaEsDelLocalActivo(f22) ? { ...f22, ...data, localId: f22.localId || localActivoI
--- fin coincidencia ---

--- coincidencia 12 pos=4195598 ---
aplicarMovimientoStock2 } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
        aplicarMovimientoStock2({
          productoId: id,
          cantidad: dif,
          tipo: "INVENTARIO",
          movimientoId: uid(),
          origen: "updateProducto",
          afectaStockTotal: true,
          permitirDeficit: true,
          motivo: "Correcci\xF3n manual de stock"
        });
      }
    }
  }
  function deleteProducto(id) {
    const p22 = productos.find((x3) => x3.id === id);
    if (!productoEsDelLocalActivo(p22)) return false;
    registrarAuditoria("Eliminar producto (borrado l\xF3gico)", p22 ? p22.nombre : id);
    setProductos((s22) => s22.map((pr) => pr.id === id ? { ...pr, activo: false, eliminadoEn: todayISO() } : pr));
  }
  function reactivarProducto(id) {
    const p22 = productos.find((x3) => x3.id === id);
    if (!productoEsDelLocalActivo(p22)) return false;
    registrarAuditoria("Reactivar producto", p22 ? p22.nombre : id);
    setProductos((s22) => s22.map((pr) => pr.id === id ? { ...pr, activo: true, eliminadoEn: null } : pr));
  }
  function registrarSalida(productoId, cantidad, opciones = {}) {
    if (almacenCongelado) return false;
    const cant = Number(cantidad);
    if (!cant || cant <= 0) return false;
    const { motivo = "Venta", precioVentaUnitario = null, referencia = "", medioPago = "Efectivo" } = opciones;
    const prod = productos.find((p22) => p22.id === productoId);
    if (!prod || !productoEsDelLocalActivo(prod)) return false;
    const esVenta2 = motivo === "Venta";
    const esMermaMotivo = MOTIVOS_MERMA.includes(motivo);
    const tipo = esVenta2 ? "VENTA" : esMermaMotivo ? "MERMA" : "OTRO";
    const ingresoUnitario = esVenta2 ? precioVentaUnitario !== null && precioVentaUnitario !== "" ? Number(precioVentaUnitario) || 0 : precioNeto(prod) : 0;
    const r2 = aplicarMovimientoStock({
      productoId,
      cantidad: -cant,
      tipo,
      movimientoId: uid(),
      origen: "registrarSalida",
      afectaStockTotal: true,
      permitirDeficit: true,
      motivo: referencia || motivo,
      camposExtra: {
        costoUnitario: Number(prod.costo) || 0,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago: esVenta2 ? medioPago : null
      }
    });
    return r2.ok;
  }
  return { addProducto, updateProducto, deleteProducto, reactivarProducto, registrarSalida, ajustarProductoPorOtro };
}
function crearLogicaPedidos({ pedidos: pedidos2, setPedidos, productos, setProductos, setMovimientos, almacenCongelado, procesarRecepcion, localActivoId }) {
  function pedidoEsDelLocalActivo(pedido) {
    if (!pedido) return false;
    if (!localActivoId) return true;
    return pedido.localId === localActivoId;
  }
  function crearPedido({ proveedorId, fechaEsperada, items }) {
    const pedido = {
      id: uid(),
      localId: localActivoId || null,
      proveedorId,
      fecha: todayISO(),
      fechaEsperada,
      estado: "Pendiente",
      items: items.map((it2) => ({ ...it2, cantidadRecibida: 0 }))
    };
    setPedidos((s22) => [pedido, ...s22]);
    return pedido;
  }
  function actualizarPedido(pedidoId, { proveedorId, fechaEsperada, items }) {
    const actual = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(actual)) return false;
    setPedidos(
      (s22) => s22.map(
        (pe2) => pe2.id === pedidoId ? { ...pe2, proveedorId, fechaEsperada, items: items.map((it2) => ({ ...it2, cantidadRecibida: 0 })) } : pe2
      )
    );
    return true;
  }
  function eliminarPedido(pedidoId) {
    const actual = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(actual)) return false;
    setPedidos((s22) => s22.filter((pe2) => pe2.id !== pedidoId));
    return true;
  }
  function cerrarPedido(pedidoId) {
    const actual = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(actual)) return false;
    setPedidos((s22) => s22.map((pe2) => pe2.id === pedidoId ? { ...pe2, estado: "Recibido", cerradoManualmente: true } : pe2));
    return true;
  }
  function recibirPedido(pedidoId, lineas) {
    if (almacenCongelado) return false;
    const pedido = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(pedido)) return false;
    const lineasConDatos = lineas.filter((ln2) => Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || localActivoId && (!prod.localId || prod.localId !== localActivoId)) return null;
      return {
        productoId: ln2.productoId,
        cantidad: Number(ln2.cantidad),
        precioBruto: Number(ln2.precioBruto) || 0,
        importe: (Number(ln2.precioBruto) || 0) * Number(ln2.cantidad),
        ivaPct: Number(ln2.ivaPct) || (prod ? prod.ivaCompra : 10) || 10,
        udsPorCaja: Number(ln2.udsPorCaja) || (prod ? prod.udsPorCaja : 1) || 1,
        descripcion: prod ? prod.nombre : "",
        unidad: prod ? prod.unidad : "unidad"
      };
    }).filter(Boolean);
    if (!lineasConDatos.length) return false;
    const { lineasResueltas, avisos } = procesarRecepcion({
      lineas: lineasConDatos,
      proveedorId: pedido.proveedorId,
      fecha: todayISO(),
      documentoTipo: "pedido",
      documentoId: pedido.id,
      documentoNumero: pedido.id.slice(-6)
    });
    setPedidos(
      (s22) => s22.map((pe2) => {
        if (pe2.id !== pedidoId) return pe2;
        const items = pe2.items.map((it2) => {
          const suyas = lineasResueltas.filter((ln2) => ln2.productoId === it2.productoId);
          if (!suyas.length) return it2;
          const recibidas = suyas.reduce((a22, ln2) => a22 + (Number(ln2.unidadesEntradas) || 0), 0);
          return { ...it2, cantidadRecibida: (Number(it2.cantidadRecibida) || 0) + recibidas };
        });
        const completo = items.every((it2) => (Number(it2.cantidadRecibida) || 0) >= (Number(it2.cantidad) || 0));
        const algo = items.some((it2) => (Number(it2.cantidadRecibida) || 0) > 0);
        return { ...pe2, items, estado: completo ? "Recibido" : algo ? "Parcial" : "Pendiente" };
      })
    );
    return { ok: true, avisos };
  }
  return { crearPedido, actualizarPedido, eliminarPedido, recibirPedido, cerrarPedido };
}
function crearLogicaFichasCosto({ productos, setFichasCosto, localActivoId }) {
  const fichaEsDelLocalActivo = (f22) => !!f22 && (!localActivoId || f22.localId === localActivoId);
  const productoEsDelLocalActivoFicha = (p22) => !!p22 && (!localActivoId || p22.localId === localActivoId);
  function addFichaCosto(data) {
    setFichasCosto((s22) => [...s22, { id: uid(), ...data, localId: localActivoId || data.localId || null }]);
  }
  function updateFichaCosto(id, data) {
    setFichasCosto((s22) => s22.map((f22) => f22.id === id && fichaEsDelLocalActivo(f22) ? { ...f22, ...data, localId: f22.localId || localActivoId || null } : f22));
  }
  function deleteFichaCosto(id) {
    setFichasCosto((s22) => s22.filter((f22) => f22.id !== id || !fichaEsDelLocalActivo(f22)));
  }
  function alergenosDeFicha(f22) {
    const set = new Set(f22.alergenosExtra || []);
    (f22.componentes || []).forEach((c22) => {
      if (!c22.productoId) return;
      const p22 = productos.find((x3) => x3.id === c22.productoId && productoEsDelLocalActivoFicha(x3));
      (p22?.alergenos || []).forEach((a2
--- fin coincidencia ---

--- coincidencia 13 pos=4228549 ---
cargo, entregarEncargo };
}
function crearLogicaVenta({ productos, setProductos, movimientos, setMovimientos, arqueos, localActivoId }) {
  function productoEsDelLocalActivoVenta(prod) {
    if (!prod) return false;
    if (!localActivoId) return true;
    return !prod.localId || prod.localId === localActivoId;
  }
  function movimientoEsDelLocalActivoVenta(m22) {
    if (!localActivoId) return true;
    if (m22.localId) return m22.localId === localActivoId;
    const prod = productos.find((p22) => p22.id === m22.productoId);
    return !!prod && (!prod.localId || prod.localId === localActivoId);
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function venderLineas(lineas, opciones = {}) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const {
      tipo = "VENTA",
      medioPago = "Efectivo",
      detallePago = null,
      operationId = null,
      origen = "venderLocal",
      documentoOrigenId = null,
      motivoBase = "TPV",
      extraPorLinea = () => ({})
    } = opciones;
    const ventaId = operationId || uid();
    let totalVenta = 0;
    if (detallePago) {
      (lineas || []).forEach((ln2) => {
        const cant = Number(ln2.cantidad) || 0;
        const prod = productos.find((p22) => p22.id === ln2.productoId);
        if (!prod || cant <= 0) return;
        const precio = Number(ln2.precioUnitario) || precioNeto(prod);
        totalVenta += cant * precio * (1 + ivaDe(prod) / 100);
      });
    }
    const movimientosGenerados = [];
    (lineas || []).forEach((ln2) => {
      if (!ln2.productoId) return;
      const cant = Number(ln2.cantidad) || 0;
      if (cant <= 0) return;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      const ingresoUnitario = Number(ln2.precioUnitario) || precioNeto(prod);
      const camposExtra = {
        costoUnitario,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago,
        ...extraPorLinea(ln2, prod)
      };
      if (detallePago && totalVenta > 0) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaDe(prod) / 100);
        const proporcion = importeLinea / totalVenta;
        camposExtra.detallePago = {
          efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)),
          tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4))
        };
      }
      const r2 = aplicarMovimientoStock({
        productoId: prod.id,
        cantidad: -cant,
        tipo,
        operationId: ventaId,
        movimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { erro
--- fin coincidencia ---

--- coincidencia 14 pos=4228674 ---
ctivoId }) {
  function productoEsDelLocalActivoVenta(prod) {
    if (!prod) return false;
    if (!localActivoId) return true;
    return !prod.localId || prod.localId === localActivoId;
  }
  function movimientoEsDelLocalActivoVenta(m22) {
    if (!localActivoId) return true;
    if (m22.localId) return m22.localId === localActivoId;
    const prod = productos.find((p22) => p22.id === m22.productoId);
    return !!prod && (!prod.localId || prod.localId === localActivoId);
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function venderLineas(lineas, opciones = {}) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const {
      tipo = "VENTA",
      medioPago = "Efectivo",
      detallePago = null,
      operationId = null,
      origen = "venderLocal",
      documentoOrigenId = null,
      motivoBase = "TPV",
      extraPorLinea = () => ({})
    } = opciones;
    const ventaId = operationId || uid();
    let totalVenta = 0;
    if (detallePago) {
      (lineas || []).forEach((ln2) => {
        const cant = Number(ln2.cantidad) || 0;
        const prod = productos.find((p22) => p22.id === ln2.productoId);
        if (!prod || cant <= 0) return;
        const precio = Number(ln2.precioUnitario) || precioNeto(prod);
        totalVenta += cant * precio * (1 + ivaDe(prod) / 100);
      });
    }
    const movimientosGenerados = [];
    (lineas || []).forEach((ln2) => {
      if (!ln2.productoId) return;
      const cant = Number(ln2.cantidad) || 0;
      if (cant <= 0) return;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      const ingresoUnitario = Number(ln2.precioUnitario) || precioNeto(prod);
      const camposExtra = {
        costoUnitario,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago,
        ...extraPorLinea(ln2, prod)
      };
      if (detallePago && totalVenta > 0) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaDe(prod) / 100);
        const proporcion = importeLinea / totalVenta;
        camposExtra.detallePago = {
          efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)),
          tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4))
        };
      }
      const r2 = aplicarMovimientoStock({
        productoId: prod.id,
        cantidad: -cant,
        tipo,
        operationId: ventaId,
        movimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
    try {
      let r2;
  
--- fin coincidencia ---

--- coincidencia 15 pos=4228873 ---
ion movimientoEsDelLocalActivoVenta(m22) {
    if (!localActivoId) return true;
    if (m22.localId) return m22.localId === localActivoId;
    const prod = productos.find((p22) => p22.id === m22.productoId);
    return !!prod && (!prod.localId || prod.localId === localActivoId);
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function venderLineas(lineas, opciones = {}) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const {
      tipo = "VENTA",
      medioPago = "Efectivo",
      detallePago = null,
      operationId = null,
      origen = "venderLocal",
      documentoOrigenId = null,
      motivoBase = "TPV",
      extraPorLinea = () => ({})
    } = opciones;
    const ventaId = operationId || uid();
    let totalVenta = 0;
    if (detallePago) {
      (lineas || []).forEach((ln2) => {
        const cant = Number(ln2.cantidad) || 0;
        const prod = productos.find((p22) => p22.id === ln2.productoId);
        if (!prod || cant <= 0) return;
        const precio = Number(ln2.precioUnitario) || precioNeto(prod);
        totalVenta += cant * precio * (1 + ivaDe(prod) / 100);
      });
    }
    const movimientosGenerados = [];
    (lineas || []).forEach((ln2) => {
      if (!ln2.productoId) return;
      const cant = Number(ln2.cantidad) || 0;
      if (cant <= 0) return;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      const ingresoUnitario = Number(ln2.precioUnitario) || precioNeto(prod);
      const camposExtra = {
        costoUnitario,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago,
        ...extraPorLinea(ln2, prod)
      };
      if (detallePago && totalVenta > 0) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaDe(prod) / 100);
        const proporcion = importeLinea / totalVenta;
        camposExtra.detallePago = {
          efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)),
          tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4))
        };
      }
      const r2 = aplicarMovimientoStock({
        productoId: prod.id,
        cantidad: -cant,
        tipo,
        operationId: ventaId,
        movimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
   
--- fin coincidencia ---

--- coincidencia 16 pos=4231244 ---
o.efectivo * proporcion).toFixed(4)),
          tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4))
        };
      }
      const r2 = aplicarMovimientoStock({
        productoId: prod.id,
        cantidad: -cant,
        tipo,
        operationId: ventaId,
        movimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      return { ok: true, n: lineasParaRpc.length, modo: "pm07-servidor", replayed: !!r2.data?.replayed, ventaId };
    } catch (error) {
      return { ok: false, ...errorVisible(error) };
    }
  }
  function anularVentaLocal(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m22) => m22.anulaVentaId === ventaId || m22.revierteMovimientoId && lineas.some((l22) => l22.id === m22.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localI
--- fin coincidencia ---

--- coincidencia 17 pos=4231523 ---
ovimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      return { ok: true, n: lineasParaRpc.length, modo: "pm07-servidor", replayed: !!r2.data?.replayed, ventaId };
    } catch (error) {
      return { ok: false, ...errorVisible(error) };
    }
  }
  function anularVentaLocal(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m22) => m22.anulaVentaId === ventaId || m22.revierteMovimientoId && lineas.some((l22) => l22.id === m22.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
    if (arqueoDelDia && typeof window !== "undefined" && window.__nubeActiva) {
      fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
    
--- fin coincidencia ---

--- coincidencia 18 pos=4231699 ---
rDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      return { ok: true, n: lineasParaRpc.length, modo: "pm07-servidor", replayed: !!r2.data?.replayed, ventaId };
    } catch (error) {
      return { ok: false, ...errorVisible(error) };
    }
  }
  function anularVentaLocal(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m22) => m22.anulaVentaId === ventaId || m22.revierteMovimientoId && lineas.some((l22) => l22.id === m22.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
    if (arqueoDelDia && typeof window !== "undefined" && window.__nubeActiva) {
      fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          titulo: "Caja cerrada, ahora desactualizada",
          cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja ce
--- fin coincidencia ---

--- coincidencia 19 pos=4236015 ---
No se ha descontado stock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      return { ok: true, n: lineasParaRpc.length, modo: "pm07-servidor", replayed: !!r2.data?.replayed, ventaId };
    } catch (error) {
      return { ok: false, ...errorVisible(error) };
    }
  }
  function anularVentaLocal(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m22) => m22.anulaVentaId === ventaId || m22.revierteMovimientoId && lineas.some((l22) => l22.id === m22.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
    if (arqueoDelDia && typeof window !== "undefined" && window.__nubeActiva) {
      fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          titulo: "Caja cerrada, ahora desactualizada",
          cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`,
          localId: lineas[0]?.localId || null,
          url: "/"
        })
      }).catch(() => {
      });
    }
    return { ok: true, lineasAnuladas: aplicadas, arqueoAfectado: !!arqueoDelDia };
  }
  async function anularVenta(ventaId, movimientosActuales, motivo = "") {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para gestionar ventas del TPV." };
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return anularVentaLocal(ventaId, movimientosActuales, motivo);
    const lineasOriginales = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineasOriginales.length === 0) return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    try {
      const supabase = await window.getSupabaseClient();
      const operationId = `rev-${ventaId}`;
      const r2 = await supabase.rpc("revertir_venta_stock_carrito", { p_operation_id: operationId, p_venta_operation_id: ventaId, p_motivo: motivo || "" });
      if (r2.error) throw r2.error;
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      const fechaVenta = lineasOriginales[0]?.fecha;
      const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
      if (arqueoDelDia) {
        fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ titulo: "Caja cerrada, ahora desactualizada", cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`, localId: r2.data?.localId || lineasOriginales[0]?.localId || null, url: "/" })
        }).catch(() => {
        });
      }
      return { ok: true, lineasAnuladas: Number(r2.data?.lineasAnuladas) || 0, arqueoAfectado: !!arqueoDelDia, modo: "pm07-servidor", sincronizada: true, replayed: !!r2.data?.replayed };
    } catch (error) {
      const msg = String(error?.message || "");
      if (msg.includes("venta_stock_ya_revertida")) return { ok: false, error: "La venta ya estaba anulada.", yaExistia: true };
      return { ok: false, error: "No se pudo confirmar la anulaci\xF3n con el servidor. No se ha modificado el stock local." };
    }
  }
  return { venderCarrito, venderLocal, anularVenta, venderLineas };
}
function crearLogicaTraspasos({ productos, setProductos, movimientos, setMovimientos, setTraspasos, registrarAuditoria, localActivoId, locales = [] }) {
  function productoEsDelLocalActivoTraspaso(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  async function traspasarStock(productoId, cantidad, direccion) {
    const cant = Number(cantidad) || 0;
    if (cant <= 0) return { ok: false, error: "Indica una cantidad mayor que cero." };
    const prod = productos.find((p22) => p22.id === productoId);
    if (!prod) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoTraspaso(prod)) return { ok: false, error: "Ese producto no pertenece al local activo." };
    const enPiso = Number(prod.stockPisoVenta) || 0;
    const total = Number(prod.stock) || 0;
    const enAlmacen = Math.max(0, total - enPiso);
    if (direccion === "a_piso" && cant > enAlmacen) {
      return { ok: false, error: `Solo hay ${fmt(enAlmacen)} ${prod.unidad} en el almac\xE9n.` };
    }
    if (direccion === "a_almacen" && cant > enPiso) {
      return { ok: false, error: `Solo hay ${fmt(enPiso)} ${prod.unidad} en el piso de venta.` };
    }
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupaba
--- fin coincidencia ---

--- coincidencia 20 pos=4236036 ---
tock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      return { ok: true, n: lineasParaRpc.length, modo: "pm07-servidor", replayed: !!r2.data?.replayed, ventaId };
    } catch (error) {
      return { ok: false, ...errorVisible(error) };
    }
  }
  function anularVentaLocal(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m22) => m22.anulaVentaId === ventaId || m22.revierteMovimientoId && lineas.some((l22) => l22.id === m22.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
    if (arqueoDelDia && typeof window !== "undefined" && window.__nubeActiva) {
      fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          titulo: "Caja cerrada, ahora desactualizada",
          cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`,
          localId: lineas[0]?.localId || null,
          url: "/"
        })
      }).catch(() => {
      });
    }
    return { ok: true, lineasAnuladas: aplicadas, arqueoAfectado: !!arqueoDelDia };
  }
  async function anularVenta(ventaId, movimientosActuales, motivo = "") {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para gestionar ventas del TPV." };
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return anularVentaLocal(ventaId, movimientosActuales, motivo);
    const lineasOriginales = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineasOriginales.length === 0) return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    try {
      const supabase = await window.getSupabaseClient();
      const operationId = `rev-${ventaId}`;
      const r2 = await supabase.rpc("revertir_venta_stock_carrito", { p_operation_id: operationId, p_venta_operation_id: ventaId, p_motivo: motivo || "" });
      if (r2.error) throw r2.error;
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      const fechaVenta = lineasOriginales[0]?.fecha;
      const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
      if (arqueoDelDia) {
        fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ titulo: "Caja cerrada, ahora desactualizada", cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`, localId: r2.data?.localId || lineasOriginales[0]?.localId || null, url: "/" })
        }).catch(() => {
        });
      }
      return { ok: true, lineasAnuladas: Number(r2.data?.lineasAnuladas) || 0, arqueoAfectado: !!arqueoDelDia, modo: "pm07-servidor", sincronizada: true, replayed: !!r2.data?.replayed };
    } catch (error) {
      const msg = String(error?.message || "");
      if (msg.includes("venta_stock_ya_revertida")) return { ok: false, error: "La venta ya estaba anulada.", yaExistia: true };
      return { ok: false, error: "No se pudo confirmar la anulaci\xF3n con el servidor. No se ha modificado el stock local." };
    }
  }
  return { venderCarrito, venderLocal, anularVenta, venderLineas };
}
function crearLogicaTraspasos({ productos, setProductos, movimientos, setMovimientos, setTraspasos, registrarAuditoria, localActivoId, locales = [] }) {
  function productoEsDelLocalActivoTraspaso(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  async function traspasarStock(productoId, cantidad, direccion) {
    const cant = Number(cantidad) || 0;
    if (cant <= 0) return { ok: false, error: "Indica una cantidad mayor que cero." };
    const prod = productos.find((p22) => p22.id === productoId);
    if (!prod) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoTraspaso(prod)) return { ok: false, error: "Ese producto no pertenece al local activo." };
    const enPiso = Number(prod.stockPisoVenta) || 0;
    const total = Number(prod.stock) || 0;
    const enAlmacen = Math.max(0, total - enPiso);
    if (direccion === "a_piso" && cant > enAlmacen) {
      return { ok: false, error: `Solo hay ${fmt(enAlmacen)} ${prod.unidad} en el almac\xE9n.` };
    }
    if (direccion === "a_almacen" && cant > enPiso) {
      return { ok: false, error: `Solo hay ${fmt(enPiso)} ${prod.unidad} en el piso de venta.` };
    }
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "functio
--- fin coincidencia ---

PATRON 'costoUnitario': 70 coincidencias
--- coincidencia 1 pos=3988934 ---
 };
  const supabase = await window.getSupabaseClient();
  let qStock = supabase.from("stock_estado").select("empresa_id,local_id,producto_id,almacen,piso,total,minimo,bajo_minimo,fraccionable,precision_cantidad,local_operable");
  let qMov = supabase.from("movimientos_stock").select("id,operation_id,tipo,empresa_id,local_id,producto_id,delta_almacen,delta_piso,delta_total,cantidad,movimiento_original_id,datos,actor_user_id,created_at").order("created_at", { ascending: false }).limit(2e3);
  const [rStock, rMov] = await Promise.all([qStock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : null;
      const st2 = exacto || activo;
      if (!st2) return prod;
      return { ...prod, stock: Number(st2.total) || 0, stockPisoVenta: Number(st2.piso) || 0, stockMinimo: Number(st2.minimo) || 0, _pm07BajoMinimo: !!st2.bajo_minimo, _pm07Servidor: true, _pm07LocalOperable: st2.local_operable !== false };
    }));
  }
  if (typeof setMovimientos === "function") {
    const server = movs.map((m4) => {
      const d2 = m4.datos && typeof m4.datos === "object" ? m4.datos : {};
      const delta = Number(m4.delta_total) || 0;
      const creado = String(m4.created_at || "");
      return {
        id: `pm07-${m4.id}`,
        operationId: m4.operation_id,
        ventaId: d2.ventaId || (m4.tipo === "VENTA" ? m4.operation_id : d2.anulaVentaId || null),
        anulaVentaId: d2.anulaVentaId || null,
        productoId: m4.producto_id,
        localId: m4.local_id,
        empresaId: m4.empresa_id,
        cantidad: delta,
        cantidadFisica: Number(m4.cantidad) || Math.abs(delta),
        tipo: m4.tipo,
        motivo: d2.motivo || m4.tipo,
        referencia: d2.referencia || "PM-07",
        costoUnitario: Number(d2.costoUnitario) || 0,
        ingresoUnitario: Number(d2.ingresoUnitario) || 0,
        ivaVentaAplicado: Number(d2.ivaVentaAplicado) || 0,
        medioPago: d2.medioPago || null,
        detallePago: d2.detallePago || null,
        fecha: creado ? creado.slice(0, 10) : todayISO(),
        hora: creado ? creado.slice(11, 16) : (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
        afectaStockTotal: delta !== 0,
        afectaStockPisoVenta: Number(m4.delta_piso) !== 0,
        movimientoOriginalId: m4.movimiento_original_id || null,
        _pm07Servidor: true
      };
    });
    setMovimientos((prev) => [...server, ...(prev || []).filter((m4) => !m4._pm07Servidor)]);
  }
  return { ok: true, stocks: stocks.length, movimientos: movs.length };
}
function redondearDineroPM08(valor) {
  const n2 = Number(valor);
  return Number.isFinite(n2) ? Math.round((n2 + Number.EPSILON) * 100) / 100 : NaN;
}
function modoSincronizadoPM08() {
  return typeof window !== "undefined" && !!window.NUBE_URL && !window.__modoPruebasLocal;
}
function horaActualPM08() {
  return (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5);
}
function clavePendientePM08(tipo, empresaId, localId, extra = "") {
  return `almacen:pm08:${tipo}:${empresaId || "sin-empresa"}:${localId || "todos"}${extra ? `:${extra}` : ""}`;
}
function leerPendientePM08(clave) {
  try {
    return JSON.parse(localStorage.getItem(clave) || "null");
  } catch {
    return null;
  }
}
function guardarPendientePM08(clave, valor) {
  try {
    localStorage.setItem(clave, JSON.stringify(valor));
    return true;
  } catch {
    return false;
  }
}
function limpiarPendientePM08(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
function prepararPendientePM08(clave, prefijo, payload) {
  const existente = leerPendientePM08(clave);
  if (existente) {
    if (JSON.stringify(existente.payload) !== JSON.stringify(payload)) {
      return { ok: false, pendiente: existente, error: "Hay una operaci\xF3n anterior pendiente en este local. Reint\xE9ntala con sus mismos datos antes de iniciar otra." };
    }
    return { ok: true, pendiente: existente, recuperada: true };
  }
  const pendiente = { operationId: `${prefijo}-${Date.now()}-${uid()}`, payload, creadoEn: (/* @__PURE__ */ new Date()).toISOString() };
  if (!guardarPendientePM08(clave, pendiente)) {
    return { ok: false, error: "No se ha podido conservar el borrador de la operaci\xF3n en este dispositivo." };
  }
  return { ok: true, pendiente, recuperada: false };
}
var operacionesPM08EnCurso = {};
function esErrorTransitorioPM08(error) {
  const codigo = String(error?.code || "").toLowerCase();
  const mensaje = String(error?.message || error || "").toLowerCase();
  return !codigo && /failed to fetch|fetch failed|network|conexi[oó]n|timeout|load failed|offline/.test(mensaje);
}
function mensajeErrorPM08(error, fallback = "No se ha podido confirmar la operaci\xF3n.") {
  const mensaje = String(error?.message || error || "");
  const mensajes = [
    ["operation_id_conflict", "El identificador de reintento ya existe con datos distintos. La operaci\xF3n no se ha repetido."],
    ["contexto_no_autorizado", "La empresa o el local no pertenecen al \xE1mbito autorizado de esta cuenta."],
    ["venta_fuera_de_contexto", "La venta pertenece a otra empresa o a otro local."],
    ["local_inactivo", "El local est\xE1 cerrado o inactivo y no admite nuevas operaciones."],
    ["periodo_caja_cerrado", "Ese d\xEDa ya tiene un arqueo activo. Anula el arqueo de forma trazable antes de modificar la caja."],
    ["arqueo_ya_existe", "Ya existe un arqueo activo para ese d\xEDa y local."],
    ["arqueo_ya_anulado", "El arqueo ya estaba anulado."],
    ["movimiento_caja_ya_revertido", "Ese movimiento de caja ya tiene un reverso."],
    ["devolucion_supera_cantidad_pendiente", "La cantidad supera lo que queda pendiente de devolver en la venta."],
    ["reembolso_supera_importe_pendiente", "El reembolso supera el importe permitido por la cantidad devuelta."],
    ["venta_ya_anulada", "La venta est\xE1 anulada y ya no admite devoluciones."],
    ["venta_con_devoluciones", "La venta ya tiene devoluciones; no puede anularse por completo."],
    ["venta_no_encontrada", "No se ha encontrado la venta original."],
    ["producto_no_pertenece_a_venta", "El producto no pertenece a la venta seleccionada."],
    ["importe_venta_no_disponible", "La venta no conserva un importe verificable para autorizar el reembolso."],
    ["stock_insuficiente", "No hay stock suficiente para completar la devoluci\xF3n al proveedor."],
    ["cantidad_invalida", "La cantidad debe ser mayor que cero y respetar la unidad del producto."],
    ["unidad_indivisible", "Este producto solo admite cantidades enteras."],
    ["precision_cantidad_excedida", "La cantidad tiene m\xE1s decimales de los permitidos para este producto."],
    ["importe_negativo", "El importe no puede ser negativo."],
    ["importe_cero", "El importe debe ser mayor que cero."],
    ["importe_invalido", "Escribe un importe v\xE1lido."],
    ["importe_fuera_rango", "El importe est\xE1 fuera del rango permitido."],
    ["importe_precision_invalida", "El importe
--- fin coincidencia ---

--- coincidencia 2 pos=3988959 ---
ait window.getSupabaseClient();
  let qStock = supabase.from("stock_estado").select("empresa_id,local_id,producto_id,almacen,piso,total,minimo,bajo_minimo,fraccionable,precision_cantidad,local_operable");
  let qMov = supabase.from("movimientos_stock").select("id,operation_id,tipo,empresa_id,local_id,producto_id,delta_almacen,delta_piso,delta_total,cantidad,movimiento_original_id,datos,actor_user_id,created_at").order("created_at", { ascending: false }).limit(2e3);
  const [rStock, rMov] = await Promise.all([qStock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : null;
      const st2 = exacto || activo;
      if (!st2) return prod;
      return { ...prod, stock: Number(st2.total) || 0, stockPisoVenta: Number(st2.piso) || 0, stockMinimo: Number(st2.minimo) || 0, _pm07BajoMinimo: !!st2.bajo_minimo, _pm07Servidor: true, _pm07LocalOperable: st2.local_operable !== false };
    }));
  }
  if (typeof setMovimientos === "function") {
    const server = movs.map((m4) => {
      const d2 = m4.datos && typeof m4.datos === "object" ? m4.datos : {};
      const delta = Number(m4.delta_total) || 0;
      const creado = String(m4.created_at || "");
      return {
        id: `pm07-${m4.id}`,
        operationId: m4.operation_id,
        ventaId: d2.ventaId || (m4.tipo === "VENTA" ? m4.operation_id : d2.anulaVentaId || null),
        anulaVentaId: d2.anulaVentaId || null,
        productoId: m4.producto_id,
        localId: m4.local_id,
        empresaId: m4.empresa_id,
        cantidad: delta,
        cantidadFisica: Number(m4.cantidad) || Math.abs(delta),
        tipo: m4.tipo,
        motivo: d2.motivo || m4.tipo,
        referencia: d2.referencia || "PM-07",
        costoUnitario: Number(d2.costoUnitario) || 0,
        ingresoUnitario: Number(d2.ingresoUnitario) || 0,
        ivaVentaAplicado: Number(d2.ivaVentaAplicado) || 0,
        medioPago: d2.medioPago || null,
        detallePago: d2.detallePago || null,
        fecha: creado ? creado.slice(0, 10) : todayISO(),
        hora: creado ? creado.slice(11, 16) : (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
        afectaStockTotal: delta !== 0,
        afectaStockPisoVenta: Number(m4.delta_piso) !== 0,
        movimientoOriginalId: m4.movimiento_original_id || null,
        _pm07Servidor: true
      };
    });
    setMovimientos((prev) => [...server, ...(prev || []).filter((m4) => !m4._pm07Servidor)]);
  }
  return { ok: true, stocks: stocks.length, movimientos: movs.length };
}
function redondearDineroPM08(valor) {
  const n2 = Number(valor);
  return Number.isFinite(n2) ? Math.round((n2 + Number.EPSILON) * 100) / 100 : NaN;
}
function modoSincronizadoPM08() {
  return typeof window !== "undefined" && !!window.NUBE_URL && !window.__modoPruebasLocal;
}
function horaActualPM08() {
  return (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5);
}
function clavePendientePM08(tipo, empresaId, localId, extra = "") {
  return `almacen:pm08:${tipo}:${empresaId || "sin-empresa"}:${localId || "todos"}${extra ? `:${extra}` : ""}`;
}
function leerPendientePM08(clave) {
  try {
    return JSON.parse(localStorage.getItem(clave) || "null");
  } catch {
    return null;
  }
}
function guardarPendientePM08(clave, valor) {
  try {
    localStorage.setItem(clave, JSON.stringify(valor));
    return true;
  } catch {
    return false;
  }
}
function limpiarPendientePM08(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
function prepararPendientePM08(clave, prefijo, payload) {
  const existente = leerPendientePM08(clave);
  if (existente) {
    if (JSON.stringify(existente.payload) !== JSON.stringify(payload)) {
      return { ok: false, pendiente: existente, error: "Hay una operaci\xF3n anterior pendiente en este local. Reint\xE9ntala con sus mismos datos antes de iniciar otra." };
    }
    return { ok: true, pendiente: existente, recuperada: true };
  }
  const pendiente = { operationId: `${prefijo}-${Date.now()}-${uid()}`, payload, creadoEn: (/* @__PURE__ */ new Date()).toISOString() };
  if (!guardarPendientePM08(clave, pendiente)) {
    return { ok: false, error: "No se ha podido conservar el borrador de la operaci\xF3n en este dispositivo." };
  }
  return { ok: true, pendiente, recuperada: false };
}
var operacionesPM08EnCurso = {};
function esErrorTransitorioPM08(error) {
  const codigo = String(error?.code || "").toLowerCase();
  const mensaje = String(error?.message || error || "").toLowerCase();
  return !codigo && /failed to fetch|fetch failed|network|conexi[oó]n|timeout|load failed|offline/.test(mensaje);
}
function mensajeErrorPM08(error, fallback = "No se ha podido confirmar la operaci\xF3n.") {
  const mensaje = String(error?.message || error || "");
  const mensajes = [
    ["operation_id_conflict", "El identificador de reintento ya existe con datos distintos. La operaci\xF3n no se ha repetido."],
    ["contexto_no_autorizado", "La empresa o el local no pertenecen al \xE1mbito autorizado de esta cuenta."],
    ["venta_fuera_de_contexto", "La venta pertenece a otra empresa o a otro local."],
    ["local_inactivo", "El local est\xE1 cerrado o inactivo y no admite nuevas operaciones."],
    ["periodo_caja_cerrado", "Ese d\xEDa ya tiene un arqueo activo. Anula el arqueo de forma trazable antes de modificar la caja."],
    ["arqueo_ya_existe", "Ya existe un arqueo activo para ese d\xEDa y local."],
    ["arqueo_ya_anulado", "El arqueo ya estaba anulado."],
    ["movimiento_caja_ya_revertido", "Ese movimiento de caja ya tiene un reverso."],
    ["devolucion_supera_cantidad_pendiente", "La cantidad supera lo que queda pendiente de devolver en la venta."],
    ["reembolso_supera_importe_pendiente", "El reembolso supera el importe permitido por la cantidad devuelta."],
    ["venta_ya_anulada", "La venta est\xE1 anulada y ya no admite devoluciones."],
    ["venta_con_devoluciones", "La venta ya tiene devoluciones; no puede anularse por completo."],
    ["venta_no_encontrada", "No se ha encontrado la venta original."],
    ["producto_no_pertenece_a_venta", "El producto no pertenece a la venta seleccionada."],
    ["importe_venta_no_disponible", "La venta no conserva un importe verificable para autorizar el reembolso."],
    ["stock_insuficiente", "No hay stock suficiente para completar la devoluci\xF3n al proveedor."],
    ["cantidad_invalida", "La cantidad debe ser mayor que cero y respetar la unidad del producto."],
    ["unidad_indivisible", "Este producto solo admite cantidades enteras."],
    ["precision_cantidad_excedida", "La cantidad tiene m\xE1s decimales de los permitidos para este producto."],
    ["importe_negativo", "El importe no puede ser negativo."],
    ["importe_cero", "El importe debe ser mayor que cero."],
    ["importe_invalido", "Escribe un importe v\xE1lido."],
    ["importe_fuera_rango", "El importe est\xE1 fuera del rango permitido."],
    ["importe_precision_invalida", "El importe solo puede tener dos dec
--- fin coincidencia ---

--- coincidencia 3 pos=4056308 ---
ber(p22.stock) || 0) * costo;
      return { id: p22.id, valor, unidades, rotacion: (Number(p22.stock) || 0) > 0 ? unidades / (Number(p22.stock) || 1) : null };
    });
    const total = conValor.reduce((a22, x3) => a22 + x3.valor, 0);
    const mapa = {};
    const detalle = {};
    if (total <= 0) {
      clasificables.forEach((p22) => {
        mapa[p22.id] = "C";
        detalle[p22.id] = { valor: 0, unidades: 0, rotacion: null };
      });
      return { mapa, detalle, baseConsumo };
    }
    const ordenados = [...conValor].sort((a22, b2) => b2.valor - a22.valor);
    let acumulado = 0;
    ordenados.forEach((x3) => {
      acumulado += x3.valor;
      const pct = acumulado / total * 100;
      mapa[x3.id] = pct <= 80 ? "A" : pct <= 95 ? "B" : "C";
      detalle[x3.id] = { valor: x3.valor, unidades: x3.unidades, rotacion: x3.rotacion };
    });
    return { mapa, detalle, baseConsumo };
  }, [productosDelLocalActivo, movimientosDelLocalActivo]);
  const clasificacionABCDelLocalActivo = analisisABCDelLocalActivo.mapa;
  const pedidosPendientes = (0, import_react4.useMemo)(() => pedidos2.filter((p22) => p22.estado !== "Recibido"), [pedidos2]);
  const margenPromedio = (0, import_react4.useMemo)(() => {
    const conPrecio = productos.filter((p22) => Number(p22.precioVenta) > 0);
    if (!conPrecio.length) return 0;
    const total = conPrecio.reduce((acc, p22) => acc + (margenDe(p22) || 0), 0);
    return total / conPrecio.length;
  }, [productos]);
  const margenPorProducto = (0, import_react4.useMemo)(() => {
    const hace90 = /* @__PURE__ */ new Date();
    hace90.setDate(hace90.getDate() - 90);
    return productos.filter((p22) => Number(p22.precioVenta) > 0).map((p22) => {
      const ventas = movimientos.filter(
        (m22) => m22.productoId === p22.id && esVenta(m22) && new Date(m22.fecha) >= hace90
      );
      const unidadesVendidas = ventas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0), 0);
      const ingresos = ventas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0), 0);
      const costes = ventas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.costoUnitario) || 0), 0);
      return { ...p22, unidadesVendidas, ingresos, beneficio: ingresos - costes, margenPct: margenDe(p22) };
    }).filter((p22) => p22.unidadesVendidas > 0).sort((a22, b2) => b2.beneficio - a22.beneficio);
  }, [productos, movimientos]);
  const rotacionPorProducto = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    return productos.map((p22) => {
      const salidas = movimientos.filter((m22) => m22.productoId === p22.id && esSalida(m22));
      const cantSalidas = salidas.reduce((a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0), 0);
      if (salidas.length === 0) {
        return { ...p22, cantSalidas: 0, consumoDiario: null, dias: null, rotacion: null };
      }
      const primera = salidas.reduce((min, m22) => new Date(m22.fecha) < min ? new Date(m22.fecha) : min, new Date(salidas[0].fecha));
      const diasTranscurridos = Math.max(1, Math.round((hoy - primera) / 864e5) + 1);
      const consumoDiario = cantSalidas / diasTranscurridos;
      const dias = consumoDiario > 0 ? (Number(p22.stock) || 0) / consumoDiario : null;
      const rotacion = dias ? 365 / dias : null;
      return { ...p22, cantSalidas, consumoDiario, dias, rotacion, diasConDatos: diasTranscurridos };
    });
  }, [productos, movimientos]);
  const patronesDesviacionConteo = (0, import_react4.useMemo)(() => {
    const porProducto = {};
    movimientos.filter((m22) => {
      if (!m22.productoId) return false;
      if (esMovimientoNuevo(m22)) return m22.tipo === "INVENTARIO" && m22.origen !== "reconciliacionStock";
      return m22.tipo === "ajuste";
    }).forEach((m22) => {
      if (!porProducto[m22.productoId]) porProducto[m22.productoId] = [];
      porProducto[m22.productoId].push(m22);
    });
    const resultado = [];
    Object.entries(porProducto).forEach(([productoId, ajustes]) => {
      if (ajustes.length < 3) return;
      const negativos = ajustes.filter((a22) => Number(a22.cantidad) < 0).length;
      const positivos = ajustes.filter((a22) => Number(a22.cantidad) > 0).length;
      const total = ajustes.length;
      const proporcionMayor = Math.max(negativos, positivos) / total;
      if (proporcionMayor < 0.75) return;
      const prod = productos.find((p22) => p22.id === productoId);
      const acumulado = ajustes.reduce((a22, m22) => a22 + Number(m22.cantidad), 0);
      const ultimaFecha = ajustes.reduce((max, m22) => m22.fecha > max ? m22.fecha : max, ajustes[0].fecha);
      resultado.push({
        productoId,
        nombre: prod ? prod.nombre : "Producto eliminado",
        unidad: prod ? prod.unidad : "",
        direccion: negativos > positivos ? "falta" : "sobra",
        numAjustes: total,
        acumulado,
        ultimaFecha
      });
    });
    return resultado.sort((a22, b2) => Math.abs(b2.acumulado) - Math.abs(a22.acumulado));
  }, [movimientos, productos]);
  function diasHastaProximaEntrega(leadTime, diasReparto) {
    if (!diasReparto || diasReparto.length === 0) return leadTime;
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    for (let n2 = leadTime; n2 <= leadTime + 14; n2++) {
      const fecha = new Date(hoy);
      fecha.setDate(fecha.getDate() + n2);
      if (diasReparto.includes(fecha.getDay())) return n2;
    }
    return leadTime;
  }
  const productosEnPedidoPendiente = (0, import_react4.useMemo)(() => {
    const ids = /* @__PURE__ */ new Set();
    pedidosPendientes.forEach((pe2) => (pe2.items || []).forEach((it2) => ids.add(it2.productoId)));
    return ids;
  }, [pedidosPendientes]);
  const DIAS_SEGURIDAD_PEDIDO = 7;
  const sugerenciasPedido = (0, import_react4.useMemo)(() => {
    return rotacionPorProducto.filter((p22) => !esNoMercancia(p22) && p22.tipo !== "elaborado" && p22.proveedorId && p22.dias !== null).map((p22) => {
      const prov = proveedores.find((pv) => pv.id === p22.proveedorId);
      const leadTime = prov ? Number(prov.leadTime) || 0 : 0;
      const diasHastaEntrega = diasHastaProximaEntrega(leadTime, prov ? prov.diasReparto : []);
      const cantidadCubrir = p22.consumoDiario * (diasHastaEntrega + DIAS_SEGURIDAD_PEDIDO);
      const cantidadSugerida = Math.max(0, Math.ceil(cantidadCubrir - (Number(p22.stock) || 0)));
      const cajasSugeridas = p22.udsPorCaja > 1 ? Math.ceil(cantidadSugerida / p22.udsPorCaja) : null;
      return { ...p22, proveedorNombre: prov ? prov.nombre : "\u2014", leadTime, diasHastaEntrega, margen: p22.dias - diasHastaEntrega, cantidadSugerida, cajasSugeridas };
    }).filter((p22) => p22.leadTime > 0 && p22.margen <= 1 && !productosEnPedidoPendiente.has(p22.id)).sort((a22, b2) => a22.margen - b2.margen);
  }, [rotacionPorProducto, proveedores, productosEnPedidoPendiente]);
  const sugerenciasPedidoDelLocalActivo = (0, import_react4.useMemo)(() => {
    if (!localActivoId) return sugerenciasPedido;
    return sugerenciasPedido.filter((p22) => p22.localId === localActivoId);
  }, [sugerenciasPedido, localActivoId]);
  const lotesCaducidad = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    const filas = [];
    albaranes.forEach((a22) => {
      if (a22.estado !== "confirmado") retu
--- fin coincidencia ---

--- coincidencia 4 pos=4082477 ---
e((a22, f22) => a22 + (f22.pendiente ?? f22.total), 0), [pendientesPago]);
  const promedioDiarioVentas = (0, import_react4.useMemo)(() => {
    const hace30 = /* @__PURE__ */ new Date();
    hace30.setDate(hace30.getDate() - 30);
    const ventas = movimientos.filter((m22) => esVenta(m22) && new Date(m22.fecha) >= hace30);
    const total = ventas.reduce(
      (a22, m22) => a22 + Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0) * (1 + (Number(m22.ivaVentaAplicado) || 0) / 100),
      0
    );
    return total / 30;
  }, [movimientos]);
  const proyeccionTesoreria = (0, import_react4.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    const dias = [];
    for (let i33 = 0; i33 < 30; i33++) {
      const d2 = new Date(hoy);
      d2.setDate(d2.getDate() + i33);
      const fechaISO = aISO(d2);
      const pagosDia = pendientesPago.filter((f22) => f22.vencimiento === fechaISO).reduce((a22, f22) => a22 + (f22.pendiente ?? f22.total), 0);
      const encargosDia = encargos.filter((e2) => e2.estado !== "Entregado" && e2.estado !== "Cancelado" && e2.fechaEntrega === fechaISO).reduce((a22, e2) => {
        const total = (e2.lineas || []).reduce((x3, l22) => x3 + (Number(l22.cantidad) || 0) * (Number(l22.precioUnitario) || 0), 0);
        return a22 + Math.max(0, total - (Number(e2.se\u00F1al) || 0));
      }, 0);
      const ingresosDia = promedioDiarioVentas + encargosDia;
      dias.push({ fecha: fechaISO, ingresosDia, pagosDia, netoDia: ingresosDia - pagosDia });
    }
    return dias;
  }, [pendientesPago, encargos, promedioDiarioVentas]);
  const ingresosPorMes = (0, import_react4.useMemo)(() => {
    const mapa = {};
    movimientos.forEach((m22) => {
      if (!esVenta(m22)) return;
      const key = m22.fecha.slice(0, 7);
      const importe = Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0);
      mapa[key] = (mapa[key] || 0) + importe;
    });
    return mapa;
  }, [movimientos]);
  const gastoPorProveedor = (0, import_react4.useMemo)(() => {
    const map = {};
    pedidos2.forEach((pe2) => {
      const total = pe2.items.reduce((a22, it2) => a22 + it2.cantidad * it2.costoUnitario, 0);
      map[pe2.proveedorId] = (map[pe2.proveedorId] || 0) + total;
    });
    return Object.entries(map).map(([proveedorId, total]) => ({ proveedor: proveedorPorId(proveedorId), total })).sort((a22, b2) => b2.total - a22.total);
  }, [pedidos2, proveedores]);
  const productosSinMovimiento = (0, import_react4.useMemo)(
    () => productos.filter((p22) => !esUtillaje(p22) && !movimientos.some((m22) => m22.productoId === p22.id && esSalida(m22))),
    [productos, movimientos]
  );
  const rotacionPorProductoDelLocalActivo = (0, import_react4.useMemo)(() => rotacionPorProducto.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [rotacionPorProducto, idsProductosDelLocalActivo]);
  const margenPorProductoDelLocalActivo = (0, import_react4.useMemo)(() => margenPorProducto.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [margenPorProducto, idsProductosDelLocalActivo]);
  const productosSinMovimientoDelLocalActivo = (0, import_react4.useMemo)(() => productosSinMovimiento.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [productosSinMovimiento, idsProductosDelLocalActivo]);
  const patronesDesviacionConteoDelLocalActivo = (0, import_react4.useMemo)(() => patronesDesviacionConteo.filter((x3) => idsProductosDelLocalActivo.has(x3.productoId)), [patronesDesviacionConteo, idsProductosDelLocalActivo]);
  const gastoPorProveedorDelLocalActivo = (0, import_react4.useMemo)(() => {
    const map = {};
    pedidosDelLocalActivo.forEach((pe2) => {
      const total = (pe2.items || []).reduce((a22, it2) => a22 + (Number(it2.cantidad) || 0) * (Number(it2.costoUnitario) || 0), 0);
      map[pe2.proveedorId] = (map[pe2.proveedorId] || 0) + total;
    });
    return Object.entries(map).map(([proveedorId, total]) => ({ proveedor: proveedorPorId(proveedorId), total })).sort((a22, b2) => b2.total - a22.total);
  }, [pedidosDelLocalActivo, proveedores]);
  if (!ready) {
    return /* @__PURE__ */ import_react4.default.createElement("div", { style: { background: C2.bg, color: C2.ink }, className: "w-full h-full min-h-[600px] flex items-center justify-center font-sans" }, /* @__PURE__ */ import_react4.default.createElement(LoaderCircle, { className: "animate-spin", size: 22 }), /* @__PURE__ */ import_react4.default.createElement("span", { className: "ml-2 text-sm" }, "Cargando almac\xE9n\u2026"));
  }
  const localPorProductoInforme = new Map(productos.map((p22) => [p22.id, p22.localId || null]));
  const idsLocalesEmpresaInforme = new Set(locales.filter((l22) => l22 && l22.empresaId === empresaDelLocalActivo?.id).map((l22) => l22.id));
  const localEsDeEmpresaInforme = (id) => !!id && idsLocalesEmpresaInforme.has(id);
  const inferirLocalLineasInforme = (lineas) => {
    const ids = [...new Set((lineas || []).map((ln2) => localPorProductoInforme.get(ln2 && ln2.productoId)).filter(Boolean))];
    return ids.length === 1 ? ids[0] : null;
  };
  const productosInforme = localInformeId ? productos.filter((p22) => p22.localId === localInformeId) : productos.filter((p22) => localEsDeEmpresaInforme(p22.localId));
  const movimientosInforme = localInformeId ? movimientos.filter((m22) => (m22.localId || localPorProductoInforme.get(m22.productoId) || null) === localInformeId) : movimientos.filter((m22) => localEsDeEmpresaInforme(m22.localId || localPorProductoInforme.get(m22.productoId) || null));
  const albaranesInforme = localInformeId ? albaranes.filter((a22) => (a22.localId || inferirLocalLineasInforme(a22.lineas)) === localInformeId) : albaranes.filter((a22) => localEsDeEmpresaInforme(a22.localId || inferirLocalLineasInforme(a22.lineas)));
  const facturasDirectasInforme = localInformeId ? facturasDirectas.filter((f22) => f22.localId === localInformeId) : facturasDirectas.filter((f22) => localEsDeEmpresaInforme(f22.localId));
  const gastosGeneralesInforme = (localInformeId ? gastosGenerales.filter((g2) => g2.localId === localInformeId) : gastosGenerales.filter((g2) => localEsDeEmpresaInforme(g2.localId))).filter((g2) => g2.estado !== "ANULADO");
  const empleadosInforme = localInformeId ? empleados.filter((e2) => e2.localId === localInformeId) : empleados.filter((e2) => localEsDeEmpresaInforme(e2.localId));
  const idsEmpleadosInforme = new Set(empleadosInforme.map((e2) => e2.id));
  const fichajesAbiertosInforme = fichajesAbiertos.filter((f22) => idsEmpleadosInforme.has(f22.empleadoId));
  const documentosPersonalProntoInforme = documentosPersonalPronto.filter((d2) => idsEmpleadosInforme.has(d2.empleadoId));
  const pedidosInforme = localInformeId ? pedidos2.filter((pe2) => (pe2.localId || inferirLocalLineasInforme(pe2.items)) === localInformeId) : pedidos2.filter((pe2) => localEsDeEmpresaInforme(pe2.localId || inferirLocalLineasInforme(pe2.items)));
  const encargosInforme = localInformeId ? encargos.filter((e2) => (e2.localId || inferirLocalLineasInforme(e2.lineas)) === localInformeId) : encargos.filter((e2) => localEsDeEmpresaInforme(e2.localId || inferirLocalLineasInforme(e2.lineas)));
  const idsFacturasInforme = /* @__PURE__ */ new Set([...albaranesInforme.map((a22) => a22.id), ...facturasDirectasInforme.map((f22) => f22.id)]);
  const pendientesPagoInforme = pendientesPago.filter((f22) => idsFacturas
--- fin coincidencia ---

--- coincidencia 5 pos=4084077 ---
argos, promedioDiarioVentas]);
  const ingresosPorMes = (0, import_react4.useMemo)(() => {
    const mapa = {};
    movimientos.forEach((m22) => {
      if (!esVenta(m22)) return;
      const key = m22.fecha.slice(0, 7);
      const importe = Math.abs(Number(m22.cantidad) || 0) * (Number(m22.ingresoUnitario) || 0);
      mapa[key] = (mapa[key] || 0) + importe;
    });
    return mapa;
  }, [movimientos]);
  const gastoPorProveedor = (0, import_react4.useMemo)(() => {
    const map = {};
    pedidos2.forEach((pe2) => {
      const total = pe2.items.reduce((a22, it2) => a22 + it2.cantidad * it2.costoUnitario, 0);
      map[pe2.proveedorId] = (map[pe2.proveedorId] || 0) + total;
    });
    return Object.entries(map).map(([proveedorId, total]) => ({ proveedor: proveedorPorId(proveedorId), total })).sort((a22, b2) => b2.total - a22.total);
  }, [pedidos2, proveedores]);
  const productosSinMovimiento = (0, import_react4.useMemo)(
    () => productos.filter((p22) => !esUtillaje(p22) && !movimientos.some((m22) => m22.productoId === p22.id && esSalida(m22))),
    [productos, movimientos]
  );
  const rotacionPorProductoDelLocalActivo = (0, import_react4.useMemo)(() => rotacionPorProducto.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [rotacionPorProducto, idsProductosDelLocalActivo]);
  const margenPorProductoDelLocalActivo = (0, import_react4.useMemo)(() => margenPorProducto.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [margenPorProducto, idsProductosDelLocalActivo]);
  const productosSinMovimientoDelLocalActivo = (0, import_react4.useMemo)(() => productosSinMovimiento.filter((p22) => idsProductosDelLocalActivo.has(p22.id)), [productosSinMovimiento, idsProductosDelLocalActivo]);
  const patronesDesviacionConteoDelLocalActivo = (0, import_react4.useMemo)(() => patronesDesviacionConteo.filter((x3) => idsProductosDelLocalActivo.has(x3.productoId)), [patronesDesviacionConteo, idsProductosDelLocalActivo]);
  const gastoPorProveedorDelLocalActivo = (0, import_react4.useMemo)(() => {
    const map = {};
    pedidosDelLocalActivo.forEach((pe2) => {
      const total = (pe2.items || []).reduce((a22, it2) => a22 + (Number(it2.cantidad) || 0) * (Number(it2.costoUnitario) || 0), 0);
      map[pe2.proveedorId] = (map[pe2.proveedorId] || 0) + total;
    });
    return Object.entries(map).map(([proveedorId, total]) => ({ proveedor: proveedorPorId(proveedorId), total })).sort((a22, b2) => b2.total - a22.total);
  }, [pedidosDelLocalActivo, proveedores]);
  if (!ready) {
    return /* @__PURE__ */ import_react4.default.createElement("div", { style: { background: C2.bg, color: C2.ink }, className: "w-full h-full min-h-[600px] flex items-center justify-center font-sans" }, /* @__PURE__ */ import_react4.default.createElement(LoaderCircle, { className: "animate-spin", size: 22 }), /* @__PURE__ */ import_react4.default.createElement("span", { className: "ml-2 text-sm" }, "Cargando almac\xE9n\u2026"));
  }
  const localPorProductoInforme = new Map(productos.map((p22) => [p22.id, p22.localId || null]));
  const idsLocalesEmpresaInforme = new Set(locales.filter((l22) => l22 && l22.empresaId === empresaDelLocalActivo?.id).map((l22) => l22.id));
  const localEsDeEmpresaInforme = (id) => !!id && idsLocalesEmpresaInforme.has(id);
  const inferirLocalLineasInforme = (lineas) => {
    const ids = [...new Set((lineas || []).map((ln2) => localPorProductoInforme.get(ln2 && ln2.productoId)).filter(Boolean))];
    return ids.length === 1 ? ids[0] : null;
  };
  const productosInforme = localInformeId ? productos.filter((p22) => p22.localId === localInformeId) : productos.filter((p22) => localEsDeEmpresaInforme(p22.localId));
  const movimientosInforme = localInformeId ? movimientos.filter((m22) => (m22.localId || localPorProductoInforme.get(m22.productoId) || null) === localInformeId) : movimientos.filter((m22) => localEsDeEmpresaInforme(m22.localId || localPorProductoInforme.get(m22.productoId) || null));
  const albaranesInforme = localInformeId ? albaranes.filter((a22) => (a22.localId || inferirLocalLineasInforme(a22.lineas)) === localInformeId) : albaranes.filter((a22) => localEsDeEmpresaInforme(a22.localId || inferirLocalLineasInforme(a22.lineas)));
  const facturasDirectasInforme = localInformeId ? facturasDirectas.filter((f22) => f22.localId === localInformeId) : facturasDirectas.filter((f22) => localEsDeEmpresaInforme(f22.localId));
  const gastosGeneralesInforme = (localInformeId ? gastosGenerales.filter((g2) => g2.localId === localInformeId) : gastosGenerales.filter((g2) => localEsDeEmpresaInforme(g2.localId))).filter((g2) => g2.estado !== "ANULADO");
  const empleadosInforme = localInformeId ? empleados.filter((e2) => e2.localId === localInformeId) : empleados.filter((e2) => localEsDeEmpresaInforme(e2.localId));
  const idsEmpleadosInforme = new Set(empleadosInforme.map((e2) => e2.id));
  const fichajesAbiertosInforme = fichajesAbiertos.filter((f22) => idsEmpleadosInforme.has(f22.empleadoId));
  const documentosPersonalProntoInforme = documentosPersonalPronto.filter((d2) => idsEmpleadosInforme.has(d2.empleadoId));
  const pedidosInforme = localInformeId ? pedidos2.filter((pe2) => (pe2.localId || inferirLocalLineasInforme(pe2.items)) === localInformeId) : pedidos2.filter((pe2) => localEsDeEmpresaInforme(pe2.localId || inferirLocalLineasInforme(pe2.items)));
  const encargosInforme = localInformeId ? encargos.filter((e2) => (e2.localId || inferirLocalLineasInforme(e2.lineas)) === localInformeId) : encargos.filter((e2) => localEsDeEmpresaInforme(e2.localId || inferirLocalLineasInforme(e2.lineas)));
  const idsFacturasInforme = /* @__PURE__ */ new Set([...albaranesInforme.map((a22) => a22.id), ...facturasDirectasInforme.map((f22) => f22.id)]);
  const pendientesPagoInforme = pendientesPago.filter((f22) => idsFacturasInforme.has(f22.id));
  const vencenProntoInforme = pendientesPagoInforme.filter((f22) => f22.dias !== null && f22.dias <= 7);
  const totalPendientePagoInforme = pendientesPagoInforme.reduce((a22, f22) => a22 + f22.total, 0);
  const valorInventarioInforme = productosInforme.filter((p22) => !esUtillaje(p22)).reduce((acc, p22) => acc + (Number(p22.stock) || 0) * Number(p22.costo || 0), 0);
  const valorUtillajeInforme = productosInforme.filter(esUtillaje).reduce((acc, p22) => acc + (Number(p22.stock) || 0) * Number(p22.costo || 0), 0);
  const stockBajoInforme = productosInforme.filter((p22) => p22.tipo !== "elaborado" && (p22._pm07Servidor ? p22._pm07BajoMinimo === true : Number(p22.stock) < Number(p22.stockMinimo || 0)));
  const productosConPrecioInforme = productosInforme.filter((p22) => Number(p22.precioVenta) > 0);
  const margenPromedioInforme = productosConPrecioInforme.length ? productosConPrecioInforme.reduce((acc, p22) => acc + (margenDe(p22) || 0), 0) / productosConPrecioInforme.length : 0;
  const pedidosPendientesInforme = pedidosInforme.filter((p22) => p22.estado !== "Recibido");
  const caducanProntoInforme = localInformeId ? caducanPronto.filter((l22) => localPorProductoInforme.get(l22.productoId) === localInformeId) : caducanPronto.filter((l22) => localEsDeEmpresaInforme(localPorProductoInforme.get(l22.productoId)));
  const idsEncargosInforme = new Set(encargosInforme.map((e2) => e2.id));
  const encargosUrgentesInforme = encargosUrgentes.filter((e2) => idsEncargosInforme.has(e2.id));
  const pisoVentaBajoInforme = localInformeId ? pisoVentaBajo.filter((p
--- fin coincidencia ---

--- coincidencia 6 pos=4195550 ---
== 0) {
        const { aplicarMovimientoStock: aplicarMovimientoStock2 } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
        aplicarMovimientoStock2({
          productoId: id,
          cantidad: dif,
          tipo: "INVENTARIO",
          movimientoId: uid(),
          origen: "updateProducto",
          afectaStockTotal: true,
          permitirDeficit: true,
          motivo: "Correcci\xF3n manual de stock"
        });
      }
    }
  }
  function deleteProducto(id) {
    const p22 = productos.find((x3) => x3.id === id);
    if (!productoEsDelLocalActivo(p22)) return false;
    registrarAuditoria("Eliminar producto (borrado l\xF3gico)", p22 ? p22.nombre : id);
    setProductos((s22) => s22.map((pr) => pr.id === id ? { ...pr, activo: false, eliminadoEn: todayISO() } : pr));
  }
  function reactivarProducto(id) {
    const p22 = productos.find((x3) => x3.id === id);
    if (!productoEsDelLocalActivo(p22)) return false;
    registrarAuditoria("Reactivar producto", p22 ? p22.nombre : id);
    setProductos((s22) => s22.map((pr) => pr.id === id ? { ...pr, activo: true, eliminadoEn: null } : pr));
  }
  function registrarSalida(productoId, cantidad, opciones = {}) {
    if (almacenCongelado) return false;
    const cant = Number(cantidad);
    if (!cant || cant <= 0) return false;
    const { motivo = "Venta", precioVentaUnitario = null, referencia = "", medioPago = "Efectivo" } = opciones;
    const prod = productos.find((p22) => p22.id === productoId);
    if (!prod || !productoEsDelLocalActivo(prod)) return false;
    const esVenta2 = motivo === "Venta";
    const esMermaMotivo = MOTIVOS_MERMA.includes(motivo);
    const tipo = esVenta2 ? "VENTA" : esMermaMotivo ? "MERMA" : "OTRO";
    const ingresoUnitario = esVenta2 ? precioVentaUnitario !== null && precioVentaUnitario !== "" ? Number(precioVentaUnitario) || 0 : precioNeto(prod) : 0;
    const r2 = aplicarMovimientoStock({
      productoId,
      cantidad: -cant,
      tipo,
      movimientoId: uid(),
      origen: "registrarSalida",
      afectaStockTotal: true,
      permitirDeficit: true,
      motivo: referencia || motivo,
      camposExtra: {
        costoUnitario: Number(prod.costo) || 0,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago: esVenta2 ? medioPago : null
      }
    });
    return r2.ok;
  }
  return { addProducto, updateProducto, deleteProducto, reactivarProducto, registrarSalida, ajustarProductoPorOtro };
}
function crearLogicaPedidos({ pedidos: pedidos2, setPedidos, productos, setProductos, setMovimientos, almacenCongelado, procesarRecepcion, localActivoId }) {
  function pedidoEsDelLocalActivo(pedido) {
    if (!pedido) return false;
    if (!localActivoId) return true;
    return pedido.localId === localActivoId;
  }
  function crearPedido({ proveedorId, fechaEsperada, items }) {
    const pedido = {
      id: uid(),
      localId: localActivoId || null,
      proveedorId,
      fecha: todayISO(),
      fechaEsperada,
      estado: "Pendiente",
      items: items.map((it2) => ({ ...it2, cantidadRecibida: 0 }))
    };
    setPedidos((s22) => [pedido, ...s22]);
    return pedido;
  }
  function actualizarPedido(pedidoId, { proveedorId, fechaEsperada, items }) {
    const actual = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(actual)) return false;
    setPedidos(
      (s22) => s22.map(
        (pe2) => pe2.id === pedidoId ? { ...pe2, proveedorId, fechaEsperada, items: items.map((it2) => ({ ...it2, cantidadRecibida: 0 })) } : pe2
      )
    );
    return true;
  }
  function eliminarPedido(pedidoId) {
    const actual = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(actual)) return false;
    setPedidos((s22) => s22.filter((pe2) => pe2.id !== pedidoId));
    return true;
  }
  function cerrarPedido(pedidoId) {
    const actual = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(actual)) return false;
    setPedidos((s22) => s22.map((pe2) => pe2.id === pedidoId ? { ...pe2, estado: "Recibido", cerradoManualmente: true } : pe2));
    return true;
  }
  function recibirPedido(pedidoId, lineas) {
    if (almacenCongelado) return false;
    const pedido = pedidos2.find((pe2) => pe2.id === pedidoId);
    if (!pedidoEsDelLocalActivo(pedido)) return false;
    const lineasConDatos = lineas.filter((ln2) => Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || localActivoId && (!prod.localId || prod.localId !== localActivoId)) return null;
      return {
        productoId: ln2.productoId,
        cantidad: Number(ln2.cantidad),
        precioBruto: Number(ln2.precioBruto) || 0,
        importe: (Number(ln2.precioBruto) || 0) * Number(ln2.cantidad),
        ivaPct: Number(ln2.ivaPct) || (prod ? prod.ivaCompra : 10) || 10,
        udsPorCaja: Number(ln2.udsPorCaja) || (prod ? prod.udsPorCaja : 1) || 1,
        descripcion: prod ? prod.nombre : "",
        unidad: prod ? prod.unidad : "unidad"
      };
    }).filter(Boolean);
    if (!lineasConDatos.length) return false;
    const { lineasResueltas, avisos } = procesarRecepcion({
      lineas: lineasConDatos,
      proveedorId: pedido.proveedorId,
      fecha: todayISO(),
      documentoTipo: "pedido",
      documentoId: pedido.id,
      documentoNumero: pedido.id.slice(-6)
    });
    setPedidos(
      (s22) => s22.map((pe2) => {
        if (pe2.id !== pedidoId) return pe2;
        const items = pe2.items.map((it2) => {
          const suyas = lineasResueltas.filter((ln2) => ln2.productoId === it2.productoId);
          if (!suyas.length) return it2;
          const recibidas = suyas.reduce((a22, ln2) => a22 + (Number(ln2.unidadesEntradas) || 0), 0);
          return { ...it2, cantidadRecibida: (Number(it2.cantidadRecibida) || 0) + recibidas };
        });
        const completo = items.every((it2) => (Number(it2.cantidadRecibida) || 0) >= (Number(it2.cantidad) || 0));
        const algo = items.some((it2) => (Number(it2.cantidadRecibida) || 0) > 0);
        return { ...pe2, items, estado: completo ? "Recibido" : algo ? "Parcial" : "Pendiente" };
      })
    );
    return { ok: true, avisos };
  }
  return { crearPedido, actualizarPedido, eliminarPedido, recibirPedido, cerrarPedido };
}
function crearLogicaFichasCosto({ productos, setFichasCosto, localActivoId }) {
  const fichaEsDelLocalActivo = (f22) => !!f22 && (!localActivoId || f22.localId === localActivoId);
  const productoEsDelLocalActivoFicha = (p22) => !!p22 && (!localActivoId || p22.localId === localActivoId);
  function addFichaCosto(data) {
    setFichasCosto((s22) => [...s22, { id: uid(), ...data, localId: localActivoId || data.localId || null }]);
  }
  function updateFichaCosto(id, data) {
    setFichasCosto((s22) => s22.map((f22) => f22.id === id && fichaEsDelLocalActivo(f22) ? { ...f22, ...data, localId: f22.localId || localActivoId || null } : f22));
  }
  function deleteFichaCosto(id) {
    setFichasCosto((s22) => s22.filter((f22) => f22.id !== id || !fichaEsDelLocalActivo(f22)));
  }
  function alergenosDeFicha(f22) {
    const set = new Set(f22.alergenosExtra || []);
    (f22.componentes || []).forEach((c22) => {
      if (!c22.productoId) return;
      const p22 = productos.find((x3) => x3.id === c22.productoId && productoEsDelLocalActivoFi
--- fin coincidencia ---

--- coincidencia 7 pos=4216207 ---
etOrdenesProduccion, registrarAuditoria, localActivoId }) {
  const productoEsDelLocalActivoProduccion = (p22) => !!p22 && (!localActivoId || p22.localId === localActivoId);
  const ordenEsDelLocalActivoProduccion = (o22) => !!o22 && (!localActivoId || o22.localId === localActivoId);
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  function producir({ fichaId, unidadesDeseadas, unidadesBuenas, ingredientesReales, empaqueReal, manoObraReal, gastosGeneralesReal, notas }) {
    const ficha = fichasCosto.find((f22) => f22.id === fichaId);
    if (!ficha) return { ok: false, error: "No se encontr\xF3 la ficha de costo." };
    if (!ficha.productoVinculadoId) return { ok: false, error: "Esta ficha no tiene un producto de venta enlazado todav\xEDa." };
    const elaboradoOriginal = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaboradoOriginal && !productoEsDelLocalActivoProduccion(elaboradoOriginal)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (!elaboradoOriginal) {
      return {
        ok: false,
        error: 'El producto de venta de esta ficha ya no existe (puede que se borrara). Ve a Fichas de costo, edita esta receta y vuelve a enlazarla con un producto, o crea uno nuevo con "Crear producto de venta". No se ha descontado ning\xFAn ingrediente.'
      };
    }
    const buenas = Number(unidadesBuenas) || 0;
    if (buenas <= 0) return { ok: false, error: "Indica cu\xE1ntas unidades buenas salieron." };
    const ordenId = uid();
    const ingredienteFueraDeLocal = ingredientesReales.find((ing) => ing.productoId && Number(ing.cantidadReal) > 0 && !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === ing.productoId)));
    if (ingredienteFueraDeLocal) return { ok: false, error: "La producci\xF3n incluye un ingrediente inexistente o de otro local." };
    let costoIngredientes = 0;
    ingredientesReales.forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const prod = productos.find((p22) => p22.id === ing.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      costoIngredientes += Number(ing.cantidadReal) * costoUnitario;
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: -Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
    });
    const costoLoteTotal = costoIngredientes + (Number(empaqueReal) || 0) + (Number(manoObraReal) || 0) + (Number(gastosGeneralesReal) || 0);
    const costoUnitarioLote = costoLoteTotal / buenas;
    const elaborado = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaborado && !productoEsDelLocalActivoProduccion(elaborado)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (elaborado) {
      const stockAnterior = Number(elaborado.stock) || 0;
      const costoAnterior = Number(elaborado.costo) || 0;
      const stockTotal = stockAnterior + buenas;
      const costoPonderado = stockTotal > 0 ? (stockAnterior * costoAnterior + buenas * costoUnitarioLote) / stockTotal : costoUnitarioLote;
      aplicarMovimientoStock({
        productoId: elaborado.id,
        cantidad: buenas,
        tipo: "PRODUCCION_ENTRADA",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
      setProductos((s22) => s22.map((p22) => p22.id === elaborado.id ? { ...p22, costo: Number(costoPonderado.toFixed(4)) } : p22));
    }
    const orden = {
      id: ordenId,
      localId: localActivoId || elaboradoOriginal.localId || null,
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      fichaId,
      fichaNombre: ficha.nombre,
      productoVinculadoId: ficha.productoVinculadoId,
      unidadesDeseadas: Number(unidadesDeseadas) || 0,
      unidadesBuenas: buenas,
      ingredientes: ingredientesReales.map((i33) => ({ ...i33 })),
      empaqueReal: Number(empaqueReal) || 0,
      manoObraReal: Number(manoObraReal) || 0,
      gastosGeneralesReal: Number(gastosGeneralesReal) || 0,
      costoLoteTotal,
      costoUnitarioLote,
      notas: notas || ""
    };
    setOrdenesProduccion((s22) => [orden, ...s22]);
    registrarAuditoria("Producci\xF3n registrada", `${ficha.nombre} \xB7 ${buenas} unidad(es) \xB7 coste \u20AC${costoUnitarioLote.toFixed(4)}/ud`);
    return { ok: true, orden };
  }
  function anularProduccion(orden) {
    if (!ordenEsDelLocalActivoProduccion(orden)) return { ok: false, error: "La producci\xF3n pertenece a otro local." };
    const idsProductosOrden = [...(orden.ingredientes || []).map((ing) => ing.productoId), orden.productoVinculadoId].filter(Boolean);
    if (idsProductosOrden.some((id) => !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === id)))) return { ok: false, error: "La producci\xF3n contiene productos de otro local." };
    function movimientoOriginalDe(productoId, tipo) {
      return movimientos.find(
        (m22) => (!localActivoId || m22.localId === localActivoId) && (m22.documentoOrigenId === orden.id || m22.ordenId === orden.id) && m22.productoId === productoId && (m22.tipo === tipo || m22.tipo === "salida" || m22.tipo === "entrada")
      );
    }
    (orden.ingredientes || []).forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const original = movimientoOriginalDe(ing.productoId, "PRODUCCION_CONSUMO");
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularProduccion",
        documentoOrigenId: orden.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
      });
    });
    const elaboradoId = orden.productoVinculadoId || (fichasCosto.find((f22) => f22.id === orden.fichaId) || {}).productoVinculadoId;
    if (elaboradoId) {
      const p22 = productos.find((pr) => pr.id === elaboradoId);
      if (p22) {
        const buenas = Number(orden.unidadesBuenas) || 0;
        const stockActual = Number(p22.stock) || 0;
        const costoActual = Number(p22.costo) || 0;
        const costoLote = Number(orden.costoUnitarioLote) || 0;
        const stockRestante = stockActual - buenas;
        const original = movimientoOriginalDe(elaboradoId, "PRODUCCION_ENTRADA");
        aplicarMovimientoStock({
          productoId: elaboradoId,
          cantidad: -buenas,
          tipo: "PRODUCCION_ENTRADA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "anularProduccion",
          documentoOrigenId: orden.id,
          afectaStockTotal: true,
          afectaStockPisoVenta: true,
    
--- fin coincidencia ---

--- coincidencia 8 pos=4216302 ---
duccion = (p22) => !!p22 && (!localActivoId || p22.localId === localActivoId);
  const ordenEsDelLocalActivoProduccion = (o22) => !!o22 && (!localActivoId || o22.localId === localActivoId);
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  function producir({ fichaId, unidadesDeseadas, unidadesBuenas, ingredientesReales, empaqueReal, manoObraReal, gastosGeneralesReal, notas }) {
    const ficha = fichasCosto.find((f22) => f22.id === fichaId);
    if (!ficha) return { ok: false, error: "No se encontr\xF3 la ficha de costo." };
    if (!ficha.productoVinculadoId) return { ok: false, error: "Esta ficha no tiene un producto de venta enlazado todav\xEDa." };
    const elaboradoOriginal = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaboradoOriginal && !productoEsDelLocalActivoProduccion(elaboradoOriginal)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (!elaboradoOriginal) {
      return {
        ok: false,
        error: 'El producto de venta de esta ficha ya no existe (puede que se borrara). Ve a Fichas de costo, edita esta receta y vuelve a enlazarla con un producto, o crea uno nuevo con "Crear producto de venta". No se ha descontado ning\xFAn ingrediente.'
      };
    }
    const buenas = Number(unidadesBuenas) || 0;
    if (buenas <= 0) return { ok: false, error: "Indica cu\xE1ntas unidades buenas salieron." };
    const ordenId = uid();
    const ingredienteFueraDeLocal = ingredientesReales.find((ing) => ing.productoId && Number(ing.cantidadReal) > 0 && !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === ing.productoId)));
    if (ingredienteFueraDeLocal) return { ok: false, error: "La producci\xF3n incluye un ingrediente inexistente o de otro local." };
    let costoIngredientes = 0;
    ingredientesReales.forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const prod = productos.find((p22) => p22.id === ing.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      costoIngredientes += Number(ing.cantidadReal) * costoUnitario;
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: -Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
    });
    const costoLoteTotal = costoIngredientes + (Number(empaqueReal) || 0) + (Number(manoObraReal) || 0) + (Number(gastosGeneralesReal) || 0);
    const costoUnitarioLote = costoLoteTotal / buenas;
    const elaborado = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaborado && !productoEsDelLocalActivoProduccion(elaborado)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (elaborado) {
      const stockAnterior = Number(elaborado.stock) || 0;
      const costoAnterior = Number(elaborado.costo) || 0;
      const stockTotal = stockAnterior + buenas;
      const costoPonderado = stockTotal > 0 ? (stockAnterior * costoAnterior + buenas * costoUnitarioLote) / stockTotal : costoUnitarioLote;
      aplicarMovimientoStock({
        productoId: elaborado.id,
        cantidad: buenas,
        tipo: "PRODUCCION_ENTRADA",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
      setProductos((s22) => s22.map((p22) => p22.id === elaborado.id ? { ...p22, costo: Number(costoPonderado.toFixed(4)) } : p22));
    }
    const orden = {
      id: ordenId,
      localId: localActivoId || elaboradoOriginal.localId || null,
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      fichaId,
      fichaNombre: ficha.nombre,
      productoVinculadoId: ficha.productoVinculadoId,
      unidadesDeseadas: Number(unidadesDeseadas) || 0,
      unidadesBuenas: buenas,
      ingredientes: ingredientesReales.map((i33) => ({ ...i33 })),
      empaqueReal: Number(empaqueReal) || 0,
      manoObraReal: Number(manoObraReal) || 0,
      gastosGeneralesReal: Number(gastosGeneralesReal) || 0,
      costoLoteTotal,
      costoUnitarioLote,
      notas: notas || ""
    };
    setOrdenesProduccion((s22) => [orden, ...s22]);
    registrarAuditoria("Producci\xF3n registrada", `${ficha.nombre} \xB7 ${buenas} unidad(es) \xB7 coste \u20AC${costoUnitarioLote.toFixed(4)}/ud`);
    return { ok: true, orden };
  }
  function anularProduccion(orden) {
    if (!ordenEsDelLocalActivoProduccion(orden)) return { ok: false, error: "La producci\xF3n pertenece a otro local." };
    const idsProductosOrden = [...(orden.ingredientes || []).map((ing) => ing.productoId), orden.productoVinculadoId].filter(Boolean);
    if (idsProductosOrden.some((id) => !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === id)))) return { ok: false, error: "La producci\xF3n contiene productos de otro local." };
    function movimientoOriginalDe(productoId, tipo) {
      return movimientos.find(
        (m22) => (!localActivoId || m22.localId === localActivoId) && (m22.documentoOrigenId === orden.id || m22.ordenId === orden.id) && m22.productoId === productoId && (m22.tipo === tipo || m22.tipo === "salida" || m22.tipo === "entrada")
      );
    }
    (orden.ingredientes || []).forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const original = movimientoOriginalDe(ing.productoId, "PRODUCCION_CONSUMO");
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularProduccion",
        documentoOrigenId: orden.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
      });
    });
    const elaboradoId = orden.productoVinculadoId || (fichasCosto.find((f22) => f22.id === orden.fichaId) || {}).productoVinculadoId;
    if (elaboradoId) {
      const p22 = productos.find((pr) => pr.id === elaboradoId);
      if (p22) {
        const buenas = Number(orden.unidadesBuenas) || 0;
        const stockActual = Number(p22.stock) || 0;
        const costoActual = Number(p22.costo) || 0;
        const costoLote = Number(orden.costoUnitarioLote) || 0;
        const stockRestante = stockActual - buenas;
        const original = movimientoOriginalDe(elaboradoId, "PRODUCCION_ENTRADA");
        aplicarMovimientoStock({
          productoId: elaboradoId,
          cantidad: -buenas,
          tipo: "PRODUCCION_ENTRADA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "anularProduccion",
          documentoOrigenId: orden.id,
          afectaStockTotal: true,
          afectaStockPisoVenta: true,
          permitirDeficit: true,
          revierteMovimientoId: original ? original.id : null,
   
--- fin coincidencia ---

--- coincidencia 9 pos=4216870 ---
rror: "No se encontr\xF3 la ficha de costo." };
    if (!ficha.productoVinculadoId) return { ok: false, error: "Esta ficha no tiene un producto de venta enlazado todav\xEDa." };
    const elaboradoOriginal = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaboradoOriginal && !productoEsDelLocalActivoProduccion(elaboradoOriginal)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (!elaboradoOriginal) {
      return {
        ok: false,
        error: 'El producto de venta de esta ficha ya no existe (puede que se borrara). Ve a Fichas de costo, edita esta receta y vuelve a enlazarla con un producto, o crea uno nuevo con "Crear producto de venta". No se ha descontado ning\xFAn ingrediente.'
      };
    }
    const buenas = Number(unidadesBuenas) || 0;
    if (buenas <= 0) return { ok: false, error: "Indica cu\xE1ntas unidades buenas salieron." };
    const ordenId = uid();
    const ingredienteFueraDeLocal = ingredientesReales.find((ing) => ing.productoId && Number(ing.cantidadReal) > 0 && !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === ing.productoId)));
    if (ingredienteFueraDeLocal) return { ok: false, error: "La producci\xF3n incluye un ingrediente inexistente o de otro local." };
    let costoIngredientes = 0;
    ingredientesReales.forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const prod = productos.find((p22) => p22.id === ing.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      costoIngredientes += Number(ing.cantidadReal) * costoUnitario;
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: -Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
    });
    const costoLoteTotal = costoIngredientes + (Number(empaqueReal) || 0) + (Number(manoObraReal) || 0) + (Number(gastosGeneralesReal) || 0);
    const costoUnitarioLote = costoLoteTotal / buenas;
    const elaborado = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaborado && !productoEsDelLocalActivoProduccion(elaborado)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (elaborado) {
      const stockAnterior = Number(elaborado.stock) || 0;
      const costoAnterior = Number(elaborado.costo) || 0;
      const stockTotal = stockAnterior + buenas;
      const costoPonderado = stockTotal > 0 ? (stockAnterior * costoAnterior + buenas * costoUnitarioLote) / stockTotal : costoUnitarioLote;
      aplicarMovimientoStock({
        productoId: elaborado.id,
        cantidad: buenas,
        tipo: "PRODUCCION_ENTRADA",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
      setProductos((s22) => s22.map((p22) => p22.id === elaborado.id ? { ...p22, costo: Number(costoPonderado.toFixed(4)) } : p22));
    }
    const orden = {
      id: ordenId,
      localId: localActivoId || elaboradoOriginal.localId || null,
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      fichaId,
      fichaNombre: ficha.nombre,
      productoVinculadoId: ficha.productoVinculadoId,
      unidadesDeseadas: Number(unidadesDeseadas) || 0,
      unidadesBuenas: buenas,
      ingredientes: ingredientesReales.map((i33) => ({ ...i33 })),
      empaqueReal: Number(empaqueReal) || 0,
      manoObraReal: Number(manoObraReal) || 0,
      gastosGeneralesReal: Number(gastosGeneralesReal) || 0,
      costoLoteTotal,
      costoUnitarioLote,
      notas: notas || ""
    };
    setOrdenesProduccion((s22) => [orden, ...s22]);
    registrarAuditoria("Producci\xF3n registrada", `${ficha.nombre} \xB7 ${buenas} unidad(es) \xB7 coste \u20AC${costoUnitarioLote.toFixed(4)}/ud`);
    return { ok: true, orden };
  }
  function anularProduccion(orden) {
    if (!ordenEsDelLocalActivoProduccion(orden)) return { ok: false, error: "La producci\xF3n pertenece a otro local." };
    const idsProductosOrden = [...(orden.ingredientes || []).map((ing) => ing.productoId), orden.productoVinculadoId].filter(Boolean);
    if (idsProductosOrden.some((id) => !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === id)))) return { ok: false, error: "La producci\xF3n contiene productos de otro local." };
    function movimientoOriginalDe(productoId, tipo) {
      return movimientos.find(
        (m22) => (!localActivoId || m22.localId === localActivoId) && (m22.documentoOrigenId === orden.id || m22.ordenId === orden.id) && m22.productoId === productoId && (m22.tipo === tipo || m22.tipo === "salida" || m22.tipo === "entrada")
      );
    }
    (orden.ingredientes || []).forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const original = movimientoOriginalDe(ing.productoId, "PRODUCCION_CONSUMO");
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularProduccion",
        documentoOrigenId: orden.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
      });
    });
    const elaboradoId = orden.productoVinculadoId || (fichasCosto.find((f22) => f22.id === orden.fichaId) || {}).productoVinculadoId;
    if (elaboradoId) {
      const p22 = productos.find((pr) => pr.id === elaboradoId);
      if (p22) {
        const buenas = Number(orden.unidadesBuenas) || 0;
        const stockActual = Number(p22.stock) || 0;
        const costoActual = Number(p22.costo) || 0;
        const costoLote = Number(orden.costoUnitarioLote) || 0;
        const stockRestante = stockActual - buenas;
        const original = movimientoOriginalDe(elaboradoId, "PRODUCCION_ENTRADA");
        aplicarMovimientoStock({
          productoId: elaboradoId,
          cantidad: -buenas,
          tipo: "PRODUCCION_ENTRADA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "anularProduccion",
          documentoOrigenId: orden.id,
          afectaStockTotal: true,
          afectaStockPisoVenta: true,
          permitirDeficit: true,
          revierteMovimientoId: original ? original.id : null,
          motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
        });
        if (stockRestante > 0) {
          const costoPrevio = (stockActual * costoActual - buenas * costoLote) / stockRestante;
          setProductos((s22) => s22.map((pr) => pr.id === elaboradoId ? { ...pr, costo: Number(Math.max(0, costoPrevio).toFixed(4)) } : pr));
        }
      }
    }
    setOrdenesProduccion((s22) => s22.filter((o22) => o22.id !== orden.id));
    registrarAuditoria(
      "Anular producci\xF3n",
      `${orden.fichaNombre} \xB7 ${orden.unidadesBuenas} unid
--- fin coincidencia ---

--- coincidencia 10 pos=4217421 ---
 no existe (puede que se borrara). Ve a Fichas de costo, edita esta receta y vuelve a enlazarla con un producto, o crea uno nuevo con "Crear producto de venta". No se ha descontado ning\xFAn ingrediente.'
      };
    }
    const buenas = Number(unidadesBuenas) || 0;
    if (buenas <= 0) return { ok: false, error: "Indica cu\xE1ntas unidades buenas salieron." };
    const ordenId = uid();
    const ingredienteFueraDeLocal = ingredientesReales.find((ing) => ing.productoId && Number(ing.cantidadReal) > 0 && !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === ing.productoId)));
    if (ingredienteFueraDeLocal) return { ok: false, error: "La producci\xF3n incluye un ingrediente inexistente o de otro local." };
    let costoIngredientes = 0;
    ingredientesReales.forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const prod = productos.find((p22) => p22.id === ing.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      costoIngredientes += Number(ing.cantidadReal) * costoUnitario;
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: -Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
    });
    const costoLoteTotal = costoIngredientes + (Number(empaqueReal) || 0) + (Number(manoObraReal) || 0) + (Number(gastosGeneralesReal) || 0);
    const costoUnitarioLote = costoLoteTotal / buenas;
    const elaborado = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaborado && !productoEsDelLocalActivoProduccion(elaborado)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (elaborado) {
      const stockAnterior = Number(elaborado.stock) || 0;
      const costoAnterior = Number(elaborado.costo) || 0;
      const stockTotal = stockAnterior + buenas;
      const costoPonderado = stockTotal > 0 ? (stockAnterior * costoAnterior + buenas * costoUnitarioLote) / stockTotal : costoUnitarioLote;
      aplicarMovimientoStock({
        productoId: elaborado.id,
        cantidad: buenas,
        tipo: "PRODUCCION_ENTRADA",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
      setProductos((s22) => s22.map((p22) => p22.id === elaborado.id ? { ...p22, costo: Number(costoPonderado.toFixed(4)) } : p22));
    }
    const orden = {
      id: ordenId,
      localId: localActivoId || elaboradoOriginal.localId || null,
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      fichaId,
      fichaNombre: ficha.nombre,
      productoVinculadoId: ficha.productoVinculadoId,
      unidadesDeseadas: Number(unidadesDeseadas) || 0,
      unidadesBuenas: buenas,
      ingredientes: ingredientesReales.map((i33) => ({ ...i33 })),
      empaqueReal: Number(empaqueReal) || 0,
      manoObraReal: Number(manoObraReal) || 0,
      gastosGeneralesReal: Number(gastosGeneralesReal) || 0,
      costoLoteTotal,
      costoUnitarioLote,
      notas: notas || ""
    };
    setOrdenesProduccion((s22) => [orden, ...s22]);
    registrarAuditoria("Producci\xF3n registrada", `${ficha.nombre} \xB7 ${buenas} unidad(es) \xB7 coste \u20AC${costoUnitarioLote.toFixed(4)}/ud`);
    return { ok: true, orden };
  }
  function anularProduccion(orden) {
    if (!ordenEsDelLocalActivoProduccion(orden)) return { ok: false, error: "La producci\xF3n pertenece a otro local." };
    const idsProductosOrden = [...(orden.ingredientes || []).map((ing) => ing.productoId), orden.productoVinculadoId].filter(Boolean);
    if (idsProductosOrden.some((id) => !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === id)))) return { ok: false, error: "La producci\xF3n contiene productos de otro local." };
    function movimientoOriginalDe(productoId, tipo) {
      return movimientos.find(
        (m22) => (!localActivoId || m22.localId === localActivoId) && (m22.documentoOrigenId === orden.id || m22.ordenId === orden.id) && m22.productoId === productoId && (m22.tipo === tipo || m22.tipo === "salida" || m22.tipo === "entrada")
      );
    }
    (orden.ingredientes || []).forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const original = movimientoOriginalDe(ing.productoId, "PRODUCCION_CONSUMO");
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularProduccion",
        documentoOrigenId: orden.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
      });
    });
    const elaboradoId = orden.productoVinculadoId || (fichasCosto.find((f22) => f22.id === orden.fichaId) || {}).productoVinculadoId;
    if (elaboradoId) {
      const p22 = productos.find((pr) => pr.id === elaboradoId);
      if (p22) {
        const buenas = Number(orden.unidadesBuenas) || 0;
        const stockActual = Number(p22.stock) || 0;
        const costoActual = Number(p22.costo) || 0;
        const costoLote = Number(orden.costoUnitarioLote) || 0;
        const stockRestante = stockActual - buenas;
        const original = movimientoOriginalDe(elaboradoId, "PRODUCCION_ENTRADA");
        aplicarMovimientoStock({
          productoId: elaboradoId,
          cantidad: -buenas,
          tipo: "PRODUCCION_ENTRADA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "anularProduccion",
          documentoOrigenId: orden.id,
          afectaStockTotal: true,
          afectaStockPisoVenta: true,
          permitirDeficit: true,
          revierteMovimientoId: original ? original.id : null,
          motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
        });
        if (stockRestante > 0) {
          const costoPrevio = (stockActual * costoActual - buenas * costoLote) / stockRestante;
          setProductos((s22) => s22.map((pr) => pr.id === elaboradoId ? { ...pr, costo: Number(Math.max(0, costoPrevio).toFixed(4)) } : pr));
        }
      }
    }
    setOrdenesProduccion((s22) => s22.filter((o22) => o22.id !== orden.id));
    registrarAuditoria(
      "Anular producci\xF3n",
      `${orden.fichaNombre} \xB7 ${orden.unidadesBuenas} unidad(es) del ${orden.fecha}`
    );
    return { ok: true };
  }
  return { producir, anularProduccion };
}
function sincronizarCobroSe\u00F1al(cobros, se\u00F1al, se\u00F1alMedioPago, fecha) {
  const resto = (cobros || []).filter((c22) => c22.concepto !== "Se\xF1al");
  const importe = Number(se\u00F1al) || 0;
  if (importe <= 0) return resto;
  const previa = (cobros || []).find((c22) => c22.concepto === "Se\xF1al");
  return [
    {
      id: previa ? previa.id : uid(),
      concepto: "Se\xF1al",
      importe,
      medioPago: se\u00F1alMedi
--- fin coincidencia ---

--- coincidencia 11 pos=4217455 ---
 Ve a Fichas de costo, edita esta receta y vuelve a enlazarla con un producto, o crea uno nuevo con "Crear producto de venta". No se ha descontado ning\xFAn ingrediente.'
      };
    }
    const buenas = Number(unidadesBuenas) || 0;
    if (buenas <= 0) return { ok: false, error: "Indica cu\xE1ntas unidades buenas salieron." };
    const ordenId = uid();
    const ingredienteFueraDeLocal = ingredientesReales.find((ing) => ing.productoId && Number(ing.cantidadReal) > 0 && !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === ing.productoId)));
    if (ingredienteFueraDeLocal) return { ok: false, error: "La producci\xF3n incluye un ingrediente inexistente o de otro local." };
    let costoIngredientes = 0;
    ingredientesReales.forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const prod = productos.find((p22) => p22.id === ing.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      costoIngredientes += Number(ing.cantidadReal) * costoUnitario;
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: -Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
    });
    const costoLoteTotal = costoIngredientes + (Number(empaqueReal) || 0) + (Number(manoObraReal) || 0) + (Number(gastosGeneralesReal) || 0);
    const costoUnitarioLote = costoLoteTotal / buenas;
    const elaborado = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaborado && !productoEsDelLocalActivoProduccion(elaborado)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (elaborado) {
      const stockAnterior = Number(elaborado.stock) || 0;
      const costoAnterior = Number(elaborado.costo) || 0;
      const stockTotal = stockAnterior + buenas;
      const costoPonderado = stockTotal > 0 ? (stockAnterior * costoAnterior + buenas * costoUnitarioLote) / stockTotal : costoUnitarioLote;
      aplicarMovimientoStock({
        productoId: elaborado.id,
        cantidad: buenas,
        tipo: "PRODUCCION_ENTRADA",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
      setProductos((s22) => s22.map((p22) => p22.id === elaborado.id ? { ...p22, costo: Number(costoPonderado.toFixed(4)) } : p22));
    }
    const orden = {
      id: ordenId,
      localId: localActivoId || elaboradoOriginal.localId || null,
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      fichaId,
      fichaNombre: ficha.nombre,
      productoVinculadoId: ficha.productoVinculadoId,
      unidadesDeseadas: Number(unidadesDeseadas) || 0,
      unidadesBuenas: buenas,
      ingredientes: ingredientesReales.map((i33) => ({ ...i33 })),
      empaqueReal: Number(empaqueReal) || 0,
      manoObraReal: Number(manoObraReal) || 0,
      gastosGeneralesReal: Number(gastosGeneralesReal) || 0,
      costoLoteTotal,
      costoUnitarioLote,
      notas: notas || ""
    };
    setOrdenesProduccion((s22) => [orden, ...s22]);
    registrarAuditoria("Producci\xF3n registrada", `${ficha.nombre} \xB7 ${buenas} unidad(es) \xB7 coste \u20AC${costoUnitarioLote.toFixed(4)}/ud`);
    return { ok: true, orden };
  }
  function anularProduccion(orden) {
    if (!ordenEsDelLocalActivoProduccion(orden)) return { ok: false, error: "La producci\xF3n pertenece a otro local." };
    const idsProductosOrden = [...(orden.ingredientes || []).map((ing) => ing.productoId), orden.productoVinculadoId].filter(Boolean);
    if (idsProductosOrden.some((id) => !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === id)))) return { ok: false, error: "La producci\xF3n contiene productos de otro local." };
    function movimientoOriginalDe(productoId, tipo) {
      return movimientos.find(
        (m22) => (!localActivoId || m22.localId === localActivoId) && (m22.documentoOrigenId === orden.id || m22.ordenId === orden.id) && m22.productoId === productoId && (m22.tipo === tipo || m22.tipo === "salida" || m22.tipo === "entrada")
      );
    }
    (orden.ingredientes || []).forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const original = movimientoOriginalDe(ing.productoId, "PRODUCCION_CONSUMO");
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularProduccion",
        documentoOrigenId: orden.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
      });
    });
    const elaboradoId = orden.productoVinculadoId || (fichasCosto.find((f22) => f22.id === orden.fichaId) || {}).productoVinculadoId;
    if (elaboradoId) {
      const p22 = productos.find((pr) => pr.id === elaboradoId);
      if (p22) {
        const buenas = Number(orden.unidadesBuenas) || 0;
        const stockActual = Number(p22.stock) || 0;
        const costoActual = Number(p22.costo) || 0;
        const costoLote = Number(orden.costoUnitarioLote) || 0;
        const stockRestante = stockActual - buenas;
        const original = movimientoOriginalDe(elaboradoId, "PRODUCCION_ENTRADA");
        aplicarMovimientoStock({
          productoId: elaboradoId,
          cantidad: -buenas,
          tipo: "PRODUCCION_ENTRADA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "anularProduccion",
          documentoOrigenId: orden.id,
          afectaStockTotal: true,
          afectaStockPisoVenta: true,
          permitirDeficit: true,
          revierteMovimientoId: original ? original.id : null,
          motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
        });
        if (stockRestante > 0) {
          const costoPrevio = (stockActual * costoActual - buenas * costoLote) / stockRestante;
          setProductos((s22) => s22.map((pr) => pr.id === elaboradoId ? { ...pr, costo: Number(Math.max(0, costoPrevio).toFixed(4)) } : pr));
        }
      }
    }
    setOrdenesProduccion((s22) => s22.filter((o22) => o22.id !== orden.id));
    registrarAuditoria(
      "Anular producci\xF3n",
      `${orden.fichaNombre} \xB7 ${orden.unidadesBuenas} unidad(es) del ${orden.fecha}`
    );
    return { ok: true };
  }
  return { producir, anularProduccion };
}
function sincronizarCobroSe\u00F1al(cobros, se\u00F1al, se\u00F1alMedioPago, fecha) {
  const resto = (cobros || []).filter((c22) => c22.concepto !== "Se\xF1al");
  const importe = Number(se\u00F1al) || 0;
  if (importe <= 0) return resto;
  const previa = (cobros || []).find((c22) => c22.concepto === "Se\xF1al");
  return [
    {
      id: previa ? previa.id : uid(),
      concepto: "Se\xF1al",
      importe,
      medioPago: se\u00F1alMedioPago || (previa ? previa.medioPag
--- fin coincidencia ---

--- coincidencia 12 pos=4218656 ---
ONSUMO",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
    });
    const costoLoteTotal = costoIngredientes + (Number(empaqueReal) || 0) + (Number(manoObraReal) || 0) + (Number(gastosGeneralesReal) || 0);
    const costoUnitarioLote = costoLoteTotal / buenas;
    const elaborado = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaborado && !productoEsDelLocalActivoProduccion(elaborado)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (elaborado) {
      const stockAnterior = Number(elaborado.stock) || 0;
      const costoAnterior = Number(elaborado.costo) || 0;
      const stockTotal = stockAnterior + buenas;
      const costoPonderado = stockTotal > 0 ? (stockAnterior * costoAnterior + buenas * costoUnitarioLote) / stockTotal : costoUnitarioLote;
      aplicarMovimientoStock({
        productoId: elaborado.id,
        cantidad: buenas,
        tipo: "PRODUCCION_ENTRADA",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
      setProductos((s22) => s22.map((p22) => p22.id === elaborado.id ? { ...p22, costo: Number(costoPonderado.toFixed(4)) } : p22));
    }
    const orden = {
      id: ordenId,
      localId: localActivoId || elaboradoOriginal.localId || null,
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      fichaId,
      fichaNombre: ficha.nombre,
      productoVinculadoId: ficha.productoVinculadoId,
      unidadesDeseadas: Number(unidadesDeseadas) || 0,
      unidadesBuenas: buenas,
      ingredientes: ingredientesReales.map((i33) => ({ ...i33 })),
      empaqueReal: Number(empaqueReal) || 0,
      manoObraReal: Number(manoObraReal) || 0,
      gastosGeneralesReal: Number(gastosGeneralesReal) || 0,
      costoLoteTotal,
      costoUnitarioLote,
      notas: notas || ""
    };
    setOrdenesProduccion((s22) => [orden, ...s22]);
    registrarAuditoria("Producci\xF3n registrada", `${ficha.nombre} \xB7 ${buenas} unidad(es) \xB7 coste \u20AC${costoUnitarioLote.toFixed(4)}/ud`);
    return { ok: true, orden };
  }
  function anularProduccion(orden) {
    if (!ordenEsDelLocalActivoProduccion(orden)) return { ok: false, error: "La producci\xF3n pertenece a otro local." };
    const idsProductosOrden = [...(orden.ingredientes || []).map((ing) => ing.productoId), orden.productoVinculadoId].filter(Boolean);
    if (idsProductosOrden.some((id) => !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === id)))) return { ok: false, error: "La producci\xF3n contiene productos de otro local." };
    function movimientoOriginalDe(productoId, tipo) {
      return movimientos.find(
        (m22) => (!localActivoId || m22.localId === localActivoId) && (m22.documentoOrigenId === orden.id || m22.ordenId === orden.id) && m22.productoId === productoId && (m22.tipo === tipo || m22.tipo === "salida" || m22.tipo === "entrada")
      );
    }
    (orden.ingredientes || []).forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const original = movimientoOriginalDe(ing.productoId, "PRODUCCION_CONSUMO");
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularProduccion",
        documentoOrigenId: orden.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
      });
    });
    const elaboradoId = orden.productoVinculadoId || (fichasCosto.find((f22) => f22.id === orden.fichaId) || {}).productoVinculadoId;
    if (elaboradoId) {
      const p22 = productos.find((pr) => pr.id === elaboradoId);
      if (p22) {
        const buenas = Number(orden.unidadesBuenas) || 0;
        const stockActual = Number(p22.stock) || 0;
        const costoActual = Number(p22.costo) || 0;
        const costoLote = Number(orden.costoUnitarioLote) || 0;
        const stockRestante = stockActual - buenas;
        const original = movimientoOriginalDe(elaboradoId, "PRODUCCION_ENTRADA");
        aplicarMovimientoStock({
          productoId: elaboradoId,
          cantidad: -buenas,
          tipo: "PRODUCCION_ENTRADA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "anularProduccion",
          documentoOrigenId: orden.id,
          afectaStockTotal: true,
          afectaStockPisoVenta: true,
          permitirDeficit: true,
          revierteMovimientoId: original ? original.id : null,
          motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
        });
        if (stockRestante > 0) {
          const costoPrevio = (stockActual * costoActual - buenas * costoLote) / stockRestante;
          setProductos((s22) => s22.map((pr) => pr.id === elaboradoId ? { ...pr, costo: Number(Math.max(0, costoPrevio).toFixed(4)) } : pr));
        }
      }
    }
    setOrdenesProduccion((s22) => s22.filter((o22) => o22.id !== orden.id));
    registrarAuditoria(
      "Anular producci\xF3n",
      `${orden.fichaNombre} \xB7 ${orden.unidadesBuenas} unidad(es) del ${orden.fecha}`
    );
    return { ok: true };
  }
  return { producir, anularProduccion };
}
function sincronizarCobroSe\u00F1al(cobros, se\u00F1al, se\u00F1alMedioPago, fecha) {
  const resto = (cobros || []).filter((c22) => c22.concepto !== "Se\xF1al");
  const importe = Number(se\u00F1al) || 0;
  if (importe <= 0) return resto;
  const previa = (cobros || []).find((c22) => c22.concepto === "Se\xF1al");
  return [
    {
      id: previa ? previa.id : uid(),
      concepto: "Se\xF1al",
      importe,
      medioPago: se\u00F1alMedioPago || (previa ? previa.medioPago : "Efectivo"),
      fecha: previa ? previa.fecha : fecha || todayISO()
    },
    ...resto
  ];
}
function crearLogicaEncargos({ encargos, setEncargos, registrarAuditoria, productos, setProductos, setMovimientos, venderLineas, localActivoId }) {
  function localDeEncargo(e2) {
    if (!e2) return null;
    if (e2.localId) return e2.localId;
    const ids = [...new Set((e2.lineas || []).map((ln2) => productos.find((p22) => p22.id === ln2.productoId)?.localId).filter(Boolean))];
    return ids.length === 1 ? ids[0] : null;
  }
  function encargoEsDelLocalActivo(e2) {
    if (!e2) return false;
    if (!localActivoId) return true;
    return localDeEncargo(e2) === localActivoId;
  }
  function addEncargo(data) {
    if (localActivoId && (data.lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return p22 && p22.localId && p22.localId !== localActivoId;
    })) return false;
    const fecha = todayISO();
    const cobros = sincronizarCobroSe\u00F1al([], data.se\u00F1al, data.se\u00F1alMedioPago, fecha);
    const idsLocalesLineas = [...new Set((data.lineas || []).map((ln2) => productos.find((p22) => p22.id === ln2.productoId)?.local
--- fin coincidencia ---

--- coincidencia 13 pos=4218872 ---
ucci\xF3n: ${ficha.nombre}`
      });
    });
    const costoLoteTotal = costoIngredientes + (Number(empaqueReal) || 0) + (Number(manoObraReal) || 0) + (Number(gastosGeneralesReal) || 0);
    const costoUnitarioLote = costoLoteTotal / buenas;
    const elaborado = productos.find((p22) => p22.id === ficha.productoVinculadoId);
    if (elaborado && !productoEsDelLocalActivoProduccion(elaborado)) return { ok: false, error: "El producto elaborado pertenece a otro local." };
    if (elaborado) {
      const stockAnterior = Number(elaborado.stock) || 0;
      const costoAnterior = Number(elaborado.costo) || 0;
      const stockTotal = stockAnterior + buenas;
      const costoPonderado = stockTotal > 0 ? (stockAnterior * costoAnterior + buenas * costoUnitarioLote) / stockTotal : costoUnitarioLote;
      aplicarMovimientoStock({
        productoId: elaborado.id,
        cantidad: buenas,
        tipo: "PRODUCCION_ENTRADA",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
      setProductos((s22) => s22.map((p22) => p22.id === elaborado.id ? { ...p22, costo: Number(costoPonderado.toFixed(4)) } : p22));
    }
    const orden = {
      id: ordenId,
      localId: localActivoId || elaboradoOriginal.localId || null,
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      fichaId,
      fichaNombre: ficha.nombre,
      productoVinculadoId: ficha.productoVinculadoId,
      unidadesDeseadas: Number(unidadesDeseadas) || 0,
      unidadesBuenas: buenas,
      ingredientes: ingredientesReales.map((i33) => ({ ...i33 })),
      empaqueReal: Number(empaqueReal) || 0,
      manoObraReal: Number(manoObraReal) || 0,
      gastosGeneralesReal: Number(gastosGeneralesReal) || 0,
      costoLoteTotal,
      costoUnitarioLote,
      notas: notas || ""
    };
    setOrdenesProduccion((s22) => [orden, ...s22]);
    registrarAuditoria("Producci\xF3n registrada", `${ficha.nombre} \xB7 ${buenas} unidad(es) \xB7 coste \u20AC${costoUnitarioLote.toFixed(4)}/ud`);
    return { ok: true, orden };
  }
  function anularProduccion(orden) {
    if (!ordenEsDelLocalActivoProduccion(orden)) return { ok: false, error: "La producci\xF3n pertenece a otro local." };
    const idsProductosOrden = [...(orden.ingredientes || []).map((ing) => ing.productoId), orden.productoVinculadoId].filter(Boolean);
    if (idsProductosOrden.some((id) => !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === id)))) return { ok: false, error: "La producci\xF3n contiene productos de otro local." };
    function movimientoOriginalDe(productoId, tipo) {
      return movimientos.find(
        (m22) => (!localActivoId || m22.localId === localActivoId) && (m22.documentoOrigenId === orden.id || m22.ordenId === orden.id) && m22.productoId === productoId && (m22.tipo === tipo || m22.tipo === "salida" || m22.tipo === "entrada")
      );
    }
    (orden.ingredientes || []).forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const original = movimientoOriginalDe(ing.productoId, "PRODUCCION_CONSUMO");
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularProduccion",
        documentoOrigenId: orden.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
      });
    });
    const elaboradoId = orden.productoVinculadoId || (fichasCosto.find((f22) => f22.id === orden.fichaId) || {}).productoVinculadoId;
    if (elaboradoId) {
      const p22 = productos.find((pr) => pr.id === elaboradoId);
      if (p22) {
        const buenas = Number(orden.unidadesBuenas) || 0;
        const stockActual = Number(p22.stock) || 0;
        const costoActual = Number(p22.costo) || 0;
        const costoLote = Number(orden.costoUnitarioLote) || 0;
        const stockRestante = stockActual - buenas;
        const original = movimientoOriginalDe(elaboradoId, "PRODUCCION_ENTRADA");
        aplicarMovimientoStock({
          productoId: elaboradoId,
          cantidad: -buenas,
          tipo: "PRODUCCION_ENTRADA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "anularProduccion",
          documentoOrigenId: orden.id,
          afectaStockTotal: true,
          afectaStockPisoVenta: true,
          permitirDeficit: true,
          revierteMovimientoId: original ? original.id : null,
          motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
        });
        if (stockRestante > 0) {
          const costoPrevio = (stockActual * costoActual - buenas * costoLote) / stockRestante;
          setProductos((s22) => s22.map((pr) => pr.id === elaboradoId ? { ...pr, costo: Number(Math.max(0, costoPrevio).toFixed(4)) } : pr));
        }
      }
    }
    setOrdenesProduccion((s22) => s22.filter((o22) => o22.id !== orden.id));
    registrarAuditoria(
      "Anular producci\xF3n",
      `${orden.fichaNombre} \xB7 ${orden.unidadesBuenas} unidad(es) del ${orden.fecha}`
    );
    return { ok: true };
  }
  return { producir, anularProduccion };
}
function sincronizarCobroSe\u00F1al(cobros, se\u00F1al, se\u00F1alMedioPago, fecha) {
  const resto = (cobros || []).filter((c22) => c22.concepto !== "Se\xF1al");
  const importe = Number(se\u00F1al) || 0;
  if (importe <= 0) return resto;
  const previa = (cobros || []).find((c22) => c22.concepto === "Se\xF1al");
  return [
    {
      id: previa ? previa.id : uid(),
      concepto: "Se\xF1al",
      importe,
      medioPago: se\u00F1alMedioPago || (previa ? previa.medioPago : "Efectivo"),
      fecha: previa ? previa.fecha : fecha || todayISO()
    },
    ...resto
  ];
}
function crearLogicaEncargos({ encargos, setEncargos, registrarAuditoria, productos, setProductos, setMovimientos, venderLineas, localActivoId }) {
  function localDeEncargo(e2) {
    if (!e2) return null;
    if (e2.localId) return e2.localId;
    const ids = [...new Set((e2.lineas || []).map((ln2) => productos.find((p22) => p22.id === ln2.productoId)?.localId).filter(Boolean))];
    return ids.length === 1 ? ids[0] : null;
  }
  function encargoEsDelLocalActivo(e2) {
    if (!e2) return false;
    if (!localActivoId) return true;
    return localDeEncargo(e2) === localActivoId;
  }
  function addEncargo(data) {
    if (localActivoId && (data.lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return p22 && p22.localId && p22.localId !== localActivoId;
    })) return false;
    const fecha = todayISO();
    const cobros = sincronizarCobroSe\u00F1al([], data.se\u00F1al, data.se\u00F1alMedioPago, fecha);
    const idsLocalesLineas = [...new Set((data.lineas || []).map((ln2) => productos.find((p22) => p22.id === ln2.productoId)?.localId).filter(Boolean))];
    const localId = data.localId || (idsLocalesLineas.length === 1 ? idsLocalesLineas[0] : null) || localActivoId || null;
    setEncargos((s22) => [{ id: uid(), estado: "Pendiente", fechaCreac
--- fin coincidencia ---

--- coincidencia 14 pos=4220912 ---
etOrdenesProduccion((s22) => [orden, ...s22]);
    registrarAuditoria("Producci\xF3n registrada", `${ficha.nombre} \xB7 ${buenas} unidad(es) \xB7 coste \u20AC${costoUnitarioLote.toFixed(4)}/ud`);
    return { ok: true, orden };
  }
  function anularProduccion(orden) {
    if (!ordenEsDelLocalActivoProduccion(orden)) return { ok: false, error: "La producci\xF3n pertenece a otro local." };
    const idsProductosOrden = [...(orden.ingredientes || []).map((ing) => ing.productoId), orden.productoVinculadoId].filter(Boolean);
    if (idsProductosOrden.some((id) => !productoEsDelLocalActivoProduccion(productos.find((p22) => p22.id === id)))) return { ok: false, error: "La producci\xF3n contiene productos de otro local." };
    function movimientoOriginalDe(productoId, tipo) {
      return movimientos.find(
        (m22) => (!localActivoId || m22.localId === localActivoId) && (m22.documentoOrigenId === orden.id || m22.ordenId === orden.id) && m22.productoId === productoId && (m22.tipo === tipo || m22.tipo === "salida" || m22.tipo === "entrada")
      );
    }
    (orden.ingredientes || []).forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const original = movimientoOriginalDe(ing.productoId, "PRODUCCION_CONSUMO");
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularProduccion",
        documentoOrigenId: orden.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
      });
    });
    const elaboradoId = orden.productoVinculadoId || (fichasCosto.find((f22) => f22.id === orden.fichaId) || {}).productoVinculadoId;
    if (elaboradoId) {
      const p22 = productos.find((pr) => pr.id === elaboradoId);
      if (p22) {
        const buenas = Number(orden.unidadesBuenas) || 0;
        const stockActual = Number(p22.stock) || 0;
        const costoActual = Number(p22.costo) || 0;
        const costoLote = Number(orden.costoUnitarioLote) || 0;
        const stockRestante = stockActual - buenas;
        const original = movimientoOriginalDe(elaboradoId, "PRODUCCION_ENTRADA");
        aplicarMovimientoStock({
          productoId: elaboradoId,
          cantidad: -buenas,
          tipo: "PRODUCCION_ENTRADA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "anularProduccion",
          documentoOrigenId: orden.id,
          afectaStockTotal: true,
          afectaStockPisoVenta: true,
          permitirDeficit: true,
          revierteMovimientoId: original ? original.id : null,
          motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
        });
        if (stockRestante > 0) {
          const costoPrevio = (stockActual * costoActual - buenas * costoLote) / stockRestante;
          setProductos((s22) => s22.map((pr) => pr.id === elaboradoId ? { ...pr, costo: Number(Math.max(0, costoPrevio).toFixed(4)) } : pr));
        }
      }
    }
    setOrdenesProduccion((s22) => s22.filter((o22) => o22.id !== orden.id));
    registrarAuditoria(
      "Anular producci\xF3n",
      `${orden.fichaNombre} \xB7 ${orden.unidadesBuenas} unidad(es) del ${orden.fecha}`
    );
    return { ok: true };
  }
  return { producir, anularProduccion };
}
function sincronizarCobroSe\u00F1al(cobros, se\u00F1al, se\u00F1alMedioPago, fecha) {
  const resto = (cobros || []).filter((c22) => c22.concepto !== "Se\xF1al");
  const importe = Number(se\u00F1al) || 0;
  if (importe <= 0) return resto;
  const previa = (cobros || []).find((c22) => c22.concepto === "Se\xF1al");
  return [
    {
      id: previa ? previa.id : uid(),
      concepto: "Se\xF1al",
      importe,
      medioPago: se\u00F1alMedioPago || (previa ? previa.medioPago : "Efectivo"),
      fecha: previa ? previa.fecha : fecha || todayISO()
    },
    ...resto
  ];
}
function crearLogicaEncargos({ encargos, setEncargos, registrarAuditoria, productos, setProductos, setMovimientos, venderLineas, localActivoId }) {
  function localDeEncargo(e2) {
    if (!e2) return null;
    if (e2.localId) return e2.localId;
    const ids = [...new Set((e2.lineas || []).map((ln2) => productos.find((p22) => p22.id === ln2.productoId)?.localId).filter(Boolean))];
    return ids.length === 1 ? ids[0] : null;
  }
  function encargoEsDelLocalActivo(e2) {
    if (!e2) return false;
    if (!localActivoId) return true;
    return localDeEncargo(e2) === localActivoId;
  }
  function addEncargo(data) {
    if (localActivoId && (data.lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return p22 && p22.localId && p22.localId !== localActivoId;
    })) return false;
    const fecha = todayISO();
    const cobros = sincronizarCobroSe\u00F1al([], data.se\u00F1al, data.se\u00F1alMedioPago, fecha);
    const idsLocalesLineas = [...new Set((data.lineas || []).map((ln2) => productos.find((p22) => p22.id === ln2.productoId)?.localId).filter(Boolean))];
    const localId = data.localId || (idsLocalesLineas.length === 1 ? idsLocalesLineas[0] : null) || localActivoId || null;
    setEncargos((s22) => [{ id: uid(), estado: "Pendiente", fechaCreacion: fecha, cobros, ...data, localId }, ...s22]);
  }
  function updateEncargo(id, data) {
    const actual = encargos.find((e2) => e2.id === id);
    if (!encargoEsDelLocalActivo(actual)) return false;
    setEncargos(
      (s22) => s22.map((e2) => {
        if (e2.id !== id) return e2;
        const actualizado = { ...e2, ...data };
        if ("se\xF1al" in data || "se\xF1alMedioPago" in data) {
          actualizado.cobros = sincronizarCobroSe\u00F1al(e2.cobros, actualizado.se\u00F1al, actualizado.se\u00F1alMedioPago, e2.fechaCreacion);
        }
        return actualizado;
      })
    );
  }
  function deleteEncargo(id) {
    const actual = encargos.find((e22) => e22.id === id);
    if (!encargoEsDelLocalActivo(actual)) return false;
    const e2 = encargos.find((x3) => x3.id === id);
    registrarAuditoria("Eliminar encargo", e2 ? `${e2.numero || "s/n"}` : id);
    setEncargos((s22) => s22.filter((e22) => e22.id !== id));
  }
  function entregarEncargo(encargo, medioPago = "Efectivo") {
    const actual = encargos.find((e2) => e2.id === encargo);
    if (!encargoEsDelLocalActivo(actual)) return false;
    const lineasParaVender = (encargo.lineas || []).map((ln2) => ({
      productoId: ln2.productoId,
      cantidad: ln2.cantidad,
      precioUnitario: ln2.precioUnitario
    }));
    venderLineas(lineasParaVender, {
      tipo: "VENTA_ENCARGO",
      medioPago,
      origen: "entregarEncargo",
      documentoOrigenId: encargo.id,
      motivoBase: `Encargo ${encargo.numero || ""}`.trim(),
      extraPorLinea: () => ({ encargoId: encargo.id, clienteId: encargo.clienteId })
    });
    const total = (encargo.lineas || []).reduce((a22, l22) => a22 + (Number(l22.cantidad) || 0) * (Number(l22.precioUnitario) || 0), 0);
    const se\u00F1al = Number(encargo.se\u00F1al) || 0;
    const resto = Math.max(0, total - se\u00F1al);
    const cobrosSinResto = (encargo.cobros || []).filter((c22) => c22.concepto !== "Resto entrega");
    const cobros = resto > 9e-3 ? [...cobrosSinResto, { id: uid(), concepto: "Re
--- fin coincidencia ---

--- coincidencia 15 pos=4228496 ---
);
  }
  return { addEncargo, updateEncargo, deleteEncargo, entregarEncargo };
}
function crearLogicaVenta({ productos, setProductos, movimientos, setMovimientos, arqueos, localActivoId }) {
  function productoEsDelLocalActivoVenta(prod) {
    if (!prod) return false;
    if (!localActivoId) return true;
    return !prod.localId || prod.localId === localActivoId;
  }
  function movimientoEsDelLocalActivoVenta(m22) {
    if (!localActivoId) return true;
    if (m22.localId) return m22.localId === localActivoId;
    const prod = productos.find((p22) => p22.id === m22.productoId);
    return !!prod && (!prod.localId || prod.localId === localActivoId);
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function venderLineas(lineas, opciones = {}) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const {
      tipo = "VENTA",
      medioPago = "Efectivo",
      detallePago = null,
      operationId = null,
      origen = "venderLocal",
      documentoOrigenId = null,
      motivoBase = "TPV",
      extraPorLinea = () => ({})
    } = opciones;
    const ventaId = operationId || uid();
    let totalVenta = 0;
    if (detallePago) {
      (lineas || []).forEach((ln2) => {
        const cant = Number(ln2.cantidad) || 0;
        const prod = productos.find((p22) => p22.id === ln2.productoId);
        if (!prod || cant <= 0) return;
        const precio = Number(ln2.precioUnitario) || precioNeto(prod);
        totalVenta += cant * precio * (1 + ivaDe(prod) / 100);
      });
    }
    const movimientosGenerados = [];
    (lineas || []).forEach((ln2) => {
      if (!ln2.productoId) return;
      const cant = Number(ln2.cantidad) || 0;
      if (cant <= 0) return;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      const ingresoUnitario = Number(ln2.precioUnitario) || precioNeto(prod);
      const camposExtra = {
        costoUnitario,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago,
        ...extraPorLinea(ln2, prod)
      };
      if (detallePago && totalVenta > 0) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaDe(prod) / 100);
        const proporcion = importeLinea / totalVenta;
        camposExtra.detallePago = {
          efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)),
          tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4))
        };
      }
      const r2 = aplicarMovimientoStock({
        productoId: prod.id,
        cantidad: -cant,
        tipo,
        operationId: ventaId,
        movimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para regi
--- fin coincidencia ---

--- coincidencia 16 pos=4228651 ---
ientos, arqueos, localActivoId }) {
  function productoEsDelLocalActivoVenta(prod) {
    if (!prod) return false;
    if (!localActivoId) return true;
    return !prod.localId || prod.localId === localActivoId;
  }
  function movimientoEsDelLocalActivoVenta(m22) {
    if (!localActivoId) return true;
    if (m22.localId) return m22.localId === localActivoId;
    const prod = productos.find((p22) => p22.id === m22.productoId);
    return !!prod && (!prod.localId || prod.localId === localActivoId);
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function venderLineas(lineas, opciones = {}) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const {
      tipo = "VENTA",
      medioPago = "Efectivo",
      detallePago = null,
      operationId = null,
      origen = "venderLocal",
      documentoOrigenId = null,
      motivoBase = "TPV",
      extraPorLinea = () => ({})
    } = opciones;
    const ventaId = operationId || uid();
    let totalVenta = 0;
    if (detallePago) {
      (lineas || []).forEach((ln2) => {
        const cant = Number(ln2.cantidad) || 0;
        const prod = productos.find((p22) => p22.id === ln2.productoId);
        if (!prod || cant <= 0) return;
        const precio = Number(ln2.precioUnitario) || precioNeto(prod);
        totalVenta += cant * precio * (1 + ivaDe(prod) / 100);
      });
    }
    const movimientosGenerados = [];
    (lineas || []).forEach((ln2) => {
      if (!ln2.productoId) return;
      const cant = Number(ln2.cantidad) || 0;
      if (cant <= 0) return;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      const ingresoUnitario = Number(ln2.precioUnitario) || precioNeto(prod);
      const camposExtra = {
        costoUnitario,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago,
        ...extraPorLinea(ln2, prod)
      };
      if (detallePago && totalVenta > 0) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaDe(prod) / 100);
        const proporcion = importeLinea / totalVenta;
        camposExtra.detallePago = {
          efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)),
          tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4))
        };
      }
      const r2 = aplicarMovimientoStock({
        productoId: prod.id,
        cantidad: -cant,
        tipo,
        operationId: ventaId,
        movimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
 
--- fin coincidencia ---

--- coincidencia 17 pos=4231180 ---
mposExtra.detallePago = {
          efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)),
          tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4))
        };
      }
      const r2 = aplicarMovimientoStock({
        productoId: prod.id,
        cantidad: -cant,
        tipo,
        operationId: ventaId,
        movimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      return { ok: true, n: lineasParaRpc.length, modo: "pm07-servidor", replayed: !!r2.data?.replayed, ventaId };
    } catch (error) {
      return { ok: false, ...errorVisible(error) };
    }
  }
  function anularVentaLocal(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m22) => m22.anulaVentaId === ventaId || m22.revierteMovimientoId && lineas.some((l22) => l22.id === m22.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find(
--- fin coincidencia ---

--- coincidencia 18 pos=4231508 ---
taId,
        movimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: false,
        motivo: motivoBase,
        camposExtra
      });
      if (r2.ok) movimientosGenerados.push(r2.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "TPV" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para abrir el TPV." };
    const incluyeOtroLocal = (lineas || []).some((ln2) => {
      const p22 = productos.find((x3) => x3.id === ln2.productoId);
      return !!p22 && !productoEsDelLocalActivoVenta(p22);
    });
    if (incluyeOtroLocal) return { ok: false, error: "La venta incluye productos de otro local." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return venderLocal(lineas, medioPago, detallePago);
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln2) => {
      const cant = Number(ln2.cantidad) || 0;
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln2) => ln2.productoId && Number(ln2.cantidad) > 0).map((ln2) => {
      const prod = productos.find((p22) => p22.id === ln2.productoId);
      const cant = Number(ln2.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = { productoId: ln2.productoId, localId: prod ? prod.localId || localActivoId || null : localActivoId || null, cantidad: cant, motivo: "Venta", costoUnitario, ingresoUnitario, ivaVentaAplicado: ivaAplicado, medioPago, referencia: "TPV", ventaId };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = { efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)), tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4)) };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    const empresas = [...new Set(lineasParaRpc.map((l22) => productos.find((p22) => p22.id === l22.productoId)?.empresaId).filter(Boolean))];
    if (empresas.length !== 1) return { ok: false, error: "No se pudo determinar una \xFAnica empresa para la venta." };
    const empresaId = empresas[0];
    const params = { p_operation_id: ventaId, p_empresa_id: empresaId, p_local_id: localActivoId, p_lineas: lineasParaRpc, p_datos: { medioPago, detallePago: detallePago || null } };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r2 = await Promise.race([
        supabase.rpc("registrar_venta_stock_carrito", params),
        new Promise((_22, reject) => setTimeout(() => reject(new Error("El servidor tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r2.error) throw r2.error;
      return r2;
    }
    function errorVisible(error) {
      const msg = String(error?.message || error || "");
      if (msg.includes("stock_insuficiente:")) return { error: "Stock insuficiente", productoId: msg.split("stock_insuficiente:").pop().trim() };
      if (msg.includes("unidad_indivisible")) return { error: "La cantidad debe ser un n\xFAmero entero para este producto." };
      if (msg.includes("precision_cantidad_excedida")) return { error: "La cantidad tiene m\xE1s decimales de los permitidos." };
      if (msg.includes("local_inactivo")) return { error: "El local ya no admite operaciones de stock." };
      if (msg.includes("contexto_no_autorizado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      return { ok: true, n: lineasParaRpc.length, modo: "pm07-servidor", replayed: !!r2.data?.replayed, ventaId };
    } catch (error) {
      return { ok: false, ...errorVisible(error) };
    }
  }
  function anularVentaLocal(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m22) => m22.anulaVentaId === ventaId || m22.revierteMovimientoId && lineas.some((l22) => l22.id === m22.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
    if (arqueoDelDia && typeof window !== "undefined" && window.__nubeActiva) {
      fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
        method: "POST",
        headers: { "Content-Type": "applicat
--- fin coincidencia ---

--- coincidencia 19 pos=4235817 ---
izado") || msg.includes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      return { ok: true, n: lineasParaRpc.length, modo: "pm07-servidor", replayed: !!r2.data?.replayed, ventaId };
    } catch (error) {
      return { ok: false, ...errorVisible(error) };
    }
  }
  function anularVentaLocal(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m22) => m22.anulaVentaId === ventaId || m22.revierteMovimientoId && lineas.some((l22) => l22.id === m22.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
    if (arqueoDelDia && typeof window !== "undefined" && window.__nubeActiva) {
      fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          titulo: "Caja cerrada, ahora desactualizada",
          cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`,
          localId: lineas[0]?.localId || null,
          url: "/"
        })
      }).catch(() => {
      });
    }
    return { ok: true, lineasAnuladas: aplicadas, arqueoAfectado: !!arqueoDelDia };
  }
  async function anularVenta(ventaId, movimientosActuales, motivo = "") {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para gestionar ventas del TPV." };
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return anularVentaLocal(ventaId, movimientosActuales, motivo);
    const lineasOriginales = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineasOriginales.length === 0) return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    try {
      const supabase = await window.getSupabaseClient();
      const operationId = `rev-${ventaId}`;
      const r2 = await supabase.rpc("revertir_venta_stock_carrito", { p_operation_id: operationId, p_venta_operation_id: ventaId, p_motivo: motivo || "" });
      if (r2.error) throw r2.error;
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      const fechaVenta = lineasOriginales[0]?.fecha;
      const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
      if (arqueoDelDia) {
        fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ titulo: "Caja cerrada, ahora desactualizada", cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`, localId: r2.data?.localId || lineasOriginales[0]?.localId || null, url: "/" })
        }).catch(() => {
        });
      }
      return { ok: true, lineasAnuladas: Number(r2.data?.lineasAnuladas) || 0, arqueoAfectado: !!arqueoDelDia, modo: "pm07-servidor", sincronizada: true, replayed: !!r2.data?.replayed };
    } catch (error) {
      const msg = String(error?.message || "");
      if (msg.includes("venta_stock_ya_revertida")) return { ok: false, error: "La venta ya estaba anulada.", yaExistia: true };
      return { ok: false, error: "No se pudo confirmar la anulaci\xF3n con el servidor. No se ha modificado el stock local." };
    }
  }
  return { venderCarrito, venderLocal, anularVenta, venderLineas };
}
function crearLogicaTraspasos({ productos, setProductos, movimientos, setMovimientos, setTraspasos, registrarAuditoria, localActivoId, locales = [] }) {
  function productoEsDelLocalActivoTraspaso(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  async function traspasarStock(productoId, cantidad, direccion) {
    const cant = Number(cantidad) || 0;
    if (cant <= 0) return { ok: false, error: "Indica una cantidad mayor que cero." };
    const prod = productos.find((p22) => p22.id === productoId);
    if (!prod) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoTraspaso(prod)) return { ok: false, error: "Ese producto no pertenece al local activo." };
    const enPiso = Number(prod.stockPisoVenta) || 0;
    const total = Number(prod.stock) || 0;
    const enAlmacen = Math.max(0, total - enPiso);
    if (direccion === "a_piso" && cant > enAlmacen) {
      return { ok: false, error: `Solo hay ${fmt(enAlmacen)} ${prod.unidad} en el almac\xE9n.` };
    }
    if (direccion === "a_almacen" && cant > enPiso) {
      r
--- fin coincidencia ---

--- coincidencia 20 pos=4235836 ---
udes("stock_no_autorizado")) return { error: "No tienes permiso para registrar esta venta en este local." };
      return { error: "No se pudo confirmar la venta con el servidor. No se ha descontado stock localmente." };
    }
    try {
      let r2;
      try {
        r2 = await intentarRpc(6e3);
      } catch (e1) {
        r2 = await intentarRpc(4e3);
      }
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      return { ok: true, n: lineasParaRpc.length, modo: "pm07-servidor", replayed: !!r2.data?.replayed, ventaId };
    } catch (error) {
      return { ok: false, ...errorVisible(error) };
    }
  }
  function anularVentaLocal(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m22) => m22.anulaVentaId === ventaId || m22.revierteMovimientoId && lineas.some((l22) => l22.id === m22.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l22) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l22));
      if (cantidadOriginal <= 0) return;
      const r2 = aplicarMovimientoStock({
        productoId: l22.productoId,
        cantidad: cantidadOriginal,
        tipo: l22.tipo === "salida" ? "VENTA" : l22.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l22.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l22.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l22.ingresoUnitario != null ? -Math.abs(Number(l22.ingresoUnitario)) : null,
          ivaVentaAplicado: l22.ivaVentaAplicado,
          medioPago: l22.medioPago,
          detallePago: l22.detallePago
        }
      });
      if (r2.ok) aplicadas++;
    });
    const fechaVenta = lineas[0]?.fecha;
    const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
    if (arqueoDelDia && typeof window !== "undefined" && window.__nubeActiva) {
      fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          titulo: "Caja cerrada, ahora desactualizada",
          cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`,
          localId: lineas[0]?.localId || null,
          url: "/"
        })
      }).catch(() => {
      });
    }
    return { ok: true, lineasAnuladas: aplicadas, arqueoAfectado: !!arqueoDelDia };
  }
  async function anularVenta(ventaId, movimientosActuales, motivo = "") {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para gestionar ventas del TPV." };
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return anularVentaLocal(ventaId, movimientosActuales, motivo);
    const lineasOriginales = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineasOriginales.length === 0) return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    try {
      const supabase = await window.getSupabaseClient();
      const operationId = `rev-${ventaId}`;
      const r2 = await supabase.rpc("revertir_venta_stock_carrito", { p_operation_id: operationId, p_venta_operation_id: ventaId, p_motivo: motivo || "" });
      if (r2.error) throw r2.error;
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      const fechaVenta = lineasOriginales[0]?.fecha;
      const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
      if (arqueoDelDia) {
        fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ titulo: "Caja cerrada, ahora desactualizada", cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`, localId: r2.data?.localId || lineasOriginales[0]?.localId || null, url: "/" })
        }).catch(() => {
        });
      }
      return { ok: true, lineasAnuladas: Number(r2.data?.lineasAnuladas) || 0, arqueoAfectado: !!arqueoDelDia, modo: "pm07-servidor", sincronizada: true, replayed: !!r2.data?.replayed };
    } catch (error) {
      const msg = String(error?.message || "");
      if (msg.includes("venta_stock_ya_revertida")) return { ok: false, error: "La venta ya estaba anulada.", yaExistia: true };
      return { ok: false, error: "No se pudo confirmar la anulaci\xF3n con el servidor. No se ha modificado el stock local." };
    }
  }
  return { venderCarrito, venderLocal, anularVenta, venderLineas };
}
function crearLogicaTraspasos({ productos, setProductos, movimientos, setMovimientos, setTraspasos, registrarAuditoria, localActivoId, locales = [] }) {
  function productoEsDelLocalActivoTraspaso(prod) {
    if (!prod) return false;
    if (!localActivoId) return false;
    return prod.localId === localActivoId;
  }
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  async function traspasarStock(productoId, cantidad, direccion) {
    const cant = Number(cantidad) || 0;
    if (cant <= 0) return { ok: false, error: "Indica una cantidad mayor que cero." };
    const prod = productos.find((p22) => p22.id === productoId);
    if (!prod) return { ok: false, error: "Producto no encontrado." };
    if (!productoEsDelLocalActivoTraspaso(prod)) return { ok: false, error: "Ese producto no pertenece al local activo." };
    const enPiso = Number(prod.stockPisoVenta) || 0;
    const total = Number(prod.stock) || 0;
    const enAlmacen = Math.max(0, total - enPiso);
    if (direccion === "a_piso" && cant > enAlmacen) {
      return { ok: false, error: `Solo hay ${fmt(enAlmacen)} ${prod.unidad} en el almac\xE9n.` };
    }
    if (direccion === "a_almacen" && cant > enPiso) {
      return { ok: false, 
--- fin coincidencia ---

PATRON 'ventaOperationId': 0 coincidencias
PATRON 'movimiento_original_id': 2 coincidencias
--- coincidencia 1 pos=3987119 ---
i = /* @__PURE__ */ new Date(`${entrada.fecha}T${entrada.hora}:00`);
      const fin = /* @__PURE__ */ new Date(`${f22.fecha}T${f22.hora}:00`);
      const horas = (fin - ini) / 36e5;
      if (horas > 0 && horas < 24) total += horas;
      entrada = null;
    }
  });
  return total;
}
function costePorHoraEquipoEnMes(nominas, fichajes, mes) {
  const nominasDelMes = nominas.filter((n2) => n2.mes === mes);
  const costeTotalMes = nominasDelMes.reduce((a22, n2) => a22 + (Number(n2.costeTotalEmpresa) || 0), 0);
  const horasTotalesMes = nominasDelMes.reduce((a22, n2) => a22 + horasDeEmpleadoEnMes(fichajes, n2.empleadoId, mes), 0);
  return horasTotalesMes > 0 ? costeTotalMes / horasTotalesMes : null;
}
async function loadKey(key, fallback, retries = 2) {
  for (let attempt = 0; attempt <= retries; attempt++) {
    try {
      const res = await window.storage.get(key, false);
      if (res && res.value) return JSON.parse(res.value);
      return fallback;
    } catch (e2) {
      if (attempt === retries) {
        console.error("No se pudo cargar", key, "tras varios intentos:", e2);
        return fallback;
      }
      await new Promise((r2) => setTimeout(r2, 350));
    }
  }
  return fallback;
}
async function saveKey(key, value) {
  try {
    await window.storage.set(key, JSON.stringify(value), false);
  } catch (e2) {
    console.error("Error guardando", key, e2);
    if (typeof window !== "undefined" && window.dispatchEvent) {
      window.dispatchEvent(new CustomEvent("fallo-guardado", { detail: { key, mensaje: e2 && e2.message } }));
    }
  }
}
async function sincronizarStockPm07({ setProductos, setMovimientos, localActivoId = null }) {
  if (typeof window === "undefined" || !window.__nubeActiva || typeof window.getSupabaseClient !== "function") return { ok: false, offline: true };
  const supabase = await window.getSupabaseClient();
  let qStock = supabase.from("stock_estado").select("empresa_id,local_id,producto_id,almacen,piso,total,minimo,bajo_minimo,fraccionable,precision_cantidad,local_operable");
  let qMov = supabase.from("movimientos_stock").select("id,operation_id,tipo,empresa_id,local_id,producto_id,delta_almacen,delta_piso,delta_total,cantidad,movimiento_original_id,datos,actor_user_id,created_at").order("created_at", { ascending: false }).limit(2e3);
  const [rStock, rMov] = await Promise.all([qStock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : null;
      const st2 = exacto || activo;
      if (!st2) return prod;
      return { ...prod, stock: Number(st2.total) || 0, stockPisoVenta: Number(st2.piso) || 0, stockMinimo: Number(st2.minimo) || 0, _pm07BajoMinimo: !!st2.bajo_minimo, _pm07Servidor: true, _pm07LocalOperable: st2.local_operable !== false };
    }));
  }
  if (typeof setMovimientos === "function") {
    const server = movs.map((m4) => {
      const d2 = m4.datos && typeof m4.datos === "object" ? m4.datos : {};
      const delta = Number(m4.delta_total) || 0;
      const creado = String(m4.created_at || "");
      return {
        id: `pm07-${m4.id}`,
        operationId: m4.operation_id,
        ventaId: d2.ventaId || (m4.tipo === "VENTA" ? m4.operation_id : d2.anulaVentaId || null),
        anulaVentaId: d2.anulaVentaId || null,
        productoId: m4.producto_id,
        localId: m4.local_id,
        empresaId: m4.empresa_id,
        cantidad: delta,
        cantidadFisica: Number(m4.cantidad) || Math.abs(delta),
        tipo: m4.tipo,
        motivo: d2.motivo || m4.tipo,
        referencia: d2.referencia || "PM-07",
        costoUnitario: Number(d2.costoUnitario) || 0,
        ingresoUnitario: Number(d2.ingresoUnitario) || 0,
        ivaVentaAplicado: Number(d2.ivaVentaAplicado) || 0,
        medioPago: d2.medioPago || null,
        detallePago: d2.detallePago || null,
        fecha: creado ? creado.slice(0, 10) : todayISO(),
        hora: creado ? creado.slice(11, 16) : (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
        afectaStockTotal: delta !== 0,
        afectaStockPisoVenta: Number(m4.delta_piso) !== 0,
        movimientoOriginalId: m4.movimiento_original_id || null,
        _pm07Servidor: true
      };
    });
    setMovimientos((prev) => [...server, ...(prev || []).filter((m4) => !m4._pm07Servidor)]);
  }
  return { ok: true, stocks: stocks.length, movimientos: movs.length };
}
function redondearDineroPM08(valor) {
  const n2 = Number(valor);
  return Number.isFinite(n2) ? Math.round((n2 + Number.EPSILON) * 100) / 100 : NaN;
}
function modoSincronizadoPM08() {
  return typeof window !== "undefined" && !!window.NUBE_URL && !window.__modoPruebasLocal;
}
function horaActualPM08() {
  return (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5);
}
function clavePendientePM08(tipo, empresaId, localId, extra = "") {
  return `almacen:pm08:${tipo}:${empresaId || "sin-empresa"}:${localId || "todos"}${extra ? `:${extra}` : ""}`;
}
function leerPendientePM08(clave) {
  try {
    return JSON.parse(localStorage.getItem(clave) || "null");
  } catch {
    return null;
  }
}
function guardarPendientePM08(clave, valor) {
  try {
    localStorage.setItem(clave, JSON.stringify(valor));
    return true;
  } catch {
    return false;
  }
}
function limpiarPendientePM08(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
function prepararPendientePM08(clave, prefijo, payload) {
  const existente = leerPendientePM08(clave);
  if (existente) {
    if (JSON.stringify(existente.payload) !== JSON.stringify(payload)) {
      return { ok: false, pendiente: existente, error: "Hay una operaci\xF3n anterior pendiente en este local. Reint\xE9ntala con sus mismos datos antes de iniciar otra." };
    }
    return { ok: true, pendiente: existente, recuperada: true };
  }
  const pendiente = { operationId: `${prefijo}-${Date.now()}-${uid()}`, payload, creadoEn: (/* @__PURE__ */ new Date()).toISOString() };
  if (!guardarPendientePM08(clave, pendiente)) {
    return { ok: false, error: "No se ha podido conservar el borrador de la operaci\xF3n en este dispositivo." };
  }
  return { ok: true, pendiente, recuperada: false };
}
var operacionesPM08EnCurso = {};
function esErrorTransitorioPM08(error) {
  const codigo = String(error?.code || "").toLowerCase();
  const mensaje = String(error?.message || error || "").toLowerCase();
  return !codigo && /failed to fetch|fetch failed|network|conexi[oó]n|timeout|load failed|offline/.test(mensaje);
}
function mensajeErrorPM08(error, fallback = "No se ha podido confirmar la operaci\xF3n.") {
  const mensaje = String(error?.message || error || "");
  const mensajes = [
    ["operation_id_conflict", "El identificador de reintento ya existe con datos distintos. La operaci\xF3n no se ha repetido."],
    ["contexto_no_autorizado", "La empresa o el local no pertenecen al \xE1mbito autorizado de esta cuenta."],
    ["venta_fuera_de_contexto", "La venta pertenece a otra empresa o a otro local."],
    ["local_inactivo", "El local e
--- fin coincidencia ---

--- coincidencia 2 pos=3989476 ---
ock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : null;
      const st2 = exacto || activo;
      if (!st2) return prod;
      return { ...prod, stock: Number(st2.total) || 0, stockPisoVenta: Number(st2.piso) || 0, stockMinimo: Number(st2.minimo) || 0, _pm07BajoMinimo: !!st2.bajo_minimo, _pm07Servidor: true, _pm07LocalOperable: st2.local_operable !== false };
    }));
  }
  if (typeof setMovimientos === "function") {
    const server = movs.map((m4) => {
      const d2 = m4.datos && typeof m4.datos === "object" ? m4.datos : {};
      const delta = Number(m4.delta_total) || 0;
      const creado = String(m4.created_at || "");
      return {
        id: `pm07-${m4.id}`,
        operationId: m4.operation_id,
        ventaId: d2.ventaId || (m4.tipo === "VENTA" ? m4.operation_id : d2.anulaVentaId || null),
        anulaVentaId: d2.anulaVentaId || null,
        productoId: m4.producto_id,
        localId: m4.local_id,
        empresaId: m4.empresa_id,
        cantidad: delta,
        cantidadFisica: Number(m4.cantidad) || Math.abs(delta),
        tipo: m4.tipo,
        motivo: d2.motivo || m4.tipo,
        referencia: d2.referencia || "PM-07",
        costoUnitario: Number(d2.costoUnitario) || 0,
        ingresoUnitario: Number(d2.ingresoUnitario) || 0,
        ivaVentaAplicado: Number(d2.ivaVentaAplicado) || 0,
        medioPago: d2.medioPago || null,
        detallePago: d2.detallePago || null,
        fecha: creado ? creado.slice(0, 10) : todayISO(),
        hora: creado ? creado.slice(11, 16) : (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
        afectaStockTotal: delta !== 0,
        afectaStockPisoVenta: Number(m4.delta_piso) !== 0,
        movimientoOriginalId: m4.movimiento_original_id || null,
        _pm07Servidor: true
      };
    });
    setMovimientos((prev) => [...server, ...(prev || []).filter((m4) => !m4._pm07Servidor)]);
  }
  return { ok: true, stocks: stocks.length, movimientos: movs.length };
}
function redondearDineroPM08(valor) {
  const n2 = Number(valor);
  return Number.isFinite(n2) ? Math.round((n2 + Number.EPSILON) * 100) / 100 : NaN;
}
function modoSincronizadoPM08() {
  return typeof window !== "undefined" && !!window.NUBE_URL && !window.__modoPruebasLocal;
}
function horaActualPM08() {
  return (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5);
}
function clavePendientePM08(tipo, empresaId, localId, extra = "") {
  return `almacen:pm08:${tipo}:${empresaId || "sin-empresa"}:${localId || "todos"}${extra ? `:${extra}` : ""}`;
}
function leerPendientePM08(clave) {
  try {
    return JSON.parse(localStorage.getItem(clave) || "null");
  } catch {
    return null;
  }
}
function guardarPendientePM08(clave, valor) {
  try {
    localStorage.setItem(clave, JSON.stringify(valor));
    return true;
  } catch {
    return false;
  }
}
function limpiarPendientePM08(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
function prepararPendientePM08(clave, prefijo, payload) {
  const existente = leerPendientePM08(clave);
  if (existente) {
    if (JSON.stringify(existente.payload) !== JSON.stringify(payload)) {
      return { ok: false, pendiente: existente, error: "Hay una operaci\xF3n anterior pendiente en este local. Reint\xE9ntala con sus mismos datos antes de iniciar otra." };
    }
    return { ok: true, pendiente: existente, recuperada: true };
  }
  const pendiente = { operationId: `${prefijo}-${Date.now()}-${uid()}`, payload, creadoEn: (/* @__PURE__ */ new Date()).toISOString() };
  if (!guardarPendientePM08(clave, pendiente)) {
    return { ok: false, error: "No se ha podido conservar el borrador de la operaci\xF3n en este dispositivo." };
  }
  return { ok: true, pendiente, recuperada: false };
}
var operacionesPM08EnCurso = {};
function esErrorTransitorioPM08(error) {
  const codigo = String(error?.code || "").toLowerCase();
  const mensaje = String(error?.message || error || "").toLowerCase();
  return !codigo && /failed to fetch|fetch failed|network|conexi[oó]n|timeout|load failed|offline/.test(mensaje);
}
function mensajeErrorPM08(error, fallback = "No se ha podido confirmar la operaci\xF3n.") {
  const mensaje = String(error?.message || error || "");
  const mensajes = [
    ["operation_id_conflict", "El identificador de reintento ya existe con datos distintos. La operaci\xF3n no se ha repetido."],
    ["contexto_no_autorizado", "La empresa o el local no pertenecen al \xE1mbito autorizado de esta cuenta."],
    ["venta_fuera_de_contexto", "La venta pertenece a otra empresa o a otro local."],
    ["local_inactivo", "El local est\xE1 cerrado o inactivo y no admite nuevas operaciones."],
    ["periodo_caja_cerrado", "Ese d\xEDa ya tiene un arqueo activo. Anula el arqueo de forma trazable antes de modificar la caja."],
    ["arqueo_ya_existe", "Ya existe un arqueo activo para ese d\xEDa y local."],
    ["arqueo_ya_anulado", "El arqueo ya estaba anulado."],
    ["movimiento_caja_ya_revertido", "Ese movimiento de caja ya tiene un reverso."],
    ["devolucion_supera_cantidad_pendiente", "La cantidad supera lo que queda pendiente de devolver en la venta."],
    ["reembolso_supera_importe_pendiente", "El reembolso supera el importe permitido por la cantidad devuelta."],
    ["venta_ya_anulada", "La venta est\xE1 anulada y ya no admite devoluciones."],
    ["venta_con_devoluciones", "La venta ya tiene devoluciones; no puede anularse por completo."],
    ["venta_no_encontrada", "No se ha encontrado la venta original."],
    ["producto_no_pertenece_a_venta", "El producto no pertenece a la venta seleccionada."],
    ["importe_venta_no_disponible", "La venta no conserva un importe verificable para autorizar el reembolso."],
    ["stock_insuficiente", "No hay stock suficiente para completar la devoluci\xF3n al proveedor."],
    ["cantidad_invalida", "La cantidad debe ser mayor que cero y respetar la unidad del producto."],
    ["unidad_indivisible", "Este producto solo admite cantidades enteras."],
    ["precision_cantidad_excedida", "La cantidad tiene m\xE1s decimales de los permitidos para este producto."],
    ["importe_negativo", "El importe no puede ser negativo."],
    ["importe_cero", "El importe debe ser mayor que cero."],
    ["importe_invalido", "Escribe un importe v\xE1lido."],
    ["importe_fuera_rango", "El importe est\xE1 fuera del rango permitido."],
    ["importe_precision_invalida", "El importe solo puede tener dos decimales."],
    ["medio_reembolso_invalido", "Elige un medio de reintegro coherente con el importe."],
    ["motivo_requerido", "Escribe el motivo de la operaci\xF3n."],
    ["caja_no_autorizada", "Tu rol no tiene permiso para operar la caja."],
    ["arqueo_no_autorizado", "Tu rol no tiene permiso para realizar arqueos."],
    ["devolucion_no_autorizada", "Tu rol no tiene permiso para registrar devoluciones."],
    ["devolucion_proveedor_no_autorizada", "Tu rol no tiene permiso para devolver mercanc\xEDa a proveedores."]
--- fin coincidencia ---

PATRON 'movimientoOriginalId': 1 coincidencias
--- coincidencia 1 pos=3989451 ---
 = await Promise.all([qStock, qMov]);
  if (rStock.error) throw rStock.error;
  if (rMov.error) throw rMov.error;
  const stocks = Array.isArray(rStock.data) ? rStock.data : [];
  const movs = Array.isArray(rMov.data) ? rMov.data : [];
  if (typeof setProductos === "function") {
    setProductos((prev) => (prev || []).map((prod) => {
      const exacto = stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === prod.localId);
      const activo = !exacto && localActivoId ? stocks.find((x3) => x3.producto_id === prod.id && x3.local_id === localActivoId) : null;
      const st2 = exacto || activo;
      if (!st2) return prod;
      return { ...prod, stock: Number(st2.total) || 0, stockPisoVenta: Number(st2.piso) || 0, stockMinimo: Number(st2.minimo) || 0, _pm07BajoMinimo: !!st2.bajo_minimo, _pm07Servidor: true, _pm07LocalOperable: st2.local_operable !== false };
    }));
  }
  if (typeof setMovimientos === "function") {
    const server = movs.map((m4) => {
      const d2 = m4.datos && typeof m4.datos === "object" ? m4.datos : {};
      const delta = Number(m4.delta_total) || 0;
      const creado = String(m4.created_at || "");
      return {
        id: `pm07-${m4.id}`,
        operationId: m4.operation_id,
        ventaId: d2.ventaId || (m4.tipo === "VENTA" ? m4.operation_id : d2.anulaVentaId || null),
        anulaVentaId: d2.anulaVentaId || null,
        productoId: m4.producto_id,
        localId: m4.local_id,
        empresaId: m4.empresa_id,
        cantidad: delta,
        cantidadFisica: Number(m4.cantidad) || Math.abs(delta),
        tipo: m4.tipo,
        motivo: d2.motivo || m4.tipo,
        referencia: d2.referencia || "PM-07",
        costoUnitario: Number(d2.costoUnitario) || 0,
        ingresoUnitario: Number(d2.ingresoUnitario) || 0,
        ivaVentaAplicado: Number(d2.ivaVentaAplicado) || 0,
        medioPago: d2.medioPago || null,
        detallePago: d2.detallePago || null,
        fecha: creado ? creado.slice(0, 10) : todayISO(),
        hora: creado ? creado.slice(11, 16) : (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
        afectaStockTotal: delta !== 0,
        afectaStockPisoVenta: Number(m4.delta_piso) !== 0,
        movimientoOriginalId: m4.movimiento_original_id || null,
        _pm07Servidor: true
      };
    });
    setMovimientos((prev) => [...server, ...(prev || []).filter((m4) => !m4._pm07Servidor)]);
  }
  return { ok: true, stocks: stocks.length, movimientos: movs.length };
}
function redondearDineroPM08(valor) {
  const n2 = Number(valor);
  return Number.isFinite(n2) ? Math.round((n2 + Number.EPSILON) * 100) / 100 : NaN;
}
function modoSincronizadoPM08() {
  return typeof window !== "undefined" && !!window.NUBE_URL && !window.__modoPruebasLocal;
}
function horaActualPM08() {
  return (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5);
}
function clavePendientePM08(tipo, empresaId, localId, extra = "") {
  return `almacen:pm08:${tipo}:${empresaId || "sin-empresa"}:${localId || "todos"}${extra ? `:${extra}` : ""}`;
}
function leerPendientePM08(clave) {
  try {
    return JSON.parse(localStorage.getItem(clave) || "null");
  } catch {
    return null;
  }
}
function guardarPendientePM08(clave, valor) {
  try {
    localStorage.setItem(clave, JSON.stringify(valor));
    return true;
  } catch {
    return false;
  }
}
function limpiarPendientePM08(clave) {
  try {
    localStorage.removeItem(clave);
  } catch {
  }
}
function prepararPendientePM08(clave, prefijo, payload) {
  const existente = leerPendientePM08(clave);
  if (existente) {
    if (JSON.stringify(existente.payload) !== JSON.stringify(payload)) {
      return { ok: false, pendiente: existente, error: "Hay una operaci\xF3n anterior pendiente en este local. Reint\xE9ntala con sus mismos datos antes de iniciar otra." };
    }
    return { ok: true, pendiente: existente, recuperada: true };
  }
  const pendiente = { operationId: `${prefijo}-${Date.now()}-${uid()}`, payload, creadoEn: (/* @__PURE__ */ new Date()).toISOString() };
  if (!guardarPendientePM08(clave, pendiente)) {
    return { ok: false, error: "No se ha podido conservar el borrador de la operaci\xF3n en este dispositivo." };
  }
  return { ok: true, pendiente, recuperada: false };
}
var operacionesPM08EnCurso = {};
function esErrorTransitorioPM08(error) {
  const codigo = String(error?.code || "").toLowerCase();
  const mensaje = String(error?.message || error || "").toLowerCase();
  return !codigo && /failed to fetch|fetch failed|network|conexi[oó]n|timeout|load failed|offline/.test(mensaje);
}
function mensajeErrorPM08(error, fallback = "No se ha podido confirmar la operaci\xF3n.") {
  const mensaje = String(error?.message || error || "");
  const mensajes = [
    ["operation_id_conflict", "El identificador de reintento ya existe con datos distintos. La operaci\xF3n no se ha repetido."],
    ["contexto_no_autorizado", "La empresa o el local no pertenecen al \xE1mbito autorizado de esta cuenta."],
    ["venta_fuera_de_contexto", "La venta pertenece a otra empresa o a otro local."],
    ["local_inactivo", "El local est\xE1 cerrado o inactivo y no admite nuevas operaciones."],
    ["periodo_caja_cerrado", "Ese d\xEDa ya tiene un arqueo activo. Anula el arqueo de forma trazable antes de modificar la caja."],
    ["arqueo_ya_existe", "Ya existe un arqueo activo para ese d\xEDa y local."],
    ["arqueo_ya_anulado", "El arqueo ya estaba anulado."],
    ["movimiento_caja_ya_revertido", "Ese movimiento de caja ya tiene un reverso."],
    ["devolucion_supera_cantidad_pendiente", "La cantidad supera lo que queda pendiente de devolver en la venta."],
    ["reembolso_supera_importe_pendiente", "El reembolso supera el importe permitido por la cantidad devuelta."],
    ["venta_ya_anulada", "La venta est\xE1 anulada y ya no admite devoluciones."],
    ["venta_con_devoluciones", "La venta ya tiene devoluciones; no puede anularse por completo."],
    ["venta_no_encontrada", "No se ha encontrado la venta original."],
    ["producto_no_pertenece_a_venta", "El producto no pertenece a la venta seleccionada."],
    ["importe_venta_no_disponible", "La venta no conserva un importe verificable para autorizar el reembolso."],
    ["stock_insuficiente", "No hay stock suficiente para completar la devoluci\xF3n al proveedor."],
    ["cantidad_invalida", "La cantidad debe ser mayor que cero y respetar la unidad del producto."],
    ["unidad_indivisible", "Este producto solo admite cantidades enteras."],
    ["precision_cantidad_excedida", "La cantidad tiene m\xE1s decimales de los permitidos para este producto."],
    ["importe_negativo", "El importe no puede ser negativo."],
    ["importe_cero", "El importe debe ser mayor que cero."],
    ["importe_invalido", "Escribe un importe v\xE1lido."],
    ["importe_fuera_rango", "El importe est\xE1 fuera del rango permitido."],
    ["importe_precision_invalida", "El importe solo puede tener dos decimales."],
    ["medio_reembolso_invalido", "Elige un medio de reintegro coherente con el importe."],
    ["motivo_requerido", "Escribe el motivo de la operaci\xF3n."],
    ["caja_no_autorizada", "Tu rol no tiene permiso para operar la caja."],
    ["arqueo_no_autorizado", "Tu rol no tiene permiso para realizar arqueos."],
    ["devolucion_no_autorizada", "Tu rol no tiene permiso para registrar devoluciones."],
    ["devolucion_proveedor_no_autorizada", "Tu rol no tiene permiso para devolver me
--- fin coincidencia ---


BLOQUE function Resultados: coincidencias=1
function Resultados({ movimientos, productos, productoPorId, gastosGenerales, addGasto, deleteGasto, empleados = [] }
BLOQUE_TRUNCADO=0; longitud=117

BLOQUE function esVenta: coincidencias=1
function esVenta(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return m22.tipo === "VENTA" || m22.tipo === "VENTA_ENCARGO";
  return m22.tipo === "salida" && m22.motivo === "Venta";
}
BLOQUE_TRUNCADO=0; longitud=200

BLOQUE function esSalida: coincidencias=1
function esSalida(m22) {
  if (!m22) return false;
  if (esMovimientoNuevo(m22)) return Number(m22.cantidad) < 0;
  return m22.tipo === "salida";
}
BLOQUE_TRUNCADO=0; longitud=147

BLOQUE function sincronizarStockPm07: coincidencias=1
async function sincronizarStockPm07({ setProductos, setMovimientos, localActivoId = null }
BLOQUE_TRUNCADO=0; longitud=90

BLOQUE function anularVenta: coincidencias=1
async function anularVenta(ventaId, movimientosActuales, motivo = "") {
    if (!localActivoId) return { ok: false, error: "Selecciona un local para gestionar ventas del TPV." };
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) return anularVentaLocal(ventaId, movimientosActuales, motivo);
    const lineasOriginales = (movimientosActuales || []).filter((m22) => m22.ventaId === ventaId || m22.operationId === ventaId).filter((m22) => esVenta(m22) || esSalida(m22)).filter((m22) => movimientoEsDelLocalActivoVenta(m22));
    if (lineasOriginales.length === 0) return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    try {
      const supabase = await window.getSupabaseClient();
      const operationId = `rev-${ventaId}`;
      const r2 = await supabase.rpc("revertir_venta_stock_carrito", { p_operation_id: operationId, p_venta_operation_id: ventaId, p_motivo: motivo || "" });
      if (r2.error) throw r2.error;
      await sincronizarStockPm07({ setProductos, setMovimientos, localActivoId });
      const fechaVenta = lineasOriginales[0]?.fecha;
      const arqueoDelDia = fechaVenta && (arqueos || []).find((a22) => a22.fecha === fechaVenta && (!localActivoId || a22.localId === localActivoId));
      if (arqueoDelDia) {
        fetch("https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/enviar-notificacion", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ titulo: "Caja cerrada, ahora desactualizada", cuerpo: `Se anul\xF3 una venta del ${fechaVenta}, d\xEDa que ya ten\xEDa la caja cerrada \u2014 revisa ese arqueo, puede que ya no cuadre.`, localId: r2.data?.localId || lineasOriginales[0]?.localId || null, url: "/" })
        }).catch(() => {
        });
      }
      return { ok: true, lineasAnuladas: Number(r2.data?.lineasAnuladas) || 0, arqueoAfectado: !!arqueoDelDia, modo: "pm07-servidor", sincronizada: true, replayed: !!r2.data?.replayed };
    } catch (error) {
      const msg = String(error?.message || "");
      if (msg.includes("venta_stock_ya_revertida")) return { ok: false, error: "La venta ya estaba anulada.", yaExistia: true };
      return { ok: false, error: "No se pudo confirmar la anulaci\xF3n con el servidor. No se ha modificado el stock local." };
    }
  }
BLOQUE_TRUNCADO=0; longitud=2510