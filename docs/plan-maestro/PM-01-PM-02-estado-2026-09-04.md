# L&A Suite · Proyecto A · Estado PM-01 / PM-02

Fecha: 2026-09-04

> Documento de evidencia de QA. No autoriza fusión a `main`, publicación, uso de datos reales ni activación de integraciones productivas.

## PM-01 · Fijar una base reproducible

**Estado:** REPRODUCIDO / BLOQUEADO PARA CIERRE

### Base candidata identificada

- Repositorio: `Plopezm1990/Almacen`
- Rama candidata auditada: `la-suite-identidad`
- SHA candidato: `8567a920430d4ec02a6e17386f23b2eaaa1cd86f`
- PR temporal de preview: `#10 · Preview temporal L&A Suite — NO FUSIONAR`
- Deploy Preview identificado: `deploy-preview-10--chic-entremet-9107cf.netlify.app`
- Netlify confirmó que ese deploy corresponde al SHA candidato anterior.
- Artefacto principal servido: `fuente.js`
- Blob Git de `fuente.js` en el candidato: `22ebd87f56f6474e8b090f577d40d5aa462558a1`

### Fuente y proceso de build

- La rama `recuperacion-fuente` conserva una fuente recompilable recuperada y documenta paridad exacta con el bundle validado del PR #4.
- Esa fuente recuperada **no demuestra paridad con el candidato actual**: su bundle y blob son distintos del `fuente.js` servido por el PR #10.
- El candidato actual contiene transformaciones que modifican directamente `fuente.js` mediante scripts y GitHub Actions. Ejemplo comprobado: `tools/corregir_caja_multilocal.py` + `.github/workflows/aplicar-caja-multilocal.yml` generan un commit de `github-actions[bot]` que modifica únicamente `fuente.js`.
- Por tanto, hoy no existe evidencia suficiente para declarar `source-recovery/fuente-recuperado.js` como fuente canónica exacta del candidato.
- La base operativa reproducible queda identificada por SHA + artefacto, pero el **origen recompilable exacto del candidato sigue pendiente de reconciliar**.

### Seguridad histórica

- La rama `seguridad-edge-functions` está en `3616d202bd43c54adef1551f22c23a6febe69644`.
- La comparación con el candidato muestra historias divergentes; no se asume que todo su contenido esté integrado.
- Cualquier cambio de esa rama deberá portarse por diff y revalidarse, no por nombre de commit.

### Cierre pendiente de PM-01

1. Reconstruir una fuente recompilable que corresponda al SHA candidato o demostrar equivalencia por secciones/paridad.
2. Definir qué archivos son fuente y cuáles son artefactos generados.
3. Evitar nuevas correcciones funcionales sobre dos fuentes paralelas hasta reconciliar esa relación.

---

## PM-02 · Asegurar QA y destino real de guardado

**Estado:** CORREGIDO PARCIALMENTE / PENDIENTE DE QA / BACKEND QA BLOQUEADO

### Destino real encontrado

- El candidato contiene configuración del proyecto Supabase real `flqercbgpgmmfaakrwkc` (`Chocoloyos S.L`).
- Supabase confirma que ese proyecto está activo.
- No existen development branches de Supabase en ese proyecto en esta comprobación.
- Netlify no incluye Functions ni Edge Functions propias en el Deploy Preview #10.

### Aislamiento que ya existía

`reset-pruebas-preview.js` activa `window.__modoPruebasLocal = true` para el alias `deploy-preview-N--chic-entremet-9107cf.netlify.app`. Después `index.html` vacía `window.NUBE_URL` y `window.NUBE_CLAVE`.

### Fallos de aislamiento encontrados

1. El filtro original no cubría el permalink inmutable de Netlify (`<deploy-id>--chic-entremet-9107cf.netlify.app`). El mismo artefacto podía abrirse por esa URL sin activar modo local.
2. El bundle candidato contiene llamadas `fetch()` directas al host productivo de Supabase. Entre las rutas comprobadas aparecen:
   - `functions/v1/enviar-notificacion`
   - `functions/v1/crear-cuenta-empleado`
   - `functions/v1/importar-albaran`
   - `functions/v1/importar-nomina`
   - `functions/v1/entrevista-personal`
   - `functions/v1/prefiltro-candidato`
3. Varias de esas llamadas directas no dependen de `window.NUBE_URL`, por lo que vaciar la configuración general no garantiza aislamiento total.
4. En los puntos inspeccionados, el candidato no utiliza las variantes `importar-albaran-prueba`, `importar-nomina-prueba` ni `enviar-notificacion-segura` como sustitución automática de QA.

### Corrección preparada

Rama: `pm02-qa-isolation`

PR: `#11 · PM-02 · Aislamiento completo de Deploy Preview` (draft, base `la-suite-identidad`)

Cambios en `reset-pruebas-preview.js`:

- ampliar el reconocimiento a alias `deploy-preview-N` y permalink inmutable de 24 caracteres hexadecimales;
- instalar, únicamente en Deploy Preview, una barrera `fetch()` que rechaza cualquier petición HTTP cuyo host sea `flqercbgpgmmfaakrwkc.supabase.co`;
- mantener producción, `main` y dominios no-preview fuera de esa barrera.

### Verificación realizada sin red productiva

Prueba local del filtro de host:

- `deploy-preview-10--...` → bloque de QA activo;
- permalink inmutable `6a99b8ef7078d00007c24f80--...` → bloque de QA activo;
- `chic-entremet-9107cf.netlify.app` → no afectado;
- `main--chic-entremet-9107cf.netlify.app` → no afectado.

Prueba local de la barrera `fetch()`:

- Supabase productivo `/functions/v1/importar-nomina` → `QA_BLOCKED_PRODUCTION_SUPABASE`;
- Supabase productivo `/rest/v1/...` → `QA_BLOCKED_PRODUCTION_SUPABASE`;
- CDN y recursos relativos de la aplicación → permitidos.

### Lo que todavía NO está validado

- Netlify no ha generado un Deploy Preview/check para el head actual del PR #11 en esta comprobación.
- No se ha ejecutado todavía una prueba de red en navegador sobre ese PR.
- No existe backend Supabase QA separado.
- No se han probado Auth, Storage, RLS ni Edge Functions contra un proyecto QA.
- No se ha enviado ningún correo, push, WhatsApp ni petición IA real durante esta revisión.

### Condición para cerrar PM-02

1. Validar la barrera sobre un Deploy Preview del cambio y demostrar en red que no existe tráfico al proyecto productivo.
2. Decidir y preparar backend QA separado antes de pruebas remotas de Auth/RLS/Storage/RPC/Edge Functions.
3. Cualquier creación de proyecto/branch de Supabase que genere coste requiere autorización expresa previa.

---

## Control de cambios de esta sesión

- `main`: SIN CAMBIOS.
- Producción Netlify: SIN CAMBIOS.
- Supabase productivo: SIN ESCRITURAS NI CAMBIOS.
- Rama nueva: `pm02-qa-isolation`.
- PR nuevo: `#11`, draft, NO FUSIONAR.

## Siguiente bloque permitido

Mientras PM-02 no tenga backend QA, se puede continuar en solo lectura con la reconciliación de fuente de PM-01 y preparar la matriz/fixtures de PM-04. Las pruebas de autorización real de LA-001/002/003/004 deben esperar a un entorno QA que no pueda afectar datos reales.
