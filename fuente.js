import {
  ArrowLeftRight,
  Boxes,
  Calculator,
  CalendarClock,
  CalendarDays,
  CalendarRange,
  Camera,
  ChartColumn,
  ChartLine,
  ChevronRight,
  CircleArrowDown,
  CircleArrowUp,
  CircleCheck,
  ClipboardList,
  Clock,
  Cog,
  Coins,
  Download,
  Droplet,
  E,
  Ellipsis,
  Eye,
  EyeOff,
  Factory,
  FileText,
  Files,
  LoaderCircle,
  LogIn,
  LogOut,
  Mail,
  Map as Map2,
  MessageCircle,
  Minus,
  Package,
  Pencil,
  Phone,
  Plus,
  Receipt,
  RotateCcwClock,
  ScanBarcode,
  Search,
  ShieldCheck,
  ShoppingBag,
  ShoppingCart,
  Stethoscope,
  Tags,
  Trash2,
  TrendingUp,
  TriangleAlert,
  Truck,
  Upload,
  UserRound,
  Users,
  Wallet,
  X,
  autoTable,
  createClient,
  require_client,
  require_react,
  utils,
  writeFileSync
} from "./chunk-WRVHFJBG.js";
import "./chunk-SULEHD65.js";
import {
  __toESM
} from "./chunk-CZ7CSFO4.js";

// fuente.jsx
var import_react = __toESM(require_react());
var import_client = __toESM(require_client());
var C = {
  bg: "var(--c-bg)",
  surface: "var(--c-surface)",
  ink: "var(--c-ink)",
  inkSoft: "var(--c-ink-soft)",
  line: "var(--c-line)",
  accent: "var(--c-accent)",
  accentSoft: "var(--c-accent-soft)",
  amber: "var(--c-amber)",
  amberSoft: "var(--c-amber-soft)",
  red: "var(--c-red)",
  redSoft: "var(--c-red-soft)",
  // Fijo a propósito: la barra lateral y las cabeceras "libro de almacén" son
  // siempre oscuras, en claro o en oscuro — no son "texto", son la marca del programa.
  // Verde extraído directamente del medallón del logo real (Chocolatería San Ginés).
  chrome: "#0C2714"
};
var LOGO = "data:image/png;base64,";
var LOGO_BLANCO = "data:image/png;base64,";
function ivaDe(p) {
  const raw = p && p.ivaVenta;
  if (raw === "" || raw === null || raw === void 0) return 10;
  const v = Number(raw);
  return isNaN(v) ? 10 : v;
}
function ivaCompraDe(p) {
  const raw = p && p.ivaCompra;
  if (raw === "" || raw === null || raw === void 0) return 10;
  const v = Number(raw);
  return isNaN(v) ? 10 : v;
}
function costoConIva(p) {
  return (Number(p && p.costo) || 0) * (1 + ivaCompraDe(p) / 100);
}
function agruparPorProveedor(productos, proveedores) {
  const grupos = /* @__PURE__ */ new Map();
  productos.forEach((p) => {
    const id = p.proveedorId || "__sin__";
    if (!grupos.has(id)) grupos.set(id, []);
    grupos.get(id).push(p);
  });
  const orden = (a, b) => (a.ubicacion || "").localeCompare(b.ubicacion || "") || (a.nombre || "").localeCompare(b.nombre || "");
  const salida = [];
  proveedores.forEach((pv) => {
    if (grupos.has(pv.id)) {
      salida.push({ id: pv.id, nombre: pv.nombre, items: grupos.get(pv.id).sort(orden) });
      grupos.delete(pv.id);
    }
  });
  grupos.forEach((items, id) => {
    if (id === "__sin__") return;
    salida.push({ id, nombre: "Proveedor no encontrado", items: items.sort(orden) });
  });
  if (grupos.has("__sin__")) {
    salida.push({ id: "__sin__", nombre: "Sin proveedor asignado", items: grupos.get("__sin__").sort(orden) });
  }
  return salida;
}
var MARCA_VERDE = [12, 39, 20];
function generarPdfConteo(conteo, diferencias, proveedores) {
  const doc = new E({ unit: "pt", format: "a4" });
  const anchoPagina = doc.internal.pageSize.getWidth();
  const margen = 40;
  doc.setFillColor(...MARCA_VERDE);
  doc.rect(0, 0, anchoPagina, 92, "F");
  try {
    const altoLogo = 44;
    const anchoLogo = altoLogo * (516 / 220);
    doc.addImage(LOGO_BLANCO, "PNG", margen, 22, anchoLogo, altoLogo);
  } catch {
  }
  doc.setTextColor(255, 255, 255);
  doc.setFont("helvetica", "bold");
  doc.setFontSize(17);
  doc.text("Chocolater\xEDa San Gin\xE9s", margen + 118, 40);
  doc.setFont("helvetica", "normal");
  doc.setFontSize(11.5);
  doc.text("Resultado del inventario", margen + 118, 58);
  const etiquetaAmbito = conteo.ambito === "piso_venta" ? "Piso de venta" : conteo.ambito === "almacen" ? "Almac\xE9n (trastienda)" : "Empresa completa";
  doc.setFontSize(9.5);
  doc.text(`${etiquetaAmbito} \xB7 ${conteo.fecha}`, margen + 118, 74);
  doc.setTextColor(40, 40, 40);
  doc.setFontSize(9);
  let metaY = 112;
  if (conteo.contadoPor) {
    doc.text(`Contado por: ${conteo.contadoPor}`, margen, metaY);
    metaY += 13;
  }
  if (conteo.revisadoPor) {
    doc.text(`Revisado por: ${conteo.revisadoPor}`, margen, metaY);
    metaY += 13;
  }
  const grupos = agruparPorProveedor(diferencias.map((d) => d.producto), proveedores);
  const cfPorProducto = {};
  diferencias.forEach((d) => {
    cfPorProducto[d.producto.id] = d.cf;
  });
  const filas = [];
  grupos.forEach((g) => {
    filas.push([{ content: g.nombre, colSpan: 5, styles: { fillColor: [237, 231, 214], textColor: MARCA_VERDE, fontStyle: "bold", fontSize: 9.5 } }]);
    g.items.forEach((p) => {
      filas.push([
        p.nombre,
        p.unidad || "\u2014",
        p.categoria || "\u2014",
        g.nombre,
        fmt(cfPorProducto[p.id])
      ]);
    });
  });
  autoTable(doc, {
    startY: Math.max(metaY + 6, 112),
    margin: { left: margen, right: margen },
    head: [["Descripci\xF3n del producto", "Unidad", "Categor\xEDa", "Proveedor", "Conteo final"]],
    body: filas,
    theme: "grid",
    styles: { fontSize: 9, cellPadding: 5, textColor: [30, 30, 30], lineColor: [225, 220, 205], lineWidth: 0.5 },
    headStyles: { fillColor: MARCA_VERDE, textColor: 255, fontStyle: "bold", fontSize: 9.5 },
    alternateRowStyles: { fillColor: [249, 246, 238] },
    columnStyles: { 4: { halign: "right", fontStyle: "bold" } },
    didParseCell: (data) => {
      if (data.row.raw.length === 1 && data.row.raw[0].colSpan) {
        data.cell.styles.fillColor = [237, 231, 214];
      }
    },
    didDrawPage: (data) => {
      const alturaPagina = doc.internal.pageSize.getHeight();
      doc.setFontSize(8);
      doc.setTextColor(140, 140, 140);
      doc.text(`Generado el ${todayISO()}`, margen, alturaPagina - 20);
      doc.text(`P\xE1gina ${doc.internal.getNumberOfPages()}`, anchoPagina - margen, alturaPagina - 20, { align: "right" });
    }
  });
  return doc;
}
var TIPOS_IVA = [0, 4, 10, 21];
var ITEMS_EMPLEADO = ["dashboard", "venta", "fichaje", "recepcion", "conteo", "appcc", "mapa", "traspasos"];
var ROLES_EMPLEADO = {
  "B\xE1sico": ["dashboard", "venta", "fichaje"],
  "Est\xE1ndar": ITEMS_EMPLEADO,
  "Encargado": [...ITEMS_EMPLEADO, "pedidos", "productos", "proveedores"]
};
var NOMBRES_ROLES = Object.keys(ROLES_EMPLEADO);
var ESTILO_IMPRESION_CLARO = {
  "--c-bg": "#F7F3E9",
  "--c-surface": "#FFFFFF",
  "--c-ink": "#17241C",
  "--c-ink-soft": "#647267",
  // oscurecido: el original (#6B7A6E) daba 4.09:1 sobre el fondo, por debajo del mínimo AA (4.5:1)
  "--c-line": "#E2DBC7",
  "--c-accent": "#8C6D2A",
  "--c-accent-soft": "#F1E7CE",
  "--c-amber": "#977022",
  // oscurecido: el original (#B08228) daba 3.46:1 sobre blanco, por debajo del mínimo AA (4.5:1)
  "--c-amber-soft": "#F6EAD2",
  "--c-red": "#B5473A",
  "--c-red-soft": "#F7E5E1",
  background: "#FFFFFF",
  color: "#17241C"
};
var ZONAS_ALMACEN = [
  "Obrador",
  "C\xE1mara de refrigeraci\xF3n",
  "Congelador",
  "Almac\xE9n seco",
  "Mostrador / vitrina",
  "Utillaje",
  "Sin asignar"
];
var DEPARTAMENTOS = ["Obrador", "Tienda", "Limpieza", "Oficina", "Utillaje", "Sin asignar"];
var CATEGORIAS_PRODUCTO = [
  "Cacao y chocolate",
  "Bebidas y licores",
  "Caf\xE9 e infusiones",
  "L\xE1cteos y huevos",
  "Panader\xEDa y boller\xEDa",
  "Frutos secos y semillas",
  "Frutas y verduras",
  "Mermeladas y untables",
  "Congelados",
  "Especias, aromas y colorantes",
  "Aceites y grasas",
  "Az\xFAcares y edulcorantes",
  "Harinas y masas",
  "Elaborados / venta",
  "Envases y embalaje",
  "Menaje y consumibles de servicio",
  "Utillaje y menaje",
  "Limpieza y qu\xEDmicos",
  "Papeler\xEDa y oficina",
  "Otra"
];
var SECCIONES_FICHAS = ["Caf\xE9s", "Chocolates", "Boller\xEDa", "Tostadas", "Bocadillos", "Dulces", "Otros"];
var ALERGENOS = [
  { id: "gluten", nombre: "Cereales con gluten", icono: "\u{1F33E}" },
  { id: "crustaceos", nombre: "Crust\xE1ceos", icono: "\u{1F990}" },
  { id: "huevos", nombre: "Huevos", icono: "\u{1F95A}" },
  { id: "pescado", nombre: "Pescado", icono: "\u{1F41F}" },
  { id: "cacahuetes", nombre: "Cacahuetes", icono: "\u{1F95C}" },
  { id: "soja", nombre: "Soja", icono: "\u{1FAD8}" },
  { id: "lacteos", nombre: "Leche y l\xE1cteos", icono: "\u{1F95B}" },
  { id: "frutos_cascara", nombre: "Frutos de c\xE1scara", icono: "\u{1F330}" },
  { id: "apio", nombre: "Apio", icono: "\u{1F96C}" },
  { id: "mostaza", nombre: "Mostaza", icono: "\u{1F32D}" },
  { id: "sesamo", nombre: "Granos de s\xE9samo", icono: "\u{1FAD3}" },
  { id: "sulfitos", nombre: "Di\xF3xido de azufre y sulfitos", icono: "\u{1F377}" },
  { id: "altramuces", nombre: "Altramuces", icono: "\u{1F331}" },
  { id: "moluscos", nombre: "Moluscos", icono: "\u{1F9AA}" }
];
var alergenoPorId = (id) => ALERGENOS.find((a) => a.id === id);
var PALABRAS_ALERGENO = {
  gluten: ["harina", "trigo", "cebada", "centeno", "avena", "espelta", "kamut", "pan", "bizcocho", "pasta", "s\xE9mola", "semola", "cusc\xFAs", "cuscus", "malta", "galleta", "hojaldre", "masa", "crep", "gofre"],
  crustaceos: ["gamba", "langostino", "cangrejo", "bogavante", "camar\xF3n", "camaron", "marisco", "carabinero"],
  huevos: ["huevo", "clara de huevo", "yema", "mayonesa", "merengue", "tortilla"],
  pescado: ["pescado", "at\xFAn", "atun", "salm\xF3n", "salmon", "bacalao", "anchoa", "merluza", "sardina", "boquer\xF3n", "boqueron"],
  cacahuetes: ["cacahuete", "man\xED", "mani", "peanut"],
  soja: ["soja", "soya", "tofu", "edamame"],
  lacteos: ["leche", "nata", "mantequilla", "queso", "yogur", "yogurt", "l\xE1cteo", "lacteo", "crema pastelera", "cuajada", "suero", "mascarpone", "reques\xF3n", "requeson"],
  frutos_cascara: ["almendra", "avellana", "nuez", "nueces", "pistacho", "anacardo", "macadamia", "casta\xF1a", "castana", "pi\xF1\xF3n", "pinon", "pralin\xE9", "praline"],
  apio: ["apio"],
  mostaza: ["mostaza"],
  sesamo: ["s\xE9samo", "sesamo", "tahini", "ajonjol\xED", "ajonjoli"],
  sulfitos: ["sulfito", "vino", "vinagre", "verm\xFA", "vermu"],
  altramuces: ["altramuz", "altramuces", "lupino"],
  moluscos: ["mejill\xF3n", "mejillon", "almeja", "calamar", "pulpo", "sepia", "ostra", "vieira", "berberecho"]
};
function sugerirAlergenosDeTexto(texto) {
  const t = String(texto || "").toLowerCase();
  if (!t.trim()) return [];
  return Object.entries(PALABRAS_ALERGENO).filter(([, palabras]) => palabras.some((p) => t.includes(p))).map(([id]) => id);
}
function udsPorCaja(p) {
  const v = Number(p && p.udsPorCaja);
  return !v || v <= 0 ? 1 : v;
}
function costoCaja(p) {
  return (Number(p && p.costo) || 0) * udsPorCaja(p);
}
function normalizarParaComparar(txt) {
  return String(txt || "").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/[^a-z0-9 ]/g, " ").replace(/\s+/g, " ").trim();
}
function productosSimilares(descripcion, productos, maximo = 5) {
  const palabras = normalizarParaComparar(descripcion).split(" ").filter((w) => w.length >= 3);
  if (palabras.length === 0) return [];
  const candidatos = productos.map((p) => {
    const nombreNorm = normalizarParaComparar(p.nombre);
    const coincidencias = palabras.filter((w) => nombreNorm.includes(w)).length;
    return { producto: p, puntuacion: coincidencias / palabras.length };
  }).filter((c) => c.puntuacion > 0).sort((a, b) => b.puntuacion - a.puntuacion);
  return candidatos.slice(0, maximo);
}
function precioNeto(p) {
  const pvp = Number(p && p.precioVenta) || 0;
  return pvp / (1 + ivaDe(p) / 100);
}
function margenDe(p) {
  const neto = precioNeto(p);
  if (neto <= 0) return null;
  return (neto - (Number(p.costo) || 0)) / neto * 100;
}
var uid = () => Date.now().toString(36) + Math.random().toString(36).slice(2, 8);
function valorCF(item) {
  if (!item) return null;
  const candidatos = [item.conteo, item.cf, item.c3, item.c2, item.c1, item.cantidadContada];
  for (const v of candidatos) {
    if (v !== "" && v !== null && v !== void 0 && !isNaN(Number(v))) return Number(v);
  }
  return null;
}
var fmt = (n) => (Number(n) || 0).toLocaleString("es-ES", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
async function hashPin(pin) {
  if (!pin) return "";
  const datos = new TextEncoder().encode("chocoloyos-pin-v1-" + pin);
  const buffer = await crypto.subtle.digest("SHA-256", datos);
  return Array.from(new Uint8Array(buffer)).map((b) => b.toString(16).padStart(2, "0")).join("");
}
if (typeof window !== "undefined") {
  window.conectarNube = async function() {
    if (!window.NUBE_URL || !window.NUBE_CLAVE) return false;
    try {
      window.__nubeCliente = createClient(window.NUBE_URL, window.NUBE_CLAVE);
      window.__nubeActiva = true;
      return true;
    } catch (e) {
      console.warn("Sin nube, se trabaja en local:", e && e.message);
      window.__nubeActiva = false;
      return false;
    }
  };
  window.getSupabaseClient = async function() {
    return window.__nubeCliente;
  };
}
function esMovimientoNuevo(m) {
  return m != null && m.operationId !== void 0 && m.operationId !== null;
}
function esSalida(m) {
  if (!m) return false;
  if (esMovimientoNuevo(m)) return Number(m.cantidad) < 0;
  return m.tipo === "salida";
}
function esEntrada(m) {
  if (!m) return false;
  if (esMovimientoNuevo(m)) return Number(m.cantidad) > 0;
  return m.tipo === "entrada";
}
function esVenta(m) {
  if (!m) return false;
  if (esMovimientoNuevo(m)) return m.tipo === "VENTA" || m.tipo === "VENTA_ENCARGO";
  return m.tipo === "salida" && m.motivo === "Venta";
}
var MOTIVOS_MERMA_ANTIGUOS = /* @__PURE__ */ new Set(["Merma", "Autoconsumo", "Rotura"]);
var MOTIVOS_MERMA = [
  "Merma",
  "Autoconsumo",
  "Rotura",
  "Producci\xF3n",
  "Caducidad",
  "Deterioro",
  "Robo o p\xE9rdida",
  "Error de registro",
  "Devoluci\xF3n no registrada",
  "Consumo interno",
  "Degustaci\xF3n",
  "Producto defectuoso"
];
function esMerma(m) {
  if (!m) return false;
  if (esMovimientoNuevo(m)) return m.tipo === "MERMA";
  return m.tipo === "salida" && MOTIVOS_MERMA_ANTIGUOS.has(m.motivo);
}
function cantidadConSigno(m) {
  if (!m) return 0;
  if (esMovimientoNuevo(m)) return Number(m.cantidad) || 0;
  const base = Number(m.cantidad) || 0;
  return m.tipo === "salida" ? -base : base;
}
var TIPOS_NO_MERCANCIA = ["utillaje", "limpieza"];
var esNoMercancia = (p) => TIPOS_NO_MERCANCIA.includes(p?.tipo || "materia_prima");
var aISO = (d) => {
  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, "0");
  const dd = String(d.getDate()).padStart(2, "0");
  return `${y}-${m}-${dd}`;
};
var todayISO = () => aISO(/* @__PURE__ */ new Date());
var primerDiaMes = (fecha) => {
  const d = new Date(fecha);
  return aISO(new Date(d.getFullYear(), d.getMonth(), 1));
};
var ultimoDiaMes = (fecha) => {
  const d = new Date(fecha);
  return aISO(new Date(d.getFullYear(), d.getMonth() + 1, 0));
};
function horasDeEmpleadoEnMes(fichajes, empleadoId, mes) {
  const delMes = fichajes.filter((f) => f.empleadoId === empleadoId && (f.fecha || "").slice(0, 7) === mes).sort((a, b) => (a.timestamp || "").localeCompare(b.timestamp || ""));
  let total = 0;
  let entrada = null;
  delMes.forEach((f) => {
    if (f.tipo === "entrada") entrada = f;
    else if (f.tipo === "salida" && entrada) {
      const ini = /* @__PURE__ */ new Date(`${entrada.fecha}T${entrada.hora}:00`);
      const fin = /* @__PURE__ */ new Date(`${f.fecha}T${f.hora}:00`);
      const horas = (fin - ini) / 36e5;
      if (horas > 0 && horas < 24) total += horas;
      entrada = null;
    }
  });
  return total;
}
function costePorHoraEquipoEnMes(nominas, fichajes, mes) {
  const nominasDelMes = nominas.filter((n) => n.mes === mes);
  const costeTotalMes = nominasDelMes.reduce((a, n) => a + (Number(n.costeTotalEmpresa) || 0), 0);
  const horasTotalesMes = nominasDelMes.reduce((a, n) => a + horasDeEmpleadoEnMes(fichajes, n.empleadoId, mes), 0);
  return horasTotalesMes > 0 ? costeTotalMes / horasTotalesMes : null;
}
async function loadKey(key, fallback, retries = 2) {
  for (let attempt = 0; attempt <= retries; attempt++) {
    try {
      const res = await window.storage.get(key, false);
      if (res && res.value) return JSON.parse(res.value);
      return fallback;
    } catch (e) {
      if (attempt === retries) {
        console.error("No se pudo cargar", key, "tras varios intentos:", e);
        return fallback;
      }
      await new Promise((r) => setTimeout(r, 350));
    }
  }
  return fallback;
}
async function saveKey(key, value) {
  try {
    await window.storage.set(key, JSON.stringify(value), false);
  } catch (e) {
    console.error("Error guardando", key, e);
    if (typeof window !== "undefined" && window.dispatchEvent) {
      window.dispatchEvent(new CustomEvent("fallo-guardado", { detail: { key, mensaje: e && e.message } }));
    }
  }
}
function GestionAlmacen() {
  const [ready, setReady] = (0, import_react.useState)(false);
  const [fallosGuardado, setFallosGuardado] = (0, import_react.useState)([]);
  (0, import_react.useEffect)(() => {
    function onFalloGuardado(e) {
      setFallosGuardado((s) => [
        { id: uid(), key: e.detail.key, mensaje: e.detail.mensaje || "", fecha: (/* @__PURE__ */ new Date()).toISOString() },
        ...s
      ].slice(0, 20));
    }
    window.addEventListener("fallo-guardado", onFalloGuardado);
    return () => window.removeEventListener("fallo-guardado", onFalloGuardado);
  }, []);
  const [tab, setTab] = (0, import_react.useState)("dashboard");
  const [disenoMenu, setDisenoMenu] = (0, import_react.useState)("B");
  const [temaOscuro, setTemaOscuro] = (0, import_react.useState)(false);
  const [modoEmpleado, setModoEmpleado] = (0, import_react.useState)(false);
  const [pinPropietario, setPinPropietario] = (0, import_react.useState)("");
  const [auditoria, setAuditoria] = (0, import_react.useState)([]);
  const [usuarioActivoId, setUsuarioActivoId] = (0, import_react.useState)(null);
  const [ordenesProduccion, setOrdenesProduccion] = (0, import_react.useState)([]);
  const [traspasos, setTraspasos] = (0, import_react.useState)([]);
  const [facturasDirectas, setFacturasDirectas] = (0, import_react.useState)([]);
  const [nominas, setNominas] = (0, import_react.useState)([]);
  const [entrevistas, setEntrevistas] = (0, import_react.useState)([]);
  const skipSaveRef = import_react.default.useRef(true);
  const autoSnapshotRef = import_react.default.useRef(false);
  const [proveedores, setProveedores] = (0, import_react.useState)([]);
  const [productos, setProductos] = (0, import_react.useState)([]);
  const [pedidos, setPedidos] = (0, import_react.useState)([]);
  const [movimientos, setMovimientos] = (0, import_react.useState)([]);
  const [conteos, setConteos] = (0, import_react.useState)([]);
  const [fichasCosto, setFichasCosto] = (0, import_react.useState)([]);
  const [albaranes, setAlbaranes] = (0, import_react.useState)([]);
  const [catalogoProv, setCatalogoProv] = (0, import_react.useState)({});
  const [gastosGenerales, setGastosGenerales] = (0, import_react.useState)([]);
  const [empleados, setEmpleados] = (0, import_react.useState)([]);
  const [fichajes, setFichajes] = (0, import_react.useState)([]);
  const [registrosAppcc, setRegistrosAppcc] = (0, import_react.useState)([]);
  const [freidoras, setFreidoras] = (0, import_react.useState)([]);
  const [registrosAceite, setRegistrosAceite] = (0, import_react.useState)([]);
  const [puntosControl, setPuntosControl] = (0, import_react.useState)([]);
  const [clientes, setClientes] = (0, import_react.useState)([]);
  const [encargos, setEncargos] = (0, import_react.useState)([]);
  const [arqueos, setArqueos] = (0, import_react.useState)([]);
  const [turnos, setTurnos] = (0, import_react.useState)([]);
  const [historial, setHistorial] = (0, import_react.useState)([]);
  (0, import_react.useEffect)(() => {
    (async () => {
      const [p, pr, pe, mo, co, fc, hi, al, cp, gg, em, fj, dm, ra, pc, cl, en, aq, tu, to, me, pin, au, op, tr, ua, fd, nom, fre, rac, entr] = await Promise.all([
        loadKey("proveedores", []),
        loadKey("productos", []),
        loadKey("pedidos", []),
        loadKey("movimientos", []),
        loadKey("conteos", []),
        loadKey("fichasCosto", []),
        loadKey("historialRespaldos", []),
        loadKey("albaranes", []),
        loadKey("catalogoProv", {}),
        loadKey("gastosGenerales", []),
        loadKey("empleados", []),
        loadKey("fichajes", []),
        loadKey("disenoMenu", "B"),
        loadKey("registrosAppcc", []),
        loadKey("puntosControl", []),
        loadKey("clientes", []),
        loadKey("encargos", []),
        loadKey("arqueos", []),
        loadKey("turnos", []),
        loadKey("temaOscuro", false),
        loadKey("modoEmpleado", false),
        loadKey("pinPropietario", ""),
        loadKey("auditoria", []),
        loadKey("ordenesProduccion", []),
        loadKey("traspasos", []),
        loadKey("usuarioActivoId", null),
        loadKey("facturasDirectas", []),
        loadKey("nominas", []),
        loadKey("entrevistas", []),
        loadKey("freidoras", []),
        loadKey("registrosAceite", [])
      ]);
      const alLimpios = (al || []).map((a) => {
        if (!a || a.fotoOrigenIA === void 0) return a;
        const copia = { ...a };
        delete copia.fotoOrigenIA;
        return copia;
      });
      setProveedores(p);
      setProductos(pr);
      setPedidos(pe);
      setMovimientos(mo);
      setConteos(co);
      setFichasCosto(fc);
      setHistorial(hi);
      setAlbaranes(alLimpios);
      const habiaFotos = (al || []).some((a) => a && a.fotoOrigenIA !== void 0);
      setCatalogoProv(cp);
      setGastosGenerales(gg);
      setEmpleados(em);
      setFichajes(fj);
      setDisenoMenu(dm || "B");
      setRegistrosAppcc(ra);
      setFreidoras(fre || []);
      setRegistrosAceite(rac || []);
      setPuntosControl(pc);
      setClientes(cl);
      setEncargos(en);
      setArqueos(aq);
      setTurnos(tu);
      setTemaOscuro(!!to);
      setModoEmpleado(!!me);
      setPinPropietario(pin || "");
      setAuditoria(au);
      setOrdenesProduccion(op);
      setTraspasos(tr);
      setUsuarioActivoId(ua || null);
      setFacturasDirectas(fd);
      setNominas(nom);
      setEntrevistas(entr);
      setReady(true);
      if (habiaFotos) await saveKey("albaranes", alLimpios);
      setTimeout(() => {
        skipSaveRef.current = false;
      }, 400);
      if (!autoSnapshotRef.current) {
        autoSnapshotRef.current = true;
        const tieneDatos = p.length || pr.length || pe.length || mo.length || co.length || fc.length || al.length || em.length;
        const hoy = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
        const yaHayUnoDeHoy = hi.some((h) => h.automatica && (h.fecha || "").slice(0, 10) === hoy);
        if (tieneDatos && !yaHayUnoDeHoy) {
          const snapshot = {
            id: uid(),
            fecha: (/* @__PURE__ */ new Date()).toISOString(),
            automatica: true,
            motivo: "automatico-diario",
            backupVersion: 3,
            data: { proveedores: p, productos: pr, pedidos: pe, movimientos: mo, conteos: co, fichasCosto: fc, albaranes: alLimpios, catalogoProv: cp, gastosGenerales: gg, empleados: em, fichajes: fj, registrosAppcc: ra, puntosControl: pc, clientes: cl, encargos: en, arqueos: aq, turnos: tu, nominas: nom, facturasDirectas: fd, ordenesProduccion: op, traspasos: tr, auditoria: au, freidoras: fre, registrosAceite: rac }
          };
          const historialFinal = [snapshot, ...hi].slice(0, 30);
          setHistorial(historialFinal);
          await saveKey("historialRespaldos", historialFinal);
        }
      }
    })();
  }, []);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("proveedores", proveedores);
  }, [proveedores, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("productos", productos);
  }, [productos, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("pedidos", pedidos);
  }, [pedidos, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("movimientos", movimientos);
  }, [movimientos, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("conteos", conteos);
  }, [conteos, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("fichasCosto", fichasCosto);
  }, [fichasCosto, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("historialRespaldos", historial);
  }, [historial, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("albaranes", albaranes);
  }, [albaranes, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("catalogoProv", catalogoProv);
  }, [catalogoProv, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("gastosGenerales", gastosGenerales);
  }, [gastosGenerales, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("empleados", empleados);
  }, [empleados, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("fichajes", fichajes);
  }, [fichajes, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("disenoMenu", disenoMenu);
  }, [disenoMenu, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("registrosAppcc", registrosAppcc);
  }, [registrosAppcc, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("freidoras", freidoras);
  }, [freidoras, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("registrosAceite", registrosAceite);
  }, [registrosAceite, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("puntosControl", puntosControl);
  }, [puntosControl, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("clientes", clientes);
  }, [clientes, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("encargos", encargos);
  }, [encargos, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("arqueos", arqueos);
  }, [arqueos, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("turnos", turnos);
  }, [turnos, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("temaOscuro", temaOscuro);
  }, [temaOscuro, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("modoEmpleado", modoEmpleado);
  }, [modoEmpleado, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("pinPropietario", pinPropietario);
  }, [pinPropietario, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("auditoria", auditoria);
  }, [auditoria, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("ordenesProduccion", ordenesProduccion);
  }, [ordenesProduccion, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("traspasos", traspasos);
  }, [traspasos, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("usuarioActivoId", usuarioActivoId);
  }, [usuarioActivoId, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("facturasDirectas", facturasDirectas);
  }, [facturasDirectas, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("entrevistas", entrevistas);
  }, [entrevistas, ready]);
  (0, import_react.useEffect)(() => {
    if (ready && !skipSaveRef.current) saveKey("nominas", nominas);
  }, [nominas, ready]);
  const itemsPermitidosEmpleado = (0, import_react.useMemo)(() => {
    const activo = usuarioActivoId ? empleados.find((e) => e.id === usuarioActivoId) : null;
    const rol = activo?.rol && ROLES_EMPLEADO[activo.rol] ? activo.rol : "Est\xE1ndar";
    return ROLES_EMPLEADO[rol];
  }, [usuarioActivoId, empleados]);
  (0, import_react.useEffect)(() => {
    if (modoEmpleado && !itemsPermitidosEmpleado.includes(tab)) setTab("dashboard");
  }, [modoEmpleado, itemsPermitidosEmpleado]);
  const productoPorId = (id) => productos.find((p) => p.id === id);
  const proveedorPorId = (id) => proveedores.find((p) => p.id === id);
  const [prefillAlbaran, setPrefillAlbaran] = (0, import_react.useState)(null);
  const [facturaDirectaResaltada, setFacturaDirectaResaltada] = (0, import_react.useState)(null);
  const [pedidoParaFotoIA, setPedidoParaFotoIA] = (0, import_react.useState)(null);
  const [pendingRestore, setPendingRestore] = (0, import_react.useState)(null);
  const [restoreError, setRestoreError] = (0, import_react.useState)("");
  const [showBackupView, setShowBackupView] = (0, import_react.useState)(false);
  const [backupText, setBackupText] = (0, import_react.useState)("");
  const [showRestoreInput, setShowRestoreInput] = (0, import_react.useState)(false);
  const [restoreText, setRestoreText] = (0, import_react.useState)("");
  const [copiado, setCopiado] = (0, import_react.useState)(false);
  const {
    crearPuntoDeGuardado,
    restaurarDesdeHistorial,
    abrirRespaldo,
    copiarRespaldo,
    abrirRestaurar,
    analizarTextoRestauracion,
    confirmarRestauracion,
    exportarExcelGeneral,
    compararConEstadoActual,
    coleccionesQueSeConservan
  } = crearLogicaRespaldos({
    proveedores,
    productos,
    pedidos,
    movimientos,
    conteos,
    fichasCosto,
    albaranes,
    catalogoProv,
    gastosGenerales,
    empleados,
    fichajes,
    registrosAppcc,
    puntosControl,
    clientes,
    encargos,
    arqueos,
    turnos,
    nominas,
    facturasDirectas,
    ordenesProduccion,
    traspasos,
    auditoria,
    freidoras,
    registrosAceite,
    setProveedores,
    setProductos,
    setPedidos,
    setMovimientos,
    setConteos,
    setFichasCosto,
    setAlbaranes,
    setCatalogoProv,
    setGastosGenerales,
    setEmpleados,
    setFichajes,
    setRegistrosAppcc,
    setPuntosControl,
    setClientes,
    setEncargos,
    setArqueos,
    setTurnos,
    setNominas,
    setFacturasDirectas,
    setOrdenesProduccion,
    setTraspasos,
    setAuditoria,
    setFreidoras,
    setRegistrosAceite,
    setHistorial,
    setPendingRestore,
    pendingRestore,
    setBackupText,
    backupText,
    setCopiado,
    setShowBackupView,
    setShowRestoreInput,
    setRestoreText,
    restoreText,
    setRestoreError,
    proveedorPorId,
    productoPorId,
    registrarAuditoria
  });
  const { addProveedor, updateProveedor, deleteProveedor } = crearLogicaProveedores({ proveedores, setProveedores, registrarAuditoria });
  const { addGasto, deleteGasto } = crearLogicaGastos({ setGastosGenerales });
  const { addEmpleado, updateEmpleado, deleteEmpleado, anonimizarEmpleado, registrarAusencia, eliminarAusencia, registrarEpi, eliminarEpi } = crearLogicaPersonal({ empleados, setEmpleados, registrarAuditoria, setNominas });
  const { addTurno, updateTurno, deleteTurno, copiarSemana } = crearLogicaTurnos({ turnos, setTurnos });
  const { producir, anularProduccion } = crearLogicaProduccion({ fichasCosto, productos, setProductos, movimientos, setMovimientos, setOrdenesProduccion, registrarAuditoria });
  const { venderCarrito, venderLocal, anularVenta, venderLineas } = crearLogicaVenta({ productos, setProductos, movimientos, setMovimientos });
  const { addCliente, updateCliente, deleteCliente, anonimizarCliente } = crearLogicaClientes({ clientes, setClientes, registrarAuditoria });
  const { addEncargo, updateEncargo, deleteEncargo, entregarEncargo } = crearLogicaEncargos({ encargos, setEncargos, registrarAuditoria, productos, setProductos, setMovimientos, venderLineas });
  const { traspasarStock } = crearLogicaTraspasos({ productos, setProductos, movimientos, setMovimientos, setTraspasos, registrarAuditoria });
  const { addArqueo, deleteArqueo } = crearLogicaCaja({ setArqueos });
  const { activarModoEmpleado, entrarComoEmpleado, salirModoEmpleado, establecerPin } = crearLogicaSeguridad({ pinPropietario, setPinPropietario, empleados, setModoEmpleado, setUsuarioActivoId });
  const { addPuntoControl, updatePuntoControl, deletePuntoControl, registrarAppcc, eliminarRegistroAppcc } = crearLogicaAppcc({ setPuntosControl, setRegistrosAppcc });
  const { fichar, addFichajeManual, updateFichaje, eliminarFichaje } = crearLogicaFichaje({ setFichajes });
  const { addFreidora, updateFreidora, deleteFreidora, registrarCambio, registrarRelleno, eliminarRegistroAceite, consumoPorCiclo } = crearLogicaAceite({ freidoras, setFreidoras, registrosAceite, setRegistrosAceite, productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  const { addFichaCosto, updateFichaCosto, deleteFichaCosto, alergenosDeFicha } = crearLogicaFichasCosto({ productos, setFichasCosto });
  const { crearProductoEnConteo, iniciarConteo, actualizarConteoItem, actualizarResponsable, finalizarConteo, aplicarAjustes, eliminarConteo } = crearLogicaConteos({ productos, setProductos, conteos, setConteos, movimientos, setMovimientos, registrarAuditoria });
  const conteoAbierto = (0, import_react.useMemo)(() => conteos.find((c) => !c.completado) || null, [conteos]);
  const almacenCongelado = !!conteoAbierto;
  const { addProducto, updateProducto, deleteProducto, reactivarProducto, registrarSalida, ajustarProductoPorOtro } = crearLogicaProductos({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria, almacenCongelado, addGasto });
  const { diagnosticarStock, corregirProducto, movimientosParaReconciliar } = crearLogicaReconciliacion({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  const { buscarEnCatalogo, aprenderReferencia, guardarAlbaran, eliminarAlbaran, marcarPagada, confirmarAlbaran, anularAlbaran, recibirConAlbaran, recibirConFotoIA, duplicadosDe, desviacionesDePrecio, procesarRecepcion } = crearLogicaAlbaranes({
    catalogoProv,
    setCatalogoProv,
    albaranes,
    setAlbaranes,
    productos,
    setProductos,
    movimientos,
    setMovimientos,
    setPedidos,
    registrarAuditoria,
    proveedorPorId,
    setPrefillAlbaran,
    setPedidoParaFotoIA,
    setTab
  });
  const { crearPedido, actualizarPedido, eliminarPedido, recibirPedido, cerrarPedido } = crearLogicaPedidos({ pedidos, setPedidos, productos, setProductos, setMovimientos, almacenCongelado, procesarRecepcion });
  const { addFacturaDirecta, updateFacturaDirecta, deleteFacturaDirecta, marcarPagadaFacturaDirecta } = crearLogicaFacturasDirectas({ facturasDirectas, setFacturasDirectas, registrarAuditoria, addGasto, deleteGasto, gastosGenerales });
  const { addNomina, updateNomina, deleteNomina } = crearLogicaNominas({ setNominas, registrarAuditoria });
  const { crearEntrevista, actualizarEntrevista, finalizarEntrevista, eliminarEntrevista } = crearLogicaEntrevistas({ entrevistas, setEntrevistas, registrarAuditoria });
  function registrarAuditoria(accion, detalle) {
    const empleadoActivo = usuarioActivoId ? empleados.find((e) => e.id === usuarioActivoId) : null;
    const usuario = modoEmpleado ? empleadoActivo ? empleadoActivo.nombre : "Empleado sin identificar" : "Propietario/a";
    const entrada = { id: uid(), fecha: todayISO(), hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5), usuario, accion, detalle };
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) {
      setAuditoria((s) => [entrada, ...s].slice(0, 500));
      return;
    }
    (async () => {
      try {
        const supabase = await window.getSupabaseClient();
        const r = await Promise.race([
          supabase.rpc("registrar_auditoria", {
            p_id: entrada.id,
            p_usuario: usuario,
            p_accion: accion,
            p_detalle: detalle,
            p_fecha: entrada.fecha,
            p_hora: entrada.hora
          }),
          new Promise((_, reject) => setTimeout(() => reject(new Error("La auditor\xEDa tard\xF3 demasiado en responder")), 6e3))
        ]);
        if (r.error) throw r.error;
        const rAud = await supabase.from("auditoria_registro").select("datos").order("creado_en", { ascending: false }).limit(500);
        if (!rAud.error && rAud.data) setAuditoria(rAud.data.map((fila) => fila.datos));
      } catch (e) {
        setAuditoria((s) => [entrada, ...s].slice(0, 500));
      }
    })();
  }
  const esUtillaje = (p) => esNoMercancia(p);
  const valorInventario = (0, import_react.useMemo)(
    () => productos.filter((p) => !esUtillaje(p)).reduce((acc, p) => acc + (Number(p.stock) || 0) * Number(p.costo || 0), 0),
    [productos]
  );
  const valorUtillaje = (0, import_react.useMemo)(
    () => productos.filter(esUtillaje).reduce((acc, p) => acc + (Number(p.stock) || 0) * Number(p.costo || 0), 0),
    [productos]
  );
  const stockBajo = (0, import_react.useMemo)(
    () => productos.filter((p) => p.tipo !== "elaborado" && p.stock <= Number(p.stockMinimo || 0)),
    [productos]
  );
  const diagnosticoStock = (0, import_react.useMemo)(() => diagnosticarStock(), [productos, movimientos]);
  const descuadresPendientes = (0, import_react.useMemo)(() => diagnosticoStock.filter((d) => !d.coincide).length, [diagnosticoStock]);
  const pisoVentaBajo = (0, import_react.useMemo)(
    () => productos.filter((p) => p.tipo !== "elaborado" && Number(p.stockMinimoPisoVenta) > 0 && (Number(p.stockPisoVenta) || 0) <= Number(p.stockMinimoPisoVenta)),
    [productos]
  );
  const analisisABC = (0, import_react.useMemo)(() => {
    const hace12meses = /* @__PURE__ */ new Date();
    hace12meses.setMonth(hace12meses.getMonth() - 12);
    const consumo = {};
    movimientos.forEach((m) => {
      if (!esSalida(m)) return;
      if (new Date(m.fecha) < hace12meses) return;
      consumo[m.productoId] = (consumo[m.productoId] || 0) + Math.abs(Number(m.cantidad) || 0);
    });
    const totalSalidas = Object.values(consumo).reduce((a, x) => a + x, 0);
    const baseConsumo = totalSalidas > 0;
    const clasificables = productos.filter((p) => !esUtillaje(p));
    const conValor = clasificables.map((p) => {
      const unidades = consumo[p.id] || 0;
      const costo = Number(p.costo) || 0;
      const valor = baseConsumo ? unidades * costo : (Number(p.stock) || 0) * costo;
      return { id: p.id, valor, unidades, rotacion: (Number(p.stock) || 0) > 0 ? unidades / (Number(p.stock) || 1) : null };
    });
    const total = conValor.reduce((a, x) => a + x.valor, 0);
    const mapa = {};
    const detalle = {};
    if (total <= 0) {
      clasificables.forEach((p) => {
        mapa[p.id] = "C";
        detalle[p.id] = { valor: 0, unidades: 0, rotacion: null };
      });
      return { mapa, detalle, baseConsumo };
    }
    const ordenados = [...conValor].sort((a, b) => b.valor - a.valor);
    let acumulado = 0;
    ordenados.forEach((x) => {
      acumulado += x.valor;
      const pct = acumulado / total * 100;
      mapa[x.id] = pct <= 80 ? "A" : pct <= 95 ? "B" : "C";
      detalle[x.id] = { valor: x.valor, unidades: x.unidades, rotacion: x.rotacion };
    });
    return { mapa, detalle, baseConsumo };
  }, [productos, movimientos]);
  const clasificacionABC = analisisABC.mapa;
  const pedidosPendientes = (0, import_react.useMemo)(() => pedidos.filter((p) => p.estado !== "Recibido"), [pedidos]);
  const margenPromedio = (0, import_react.useMemo)(() => {
    const conPrecio = productos.filter((p) => Number(p.precioVenta) > 0);
    if (!conPrecio.length) return 0;
    const total = conPrecio.reduce((acc, p) => acc + (margenDe(p) || 0), 0);
    return total / conPrecio.length;
  }, [productos]);
  const margenPorProducto = (0, import_react.useMemo)(() => {
    const hace90 = /* @__PURE__ */ new Date();
    hace90.setDate(hace90.getDate() - 90);
    return productos.filter((p) => Number(p.precioVenta) > 0).map((p) => {
      const ventas = movimientos.filter(
        (m) => m.productoId === p.id && esVenta(m) && new Date(m.fecha) >= hace90
      );
      const unidadesVendidas = ventas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0), 0);
      const ingresos = ventas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.ingresoUnitario) || 0), 0);
      const costes = ventas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0), 0);
      return { ...p, unidadesVendidas, ingresos, beneficio: ingresos - costes, margenPct: margenDe(p) };
    }).filter((p) => p.unidadesVendidas > 0).sort((a, b) => b.beneficio - a.beneficio);
  }, [productos, movimientos]);
  const rotacionPorProducto = (0, import_react.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    return productos.map((p) => {
      const salidas = movimientos.filter((m) => m.productoId === p.id && esSalida(m));
      const cantSalidas = salidas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0), 0);
      if (salidas.length === 0) {
        return { ...p, cantSalidas: 0, consumoDiario: null, dias: null, rotacion: null };
      }
      const primera = salidas.reduce((min, m) => new Date(m.fecha) < min ? new Date(m.fecha) : min, new Date(salidas[0].fecha));
      const diasTranscurridos = Math.max(1, Math.round((hoy - primera) / 864e5) + 1);
      const consumoDiario = cantSalidas / diasTranscurridos;
      const dias = consumoDiario > 0 ? (Number(p.stock) || 0) / consumoDiario : null;
      const rotacion = dias ? 365 / dias : null;
      return { ...p, cantSalidas, consumoDiario, dias, rotacion, diasConDatos: diasTranscurridos };
    });
  }, [productos, movimientos]);
  const patronesDesviacionConteo = (0, import_react.useMemo)(() => {
    const porProducto = {};
    movimientos.filter((m) => {
      if (!m.productoId) return false;
      if (esMovimientoNuevo(m)) return m.tipo === "INVENTARIO" && m.origen !== "reconciliacionStock";
      return m.tipo === "ajuste";
    }).forEach((m) => {
      if (!porProducto[m.productoId]) porProducto[m.productoId] = [];
      porProducto[m.productoId].push(m);
    });
    const resultado = [];
    Object.entries(porProducto).forEach(([productoId, ajustes]) => {
      if (ajustes.length < 3) return;
      const negativos = ajustes.filter((a) => Number(a.cantidad) < 0).length;
      const positivos = ajustes.filter((a) => Number(a.cantidad) > 0).length;
      const total = ajustes.length;
      const proporcionMayor = Math.max(negativos, positivos) / total;
      if (proporcionMayor < 0.75) return;
      const prod = productos.find((p) => p.id === productoId);
      const acumulado = ajustes.reduce((a, m) => a + Number(m.cantidad), 0);
      const ultimaFecha = ajustes.reduce((max, m) => m.fecha > max ? m.fecha : max, ajustes[0].fecha);
      resultado.push({
        productoId,
        nombre: prod ? prod.nombre : "Producto eliminado",
        unidad: prod ? prod.unidad : "",
        direccion: negativos > positivos ? "falta" : "sobra",
        numAjustes: total,
        acumulado,
        ultimaFecha
      });
    });
    return resultado.sort((a, b) => Math.abs(b.acumulado) - Math.abs(a.acumulado));
  }, [movimientos, productos]);
  function diasHastaProximaEntrega(leadTime, diasReparto) {
    if (!diasReparto || diasReparto.length === 0) return leadTime;
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    for (let n = leadTime; n <= leadTime + 14; n++) {
      const fecha = new Date(hoy);
      fecha.setDate(fecha.getDate() + n);
      if (diasReparto.includes(fecha.getDay())) return n;
    }
    return leadTime;
  }
  const productosEnPedidoPendiente = (0, import_react.useMemo)(() => {
    const ids = /* @__PURE__ */ new Set();
    pedidosPendientes.forEach((pe) => (pe.items || []).forEach((it) => ids.add(it.productoId)));
    return ids;
  }, [pedidosPendientes]);
  const DIAS_SEGURIDAD_PEDIDO = 7;
  const sugerenciasPedido = (0, import_react.useMemo)(() => {
    return rotacionPorProducto.filter((p) => !esNoMercancia(p) && p.tipo !== "elaborado" && p.proveedorId && p.dias !== null).map((p) => {
      const prov = proveedores.find((pv) => pv.id === p.proveedorId);
      const leadTime = prov ? Number(prov.leadTime) || 0 : 0;
      const diasHastaEntrega = diasHastaProximaEntrega(leadTime, prov ? prov.diasReparto : []);
      const cantidadCubrir = p.consumoDiario * (diasHastaEntrega + DIAS_SEGURIDAD_PEDIDO);
      const cantidadSugerida = Math.max(0, Math.ceil(cantidadCubrir - (Number(p.stock) || 0)));
      const cajasSugeridas = p.udsPorCaja > 1 ? Math.ceil(cantidadSugerida / p.udsPorCaja) : null;
      return { ...p, proveedorNombre: prov ? prov.nombre : "\u2014", leadTime, diasHastaEntrega, margen: p.dias - diasHastaEntrega, cantidadSugerida, cajasSugeridas };
    }).filter((p) => p.leadTime > 0 && p.margen <= 1 && !productosEnPedidoPendiente.has(p.id)).sort((a, b) => a.margen - b.margen);
  }, [rotacionPorProducto, proveedores, productosEnPedidoPendiente]);
  const lotesCaducidad = (0, import_react.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    const filas = [];
    albaranes.forEach((a) => {
      if (a.estado !== "confirmado") return;
      (a.lineas || []).forEach((ln, idx) => {
        if (!ln.caducidad) return;
        const f = new Date(ln.caducidad);
        if (isNaN(f)) return;
        const dias = Math.round((f - hoy) / 864e5);
        const prod = productos.find((x) => x.id === ln.productoId);
        filas.push({
          // El índice de línea evita que dos lotes distintos del mismo
          // producto en el mismo albarán (dos entregas con caducidades
          // distintas) generen la misma clave y colisionen como key de React.
          id: `${a.id}-${idx}-${ln.codigoProveedor || ln.descripcion}`,
          productoId: ln.productoId,
          nombre: prod ? prod.nombre : ln.descripcion,
          lote: ln.lote || "",
          caducidad: ln.caducidad,
          dias,
          unidades: Number(ln.unidadesEntradas) || 0,
          albaran: a.numero,
          proveedorId: a.proveedorId
        });
      });
    });
    const porProducto = {};
    filas.forEach((l) => {
      const key = l.productoId || l.nombre;
      if (!porProducto[key]) porProducto[key] = [];
      porProducto[key].push(l);
    });
    const filasAjustadas = [];
    Object.entries(porProducto).forEach(([key, lotes]) => {
      const prod = productos.find((p) => p.id === key);
      const stockActual = prod ? Number(prod.stock) || 0 : lotes.reduce((a, l) => a + l.unidades, 0);
      const ordenados = [...lotes].sort((x, y) => x.dias - y.dias);
      const totalNominal = ordenados.reduce((a, l) => a + l.unidades, 0);
      let porDescontar = Math.max(0, totalNominal - stockActual);
      ordenados.forEach((l) => {
        if (porDescontar >= l.unidades) {
          porDescontar -= l.unidades;
          return;
        }
        const quedan = l.unidades - porDescontar;
        porDescontar = 0;
        filasAjustadas.push({ ...l, unidades: quedan, parcial: quedan < l.unidades });
      });
    });
    return filasAjustadas.sort((x, y) => x.dias - y.dias);
  }, [albaranes, productos]);
  const caducanPronto = (0, import_react.useMemo)(() => lotesCaducidad.filter((l) => l.dias <= 30), [lotesCaducidad]);
  const recordatorioConteo = (0, import_react.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    const ambitos = [
      { id: "piso_venta", nombre: "Piso de venta", limiteDias: 14 },
      { id: "almacen", nombre: "Almac\xE9n (trastienda)", limiteDias: 30 },
      { id: "total", nombre: "Total (empresa completa)", limiteDias: 30 }
    ];
    return ambitos.map((amb) => {
      const delAmbito = conteos.filter((c) => c.completado && (c.ambito || "total") === amb.id);
      if (delAmbito.length === 0) {
        return { ...amb, ultimaFecha: null, diasDesde: null, atrasado: true };
      }
      const ultima = delAmbito.reduce((max, c) => c.fecha > max ? c.fecha : max, delAmbito[0].fecha);
      const diasDesde = Math.round((hoy - new Date(ultima)) / 864e5);
      return { ...amb, ultimaFecha: ultima, diasDesde, atrasado: diasDesde > amb.limiteDias };
    });
  }, [conteos]);
  const conteosAtrasados = (0, import_react.useMemo)(() => recordatorioConteo.filter((a) => a.atrasado), [recordatorioConteo]);
  const alertasAppcc = (0, import_react.useMemo)(() => {
    const hoy = todayISO();
    const activos = (puntosControl || []).filter((p) => p.activo !== false);
    const pendientes = activos.filter((p) => p.frecuencia !== "En cada recepci\xF3n").map((p) => {
      const registrosDelPunto = (registrosAppcc || []).filter((r) => r.puntoId === p.id);
      const deHoy = registrosDelPunto.filter((r) => r.fecha === hoy).length;
      let faltan = 0;
      if (p.frecuencia === "Diaria") faltan = deHoy >= 1 ? 0 : 1;
      else if (p.frecuencia === "Dos veces al d\xEDa") faltan = Math.max(0, 2 - deHoy);
      else if (p.frecuencia === "Semanal") {
        const hace7 = /* @__PURE__ */ new Date();
        hace7.setDate(hace7.getDate() - 7);
        const enLaSemana = registrosDelPunto.some((r) => new Date(r.fecha) >= hace7);
        faltan = enLaSemana ? 0 : 1;
      }
      return { ...p, faltan };
    }).filter((p) => p.faltan > 0);
    const desviaciones = (registrosAppcc || []).filter((r) => r.conforme === false).filter((r) => {
      const posteriorConforme = (registrosAppcc || []).some(
        (otro) => otro.puntoId === r.puntoId && otro.conforme !== false && `${otro.fecha} ${otro.hora || ""}` > `${r.fecha} ${r.hora || ""}`
      );
      return !posteriorConforme;
    }).sort((a, b) => `${b.fecha} ${b.hora || ""}`.localeCompare(`${a.fecha} ${a.hora || ""}`));
    return { pendientes, desviaciones };
  }, [puntosControl, registrosAppcc]);
  (0, import_react.useEffect)(() => {
    const emp = usuarioActivoId ? empleados.find((e) => e.id === usuarioActivoId) : null;
    window.__usuarioActivoNombre = modoEmpleado ? emp ? emp.nombre : "Empleado sin identificar" : "Propietario/a";
  }, [usuarioActivoId, empleados, modoEmpleado]);
  const documentosPersonalCaducan = (0, import_react.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    const filas = [];
    empleados.filter((e) => e.activo !== false).forEach((e) => {
      (e.documentos || []).forEach((d) => {
        if (!d.fechaCaducidad) return;
        const f = new Date(d.fechaCaducidad);
        if (isNaN(f)) return;
        const dias = Math.round((f - hoy) / 864e5);
        filas.push({ id: `${e.id}-doc-${d.id}`, empleado: e.nombre, concepto: d.nombre, fecha: d.fechaCaducidad, dias });
      });
      if (e.fechaFinContrato) {
        const f = new Date(e.fechaFinContrato);
        if (!isNaN(f)) {
          const dias = Math.round((f - hoy) / 864e5);
          filas.push({ id: `${e.id}-contrato`, empleado: e.nombre, concepto: "Fin de contrato", fecha: e.fechaFinContrato, dias });
        }
      }
    });
    return filas.sort((a, b) => a.dias - b.dias);
  }, [empleados]);
  const documentosPersonalPronto = (0, import_react.useMemo)(() => documentosPersonalCaducan.filter((d) => d.dias <= 30), [documentosPersonalCaducan]);
  const encargosPendientes = (0, import_react.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    return encargos.filter((e) => e.estado !== "Entregado" && e.estado !== "Cancelado").map((e) => {
      const f = new Date(e.fechaEntrega);
      const dias = isNaN(f) ? null : Math.round((f - hoy) / 864e5);
      const total = (e.lineas || []).reduce((a, ln) => a + (Number(ln.cantidad) || 0) * (Number(ln.precioUnitario) || 0), 0);
      return { ...e, dias, total, cliente: clientes.find((c) => c.id === e.clienteId) };
    }).sort((a, b) => (a.dias ?? 9999) - (b.dias ?? 9999));
  }, [encargos, clientes]);
  const encargosUrgentes = (0, import_react.useMemo)(() => encargosPendientes.filter((e) => e.dias !== null && e.dias <= 2), [encargosPendientes]);
  const analisisClientes = (0, import_react.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    return clientes.map((c) => {
      const susVentas = movimientos.filter((m) => m.clienteId === c.id && esVenta(m));
      const facturado = susVentas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.ingresoUnitario) || 0), 0);
      const fechas = susVentas.map((m) => m.fecha).sort();
      const ultima = fechas[fechas.length - 1] || null;
      const diasSinComprar = ultima ? Math.round((hoy - new Date(ultima)) / 864e5) : null;
      const nEncargos = encargos.filter((e) => e.clienteId === c.id && e.estado === "Entregado").length;
      return { ...c, facturado, ultima, diasSinComprar, nEncargos };
    }).sort((a, b) => b.facturado - a.facturado);
  }, [clientes, movimientos, encargos]);
  const clientesDormidos = (0, import_react.useMemo)(
    () => analisisClientes.filter((c) => c.nEncargos > 0 && c.diasSinComprar !== null && c.diasSinComprar > 60),
    [analisisClientes]
  );
  const ventaCruzada = (0, import_react.useMemo)(() => {
    const entregados = encargos.filter((e) => e.estado === "Entregado");
    const pares = {};
    entregados.forEach((e) => {
      const ids = [...new Set((e.lineas || []).map((l) => l.productoId).filter(Boolean))];
      for (let i = 0; i < ids.length; i++) {
        for (let j = i + 1; j < ids.length; j++) {
          const key = [ids[i], ids[j]].sort().join("|");
          pares[key] = (pares[key] || 0) + 1;
        }
      }
    });
    return {
      base: entregados.length,
      filas: Object.entries(pares).map(([key, veces]) => {
        const [a, b] = key.split("|");
        return { a: productos.find((p) => p.id === a), b: productos.find((p) => p.id === b), veces };
      }).filter((x) => x.a && x.b).sort((x, y) => y.veces - x.veces).slice(0, 8)
    };
  }, [encargos, productos]);
  const appccPendientesHoy = alertasAppcc.pendientes;
  const fichajesAbiertos = (0, import_react.useMemo)(() => {
    const hoy = todayISO();
    const porEmpleadoFecha = {};
    fichajes.forEach((f) => {
      const key = `${f.empleadoId}-${f.fecha}`;
      if (!porEmpleadoFecha[key]) porEmpleadoFecha[key] = [];
      porEmpleadoFecha[key].push(f);
    });
    const abiertos = [];
    Object.entries(porEmpleadoFecha).forEach(([key, lista]) => {
      const fecha = lista[0].fecha;
      if (fecha === hoy) return;
      const entradas = lista.filter((f) => f.tipo === "entrada").length;
      const salidas = lista.filter((f) => f.tipo === "salida").length;
      if (entradas > salidas) {
        const empleadoId = lista[0].empleadoId;
        const emp = empleados.find((e) => e.id === empleadoId);
        abiertos.push({ empleadoId, fecha, nombre: emp ? emp.nombre : "\u2014" });
      }
    });
    return abiertos;
  }, [fichajes, empleados]);
  const facturasPorPagar = (0, import_react.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    function calcularVencimiento(fechaFactura, diasPago) {
      if (diasPago === null || !fechaFactura) return { vencimiento: null, dias: null };
      const v = new Date(fechaFactura);
      v.setDate(v.getDate() + diasPago);
      return { vencimiento: aISO(v), dias: Math.round((v - hoy) / 864e5) };
    }
    const deAlbaranes = albaranes.filter((a) => a.estado === "confirmado" && a.esFactura !== false).map((a) => {
      const prov = proveedorPorId(a.proveedorId);
      const base = a.lineas.reduce((x, ln) => x + (Number(ln.importe) || 0), 0);
      const ivaLineas = a.lineas.reduce((x, ln) => x + (Number(ln.importe) || 0) * ((Number(ln.ivaPct) || 0) / 100), 0);
      const cargosBase = Number(a.cargos) || 0;
      const cargosIva = cargosBase * ((Number(a.cargosIva) || 0) / 100);
      const total = base + ivaLineas + cargosBase + cargosIva;
      const fechaFactura = a.fechaFactura || a.fecha;
      const diasPago = prov ? Number(prov.diasPago) || null : null;
      const { vencimiento, dias } = calcularVencimiento(fechaFactura, diasPago);
      return {
        id: a.id,
        origen: "albaran",
        concepto: `Mercanc\xEDa \xB7 ${prov ? prov.nombre : "\u2014"}`,
        proveedor: prov,
        numeroFactura: a.numeroFactura || a.numero || "s/n",
        fechaFactura,
        vencimiento,
        dias,
        total,
        pagada: !!a.pagada
      };
    });
    const deDirectas = facturasDirectas.map((f) => {
      const prov = proveedorPorId(f.proveedorId);
      const diasPago = prov ? Number(prov.diasPago) || null : null;
      const auto = calcularVencimiento(f.fecha, diasPago);
      const vencimiento = f.vencimiento || auto.vencimiento;
      const dias = vencimiento ? Math.round((new Date(vencimiento) - hoy) / 864e5) : null;
      return {
        id: f.id,
        origen: "directa",
        concepto: f.concepto || "Factura directa",
        proveedor: prov,
        numeroFactura: f.numeroFactura || "s/n",
        fechaFactura: f.fecha,
        vencimiento,
        dias,
        total: Number(f.importeTotal) || 0,
        pagada: !!f.pagada
      };
    });
    return [...deAlbaranes, ...deDirectas].sort((x, y) => {
      if (x.pagada !== y.pagada) return x.pagada ? 1 : -1;
      if (x.dias === null) return 1;
      if (y.dias === null) return -1;
      return x.dias - y.dias;
    });
  }, [albaranes, proveedores, facturasDirectas]);
  const pendientesPago = (0, import_react.useMemo)(() => facturasPorPagar.filter((f) => !f.pagada), [facturasPorPagar]);
  const vencenPronto = (0, import_react.useMemo)(() => pendientesPago.filter((f) => f.dias !== null && f.dias <= 7), [pendientesPago]);
  const totalPendientePago = (0, import_react.useMemo)(() => pendientesPago.reduce((a, f) => a + f.total, 0), [pendientesPago]);
  const promedioDiarioVentas = (0, import_react.useMemo)(() => {
    const hace30 = /* @__PURE__ */ new Date();
    hace30.setDate(hace30.getDate() - 30);
    const ventas = movimientos.filter((m) => esVenta(m) && new Date(m.fecha) >= hace30);
    const total = ventas.reduce(
      (a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.ingresoUnitario) || 0) * (1 + (Number(m.ivaVentaAplicado) || 0) / 100),
      0
    );
    return total / 30;
  }, [movimientos]);
  const proyeccionTesoreria = (0, import_react.useMemo)(() => {
    const hoy = /* @__PURE__ */ new Date();
    hoy.setHours(0, 0, 0, 0);
    const dias = [];
    for (let i = 0; i < 30; i++) {
      const d = new Date(hoy);
      d.setDate(d.getDate() + i);
      const fechaISO = aISO(d);
      const pagosDia = pendientesPago.filter((f) => f.vencimiento === fechaISO).reduce((a, f) => a + f.total, 0);
      const encargosDia = encargos.filter((e) => e.estado !== "Entregado" && e.estado !== "Cancelado" && e.fechaEntrega === fechaISO).reduce((a, e) => {
        const total = (e.lineas || []).reduce((x, l) => x + (Number(l.cantidad) || 0) * (Number(l.precioUnitario) || 0), 0);
        return a + Math.max(0, total - (Number(e.se\u00F1al) || 0));
      }, 0);
      const ingresosDia = promedioDiarioVentas + encargosDia;
      dias.push({ fecha: fechaISO, ingresosDia, pagosDia, netoDia: ingresosDia - pagosDia });
    }
    return dias;
  }, [pendientesPago, encargos, promedioDiarioVentas]);
  const ingresosPorMes = (0, import_react.useMemo)(() => {
    const mapa = {};
    movimientos.forEach((m) => {
      if (!esVenta(m)) return;
      const key = m.fecha.slice(0, 7);
      const importe = Math.abs(Number(m.cantidad) || 0) * (Number(m.ingresoUnitario) || 0);
      mapa[key] = (mapa[key] || 0) + importe;
    });
    return mapa;
  }, [movimientos]);
  const gastoPorProveedor = (0, import_react.useMemo)(() => {
    const map = {};
    pedidos.forEach((pe) => {
      const total = pe.items.reduce((a, it) => a + it.cantidad * it.costoUnitario, 0);
      map[pe.proveedorId] = (map[pe.proveedorId] || 0) + total;
    });
    return Object.entries(map).map(([proveedorId, total]) => ({ proveedor: proveedorPorId(proveedorId), total })).sort((a, b) => b.total - a.total);
  }, [pedidos, proveedores]);
  const productosSinMovimiento = (0, import_react.useMemo)(
    () => productos.filter((p) => !esUtillaje(p) && !movimientos.some((m) => m.productoId === p.id && esSalida(m))),
    [productos, movimientos]
  );
  if (!ready) {
    return /* @__PURE__ */ import_react.default.createElement("div", { style: { background: C.bg, color: C.ink }, className: "w-full h-full min-h-[600px] flex items-center justify-center font-sans" }, /* @__PURE__ */ import_react.default.createElement(LoaderCircle, { className: "animate-spin", size: 22 }), /* @__PURE__ */ import_react.default.createElement("span", { className: "ml-2 text-sm" }, "Cargando almac\xE9n\u2026"));
  }
  const contenido = /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, tab === "dashboard" && /* @__PURE__ */ import_react.default.createElement(
    Dashboard,
    {
      valorInventario,
      valorUtillaje,
      stockBajo,
      pedidosPendientes,
      margenPromedio,
      movimientos,
      productos,
      caducanPronto,
      proveedorPorId,
      vencenPronto,
      totalPendientePago,
      documentosPersonalPronto,
      fichajesAbiertos,
      encargosUrgentes,
      pisoVentaBajo,
      sugerenciasPedido,
      setTab,
      recordatorioConteo,
      alertasAppcc,
      fallosGuardado,
      diagnosticoStock
    }
  ), tab === "direccion" && /* @__PURE__ */ import_react.default.createElement(
    PanelDireccion,
    {
      movimientos,
      gastosGenerales,
      nominas,
      empleados,
      promedioDiarioVentas,
      proyeccionTesoreria,
      stockBajo,
      descuadresStock: diagnosticoStock.filter((d) => !d.coincide)
    }
  ), tab === "proveedores" && /* @__PURE__ */ import_react.default.createElement(Proveedores, { proveedores, addProveedor, updateProveedor, deleteProveedor, pedidos }), tab === "productos" && /* @__PURE__ */ import_react.default.createElement(
    Productos,
    {
      productos,
      proveedores,
      proveedorPorId,
      addProducto,
      updateProducto,
      deleteProducto,
      reactivarProducto,
      registrarSalida,
      clasificacionABC,
      almacenCongelado,
      movimientos,
      fichasCosto,
      updateFichaCosto,
      ajustarProductoPorOtro
    }
  ), tab === "historial_producto" && /* @__PURE__ */ import_react.default.createElement(
    HistorialProducto,
    {
      productos,
      movimientos,
      pedidos,
      albaranes,
      traspasos,
      proveedorPorId
    }
  ), tab === "aceite" && /* @__PURE__ */ import_react.default.createElement(
    AceiteFreidoras,
    {
      freidoras,
      registrosAceite,
      productos,
      addFreidora,
      updateFreidora,
      deleteFreidora,
      registrarCambio,
      registrarRelleno,
      eliminarRegistroAceite,
      consumoPorCiclo
    }
  ), tab === "buscar" && /* @__PURE__ */ import_react.default.createElement(
    BusquedaGlobal,
    {
      productos,
      proveedores,
      clientes,
      fichasCosto,
      empleados,
      setTab
    }
  ), tab === "pedidos" && /* @__PURE__ */ import_react.default.createElement(
    Pedidos,
    {
      pedidos,
      proveedores,
      productos,
      crearPedido,
      actualizarPedido,
      eliminarPedido,
      proveedorPorId,
      productoPorId,
      sugerenciasPedido,
      addProducto
    }
  ), tab === "recepcion" && /* @__PURE__ */ import_react.default.createElement(Recepcion, { pedidos, proveedorPorId, productoPorId, recibirPedido, almacenCongelado, recibirConAlbaran, recibirConFotoIA, cerrarPedido }), tab === "conteo" && /* @__PURE__ */ import_react.default.createElement(
    InventarioCiego,
    {
      productos,
      proveedores,
      conteos,
      iniciarConteo,
      actualizarConteoItem,
      actualizarResponsable,
      finalizarConteo,
      aplicarAjustes,
      eliminarConteo,
      productoPorId,
      crearProductoEnConteo,
      clasificacionABC,
      almacenCongelado
    }
  ), tab === "reportes" && /* @__PURE__ */ import_react.default.createElement(
    Reportes,
    {
      rotacionPorProducto,
      gastoPorProveedor,
      productosSinMovimiento,
      valorInventario,
      margenPorProducto,
      patronesDesviacionConteo
    }
  ), tab === "resultados" && /* @__PURE__ */ import_react.default.createElement(
    Resultados,
    {
      movimientos,
      productos,
      productoPorId,
      gastosGenerales,
      addGasto,
      deleteGasto,
      empleados
    }
  ), tab === "fichas" && /* @__PURE__ */ import_react.default.createElement(
    FichasCosto,
    {
      fichasCosto,
      productos,
      addFichaCosto,
      updateFichaCosto,
      deleteFichaCosto,
      updateProducto,
      addProducto,
      ordenesProduccion,
      nominas,
      fichajes
    }
  ), tab === "produccion" && /* @__PURE__ */ import_react.default.createElement(Produccion, { fichasCosto, productos, ordenesProduccion, producir, anularProduccion, traspasarStock }), tab === "mermas" && /* @__PURE__ */ import_react.default.createElement(Mermas, { productos, movimientos, registrarSalida, almacenCongelado }), tab === "etiquetas" && /* @__PURE__ */ import_react.default.createElement(EtiquetasCatalogo, { productos, fichasCosto, alergenosDeFicha }), tab === "albaranes" && /* @__PURE__ */ import_react.default.createElement(
    Albaranes,
    {
      albaranes,
      proveedores,
      productos,
      proveedorPorId,
      buscarEnCatalogo,
      guardarAlbaran,
      eliminarAlbaran,
      confirmarAlbaran,
      anularAlbaran,
      marcarPagada,
      duplicadosDe,
      desviacionesDePrecio,
      prefill: prefillAlbaran,
      limpiarPrefill: () => setPrefillAlbaran(null),
      pedidoParaFotoIA,
      limpiarPedidoParaFotoIA: () => setPedidoParaFotoIA(null),
      pedidos
    }
  ), tab === "pagos" && /* @__PURE__ */ import_react.default.createElement(
    CuentasPorPagar,
    {
      facturasPorPagar,
      totalPendientePago,
      marcarPagada,
      marcarPagadaFacturaDirecta,
      addFacturaDirecta,
      deleteFacturaDirecta,
      proveedores,
      resaltadaId: facturaDirectaResaltada,
      limpiarResaltada: () => setFacturaDirectaResaltada(null)
    }
  ), tab === "personal" && /* @__PURE__ */ import_react.default.createElement(
    Personal,
    {
      empleados,
      addEmpleado,
      updateEmpleado,
      deleteEmpleado,
      anonimizarEmpleado,
      registrarAusencia,
      eliminarAusencia,
      registrarEpi,
      eliminarEpi,
      documentosPersonalCaducan,
      fichajes,
      nominas,
      entrevistas,
      crearEntrevista,
      actualizarEntrevista,
      finalizarEntrevista,
      eliminarEntrevista
    }
  ), tab === "fichaje" && /* @__PURE__ */ import_react.default.createElement(
    RegistroHorario,
    {
      empleados,
      fichajes,
      fichar,
      addFichajeManual,
      updateFichaje,
      eliminarFichaje,
      fichajesAbiertos
    }
  ), tab === "nominas" && /* @__PURE__ */ import_react.default.createElement(
    CostePersonal,
    {
      empleados,
      nominas,
      addNomina,
      updateNomina,
      deleteNomina,
      fichajes,
      movimientos
    }
  ), tab === "venta" && /* @__PURE__ */ import_react.default.createElement(VentaRapida, { productos, venderCarrito, anularVenta, movimientos, registrarAuditoria }), tab === "encargos" && /* @__PURE__ */ import_react.default.createElement(
    Encargos,
    {
      encargosPendientes,
      encargos,
      clientes,
      productos,
      addEncargo,
      updateEncargo,
      deleteEncargo,
      entregarEncargo,
      addCliente
    }
  ), tab === "clientes" && /* @__PURE__ */ import_react.default.createElement(
    Clientes,
    {
      analisisClientes,
      clientesDormidos,
      ventaCruzada,
      addCliente,
      updateCliente,
      deleteCliente,
      anonimizarCliente
    }
  ), tab === "facturas" && /* @__PURE__ */ import_react.default.createElement(
    Facturas,
    {
      albaranes,
      facturasDirectas,
      proveedorPorId,
      irAAlbaran: (albId) => {
        const a = albaranes.find((x) => x.id === albId);
        if (a) {
          setPrefillAlbaran(a);
          setTab("albaranes");
        }
      },
      irAFacturaDirecta: (fdId) => {
        setFacturaDirectaResaltada(fdId);
        setTab("pagos");
      }
    }
  ), tab === "libroiva" && /* @__PURE__ */ import_react.default.createElement(LibroIva, { movimientos, productos, albaranes, proveedorPorId, facturasDirectas }), tab === "caja" && /* @__PURE__ */ import_react.default.createElement(ArqueoCaja, { movimientos, arqueos, addArqueo, deleteArqueo, encargos }), tab === "tesoreria" && /* @__PURE__ */ import_react.default.createElement(Tesoreria, { proyeccionTesoreria, promedioDiarioVentas }), tab === "estacionalidad" && /* @__PURE__ */ import_react.default.createElement(Estacionalidad, { ingresosPorMes }), tab === "turnos" && /* @__PURE__ */ import_react.default.createElement(
    Turnos,
    {
      empleados,
      turnos,
      addTurno,
      updateTurno,
      deleteTurno,
      copiarSemana
    }
  ), tab === "mapa" && /* @__PURE__ */ import_react.default.createElement(MapaAlmacen, { productos, proveedorPorId }), tab === "traspasos" && /* @__PURE__ */ import_react.default.createElement(Traspasos, { productos, traspasos, traspasarStock, pisoVentaBajo, fichasCosto }), tab === "appcc" && /* @__PURE__ */ import_react.default.createElement(
    Appcc,
    {
      puntosControl,
      registrosAppcc,
      addPuntoControl,
      updatePuntoControl,
      deletePuntoControl,
      registrarAppcc,
      eliminarRegistroAppcc,
      appccPendientesHoy,
      productos,
      fichasCosto,
      alergenosDeFicha
    }
  ), tab === "saldo" && /* @__PURE__ */ import_react.default.createElement(SaldoAlmacen, { productos, proveedores, valorInventario, valorUtillaje, proveedorPorId, clasificacionABC, analisisABC }), tab === "respaldos" && /* @__PURE__ */ import_react.default.createElement(
    Respaldos,
    {
      historial,
      crearPuntoDeGuardado,
      restaurarDesdeHistorial,
      abrirRespaldo,
      abrirRestaurar,
      exportarExcelGeneral,
      pinPropietario,
      establecerPin,
      activarModoEmpleado
    }
  ), tab === "auditoria" && /* @__PURE__ */ import_react.default.createElement(Auditoria, { auditoria }), tab === "diagnostico" && /* @__PURE__ */ import_react.default.createElement(DiagnosticoStock, { diagnostico: diagnosticoStock, corregirProducto, movimientosParaReconciliar }));
  const itemsMeta = [
    { id: "dashboard", label: "Panel general", icon: ChartColumn },
    { id: "direccion", label: "Panel de direcci\xF3n", icon: TrendingUp },
    { id: "proveedores", label: "Proveedores", icon: Truck },
    { id: "productos", label: "Productos", icon: Package },
    { id: "buscar", label: "Buscar", icon: Search },
    { id: "historial_producto", label: "Historial de producto", icon: RotateCcwClock },
    { id: "pedidos", label: "Pedidos", icon: ShoppingCart, badge: pedidosPendientes.length },
    { id: "recepcion", label: "Recepci\xF3n", icon: ClipboardList },
    { id: "albaranes", label: "Albaranes", icon: FileText },
    { id: "facturas", label: "Facturas", icon: Files },
    { id: "pagos", label: "Cuentas por pagar", icon: Coins, badge: vencenPronto.length, badgeColor: C.red },
    { id: "conteo", label: "Inventario ciego", icon: Boxes, badge: stockBajo.length, badgeColor: C.amber },
    { id: "saldo", label: "Saldo de almac\xE9n", icon: ClipboardList },
    { id: "mapa", label: "Mapa de almac\xE9n", icon: Map2 },
    { id: "traspasos", label: "Traspasos", icon: ArrowLeftRight, badge: pisoVentaBajo.length, badgeColor: C.amber },
    { id: "fichas", label: "Fichas de costo", icon: Calculator },
    { id: "produccion", label: "Producci\xF3n", icon: Factory },
    { id: "mermas", label: "Mermas", icon: Trash2 },
    { id: "etiquetas", label: "Etiquetas y cat\xE1logo", icon: Tags },
    { id: "reportes", label: "Reportes y rotaci\xF3n", icon: ChartColumn },
    { id: "resultados", label: "Resultados", icon: TrendingUp },
    { id: "personal", label: "Personal", icon: Users, badge: documentosPersonalPronto.length, badgeColor: C.amber },
    { id: "fichaje", label: "Registro horario", icon: Clock, badge: fichajesAbiertos.length, badgeColor: C.amber },
    { id: "nominas", label: "Coste de personal", icon: Coins },
    { id: "turnos", label: "Cuadrante de turnos", icon: CalendarClock },
    { id: "appcc", label: "Control sanitario", icon: ShieldCheck, badge: appccPendientesHoy.length, badgeColor: C.amber },
    { id: "aceite", label: "Aceite de freidoras", icon: Droplet },
    { id: "venta", label: "Venta r\xE1pida", icon: ShoppingBag },
    { id: "encargos", label: "Encargos", icon: CalendarDays, badge: encargosUrgentes.length, badgeColor: C.red },
    { id: "clientes", label: "Clientes", icon: UserRound },
    { id: "libroiva", label: "Libro de IVA", icon: Receipt },
    { id: "caja", label: "Arqueo de caja", icon: Wallet },
    { id: "tesoreria", label: "Tesorer\xEDa", icon: ChartLine },
    { id: "estacionalidad", label: "Estacionalidad", icon: CalendarRange },
    { id: "auditoria", label: "Auditor\xEDa", icon: RotateCcwClock },
    { id: "diagnostico", label: "Reconciliaci\xF3n de stock", icon: Stethoscope, badge: descuadresPendientes, badgeColor: C.red },
    { id: "respaldos", label: "Respaldos", icon: Cog }
  ];
  const porId = (id) => itemsMeta.find((x) => x.id === id);
  const pick = (ids) => {
    const filtrados = modoEmpleado ? ids.filter((id) => itemsPermitidosEmpleado.includes(id)) : ids;
    return filtrados.map(porId).filter(Boolean);
  };
  const gruposA = [
    { titulo: null, items: pick(["dashboard", "buscar"]) },
    { titulo: "Compras", items: pick(["proveedores", "pedidos", "recepcion", "albaranes"]) },
    { titulo: "Almac\xE9n", items: pick(["productos", "historial_producto", "conteo", "saldo", "mapa", "traspasos"]) },
    { titulo: "Ventas", items: pick(["venta", "encargos", "clientes"]) },
    { titulo: "Costes y producci\xF3n", items: pick(["fichas", "produccion", "mermas", "etiquetas"]) },
    { titulo: "Finanzas", items: pick(["direccion", "resultados", "pagos", "facturas", "reportes", "libroiva", "caja", "tesoreria", "estacionalidad"]) },
    { titulo: "RRHH", items: pick(["personal", "fichaje", "turnos", "nominas"]) },
    { titulo: "Calidad", items: pick(["appcc", "aceite"]) },
    { titulo: "Sistema", items: pick(["auditoria", "diagnostico", "respaldos"]) }
  ];
  const gruposB = [
    { titulo: null, items: pick(["dashboard", "buscar"]) },
    { titulo: "Ciclo de compra", items: pick(["proveedores", "pedidos", "recepcion", "albaranes", "pagos", "facturas"]) },
    { titulo: "Ciclo de almac\xE9n", items: pick(["productos", "historial_producto", "conteo", "saldo", "mapa", "traspasos"]) },
    { titulo: "Ciclo de venta", items: pick(["venta", "encargos", "clientes"]) },
    { titulo: "Ciclo de coste y an\xE1lisis", items: pick(["fichas", "produccion", "mermas", "etiquetas", "resultados", "reportes", "libroiva", "caja", "tesoreria", "estacionalidad"]) },
    { titulo: "Ciclo de personal", items: pick(["personal", "fichaje", "turnos", "nominas"]) },
    { titulo: "Ciclo de calidad", items: pick(["appcc", "aceite"]) },
    { titulo: "Ajustes", items: pick(["auditoria", "respaldos"]) }
  ];
  const categoriasC = {
    comprar: { titulo: "Comprar", items: pick(["proveedores", "pedidos", "recepcion", "albaranes", "pagos"]) },
    almacen: { titulo: "Almac\xE9n", items: pick(["productos", "historial_producto", "conteo", "saldo", "mapa", "traspasos"]) },
    mas: [
      { titulo: "Vender", items: pick(["venta", "encargos", "clientes"]) },
      { titulo: "Dinero", items: pick(["resultados", "facturas", "reportes", "libroiva", "caja", "tesoreria", "estacionalidad"]) },
      { titulo: "Producto", items: pick(["fichas", "produccion", "mermas", "etiquetas"]) },
      { titulo: "Equipo", items: pick(["personal", "fichaje", "turnos", "nominas"]) },
      { titulo: "Calidad", items: pick(["appcc", "aceite"]) },
      { titulo: "Ajustes", items: pick(["auditoria", "respaldos"]) }
    ]
  };
  return /* @__PURE__ */ import_react.default.createElement("div", { "data-tema": temaOscuro ? "oscuro" : "claro", style: { background: C.bg, color: C.ink, fontFamily: "'IBM Plex Sans', ui-sans-serif, system-ui" }, className: disenoMenu === "C" ? "w-full min-h-[700px] flex flex-col text-[14px]" : "w-full min-h-[700px] flex flex-col md:flex-row text-[14px]" }, fallosGuardado.length > 0 && /* @__PURE__ */ import_react.default.createElement(
    "div",
    {
      className: "text-[12.5px] px-3 py-2 flex items-center justify-between gap-2",
      style: { background: C.red, color: "#fff", position: "sticky", top: 0, zIndex: 40 }
    },
    /* @__PURE__ */ import_react.default.createElement("span", null, "\u26A0 Un cambio no se ha podido guardar (", fallosGuardado[0].key, "). Comprueba tu conexi\xF3n \u2014 si sigue as\xED, haz una copia desde Respaldos antes de seguir trabajando."),
    /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setFallosGuardado([]), style: { color: "#fff", fontWeight: 600 } }, "\u2715")
  ), /* @__PURE__ */ import_react.default.createElement("style", null, `
        @import url('https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&family=IBM+Plex+Mono:wght@500;600&display=swap');
        :root, [data-tema="claro"] {
          --c-bg: #F7F3E9;
          --c-surface: #FFFFFF;
          --c-ink: #17241C;
          --c-ink-soft: #647267;
          --c-line: #E2DBC7;
          --c-accent: #8C6D2A;
          --c-accent-soft: #F1E7CE;
          --c-amber: #977022;
          --c-amber-soft: #F6EAD2;
          --c-red: #B5473A;
          --c-red-soft: #F7E5E1;
        }
        [data-tema="oscuro"] {
          --c-bg: #081A10;
          --c-surface: #0F2818;
          --c-ink: #F0E6D0;
          --c-ink-soft: #93A692;
          --c-line: #1E3A26;
          --c-accent: #D8B876;
          --c-accent-soft: #1C3322;
          --c-amber: #E8C066;
          --c-amber-soft: #2B2312;
          --c-red: #E37A69;
          --c-red-soft: #2B1712;
        }
        .mono { font-family: 'IBM Plex Mono', ui-monospace, monospace; }
        input, select { font-family: inherit; }
        ::placeholder { color: #9AA5A0; }

        @media print {
          @page { size: A4; margin: 12mm; }
          body * { visibility: hidden !important; }
          .zona-impresion, .zona-impresion * { visibility: visible !important; }
          .zona-impresion {
            position: absolute !important;
            left: 0; top: 0; width: 100%;
            background: #fff !important;
          }
          .no-imprimir { display: none !important; }
          .zona-impresion table { page-break-inside: auto; }
          .zona-impresion tr { page-break-inside: avoid; page-break-after: auto; }
          .zona-impresion thead { display: table-header-group; }
          /* Lo impreso siempre en claro, sin importar el tema activo en pantalla */
          :root, [data-tema="oscuro"], [data-tema="claro"] {
            --c-bg: #F7F3E9 !important;
            --c-surface: #FFFFFF !important;
            --c-ink: #17241C !important;
            --c-ink-soft: #647267 !important;
            --c-line: #E2DBC7 !important;
            --c-accent: #8C6D2A !important;
            --c-accent-soft: #F1E7CE !important;
            --c-amber: #977022 !important;
            --c-amber-soft: #F6EAD2 !important;
            --c-red: #B5473A !important;
            --c-red-soft: #F7E5E1 !important;
          }
        }
      `), disenoMenu === "C" ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(TopBarC, { disenoMenu, setDisenoMenu, tab, setTab, pendientes: pedidosPendientes.length, temaOscuro, setTemaOscuro, modoEmpleado, salirModoEmpleado, empleados, usuarioActivoId, entrarComoEmpleado, pinPropietario }), /* @__PURE__ */ import_react.default.createElement("main", { className: "flex-1 p-4 overflow-y-auto", style: { maxHeight: "900px" } }, contenido), /* @__PURE__ */ import_react.default.createElement(BottomNavC, { tab, setTab, categorias: categoriasC, dashboardItem: porId("dashboard") })) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(
    SidebarGrupos,
    {
      grupos: disenoMenu === "A" ? gruposA : gruposB,
      tab,
      setTab,
      disenoMenu,
      setDisenoMenu,
      temaOscuro,
      setTemaOscuro,
      modoEmpleado,
      salirModoEmpleado,
      empleados,
      usuarioActivoId,
      entrarComoEmpleado,
      pinPropietario
    }
  ), /* @__PURE__ */ import_react.default.createElement("main", { className: "flex-1 p-4 md:p-7 overflow-y-auto", style: { maxHeight: "900px" } }, contenido)), showBackupView && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowBackupView(false), title: "Tu respaldo" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2", style: { color: C.ink } }, 'Copia este texto y gu\xE1rdalo en alg\xFAn lugar seguro (notas, correo, Google Drive). Para restaurarlo m\xE1s adelante, p\xE9galo en "Restaurar respaldo".'), /* @__PURE__ */ import_react.default.createElement(
    "textarea",
    {
      readOnly: true,
      value: backupText,
      onFocus: (e) => e.target.select(),
      className: "w-full rounded-lg p-2 text-[11px] mono mb-3",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink, height: 160, resize: "none" }
    }
  ), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: copiarRespaldo }, copiado ? "\xA1Copiado!" : "Copiar al portapapeles"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowBackupView(false) }, "Cerrar")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-2", style: { color: C.inkSoft } }, "Si el bot\xF3n no copia solo, toca dentro del recuadro de texto, selecciona todo y c\xF3pialo manualmente.")), showRestoreInput && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowRestoreInput(false), title: "Restaurar respaldo" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2", style: { color: C.ink } }, "Pega aqu\xED el texto completo del respaldo que copiaste anteriormente."), /* @__PURE__ */ import_react.default.createElement(
    "textarea",
    {
      value: restoreText,
      onChange: (e) => setRestoreText(e.target.value),
      placeholder: "Pega aqu\xED el texto del respaldo\u2026",
      className: "w-full rounded-lg p-2 text-[11px] mono mb-3",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink, height: 160, resize: "none" }
    }
  ), restoreError && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, restoreError), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: analizarTextoRestauracion }, "Continuar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowRestoreInput(false) }, "Cancelar"))), pendingRestore && (() => {
    const comparacion = compararConEstadoActual(pendingRestore);
    const seConservan = coleccionesQueSeConservan(pendingRestore);
    const perdidas = comparacion.filter((c) => c.diferencia < 0);
    return /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setPendingRestore(null), title: "Restaurar respaldo" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-1", style: { color: C.ink } }, pendingRestore.exportadoEl ? `Respaldo del ${new Date(pendingRestore.exportadoEl).toLocaleString("es-ES")}` : "Respaldo sin fecha registrada"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3", style: { color: C.inkSoft } }, "Formato ", pendingRestore.backupVersion || pendingRestore.version || "antiguo"), perdidas.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-1" }, "\u26A0 Este respaldo tiene menos datos que lo que hay ahora"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2" }, "Al restaurarlo desaparecer\xEDa lo creado despu\xE9s. Se guardar\xE1 un punto de recuperaci\xF3n antes, por si te arrepientes."), perdidas.map((c) => /* @__PURE__ */ import_react.default.createElement("div", { key: c.clave, className: "text-[11.5px] mono" }, c.nombre, ": ", c.actual, " \u2192 ", c.respaldo, " (", c.diferencia, ")"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-1", style: { color: C.inkSoft } }, "Qu\xE9 contiene, comparado con ahora"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3 space-y-0.5", style: { color: C.inkSoft } }, comparacion.map((c) => /* @__PURE__ */ import_react.default.createElement("div", { key: c.clave, className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", null, c.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: c.diferencia < 0 ? C.red : C.inkSoft } }, c.actual, " \u2192 ", c.respaldo)))), seConservan.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.bg, border: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]" }, "Este respaldo es anterior y no incluye: ", /* @__PURE__ */ import_react.default.createElement("b", null, seConservan.join(", ")), ". Esos datos", /* @__PURE__ */ import_react.default.createElement("b", null, " se conservan tal como est\xE1n ahora"), " \u2014 no se borran.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-4", style: { color: C.red } }, "Restaurar sustituye lo que tengas cargado ahora por el contenido de este respaldo."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: confirmarRestauracion }, "Restaurar respaldo"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setPendingRestore(null) }, "Cancelar")));
  })());
}
function crearLogicaProveedores({ proveedores, setProveedores, registrarAuditoria }) {
  function addProveedor(data) {
    setProveedores((s) => [...s, { id: uid(), ...data }]);
  }
  function updateProveedor(id, data) {
    setProveedores((s) => s.map((p) => p.id === id ? { ...p, ...data } : p));
  }
  function deleteProveedor(id) {
    const p = proveedores.find((x) => x.id === id);
    registrarAuditoria("Eliminar proveedor", p ? p.nombre : id);
    setProveedores((s) => s.filter((p2) => p2.id !== id));
  }
  return { addProveedor, updateProveedor, deleteProveedor };
}
function crearLogicaPersonal({ empleados, setEmpleados, registrarAuditoria, setNominas }) {
  function addEmpleado(data) {
    setEmpleados((s) => [...s, { id: uid(), activo: true, documentos: [], ...data }]);
  }
  function updateEmpleado(id, data) {
    setEmpleados((s) => s.map((e) => e.id === id ? { ...e, ...data } : e));
  }
  function deleteEmpleado(id) {
    const e = empleados.find((x) => x.id === id);
    registrarAuditoria("Eliminar empleado", e ? e.nombre : id);
    setEmpleados((s) => s.filter((e2) => e2.id !== id));
    if (setNominas) setNominas((s) => s.filter((n) => n.empleadoId !== id));
  }
  function anonimizarEmpleado(id) {
    const e = empleados.find((x) => x.id === id);
    registrarAuditoria("Anonimizar empleado", e ? e.nombre : id);
    setEmpleados(
      (s) => s.map(
        (emp) => emp.id === id ? {
          ...emp,
          nombre: "Empleado anonimizado",
          dni: "",
          pin: "",
          documentos: [],
          ausencias: [],
          anonimizado: true,
          activo: false
        } : emp
      )
    );
  }
  function registrarAusencia(empleadoId, ausencia) {
    setEmpleados(
      (s) => s.map((e) => e.id === empleadoId ? { ...e, ausencias: [...e.ausencias || [], { id: uid(), ...ausencia }] } : e)
    );
  }
  function eliminarAusencia(empleadoId, ausenciaId) {
    setEmpleados(
      (s) => s.map((e) => e.id === empleadoId ? { ...e, ausencias: (e.ausencias || []).filter((a) => a.id !== ausenciaId) } : e)
    );
  }
  function registrarEpi(empleadoId, epi) {
    setEmpleados(
      (s) => s.map((e) => e.id === empleadoId ? { ...e, epis: [...e.epis || [], { id: uid(), fecha: todayISO(), ...epi }] } : e)
    );
  }
  function eliminarEpi(empleadoId, epiId) {
    setEmpleados(
      (s) => s.map((e) => e.id === empleadoId ? { ...e, epis: (e.epis || []).filter((x) => x.id !== epiId) } : e)
    );
  }
  return { addEmpleado, updateEmpleado, deleteEmpleado, anonimizarEmpleado, registrarAusencia, eliminarAusencia, registrarEpi, eliminarEpi };
}
function crearLogicaTurnos({ turnos, setTurnos }) {
  function addTurno(data) {
    setTurnos((s) => [...s, { id: uid(), ...data }]);
  }
  function updateTurno(id, data) {
    setTurnos((s) => s.map((t) => t.id === id ? { ...t, ...data } : t));
  }
  function deleteTurno(id) {
    setTurnos((s) => s.filter((t) => t.id !== id));
  }
  function copiarSemana(desdeFechas, haciaFechas) {
    const nuevos = [];
    turnos.forEach((t) => {
      const idx = desdeFechas.indexOf(t.fecha);
      if (idx === -1) return;
      nuevos.push({ id: uid(), empleadoId: t.empleadoId, fecha: haciaFechas[idx], tipo: t.tipo, horaInicio: t.horaInicio, horaFin: t.horaFin, notas: t.notas });
    });
    if (nuevos.length) setTurnos((s) => [...s, ...nuevos]);
    return nuevos.length;
  }
  return { addTurno, updateTurno, deleteTurno, copiarSemana };
}
function crearLogicaAppcc({ setPuntosControl, setRegistrosAppcc }) {
  function addPuntoControl(data) {
    setPuntosControl((s) => [...s, { id: uid(), activo: true, ...data }]);
  }
  function updatePuntoControl(id, data) {
    setPuntosControl((s) => s.map((p) => p.id === id ? { ...p, ...data } : p));
  }
  function deletePuntoControl(id) {
    setPuntosControl((s) => s.filter((p) => p.id !== id));
  }
  function registrarAppcc(data) {
    setRegistrosAppcc((s) => [{ id: uid(), fecha: todayISO(), hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5), ...data }, ...s]);
  }
  function eliminarRegistroAppcc(id) {
    setRegistrosAppcc((s) => s.filter((r) => r.id !== id));
  }
  return { addPuntoControl, updatePuntoControl, deletePuntoControl, registrarAppcc, eliminarRegistroAppcc };
}
function crearLogicaFichaje({ setFichajes }) {
  function fichar(empleadoId, tipo) {
    const ahora = /* @__PURE__ */ new Date();
    setFichajes((s) => [
      { id: uid(), empleadoId, tipo, fecha: todayISO(), hora: ahora.toTimeString().slice(0, 5), timestamp: ahora.toISOString() },
      ...s
    ]);
  }
  function addFichajeManual(data) {
    setFichajes((s) => [{ id: uid(), ...data }, ...s]);
  }
  function updateFichaje(id, data) {
    setFichajes((s) => s.map((f) => f.id === id ? { ...f, ...data } : f));
  }
  function eliminarFichaje(id) {
    setFichajes((s) => s.filter((f) => f.id !== id));
  }
  return { fichar, addFichajeManual, updateFichaje, eliminarFichaje };
}
function crearLogicaGastos({ setGastosGenerales }) {
  function addGasto(data) {
    setGastosGenerales((s) => [...s, { id: uid(), ...data }]);
  }
  function deleteGasto(id) {
    setGastosGenerales((s) => s.filter((g) => g.id !== id));
  }
  return { addGasto, deleteGasto };
}
function crearLogicaCaja({ setArqueos }) {
  function addArqueo(data) {
    setArqueos((s) => [{ id: uid(), fecha: todayISO(), ...data }, ...s]);
  }
  function deleteArqueo(id) {
    setArqueos((s) => s.filter((a) => a.id !== id));
  }
  return { addArqueo, deleteArqueo };
}
function crearLogicaClientes({ clientes, setClientes, registrarAuditoria }) {
  function addCliente(data) {
    const nuevo = { id: uid(), fechaAlta: todayISO(), ...data };
    setClientes((s) => [...s, nuevo]);
    return nuevo;
  }
  function updateCliente(id, data) {
    setClientes((s) => s.map((c) => c.id === id ? { ...c, ...data } : c));
  }
  function deleteCliente(id) {
    const c = clientes.find((x) => x.id === id);
    registrarAuditoria("Eliminar cliente", c ? c.nombre : id);
    setClientes((s) => s.filter((c2) => c2.id !== id));
  }
  function anonimizarCliente(id) {
    const c = clientes.find((x) => x.id === id);
    registrarAuditoria("Anonimizar cliente", c ? c.nombre : id);
    setClientes(
      (s) => s.map(
        (cli) => cli.id === id ? { ...cli, nombre: "Cliente anonimizado", telefono: "", email: "", notas: "", anonimizado: true } : cli
      )
    );
  }
  return { addCliente, updateCliente, deleteCliente, anonimizarCliente };
}
function crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria }) {
  const idsConocidos = new Set(movimientos.map((m) => m.id));
  const porId = new Map(movimientos.map((m) => [m.id, m]));
  const snapshotLocal = /* @__PURE__ */ new Map();
  function productoActual(productoId) {
    if (snapshotLocal.has(productoId)) return snapshotLocal.get(productoId);
    return productos.find((p) => p.id === productoId);
  }
  function calcularStockTeorico(productoId, campo = "stock") {
    return movimientos.filter((m) => m.productoId === productoId).filter((m) => m.origen !== "reconciliacionStock").filter((m) => {
      if (esMovimientoNuevo(m)) {
        return !!m[campo === "stockPisoVenta" ? "afectaStockPisoVenta" : "afectaStockTotal"];
      }
      return campo !== "stockPisoVenta";
    }).reduce((suma, m) => suma + cantidadConSigno(m), 0);
  }
  function aplicarMovimientoStock({
    productoId,
    cantidad,
    tipo,
    operationId,
    movimientoId,
    origen,
    documentoOrigenId,
    usuario,
    dispositivo,
    motivo,
    permitirDeficit = false,
    afectaStockTotal = true,
    afectaStockPisoVenta = false,
    revierteMovimientoId = null,
    camposExtra = null
    // campos económicos que el motor no conoce (ingresoUnitario,
    // medioPago...) — se escriben en el mismo momento de crear
    // el movimiento, nunca como un parche después.
  }) {
    const id = movimientoId || uid();
    if (idsConocidos.has(id)) {
      const existente = porId.get(id);
      if (existente && Number(existente.cantidad) !== Number(cantidad)) {
        return { ok: false, error: `El movimiento ${id} ya existe con una cantidad distinta (${existente.cantidad} \u2260 ${cantidad}). No se sobrescribe.` };
      }
      return { ok: true, movimiento: existente, yaExistia: true };
    }
    const producto = productoActual(productoId);
    if (!producto) return { ok: false, error: "Producto no encontrado." };
    const cantidadNum = Number(cantidad) || 0;
    const resultadoCampos = {};
    let deficitGenerado = 0;
    if (afectaStockTotal) {
      const stockActual = Number(producto.stock) || 0;
      const deficitActual = Number(producto.deficitPendiente) || 0;
      const teoricoAntes = stockActual - deficitActual;
      const teoricoDespues = teoricoAntes + cantidadNum;
      const deficitNuevo = Math.max(0, -teoricoDespues);
      if (deficitNuevo > deficitActual && !permitirDeficit) {
        return { ok: false, error: `Stock insuficiente: hay ${Math.max(0, teoricoAntes)}, se piden ${-cantidadNum}.` };
      }
      resultadoCampos.stock = Math.max(0, teoricoDespues);
      resultadoCampos.deficitPendiente = deficitNuevo;
      deficitGenerado = Math.max(0, deficitNuevo - deficitActual);
    }
    if (afectaStockPisoVenta) {
      const pisoActual = Number(producto.stockPisoVenta) || 0;
      resultadoCampos.stockPisoVenta = Math.max(0, pisoActual + cantidadNum);
    }
    const movimiento = {
      id,
      operationId: operationId || id,
      productoId,
      cantidad: cantidadNum,
      tipo,
      motivo: motivo || "",
      fecha: todayISO(),
      origen: origen || "",
      documentoOrigenId: documentoOrigenId || null,
      usuario: usuario || "",
      dispositivo: dispositivo || "",
      stockAnterior: Number(producto.stock) || 0,
      stockPosterior: resultadoCampos.stock !== void 0 ? resultadoCampos.stock : Number(producto.stock) || 0,
      afectaStockTotal,
      afectaStockPisoVenta,
      revierteMovimientoId,
      ...camposExtra || {}
    };
    setProductos((s) => s.map((p) => p.id === productoId ? { ...p, ...resultadoCampos } : p));
    snapshotLocal.set(productoId, { ...producto, ...resultadoCampos });
    setMovimientos((s) => [movimiento, ...s]);
    idsConocidos.add(id);
    porId.set(id, movimiento);
    if (deficitGenerado > 0 && registrarAuditoria) {
      registrarAuditoria("deficit_stock_detectado", `${producto.nombre}: ${deficitGenerado} sin cobertura (${tipo}, operaci\xF3n ${movimiento.operationId})`);
    }
    return { ok: true, movimiento, yaExistia: false };
  }
  return { aplicarMovimientoStock, calcularStockTeorico };
}
function crearLogicaReconciliacion({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria }) {
  const { aplicarMovimientoStock, calcularStockTeorico } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  function diagnosticarStock() {
    return productos.map((p) => {
      const movimientosDelProducto = movimientos.filter((m) => m.productoId === p.id);
      const stockCache = Number(p.stock) || 0;
      const deficitCache = Number(p.deficitPendiente) || 0;
      const teoricoCache = stockCache - deficitCache;
      const teoricoReal = calcularStockTeorico(p.id, "stock");
      const diferencia = Number((teoricoReal - teoricoCache).toFixed(4));
      const coincide = Math.abs(diferencia) < 0.01;
      const deficitReal = Math.max(0, -teoricoReal);
      const tieneDeficit = deficitReal >= 0.01;
      let estado;
      if (coincide && !tieneDeficit) estado = "OK";
      else if (coincide && tieneDeficit) estado = "DEFICIT";
      else if (!coincide && !tieneDeficit) estado = "DISCREPANCIA";
      else estado = "DEFICIT_Y_DISCREPANCIA";
      const ultimoMovimiento = movimientosDelProducto.reduce(
        (max, m) => !max || (m.fecha || "") > max ? m.fecha : max,
        null
      );
      return {
        productoId: p.id,
        nombre: p.nombre,
        stockCache,
        deficitCache,
        teoricoCache,
        teoricoReal,
        diferencia,
        coincide,
        deficitReal,
        estado,
        numMovimientos: movimientosDelProducto.length,
        ultimoMovimiento
      };
    }).sort((a, b) => Math.abs(b.diferencia) - Math.abs(a.diferencia));
  }
  function movimientosParaReconciliar(productoId) {
    return movimientos.filter((m) => m.productoId === productoId).slice().sort((a, b) => (b.fecha || "").localeCompare(a.fecha || ""));
  }
  function corregirProducto(productoId) {
    const p = productos.find((pr) => pr.id === productoId);
    if (!p) return { ok: false, error: "Producto no encontrado." };
    const teoricoReal = calcularStockTeorico(productoId, "stock");
    const stockCache = Number(p.stock) || 0;
    const deficitCache = Number(p.deficitPendiente) || 0;
    const teoricoCache = stockCache - deficitCache;
    const cantidadCorreccion = Number((teoricoReal - teoricoCache).toFixed(4));
    if (Math.abs(cantidadCorreccion) < 0.01) return { ok: true, sinCambios: true };
    const r = aplicarMovimientoStock({
      productoId,
      cantidad: cantidadCorreccion,
      tipo: "INVENTARIO",
      operationId: uid(),
      movimientoId: uid(),
      origen: "reconciliacionStock",
      documentoOrigenId: null,
      afectaStockTotal: true,
      afectaStockPisoVenta: false,
      permitirDeficit: true,
      motivo: `Reconciliaci\xF3n: la cach\xE9 dec\xEDa ${fmt(teoricoCache)}, el hist\xF3rico dec\xEDa ${fmt(teoricoReal)}`
    });
    if (!r.ok) return r;
    registrarAuditoria(
      "Reconciliaci\xF3n de stock",
      `${p.nombre}: la cach\xE9 dec\xEDa ${fmt(teoricoCache)}, los movimientos reales dicen ${fmt(teoricoReal)} \u2014 corregido a ${fmt(teoricoReal)}.`
    );
    return { ok: true, antes: teoricoCache, despues: teoricoReal, movimiento: r.movimiento };
  }
  return { diagnosticarStock, corregirProducto, movimientosParaReconciliar };
}
function crearLogicaProductos({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria, almacenCongelado, addGasto }) {
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  function ajustarProductoPorOtro({ productoOrigenId, productoDestinoId, cantidadOrigen, cantidadDestino, motivo }) {
    if (almacenCongelado) return { ok: false, error: "El almac\xE9n est\xE1 congelado por un conteo en curso." };
    if (!productoOrigenId || !productoDestinoId) return { ok: false, error: "Elige los dos productos." };
    if (productoOrigenId === productoDestinoId) return { ok: false, error: "Elige dos productos distintos." };
    const origen = productos.find((p) => p.id === productoOrigenId);
    const destino = productos.find((p) => p.id === productoDestinoId);
    if (!origen || !destino) return { ok: false, error: "Alguno de los productos ya no existe." };
    const cantSale = Number(cantidadOrigen) || 0;
    const cantEntra = Number(cantidadDestino) || 0;
    if (cantSale <= 0 || cantEntra <= 0) return { ok: false, error: "Indica cu\xE1nto sale y cu\xE1nto entra." };
    if (cantSale > (Number(origen.stock) || 0)) {
      return { ok: false, error: `No hay tanto stock de ${origen.nombre}: quedan ${fmt(origen.stock)}.` };
    }
    const costoSale = cantSale * (Number(origen.costo) || 0);
    const costoEntra = cantEntra * (Number(destino.costo) || 0);
    const diferencia = costoEntra - costoSale;
    const operationId = uid();
    const rSale = aplicarMovimientoStock({
      productoId: productoOrigenId,
      cantidad: -cantSale,
      tipo: "AJUSTE_ENTRE_PRODUCTOS",
      operationId,
      movimientoId: uid(),
      origen: "ajustarProductoPorOtro",
      afectaStockTotal: true,
      permitirDeficit: false,
      motivo: `Cambiado por ${destino.nombre}${motivo ? " \xB7 " + motivo : ""}`
    });
    if (!rSale.ok) return rSale;
    const rEntra = aplicarMovimientoStock({
      productoId: productoDestinoId,
      cantidad: cantEntra,
      tipo: "AJUSTE_ENTRE_PRODUCTOS",
      operationId,
      movimientoId: uid(),
      origen: "ajustarProductoPorOtro",
      afectaStockTotal: true,
      permitirDeficit: true,
      motivo: `Viene de ${origen.nombre}${motivo ? " \xB7 " + motivo : ""}`
    });
    if (Math.abs(diferencia) >= 0.01 && addGasto) {
      addGasto({
        concepto: `Ajuste ${origen.nombre} \u2192 ${destino.nombre}${motivo ? " \xB7 " + motivo : ""}`,
        importe: Number((-diferencia).toFixed(2)),
        fecha: todayISO()
      });
    }
    registrarAuditoria(
      "Ajuste entre productos",
      `${fmt(cantSale)} de ${origen.nombre} \u2192 ${fmt(cantEntra)} de ${destino.nombre} \xB7 diferencia \u20AC${fmt(Math.abs(diferencia))}${motivo ? " \xB7 " + motivo : ""}`
    );
    return { ok: true, costoSale, costoEntra, diferencia };
  }
  function addProducto(data) {
    const nuevo = { id: uid(), stock: Number(data.stock) || 0, ...data };
    setProductos((s) => [...s, nuevo]);
    const stockInicial = Number(data.stock) || 0;
    if (stockInicial > 0) {
      setMovimientos((s) => [
        {
          id: uid(),
          operationId: uid(),
          productoId: nuevo.id,
          cantidad: stockInicial,
          tipo: "OTRO",
          motivo: "Alta de producto con stock inicial",
          fecha: todayISO(),
          origen: "addProducto",
          documentoOrigenId: null,
          stockAnterior: 0,
          stockPosterior: stockInicial,
          afectaStockTotal: true,
          afectaStockPisoVenta: false,
          revierteMovimientoId: null
        },
        ...s
      ]);
    }
    return nuevo;
  }
  function updateProducto(id, data) {
    const anterior = productos.find((p) => p.id === id);
    const { stock: stockNuevoForm, ...restoDatos } = data;
    if (Object.keys(restoDatos).length > 0) {
      setProductos((s) => s.map((p) => p.id === id ? { ...p, ...restoDatos } : p));
    }
    if (anterior && stockNuevoForm !== void 0) {
      const stockActual = Number(anterior.stock) || 0;
      const deficitActual = Number(anterior.deficitPendiente) || 0;
      const stockTeoricoAntes = stockActual - deficitActual;
      const valorNuevo = Number(stockNuevoForm) || 0;
      const dif = valorNuevo - stockTeoricoAntes;
      if (dif !== 0) {
        const { aplicarMovimientoStock: aplicarMovimientoStock2 } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
        aplicarMovimientoStock2({
          productoId: id,
          cantidad: dif,
          tipo: "INVENTARIO",
          movimientoId: uid(),
          origen: "updateProducto",
          afectaStockTotal: true,
          permitirDeficit: true,
          motivo: "Correcci\xF3n manual de stock"
        });
      }
    }
  }
  function deleteProducto(id) {
    const p = productos.find((x) => x.id === id);
    registrarAuditoria("Eliminar producto (borrado l\xF3gico)", p ? p.nombre : id);
    setProductos((s) => s.map((pr) => pr.id === id ? { ...pr, activo: false, eliminadoEn: todayISO() } : pr));
  }
  function reactivarProducto(id) {
    const p = productos.find((x) => x.id === id);
    registrarAuditoria("Reactivar producto", p ? p.nombre : id);
    setProductos((s) => s.map((pr) => pr.id === id ? { ...pr, activo: true, eliminadoEn: null } : pr));
  }
  function registrarSalida(productoId, cantidad, opciones = {}) {
    if (almacenCongelado) return false;
    const cant = Number(cantidad);
    if (!cant || cant <= 0) return false;
    const { motivo = "Venta", precioVentaUnitario = null, referencia = "", medioPago = "Efectivo" } = opciones;
    const prod = productos.find((p) => p.id === productoId);
    if (!prod) return false;
    const esVenta2 = motivo === "Venta";
    const esMermaMotivo = MOTIVOS_MERMA.includes(motivo);
    const tipo = esVenta2 ? "VENTA" : esMermaMotivo ? "MERMA" : "OTRO";
    const ingresoUnitario = esVenta2 ? precioVentaUnitario !== null && precioVentaUnitario !== "" ? Number(precioVentaUnitario) || 0 : precioNeto(prod) : 0;
    const r = aplicarMovimientoStock({
      productoId,
      cantidad: -cant,
      tipo,
      movimientoId: uid(),
      origen: "registrarSalida",
      afectaStockTotal: true,
      permitirDeficit: true,
      motivo: referencia || motivo,
      camposExtra: {
        costoUnitario: Number(prod.costo) || 0,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago: esVenta2 ? medioPago : null
      }
    });
    return r.ok;
  }
  return { addProducto, updateProducto, deleteProducto, reactivarProducto, registrarSalida, ajustarProductoPorOtro };
}
function crearLogicaPedidos({ pedidos, setPedidos, productos, setProductos, setMovimientos, almacenCongelado, procesarRecepcion }) {
  function crearPedido({ proveedorId, fechaEsperada, items }) {
    const pedido = {
      id: uid(),
      proveedorId,
      fecha: todayISO(),
      fechaEsperada,
      estado: "Pendiente",
      items: items.map((it) => ({ ...it, cantidadRecibida: 0 }))
    };
    setPedidos((s) => [pedido, ...s]);
    return pedido;
  }
  function actualizarPedido(pedidoId, { proveedorId, fechaEsperada, items }) {
    setPedidos(
      (s) => s.map(
        (pe) => pe.id === pedidoId ? { ...pe, proveedorId, fechaEsperada, items: items.map((it) => ({ ...it, cantidadRecibida: 0 })) } : pe
      )
    );
  }
  function eliminarPedido(pedidoId) {
    setPedidos((s) => s.filter((pe) => pe.id !== pedidoId));
  }
  function cerrarPedido(pedidoId) {
    setPedidos((s) => s.map((pe) => pe.id === pedidoId ? { ...pe, estado: "Recibido", cerradoManualmente: true } : pe));
  }
  function recibirPedido(pedidoId, lineas) {
    if (almacenCongelado) return false;
    const pedido = pedidos.find((pe) => pe.id === pedidoId);
    if (!pedido) return false;
    const lineasConDatos = lineas.filter((ln) => Number(ln.cantidad) > 0).map((ln) => {
      const prod = productos.find((p) => p.id === ln.productoId);
      return {
        productoId: ln.productoId,
        cantidad: Number(ln.cantidad),
        precioBruto: Number(ln.precioBruto) || 0,
        importe: (Number(ln.precioBruto) || 0) * Number(ln.cantidad),
        ivaPct: Number(ln.ivaPct) || (prod ? prod.ivaCompra : 10) || 10,
        udsPorCaja: Number(ln.udsPorCaja) || (prod ? prod.udsPorCaja : 1) || 1,
        descripcion: prod ? prod.nombre : "",
        unidad: prod ? prod.unidad : "unidad"
      };
    });
    if (!lineasConDatos.length) return false;
    const { lineasResueltas, avisos } = procesarRecepcion({
      lineas: lineasConDatos,
      proveedorId: pedido.proveedorId,
      fecha: todayISO(),
      documentoTipo: "pedido",
      documentoId: pedido.id,
      documentoNumero: pedido.id.slice(-6)
    });
    setPedidos(
      (s) => s.map((pe) => {
        if (pe.id !== pedidoId) return pe;
        const items = pe.items.map((it) => {
          const suyas = lineasResueltas.filter((ln) => ln.productoId === it.productoId);
          if (!suyas.length) return it;
          const recibidas = suyas.reduce((a, ln) => a + (Number(ln.unidadesEntradas) || 0), 0);
          return { ...it, cantidadRecibida: (Number(it.cantidadRecibida) || 0) + recibidas };
        });
        const completo = items.every((it) => (Number(it.cantidadRecibida) || 0) >= (Number(it.cantidad) || 0));
        const algo = items.some((it) => (Number(it.cantidadRecibida) || 0) > 0);
        return { ...pe, items, estado: completo ? "Recibido" : algo ? "Parcial" : "Pendiente" };
      })
    );
    return { ok: true, avisos };
  }
  return { crearPedido, actualizarPedido, eliminarPedido, recibirPedido, cerrarPedido };
}
function crearLogicaFichasCosto({ productos, setFichasCosto }) {
  function addFichaCosto(data) {
    setFichasCosto((s) => [...s, { id: uid(), ...data }]);
  }
  function updateFichaCosto(id, data) {
    setFichasCosto((s) => s.map((f) => f.id === id ? { ...f, ...data } : f));
  }
  function deleteFichaCosto(id) {
    setFichasCosto((s) => s.filter((f) => f.id !== id));
  }
  function alergenosDeFicha(f) {
    const set = new Set(f.alergenosExtra || []);
    (f.componentes || []).forEach((c) => {
      if (!c.productoId) return;
      const p = productos.find((x) => x.id === c.productoId);
      (p?.alergenos || []).forEach((a) => set.add(a));
    });
    return [...set];
  }
  return { addFichaCosto, updateFichaCosto, deleteFichaCosto, alergenosDeFicha };
}
function crearLogicaConteos({ productos, setProductos, conteos, setConteos, movimientos, setMovimientos, registrarAuditoria }) {
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  function crearProductoEnConteo(conteoId, datos, cantidadContada) {
    const nuevoId = uid();
    const nuevoProducto = {
      id: nuevoId,
      stock: 0,
      codigo: datos.codigo || "",
      nombre: datos.nombre,
      categoria: datos.categoria || "",
      unidad: datos.unidad || "unidad",
      costo: Number(datos.costo) || 0,
      precioVenta: Number(datos.precioVenta) || 0,
      stockMinimo: Number(datos.stockMinimo) || 0,
      ubicacion: datos.ubicacion || "",
      proveedorId: datos.proveedorId || ""
    };
    setProductos((s) => [...s, nuevoProducto]);
    setConteos(
      (s) => s.map(
        (c) => c.id === conteoId ? { ...c, items: [...c.items, { productoId: nuevoId, cantidadContada }] } : c
      )
    );
  }
  function iniciarConteo(ambito = "total") {
    let productosDelConteo;
    if (ambito === "piso_venta") {
      productosDelConteo = productos.filter((p) => p.tipo === "elaborado" || Number(p.precioVenta) > 0);
    } else if (ambito === "almacen") {
      productosDelConteo = productos.filter((p) => p.tipo !== "elaborado");
    } else {
      productosDelConteo = productos;
    }
    const conteo = {
      id: uid(),
      fecha: todayISO(),
      completado: false,
      ambito,
      responsables: { contadoPor: "", revisor: "" },
      items: productosDelConteo.map((p) => ({ productoId: p.id, conteo: "" }))
    };
    setConteos((s) => [conteo, ...s]);
    return conteo.id;
  }
  function actualizarConteoItem(conteoId, productoId, campo, valor) {
    setConteos(
      (s) => s.map(
        (c) => c.id === conteoId ? { ...c, items: c.items.map((it) => it.productoId === productoId ? { ...it, [campo]: valor } : it) } : c
      )
    );
  }
  function actualizarResponsable(conteoId, campo, valor) {
    setConteos(
      (s) => s.map((c) => c.id === conteoId ? { ...c, responsables: { ...c.responsables || {}, [campo]: valor } } : c)
    );
  }
  function finalizarConteo(conteoId) {
    setConteos((s) => s.map((c) => c.id === conteoId ? { ...c, completado: true } : c));
  }
  function eliminarConteo(conteoId) {
    const conteo = conteos.find((c) => c.id === conteoId);
    if (!conteo) return { ok: false, error: "Ese conteo ya no existe." };
    const movimientosDelConteo = movimientos.filter(
      (m) => m.documentoOrigenId === conteoId && m.origen === "aplicarAjustes"
    );
    let revertidos = 0;
    movimientosDelConteo.forEach((m) => {
      const cantidadOriginal = cantidadConSigno(m);
      const r = aplicarMovimientoStock({
        productoId: m.productoId,
        cantidad: -cantidadOriginal,
        tipo: m.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "eliminarConteo",
        documentoOrigenId: conteoId,
        afectaStockTotal: !!m.afectaStockTotal,
        afectaStockPisoVenta: !!m.afectaStockPisoVenta,
        permitirDeficit: true,
        revierteMovimientoId: m.id,
        motivo: `Eliminaci\xF3n del conteo del ${conteo.fecha}`
      });
      if (r.ok) revertidos++;
    });
    setConteos((s) => s.filter((c) => c.id !== conteoId));
    registrarAuditoria(
      "Eliminar conteo",
      `Conteo del ${conteo.fecha} (${conteo.ambito || "total"}) \u2014 ${revertidos} movimiento(s) revertido(s)`
    );
    return { ok: true, revertidos };
  }
  function aplicarAjustes(conteoId, motivos = {}) {
    const conteo = conteos.find((c) => c.id === conteoId);
    if (!conteo) return { ajustados: 0, traspasados: [] };
    const esPisoVenta = conteo.ambito === "piso_venta";
    const esAlmacen = conteo.ambito === "almacen";
    const idsAplicados = [];
    const traspasados = [];
    const operationIdDeEsteAjuste = uid();
    conteo.items.forEach((item) => {
      const p = productos.find((pr) => pr.id === item.productoId);
      if (!p) return;
      const valorFinal = valorCF(item);
      if (valorFinal === null) return;
      const stockActual = Number(p.stock) || 0;
      const deficitActual = Number(p.deficitPendiente) || 0;
      const stockTeoricoTotal = stockActual - deficitActual;
      if (esAlmacen) {
        const enPisoAntes = Number(p.stockPisoVenta) || 0;
        const anteriorAlmacenTeoricoFalta = stockTeoricoTotal - enPisoAntes;
        const difAlmacen = valorFinal - anteriorAlmacenTeoricoFalta;
        if (difAlmacen < 0) {
          const cantidadATraspasar = -difAlmacen;
          const r2 = aplicarMovimientoStock({
            productoId: p.id,
            cantidad: cantidadATraspasar,
            tipo: "TRASPASO_A_PISO",
            operationId: operationIdDeEsteAjuste,
            movimientoId: uid(),
            origen: "aplicarAjustes",
            documentoOrigenId: conteoId,
            afectaStockTotal: false,
            afectaStockPisoVenta: true,
            permitirDeficit: true,
            motivo: `Faltante de almac\xE9n trasladado al piso de venta (se asume ya expuesto, sin registrar) \xB7 ${motivos[p.id] || "Sin especificar"}`
          });
          if (r2.ok) {
            idsAplicados.push(r2.movimiento.id);
            traspasados.push({ productoId: p.id, nombre: p.nombre, cantidad: cantidadATraspasar, tipoTraspaso: "falta" });
          }
          return;
        }
      }
      let dif, motivoTexto, afectaStockPisoVenta = false;
      if (esPisoVenta) {
        const anteriorPiso = Number(p.stockPisoVenta) || 0;
        dif = valorFinal - anteriorPiso;
        motivoTexto = `Ajuste por inventario (piso de venta) \xB7 ${motivos[p.id] || "Sin especificar"}`;
        afectaStockPisoVenta = true;
      } else if (esAlmacen) {
        const enPiso = Number(p.stockPisoVenta) || 0;
        const anteriorAlmacenTeorico = stockTeoricoTotal - enPiso;
        dif = valorFinal - anteriorAlmacenTeorico;
        motivoTexto = `Ajuste por inventario (almac\xE9n) \xB7 ${motivos[p.id] || "Sin especificar"}`;
      } else {
        dif = valorFinal - stockTeoricoTotal;
        motivoTexto = `Ajuste por inventario \xB7 ${motivos[p.id] || "Sin especificar"}`;
      }
      if (dif === 0) return;
      const r = aplicarMovimientoStock({
        productoId: p.id,
        cantidad: dif,
        tipo: "INVENTARIO",
        operationId: operationIdDeEsteAjuste,
        movimientoId: uid(),
        origen: "aplicarAjustes",
        documentoOrigenId: conteoId,
        afectaStockTotal: true,
        afectaStockPisoVenta,
        permitirDeficit: true,
        motivo: motivoTexto
      });
      if (r.ok) idsAplicados.push(r.movimiento.id);
      if (esAlmacen && dif > 0 && r.ok) {
        aplicarMovimientoStock({
          productoId: p.id,
          cantidad: dif,
          tipo: "TRASPASO_A_PISO",
          operationId: operationIdDeEsteAjuste,
          movimientoId: uid(),
          origen: "aplicarAjustes",
          documentoOrigenId: conteoId,
          afectaStockTotal: false,
          afectaStockPisoVenta: true,
          permitirDeficit: true,
          motivo: `Sobrante de inventario trasladado al piso de venta \xB7 ${motivos[p.id] || "Sin especificar"}`
        });
        traspasados.push({ productoId: p.id, nombre: p.nombre, cantidad: dif, tipoTraspaso: "sobra" });
      }
      if (!esPisoVenta && !esAlmacen && r.ok) {
        const pisoActual = Number(productos.find((pr) => pr.id === p.id)?.stockPisoVenta) || 0;
        if (pisoActual > valorFinal) {
          aplicarMovimientoStock({
            productoId: p.id,
            cantidad: -(pisoActual - valorFinal),
            tipo: "INVENTARIO",
            operationId: operationIdDeEsteAjuste,
            movimientoId: uid(),
            origen: "aplicarAjustes",
            documentoOrigenId: conteoId,
            afectaStockTotal: false,
            afectaStockPisoVenta: true,
            permitirDeficit: true,
            motivo: "El piso no puede superar el nuevo total contado"
          });
        }
      }
    });
    if (idsAplicados.length) {
      registrarAuditoria("Aplicar ajustes de inventario", `${idsAplicados.length} producto(s) ajustado(s)`);
    }
    return { ajustados: idsAplicados.length, traspasados };
  }
  function eliminarConteo(conteoId) {
    const conteo = conteos.find((c) => c.id === conteoId);
    if (!conteo) return { ok: false, error: "Conteo no encontrado." };
    const generados = movimientos.filter((m) => m.documentoOrigenId === conteoId && m.origen === "aplicarAjustes");
    const operationIdDeEstaAnulacion = uid();
    let revertidos = 0;
    generados.forEach((m) => {
      const r = aplicarMovimientoStock({
        productoId: m.productoId,
        cantidad: -(Number(m.cantidad) || 0),
        tipo: m.tipo,
        operationId: operationIdDeEstaAnulacion,
        movimientoId: uid(),
        origen: "eliminarConteo",
        documentoOrigenId: conteoId,
        afectaStockTotal: !!m.afectaStockTotal,
        afectaStockPisoVenta: !!m.afectaStockPisoVenta,
        permitirDeficit: true,
        revierteMovimientoId: m.id,
        motivo: `Eliminaci\xF3n del conteo del ${conteo.fecha}`
      });
      if (r.ok) revertidos++;
    });
    setConteos((s) => s.filter((c) => c.id !== conteoId));
    registrarAuditoria(
      "Eliminar conteo",
      `Conteo del ${conteo.fecha}${conteo.ambito && conteo.ambito !== "total" ? ` (${conteo.ambito})` : ""} \xB7 ${revertidos} movimiento(s) revertido(s)`
    );
    return { ok: true, revertidos };
  }
  return { crearProductoEnConteo, iniciarConteo, actualizarConteoItem, actualizarResponsable, finalizarConteo, aplicarAjustes, eliminarConteo };
}
function crearLogicaProduccion({ fichasCosto, productos, setProductos, movimientos, setMovimientos, setOrdenesProduccion, registrarAuditoria }) {
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  function producir({ fichaId, unidadesDeseadas, unidadesBuenas, ingredientesReales, empaqueReal, manoObraReal, gastosGeneralesReal, notas }) {
    const ficha = fichasCosto.find((f) => f.id === fichaId);
    if (!ficha) return { ok: false, error: "No se encontr\xF3 la ficha de costo." };
    if (!ficha.productoVinculadoId) return { ok: false, error: "Esta ficha no tiene un producto de venta enlazado todav\xEDa." };
    const elaboradoOriginal = productos.find((p) => p.id === ficha.productoVinculadoId);
    if (!elaboradoOriginal) {
      return {
        ok: false,
        error: 'El producto de venta de esta ficha ya no existe (puede que se borrara). Ve a Fichas de costo, edita esta receta y vuelve a enlazarla con un producto, o crea uno nuevo con "Crear producto de venta". No se ha descontado ning\xFAn ingrediente.'
      };
    }
    const buenas = Number(unidadesBuenas) || 0;
    if (buenas <= 0) return { ok: false, error: "Indica cu\xE1ntas unidades buenas salieron." };
    const ordenId = uid();
    let costoIngredientes = 0;
    ingredientesReales.forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const prod = productos.find((p) => p.id === ing.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      costoIngredientes += Number(ing.cantidadReal) * costoUnitario;
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: -Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
    });
    const costoLoteTotal = costoIngredientes + (Number(empaqueReal) || 0) + (Number(manoObraReal) || 0) + (Number(gastosGeneralesReal) || 0);
    const costoUnitarioLote = costoLoteTotal / buenas;
    const elaborado = productos.find((p) => p.id === ficha.productoVinculadoId);
    if (elaborado) {
      const stockAnterior = Number(elaborado.stock) || 0;
      const costoAnterior = Number(elaborado.costo) || 0;
      const stockTotal = stockAnterior + buenas;
      const costoPonderado = stockTotal > 0 ? (stockAnterior * costoAnterior + buenas * costoUnitarioLote) / stockTotal : costoUnitarioLote;
      aplicarMovimientoStock({
        productoId: elaborado.id,
        cantidad: buenas,
        tipo: "PRODUCCION_ENTRADA",
        operationId: ordenId,
        movimientoId: uid(),
        origen: "producir",
        documentoOrigenId: ordenId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        motivo: `Producci\xF3n: ${ficha.nombre}`
      });
      setProductos((s) => s.map((p) => p.id === elaborado.id ? { ...p, costo: Number(costoPonderado.toFixed(4)) } : p));
    }
    const orden = {
      id: ordenId,
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      fichaId,
      fichaNombre: ficha.nombre,
      productoVinculadoId: ficha.productoVinculadoId,
      unidadesDeseadas: Number(unidadesDeseadas) || 0,
      unidadesBuenas: buenas,
      ingredientes: ingredientesReales.map((i) => ({ ...i })),
      empaqueReal: Number(empaqueReal) || 0,
      manoObraReal: Number(manoObraReal) || 0,
      gastosGeneralesReal: Number(gastosGeneralesReal) || 0,
      costoLoteTotal,
      costoUnitarioLote,
      notas: notas || ""
    };
    setOrdenesProduccion((s) => [orden, ...s]);
    registrarAuditoria("Producci\xF3n registrada", `${ficha.nombre} \xB7 ${buenas} unidad(es) \xB7 coste \u20AC${costoUnitarioLote.toFixed(4)}/ud`);
    return { ok: true, orden };
  }
  function anularProduccion(orden) {
    function movimientoOriginalDe(productoId, tipo) {
      return movimientos.find(
        (m) => (m.documentoOrigenId === orden.id || m.ordenId === orden.id) && m.productoId === productoId && (m.tipo === tipo || m.tipo === "salida" || m.tipo === "entrada")
      );
    }
    (orden.ingredientes || []).forEach((ing) => {
      if (!ing.productoId || !(Number(ing.cantidadReal) > 0)) return;
      const original = movimientoOriginalDe(ing.productoId, "PRODUCCION_CONSUMO");
      aplicarMovimientoStock({
        productoId: ing.productoId,
        cantidad: Number(ing.cantidadReal),
        tipo: "PRODUCCION_CONSUMO",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularProduccion",
        documentoOrigenId: orden.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
      });
    });
    const elaboradoId = orden.productoVinculadoId || (fichasCosto.find((f) => f.id === orden.fichaId) || {}).productoVinculadoId;
    if (elaboradoId) {
      const p = productos.find((pr) => pr.id === elaboradoId);
      if (p) {
        const buenas = Number(orden.unidadesBuenas) || 0;
        const stockActual = Number(p.stock) || 0;
        const costoActual = Number(p.costo) || 0;
        const costoLote = Number(orden.costoUnitarioLote) || 0;
        const stockRestante = stockActual - buenas;
        const original = movimientoOriginalDe(elaboradoId, "PRODUCCION_ENTRADA");
        aplicarMovimientoStock({
          productoId: elaboradoId,
          cantidad: -buenas,
          tipo: "PRODUCCION_ENTRADA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "anularProduccion",
          documentoOrigenId: orden.id,
          afectaStockTotal: true,
          afectaStockPisoVenta: true,
          permitirDeficit: true,
          revierteMovimientoId: original ? original.id : null,
          motivo: `Anulaci\xF3n de producci\xF3n: ${orden.fichaNombre}`
        });
        if (stockRestante > 0) {
          const costoPrevio = (stockActual * costoActual - buenas * costoLote) / stockRestante;
          setProductos((s) => s.map((pr) => pr.id === elaboradoId ? { ...pr, costo: Number(Math.max(0, costoPrevio).toFixed(4)) } : pr));
        }
      }
    }
    setOrdenesProduccion((s) => s.filter((o) => o.id !== orden.id));
    registrarAuditoria(
      "Anular producci\xF3n",
      `${orden.fichaNombre} \xB7 ${orden.unidadesBuenas} unidad(es) del ${orden.fecha}`
    );
    return { ok: true };
  }
  return { producir, anularProduccion };
}
function sincronizarCobroSe\u00F1al(cobros, se\u00F1al, se\u00F1alMedioPago, fecha) {
  const resto = (cobros || []).filter((c) => c.concepto !== "Se\xF1al");
  const importe = Number(se\u00F1al) || 0;
  if (importe <= 0) return resto;
  const previa = (cobros || []).find((c) => c.concepto === "Se\xF1al");
  return [
    {
      id: previa ? previa.id : uid(),
      concepto: "Se\xF1al",
      importe,
      medioPago: se\u00F1alMedioPago || (previa ? previa.medioPago : "Efectivo"),
      fecha: previa ? previa.fecha : fecha || todayISO()
    },
    ...resto
  ];
}
function crearLogicaEncargos({ encargos, setEncargos, registrarAuditoria, productos, setProductos, setMovimientos, venderLineas }) {
  function addEncargo(data) {
    const fecha = todayISO();
    const cobros = sincronizarCobroSe\u00F1al([], data.se\u00F1al, data.se\u00F1alMedioPago, fecha);
    setEncargos((s) => [{ id: uid(), estado: "Pendiente", fechaCreacion: fecha, cobros, ...data }, ...s]);
  }
  function updateEncargo(id, data) {
    setEncargos(
      (s) => s.map((e) => {
        if (e.id !== id) return e;
        const actualizado = { ...e, ...data };
        if ("se\xF1al" in data || "se\xF1alMedioPago" in data) {
          actualizado.cobros = sincronizarCobroSe\u00F1al(e.cobros, actualizado.se\u00F1al, actualizado.se\u00F1alMedioPago, e.fechaCreacion);
        }
        return actualizado;
      })
    );
  }
  function deleteEncargo(id) {
    const e = encargos.find((x) => x.id === id);
    registrarAuditoria("Eliminar encargo", e ? `${e.numero || "s/n"}` : id);
    setEncargos((s) => s.filter((e2) => e2.id !== id));
  }
  function entregarEncargo(encargo, medioPago = "Efectivo") {
    const lineasParaVender = (encargo.lineas || []).map((ln) => ({
      productoId: ln.productoId,
      cantidad: ln.cantidad,
      precioUnitario: ln.precioUnitario
    }));
    venderLineas(lineasParaVender, {
      tipo: "VENTA_ENCARGO",
      medioPago,
      origen: "entregarEncargo",
      documentoOrigenId: encargo.id,
      motivoBase: `Encargo ${encargo.numero || ""}`.trim(),
      extraPorLinea: () => ({ encargoId: encargo.id, clienteId: encargo.clienteId })
    });
    const total = (encargo.lineas || []).reduce((a, l) => a + (Number(l.cantidad) || 0) * (Number(l.precioUnitario) || 0), 0);
    const se\u00F1al = Number(encargo.se\u00F1al) || 0;
    const resto = Math.max(0, total - se\u00F1al);
    const cobrosSinResto = (encargo.cobros || []).filter((c) => c.concepto !== "Resto entrega");
    const cobros = resto > 9e-3 ? [...cobrosSinResto, { id: uid(), concepto: "Resto entrega", importe: resto, medioPago, fecha: todayISO() }] : cobrosSinResto;
    updateEncargo(encargo.id, { estado: "Entregado", fechaEntregaReal: todayISO(), cobros });
  }
  return { addEncargo, updateEncargo, deleteEncargo, entregarEncargo };
}
function crearLogicaVenta({ productos, setProductos, movimientos, setMovimientos }) {
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos });
  function venderLineas(lineas, opciones = {}) {
    const {
      tipo = "VENTA",
      medioPago = "Efectivo",
      detallePago = null,
      operationId = null,
      origen = "venderLocal",
      documentoOrigenId = null,
      motivoBase = "Venta r\xE1pida",
      extraPorLinea = () => ({})
    } = opciones;
    const ventaId = operationId || uid();
    let totalVenta = 0;
    if (detallePago) {
      (lineas || []).forEach((ln) => {
        const cant = Number(ln.cantidad) || 0;
        const prod = productos.find((p) => p.id === ln.productoId);
        if (!prod || cant <= 0) return;
        const precio = Number(ln.precioUnitario) || precioNeto(prod);
        totalVenta += cant * precio * (1 + ivaDe(prod) / 100);
      });
    }
    const movimientosGenerados = [];
    (lineas || []).forEach((ln) => {
      if (!ln.productoId) return;
      const cant = Number(ln.cantidad) || 0;
      if (cant <= 0) return;
      const prod = productos.find((p) => p.id === ln.productoId);
      if (!prod) return;
      const costoUnitario = Number(prod.costo) || 0;
      const ingresoUnitario = Number(ln.precioUnitario) || precioNeto(prod);
      const camposExtra = {
        costoUnitario,
        ingresoUnitario,
        ivaVentaAplicado: ivaDe(prod),
        medioPago,
        ...extraPorLinea(ln, prod)
      };
      if (detallePago && totalVenta > 0) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaDe(prod) / 100);
        const proporcion = importeLinea / totalVenta;
        camposExtra.detallePago = {
          efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)),
          tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4))
        };
      }
      const r = aplicarMovimientoStock({
        productoId: prod.id,
        cantidad: -cant,
        tipo,
        operationId: ventaId,
        movimientoId: uid(),
        origen,
        documentoOrigenId: documentoOrigenId || ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        motivo: motivoBase,
        camposExtra
      });
      if (r.ok) movimientosGenerados.push(r.movimiento);
    });
    return { ok: true, n: movimientosGenerados.length, modo: "local", ventaId, movimientos: movimientosGenerados };
  }
  function venderLocal(lineas, medioPago, detallePago) {
    return venderLineas(lineas, { tipo: "VENTA", medioPago, detallePago, origen: "venderLocal", motivoBase: "Venta r\xE1pida" });
  }
  async function venderCarrito(lineas, medioPago = "Efectivo", detallePago = null) {
    const hayConexion = typeof window !== "undefined" && window.__nubeActiva && typeof window.getSupabaseClient === "function";
    if (!hayConexion) {
      return venderLocal(lineas, medioPago, detallePago);
    }
    const ventaId = uid();
    let totalVenta = 0;
    (lineas || []).forEach((ln) => {
      const cant = Number(ln.cantidad) || 0;
      const prod = productos.find((p) => p.id === ln.productoId);
      if (!prod || cant <= 0) return;
      totalVenta += cant * precioNeto(prod) * (1 + ivaDe(prod) / 100);
    });
    const lineasParaRpc = (lineas || []).filter((ln) => ln.productoId && Number(ln.cantidad) > 0).map((ln) => {
      const prod = productos.find((p) => p.id === ln.productoId);
      const cant = Number(ln.cantidad);
      const costoUnitario = prod ? Number(prod.costo) || 0 : 0;
      const ingresoUnitario = prod ? precioNeto(prod) : 0;
      const ivaAplicado = prod ? ivaDe(prod) : 0;
      const datosLinea = {
        productoId: ln.productoId,
        cantidad: cant,
        motivo: "Venta",
        costoUnitario,
        ingresoUnitario,
        ivaVentaAplicado: ivaAplicado,
        medioPago,
        referencia: "Venta r\xE1pida"
      };
      if (detallePago && totalVenta > 0 && prod) {
        const importeLinea = cant * ingresoUnitario * (1 + ivaAplicado / 100);
        const proporcion = importeLinea / totalVenta;
        datosLinea.detallePago = {
          efectivo: Number((detallePago.efectivo * proporcion).toFixed(4)),
          tarjeta: Number((detallePago.tarjeta * proporcion).toFixed(4))
        };
      }
      return datosLinea;
    });
    if (lineasParaRpc.length === 0) return { ok: false, error: "Carrito vac\xEDo" };
    async function intentarRpc(msTimeout) {
      const supabase = await window.getSupabaseClient();
      const r = await Promise.race([
        supabase.rpc("descontar_stock_carrito", { p_lineas: lineasParaRpc, p_venta_id: ventaId }),
        new Promise((_, reject) => setTimeout(() => reject(new Error("El RPC tard\xF3 demasiado en responder")), msTimeout))
      ]);
      if (r.error) throw r.error;
      return r;
    }
    async function sincronizarTrasExito() {
      const supabase = await window.getSupabaseClient();
      const [rProd, movLeidos] = await Promise.all([
        supabase.from("almacen_kv").select("value").eq("key", "productos").maybeSingle(),
        window.storage.get("movimientos")
      ]);
      if (!rProd.error && rProd.data && Array.isArray(rProd.data.value)) {
        const idsVendidos = new Set(lineasParaRpc.map((l) => l.productoId));
        const porIdEnNube = {};
        rProd.data.value.forEach((p) => {
          if (p && p.id) porIdEnNube[p.id] = p;
        });
        setProductos((s) => s.map((p) => idsVendidos.has(p.id) && porIdEnNube[p.id] ? porIdEnNube[p.id] : p));
      }
      if (movLeidos && movLeidos.value) {
        try {
          setMovimientos(JSON.parse(movLeidos.value));
        } catch {
        }
      }
    }
    try {
      const r = await intentarRpc(6e3);
      if (r.data && r.data.ok === false) {
        return { ok: false, error: r.data.error, productoId: r.data.productoId };
      }
      await sincronizarTrasExito();
      return { ok: true, n: lineasParaRpc.length, modo: "atomico" };
    } catch (primerError) {
      try {
        const r2 = await intentarRpc(4e3);
        if (r2.data && r2.data.ok === false) {
          return { ok: false, error: r2.data.error, productoId: r2.data.productoId };
        }
        await sincronizarTrasExito();
        return { ok: true, n: lineasParaRpc.length, modo: "atomico", reintentada: true };
      } catch (segundoError) {
        return venderLocal(lineas, medioPago, detallePago);
      }
    }
  }
  function anularVenta(ventaId, movimientosActuales, motivo = "") {
    if (!ventaId) return { ok: false, error: "Esa venta no tiene identificador y no se puede anular." };
    const lineas = (movimientosActuales || []).filter((m) => m.ventaId === ventaId || m.operationId === ventaId).filter((m) => esVenta(m) || esSalida(m));
    if (lineas.length === 0) {
      return { ok: false, error: "No se han encontrado las l\xEDneas de esa venta." };
    }
    const yaAnulada = (movimientosActuales || []).some((m) => m.anulaVentaId === ventaId || m.revierteMovimientoId && lineas.some((l) => l.id === m.revierteMovimientoId));
    if (yaAnulada) {
      return { ok: false, error: "Esa venta ya estaba anulada." };
    }
    let aplicadas = 0;
    lineas.forEach((l) => {
      const cantidadOriginal = Math.abs(cantidadConSigno(l));
      if (cantidadOriginal <= 0) return;
      const r = aplicarMovimientoStock({
        productoId: l.productoId,
        cantidad: cantidadOriginal,
        tipo: l.tipo === "salida" ? "VENTA" : l.tipo,
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularVenta",
        documentoOrigenId: ventaId,
        afectaStockTotal: true,
        afectaStockPisoVenta: true,
        permitirDeficit: true,
        revierteMovimientoId: l.id,
        motivo: `Anulaci\xF3n de venta${motivo ? " \xB7 " + motivo : ""}`,
        camposExtra: {
          anulaVentaId: ventaId,
          costoUnitario: l.costoUnitario,
          // El ingreso se anota en negativo para que los informes de ventas
          // resten esta operación en vez de contarla como un ingreso nuevo.
          ingresoUnitario: l.ingresoUnitario != null ? -Math.abs(Number(l.ingresoUnitario)) : null,
          ivaVentaAplicado: l.ivaVentaAplicado,
          medioPago: l.medioPago,
          detallePago: l.detallePago
        }
      });
      if (r.ok) aplicadas++;
    });
    return { ok: true, lineasAnuladas: aplicadas };
  }
  return { venderCarrito, venderLocal, anularVenta, venderLineas };
}
function crearLogicaTraspasos({ productos, setProductos, movimientos, setMovimientos, setTraspasos, registrarAuditoria }) {
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  function traspasarStock(productoId, cantidad, direccion) {
    const cant = Number(cantidad) || 0;
    if (cant <= 0) return { ok: false, error: "Indica una cantidad mayor que cero." };
    const prod = productos.find((p) => p.id === productoId);
    if (!prod) return { ok: false, error: "Producto no encontrado." };
    const enPiso = Number(prod.stockPisoVenta) || 0;
    const total = Number(prod.stock) || 0;
    const enAlmacen = total - enPiso;
    if (direccion === "a_piso" && cant > enAlmacen) {
      return { ok: false, error: `Solo hay ${fmt(enAlmacen)} ${prod.unidad} en el almac\xE9n.` };
    }
    if (direccion === "a_almacen" && cant > enPiso) {
      return { ok: false, error: `Solo hay ${fmt(enPiso)} ${prod.unidad} en el piso de venta.` };
    }
    const cantidadConSigno2 = direccion === "a_piso" ? cant : -cant;
    const movimientoId = uid();
    const r = aplicarMovimientoStock({
      productoId,
      cantidad: cantidadConSigno2,
      tipo: direccion === "a_piso" ? "TRASPASO_A_PISO" : "TRASPASO_A_ALMACEN",
      movimientoId,
      origen: "traspasarStock",
      afectaStockTotal: false,
      afectaStockPisoVenta: true,
      permitirDeficit: false
      // rechazo duro, ya validado arriba con el mensaje concreto
    });
    if (!r.ok) return r;
    setTraspasos((s) => [
      { id: uid(), productoId, nombre: prod.nombre, cantidad: cant, direccion, fecha: todayISO(), hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5), movimientoId },
      ...s
    ]);
    return { ok: true };
  }
  return { traspasarStock };
}
function crearLogicaSeguridad({ pinPropietario, setPinPropietario, empleados, setModoEmpleado, setUsuarioActivoId }) {
  function activarModoEmpleado() {
    if (!pinPropietario) return false;
    setModoEmpleado(true);
    return true;
  }
  async function entrarComoEmpleado(empleadoId, pinIntento) {
    const emp = empleados.find((e) => e.id === empleadoId && e.activo !== false);
    if (!emp || !emp.pin) return false;
    const hashIntento = await hashPin(pinIntento);
    if (hashIntento !== emp.pin) return false;
    setModoEmpleado(true);
    setUsuarioActivoId(empleadoId);
    return true;
  }
  async function salirModoEmpleado(pinIntento) {
    if (!pinPropietario) {
      setModoEmpleado(false);
      setUsuarioActivoId(null);
      return true;
    }
    const hashIntento = await hashPin(pinIntento);
    if (hashIntento === pinPropietario) {
      setModoEmpleado(false);
      setUsuarioActivoId(null);
      return true;
    }
    return false;
  }
  async function establecerPin(nuevoPin) {
    setPinPropietario(await hashPin(nuevoPin));
  }
  return { activarModoEmpleado, entrarComoEmpleado, salirModoEmpleado, establecerPin };
}
function crearLogicaAlbaranes({
  catalogoProv,
  setCatalogoProv,
  albaranes,
  setAlbaranes,
  productos,
  setProductos,
  movimientos,
  setMovimientos,
  setPedidos,
  registrarAuditoria,
  proveedorPorId,
  setPrefillAlbaran,
  setPedidoParaFotoIA,
  setTab
}) {
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  const claveCat = (proveedorId, codigo) => `${proveedorId}::${String(codigo || "").trim().toUpperCase()}`;
  const normalizarDescripcion = (t) => String(t || "").normalize("NFD").replace(/[\u0300-\u036f]/g, "").toUpperCase().replace(/\s+/g, " ").trim();
  const claveCatDesc = (proveedorId, descripcion) => `${proveedorId}::DESC::${normalizarDescripcion(descripcion)}`;
  function buscarEnCatalogo(proveedorId, codigo, descripcion) {
    if (codigo) {
      const porCodigo = catalogoProv[claveCat(proveedorId, codigo)];
      if (porCodigo) return porCodigo;
    }
    if (descripcion && normalizarDescripcion(descripcion)) {
      return catalogoProv[claveCatDesc(proveedorId, descripcion)] || null;
    }
    return null;
  }
  function aprenderReferencia(proveedorId, codigo, datos) {
    if (!codigo) return;
    setCatalogoProv((s) => ({ ...s, [claveCat(proveedorId, codigo)]: { ...s[claveCat(proveedorId, codigo)] || {}, ...datos } }));
  }
  function sinFotoIncrustada(a) {
    if (!a || a.fotoOrigenIA === void 0) return a;
    const copia = { ...a };
    delete copia.fotoOrigenIA;
    return copia;
  }
  function guardarAlbaran(alb) {
    const limpio = sinFotoIncrustada(alb);
    setAlbaranes((s) => {
      const existe = s.some((a) => a.id === limpio.id);
      return existe ? s.map((a) => a.id === limpio.id ? limpio : a) : [limpio, ...s];
    });
  }
  function duplicadosDe(alb) {
    if (!alb || !alb.proveedorId) return { albaran: null, factura: null };
    const otros = albaranes.filter((a) => a.id !== alb.id && a.proveedorId === alb.proveedorId);
    const norm = (v) => String(v || "").trim().toLowerCase();
    const numero = norm(alb.numero);
    const numFactura = norm(alb.numeroFactura);
    return {
      albaran: numero ? otros.find((a) => norm(a.numero) === numero) || null : null,
      factura: numFactura ? otros.find((a) => norm(a.numeroFactura) === numFactura) || null : null
    };
  }
  function desviacionesDePrecio(alb, umbralPct = 10) {
    if (!alb || !alb.lineas) return [];
    const avisos = [];
    alb.lineas.forEach((ln) => {
      const prod = ln.productoId ? productos.find((p) => p.id === ln.productoId) : null;
      if (!prod) return;
      const costoAnterior = Number(prod.costo) || 0;
      if (costoAnterior <= 0) return;
      const unidades = Number(ln.unidadesEntradas) || Number(ln.unidades) || 0;
      const importe = Number(ln.importe) || 0;
      if (unidades <= 0 || importe <= 0) return;
      const precioNuevo = importe / unidades;
      const variacion = (precioNuevo - costoAnterior) / costoAnterior * 100;
      if (Math.abs(variacion) >= umbralPct) {
        avisos.push({
          productoId: prod.id,
          nombre: prod.nombre,
          antes: costoAnterior,
          ahora: precioNuevo,
          variacion: Math.round(variacion * 10) / 10
        });
      }
    });
    return avisos.sort((a, b) => Math.abs(b.variacion) - Math.abs(a.variacion));
  }
  function eliminarAlbaran(id) {
    const a = albaranes.find((x) => x.id === id);
    if (!a) return;
    registrarAuditoria("Eliminar albar\xE1n", `${proveedorPorId(a.proveedorId)?.nombre || "\u2014"} \xB7 ${a.numero || "s/n"}`);
    if (a.estado === "confirmado") {
      a.lineas.forEach((ln) => {
        const restar = Number(ln.unidadesEntradas) || 0;
        if (!ln.productoId || restar <= 0) return;
        const original = movimientos.find(
          (m) => (m.documentoOrigenId === a.id || m.albaranId === a.id) && m.productoId === ln.productoId
        );
        aplicarMovimientoStock({
          productoId: ln.productoId,
          cantidad: -restar,
          tipo: "COMPRA",
          operationId: uid(),
          movimientoId: uid(),
          origen: "eliminarAlbaran",
          documentoOrigenId: a.id,
          afectaStockTotal: true,
          permitirDeficit: true,
          revierteMovimientoId: original ? original.id : null,
          motivo: `Eliminaci\xF3n del albar\xE1n ${a.numero || "s/n"}`
        });
      });
      if (a.pedidoId) {
        setPedidos(
          (prev) => prev.map((pe) => {
            if (pe.id !== a.pedidoId) return pe;
            const items = pe.items.map((it) => {
              const suyas = a.lineas.filter((ln) => ln.productoId === it.productoId);
              if (!suyas.length) return it;
              const restar = suyas.reduce((acc, ln) => acc + (Number(ln.unidadesEntradas) || 0), 0);
              return { ...it, cantidadRecibida: Math.max(0, (Number(it.cantidadRecibida) || 0) - restar) };
            });
            const completo = items.every((it) => (Number(it.cantidadRecibida) || 0) >= (Number(it.cantidad) || 0));
            const algo = items.some((it) => (Number(it.cantidadRecibida) || 0) > 0);
            return { ...pe, items, estado: completo ? "Recibido" : algo ? "Parcial" : "Pendiente" };
          })
        );
      }
    }
    setAlbaranes((s) => s.filter((x) => x.id !== id));
  }
  function marcarPagada(id, pagada) {
    const a = albaranes.find((x) => x.id === id);
    registrarAuditoria(pagada ? "Marcar factura pagada" : "Marcar factura pendiente", a ? `${proveedorPorId(a.proveedorId)?.nombre || "\u2014"} \xB7 ${a.numeroFactura || a.numero || "s/n"}` : id);
    setAlbaranes((s) => s.map((a2) => a2.id === id ? { ...a2, pagada, fechaPago: pagada ? todayISO() : "" } : a2));
  }
  function procesarRecepcion({ lineas, proveedorId, fecha, documentoTipo, documentoId, documentoNumero }) {
    const avisos = [];
    const lineasResueltas = [];
    lineas.forEach((ln) => {
      const uds = Number(ln.udsPorCaja) > 0 ? Number(ln.udsPorCaja) : 1;
      const cantidad = Number(ln.cantidad) || 0;
      const importe = Number(ln.importe) || (Number(ln.precioBruto) || 0) * cantidad;
      const unidadesTotales = ln.tipoUnidad === "peso" ? cantidad : cantidad * uds;
      const costoUnitario = unidadesTotales > 0 ? importe / unidadesTotales : 0;
      const cat = buscarEnCatalogo(proveedorId, ln.codigoProveedor, ln.descripcion);
      let prod = null;
      if (ln.productoId) prod = productos.find((p) => p.id === ln.productoId);
      if (!prod && cat && cat.productoId) prod = productos.find((p) => p.id === cat.productoId);
      const motivoDoc = `${documentoTipo === "albaran" ? "Albar\xE1n" : "Pedido"} ${documentoNumero || "s/n"}`;
      if (prod) {
        const stockAnterior = Number(prod.stock) || 0;
        const costoAnterior = Number(prod.costo) || 0;
        if (costoAnterior > 0 && costoUnitario > 0) {
          const variacion = (costoUnitario - costoAnterior) / costoAnterior * 100;
          if (Math.abs(variacion) >= 3) {
            avisos.push({ nombre: prod.nombre, anterior: costoAnterior, nuevo: costoUnitario, variacion });
          }
        }
        const stockTotal = stockAnterior + unidadesTotales;
        const costoPonderado = stockTotal > 0 ? (stockAnterior * costoAnterior + unidadesTotales * costoUnitario) / stockTotal : costoUnitario;
        aplicarMovimientoStock({
          productoId: prod.id,
          cantidad: unidadesTotales,
          tipo: "COMPRA",
          movimientoId: uid(),
          origen: "procesarRecepcion",
          documentoOrigenId: documentoId,
          afectaStockTotal: true,
          permitirDeficit: true,
          motivo: `${motivoDoc}${ln.lote ? " \xB7 lote " + ln.lote : ""}`
        });
        setProductos((s) => s.map(
          (p) => p.id === prod.id ? {
            ...p,
            costo: costoUnitario > 0 ? Number(costoPonderado.toFixed(4)) : p.costo,
            ivaCompra: Number(ln.ivaPct) || p.ivaCompra || 10,
            udsPorCaja: uds,
            proveedorId: p.proveedorId || proveedorId,
            contenido: ln.contenido || p.contenido || "",
            unidadContenido: ln.unidadContenido || p.unidadContenido || ""
          } : p
        ));
        lineasResueltas.push({ ...ln, productoId: prod.id, unidadesEntradas: unidadesTotales });
      } else {
        const nuevoId = uid();
        const nuevo = {
          id: nuevoId,
          codigo: ln.codigoProveedor || "",
          nombre: ln.descripcion || "Sin descripci\xF3n",
          categoria: "",
          unidad: ln.unidad || "unidad",
          costo: Number(costoUnitario.toFixed(4)),
          ivaCompra: Number(ln.ivaPct) || 10,
          udsPorCaja: uds,
          precioVenta: 0,
          ivaVenta: 10,
          stock: unidadesTotales,
          stockMinimo: 0,
          ubicacion: "",
          proveedorId,
          contenido: ln.contenido || "",
          unidadContenido: ln.unidadContenido || ""
        };
        setProductos((s) => [...s, nuevo]);
        setMovimientos((s) => [
          {
            id: uid(),
            operationId: uid(),
            productoId: nuevoId,
            cantidad: unidadesTotales,
            tipo: "COMPRA",
            motivo: `${motivoDoc} \xB7 alta autom\xE1tica`,
            fecha: fecha || todayISO(),
            origen: "procesarRecepcion",
            documentoOrigenId: documentoId,
            stockAnterior: 0,
            stockPosterior: unidadesTotales,
            afectaStockTotal: true,
            afectaStockPisoVenta: false,
            revierteMovimientoId: null
          },
          ...s
        ]);
        lineasResueltas.push({ ...ln, productoId: nuevoId, unidadesEntradas: unidadesTotales });
      }
    });
    setCatalogoProv((prev) => {
      const copia = { ...prev };
      lineasResueltas.forEach((ln) => {
        if (!ln.productoId) return;
        const datos = {
          descripcion: ln.descripcion,
          unidad: ln.unidad,
          udsPorCaja: Number(ln.udsPorCaja) || 1,
          ivaPct: Number(ln.ivaPct) || 10,
          tipoUnidad: ln.tipoUnidad || "unidad",
          precioPor: ln.precioPor || "bulto",
          contenido: ln.contenido || "",
          unidadContenido: ln.unidadContenido || "",
          productoId: ln.productoId,
          ultimoPrecio: Number(ln.precioBruto) || 0,
          ultimoDto: Number(ln.dtoPct) || 0
        };
        if (ln.codigoProveedor) copia[claveCat(proveedorId, ln.codigoProveedor)] = datos;
        if (ln.descripcion && normalizarDescripcion(ln.descripcion)) copia[claveCatDesc(proveedorId, ln.descripcion)] = datos;
      });
      return copia;
    });
    return { lineasResueltas, avisos };
  }
  function confirmarAlbaran(alb) {
    const { lineasResueltas, avisos } = procesarRecepcion({
      lineas: alb.lineas,
      proveedorId: alb.proveedorId,
      fecha: alb.fecha,
      documentoTipo: "albaran",
      documentoId: alb.id,
      documentoNumero: alb.numero
    });
    if (alb.pedidoId) {
      setPedidos(
        (prev) => prev.map((pe) => {
          if (pe.id !== alb.pedidoId) return pe;
          const items = pe.items.map((it) => {
            const suyas = lineasResueltas.filter((ln) => ln.productoId === it.productoId);
            if (!suyas.length) return it;
            const recibidas = suyas.reduce((a, ln) => a + (Number(ln.unidadesEntradas) || 0), 0);
            return { ...it, cantidadRecibida: (Number(it.cantidadRecibida) || 0) + recibidas };
          });
          const completo = items.every((it) => (Number(it.cantidadRecibida) || 0) >= (Number(it.cantidad) || 0));
          const algo = items.some((it) => (Number(it.cantidadRecibida) || 0) > 0);
          return { ...pe, items, estado: completo ? "Recibido" : algo ? "Parcial" : "Pendiente" };
        })
      );
    }
    guardarAlbaran({ ...alb, lineas: lineasResueltas, estado: "confirmado", avisosPrecio: avisos });
    if (avisos.length) {
      registrarAuditoria(
        "Variaci\xF3n de precio en albar\xE1n",
        `${proveedorPorId(alb.proveedorId)?.nombre || "\u2014"} \xB7 ${avisos.map((a) => `${a.nombre} ${a.variacion > 0 ? "+" : ""}${fmt(a.variacion)}%`).join(", ")}`
      );
    }
    return avisos;
  }
  function anularAlbaran(alb) {
    registrarAuditoria("Anular entrada de albar\xE1n", `${proveedorPorId(alb.proveedorId)?.nombre || "\u2014"} \xB7 ${alb.numero || "s/n"}`);
    alb.lineas.forEach((ln) => {
      const restar = Number(ln.unidadesEntradas) || 0;
      if (!ln.productoId || restar <= 0) return;
      const original = movimientos.find(
        (m) => (m.documentoOrigenId === alb.id || m.albaranId === alb.id) && m.productoId === ln.productoId
      );
      aplicarMovimientoStock({
        productoId: ln.productoId,
        cantidad: -restar,
        tipo: "COMPRA",
        operationId: uid(),
        movimientoId: uid(),
        origen: "anularAlbaran",
        documentoOrigenId: alb.id,
        afectaStockTotal: true,
        permitirDeficit: true,
        revierteMovimientoId: original ? original.id : null,
        motivo: `Anulaci\xF3n del albar\xE1n ${alb.numero || "s/n"}`
      });
    });
    guardarAlbaran({ ...alb, estado: "borrador", avisosPrecio: [] });
  }
  function recibirConAlbaran(pedido) {
    const lineas = pedido.items.map((it) => {
      const prod = productos.find((x) => x.id === it.productoId);
      const pendiente = Math.max(0, (Number(it.cantidad) || 0) - (Number(it.cantidadRecibida) || 0));
      const uds = prod && Number(prod.udsPorCaja) > 0 ? Number(prod.udsPorCaja) : 1;
      return {
        codigoProveedor: prod ? prod.codigo || "" : "",
        descripcion: prod ? prod.nombre : "",
        unidad: prod ? prod.unidad : "unidad",
        tipoUnidad: "unidad",
        cantidad: uds > 1 ? pendiente / uds : pendiente,
        udsPorCaja: uds,
        precioPor: "unidad",
        precioBruto: it.costoUnitario,
        dtoPct: "",
        importe: "",
        ivaPct: prod ? ivaCompraDe(prod) : 10,
        lote: "",
        caducidad: "",
        contenido: "",
        unidadContenido: "",
        productoId: it.productoId,
        cantidadPedida: pendiente
      };
    });
    setPrefillAlbaran({
      id: uid(),
      proveedorId: pedido.proveedorId,
      numero: "",
      fecha: todayISO(),
      totalPapel: "",
      cargos: "",
      cargosConcepto: "",
      cargosIva: 21,
      esFactura: true,
      numeroFactura: "",
      fechaFactura: todayISO(),
      pagada: false,
      fechaPago: "",
      pedidoId: pedido.id,
      lineas: lineas.length ? lineas : [],
      estado: "borrador"
    });
    setTab("albaranes");
  }
  function recibirConFotoIA(pedido) {
    setPedidoParaFotoIA(pedido);
    setTab("albaranes");
  }
  return { buscarEnCatalogo, aprenderReferencia, guardarAlbaran, eliminarAlbaran, marcarPagada, confirmarAlbaran, anularAlbaran, recibirConAlbaran, recibirConFotoIA, duplicadosDe, desviacionesDePrecio, procesarRecepcion };
}
function crearLogicaRespaldos({
  // datos actuales, para leer y para meter en el respaldo
  proveedores,
  productos,
  pedidos,
  movimientos,
  conteos,
  fichasCosto,
  albaranes,
  catalogoProv,
  gastosGenerales,
  empleados,
  fichajes,
  registrosAppcc,
  puntosControl,
  clientes,
  encargos,
  arqueos,
  turnos,
  nominas,
  facturasDirectas,
  ordenesProduccion,
  traspasos,
  auditoria,
  freidoras,
  registrosAceite,
  // setters, para cuando se restaura un respaldo
  setProveedores,
  setProductos,
  setPedidos,
  setMovimientos,
  setConteos,
  setFichasCosto,
  setAlbaranes,
  setCatalogoProv,
  setGastosGenerales,
  setEmpleados,
  setFichajes,
  setRegistrosAppcc,
  setPuntosControl,
  setClientes,
  setEncargos,
  setArqueos,
  setTurnos,
  setNominas,
  setFacturasDirectas,
  setOrdenesProduccion,
  setTraspasos,
  setAuditoria,
  setFreidoras,
  setRegistrosAceite,
  // estado propio de la pantalla de respaldos
  setHistorial,
  setPendingRestore,
  pendingRestore,
  setBackupText,
  backupText,
  setCopiado,
  setShowBackupView,
  setShowRestoreInput,
  setRestoreText,
  restoreText,
  setRestoreError,
  // extras para el Excel y la auditoría
  proveedorPorId,
  productoPorId,
  registrarAuditoria
}) {
  function datosDelNegocio() {
    return {
      proveedores,
      productos,
      pedidos,
      movimientos,
      conteos,
      fichasCosto,
      albaranes,
      catalogoProv,
      gastosGenerales,
      empleados,
      fichajes,
      registrosAppcc,
      puntosControl,
      clientes,
      encargos,
      arqueos,
      turnos,
      nominas,
      facturasDirectas,
      ordenesProduccion,
      traspasos,
      auditoria,
      freidoras,
      registrosAceite
    };
  }
  const SETTERS = {
    proveedores: setProveedores,
    productos: setProductos,
    pedidos: setPedidos,
    movimientos: setMovimientos,
    conteos: setConteos,
    fichasCosto: setFichasCosto,
    albaranes: setAlbaranes,
    catalogoProv: setCatalogoProv,
    gastosGenerales: setGastosGenerales,
    empleados: setEmpleados,
    fichajes: setFichajes,
    registrosAppcc: setRegistrosAppcc,
    puntosControl: setPuntosControl,
    clientes: setClientes,
    encargos: setEncargos,
    arqueos: setArqueos,
    turnos: setTurnos,
    nominas: setNominas,
    freidoras: setFreidoras,
    registrosAceite: setRegistrosAceite,
    facturasDirectas: setFacturasDirectas,
    ordenesProduccion: setOrdenesProduccion,
    traspasos: setTraspasos,
    auditoria: setAuditoria
  };
  const NOMBRES = {
    proveedores: "Proveedores",
    productos: "Productos",
    pedidos: "Pedidos",
    movimientos: "Movimientos",
    conteos: "Conteos",
    fichasCosto: "Fichas de costo",
    albaranes: "Albaranes",
    catalogoProv: "C\xF3digos de proveedor",
    gastosGenerales: "Gastos generales",
    empleados: "Empleados",
    fichajes: "Fichajes",
    registrosAppcc: "Registros APPCC",
    puntosControl: "Puntos de control",
    clientes: "Clientes",
    encargos: "Encargos",
    arqueos: "Arqueos de caja",
    turnos: "Turnos",
    nominas: "N\xF3minas",
    freidoras: "Freidoras",
    registrosAceite: "Registros de aceite",
    facturasDirectas: "Facturas directas",
    ordenesProduccion: "\xD3rdenes de producci\xF3n",
    traspasos: "Traspasos",
    auditoria: "Auditor\xEDa"
  };
  function cuantos(valor) {
    if (Array.isArray(valor)) return valor.length;
    if (valor && typeof valor === "object") return Object.keys(valor).length;
    return 0;
  }
  function compararConEstadoActual(respaldo) {
    if (!respaldo) return [];
    const actual = datosDelNegocio();
    return Object.keys(NOMBRES).filter((clave) => respaldo[clave] !== void 0).map((clave) => {
      const nActual = cuantos(actual[clave]);
      const nRespaldo = cuantos(respaldo[clave]);
      return { clave, nombre: NOMBRES[clave], actual: nActual, respaldo: nRespaldo, diferencia: nRespaldo - nActual };
    });
  }
  function coleccionesQueSeConservan(respaldo) {
    if (!respaldo) return [];
    return Object.keys(NOMBRES).filter((clave) => respaldo[clave] === void 0).map((clave) => NOMBRES[clave]);
  }
  function crearPuntoDeGuardado(motivo = "manual") {
    const snapshot = {
      id: uid(),
      fecha: (/* @__PURE__ */ new Date()).toISOString(),
      automatica: motivo !== "manual",
      motivo,
      backupVersion: 3,
      data: datosDelNegocio()
    };
    setHistorial((s) => [snapshot, ...s].slice(0, 30));
    return snapshot;
  }
  function restaurarDesdeHistorial(snapshot) {
    setPendingRestore({ ...snapshot.data, exportadoEl: snapshot.fecha });
  }
  function abrirRespaldo() {
    const data = {
      version: 2,
      // se mantiene por compatibilidad con lectores antiguos
      backupVersion: 3,
      // 3 = incluye nóminas, facturas, producción, traspasos y auditoría
      exportadoEl: (/* @__PURE__ */ new Date()).toISOString(),
      ...datosDelNegocio()
    };
    setBackupText(JSON.stringify(data));
    setCopiado(false);
    setShowBackupView(true);
  }
  function copiarRespaldo() {
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(backupText).then(() => setCopiado(true)).catch(() => setCopiado(false));
    }
  }
  function abrirRestaurar() {
    setRestoreText("");
    setRestoreError("");
    setShowRestoreInput(true);
  }
  function validarRespaldo(data) {
    if (!data || typeof data !== "object" || Array.isArray(data)) {
      return "El respaldo no es v\xE1lido: no tiene el formato esperado.";
    }
    const conocidas = Object.keys(NOMBRES).filter((clave) => data[clave] !== void 0);
    if (conocidas.length === 0) {
      return "El respaldo no es v\xE1lido o est\xE1 incompleto: no contiene ninguna colecci\xF3n reconocible.";
    }
    for (const clave of conocidas) {
      const valor = data[clave];
      const esObjetoValido = clave === "catalogoProv" && valor && typeof valor === "object" && !Array.isArray(valor);
      if (!Array.isArray(valor) && !esObjetoValido) {
        return `El respaldo est\xE1 da\xF1ado: "${NOMBRES[clave]}" no tiene el formato correcto.`;
      }
    }
    return null;
  }
  function analizarTextoRestauracion() {
    let data;
    try {
      data = JSON.parse(restoreText.trim());
    } catch (err) {
      setRestoreError("Ese texto no parece un respaldo v\xE1lido. Revisa que lo hayas copiado completo.");
      return;
    }
    const error = validarRespaldo(data);
    if (error) {
      setRestoreError(error);
      return;
    }
    setRestoreError("");
    setShowRestoreInput(false);
    setPendingRestore(data);
  }
  function confirmarRestauracion() {
    if (!pendingRestore) return;
    const puntoPrevio = crearPuntoDeGuardado("previo-a-restauracion");
    let restauradas = 0;
    Object.entries(SETTERS).forEach(([clave, setter]) => {
      if (pendingRestore[clave] !== void 0 && setter) {
        setter(pendingRestore[clave]);
        restauradas++;
      }
    });
    if (registrarAuditoria) {
      const version = pendingRestore.backupVersion || pendingRestore.version || "antigua";
      const fechaResp = pendingRestore.exportadoEl ? new Date(pendingRestore.exportadoEl).toLocaleString("es-ES") : "sin fecha";
      registrarAuditoria(
        "Restaurar respaldo",
        `${restauradas} colecciones restauradas \xB7 respaldo del ${fechaResp} (formato ${version}) \xB7 punto de recuperaci\xF3n creado: ${puntoPrevio.id}`
      );
    }
    setPendingRestore(null);
  }
  function exportarExcelGeneral() {
    const wb = utils.book_new();
    const hojaProductos = productos.map((p) => ({
      C\u00F3digo: p.codigo || "",
      Nombre: p.nombre,
      Categor\u00EDa: p.categoria || "",
      Tipo: p.tipo || "materia_prima",
      Unidad: p.unidad,
      "Costo sin IVA": Number(p.costo) || 0,
      "IVA compra %": ivaCompraDe(p),
      "Precio venta c/IVA": Number(p.precioVenta) || 0,
      "IVA venta %": ivaDe(p),
      Stock: Number(p.stock) || 0,
      "Stock m\xEDnimo": Number(p.stockMinimo) || 0,
      Proveedor: proveedorPorId(p.proveedorId)?.nombre || ""
    }));
    utils.book_append_sheet(wb, utils.json_to_sheet(hojaProductos), "Productos");
    const hojaMov = movimientos.map((m) => ({
      Fecha: m.fecha,
      Tipo: m.tipo,
      Motivo: m.motivo || "",
      Producto: productoPorId(m.productoId)?.nombre || "",
      Cantidad: m.cantidad,
      "Costo unitario": m.costoUnitario || "",
      "Ingreso unitario": m.ingresoUnitario || "",
      "Medio de pago": m.medioPago || "",
      Referencia: m.referencia || ""
    }));
    utils.book_append_sheet(wb, utils.json_to_sheet(hojaMov), "Movimientos");
    const hojaAlb = albaranes.filter((a) => a.estado === "confirmado").map((a) => {
      const base = (a.lineas || []).reduce((x, ln) => x + (Number(ln.importe) || 0), 0);
      return {
        Fecha: a.fechaFactura || a.fecha,
        Proveedor: proveedorPorId(a.proveedorId)?.nombre || "",
        "N\xBA albar\xE1n": a.numero || "",
        "N\xBA factura": a.numeroFactura || "",
        Base: Number(base.toFixed(2)),
        Pagada: a.pagada ? "S\xED" : "No"
      };
    });
    utils.book_append_sheet(wb, utils.json_to_sheet(hojaAlb), "Albaranes");
    writeFileSync(wb, `datos_almacen_${todayISO()}.xlsx`);
  }
  return {
    crearPuntoDeGuardado,
    restaurarDesdeHistorial,
    abrirRespaldo,
    copiarRespaldo,
    abrirRestaurar,
    analizarTextoRestauracion,
    confirmarRestauracion,
    exportarExcelGeneral,
    compararConEstadoActual,
    coleccionesQueSeConservan
  };
}
function litrosPorUnidadDeStock(producto) {
  if (!producto || !(Number(producto.contenido) > 0) || !producto.unidadContenido) return 1;
  const unidad = String(producto.unidadContenido).trim().toLowerCase();
  if (/^l(itro)?s?$/.test(unidad)) return Number(producto.contenido);
  if (/^ml$|^mililitros?$/.test(unidad)) return Number(producto.contenido) / 1e3;
  return 1;
}
function litrosDisponibles(producto) {
  if (!producto) return 0;
  return (Number(producto.stock) || 0) * litrosPorUnidadDeStock(producto);
}
function crearLogicaAceite({ freidoras, setFreidoras, registrosAceite, setRegistrosAceite, productos, setProductos, movimientos, setMovimientos, registrarAuditoria }) {
  const { aplicarMovimientoStock } = crearMotorStock({ productos, setProductos, movimientos, setMovimientos, registrarAuditoria });
  function addFreidora(data) {
    const nueva = {
      id: uid(),
      nombre: data.nombre,
      litrosCarga: Number(data.litrosCarga) || 0,
      rellenoHabitual: Number(data.rellenoHabitual) || 5,
      productoAceiteId: data.productoAceiteId || "",
      activa: true
    };
    setFreidoras((s) => [...s, nueva]);
    return nueva;
  }
  function updateFreidora(id, data) {
    setFreidoras((s) => s.map((f) => f.id === id ? { ...f, ...data } : f));
  }
  function deleteFreidora(id) {
    const f = freidoras.find((x) => x.id === id);
    registrarAuditoria("Eliminar freidora", f ? f.nombre : id);
    setFreidoras((s) => s.filter((f2) => f2.id !== id));
  }
  function descontarAceite(productoAceiteId, litros, motivo, referencia, registroAceiteId) {
    if (!productoAceiteId || !(litros > 0)) return { ok: false, error: "Falta el aceite o los litros." };
    const prod = productos.find((p) => p.id === productoAceiteId);
    if (!prod) return { ok: false, error: "El aceite enlazado ya no existe en el cat\xE1logo." };
    const costoUnitario = Number(prod.costo) || 0;
    const factor = litrosPorUnidadDeStock(prod);
    const unidadesARestar = litros / factor;
    const r = aplicarMovimientoStock({
      productoId: productoAceiteId,
      cantidad: -unidadesARestar,
      tipo: "ACEITE_CONSUMO",
      movimientoId: registroAceiteId ? `aceite-${registroAceiteId}` : uid(),
      origen: "descontarAceite",
      documentoOrigenId: registroAceiteId || null,
      afectaStockTotal: true,
      permitirDeficit: true,
      motivo
    });
    if (!r.ok) return r;
    const stockDespues = r.movimiento.stockPosterior;
    return { ok: true, stockRestante: stockDespues, litrosRestantes: stockDespues * factor, coste: unidadesARestar * costoUnitario };
  }
  function registrarCambio({ freidoraId, litros, tipoCambio, observaciones, responsable }) {
    const f = freidoras.find((x) => x.id === freidoraId);
    if (!f) return { ok: false, error: "Selecciona una freidora." };
    const litrosUsados = Number(litros) || Number(f.litrosCarga) || 0;
    if (!(litrosUsados > 0)) return { ok: false, error: "Indica cu\xE1ntos litros lleva el cambio." };
    const registroId = uid();
    const r = descontarAceite(f.productoAceiteId, litrosUsados, "Cambio de aceite", `Cambio de aceite \xB7 ${f.nombre}`, registroId);
    if (!r.ok) return r;
    const registro = {
      id: registroId,
      freidoraId,
      freidoraNombre: f.nombre,
      productoAceiteId: f.productoAceiteId,
      tipo: "cambio",
      tipoCambio: tipoCambio || "Voluntario",
      litros: litrosUsados,
      observaciones: observaciones || "",
      responsable: responsable || "",
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      coste: r.coste
    };
    setRegistrosAceite((s) => [registro, ...s]);
    registrarAuditoria("Cambio de aceite", `${f.nombre} \xB7 ${litrosUsados} L${observaciones ? " \xB7 " + observaciones : ""}`);
    return { ok: true, registro, stockRestante: r.stockRestante };
  }
  function registrarRelleno({ freidoraId, litros, responsable }) {
    const f = freidoras.find((x) => x.id === freidoraId);
    if (!f) return { ok: false, error: "Selecciona una freidora." };
    const litrosUsados = Number(litros) || Number(f.rellenoHabitual) || 0;
    if (!(litrosUsados > 0)) return { ok: false, error: "Indica cu\xE1ntos litros se han rellenado." };
    const registroId = uid();
    const r = descontarAceite(f.productoAceiteId, litrosUsados, "Relleno de aceite", `Relleno \xB7 ${f.nombre}`, registroId);
    if (!r.ok) return r;
    const registro = {
      id: registroId,
      freidoraId,
      freidoraNombre: f.nombre,
      productoAceiteId: f.productoAceiteId,
      tipo: "relleno",
      litros: litrosUsados,
      responsable: responsable || "",
      fecha: todayISO(),
      hora: (/* @__PURE__ */ new Date()).toTimeString().slice(0, 5),
      coste: r.coste
    };
    setRegistrosAceite((s) => [registro, ...s]);
    return { ok: true, registro, stockRestante: r.stockRestante };
  }
  function eliminarRegistroAceite(registro) {
    const freidoraDelRegistro = freidoras.find((f) => f.id === registro.freidoraId);
    const productoAceiteId = registro.productoAceiteId || (freidoraDelRegistro ? freidoraDelRegistro.productoAceiteId : null);
    if (productoAceiteId) {
      const movimientoOriginal = movimientos.find(
        (m) => m.documentoOrigenId === registro.id || m.registroAceiteId === registro.id
      );
      if (movimientoOriginal) {
        aplicarMovimientoStock({
          productoId: productoAceiteId,
          cantidad: -Number(movimientoOriginal.cantidad || 0),
          tipo: movimientoOriginal.tipo || "ACEITE_CONSUMO",
          movimientoId: `${movimientoOriginal.id}-anulado`,
          origen: "eliminarRegistroAceite",
          documentoOrigenId: registro.id,
          afectaStockTotal: true,
          permitirDeficit: true,
          revierteMovimientoId: movimientoOriginal.id,
          motivo: "Eliminaci\xF3n de registro de aceite"
        });
      } else {
        const prod = productos.find((p) => p.id === productoAceiteId);
        const factor = litrosPorUnidadDeStock(prod);
        const unidadesADevolver = (Number(registro.litros) || 0) / factor;
        aplicarMovimientoStock({
          productoId: productoAceiteId,
          cantidad: unidadesADevolver,
          tipo: "ACEITE_DEVOLUCION",
          movimientoId: uid(),
          origen: "eliminarRegistroAceite",
          documentoOrigenId: registro.id,
          afectaStockTotal: true,
          permitirDeficit: true,
          motivo: "Eliminaci\xF3n de registro de aceite (sin movimiento original localizable)"
        });
      }
    }
    setRegistrosAceite((s) => s.filter((r) => r.id !== registro.id));
    registrarAuditoria(
      "Eliminar registro de aceite",
      `${registro.freidoraNombre || "\u2014"} \xB7 ${registro.tipo === "cambio" ? "Cambio" : "Relleno"} de ${registro.litros} L` + (productoAceiteId ? "" : " \xB7 AVISO: no se pudo identificar el aceite, el stock no se ha devuelto")
    );
    return { ok: true, stockDevuelto: !!productoAceiteId };
  }
  function consumoPorCiclo(freidoraId, registros) {
    const lista = registros || registrosAceite || [];
    const suyos = lista.filter((r) => r.freidoraId === freidoraId);
    const posCambio = suyos.findIndex((r) => r.tipo === "cambio");
    if (posCambio === -1) return null;
    const ultimoCambio = suyos[posCambio];
    const posteriores = suyos.slice(0, posCambio).filter((r) => r.tipo === "relleno");
    const litrosRelleno = posteriores.reduce((a, r) => a + (Number(r.litros) || 0), 0);
    const dias = Math.max(1, Math.round((new Date(todayISO()) - new Date(ultimoCambio.fecha)) / 864e5));
    return {
      desde: ultimoCambio.fecha,
      litrosCambio: Number(ultimoCambio.litros) || 0,
      litrosRelleno,
      litrosTotal: (Number(ultimoCambio.litros) || 0) + litrosRelleno,
      numRellenos: posteriores.length,
      dias,
      coste: (Number(ultimoCambio.coste) || 0) + posteriores.reduce((a, r) => a + (Number(r.coste) || 0), 0)
    };
  }
  return { addFreidora, updateFreidora, deleteFreidora, registrarCambio, registrarRelleno, eliminarRegistroAceite, consumoPorCiclo };
}
function crearLogicaFacturasDirectas({ facturasDirectas, setFacturasDirectas, registrarAuditoria, addGasto, deleteGasto, gastosGenerales }) {
  function addFacturaDirecta(data) {
    const base = Number(data.importeBase) || 0;
    const ivaPct = Number(data.ivaPct) || 0;
    const importeIva = base * (ivaPct / 100);
    const importeTotal = base + importeIva;
    setFacturasDirectas((s) => [
      { id: uid(), pagada: false, ...data, importeBase: base, ivaPct, importeIva, importeTotal },
      ...s
    ]);
  }
  function updateFacturaDirecta(id, data) {
    setFacturasDirectas(
      (s) => s.map((f) => {
        if (f.id !== id) return f;
        const actualizada = { ...f, ...data };
        const base = Number(actualizada.importeBase) || 0;
        const ivaPct = Number(actualizada.ivaPct) || 0;
        return { ...actualizada, importeBase: base, ivaPct, importeIva: base * (ivaPct / 100), importeTotal: base + base * (ivaPct / 100) };
      })
    );
  }
  function deleteFacturaDirecta(id) {
    setFacturasDirectas((s) => {
      const f = s.find((x) => x.id === id);
      registrarAuditoria("Eliminar factura directa", f ? `${f.concepto} \xB7 \u20AC${(Number(f.importeTotal) || 0).toFixed(2)}` : id);
      return s.filter((x) => x.id !== id);
    });
    if (deleteGasto && gastosGenerales) {
      const previo = gastosGenerales.find((g) => g.facturaDirectaId === id);
      if (previo) deleteGasto(previo.id);
    }
  }
  function marcarPagadaFacturaDirecta(id, pagada) {
    const f = facturasDirectas.find((x) => x.id === id);
    setFacturasDirectas((s) => s.map((x) => x.id === id ? { ...x, pagada, fechaPago: pagada ? todayISO() : "" } : x));
    if (pagada) {
      registrarAuditoria("Marcar factura directa como pagada", id);
      if (f && addGasto) {
        addGasto({
          concepto: f.concepto || "Factura directa",
          importe: Number(f.importeTotal) || 0,
          fecha: todayISO(),
          facturaDirectaId: id,
          categoria: f.categoria || ""
        });
      }
    } else if (deleteGasto && gastosGenerales) {
      const previo = gastosGenerales.find((g) => g.facturaDirectaId === id);
      if (previo) deleteGasto(previo.id);
    }
  }
  return { addFacturaDirecta, updateFacturaDirecta, deleteFacturaDirecta, marcarPagadaFacturaDirecta };
}
function crearLogicaNominas({ setNominas, registrarAuditoria }) {
  function addNomina(data) {
    const bruto = Number(data.brutoTotal) || 0;
    const ss = Number(data.seguridadSocialEmpresa) || 0;
    setNominas((s) => {
      const sinEsteMes = s.filter((n) => !(n.empleadoId === data.empleadoId && n.mes === data.mes));
      return [
        { id: uid(), ...data, brutoTotal: bruto, seguridadSocialEmpresa: ss, costeTotalEmpresa: bruto + ss },
        ...sinEsteMes
      ];
    });
  }
  function updateNomina(id, data) {
    const bruto = Number(data.brutoTotal) || 0;
    const ss = Number(data.seguridadSocialEmpresa) || 0;
    setNominas(
      (s) => s.map((n) => n.id === id ? { ...n, ...data, brutoTotal: bruto, seguridadSocialEmpresa: ss, costeTotalEmpresa: bruto + ss } : n)
    );
    registrarAuditoria("Editar registro de n\xF3mina", `${data.mes || ""} \xB7 \u20AC${(bruto + ss).toFixed(2)}`);
  }
  function deleteNomina(id) {
    setNominas((s) => {
      const n = s.find((x) => x.id === id);
      registrarAuditoria("Eliminar registro de n\xF3mina", n ? `${n.mes} \xB7 \u20AC${(Number(n.costeTotalEmpresa) || 0).toFixed(2)}` : id);
      return s.filter((x) => x.id !== id);
    });
  }
  return { addNomina, updateNomina, deleteNomina };
}
function crearLogicaEntrevistas({ entrevistas, setEntrevistas, registrarAuditoria }) {
  function crearEntrevista(candidatoNombre) {
    const nueva = {
      id: uid(),
      candidatoNombre: candidatoNombre.trim(),
      fecha: todayISO(),
      estado: "en_curso",
      // en_curso | completada
      historial: [],
      // [{ pregunta, respuesta }]
      informe: null
    };
    setEntrevistas((s) => [nueva, ...s]);
    return nueva;
  }
  function actualizarEntrevista(id, cambios) {
    setEntrevistas((s) => s.map((e) => e.id === id ? { ...e, ...cambios } : e));
  }
  function finalizarEntrevista(id, informe) {
    setEntrevistas((s) => s.map((e) => e.id === id ? { ...e, estado: "completada", informe } : e));
    const ent = entrevistas.find((e) => e.id === id);
    registrarAuditoria(
      "Entrevista completada",
      `${ent ? ent.candidatoNombre : id} \xB7 recomendado: ${informe?.puesto_principal || "\u2014"}`
    );
  }
  function eliminarEntrevista(id) {
    const ent = entrevistas.find((e) => e.id === id);
    registrarAuditoria("Eliminar entrevista", ent ? ent.candidatoNombre : id);
    setEntrevistas((s) => s.filter((e) => e.id !== id));
  }
  return { crearEntrevista, actualizarEntrevista, finalizarEntrevista, eliminarEntrevista };
}
function SelectorDiseno({ disenoMenu, setDisenoMenu, oscuro = false }) {
  const opciones = [
    { id: "A", label: "Depart." },
    { id: "B", label: "Ciclos" },
    { id: "C", label: "R\xE1pido" }
  ];
  return /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-1 rounded-lg p-0.5 shrink-0", style: { background: oscuro ? "rgba(255,255,255,0.08)" : C.bg } }, opciones.map((o) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: o.id,
      onClick: () => setDisenoMenu(o.id),
      className: "text-[10.5px] font-semibold px-2 py-1 rounded-md whitespace-nowrap",
      style: {
        background: disenoMenu === o.id ? C.accent : "transparent",
        color: disenoMenu === o.id ? "#fff" : oscuro ? "#B9C7C0" : C.inkSoft
      }
    },
    o.label
  )));
}
function BotonTema({ temaOscuro, setTemaOscuro }) {
  return /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      onClick: () => setTemaOscuro(!temaOscuro),
      className: "text-[10.5px] font-semibold px-2 py-1 rounded-md whitespace-nowrap shrink-0",
      style: { background: "rgba(255,255,255,0.08)", color: "#B9C7C0" },
      title: temaOscuro ? "Cambiar a modo claro" : "Cambiar a modo oscuro"
    },
    temaOscuro ? "\u2600\uFE0F Claro" : "\u{1F319} Oscuro"
  );
}
function BotonModoEmpleado({ modoEmpleado, salirModoEmpleado, empleados = [], usuarioActivoId, entrarComoEmpleado, pinPropietario }) {
  const [pantalla, setPantalla] = (0, import_react.useState)(null);
  const [empleadoId, setEmpleadoId] = (0, import_react.useState)("");
  const [pin, setPin] = (0, import_react.useState)("");
  const [error, setError] = (0, import_react.useState)("");
  const conAcceso = empleados.filter((e) => e.activo !== false && e.pin);
  const empleadoActivo = empleados.find((e) => e.id === usuarioActivoId);
  function cerrar() {
    setPantalla(null);
    setPin("");
    setError("");
    setEmpleadoId("");
  }
  function elegirEmpleado(id) {
    setEmpleadoId(id);
    setPantalla("codigo");
    setError("");
  }
  async function intentarEntrar() {
    if (await entrarComoEmpleado(empleadoId, pin)) {
      cerrar();
    } else {
      setError("C\xF3digo incorrecto.");
    }
  }
  async function intentarSalir() {
    if (await salirModoEmpleado(pin)) {
      cerrar();
    } else {
      setError("PIN del propietario incorrecto.");
    }
  }
  return /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      onClick: () => setPantalla(modoEmpleado ? "salir" : "elegir"),
      className: "text-[10.5px] font-semibold px-2 py-1 rounded-md whitespace-nowrap shrink-0",
      style: { background: "rgba(255,255,255,0.08)", color: "#B9C7C0" },
      title: modoEmpleado ? "Toca para cambiar de usuario o salir" : "Entrar en modo empleado"
    },
    modoEmpleado ? `\u{1F512} ${empleadoActivo ? empleadoActivo.nombre : "Empleado"}` : "\u{1F513} Modo empleado"
  ), pantalla === "elegir" && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: cerrar, title: "\xBFQui\xE9n eres?" }, conAcceso.length === 0 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]", style: { color: C.inkSoft } }, "Todav\xEDa no hay ning\xFAn empleado con c\xF3digo de acceso. Se pone en Personal, editando su ficha.") : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, conAcceso.map((e) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: e.id,
      onClick: () => elegirEmpleado(e.id),
      className: "w-full text-left px-3 py-2.5 rounded-lg text-[13px] font-medium",
      style: { background: C.bg, border: `1px solid ${C.line}` }
    },
    e.nombre
  )))), pantalla === "codigo" && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: cerrar, title: "Tu c\xF3digo de acceso" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "C\xF3digo" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "password", inputMode: "numeric", value: pin, onChange: (e) => setPin(e.target.value), autoFocus: true })), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: intentarEntrar }, "Entrar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setPantalla("elegir") }, "Atr\xE1s"))), pantalla === "salir" && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: cerrar, title: `Modo empleado \u2014 ${empleadoActivo ? empleadoActivo.nombre : "sin identificar"}` }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    cerrar();
    setPantalla("elegir");
  } }, "Cambiar de usuario")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-1", style: { color: C.inkSoft } }, "O salir a modo propietario"), !pinPropietario && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2 p-2 rounded-lg", style: { background: C.amberSoft, color: C.ink } }, 'Todav\xEDa no has creado un PIN de propietario, as\xED que puedes salir directamente sin escribir nada \u2014 toca "Salir". En cuanto puedas, cr\xE9alo en Respaldos para proteger la salida de verdad.'), /* @__PURE__ */ import_react.default.createElement(Field, { label: "PIN del propietario" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "password", inputMode: "numeric", value: pin, onChange: (e) => setPin(e.target.value), autoFocus: true })), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: intentarSalir }, "Salir"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: cerrar }, "Cancelar"))));
}
function SidebarGrupos({ grupos, tab, setTab, disenoMenu, setDisenoMenu, temaOscuro, setTemaOscuro, modoEmpleado, salirModoEmpleado, empleados, usuarioActivoId, entrarComoEmpleado, pinPropietario }) {
  return /* @__PURE__ */ import_react.default.createElement("aside", { style: { background: C.chrome, color: "#fff" }, className: "w-full md:w-64 shrink-0 p-4 md:p-5 flex md:flex-col gap-1 overflow-x-auto md:overflow-visible" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "hidden md:block mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "rounded-lg px-3 py-3.5 mb-2.5", style: { background: "#fff" } }, /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { width: "100%", height: "auto", display: "block" } })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] tracking-widest uppercase mb-3", style: { color: "#9CB6A9" } }, "Control de almac\xE9n"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1.5 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(SelectorDiseno, { disenoMenu, setDisenoMenu, oscuro: true }), /* @__PURE__ */ import_react.default.createElement(BotonTema, { temaOscuro, setTemaOscuro }), /* @__PURE__ */ import_react.default.createElement(BotonModoEmpleado, { modoEmpleado, salirModoEmpleado, empleados, usuarioActivoId, entrarComoEmpleado, pinPropietario }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "md:hidden shrink-0 flex items-center gap-1.5 pr-2" }, /* @__PURE__ */ import_react.default.createElement(SelectorDiseno, { disenoMenu, setDisenoMenu, oscuro: true }), /* @__PURE__ */ import_react.default.createElement(BotonTema, { temaOscuro, setTemaOscuro }), /* @__PURE__ */ import_react.default.createElement(BotonModoEmpleado, { modoEmpleado, salirModoEmpleado, empleados, usuarioActivoId, entrarComoEmpleado, pinPropietario })), grupos.map((g, gi) => /* @__PURE__ */ import_react.default.createElement("div", { key: gi, className: "contents md:block md:mb-1" }, g.titulo && /* @__PURE__ */ import_react.default.createElement("div", { className: "hidden md:block text-[10.5px] font-semibold uppercase tracking-wide mt-3 mb-1 px-3", style: { color: "#7E9186" } }, g.titulo), g.titulo && /* @__PURE__ */ import_react.default.createElement("div", { className: "md:hidden shrink-0 self-stretch mx-1", style: { borderLeft: "1px solid rgba(255,255,255,0.15)" } }), g.items.map((it) => {
    const Icon = it.icon;
    const active = tab === it.id;
    return /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: it.id,
        onClick: () => setTab(it.id),
        className: "flex items-center gap-2.5 px-3 py-2.5 rounded-lg text-left shrink-0 md:shrink",
        style: {
          background: active ? "rgba(255,255,255,0.12)" : "transparent",
          color: active ? "#fff" : "#B9C7C0"
        }
      },
      /* @__PURE__ */ import_react.default.createElement(Icon, { size: 17 }),
      /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[13px] font-medium whitespace-nowrap" }, it.label),
      !!it.badge && /* @__PURE__ */ import_react.default.createElement(
        "span",
        {
          className: "ml-auto text-[10px] font-semibold px-1.5 py-0.5 rounded-full mono",
          style: { background: it.badgeColor || C.accent, color: "#fff" }
        },
        it.badge
      )
    );
  }))));
}
function TopBarC({ disenoMenu, setDisenoMenu, temaOscuro, setTemaOscuro, modoEmpleado, salirModoEmpleado, empleados, usuarioActivoId, entrarComoEmpleado, pinPropietario }) {
  return /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between px-4 py-2.5 shrink-0", style: { background: C.chrome, color: "#fff" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2 min-w-0" }, /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 26, width: "auto", background: "#fff", borderRadius: 6, padding: 2 } }), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11.5px] font-semibold truncate", style: { color: "#9CB6A9" } }, "Control de almac\xE9n")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement(SelectorDiseno, { disenoMenu, setDisenoMenu, oscuro: true }), /* @__PURE__ */ import_react.default.createElement(BotonTema, { temaOscuro, setTemaOscuro }), /* @__PURE__ */ import_react.default.createElement(BotonModoEmpleado, { modoEmpleado, salirModoEmpleado, empleados, usuarioActivoId, entrarComoEmpleado, pinPropietario })));
}
function BotonItemSheet({ it, tab, onPick }) {
  const Icon = it.icon;
  const active = tab === it.id;
  return /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      onClick: onPick,
      className: "w-full flex items-center gap-2.5 px-3 py-2.5 rounded-lg text-left mb-1",
      style: { background: active ? C.accentSoft : C.bg, color: C.ink }
    },
    /* @__PURE__ */ import_react.default.createElement(Icon, { size: 17, color: active ? C.accent : C.inkSoft }),
    /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[13px] font-medium" }, it.label),
    !!it.badge && /* @__PURE__ */ import_react.default.createElement("span", { className: "ml-auto text-[10px] font-semibold px-1.5 py-0.5 rounded-full mono", style: { background: it.badgeColor || C.accent, color: "#fff" } }, it.badge)
  );
}
function BottomNavC({ tab, setTab, categorias, dashboardItem }) {
  const [sheet, setSheet] = (0, import_react.useState)(null);
  const enComprar = categorias.comprar.items.some((i) => i.id === tab);
  const enAlmacen = categorias.almacen.items.some((i) => i.id === tab);
  const enMas = categorias.mas.some((g) => g.items.some((i) => i.id === tab));
  const enHoy = tab === "dashboard";
  const badgeComprar = categorias.comprar.items.some((i) => i.badge > 0);
  const badgeAlmacen = categorias.almacen.items.some((i) => i.badge > 0);
  const badgeMas = categorias.mas.some((g) => g.items.some((i) => i.badge > 0));
  function BotonNav({ activo, icon: Icon, label, onClick, badge }) {
    return /* @__PURE__ */ import_react.default.createElement("button", { onClick, className: "flex-1 flex flex-col items-center gap-0.5 py-2 relative" }, /* @__PURE__ */ import_react.default.createElement(Icon, { size: 20, color: activo ? C.accent : C.inkSoft }), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[10.5px] font-medium", style: { color: activo ? C.accent : C.inkSoft } }, label), !!badge && /* @__PURE__ */ import_react.default.createElement("span", { className: "absolute top-1.5 right-[28%] w-2 h-2 rounded-full", style: { background: C.red } }));
  }
  return /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex shrink-0", style: { background: C.surface, borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement(BotonNav, { activo: enHoy, icon: ChartColumn, label: "Hoy", onClick: () => setTab("dashboard") }), /* @__PURE__ */ import_react.default.createElement(BotonNav, { activo: enComprar, icon: ShoppingCart, label: "Comprar", onClick: () => setSheet("comprar"), badge: badgeComprar }), /* @__PURE__ */ import_react.default.createElement(BotonNav, { activo: enAlmacen, icon: Package, label: "Almac\xE9n", onClick: () => setSheet("almacen"), badge: badgeAlmacen }), /* @__PURE__ */ import_react.default.createElement(BotonNav, { activo: enMas, icon: Ellipsis, label: "M\xE1s", onClick: () => setSheet("mas"), badge: badgeMas })), sheet === "comprar" && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setSheet(null), title: categorias.comprar.titulo }, categorias.comprar.items.map((it) => /* @__PURE__ */ import_react.default.createElement(BotonItemSheet, { key: it.id, it, tab, onPick: () => {
    setTab(it.id);
    setSheet(null);
  } }))), sheet === "almacen" && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setSheet(null), title: categorias.almacen.titulo }, categorias.almacen.items.map((it) => /* @__PURE__ */ import_react.default.createElement(BotonItemSheet, { key: it.id, it, tab, onPick: () => {
    setTab(it.id);
    setSheet(null);
  } }))), sheet === "mas" && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setSheet(null), title: "M\xE1s" }, categorias.mas.map((g) => /* @__PURE__ */ import_react.default.createElement("div", { key: g.titulo, className: "mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] font-semibold uppercase tracking-wide mb-1", style: { color: C.inkSoft } }, g.titulo), g.items.map((it) => /* @__PURE__ */ import_react.default.createElement(BotonItemSheet, { key: it.id, it, tab, onPick: () => {
    setTab(it.id);
    setSheet(null);
  } }))))));
}
function Card({ children, className = "", style = {} }) {
  return /* @__PURE__ */ import_react.default.createElement("div", { className: `rounded-xl p-4 ${className}`, style: { background: C.surface, border: `1px solid ${C.line}`, ...style } }, children);
}
function SectionTitle({ children, action }) {
  return /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-4" }, /* @__PURE__ */ import_react.default.createElement("h2", { className: "text-[18px] font-semibold" }, children), action);
}
function Btn({ children, onClick, variant = "primary", type = "button", small, disabled = false }) {
  const styles = {
    primary: { background: C.accent, color: "#fff" },
    ghost: { background: "transparent", color: C.ink, border: `1px solid ${C.line}` },
    danger: { background: C.redSoft, color: C.red }
  };
  return /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      type,
      onClick,
      disabled,
      className: `inline-flex items-center gap-1.5 rounded-lg font-medium ${small ? "px-2.5 py-1.5 text-[12px]" : "px-3.5 py-2 text-[13px]"}`,
      style: { ...styles[variant], opacity: disabled ? 0.45 : 1, cursor: disabled ? "not-allowed" : "pointer" }
    },
    children
  );
}
function LinkBtn({ children, href, variant = "ghost", small, newTab = true }) {
  const styles = {
    primary: { background: C.accent, color: "#fff" },
    ghost: { background: "transparent", color: C.ink, border: `1px solid ${C.line}` },
    danger: { background: C.redSoft, color: C.red }
  };
  return /* @__PURE__ */ import_react.default.createElement(
    "a",
    {
      href,
      ...newTab ? { target: "_blank", rel: "noopener noreferrer" } : {},
      className: `inline-flex items-center gap-1.5 rounded-lg font-medium no-underline ${small ? "px-2.5 py-1.5 text-[12px]" : "px-3.5 py-2 text-[13px]"}`,
      style: styles[variant]
    },
    children
  );
}
function Input(props) {
  return /* @__PURE__ */ import_react.default.createElement(
    "input",
    {
      ...props,
      className: `w-full rounded-lg px-3 py-2 text-[13px] outline-none ${props.className || ""}`,
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink, ...props.style || {} }
    }
  );
}
function archivoABase64(file) {
  return new Promise((resolve, reject) => {
    const lector = new FileReader();
    lector.onload = () => {
      const [cabecera, base64] = lector.result.split(",");
      const mediaType = cabecera.match(/data:(.*?);/)[1];
      resolve({ base64, mediaType, previsualizacion: lector.result });
    };
    lector.onerror = reject;
    lector.readAsDataURL(file);
  });
}
function Field({ label, children }) {
  return /* @__PURE__ */ import_react.default.createElement("label", { className: "block mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-1", style: { color: C.inkSoft } }, label), children);
}
var CATEGORIAS_GASTO = [
  "Alquiler",
  "Suministros (luz, agua, gas)",
  "Gestor\xEDa y asesor\xEDa",
  "Seguros",
  "Mantenimiento y reparaciones",
  "Marketing y publicidad",
  "Suscripciones y software",
  "Comisiones bancarias y TPV",
  "Transporte y log\xEDstica",
  "Limpieza y qu\xEDmicos",
  "Otra"
];
function CampoCategoria({ value, onChange, opciones = CATEGORIAS_PRODUCTO }) {
  const [modoLibre, setModoLibre] = (0, import_react.useState)(() => !!value && !opciones.includes(value));
  const seleccionado = modoLibre ? "Otra" : value || "";
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: seleccionado,
      onChange: (e) => {
        const v = e.target.value;
        if (v === "Otra") {
          setModoLibre(true);
        } else {
          setModoLibre(false);
          onChange(v);
        }
      },
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Sin categor\xEDa"),
    opciones.filter((c) => c !== "Otra").map((c) => /* @__PURE__ */ import_react.default.createElement("option", { key: c, value: c }, c)),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Otra" }, "Otra\u2026")
  ), modoLibre && /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      className: "mt-1.5",
      value,
      onChange: (e) => onChange(e.target.value),
      placeholder: "Escribe la categor\xEDa"
    }
  ));
}
function Empty({ text }) {
  return /* @__PURE__ */ import_react.default.createElement("div", { className: "text-center py-10 text-[13px]", style: { color: C.inkSoft } }, text);
}
function Pill({ children, color }) {
  return /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11px] font-semibold px-2 py-0.5 rounded-full", style: { background: color + "22", color } }, children);
}
function Dashboard({ valorInventario, valorUtillaje = 0, stockBajo, pedidosPendientes, margenPromedio, movimientos, productos, caducanPronto = [], proveedorPorId, vencenPronto = [], totalPendientePago = 0, documentosPersonalPronto = [], fichajesAbiertos = [], encargosUrgentes = [], pisoVentaBajo = [], sugerenciasPedido = [], setTab, recordatorioConteo = [], alertasAppcc = { pendientes: [], desviaciones: [] }, fallosGuardado = [], diagnosticoStock = [] }) {
  const entradas30 = movimientos.filter((m) => esEntrada(m)).reduce((a, m) => a + Math.abs(cantidadConSigno(m)), 0);
  const salidas30 = movimientos.filter((m) => esSalida(m)).reduce((a, m) => a + Math.abs(cantidadConSigno(m)), 0);
  const [abierta, setAbierta] = (0, import_react.useState)(null);
  const diasTexto = (d) => d < 0 ? `vencido hace ${Math.abs(d)} d` : d === 0 ? "hoy" : `en ${d} d`;
  const diasColor = (d) => d < 0 ? C.red : d <= 7 ? C.amber : C.inkSoft;
  const descuadresStock = diagnosticoStock.filter((d) => !d.coincide).sort((a, b) => Math.abs(b.diferencia) - Math.abs(a.diferencia));
  const tarjetas = [
    {
      id: "saldo",
      titulo: "Saldo de almac\xE9n",
      valor: `\u20AC${fmt(valorInventario)}`,
      nota: valorUtillaje > 0 ? `+ \u20AC${fmt(valorUtillaje)} en utillaje y limpieza (aparte, no es mercanc\xEDa)` : null,
      tab: "saldo",
      tabNombre: "Saldo de almac\xE9n",
      items: []
    },
    {
      id: "margen",
      titulo: "Margen promedio",
      valor: `${fmt(margenPromedio)}%`,
      color: C.accent,
      tab: "resultados",
      tabNombre: "Resultados",
      items: []
    },
    {
      id: "pedidos",
      titulo: "Pedidos pendientes",
      valor: pedidosPendientes.length,
      tab: "pedidos",
      tabNombre: "Pedidos",
      items: pedidosPendientes.map((pe) => ({
        clave: pe.id,
        principal: (proveedorPorId(pe.proveedorId) || {}).nombre || "Proveedor eliminado",
        secundario: `${(pe.items || []).length} producto(s) \xB7 ${pe.estado}`,
        derecha: pe.fechaEsperada ? `llega ${new Date(pe.fechaEsperada).toLocaleDateString("es-ES")}` : "sin fecha"
      }))
    },
    {
      id: "stockbajo",
      titulo: "Productos en stock bajo",
      valor: stockBajo.length,
      color: stockBajo.length ? C.red : C.ink,
      tab: "productos",
      tabNombre: "Productos",
      items: stockBajo.map((p) => ({
        clave: p.id,
        principal: p.nombre,
        secundario: p.zona || "",
        derecha: `${fmt(p.stock)} / m\xEDn. ${fmt(p.stockMinimo)}`,
        derechaColor: C.red
      }))
    },
    {
      id: "caducidades",
      titulo: "Lotes que caducan (30 d)",
      valor: caducanPronto.length,
      color: caducanPronto.some((l) => l.dias < 0) ? C.red : caducanPronto.length ? C.amber : C.ink,
      tab: "productos",
      tabNombre: "Productos",
      items: caducanPronto.map((l) => ({
        clave: l.id,
        principal: l.nombre,
        secundario: `${l.lote ? `lote ${l.lote} \xB7 ` : ""}${l.caducidad}${l.unidades ? ` \xB7 ${l.parcial ? "quedan aprox. " : ""}${fmt(l.unidades)} uds` : ""}`,
        derecha: diasTexto(l.dias),
        derechaColor: diasColor(l.dias)
      }))
    },
    {
      id: "facturas",
      titulo: "Facturas por pagar (7 d)",
      valor: vencenPronto.length,
      color: vencenPronto.some((f) => f.dias < 0) ? C.red : vencenPronto.length ? C.amber : C.ink,
      nota: totalPendientePago > 0 ? `\u20AC${fmt(totalPendientePago)} pendiente en total` : null,
      tab: "pagos",
      tabNombre: "Cuentas por pagar",
      items: vencenPronto.map((f) => ({
        clave: f.id,
        principal: f.proveedor && f.proveedor.nombre || "Sin proveedor",
        secundario: `${f.concepto || ""} \xB7 \u20AC${fmt(f.total)}`,
        derecha: diasTexto(f.dias),
        derechaColor: diasColor(f.dias)
      }))
    },
    {
      id: "encargos",
      titulo: "Encargos urgentes",
      valor: encargosUrgentes.length,
      color: encargosUrgentes.some((e) => e.dias < 0) ? C.red : encargosUrgentes.length ? C.amber : C.ink,
      nota: encargosUrgentes.length > 0 ? "en 2 d\xEDas o menos" : null,
      tab: "encargos",
      tabNombre: "Encargos",
      items: encargosUrgentes.map((e) => ({
        clave: e.id,
        principal: `Encargo ${e.numero || "s/n"}`,
        secundario: `${(e.lineas || []).length} producto(s)${e.horaEntrega ? ` \xB7 ${e.horaEntrega}` : ""}`,
        derecha: diasTexto(e.dias),
        derechaColor: diasColor(e.dias)
      }))
    },
    {
      id: "reponer",
      titulo: "Reponer piso de venta",
      valor: pisoVentaBajo.length,
      color: pisoVentaBajo.length ? C.amber : C.ink,
      nota: pisoVentaBajo.length > 0 ? "sacar del almac\xE9n al mostrador" : null,
      tab: "traspasos",
      tabNombre: "Traspasos",
      items: pisoVentaBajo.map((p) => ({
        clave: p.id,
        principal: p.nombre,
        secundario: `${fmt((Number(p.stock) || 0) - (Number(p.stockPisoVenta) || 0))} en el almac\xE9n`,
        derecha: `${fmt(p.stockPisoVenta || 0)} / m\xEDn. ${fmt(p.stockMinimoPisoVenta)}`,
        derechaColor: C.amber
      }))
    },
    {
      id: "fallos_guardado",
      titulo: "Guardado: cambios sin confirmar",
      valor: fallosGuardado.length,
      color: fallosGuardado.length ? C.red : C.ink,
      nota: fallosGuardado.length > 0 ? "algo no se ha podido guardar \u2014 revisa la conexi\xF3n antes de seguir" : null,
      tab: "respaldos",
      tabNombre: "Respaldos",
      items: fallosGuardado.slice(0, 10).map((f) => ({
        clave: f.id,
        principal: f.key,
        secundario: f.mensaje || "Error al guardar",
        derecha: new Date(f.fecha).toLocaleTimeString("es-ES", { hour: "2-digit", minute: "2-digit" }),
        derechaColor: C.red
      }))
    },
    {
      id: "appcc_desviaciones",
      titulo: "APPCC: desviaciones sin resolver",
      valor: alertasAppcc.desviaciones.length,
      color: alertasAppcc.desviaciones.length ? C.red : C.ink,
      nota: alertasAppcc.desviaciones.length > 0 ? "un valor sali\xF3 fuera de rango y no se ha vuelto a medir bien" : null,
      tab: "appcc",
      tabNombre: "APPCC",
      items: alertasAppcc.desviaciones.slice(0, 10).map((r) => ({
        clave: r.id,
        principal: r.puntoNombre || "Punto de control",
        secundario: `${r.fecha}${r.hora ? " " + r.hora : ""}${r.observaciones ? " \xB7 " + r.observaciones : ""}`,
        derecha: r.valor != null && r.valor !== "" ? String(r.valor) : "no conforme",
        derechaColor: C.red
      }))
    },
    {
      id: "appcc_pendientes",
      titulo: "APPCC: controles de hoy",
      valor: alertasAppcc.pendientes.length,
      color: alertasAppcc.pendientes.length ? C.amber : C.ink,
      nota: alertasAppcc.pendientes.length > 0 ? "pendientes de registrar" : null,
      tab: "appcc",
      tabNombre: "APPCC",
      items: alertasAppcc.pendientes.map((p) => ({
        clave: p.id,
        principal: p.nombre,
        secundario: p.frecuencia,
        derecha: p.faltan > 1 ? `faltan ${p.faltan}` : "pendiente",
        derechaColor: C.amber
      }))
    },
    {
      id: "pedirya",
      titulo: "Pedir ya",
      valor: sugerenciasPedido.length,
      color: sugerenciasPedido.length ? C.red : C.ink,
      nota: sugerenciasPedido.length > 0 ? "se agotan antes de que llegue el pedido" : null,
      tab: "pedidos",
      tabNombre: "Pedidos",
      items: sugerenciasPedido.map((p) => ({
        clave: p.id,
        principal: p.nombre,
        secundario: `${p.proveedorNombre} \xB7 pide ${fmt(p.cantidadSugerida)} ${p.unidad}${p.cajasSugeridas ? ` (${p.cajasSugeridas} caja${p.cajasSugeridas > 1 ? "s" : ""})` : ""}`,
        derecha: `${Math.round(p.dias)} d de stock \xB7 llega en ${p.diasHastaEntrega}`,
        derechaColor: C.red
      }))
    },
    {
      id: "descuadresstock",
      titulo: "Descuadres de stock",
      valor: descuadresStock.length,
      color: descuadresStock.length ? C.red : C.ink,
      nota: descuadresStock.length > 0 ? "la cach\xE9 no coincide con el hist\xF3rico de movimientos" : null,
      tab: "diagnostico",
      tabNombre: "Reconciliaci\xF3n de stock",
      items: descuadresStock.map((d) => ({
        clave: d.productoId,
        principal: d.nombre,
        secundario: `Cach\xE9 ${fmt(d.teoricoCache)} \xB7 hist\xF3rico ${fmt(d.teoricoReal)}`,
        derecha: `${d.diferencia > 0 ? "+" : ""}${fmt(d.diferencia)}`,
        derechaColor: C.red
      }))
    },
    {
      id: "personal",
      titulo: "Personal: avisos",
      valor: documentosPersonalPronto.length,
      color: documentosPersonalPronto.some((d) => d.dias < 0) ? C.red : documentosPersonalPronto.length ? C.amber : C.ink,
      nota: fichajesAbiertos.length > 0 ? `${fichajesAbiertos.length} fichaje(s) sin cerrar` : null,
      notaColor: fichajesAbiertos.length > 0 ? C.amber : null,
      tab: "personal",
      tabNombre: "Personal",
      items: documentosPersonalPronto.map((d) => ({
        clave: d.id,
        principal: d.empleado,
        secundario: `${d.concepto} \xB7 ${d.fecha}`,
        derecha: diasTexto(d.dias),
        derechaColor: diasColor(d.dias)
      }))
    },
    {
      id: "conteos",
      titulo: "Conteos atrasados",
      valor: recordatorioConteo.filter((a) => a.atrasado).length,
      color: recordatorioConteo.some((a) => a.atrasado) ? C.amber : C.ink,
      tab: "conteo",
      tabNombre: "Inventario ciego",
      items: recordatorioConteo.filter((a) => a.atrasado).map((a) => ({
        clave: a.id,
        principal: a.nombre,
        secundario: a.ultimaFecha ? `\xFAltimo conteo: ${a.ultimaFecha}` : "todav\xEDa no se ha contado nunca",
        derecha: a.diasDesde === null ? "sin datos" : `hace ${a.diasDesde} d`,
        derechaColor: C.amber
      }))
    }
  ];
  const tarjetaAbierta = tarjetas.find((t) => t.id === abierta);
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-4 pb-3", style: { borderBottom: `2px solid ${C.accent}` } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("h2", { className: "text-[18px] font-semibold" }, "Panel general"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, (/* @__PURE__ */ new Date()).toLocaleDateString("es-ES", { weekday: "long", day: "numeric", month: "long", year: "numeric" }))), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 56, width: "auto" } })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2", style: { color: C.inkSoft } }, "Toca cualquier cifra para ver el detalle."), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 md:grid-cols-4 gap-3 mb-3" }, tarjetas.map((t) => {
    const activa = abierta === t.id;
    return /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: t.id,
        onClick: () => setAbierta(activa ? null : t.id),
        className: "text-left rounded-xl p-4",
        style: {
          background: C.surface,
          border: `1px solid ${activa ? C.accent : C.line}`,
          boxShadow: activa ? `0 0 0 1px ${C.accent}` : "none"
        }
      },
      /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, t.titulo),
      /* @__PURE__ */ import_react.default.createElement("div", { className: "text-2xl font-semibold mono mt-1", style: { color: t.color || C.ink } }, t.valor),
      t.nota && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-0.5", style: { color: t.notaColor || C.inkSoft } }, t.nota)
    );
  })), tarjetaAbierta && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-6", style: { borderColor: C.accent } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold" }, tarjetaAbierta.titulo), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setAbierta(null), "aria-label": "Cerrar" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 16, color: C.inkSoft }))), tarjetaAbierta.items.length === 0 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3", style: { color: C.inkSoft } }, "No hay nada pendiente aqu\xED ahora mismo.") : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5 mb-3 max-h-72 overflow-y-auto" }, tarjetaAbierta.items.map((it) => /* @__PURE__ */ import_react.default.createElement("div", { key: it.clave, className: "flex items-start justify-between py-1.5 border-b last:border-0", style: { borderColor: C.line } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "pr-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, it.principal), it.secundario && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, it.secundario)), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11.5px] mono font-semibold whitespace-nowrap", style: { color: it.derechaColor || C.inkSoft } }, it.derecha)))), setTab && /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => {
    setTab(tarjetaAbierta.tab);
    setAbierta(null);
  } }, "Ir a ", tarjetaAbierta.tabNombre, " ", /* @__PURE__ */ import_react.default.createElement(ChevronRight, { size: 13 }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3 flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement(TriangleAlert, { size: 15, color: C.amber }), " Alertas de reposici\xF3n"), stockBajo.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No hay productos por debajo de su stock m\xEDnimo." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, stockBajo.map((p) => /* @__PURE__ */ import_react.default.createElement("div", { key: p.id, className: "flex items-center justify-between py-1.5 border-b last:border-0", style: { borderColor: C.line } }, /* @__PURE__ */ import_react.default.createElement("span", null, p.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono text-[12px]", style: { color: C.amber } }, p.stock, " / m\xEDn. ", p.stockMinimo))))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3 flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement(TriangleAlert, { size: 15, color: caducanPronto.some((l) => l.dias < 0) ? C.red : C.amber }), "Caducidades pr\xF3ximas"), caducanPronto.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Ning\xFAn lote caduca en los pr\xF3ximos 30 d\xEDas." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5 max-h-48 overflow-y-auto" }, caducanPronto.slice(0, 12).map((l) => {
    const vencido = l.dias < 0;
    const urgente = l.dias >= 0 && l.dias <= 7;
    return /* @__PURE__ */ import_react.default.createElement("div", { key: l.id, className: "flex items-center justify-between py-1 border-b last:border-0", style: { borderColor: C.line } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, l.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mono", style: { color: C.inkSoft } }, l.lote ? `lote ${l.lote} \xB7 ` : "", l.caducidad, l.unidades ? ` \xB7 ${l.parcial ? "quedan aprox. " : ""}${fmt(l.unidades)} uds` : "")), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11.5px] mono font-semibold", style: { color: vencido ? C.red : urgente ? C.amber : C.inkSoft } }, vencido ? `caducado hace ${Math.abs(l.dias)} d` : l.dias === 0 ? "caduca hoy" : `${l.dias} d\xEDas`));
  }))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, "Movimiento reciente de almac\xE9n"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-6 mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1 text-[12px]", style: { color: C.accent } }, /* @__PURE__ */ import_react.default.createElement(CircleArrowUp, { size: 14 }), " Entradas totales"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono" }, entradas30)), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1 text-[12px]", style: { color: C.red } }, /* @__PURE__ */ import_react.default.createElement(CircleArrowDown, { size: 14 }), " Salidas totales"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono" }, salidas30))), movimientos.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "A\xFAn no hay movimientos registrados." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5 max-h-40 overflow-y-auto" }, movimientos.slice(0, 8).map((m) => {
    const p = productos.find((x) => x.id === m.productoId);
    return /* @__PURE__ */ import_react.default.createElement("div", { key: m.id, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, p ? p.nombre : "\u2014", " \xB7 ", m.motivo || m.referencia), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: esEntrada(m) ? C.accent : esSalida(m) ? C.red : C.inkSoft } }, esEntrada(m) ? "+" : esSalida(m) ? "-" : "\xB1", Math.abs(cantidadConSigno(m))));
  })))));
}
var DIAS_REPARTO = [
  { valor: 1, corta: "L", nombre: "Lunes" },
  { valor: 2, corta: "M", nombre: "Martes" },
  { valor: 3, corta: "X", nombre: "Mi\xE9rcoles" },
  { valor: 4, corta: "J", nombre: "Jueves" },
  { valor: 5, corta: "V", nombre: "Viernes" },
  { valor: 6, corta: "S", nombre: "S\xE1bado" },
  { valor: 0, corta: "D", nombre: "Domingo" }
];
function Proveedores({ proveedores, addProveedor, updateProveedor, deleteProveedor, pedidos }) {
  const blankProv = { nombre: "", contacto: "", telefono: "", email: "", condiciones: "", leadTime: "", diasPago: "", diasReparto: [] };
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [form, setForm] = (0, import_react.useState)(blankProv);
  const [error, setError] = (0, import_react.useState)("");
  const [editFor, setEditFor] = (0, import_react.useState)(null);
  const [editForm, setEditForm] = (0, import_react.useState)(blankProv);
  const [editError, setEditError] = (0, import_react.useState)("");
  const [confirmDeleteId, setConfirmDeleteId] = (0, import_react.useState)(null);
  function submit() {
    if (!form.nombre.trim()) {
      setError("Escribe el nombre del proveedor.");
      return;
    }
    setError("");
    addProveedor(form);
    setForm(blankProv);
    setShowForm(false);
  }
  function openEdit(pv) {
    setEditFor(pv.id);
    setEditForm({
      nombre: pv.nombre || "",
      contacto: pv.contacto || "",
      telefono: pv.telefono || "",
      email: pv.email || "",
      condiciones: pv.condiciones || "",
      leadTime: pv.leadTime || "",
      diasPago: pv.diasPago || "",
      diasReparto: pv.diasReparto || []
    });
    setEditError("");
  }
  function submitEdit() {
    if (!editForm.nombre.trim()) {
      setEditError("Escribe el nombre del proveedor.");
      return;
    }
    updateProveedor(editFor, editForm);
    setEditFor(null);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => setShowForm((s) => !s) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nuevo proveedor") }, "Proveedores"), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre del proveedor" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.nombre, onChange: (e) => setForm({ ...form, nombre: e.target.value }), placeholder: "Distribuidora del Norte" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Persona de contacto" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.contacto, onChange: (e) => setForm({ ...form, contacto: e.target.value }), placeholder: "Nombre" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tel\xE9fono (WhatsApp)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.telefono, onChange: (e) => setForm({ ...form, telefono: e.target.value }), placeholder: "+34 600 000 000" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Correo electr\xF3nico" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "email", value: form.email, onChange: (e) => setForm({ ...form, email: e.target.value }), placeholder: "pedidos@proveedor.com" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Condiciones de pago (texto libre)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.condiciones, onChange: (e) => setForm({ ...form, condiciones: e.target.value }), placeholder: "Transferencia 45 d\xEDas F/F" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "D\xEDas de pago (para calcular vencimientos)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.diasPago, onChange: (e) => setForm({ ...form, diasPago: e.target.value }), placeholder: "45" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tiempo de entrega (d\xEDas)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.leadTime, onChange: (e) => setForm({ ...form, leadTime: e.target.value }), placeholder: "5" }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-1", style: { color: C.inkSoft } }, "\xBFQu\xE9 d\xEDas reparte? (opcional)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-1.5" }, DIAS_REPARTO.map((d) => {
    const activo = (form.diasReparto || []).includes(d.valor);
    return /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: d.valor,
        type: "button",
        onClick: () => setForm((f) => ({
          ...f,
          diasReparto: (f.diasReparto || []).includes(d.valor) ? f.diasReparto.filter((x) => x !== d.valor) : [...f.diasReparto || [], d.valor]
        })),
        className: "w-9 h-9 rounded-full text-[12.5px] font-semibold",
        style: { background: activo ? C.accent : C.surface, color: activo ? "#fff" : C.inkSoft, border: `1px solid ${activo ? C.accent : C.line}` },
        title: d.nombre
      },
      d.corta
    );
  })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: C.inkSoft } }, "Si no marcas ninguno, se asume que puede repartir cualquier d\xEDa.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-2", style: { color: C.inkSoft } }, '"D\xEDas de pago" es el plazo real hasta que toca pagar la factura (lo que ves como "45 d\xEDas F/F" o "TR 30 d\xEDas" en el papel). Es distinto del tiempo de entrega, que es cu\xE1nto tarda en llegarte el pedido.'), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mt-1" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, "Guardar proveedor"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowForm(false);
    setError("");
  } }, "Cancelar"))), proveedores.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has a\xF1adido proveedores." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-3" }, proveedores.map((pv) => {
    const nPedidos = pedidos.filter((p) => p.proveedorId === pv.id).length;
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: pv.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold" }, pv.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mt-0.5", style: { color: C.inkSoft } }, pv.contacto)), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setConfirmDeleteId(pv.id), "aria-label": "Eliminar proveedor" }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 15, color: C.inkSoft }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-3 text-[12px] space-y-1", style: { color: C.inkSoft } }, pv.email && /* @__PURE__ */ import_react.default.createElement("div", null, "\u2709 ", pv.email), pv.telefono && /* @__PURE__ */ import_react.default.createElement("div", null, "\u260E ", pv.telefono), pv.condiciones && /* @__PURE__ */ import_react.default.createElement("div", null, "Condiciones: ", pv.condiciones), pv.leadTime && /* @__PURE__ */ import_react.default.createElement("div", null, "Entrega estimada: ", pv.leadTime, " d\xEDas"), pv.diasReparto && pv.diasReparto.length > 0 && /* @__PURE__ */ import_react.default.createElement("div", null, "Reparte: ", DIAS_REPARTO.filter((d) => pv.diasReparto.includes(d.valor)).map((d) => d.nombre).join(", ")), pv.diasPago && /* @__PURE__ */ import_react.default.createElement("div", null, "Pago a ", pv.diasPago, " d\xEDas"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono" }, nPedidos, " pedido(s) registrados")), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => openEdit(pv) }, "Editar")));
  })), editFor && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setEditFor(null), title: "Editar proveedor" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre del proveedor" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.nombre, onChange: (e) => setEditForm({ ...editForm, nombre: e.target.value }), autoFocus: true })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Persona de contacto" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.contacto, onChange: (e) => setEditForm({ ...editForm, contacto: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tel\xE9fono (WhatsApp)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.telefono, onChange: (e) => setEditForm({ ...editForm, telefono: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Correo electr\xF3nico" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "email", value: editForm.email, onChange: (e) => setEditForm({ ...editForm, email: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Condiciones de pago (texto libre)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.condiciones, onChange: (e) => setEditForm({ ...editForm, condiciones: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "D\xEDas de pago (para vencimientos)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: editForm.diasPago, onChange: (e) => setEditForm({ ...editForm, diasPago: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tiempo de entrega (d\xEDas)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: editForm.leadTime, onChange: (e) => setEditForm({ ...editForm, leadTime: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement("div", { className: "mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-1", style: { color: C.inkSoft } }, "\xBFQu\xE9 d\xEDas reparte? (opcional)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-1.5" }, DIAS_REPARTO.map((d) => {
    const activo = (editForm.diasReparto || []).includes(d.valor);
    return /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: d.valor,
        type: "button",
        onClick: () => setEditForm((f) => ({
          ...f,
          diasReparto: (f.diasReparto || []).includes(d.valor) ? f.diasReparto.filter((x) => x !== d.valor) : [...f.diasReparto || [], d.valor]
        })),
        className: "w-9 h-9 rounded-full text-[12.5px] font-semibold",
        style: { background: activo ? C.accent : C.surface, color: activo ? "#fff" : C.inkSoft, border: `1px solid ${activo ? C.accent : C.line}` },
        title: d.nombre
      },
      d.corta
    );
  }))), editError && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, editError), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mt-1" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submitEdit }, "Guardar cambios"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setEditFor(null) }, "Cancelar"))), confirmDeleteId && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmDeleteId(null), title: "Eliminar proveedor" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-4" }, "Se borra la ficha del proveedor. Los pedidos y albaranes ya hechos con \xE9l no se tocan, siguen en tu historial."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: () => {
    deleteProveedor(confirmDeleteId);
    setConfirmDeleteId(null);
  } }, "S\xED, eliminar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmDeleteId(null) }, "Cancelar"))));
}
function Productos({ productos, proveedores, proveedorPorId, addProducto, updateProducto, deleteProducto, reactivarProducto, registrarSalida, clasificacionABC, almacenCongelado, movimientos = [], fichasCosto = [], updateFichaCosto, ajustarProductoPorOtro }) {
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [showAjusteCruce, setShowAjusteCruce] = (0, import_react.useState)(false);
  const [cruce, setCruce] = (0, import_react.useState)({ origenId: "", destinoId: "", cantOrigen: "", cantDestino: "", motivo: "" });
  const [errorCruce, setErrorCruce] = (0, import_react.useState)("");
  const [verEliminados, setVerEliminados] = (0, import_react.useState)(false);
  const [confirmarEliminarProducto, setConfirmarEliminarProducto] = (0, import_react.useState)(null);
  const previoCruce = (0, import_react.useMemo)(() => {
    const o = productos.find((p) => p.id === cruce.origenId);
    const d = productos.find((p) => p.id === cruce.destinoId);
    if (!o || !d) return null;
    const cs = Number(cruce.cantOrigen) || 0;
    const ce = Number(cruce.cantDestino) || 0;
    const valorSale = cs * (Number(o.costo) || 0);
    const valorEntra = ce * (Number(d.costo) || 0);
    return {
      origen: o,
      destino: d,
      valorSale,
      valorEntra,
      diferencia: valorEntra - valorSale,
      mismoCosto: (Number(o.costo) || 0) === (Number(d.costo) || 0),
      stockInsuficiente: cs > (Number(o.stock) || 0)
    };
  }, [productos, cruce]);
  function confirmarCruce() {
    if (!ajustarProductoPorOtro) return;
    const r = ajustarProductoPorOtro({
      productoOrigenId: cruce.origenId,
      productoDestinoId: cruce.destinoId,
      cantidadOrigen: cruce.cantOrigen,
      cantidadDestino: cruce.cantDestino,
      motivo: cruce.motivo.trim()
    });
    if (!r.ok) {
      setErrorCruce(r.error);
      return;
    }
    setShowAjusteCruce(false);
    setCruce({ origenId: "", destinoId: "", cantOrigen: "", cantDestino: "", motivo: "" });
    setErrorCruce("");
  }
  const blank = { codigo: "", codigoBarras: "", nombre: "", categoria: "", unidad: "unidad", tipo: "materia_prima", alergenos: [], costo: "", ivaCompra: 10, udsPorCaja: 1, nombreEmpaque: "caja", precioVenta: "", ivaVenta: 10, stock: "", stockMinimo: "", stockPisoVenta: 0, stockMinimoPisoVenta: "", zona: "", ubicacion: "", proveedorId: "", contenido: "", unidadContenido: "", departamento: "Obrador" };
  const [form, setForm] = (0, import_react.useState)(blank);
  const [salidaFor, setSalidaFor] = (0, import_react.useState)(null);
  const [salidaCant, setSalidaCant] = (0, import_react.useState)("");
  const [salidaMotivo, setSalidaMotivo] = (0, import_react.useState)("Venta");
  const [salidaMedioPago, setSalidaMedioPago] = (0, import_react.useState)("Efectivo");
  const [salidaPrecio, setSalidaPrecio] = (0, import_react.useState)("");
  const [error, setError] = (0, import_react.useState)("");
  const [editFor, setEditFor] = (0, import_react.useState)(null);
  const [editForm, setEditForm] = (0, import_react.useState)(blank);
  const [editError, setEditError] = (0, import_react.useState)("");
  const [busqueda, setBusqueda] = (0, import_react.useState)("");
  const [filtroTipo, setFiltroTipo] = (0, import_react.useState)("todos");
  const [filtroDepartamento, setFiltroDepartamento] = (0, import_react.useState)("todos");
  const [showScanner, setShowScanner] = (0, import_react.useState)(false);
  const [kardexId, setKardexId] = (0, import_react.useState)(null);
  const TIPOS_FILTRO = [
    { id: "todos", label: "Todos" },
    { id: "materia_prima", label: "Materias primas" },
    { id: "consumible", label: "Consumibles" },
    { id: "elaborado", label: "Elaborados" },
    { id: "utillaje", label: "Utillaje" },
    { id: "limpieza", label: "Limpieza" }
  ];
  const productosFiltrados = productos.filter((p) => {
    if (!verEliminados && p.activo === false) return false;
    if (filtroTipo !== "todos" && (p.tipo || "materia_prima") !== filtroTipo) return false;
    if (filtroDepartamento !== "todos" && (p.departamento || "Sin asignar") !== filtroDepartamento) return false;
    const q = busqueda.trim().toLowerCase();
    if (!q) return true;
    return [p.nombre, p.codigo, p.codigoBarras, p.categoria, p.ubicacion, p.zona].filter(Boolean).some((campo) => String(campo).toLowerCase().includes(q));
  });
  const numEliminados = (0, import_react.useMemo)(() => productos.filter((p) => p.activo === false).length, [productos]);
  function submit() {
    if (!form.nombre.trim()) {
      setError("Escribe el nombre del producto.");
      return;
    }
    setError("");
    addProducto({ ...form, costo: Number(form.costo) || 0, ivaCompra: Number(form.ivaCompra) || 0, udsPorCaja: Number(form.udsPorCaja) || 1, precioVenta: Number(form.precioVenta) || 0, ivaVenta: Number(form.ivaVenta) || 0, stockMinimo: Number(form.stockMinimo) || 0 });
    setForm(blank);
    setShowForm(false);
  }
  function openEdit(p) {
    setEditFor(p.id);
    setEditForm({
      codigo: p.codigo || "",
      codigoBarras: p.codigoBarras || "",
      nombre: p.nombre || "",
      categoria: p.categoria || "",
      tipo: p.tipo || "materia_prima",
      alergenos: p.alergenos || [],
      unidad: p.unidad || "unidad",
      costo: p.costo ?? "",
      ivaCompra: p.ivaCompra ?? 10,
      udsPorCaja: p.udsPorCaja ?? 1,
      nombreEmpaque: p.nombreEmpaque || "caja",
      precioVenta: p.precioVenta ?? "",
      ivaVenta: p.ivaVenta ?? 10,
      stock: p.stock ?? "",
      stockMinimo: p.stockMinimo ?? "",
      stockPisoVenta: p.stockPisoVenta ?? 0,
      stockMinimoPisoVenta: p.stockMinimoPisoVenta ?? "",
      zona: p.zona || "",
      ubicacion: p.ubicacion || "",
      proveedorId: p.proveedorId || "",
      departamento: p.departamento || "Obrador",
      contenido: p.contenido ?? "",
      unidadContenido: p.unidadContenido || ""
    });
    setEditError("");
  }
  const [avisoContenido, setAvisoContenido] = (0, import_react.useState)(null);
  function fichasQueUsan(productoId) {
    return (fichasCosto || []).filter((f) => (f.componentes || []).some((c) => c.productoId === productoId));
  }
  function submitEdit() {
    if (!editForm.nombre.trim()) {
      setEditError("Escribe el nombre del producto.");
      return;
    }
    const anterior = productos.find((p) => p.id === editFor);
    const contenidoAntes = Number(anterior?.contenido) || 0;
    const contenidoAhora = Number(editForm.contenido) || 0;
    const cambioContenido = contenidoAntes > 0 && contenidoAhora > 0 && contenidoAntes !== contenidoAhora;
    const afectadas = cambioContenido ? fichasQueUsan(editFor) : [];
    updateProducto(editFor, {
      ...editForm,
      costo: Number(editForm.costo) || 0,
      ivaCompra: Number(editForm.ivaCompra) || 0,
      udsPorCaja: Number(editForm.udsPorCaja) || 1,
      precioVenta: Number(editForm.precioVenta) || 0,
      ivaVenta: Number(editForm.ivaVenta) || 0,
      stock: Number(editForm.stock) || 0,
      stockMinimo: Number(editForm.stockMinimo) || 0
    });
    setEditFor(null);
    if (afectadas.length > 0) {
      setAvisoContenido({
        productoId: editFor,
        nombre: editForm.nombre,
        contenidoAntes,
        contenidoAhora,
        unidadContenido: editForm.unidadContenido || "",
        fichas: afectadas
      });
    }
  }
  function convertirFichasAfectadas() {
    if (!avisoContenido || !updateFichaCosto) return;
    const factor = avisoContenido.contenidoAntes / avisoContenido.contenidoAhora;
    avisoContenido.fichas.forEach((f) => {
      updateFichaCosto(f.id, {
        componentes: (f.componentes || []).map(
          (c) => c.productoId === avisoContenido.productoId ? { ...c, cantidad: (Number(c.cantidad) || 0) * factor } : c
        )
      });
    });
    setAvisoContenido(null);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowAjusteCruce(true);
    setErrorCruce("");
  } }, /* @__PURE__ */ import_react.default.createElement(ArrowLeftRight, { size: 15 }), " Ajuste entre productos"), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => setShowForm((s) => !s) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nuevo producto")) }, "Productos"), almacenCongelado && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start gap-2 text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement(TriangleAlert, { size: 16, color: C.amber, style: { marginTop: 2, flexShrink: 0 } }), /* @__PURE__ */ import_react.default.createElement("span", null, /* @__PURE__ */ import_react.default.createElement("b", null, "Almac\xE9n congelado por conteo en curso."), " Las entradas y salidas est\xE1n bloqueadas hasta que finalices el conteo en Inventario ciego."))), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "1 \xB7 Identificaci\xF3n" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.nombre, onChange: (e) => setForm({ ...form, nombre: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "C\xF3digo / SKU (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.codigo, onChange: (e) => setForm({ ...form, codigo: e.target.value }), placeholder: "Interno o de proveedor" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Categor\xEDa" }, /* @__PURE__ */ import_react.default.createElement(CampoCategoria, { value: form.categoria, onChange: (v) => setForm({ ...form, categoria: v }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Departamento" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.departamento, onChange: (e) => setForm({ ...form, departamento: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, DEPARTAMENTOS.map((d) => /* @__PURE__ */ import_react.default.createElement("option", { key: d, value: d }, d)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Unidad de medida (c\xF3mo lo cuentas)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.unidad, onChange: (e) => setForm({ ...form, unidad: e.target.value }), placeholder: "unidad, kg, botella\u2026" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tipo de producto" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: form.tipo || "materia_prima",
      onChange: (e) => setForm({ ...form, tipo: e.target.value }),
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "materia_prima" }, "Materia prima (se transforma)"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "consumible" }, "Consumible / desechable (vaso, tapa, servilleta)"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "utillaje" }, "Utillaje / menaje (jarra, bandeja: no se consume)"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "limpieza" }, "Limpieza / qu\xEDmicos (no es mercanc\xEDa, no entra en recetas)"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "elaborado" }, "Elaborado (lo que vendes al cliente)")
  ))), esNoMercancia(form) && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: C.inkSoft } }, "No cuenta en el valor de mercanc\xEDa ni en el ABC, y no aparece como ingrediente en las fichas de costo. Su stock s\xED se sigue controlando, para poder reponerlo. Sigue teniendo stock y aviso de reposici\xF3n, para cuando se rompa algo.")), !esNoMercancia(form) && /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "2 \xB7 Al\xE9rgenos" }, /* @__PURE__ */ import_react.default.createElement(
    SelectorAlergenos,
    {
      seleccionados: form.alergenos || [],
      onCambio: (lista) => setForm({ ...form, alergenos: lista }),
      nombreParaSugerir: form.nombre
    }
  )), /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "3 \xB7 Compra" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "IVA de compra de ESTE producto (%)" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.ivaCompra, onChange: (e) => setForm({ ...form, ivaCompra: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, TIPOS_IVA.map((t) => /* @__PURE__ */ import_react.default.createElement("option", { key: t, value: t }, t, "%", t === 4 ? " \u2014 pan, harina, leche, huevos" : t === 10 ? " \u2014 resto de alimentos" : t === 21 ? " \u2014 general, limpieza" : "")))), /* @__PURE__ */ import_react.default.createElement(CompraPorCaja, { form, setForm })), /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "4 \xB7 Venta" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "IVA de venta de ESTE producto (%)" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.ivaVenta, onChange: (e) => setForm({ ...form, ivaVenta: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, TIPOS_IVA.map((t) => /* @__PURE__ */ import_react.default.createElement("option", { key: t, value: t }, t, "%")))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio de venta CON IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.precioVenta, onChange: (e) => setForm({ ...form, precioVenta: e.target.value }) }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, "D\xE9jalo vac\xEDo si es una materia prima que no vendes suelta.")), /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "5 \xB7 Almac\xE9n" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Stock inicial" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.stock, onChange: (e) => setForm({ ...form, stock: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Stock m\xEDnimo (reorden)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.stockMinimo, onChange: (e) => setForm({ ...form, stockMinimo: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Zona del almac\xE9n" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.zona, onChange: (e) => setForm({ ...form, zona: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Sin asignar"), ZONAS_ALMACEN.filter((z) => z !== "Sin asignar").map((z) => /* @__PURE__ */ import_react.default.createElement("option", { key: z, value: z }, z)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Ubicaci\xF3n exacta (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.ubicacion, onChange: (e) => setForm({ ...form, ubicacion: e.target.value }), placeholder: "Estante 3, balda 2\u2026" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "C\xF3digo de barras (EAN, opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.codigoBarras, onChange: (e) => setForm({ ...form, codigoBarras: e.target.value }), placeholder: "Escanea o escribe el EAN" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Proveedor principal (opcional)" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.proveedorId, onChange: (e) => setForm({ ...form, proveedorId: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Sin asignar"), proveedores.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre)))), (form.tipo === "elaborado" || Number(form.precioVenta) > 0) && /* @__PURE__ */ import_react.default.createElement(Field, { label: "M\xEDnimo en piso de venta (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.stockMinimoPisoVenta, onChange: (e) => setForm({ ...form, stockMinimoPisoVenta: e.target.value }), placeholder: "Avisa cuando quede poco en el mostrador" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-1", style: { color: C.inkSoft } }, "Si compras esto por bulto (bandeja, caja\u2026) pero lo usas por peso en las recetas, di aqu\xED cu\xE1nto trae cada bulto. As\xED en Fichas de costo podr\xE1s poner la cantidad directamente en gramos o kilos."), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Contenido de cada bulto (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.contenido, onChange: (e) => setForm({ ...form, contenido: e.target.value }), placeholder: "Ej. 300" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Unidad del contenido" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.unidadContenido, onChange: (e) => setForm({ ...form, unidadContenido: e.target.value }), placeholder: "Ej. g, kg, ml" }))))), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mt-1" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, "Guardar producto"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowForm(false);
    setError("");
  } }, "Cancelar"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "relative flex-1" }, /* @__PURE__ */ import_react.default.createElement(Search, { size: 15, color: C.inkSoft, style: { position: "absolute", left: 10, top: 10 } }), /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      value: busqueda,
      onChange: (e) => setBusqueda(e.target.value),
      placeholder: "Buscar por nombre, c\xF3digo, EAN, ubicaci\xF3n\u2026",
      style: { paddingLeft: 32 }
    }
  )), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowScanner(true) }, /* @__PURE__ */ import_react.default.createElement(ScanBarcode, { size: 16 }), " Escanear")), showScanner && /* @__PURE__ */ import_react.default.createElement(
    EscanerCodigoBarras,
    {
      onDetectado: (codigo) => {
        setBusqueda(codigo);
        setShowScanner(false);
      },
      onClose: () => setShowScanner(false)
    }
  ), productos.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has a\xF1adido productos." }) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-2 overflow-x-auto" }, TIPOS_FILTRO.map((t) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: t.id,
      onClick: () => setFiltroTipo(t.id),
      className: "text-[12px] font-medium px-3 py-1.5 rounded-full whitespace-nowrap",
      style: {
        background: filtroTipo === t.id ? C.chrome : C.surface,
        color: filtroTipo === t.id ? "#fff" : C.inkSoft,
        border: `1px solid ${filtroTipo === t.id ? C.chrome : C.line}`
      }
    },
    t.label
  ))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-3 overflow-x-auto" }, ["todos", ...DEPARTAMENTOS].map((d) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: d,
      onClick: () => setFiltroDepartamento(d),
      className: "text-[11.5px] font-medium px-3 py-1 rounded-full whitespace-nowrap",
      style: {
        background: filtroDepartamento === d ? C.accent : C.surface,
        color: filtroDepartamento === d ? "#fff" : C.inkSoft,
        border: `1px solid ${filtroDepartamento === d ? C.accent : C.line}`
      }
    },
    d === "todos" ? "Todos los departamentos" : d
  ))), numEliminados > 0 && /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      onClick: () => setVerEliminados((v) => !v),
      className: "text-[11.5px] font-medium mb-3 flex items-center gap-1",
      style: { color: C.inkSoft }
    },
    verEliminados ? "Ocultar" : "Ver",
    " ",
    numEliminados,
    " producto",
    numEliminados !== 1 ? "s" : "",
    " eliminado",
    numEliminados !== 1 ? "s" : ""
  ), productosFiltrados.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: `Ning\xFAn producto coincide con "${busqueda}".` }) : /* @__PURE__ */ import_react.default.createElement(Card, { style: { padding: 0 } }, /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", { style: { color: C.inkSoft, borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2.5 px-3" }, "Producto"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2.5 px-3" }, "Stock"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2.5 px-3" }, "Costo s/IVA"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2.5 px-3" }, "PVP c/IVA"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2.5 px-3" }, "Margen"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-center font-medium py-2.5 px-3" }, "ABC"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2.5 px-3" }))), /* @__PURE__ */ import_react.default.createElement("tbody", null, productosFiltrados.map((p) => {
    const margen = margenDe(p);
    const bajo = p.stock <= Number(p.stockMinimo || 0);
    return /* @__PURE__ */ import_react.default.createElement("tr", { key: p.id, style: { borderBottom: `1px solid ${C.line}`, opacity: p.activo === false ? 0.55 : 1 } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2.5 px-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-medium flex items-center gap-1.5" }, p.nombre, p.activo === false && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.red }, "Eliminado"), esNoMercancia(p) && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.inkSoft }, p.tipo), p.tipo === "consumible" && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.accent }, "consumible"), p.tipo === "elaborado" && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.amber }, "elaborado")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, [p.codigo, p.categoria, p.ubicacion].filter(Boolean).join(" \xB7 "), p.proveedorId && proveedorPorId(p.proveedorId) ? ` \xB7 ${proveedorPorId(p.proveedorId).nombre}` : "")), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2.5 px-3 mono" }, p.stock, " ", p.unidad, bajo && /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.amber }, "bajo m\xEDnimo")), (p.tipo === "elaborado" || Number(p.precioVenta) > 0) && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-0.5", style: { color: C.inkSoft } }, fmt(p.stockPisoVenta || 0), " en piso \xB7 ", fmt((Number(p.stock) || 0) - (Number(p.stockPisoVenta) || 0)), " en almac\xE9n")), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2.5 px-3 mono" }, "\u20AC", fmt(p.costo), Number(p.costo) > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "c/IVA \u20AC", fmt(costoConIva(p)), " \xB7 ", ivaCompraDe(p), "%", udsPorCaja(p) > 1 && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, " \xB7 caja ", udsPorCaja(p), " = \u20AC", fmt(costoCaja(p))))), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2.5 px-3 mono" }, "\u20AC", fmt(p.precioVenta), Number(p.precioVenta) > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "neto \u20AC", fmt(precioNeto(p)), " \xB7 IVA ", ivaDe(p), "%")), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2.5 px-3 mono", style: { color: margen == null ? C.inkSoft : margen < 15 ? C.red : C.accent } }, margen == null ? "\u2014" : `${fmt(margen)}%`), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2.5 px-3 text-center" }, esNoMercancia(p) ? /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.inkSoft }, "\u2014") : /* @__PURE__ */ import_react.default.createElement(Pill, { color: (clasificacionABC[p.id] || "C") === "A" ? C.red : (clasificacionABC[p.id] || "C") === "B" ? C.amber : C.inkSoft }, clasificacionABC[p.id] || "C")), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2.5 px-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 justify-end" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setKardexId(p.id) }, "Kardex"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => openEdit(p) }, "Editar"), !almacenCongelado && /* @__PURE__ */ import_react.default.createElement(
      Btn,
      {
        small: true,
        variant: "ghost",
        onClick: () => {
          setSalidaFor(p.id);
          setSalidaMotivo("Venta");
          setSalidaPrecio(Number(p.precioVenta) > 0 ? String(precioNeto(p).toFixed(2)) : "");
        }
      },
      "Registrar salida"
    ), p.activo === false ? /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => reactivarProducto(p.id) }, "Reactivar") : /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setConfirmarEliminarProducto(p), "aria-label": "Eliminar producto" }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 14, color: C.inkSoft })))));
  }))))), salidaFor && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => {
    setSalidaFor(null);
    setSalidaCant("");
  }, title: "Registrar salida de almac\xE9n" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Motivo" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: salidaMotivo,
      onChange: (e) => setSalidaMotivo(e.target.value),
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Venta" }, "Venta"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Merma" }, "Merma / caducidad"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Autoconsumo" }, "Autoconsumo"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Rotura" }, "Rotura / rechazo"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Producci\xF3n" }, "Merma de producci\xF3n"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Caducidad" }, "Caducidad"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Deterioro" }, "Deterioro"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Robo o p\xE9rdida" }, "Robo o p\xE9rdida"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Error de registro" }, "Error de registro"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Devoluci\xF3n no registrada" }, "Devoluci\xF3n no registrada"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Consumo interno" }, "Consumo interno"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Degustaci\xF3n" }, "Degustaci\xF3n"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Producto defectuoso" }, "Producto defectuoso"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "Otro" }, "Otro")
  )), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Cantidad a retirar" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: salidaCant, onChange: (e) => setSalidaCant(e.target.value), autoFocus: true })), salidaMotivo === "Venta" && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio de venta unitario, sin IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: salidaPrecio, onChange: (e) => setSalidaPrecio(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Medio de pago" }, /* @__PURE__ */ import_react.default.createElement("select", { value: salidaMedioPago, onChange: (e) => setSalidaMedioPago(e.target.value), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("option", null, "Efectivo"), /* @__PURE__ */ import_react.default.createElement("option", null, "Tarjeta"), /* @__PURE__ */ import_react.default.createElement("option", null, "Transferencia"), /* @__PURE__ */ import_react.default.createElement("option", null, "Otro")))), salidaMotivo !== "Venta" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, "Esto se contar\xE1 como p\xE9rdida al coste, no como venta, cuando mires Resultados."), /* @__PURE__ */ import_react.default.createElement(
    Btn,
    {
      onClick: () => {
        registrarSalida(salidaFor, salidaCant, { motivo: salidaMotivo, precioVentaUnitario: salidaMotivo === "Venta" ? salidaPrecio : 0, medioPago: salidaMedioPago });
        setSalidaFor(null);
        setSalidaCant("");
      }
    },
    "Confirmar salida"
  )), editFor && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setEditFor(null), title: "Editar producto", ancho: "max-w-2xl" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.nombre, onChange: (e) => setEditForm({ ...editForm, nombre: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "C\xF3digo / SKU" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.codigo, onChange: (e) => setEditForm({ ...editForm, codigo: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Categor\xEDa" }, /* @__PURE__ */ import_react.default.createElement(CampoCategoria, { value: editForm.categoria, onChange: (v) => setEditForm({ ...editForm, categoria: v }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Departamento" }, /* @__PURE__ */ import_react.default.createElement("select", { value: editForm.departamento, onChange: (e) => setEditForm({ ...editForm, departamento: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, DEPARTAMENTOS.map((d) => /* @__PURE__ */ import_react.default.createElement("option", { key: d, value: d }, d)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Unidad de medida" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.unidad, onChange: (e) => setEditForm({ ...editForm, unidad: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tipo de producto" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: editForm.tipo || "materia_prima",
      onChange: (e) => setEditForm({ ...editForm, tipo: e.target.value }),
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "materia_prima" }, "Materia prima (se transforma)"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "consumible" }, "Consumible / desechable"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "utillaje" }, "Utillaje / menaje (no se consume)"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "limpieza" }, "Limpieza / qu\xEDmicos (no es mercanc\xEDa)"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "elaborado" }, "Elaborado (venta al cliente)")
  )), !esNoMercancia(editForm) && /* @__PURE__ */ import_react.default.createElement("div", { className: "col-span-2 mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-1", style: { color: C.inkSoft } }, "Al\xE9rgenos"), /* @__PURE__ */ import_react.default.createElement(
    SelectorAlergenos,
    {
      seleccionados: editForm.alergenos || [],
      onCambio: (lista) => setEditForm({ ...editForm, alergenos: lista }),
      nombreParaSugerir: editForm.nombre
    }
  )), /* @__PURE__ */ import_react.default.createElement(Field, { label: "IVA de compra de ESTE producto (%)" }, /* @__PURE__ */ import_react.default.createElement("select", { value: editForm.ivaCompra, onChange: (e) => setEditForm({ ...editForm, ivaCompra: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, TIPOS_IVA.map((t) => /* @__PURE__ */ import_react.default.createElement("option", { key: t, value: t }, t, "%", t === 4 ? " \u2014 pan, harina, leche, huevos" : t === 10 ? " \u2014 resto de alimentos" : t === 21 ? " \u2014 general, limpieza" : "")))), /* @__PURE__ */ import_react.default.createElement("div", { className: "col-span-2" }, /* @__PURE__ */ import_react.default.createElement(CompraPorCaja, { form: editForm, setForm: setEditForm })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "IVA de venta de ESTE producto (%)" }, /* @__PURE__ */ import_react.default.createElement("select", { value: editForm.ivaVenta, onChange: (e) => setEditForm({ ...editForm, ivaVenta: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, TIPOS_IVA.map((t) => /* @__PURE__ */ import_react.default.createElement("option", { key: t, value: t }, t, "%")))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio de venta CON IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: editForm.precioVenta, onChange: (e) => setEditForm({ ...editForm, precioVenta: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Stock actual" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: editForm.stock, onChange: (e) => setEditForm({ ...editForm, stock: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Stock m\xEDnimo" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: editForm.stockMinimo, onChange: (e) => setEditForm({ ...editForm, stockMinimo: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Zona del almac\xE9n" }, /* @__PURE__ */ import_react.default.createElement("select", { value: editForm.zona, onChange: (e) => setEditForm({ ...editForm, zona: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Sin asignar"), ZONAS_ALMACEN.filter((z) => z !== "Sin asignar").map((z) => /* @__PURE__ */ import_react.default.createElement("option", { key: z, value: z }, z)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Ubicaci\xF3n" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.ubicacion, onChange: (e) => setEditForm({ ...editForm, ubicacion: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "C\xF3digo de barras (EAN)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.codigoBarras, onChange: (e) => setEditForm({ ...editForm, codigoBarras: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Proveedor principal" }, /* @__PURE__ */ import_react.default.createElement("select", { value: editForm.proveedorId, onChange: (e) => setEditForm({ ...editForm, proveedorId: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Sin asignar"), proveedores.map((pv) => /* @__PURE__ */ import_react.default.createElement("option", { key: pv.id, value: pv.id }, pv.nombre)))), (editForm.tipo === "elaborado" || Number(editForm.precioVenta) > 0) && /* @__PURE__ */ import_react.default.createElement(Field, { label: "M\xEDnimo en piso de venta" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: editForm.stockMinimoPisoVenta, onChange: (e) => setEditForm({ ...editForm, stockMinimoPisoVenta: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Contenido de cada bulto" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: editForm.contenido, onChange: (e) => setEditForm({ ...editForm, contenido: e.target.value }), placeholder: "Ej. 300" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Unidad del contenido" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: editForm.unidadContenido, onChange: (e) => setEditForm({ ...editForm, unidadContenido: e.target.value }), placeholder: "Ej. g, kg, ml" })))), editError && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, editError), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mt-1" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submitEdit }, "Guardar cambios"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setEditFor(null) }, "Cancelar"))), kardexId && (() => {
    const p = productos.find((x) => x.id === kardexId);
    if (!p) return null;
    const suyos = [...movimientos.filter((m) => m.productoId === kardexId)].sort((a, b) => (a.fecha + (a.id || "")).localeCompare(b.fecha + (b.id || "")));
    let saldo = 0;
    const filas = suyos.map((m) => {
      const delta = cantidadConSigno(m);
      saldo += delta;
      return { ...m, delta, saldo };
    });
    return /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setKardexId(null), title: `Kardex \u2014 ${p.nombre}`, ancho: "max-w-2xl" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.inkSoft } }, "Stock actual: ", /* @__PURE__ */ import_react.default.createElement("b", { className: "mono" }, p.stock, " ", p.unidad), " \xB7 Hist\xF3rico de ", filas.length, " movimiento(s)"), filas.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin movimientos registrados todav\xEDa." }) : /* @__PURE__ */ import_react.default.createElement("div", { style: { maxHeight: 400, overflowY: "auto" } }, /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", { style: { color: C.inkSoft, borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-1.5 px-2" }, "Fecha"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-1.5 px-2" }, "Motivo"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-right font-medium py-1.5 px-2" }, "Movimiento"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-right font-medium py-1.5 px-2" }, "Saldo"))), /* @__PURE__ */ import_react.default.createElement("tbody", null, [...filas].reverse().map((m, i) => /* @__PURE__ */ import_react.default.createElement("tr", { key: m.id, style: { background: i % 2 ? C.bg : C.surface } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2 mono", style: { border: `1px solid ${C.line}` } }, m.fecha), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2", style: { border: `1px solid ${C.line}` } }, m.motivo || m.referencia || m.tipo), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2 text-right mono", style: { border: `1px solid ${C.line}`, color: m.delta >= 0 ? C.accent : C.red } }, m.delta >= 0 ? "+" : "", fmt(m.delta)), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2 text-right mono font-semibold", style: { border: `1px solid ${C.line}` } }, fmt(m.saldo))))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setKardexId(null) }, "Cerrar")));
  })(), showAjusteCruce && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowAjusteCruce(false), title: "Ajuste entre productos", ancho: "max-w-2xl" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.inkSoft } }, "Para cuando en el conteo sobra de uno y falta de otro porque se confundieron entre s\xED. Se descuenta de uno y se suma al otro en la misma operaci\xF3n."), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Sale de este producto" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: cruce.origenId,
      onChange: (e) => setCruce({ ...cruce, origenId: e.target.value }),
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "\u2014 Elige \u2014"),
    productos.slice().sort((a, b) => a.nombre.localeCompare(b.nombre, "es")).map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre, " (quedan ", fmt(p.stock), ")"))
  )), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Cantidad que sale" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: cruce.cantOrigen, onChange: (e) => setCruce({ ...cruce, cantOrigen: e.target.value }) }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Entra en este producto" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: cruce.destinoId,
      onChange: (e) => setCruce({ ...cruce, destinoId: e.target.value }),
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "\u2014 Elige \u2014"),
    productos.slice().sort((a, b) => a.nombre.localeCompare(b.nombre, "es")).map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre, " (quedan ", fmt(p.stock), ")"))
  )), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Cantidad que entra" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: cruce.cantDestino, onChange: (e) => setCruce({ ...cruce, cantDestino: e.target.value }) }))), previoCruce && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.bg, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-1" }, "Sale: ", /* @__PURE__ */ import_react.default.createElement("b", { className: "mono" }, "\u20AC", fmt(previoCruce.valorSale)), " de ", previoCruce.origen.nombre, /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, " (\u20AC", fmt(previoCruce.origen.costo), "/ud)")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2" }, "Entra: ", /* @__PURE__ */ import_react.default.createElement("b", { className: "mono" }, "\u20AC", fmt(previoCruce.valorEntra)), " de ", previoCruce.destino.nombre, /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, " (\u20AC", fmt(previoCruce.destino.costo), "/ud)")), previoCruce.mismoCosto && Math.abs(previoCruce.diferencia) < 0.01 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]", style: { color: C.accent } }, "\u2713 Mismo coste: el ajuste no genera ninguna diferencia de dinero.") : /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]", style: { color: previoCruce.diferencia < 0 ? C.red : C.accent } }, previoCruce.diferencia < 0 ? `Diferencia: se pierden \u20AC${fmt(Math.abs(previoCruce.diferencia))} \u2014 ir\xE1 a gastos generales.` : `Diferencia: se ganan \u20AC${fmt(previoCruce.diferencia)} \u2014 restar\xE1 de gastos generales.`), previoCruce.stockInsuficiente && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mt-2", style: { color: C.red } }, "\u26A0 No hay tanto stock de ", previoCruce.origen.nombre, ": quedan ", fmt(previoCruce.origen.stock), ".")), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Motivo (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: cruce.motivo, onChange: (e) => setCruce({ ...cruce, motivo: e.target.value }), placeholder: "Se confundieron en el conteo\u2026" })), errorCruce && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, errorCruce), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: confirmarCruce, disabled: !previoCruce || previoCruce.stockInsuficiente }, "Hacer el ajuste"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowAjusteCruce(false) }, "Cancelar"))), avisoContenido && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setAvisoContenido(null), title: "Ojo: has cambiado el contenido del envase" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3" }, /* @__PURE__ */ import_react.default.createElement("b", null, avisoContenido.nombre), " pas\xF3 de ", fmt(avisoContenido.contenidoAntes), " a", " ", fmt(avisoContenido.contenidoAhora), " ", avisoContenido.unidadContenido, " por envase."), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, "Hay ", avisoContenido.fichas.length, " ficha(s) de costo que usan este producto. Sus cantidades est\xE1n guardadas por envase, as\xED que ahora significan una cantidad real distinta \u2014 y el coste no cambia, as\xED que no se notar\xEDa en ning\xFAn informe.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1 mb-3" }, avisoContenido.fichas.map((f) => /* @__PURE__ */ import_react.default.createElement("div", { key: f.id, className: "text-[12px]", style: { color: C.inkSoft } }, "\xB7 ", f.nombre))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, "Si el envase cambi\xF3 de formato de verdad, convierte para que las recetas sigan usando la misma cantidad real de siempre. Si solo estabas corrigiendo un dato mal metido, deja las fichas como est\xE1n."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: convertirFichasAfectadas }, "Convertir las fichas"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setAvisoContenido(null) }, "Dejarlas como est\xE1n"))), confirmarEliminarProducto && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmarEliminarProducto(null), title: "Eliminar producto" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] mb-3" }, "Vas a eliminar ", /* @__PURE__ */ import_react.default.createElement("b", null, confirmarEliminarProducto.nombre), "."), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-4 p-3 rounded-lg", style: { background: C.surfaceSoft, color: C.inkSoft } }, movimientos.some((m) => m.productoId === confirmarEliminarProducto.id) ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, 'Este producto tiene movimientos en su historial (compras, ventas, ajustes...) \u2014 no se pierden: el producto deja de aparecer en las pantallas normales, pero se conserva para que ese hist\xF3rico siga mostrando su nombre correctamente. Puedes reactivarlo cuando quieras desde "Ver eliminados".') : 'Este producto no tiene ning\xFAn movimiento registrado todav\xEDa. Se puede reactivar en cualquier momento desde "Ver eliminados".'), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmarEliminarProducto(null) }, "Cancelar"), /* @__PURE__ */ import_react.default.createElement(
    Btn,
    {
      onClick: () => {
        deleteProducto(confirmarEliminarProducto.id);
        setConfirmarEliminarProducto(null);
      }
    },
    "Confirmar eliminaci\xF3n"
  ))));
}
function InputCalc({ valor, onEscribir, decimales = 2, ...props }) {
  const [borrador, setBorrador] = (0, import_react.useState)(null);
  const formateado = valor === "" || valor === null || valor === void 0 || isNaN(Number(valor)) ? "" : Number(valor).toFixed(decimales);
  const mostrado = borrador !== null ? borrador : formateado;
  return /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      ...props,
      type: "number",
      value: mostrado,
      onChange: (e) => {
        setBorrador(e.target.value);
        onEscribir(e.target.value);
      },
      onBlur: () => setBorrador(null)
    }
  );
}
function Bloque({ titulo, children }) {
  return /* @__PURE__ */ import_react.default.createElement("div", { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] font-semibold uppercase tracking-wider mb-2 pb-1", style: { color: C.accent, borderBottom: `1px solid ${C.line}` } }, titulo), children);
}
function CompraPorCaja({ form, setForm }) {
  const iva = Number(form.ivaCompra) || 0;
  const uds = Number(form.udsPorCaja) || 1;
  const costoUd = Number(form.costo) || 0;
  const desdeCaja = (valor, conIva) => {
    if (valor === "") {
      setForm({ ...form, costo: "" });
      return;
    }
    let neto = Number(valor);
    if (isNaN(neto)) return;
    if (conIva) neto = neto / (1 + iva / 100);
    setForm({ ...form, costo: String(uds > 0 ? neto / uds : neto) });
  };
  const desdeUnidadBruta = (valor) => {
    if (valor === "") {
      setForm({ ...form, costo: "" });
      return;
    }
    const n = Number(valor);
    if (isNaN(n)) return;
    setForm({ ...form, costo: String(n / (1 + iva / 100)) });
  };
  const cajaNeta = costoUd > 0 ? costoUd * uds : "";
  const cajaBruta = costoUd > 0 ? costoUd * uds * (1 + iva / 100) : "";
  const unidadBruta = costoUd > 0 ? costoUd * (1 + iva / 100) : "";
  return /* @__PURE__ */ import_react.default.createElement("div", { className: "rounded-lg p-3 mb-3", style: { background: C.bg, border: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-1" }, "C\xF3mo te lo vende el proveedor"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3", style: { color: C.inkSoft } }, "Escribe en la casilla que traiga tu factura. Las dem\xE1s se calculan solas."), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Unidades por caja (1 si lo compras suelto)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "1", value: form.udsPorCaja, onChange: (e) => setForm({ ...form, udsPorCaja: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio CAJA sin IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(InputCalc, { step: "0.01", valor: cajaNeta, decimales: 2, onEscribir: (v) => desdeCaja(v, false) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio CAJA con IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(InputCalc, { step: "0.01", valor: cajaBruta, decimales: 2, onEscribir: (v) => desdeCaja(v, true) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Costo UNIDAD sin IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(InputCalc, { step: "0.0001", valor: form.costo, decimales: 4, onEscribir: (v) => setForm({ ...form, costo: v }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Costo UNIDAD con IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(InputCalc, { step: "0.0001", valor: unidadBruta, decimales: 4, onEscribir: desdeUnidadBruta }))), uds > 1 && costoUd > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mono", style: { color: C.accent } }, "Caja de ", uds, ": \u20AC", fmt(costoUd * uds), " sin IVA \xB7 \u20AC", fmt(costoUd * uds * (1 + iva / 100)), " con IVA \u2192 unidad \u20AC", fmt(costoUd)));
}
function Modal({ children, onClose, title, ancho = "max-w-md" }) {
  const cajaRef = (0, import_react.useRef)(null);
  (0, import_react.useEffect)(() => {
    if (cajaRef.current) cajaRef.current.focus();
  }, []);
  const onCloseRef = (0, import_react.useRef)(onClose);
  onCloseRef.current = onClose;
  (0, import_react.useEffect)(() => {
    function alTecla(e) {
      if (e.key === "Escape") onCloseRef.current();
    }
    document.addEventListener("keydown", alTecla);
    return () => document.removeEventListener("keydown", alTecla);
  }, []);
  return /* @__PURE__ */ import_react.default.createElement("div", { className: "fixed inset-0 z-50 flex items-start justify-center p-4 overflow-y-auto", style: { background: "rgba(20,32,28,0.45)" } }, /* @__PURE__ */ import_react.default.createElement(
    "div",
    {
      ref: cajaRef,
      tabIndex: -1,
      role: "dialog",
      "aria-modal": "true",
      "aria-label": title,
      className: `rounded-xl p-5 w-full ${ancho} my-4`,
      style: { background: C.surface, outline: "none" }
    },
    /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold text-[15px]" }, title), /* @__PURE__ */ import_react.default.createElement("button", { onClick: onClose, "aria-label": "Cerrar" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 17 }))),
    children
  ));
}
function Pedidos({ pedidos, proveedores, productos, crearPedido, actualizarPedido, eliminarPedido, proveedorPorId, productoPorId, sugerenciasPedido = [], addProducto }) {
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [proveedorId, setProveedorId] = (0, import_react.useState)("");
  const [verTodosProductos, setVerTodosProductos] = (0, import_react.useState)(false);
  const [fechaEsperada, setFechaEsperada] = (0, import_react.useState)("");
  const [items, setItems] = (0, import_react.useState)([]);
  const [enviarPedido, setEnviarPedido] = (0, import_react.useState)(null);
  const [editingId, setEditingId] = (0, import_react.useState)(null);
  const [confirmDeleteId, setConfirmDeleteId] = (0, import_react.useState)(null);
  const [creandoNuevoIdx, setCreandoNuevoIdx] = (0, import_react.useState)(null);
  const [nuevoProd, setNuevoProd] = (0, import_react.useState)({ nombre: "", unidad: "unidad", costo: "" });
  function addItemRow() {
    if (!productos.length) return;
    setItems((s) => [...s, { productoId: productos[0].id, cantidad: 1, costoUnitario: productos[0].costo }]);
  }
  function updateItem(idx, field, value) {
    setItems(
      (s) => s.map((it, i) => {
        if (i !== idx) return it;
        if (field === "productoId") {
          if (value === "__nuevo__") {
            setNuevoProd({ nombre: "", unidad: "unidad", costo: "" });
            setCreandoNuevoIdx(idx);
            return it;
          }
          const p = productos.find((pr) => pr.id === value);
          return { ...it, productoId: value, costoUnitario: p ? p.costo : it.costoUnitario };
        }
        return { ...it, [field]: value };
      })
    );
  }
  function guardarProductoNuevo() {
    if (!nuevoProd.nombre.trim()) return;
    const creado = addProducto({
      nombre: nuevoProd.nombre.trim(),
      unidad: nuevoProd.unidad || "unidad",
      tipo: "materia_prima",
      costo: Number(nuevoProd.costo) || 0,
      ivaCompra: 10,
      proveedorId: proveedorId || "",
      stock: 0,
      stockMinimo: 0
    });
    setItems((s) => s.map((it, i) => i === creandoNuevoIdx ? { ...it, productoId: creado.id, costoUnitario: creado.costo } : it));
    setCreandoNuevoIdx(null);
  }
  function removeItem(idx) {
    setItems((s) => s.filter((_, i) => i !== idx));
  }
  const [error, setError] = (0, import_react.useState)("");
  function resetForm() {
    setItems([]);
    setProveedorId("");
    setFechaEsperada("");
    setEditingId(null);
    setError("");
    setVerTodosProductos(false);
  }
  function openEdit(pedido) {
    setEditingId(pedido.id);
    setProveedorId(pedido.proveedorId);
    setFechaEsperada(pedido.fechaEsperada || "");
    setItems(pedido.items.map((it) => ({ productoId: it.productoId, cantidad: it.cantidad, costoUnitario: it.costoUnitario })));
    setError("");
    setVerTodosProductos(false);
    setShowForm(true);
  }
  function prepararPedidoDesdeSugerencias(proveedorId2, sugerenciasDelProveedor) {
    setEditingId(null);
    setProveedorId(proveedorId2);
    setFechaEsperada("");
    setItems(
      sugerenciasDelProveedor.map((p) => ({
        productoId: p.id,
        // Usa la cantidad ya calculada centralizadamente, que sí resta el
        // stock que todavía queda — pedir la cobertura completa sin restarlo
        // significaba pedir de más cada vez que quedaba algo en el almacén.
        cantidad: Math.max(1, p.cantidadSugerida ?? Math.ceil((p.consumoDiario || 0) * (p.diasHastaEntrega + 7))),
        costoUnitario: p.costo
      }))
    );
    setError("");
    setVerTodosProductos(false);
    setShowForm(true);
  }
  function submit() {
    if (!proveedorId) {
      setError("Selecciona un proveedor.");
      return;
    }
    if (items.length === 0) {
      setError("A\xF1ade al menos un producto al pedido.");
      return;
    }
    setError("");
    const itemsLimpios = items.map((it) => ({ productoId: it.productoId, cantidad: Number(it.cantidad), costoUnitario: Number(it.costoUnitario) }));
    if (editingId) {
      actualizarPedido(editingId, { proveedorId, fechaEsperada, items: itemsLimpios });
      setShowForm(false);
      resetForm();
    } else {
      const pedido = crearPedido({ proveedorId, fechaEsperada, items: itemsLimpios });
      setShowForm(false);
      resetForm();
      setEnviarPedido(pedido);
    }
  }
  function textoPedido(pedido) {
    const prov = proveedorPorId(pedido.proveedorId);
    const lineas = pedido.items.map((it, i) => {
      const p = productoPorId(it.productoId);
      const unidad = p ? p.unidad : "";
      return `${i + 1}. ${p ? p.nombre : "Producto eliminado"} \u2014 ${fmt(it.cantidad)} ${unidad} (a \u20AC${fmt(it.costoUnitario)}/${unidad})`;
    });
    const total = pedido.items.reduce((a, it) => a + it.cantidad * it.costoUnitario, 0);
    const ivaTotal = pedido.items.reduce((a, it) => {
      const prod = productoPorId(it.productoId);
      return a + it.cantidad * it.costoUnitario * (ivaCompraDe(prod) / 100);
    }, 0);
    const separador = "\u2014\u2014\u2014\u2014\u2014\u2014\u2014\u2014\u2014\u2014\u2014";
    return [
      `PEDIDO \u2014 ${prov ? prov.nombre : "Proveedor"}`,
      `Fecha del pedido: ${new Date(pedido.fecha).toLocaleDateString("es-ES")}`,
      `Entrega esperada: ${pedido.fechaEsperada ? new Date(pedido.fechaEsperada).toLocaleDateString("es-ES") : "por confirmar"}`,
      "",
      "Productos pedidos:",
      lineas.join("\n"),
      "",
      separador,
      `Base imponible: \u20AC${fmt(total)}`,
      `IVA: \u20AC${fmt(ivaTotal)}`,
      `TOTAL: \u20AC${fmt(total + ivaTotal)}`,
      separador,
      "",
      "Gracias."
    ].join("\n");
  }
  function mailtoHref(pedido) {
    const prov = proveedorPorId(pedido.proveedorId);
    const texto = textoPedido(pedido);
    return `mailto:${prov?.email || ""}?subject=${encodeURIComponent("Pedido - " + (prov?.nombre || ""))}&body=${encodeURIComponent(texto)}`;
  }
  function gmailAppHref(pedido) {
    const prov = proveedorPorId(pedido.proveedorId);
    const texto = textoPedido(pedido);
    return `googlegmail:///co?to=${encodeURIComponent(prov?.email || "")}&subject=${encodeURIComponent("Pedido - " + (prov?.nombre || ""))}&body=${encodeURIComponent(texto)}`;
  }
  function gmailWebHref(pedido) {
    const prov = proveedorPorId(pedido.proveedorId);
    const texto = textoPedido(pedido);
    const params = new URLSearchParams({
      view: "cm",
      fs: "1",
      to: prov?.email || "",
      su: "Pedido - " + (prov?.nombre || ""),
      body: texto
    });
    return `https://mail.google.com/mail/?${params.toString()}`;
  }
  const [compartirError, setCompartirError] = (0, import_react.useState)("");
  async function compartirPedido(pedido) {
    const prov = proveedorPorId(pedido.proveedorId);
    const texto = textoPedido(pedido);
    if (navigator.share) {
      try {
        await navigator.share({
          title: `Pedido - ${prov?.nombre || ""}`,
          text: texto
        });
        setCompartirError("");
      } catch (e) {
        if (e && e.name !== "AbortError") setCompartirError("No se pudo abrir el men\xFA de compartir.");
      }
    } else {
      setCompartirError("Tu navegador no permite compartir. Usa las otras opciones o copia el texto.");
    }
  }
  function whatsappHref(pedido) {
    const prov = proveedorPorId(pedido.proveedorId);
    const texto = textoPedido(pedido);
    const numero = (prov?.telefono || "").replace(/[^0-9]/g, "");
    return `https://wa.me/${numero}?text=${encodeURIComponent(texto)}`;
  }
  function llamarHref(pedido) {
    const prov = proveedorPorId(pedido.proveedorId);
    const numero = (prov?.telefono || "").replace(/[^0-9+]/g, "");
    return `tel:${numero}`;
  }
  const [correoPedido, setCorreoPedido] = (0, import_react.useState)(null);
  const estadoColor = { Pendiente: C.amber, Parcial: C.accent, Recibido: C.inkSoft };
  const productosSeleccionables = !proveedorId || verTodosProductos ? productos : productos.filter((p) => p.proveedorId === proveedorId);
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
    if (showForm) {
      resetForm();
    }
    setShowForm((s) => !s);
  } }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nuevo pedido") }, "Pedidos de compra"), sugerenciasPedido.length > 0 && (() => {
    const porProveedor = {};
    sugerenciasPedido.forEach((p) => {
      if (!porProveedor[p.proveedorId]) porProveedor[p.proveedorId] = { nombre: p.proveedorNombre, items: [] };
      porProveedor[p.proveedorId].items.push(p);
    });
    return /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-2" }, "Punto de pedido: se te va a agotar antes de que llegue"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, "Al ritmo de venta real de los \xFAltimos d\xEDas, estos productos se agotar\xEDan antes de que llegara un pedido hecho hoy \u2014 seg\xFAn el plazo de entrega que tienes puesto en cada proveedor."), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, Object.entries(porProveedor).map(([provId, { nombre, items: items2 }]) => /* @__PURE__ */ import_react.default.createElement("div", { key: provId, className: "rounded-lg p-2.5", style: { background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-1.5" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px] font-medium" }, nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-1.5" }, proveedorPorId(provId)?.telefono && /* @__PURE__ */ import_react.default.createElement(LinkBtn, { small: true, href: `tel:${(proveedorPorId(provId).telefono || "").replace(/[^0-9+]/g, "")}` }, /* @__PURE__ */ import_react.default.createElement(Phone, { size: 13 }), " Llamar"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => prepararPedidoDesdeSugerencias(provId, items2) }, "Preparar pedido"))), items2.map((p) => /* @__PURE__ */ import_react.default.createElement("div", { key: p.id, className: "flex items-center justify-between text-[11.5px]", style: { color: C.inkSoft } }, /* @__PURE__ */ import_react.default.createElement("span", null, p.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, Math.round(p.dias), " d\xEDa(s) de stock \xB7 pide ", fmt(p.cantidadSugerida), " ", p.unidad, p.cajasSugeridas ? ` (${p.cajasSugeridas} caja${p.cajasSugeridas > 1 ? "s" : ""})` : "")))))));
  })(), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, editingId ? "Editando pedido pendiente" : "Nuevo pedido"), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Proveedor" }, /* @__PURE__ */ import_react.default.createElement("select", { value: proveedorId, onChange: (e) => {
    setProveedorId(e.target.value);
    setVerTodosProductos(false);
  }, className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Selecciona un proveedor"), proveedores.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Fecha esperada de entrega" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: fechaEsperada, onChange: (e) => setFechaEsperada(e.target.value) }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mb-2 flex items-center justify-between flex-wrap gap-1" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium", style: { color: C.inkSoft } }, "Productos del pedido"), proveedorId && /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: () => setVerTodosProductos((s) => !s), className: "text-[11.5px] font-medium", style: { color: C.accent } }, verTodosProductos ? "Ver solo los de este proveedor" : "No lo encuentro, ver todos los productos")), productosSeleccionables.length === 0 && proveedorId && !verTodosProductos && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2 p-2 rounded-lg", style: { background: C.amberSoft, color: C.ink } }, 'Ning\xFAn producto tiene este proveedor asignado todav\xEDa. Ponlo en la ficha de cada producto, o toca "ver todos los productos" arriba para elegir de todas formas.'), items.map((it, idx) => /* @__PURE__ */ import_react.default.createElement("div", { key: idx }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 items-center mb-2" }, /* @__PURE__ */ import_react.default.createElement("select", { value: it.productoId, onChange: (e) => updateItem(idx, "productoId", e.target.value), className: "flex-1 rounded-lg px-2 py-2 text-[12.5px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "\u2014 Elige un producto \u2014"), productosSeleccionables.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre)), /* @__PURE__ */ import_react.default.createElement("option", { value: "__nuevo__" }, "+ Es un producto nuevo, nunca ha entrado\u2026")), /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", style: { width: 80 }, value: it.cantidad, onChange: (e) => updateItem(idx, "cantidad", e.target.value), placeholder: "Cant." }), /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", style: { width: 100 }, value: it.costoUnitario, onChange: (e) => updateItem(idx, "costoUnitario", e.target.value), placeholder: "Costo u." }), /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: () => removeItem(idx), "aria-label": "Quitar de la lista" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 16, color: C.inkSoft }))), creandoNuevoIdx === idx && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-2", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2" }, "Nuevo producto \u2014 se dar\xE1 de alta en el cat\xE1logo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-2 mb-2" }, /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      value: nuevoProd.nombre,
      onChange: (e) => setNuevoProd({ ...nuevoProd, nombre: e.target.value }),
      placeholder: "Nombre del producto",
      style: { gridColumn: "span 3" }
    }
  ), /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      value: nuevoProd.unidad,
      onChange: (e) => setNuevoProd({ ...nuevoProd, unidad: e.target.value }),
      placeholder: "Unidad (kg, unidad\u2026)"
    }
  ), /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      type: "number",
      step: "0.01",
      value: nuevoProd.costo,
      onChange: (e) => setNuevoProd({ ...nuevoProd, costo: e.target.value }),
      placeholder: "Precio de compra",
      style: { gridColumn: "span 2" }
    }
  )), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => guardarProductoNuevo(idx), disabled: !nuevoProd.nombre.trim() }, "Crear y a\xF1adir al pedido"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => {
    setCreandoNuevoIdx(null);
    updateItem(idx, "productoId", "");
  } }, "Cancelar"))), (() => {
    const prod = productos.find((pr) => pr.id === it.productoId);
    const uds = udsPorCaja(prod);
    if (!prod || uds <= 1) return null;
    const cajas = Number(it.cantidad) / uds;
    const enteras = Number.isInteger(cajas);
    return /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-2 flex items-center gap-2 flex-wrap", style: { color: enteras ? C.accent : C.amber } }, /* @__PURE__ */ import_react.default.createElement("span", null, fmt(cajas), " caja(s) de ", uds, " ", prod.unidad, !enteras && " \u2014 no es un n\xFAmero exacto de cajas"), /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        type: "button",
        onClick: () => updateItem(idx, "cantidad", String(Math.ceil(cajas) * uds)),
        className: "underline",
        style: { color: C.accent }
      },
      "redondear a ",
      Math.ceil(cajas),
      " caja(s) = ",
      Math.ceil(cajas) * uds,
      " ",
      prod.unidad
    ));
  })())), /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: addItemRow, className: "text-[12.5px] font-medium mb-4", style: { color: C.accent } }, "+ A\xF1adir producto al pedido"), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, editingId ? "Guardar cambios" : "Crear pedido"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowForm(false);
    resetForm();
  } }, "Cancelar")))), pedidos.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No hay pedidos registrados todav\xEDa." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, pedidos.map((pe) => {
    const prov = proveedorPorId(pe.proveedorId);
    const total = pe.items.reduce((a, it) => a + it.cantidad * it.costoUnitario, 0);
    const ivaPedido = pe.items.reduce((a, it) => {
      const prod = productoPorId(it.productoId);
      return a + it.cantidad * it.costoUnitario * (ivaCompraDe(prod) / 100);
    }, 0);
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: pe.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold" }, prov ? prov.nombre : "Proveedor eliminado"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, "Pedido ", pe.fecha, " \xB7 Entrega esperada ", pe.fechaEsperada || "\u2014")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-3" }, /* @__PURE__ */ import_react.default.createElement(Pill, { color: estadoColor[pe.estado] }, pe.estado), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, "\u20AC", fmt(total + ivaPedido)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mono", style: { color: C.inkSoft } }, "base \u20AC", fmt(total), " + IVA \u20AC", fmt(ivaPedido))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 flex gap-2 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => {
      setCorreoPedido(pe);
    } }, /* @__PURE__ */ import_react.default.createElement(Mail, { size: 13 }), " Enviar por correo"), /* @__PURE__ */ import_react.default.createElement(LinkBtn, { small: true, href: whatsappHref(pe) }, /* @__PURE__ */ import_react.default.createElement(MessageCircle, { size: 13 }), " Enviar por WhatsApp"), proveedorPorId(pe.proveedorId)?.telefono && /* @__PURE__ */ import_react.default.createElement(LinkBtn, { small: true, href: llamarHref(pe) }, /* @__PURE__ */ import_react.default.createElement(Phone, { size: 13 }), " Llamar"), pe.estado === "Pendiente" && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => openEdit(pe) }, "Editar"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "danger", onClick: () => setConfirmDeleteId(pe.id) }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 13 }), " Eliminar"))));
  })), confirmDeleteId && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmDeleteId(null), title: "Eliminar pedido" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-4", style: { color: C.ink } }, "\xBFSeguro que quieres eliminar este pedido? Esta acci\xF3n no se puede deshacer."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: () => {
    eliminarPedido(confirmDeleteId);
    setConfirmDeleteId(null);
  } }, "S\xED, eliminar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmDeleteId(null) }, "Cancelar"))), correoPedido && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setCorreoPedido(null), title: "Enviar pedido por correo" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3", style: { color: C.ink } }, "Prueba los botones de Gmail. Si no se abren, m\xE1s abajo tienes el correo listo para copiar y pegar."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-col gap-2 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => compartirPedido(correoPedido) }, /* @__PURE__ */ import_react.default.createElement(Mail, { size: 14 }), " Compartir con Gmail u otra app"), /* @__PURE__ */ import_react.default.createElement(LinkBtn, { href: gmailWebHref(correoPedido) }, /* @__PURE__ */ import_react.default.createElement(Mail, { size: 14 }), " Abrir en Gmail (navegador)"), /* @__PURE__ */ import_react.default.createElement(LinkBtn, { href: mailtoHref(correoPedido), newTab: false }, /* @__PURE__ */ import_react.default.createElement(Mail, { size: 14 }), " App de correo del sistema")), compartirError && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.red } }, compartirError), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2 pt-3", style: { color: C.inkSoft, borderTop: `1px solid ${C.line}` } }, "Si ninguno abre, toca cada campo (se selecciona solo) y c\xF3pialo:"), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Destinatario" }, /* @__PURE__ */ import_react.default.createElement(Input, { readOnly: true, value: proveedorPorId(correoPedido.proveedorId)?.email || "", onFocus: (e) => e.target.select() })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Asunto" }, /* @__PURE__ */ import_react.default.createElement(Input, { readOnly: true, value: `Pedido - ${proveedorPorId(correoPedido.proveedorId)?.nombre || ""}`, onFocus: (e) => e.target.select() })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Mensaje" }, /* @__PURE__ */ import_react.default.createElement(
    "textarea",
    {
      readOnly: true,
      value: textoPedido(correoPedido),
      onFocus: (e) => e.target.select(),
      className: "w-full rounded-lg p-2 text-[12px] mono",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink, height: 160, resize: "none" }
    }
  )), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setCorreoPedido(null) }, "Cerrar")), enviarPedido && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setEnviarPedido(null), title: "Pedido creado \u2014 \xBFc\xF3mo quieres enviarlo?" }, /* @__PURE__ */ import_react.default.createElement("p", { className: "text-[12.5px] mb-4", style: { color: C.inkSoft } }, "Puedes enviarlo ahora al proveedor o hacerlo m\xE1s tarde desde la lista de pedidos."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
    setCorreoPedido(enviarPedido);
    setEnviarPedido(null);
  } }, /* @__PURE__ */ import_react.default.createElement(Mail, { size: 14 }), " Correo"), /* @__PURE__ */ import_react.default.createElement(LinkBtn, { variant: "primary", href: whatsappHref(enviarPedido) }, /* @__PURE__ */ import_react.default.createElement(MessageCircle, { size: 14 }), " WhatsApp"), proveedorPorId(enviarPedido.proveedorId)?.telefono && /* @__PURE__ */ import_react.default.createElement(LinkBtn, { href: llamarHref(enviarPedido) }, /* @__PURE__ */ import_react.default.createElement(Phone, { size: 14 }), " Llamar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setEnviarPedido(null) }, "Ahora no"))));
}
function Recepcion({ pedidos, proveedorPorId, productoPorId, recibirPedido, almacenCongelado, recibirConAlbaran, recibirConFotoIA, cerrarPedido }) {
  const [activos, setActivos] = (0, import_react.useState)({});
  const [cerrando, setCerrando] = (0, import_react.useState)(null);
  const pendientes = pedidos.filter((p) => p.estado !== "Recibido");
  function setCampo(pedidoId, productoId, campo, val) {
    setActivos((s) => ({
      ...s,
      [pedidoId]: { ...s[pedidoId] || {}, [productoId]: { ...(s[pedidoId] || {})[productoId] || {}, [campo]: val } }
    }));
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Recepci\xF3n de mercanc\xEDa"), almacenCongelado && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start gap-2 text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement(TriangleAlert, { size: 16, color: C.amber, style: { marginTop: 2, flexShrink: 0 } }), /* @__PURE__ */ import_react.default.createElement("span", null, /* @__PURE__ */ import_react.default.createElement("b", null, "Almac\xE9n congelado por conteo en curso."), " Las entradas y salidas est\xE1n bloqueadas hasta que finalices el conteo en Inventario ciego."))), pendientes.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No hay pedidos pendientes de recibir." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-3" }, pendientes.map((pe) => {
    const prov = proveedorPorId(pe.proveedorId);
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: pe.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold" }, prov ? prov.nombre : "\u2014"), /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.amber }, pe.estado)), pe.items.map((it) => {
      const p = productoPorId(it.productoId);
      const pendiente = it.cantidad - it.cantidadRecibida;
      const campo = (activos[pe.id] || {})[it.productoId] || {};
      const precioPrecargado = campo.precio !== void 0 ? campo.precio : p ? p.costo || "" : "";
      const ivaPrecargado = campo.iva !== void 0 ? campo.iva : p ? p.ivaCompra || 10 : 10;
      return /* @__PURE__ */ import_react.default.createElement("div", { key: it.productoId, className: "py-1.5 border-b last:border-0", style: { borderColor: C.line } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", null, p ? p.nombre : "\u2014"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mono", style: { color: C.inkSoft } }, "Pedido: ", it.cantidad, " \xB7 Recibido: ", it.cantidadRecibida, " \xB7 Pendiente: ", pendiente)), /* @__PURE__ */ import_react.default.createElement(
        Input,
        {
          type: "number",
          style: { width: 90 },
          placeholder: "Cant. hoy",
          value: campo.cantidad || "",
          onChange: (e) => setCampo(pe.id, it.productoId, "cantidad", e.target.value),
          disabled: pendiente <= 0
        }
      )), Number(campo.cantidad) > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2 mt-1.5" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11px]", style: { color: C.inkSoft } }, "Precio/ud (\u20AC)"), /* @__PURE__ */ import_react.default.createElement(
        Input,
        {
          type: "number",
          step: "0.01",
          style: { width: 80 },
          value: precioPrecargado,
          onChange: (e) => setCampo(pe.id, it.productoId, "precio", e.target.value)
        }
      ), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11px]", style: { color: C.inkSoft } }, "IVA %"), /* @__PURE__ */ import_react.default.createElement(
        Input,
        {
          type: "number",
          style: { width: 60 },
          value: ivaPrecargado,
          onChange: (e) => setCampo(pe.id, it.productoId, "iva", e.target.value)
        }
      )));
    }), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-3 flex flex-wrap gap-2" }, !almacenCongelado && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => recibirConAlbaran(pe) }, /* @__PURE__ */ import_react.default.createElement(FileText, { size: 14 }), " Recibir con albar\xE1n"), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => recibirConFotoIA(pe) }, /* @__PURE__ */ import_react.default.createElement(Camera, { size: 14 }), " Recibir con foto IA"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
      const lineas = pe.items.filter((it) => Number((activos[pe.id] || {})[it.productoId]?.cantidad) > 0).map((it) => {
        const campo = (activos[pe.id] || {})[it.productoId] || {};
        const p = productoPorId(it.productoId);
        return {
          productoId: it.productoId,
          cantidad: campo.cantidad,
          // Si no se tocó el precio, se usa el precargado
          // (el coste actual del producto) — así confirmar
          // sin escribir nada sigue siendo "llegó igual".
          precioBruto: campo.precio !== void 0 ? campo.precio : p ? p.costo : 0,
          ivaPct: campo.iva !== void 0 ? campo.iva : p ? p.ivaCompra : 10
        };
      });
      recibirPedido(pe.id, lineas);
      setActivos((s) => ({ ...s, [pe.id]: {} }));
    } }, /* @__PURE__ */ import_react.default.createElement(CircleCheck, { size: 14 }), " Recibir sin albar\xE1n")), cerrarPedido && /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setCerrando(pe) }, "Cerrar pedido")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-2", style: { color: C.inkSoft } }, "Con albar\xE1n registras adem\xE1s precios, descuentos, lotes y caducidades, y el pedido se cierra solo. Con foto IA, haces una foto del papel del proveedor y ella rellena el borrador por ti. La otra opci\xF3n solo suma stock, sin dejar rastro del papel."));
  })), cerrando && (() => {
    const faltan = cerrando.items.map((it) => ({ ...it, producto: productoPorId(it.productoId), pendiente: (Number(it.cantidad) || 0) - (Number(it.cantidadRecibida) || 0) })).filter((it) => it.pendiente > 0);
    return /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setCerrando(null), title: "\xBFDar el pedido por cerrado?" }, faltan.length === 0 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3" }, "Ha llegado todo lo pedido. Se marcar\xE1 como recibido.") : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2" }, "Todav\xEDa falta por llegar:"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1 mb-3" }, faltan.map((it) => /* @__PURE__ */ import_react.default.createElement("div", { key: it.productoId, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, it.producto ? it.producto.nombre : "\u2014"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: C.amber } }, "faltan ", fmt(it.pendiente))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.inkSoft } }, "Ci\xE9rralo si el proveedor ya no va a servir el resto. ", /* @__PURE__ */ import_react.default.createElement("b", null, "No suma nada al stock"), " \u2014 solo saca el pedido de esta lista para que deje de aparecer como pendiente.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
      cerrarPedido(cerrando.id);
      setCerrando(null);
    } }, "S\xED, cerrar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setCerrando(null) }, "Cancelar")));
  })());
}
function textoHojaConteo(productos, proveedores = []) {
  const fecha = (/* @__PURE__ */ new Date()).toLocaleDateString("es-ES", { day: "2-digit", month: "2-digit", year: "numeric" });
  const grupos = agruparPorProveedor(productos, proveedores);
  let n = 0;
  const bloques = grupos.map((g) => {
    const lineas = g.items.map((p) => {
      n += 1;
      return `${n}. ${p.nombre} (${p.unidad})
   Conteo: _____________`;
    });
    return `--- ${g.nombre.toUpperCase()} ---

${lineas.join("\n\n")}`;
  });
  return `HOJA DE CONTEO DE INVENTARIO
Fecha: ${fecha}

${bloques.join("\n\n")}

Contado por: ____________________`;
}
function InventarioCiego({ productos, proveedores, conteos, iniciarConteo, actualizarConteoItem, actualizarResponsable, finalizarConteo, aplicarAjustes, eliminarConteo, productoPorId, crearProductoEnConteo, clasificacionABC, almacenCongelado }) {
  const [activoId, setActivoId] = (0, import_react.useState)(null);
  const activo = conteos.find((c) => c.id === activoId);
  const [showNuevo, setShowNuevo] = (0, import_react.useState)(false);
  const [resumenCierre, setResumenCierre] = (0, import_react.useState)(null);
  const [confirmarEliminar, setConfirmarEliminar] = (0, import_react.useState)(null);
  const [procesandoCierre, setProcesandoCierre] = (0, import_react.useState)(false);
  const [procesandoEliminar, setProcesandoEliminar] = (0, import_react.useState)(false);
  const blankNuevo = { codigo: "", nombre: "", categoria: "", unidad: "unidad", costo: "", precioVenta: "", stockMinimo: "", ubicacion: "", proveedorId: "", cantidadContada: "" };
  const [nuevo, setNuevo] = (0, import_react.useState)(blankNuevo);
  const [errorNuevo, setErrorNuevo] = (0, import_react.useState)("");
  const [motivos, setMotivos] = (0, import_react.useState)({});
  const [modoEntrada, setModoEntrada] = (0, import_react.useState)({});
  const [cajasTexto, setCajasTexto] = (0, import_react.useState)({});
  const [verHoja, setVerHoja] = (0, import_react.useState)(false);
  const [ambitoHoja, setAmbitoHoja] = (0, import_react.useState)("total");
  const productosHoja = (0, import_react.useMemo)(() => {
    if (ambitoHoja === "piso_venta") return productos.filter((p) => p.tipo === "elaborado" || Number(p.precioVenta) > 0);
    if (ambitoHoja === "almacen") return productos.filter((p) => p.tipo !== "elaborado");
    return productos;
  }, [productos, ambitoHoja]);
  const [verHojaCompletada, setVerHojaCompletada] = (0, import_react.useState)(false);
  const MOTIVOS = ["Rotura", "Merma / caducidad", "Robo o p\xE9rdida", "Error de captura", "Devoluci\xF3n no registrada", "Otro"];
  function submitNuevo() {
    if (!nuevo.nombre.trim()) {
      setErrorNuevo("Escribe el nombre del producto.");
      return;
    }
    if (nuevo.cantidadContada === "" || isNaN(Number(nuevo.cantidadContada))) {
      setErrorNuevo("Escribe la cantidad que contaste.");
      return;
    }
    setErrorNuevo("");
    const { cantidadContada, ...datosProducto } = nuevo;
    crearProductoEnConteo(activo.id, datosProducto, cantidadContada);
    setNuevo(blankNuevo);
    setShowNuevo(false);
  }
  const esPisoVenta = activo?.ambito === "piso_venta";
  const esAlmacen = activo?.ambito === "almacen";
  function referenciaSistema(p) {
    if (esPisoVenta) return Number(p.stockPisoVenta) || 0;
    if (esAlmacen) return Math.max(0, (Number(p.stock) || 0) - (Number(p.stockPisoVenta) || 0));
    return Number(p.stock) || 0;
  }
  const diferencias = !activo ? [] : activo.items.map((it) => {
    const p = productoPorId(it.productoId);
    if (!p) return null;
    const cf = valorCF(it);
    if (cf === null) return null;
    const referencia = referenciaSistema(p);
    const dif = cf - referencia;
    const impacto = dif * (Number(p.costo) || 0);
    return { producto: p, item: it, cf, dif, impacto };
  }).filter(Boolean).sort((a, b) => Math.abs(b.impacto) - Math.abs(a.impacto));
  const impactoTotal = diferencias.reduce((a, d) => a + d.impacto, 0);
  const conDescuadre = diferencias.filter((d) => d.dif !== 0);
  const abcColor = { A: C.red, B: C.amber, C: C.inkSoft };
  function textoHojaConteoCompletada() {
    const fecha = activo.fecha;
    const grupos = agruparPorProveedor(diferencias.map((d) => d.producto), proveedores);
    const cfPorProducto = {};
    diferencias.forEach((d) => {
      cfPorProducto[d.producto.id] = d.cf;
    });
    let n = 0;
    const bloques = grupos.map((g) => {
      const lineas = g.items.map((p) => {
        n += 1;
        return `${n}. ${p.nombre} (${p.unidad}): ${fmt(cfPorProducto[p.id])}`;
      });
      return `--- ${g.nombre.toUpperCase()} ---
${lineas.join("\n")}`;
    });
    const contadoPor = activo.responsables && activo.responsables.contadoPor ? activo.responsables.contadoPor : "\u2014";
    return `HOJA DE CONTEO \u2014 YA CONTADO
Fecha: ${fecha}
Contado por: ${contadoPor}

${bloques.join("\n\n")}`;
  }
  function textoResultadoConteo() {
    const ambito = esPisoVenta ? "Piso de venta" : esAlmacen ? "Almac\xE9n (trastienda)" : "Todo (empresa completa)";
    const lineas = diferencias.filter((d) => d.dif !== 0).map((d) => {
      const motivo = motivos[d.producto.id] ? ` \xB7 ${motivos[d.producto.id]}` : "";
      return `${d.producto.nombre}: sistema ${fmt(referenciaSistema(d.producto))}, contado ${d.cf} (${d.dif > 0 ? "+" : ""}${d.dif}) \xB7 \u20AC${fmt(Math.abs(d.impacto))}${motivo}`;
    });
    const cuerpo = lineas.length > 0 ? lineas.join("\n") : "Sin descuadres \u2014 todo cuadraba.";
    return `RESULTADO DE INVENTARIO \u2014 ${activo.fecha}
${ambito}

Descuadrados: ${conDescuadre.length}
Impacto neto: ${impactoTotal >= 0 ? "+" : "\u2212"}\u20AC${fmt(Math.abs(impactoTotal))}

${cuerpo}`;
  }
  async function compartirPdfWhatsApp() {
    const doc = generarPdfConteo(activo, diferencias, proveedores);
    const nombreArchivo = `Inventario San Gines ${activo.fecha}.pdf`;
    try {
      const blob = doc.output("blob");
      const archivo = new File([blob], nombreArchivo, { type: "application/pdf" });
      if (typeof navigator !== "undefined" && navigator.canShare && navigator.canShare({ files: [archivo] })) {
        await navigator.share({
          files: [archivo],
          title: "Resultado del inventario",
          text: `Chocolater\xEDa San Gin\xE9s \xB7 Inventario del ${activo.fecha}`
        });
        return;
      }
    } catch (e) {
      if (e && e.name === "AbortError") return;
    }
    doc.save(nombreArchivo);
    window.open(`https://wa.me/?text=${encodeURIComponent(textoResultadoConteo())}`, "_blank");
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(
    SectionTitle,
    {
      action: /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setVerHoja(true) }, /* @__PURE__ */ import_react.default.createElement(ClipboardList, { size: 15 }), " Hoja para contar"), !activo && /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => setActivoId(iniciarConteo("total")) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Contar todo (empresa completa)"), !activo && /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setActivoId(iniciarConteo("piso_venta")) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Contar solo el piso de venta"), !activo && /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setActivoId(iniciarConteo("almacen")) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Contar solo el almac\xE9n (trastienda, sin elaborados)"))
    },
    "Inventario ciego"
  ), resumenCierre && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between gap-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, resumenCierre.ajustados === 0 ? "Todo coincid\xEDa \u2014 no hizo falta ning\xFAn ajuste." : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, resumenCierre.ajustados, " producto(s) ajustado(s).", resumenCierre.traspasados.filter((t) => t.tipoTraspaso === "sobra").length > 0 && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, " ", "De ellos, ", resumenCierre.traspasados.filter((t) => t.tipoTraspaso === "sobra").length, " ten\xEDan sobrante y se traspasaron solos al piso de venta:", " ", resumenCierre.traspasados.filter((t) => t.tipoTraspaso === "sobra").map((t) => `${t.nombre} (+${fmt(t.cantidad)})`).join(", "), "."), resumenCierre.traspasados.filter((t) => t.tipoTraspaso === "falta").length > 0 && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, " ", resumenCierre.traspasados.filter((t) => t.tipoTraspaso === "falta").length, " ten\xEDan menos de lo esperado en almac\xE9n \u2014 se asume que ya estaban en el piso de venta sin registrar, y se traspasaron ah\xED (el total no cambi\xF3, no se cuenta como merma):", " ", resumenCierre.traspasados.filter((t) => t.tipoTraspaso === "falta").map((t) => `${t.nombre} (+${fmt(t.cantidad)})`).join(", "), "."))), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setResumenCierre(null), className: "shrink-0", style: { color: C.inkSoft } }, /* @__PURE__ */ import_react.default.createElement(X, { size: 16 })))), almacenCongelado && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start gap-2 text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement(TriangleAlert, { size: 16, color: C.amber, style: { marginTop: 2, flexShrink: 0 } }), /* @__PURE__ */ import_react.default.createElement("span", null, /* @__PURE__ */ import_react.default.createElement("b", null, "Almac\xE9n congelado."), " Mientras haya un conteo abierto no se pueden registrar entradas ni salidas, para que el descuadre no salga falseado. Finaliza el conteo para desbloquearlo."))), !activo && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start gap-2 text-[12.5px]", style: { color: C.ink } }, /* @__PURE__ */ import_react.default.createElement(EyeOff, { size: 16, style: { marginTop: 2, flexShrink: 0 } }), /* @__PURE__ */ import_react.default.createElement("span", null, "Durante el conteo no se muestra la existencia del sistema: cuentas f\xEDsicamente y anotas la cantidad. Al finalizar ver\xE1s las diferencias contra el stock registrado, valoradas en euros."))), activo && !activo.completado && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3 flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement(Eye, { size: 15 }), " Captura de conteos", /* @__PURE__ */ import_react.default.createElement(Pill, { color: esPisoVenta || esAlmacen ? C.amber : C.inkSoft }, esPisoVenta ? "Solo piso de venta" : esAlmacen ? "Solo almac\xE9n (trastienda)" : "Todo (empresa completa)")), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Contado por" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: (activo.responsables || {}).contadoPor || "", onChange: (e) => actualizarResponsable(activo.id, "contadoPor", e.target.value), placeholder: "Nombre" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Revisado por (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: (activo.responsables || {}).revisor || "", onChange: (e) => actualizarResponsable(activo.id, "revisor", e.target.value), placeholder: "Responsable" }))), /* @__PURE__ */ import_react.default.createElement("div", { style: { overflowX: "auto" } }, /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[12px]", style: { borderCollapse: "collapse" } }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff", width: 30 } }, "N\xBA"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Descripci\xF3n del producto"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { background: C.chrome, color: "#fff", width: 58 } }, "U/M"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { background: C.chrome, color: "#fff", width: 96 } }, "Conteo"))), /* @__PURE__ */ import_react.default.createElement("tbody", null, (() => {
    const enConteo = activo.items.map((it) => ({ it, p: productoPorId(it.productoId) })).filter((x) => x.p);
    const grupos = agruparPorProveedor(enConteo.map((x) => x.p), proveedores);
    const porId = new Map(enConteo.map((x) => [x.p.id, x.it]));
    let n = 0;
    return grupos.map((g) => /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, { key: g.id }, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("td", { colSpan: 4, className: "py-1.5 px-2 font-semibold text-[11.5px]", style: { background: C.accentSoft, border: `1px solid ${C.line}` } }, g.nombre)), g.items.map((p) => {
      const it = porId.get(p.id);
      if (!it) return null;
      n += 1;
      const idx = n;
      const abc = esNoMercancia(p) ? "\u2014" : clasificacionABC[p.id] || "C";
      const sinContar = (it.conteo ?? valorCF(it) ?? "") === "";
      return /* @__PURE__ */ import_react.default.createElement("tr", { key: it.productoId, style: { background: idx % 2 ? C.bg : C.surface } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 mono", style: { border: `1px solid ${C.line}` } }, idx + 1), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2", style: { border: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement("span", null, p.nombre), /* @__PURE__ */ import_react.default.createElement(Pill, { color: abcColor[abc] || C.inkSoft }, abc)), p.codigo && /* @__PURE__ */ import_react.default.createElement("div", { className: "mono text-[10.5px]", style: { color: C.inkSoft } }, p.codigo)), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-center", style: { border: `1px solid ${C.line}` } }, p.unidad), /* @__PURE__ */ import_react.default.createElement("td", { className: "p-1", style: { border: `1px solid ${C.line}` } }, (() => {
        const uds = udsPorCaja(p);
        const puedeContarPorCajas = uds > 1;
        const modo = modoEntrada[p.id] || "unidad";
        const valorNativo = it.conteo ?? valorCF(it) ?? "";
        const textoCajas = cajasTexto[p.id] !== void 0 ? cajasTexto[p.id] : valorNativo !== "" ? String(Math.round(Number(valorNativo) / uds * 100) / 100) : "";
        const valorMostrado = modo === "caja" ? textoCajas : valorNativo;
        return /* @__PURE__ */ import_react.default.createElement("div", null, puedeContarPorCajas && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-1 mb-1 justify-center" }, /* @__PURE__ */ import_react.default.createElement(
          "button",
          {
            type: "button",
            onClick: () => {
              setModoEntrada((s) => ({ ...s, [p.id]: "unidad" }));
            },
            className: "text-[9.5px] px-1.5 py-0.5 rounded",
            style: {
              background: modo === "unidad" ? C.accent : C.surface,
              color: modo === "unidad" ? "#fff" : C.inkSoft,
              border: `1px solid ${C.line}`
            }
          },
          "Uds"
        ), /* @__PURE__ */ import_react.default.createElement(
          "button",
          {
            type: "button",
            onClick: () => {
              setCajasTexto((s) => ({
                ...s,
                [p.id]: valorNativo !== "" ? String(Math.round(Number(valorNativo) / uds * 100) / 100) : ""
              }));
              setModoEntrada((s) => ({ ...s, [p.id]: "caja" }));
            },
            className: "text-[9.5px] px-1.5 py-0.5 rounded",
            style: {
              background: modo === "caja" ? C.accent : C.surface,
              color: modo === "caja" ? "#fff" : C.inkSoft,
              border: `1px solid ${C.line}`
            }
          },
          "Cajas (",
          uds,
          "/caja)"
        )), /* @__PURE__ */ import_react.default.createElement(
          "input",
          {
            type: "number",
            inputMode: "decimal",
            value: valorMostrado,
            onChange: (e) => {
              const tecleado = e.target.value;
              if (modo === "caja") {
                setCajasTexto((s) => ({ ...s, [p.id]: tecleado }));
                const enUnidades = tecleado === "" ? "" : Number(tecleado) * uds;
                actualizarConteoItem(activo.id, it.productoId, "conteo", enUnidades === "" ? "" : String(enUnidades));
              } else {
                actualizarConteoItem(activo.id, it.productoId, "conteo", tecleado);
              }
            },
            className: "w-full rounded px-2 py-1.5 text-[13px] text-center outline-none mono",
            style: {
              border: `1px solid ${sinContar ? C.line : C.accent}`,
              background: sinContar ? C.surface : C.accentSoft,
              fontWeight: 600
            }
          }
        ), /* @__PURE__ */ import_react.default.createElement(
          "div",
          {
            className: "text-[10px] text-center mt-0.5",
            style: { color: C.inkSoft, visibility: modo === "caja" && valorNativo !== "" ? "visible" : "hidden" }
          },
          "= ",
          modo === "caja" && valorNativo !== "" ? fmt(Number(valorNativo)) : "0",
          " ",
          p.unidad
        ));
      })()));
    })));
  })()))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mt-2", style: { color: C.inkSoft } }, "Las casillas en verde ya tienen conteo capturado. Las que quedan en blanco est\xE1n pendientes. El orden es el mismo que el de la hoja impresa, agrupado por proveedor."), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-4 flex gap-2 items-center flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => finalizarConteo(activo.id) }, "Finalizar conteo"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowNuevo((s) => !s) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 14 }), " Encontr\xE9 un producto que no est\xE1 en el sistema")), showNuevo && /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-4 pt-4", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-medium mb-3", style: { color: C.inkSoft } }, "Da de alta el producto encontrado y captura la cantidad contada. Se a\xF1adir\xE1 al cat\xE1logo y a este conteo."), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: nuevo.nombre, onChange: (e) => setNuevo({ ...nuevo, nombre: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "C\xF3digo / SKU (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: nuevo.codigo, onChange: (e) => setNuevo({ ...nuevo, codigo: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Categor\xEDa" }, /* @__PURE__ */ import_react.default.createElement(CampoCategoria, { value: nuevo.categoria, onChange: (v) => setNuevo({ ...nuevo, categoria: v }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Unidad de medida" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: nuevo.unidad, onChange: (e) => setNuevo({ ...nuevo, unidad: e.target.value }), placeholder: "unidad, kg, caja\u2026" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Costo de compra (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: nuevo.costo, onChange: (e) => setNuevo({ ...nuevo, costo: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio de venta (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: nuevo.precioVenta, onChange: (e) => setNuevo({ ...nuevo, precioVenta: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Stock m\xEDnimo (reorden)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: nuevo.stockMinimo, onChange: (e) => setNuevo({ ...nuevo, stockMinimo: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Ubicaci\xF3n en almac\xE9n (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: nuevo.ubicacion, onChange: (e) => setNuevo({ ...nuevo, ubicacion: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Proveedor principal (opcional)" }, /* @__PURE__ */ import_react.default.createElement("select", { value: nuevo.proveedorId, onChange: (e) => setNuevo({ ...nuevo, proveedorId: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Sin asignar"), proveedores.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Cantidad contada f\xEDsicamente" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: nuevo.cantidadContada, onChange: (e) => setNuevo({ ...nuevo, cantidadContada: e.target.value }) }))), errorNuevo && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, errorNuevo), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mt-1" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submitNuevo }, "A\xF1adir al conteo"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowNuevo(false);
    setErrorNuevo("");
  } }, "Cancelar")))), activo && activo.completado && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between flex-wrap gap-2 mb-1 no-imprimir" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold" }, "Resultado del conteo \u2014 ", activo.fecha), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => window.print() }, "Imprimir / Guardar PDF"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: compartirPdfWhatsApp }, /* @__PURE__ */ import_react.default.createElement(FileText, { size: 13 }), " Enviar PDF por WhatsApp"), /* @__PURE__ */ import_react.default.createElement(LinkBtn, { href: `https://wa.me/?text=${encodeURIComponent(textoResultadoConteo())}` }, /* @__PURE__ */ import_react.default.createElement(MessageCircle, { size: 13 }), " WhatsApp (solo texto)"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3 no-imprimir", style: { color: C.inkSoft } }, "Ordenado por impacto econ\xF3mico: lo que m\xE1s dinero mueve aparece primero."), /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end justify-between mb-3 pb-3", style: { borderBottom: "2px solid #9C7A34" } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[16px] font-semibold" }, "Resultado del conteo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: "#6B7A6E" } }, activo.fecha, " \xB7 ", esPisoVenta ? "Piso de venta" : esAlmacen ? "Almac\xE9n (trastienda)" : "Todo (empresa completa)")), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 48, width: "auto" } })), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, { style: { background: C.bg } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Productos descuadrados"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1", style: { color: conDescuadre.length ? C.amber : C.accent } }, conDescuadre.length)), /* @__PURE__ */ import_react.default.createElement(Card, { style: { background: C.bg } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Impacto econ\xF3mico neto"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1", style: { color: impactoTotal < 0 ? C.red : impactoTotal > 0 ? C.accent : C.ink } }, impactoTotal >= 0 ? "+" : "\u2212", "\u20AC", fmt(Math.abs(impactoTotal))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, diferencias.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No se captur\xF3 ning\xFAn conteo." }) : diferencias.map((d) => /* @__PURE__ */ import_react.default.createElement(Card, { key: d.producto.id, style: { background: d.dif === 0 ? C.surface : C.bg } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between mb-1" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-medium" }, d.producto.nombre), /* @__PURE__ */ import_react.default.createElement(Pill, { color: abcColor[esNoMercancia(d.producto) ? "\u2014" : clasificacionABC[d.producto.id] || "C"] || C.inkSoft }, esNoMercancia(d.producto) ? "\u2014" : clasificacionABC[d.producto.id] || "C")), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-4 gap-2 text-[12px] mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft }, className: "text-[10.5px]" }, esPisoVenta ? "Sistema (piso)" : esAlmacen ? "Sistema (almac\xE9n)" : "Sistema"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono" }, fmt(referenciaSistema(d.producto)))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft }, className: "text-[10.5px]" }, "Conteo final"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, d.cf)), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft }, className: "text-[10.5px]" }, "Diferencia"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold", style: { color: d.dif === 0 ? C.inkSoft : d.dif < 0 ? C.red : C.accent } }, d.dif > 0 ? `+${d.dif}` : d.dif)), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft }, className: "text-[10.5px]" }, "Impacto"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold", style: { color: d.impacto === 0 ? C.inkSoft : d.impacto < 0 ? C.red : C.accent } }, d.impacto >= 0 ? "" : "\u2212", "\u20AC", fmt(Math.abs(d.impacto))))), d.dif !== 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Motivo del descuadre" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: motivos[d.producto.id] || "",
      onChange: (e) => setMotivos({ ...motivos, [d.producto.id]: e.target.value }),
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "\u2014 Selecciona un motivo \u2014"),
    MOTIVOS.map((m) => /* @__PURE__ */ import_react.default.createElement("option", { key: m, value: m }, m))
  ))), d.dif !== 0 && motivos[d.producto.id] && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Motivo: ", motivos[d.producto.id]))))), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setVerHojaCompletada((s) => !s), className: "text-[12.5px] font-medium mt-2 mb-2 no-imprimir", style: { color: C.accent } }, verHojaCompletada ? "Ocultar" : "Ver", " hoja de conteo ya rellena"), verHojaCompletada && /* @__PURE__ */ import_react.default.createElement("div", { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-2 mb-3 no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => window.print() }, "Imprimir / Guardar PDF"), /* @__PURE__ */ import_react.default.createElement(LinkBtn, { href: `https://wa.me/?text=${encodeURIComponent(textoHojaConteoCompletada())}` }, /* @__PURE__ */ import_react.default.createElement(MessageCircle, { size: 13 }), " WhatsApp")), /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end justify-between mb-3 pb-3", style: { borderBottom: "2px solid #9C7A34" } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[17px] font-semibold" }, "Hoja de conteo \u2014 ya contado"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: "#6B7A6E" } }, "Fecha: ", activo.fecha, " \xB7 Contado por: ", activo.responsables && activo.responsables.contadoPor ? activo.responsables.contadoPor : "\u2014")), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 62, width: "auto" } })), /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[11.5px]", style: { borderCollapse: "collapse" } }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff", width: 28 } }, "N\xBA"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Descripci\xF3n del producto"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { background: C.chrome, color: "#fff", width: 60 } }, "U/M"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { background: C.chrome, color: "#fff", width: 90 } }, "Contado"))), /* @__PURE__ */ import_react.default.createElement("tbody", null, (() => {
    let n = 0;
    const cfPorProducto = {};
    diferencias.forEach((d) => {
      cfPorProducto[d.producto.id] = d.cf;
    });
    return agruparPorProveedor(diferencias.map((d) => d.producto), proveedores).map((g) => /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, { key: g.id }, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("td", { colSpan: 4, className: "py-1.5 px-2 font-semibold text-[11.5px]", style: { background: C.accentSoft, border: `1px solid ${C.line}` } }, g.nombre, " \xB7 ", g.items.length, " referencia(s)")), g.items.map((p) => {
      n += 1;
      return /* @__PURE__ */ import_react.default.createElement("tr", { key: p.id, style: { background: n % 2 ? C.bg : C.surface } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 mono", style: { border: `1px solid ${C.line}` } }, n), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2", style: { border: `1px solid ${C.line}` } }, p.nombre, p.codigo && /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: C.inkSoft } }, " \xB7 ", p.codigo)), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-center", style: { border: `1px solid ${C.line}` } }, p.unidad), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-center mono font-semibold", style: { border: `1px solid ${C.line}` } }, fmt(cfPorProducto[p.id])));
    })));
  })())))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-4 flex gap-2 flex-wrap no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(
    Btn,
    {
      disabled: procesandoCierre,
      onClick: () => {
        if (procesandoCierre) return;
        setProcesandoCierre(true);
        const r = aplicarAjustes(activo.id, motivos);
        setMotivos({});
        setResumenCierre(r);
        setActivoId(null);
        setProcesandoCierre(false);
      }
    },
    procesandoCierre ? "Aplicando\u2026" : "Aplicar ajustes al stock"
  ), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setActivoId(null), disabled: procesandoCierre }, "Cerrar sin ajustar"))), verHoja && /* @__PURE__ */ import_react.default.createElement("div", { className: "fixed inset-0 z-50 overflow-y-auto", style: { background: "rgba(20,32,28,0.45)" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "min-h-full p-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "rounded-xl p-4 mx-auto", style: { background: C.surface, maxWidth: 820 } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-3 no-imprimir" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold text-[15px]" }, "Hoja para contar"), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setVerHoja(false), "aria-label": "Cerrar" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 18 }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-2 mb-2 no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => window.print() }, "Imprimir / Guardar PDF"), /* @__PURE__ */ import_react.default.createElement(LinkBtn, { href: `https://wa.me/?text=${encodeURIComponent(textoHojaConteo(productosHoja, proveedores))}` }, /* @__PURE__ */ import_react.default.createElement(MessageCircle, { size: 14 }), " WhatsApp"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setVerHoja(false) }, "Cerrar")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-1.5 mb-4 no-imprimir" }, [
    { id: "total", label: "Todo" },
    { id: "almacen", label: "Trastienda (sin elaborados)" },
    { id: "piso_venta", label: "Piso de venta" }
  ].map((op) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: op.id,
      onClick: () => setAmbitoHoja(op.id),
      className: "text-[12px] font-medium px-3 py-1.5 rounded-full",
      style: {
        background: ambitoHoja === op.id ? C.accent : C.surface,
        color: ambitoHoja === op.id ? "#fff" : C.inkSoft,
        border: `1px solid ${ambitoHoja === op.id ? C.accent : C.line}`
      }
    },
    op.label
  ))), /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end justify-between mb-3 pb-3", style: { borderBottom: `2px solid ${C.accent}` } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[17px] font-semibold" }, "Hoja de conteo de inventario"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, "Fecha: ", (/* @__PURE__ */ new Date()).toLocaleDateString("es-ES", { day: "2-digit", month: "2-digit", year: "numeric" }))), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 62, width: "auto" } })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3", style: { color: C.inkSoft } }, 'Cuenta f\xEDsicamente cada producto y anota la cantidad en la columna "Conteo".'), /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[11.5px]", style: { borderCollapse: "collapse" } }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff", width: 28 } }, "N\xBA"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Descripci\xF3n del producto"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { background: C.chrome, color: "#fff", width: 60 } }, "U/M"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { background: C.chrome, color: "#fff", width: 90 } }, "Conteo"))), /* @__PURE__ */ import_react.default.createElement("tbody", null, (() => {
    let n = 0;
    return agruparPorProveedor(productosHoja, proveedores).map((g) => /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, { key: g.id }, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("td", { colSpan: 4, className: "py-1.5 px-2 font-semibold text-[11.5px]", style: { background: C.accentSoft, border: `1px solid ${C.line}` } }, g.nombre, " \xB7 ", g.items.length, " referencia(s)")), g.items.map((p) => {
      n += 1;
      return /* @__PURE__ */ import_react.default.createElement("tr", { key: p.id, style: { background: n % 2 ? C.bg : C.surface } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 mono", style: { border: `1px solid ${C.line}` } }, n), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2", style: { border: `1px solid ${C.line}` } }, p.nombre, p.codigo && /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: C.inkSoft } }, " \xB7 ", p.codigo)), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-center", style: { border: `1px solid ${C.line}` } }, p.unidad), /* @__PURE__ */ import_react.default.createElement("td", { style: { border: `1px solid ${C.line}`, height: 32, background: "#F0F4F1" } }));
    })));
  })())), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-6 text-[11px] grid grid-cols-2 gap-6", style: { color: C.inkSoft } }, /* @__PURE__ */ import_react.default.createElement("div", null, "Contado por: _________________________"), /* @__PURE__ */ import_react.default.createElement("div", null, "Fecha y firma: _________________________")))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Historial de conteos"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, conteos.filter((c) => c.id !== activoId).length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin conteos anteriores." }) : conteos.filter((c) => c.id !== activoId).map((c) => /* @__PURE__ */ import_react.default.createElement(Card, { key: c.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", null, "Conteo del ", c.fecha, c.ambito === "piso_venta" && /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11px]", style: { color: C.inkSoft } }, " \xB7 piso de venta"), c.ambito === "almacen" && /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11px]", style: { color: C.inkSoft } }, " \xB7 almac\xE9n (trastienda)")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement(Pill, { color: c.completado ? C.accent : C.amber }, c.completado ? "Completado" : "En curso"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setActivoId(c.id) }, "Ver ", /* @__PURE__ */ import_react.default.createElement(ChevronRight, { size: 13 })), eliminarConteo && /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setConfirmarEliminar(c), style: { color: C.red } }, "Eliminar")))))), confirmarEliminar && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmarEliminar(null), title: "Eliminar conteo" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] mb-3" }, "Vas a eliminar el conteo del ", /* @__PURE__ */ import_react.default.createElement("b", null, confirmarEliminar.fecha), confirmarEliminar.ambito && confirmarEliminar.ambito !== "total" && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, " (", confirmarEliminar.ambito === "piso_venta" ? "piso de venta" : "almac\xE9n", ")"), "."), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-4 p-3 rounded-lg", style: { background: C.surfaceSoft, color: C.inkSoft } }, confirmarEliminar.completado ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, "Si este conteo lleg\xF3 a aplicar ajustes al stock (incluidos los traspasos autom\xE1ticos al piso de venta), todo eso se revierte \u2014 con un movimiento nuevo por cada uno, sin borrar ning\xFAn movimiento anterior. El conteo desaparece de este historial.") : "Este conteo no lleg\xF3 a aplicarse, as\xED que no hay ning\xFAn ajuste que revertir \u2014 solo desaparecer\xE1 de la lista."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmarEliminar(null), disabled: procesandoEliminar }, "Cancelar"), /* @__PURE__ */ import_react.default.createElement(
    Btn,
    {
      disabled: procesandoEliminar,
      onClick: () => {
        if (procesandoEliminar) return;
        setProcesandoEliminar(true);
        eliminarConteo(confirmarEliminar.id);
        setConfirmarEliminar(null);
        setProcesandoEliminar(false);
      }
    },
    procesandoEliminar ? "Eliminando\u2026" : "Confirmar eliminaci\xF3n"
  ))));
}
function Reportes({ rotacionPorProducto, gastoPorProveedor, productosSinMovimiento, valorInventario, margenPorProducto = [], patronesDesviacionConteo = [] }) {
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Reportes y rotaci\xF3n"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => window.print(), className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(FileText, { size: 13 }), " Imprimir / Guardar PDF")), /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end justify-between mb-4 pb-3", style: { borderBottom: "2px solid #9C7A34" } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[17px] font-semibold" }, "Reportes y rotaci\xF3n"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: "#6B7A6E" } }, "Generado el ", todayISO())), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 56, width: "auto" } })), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { padding: 0 } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "p-4 pb-2 text-[13px] font-semibold" }, "Qu\xE9 te da m\xE1s beneficio de verdad"), /* @__PURE__ */ import_react.default.createElement("div", { className: "px-4 pb-2 text-[11.5px]", style: { color: C.inkSoft } }, "\xDAltimos 90 d\xEDas. Ordenado por beneficio bruto generado, no solo por % de margen \u2014 un producto con margen alt\xEDsimo pero que apenas se vende importa menos que uno normal que se vende sin parar."), margenPorProducto.length === 0 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "p-4" }, /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no hay ventas registradas en los \xFAltimos 90 d\xEDas." })) : /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", { style: { color: C.inkSoft, borderTop: `1px solid ${C.line}`, borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2 px-4" }, "Producto"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2 px-4" }, "Unidades (90d)"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2 px-4" }, "Margen"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2 px-4" }, "Beneficio (90d)"))), /* @__PURE__ */ import_react.default.createElement("tbody", null, margenPorProducto.map((p, i) => /* @__PURE__ */ import_react.default.createElement("tr", { key: p.id, style: { borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-4" }, p.nombre, i === 0 && /* @__PURE__ */ import_react.default.createElement("span", { className: "ml-1.5", title: "El que m\xE1s beneficio genera" }, "\u{1F3C6}")), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-4 mono" }, fmt(p.unidadesVendidas)), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-4 mono", style: { color: p.margenPct == null ? C.inkSoft : p.margenPct < 20 ? C.red : p.margenPct < 40 ? C.amber : C.accent } }, p.margenPct == null ? "\u2014" : `${fmt(p.margenPct)}%`), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-4 mono font-semibold" }, "\u20AC", fmt(p.beneficio))))))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { padding: 0 } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "p-4 pb-2 text-[13px] font-semibold" }, "Rotaci\xF3n de inventario por producto"), /* @__PURE__ */ import_react.default.createElement("div", { className: "px-4 pb-2 text-[11.5px]", style: { color: C.inkSoft } }, "D\xEDas de stock = existencia actual \xF7 consumo diario real (desde la primera salida registrada de ese producto hasta hoy). Con pocas semanas de historial, el n\xFAmero mejora seg\xFAn vayas registrando m\xE1s ventas."), /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", { style: { color: C.inkSoft, borderTop: `1px solid ${C.line}`, borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2 px-4" }, "Producto"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2 px-4" }, "Salidas registradas"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2 px-4" }, "Consumo diario"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2 px-4" }, "D\xEDas de stock"))), /* @__PURE__ */ import_react.default.createElement("tbody", null, [...rotacionPorProducto].sort((a, b) => (a.dias ?? Infinity) - (b.dias ?? Infinity)).map((p) => /* @__PURE__ */ import_react.default.createElement("tr", { key: p.id, style: { borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-4" }, p.nombre), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-4 mono" }, p.cantSalidas), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-4 mono" }, p.consumoDiario == null ? "\u2014" : fmt(p.consumoDiario), " /d\xEDa"), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-4 mono font-semibold", style: { color: p.dias == null ? C.inkSoft : p.dias < 7 ? C.red : p.dias < 21 ? C.amber : C.accent } }, p.dias == null ? "\u2014" : Math.round(p.dias)))))), rotacionPorProducto.length === 0 && /* @__PURE__ */ import_react.default.createElement(Empty, { text: "A\xF1ade productos para ver su rotaci\xF3n." })), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, "Gasto total por proveedor"), gastoPorProveedor.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "A\xFAn no hay pedidos registrados." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, gastoPorProveedor.map((g, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", null, g.proveedor ? g.proveedor.nombre : "Proveedor eliminado"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-medium" }, "\u20AC", fmt(g.total)))))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, "Productos sin ninguna salida"), productosSinMovimiento.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todos los productos tienen movimiento de salida." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, productosSinMovimiento.map((p) => /* @__PURE__ */ import_react.default.createElement("div", { key: p.id, className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, p.nombre), /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.amber }, "sin rotaci\xF3n"))))), patronesDesviacionConteo.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-1" }, "Desviaciones que se repiten en conteos"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, "Estos productos llevan varios conteos desvi\xE1ndose casi siempre para el mismo lado \u2014 la se\xF1al real de un problema recurrente (merma, receta mal calculada), no de un descuadre suelto."), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, patronesDesviacionConteo.map((p) => /* @__PURE__ */ import_react.default.createElement("div", { key: p.productoId, className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", null, p.nombre), /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft, fontSize: 11 } }, p.numAjustes, " ajustes \xB7 \xFAltimo el ", p.ultimaFecha)), /* @__PURE__ */ import_react.default.createElement(Pill, { color: p.direccion === "falta" ? C.red : C.amber }, p.direccion === "falta" ? "\u2212" : "+", fmt(Math.abs(p.acumulado)), " ", p.unidad, " acumulado"))))))));
}
function fichaVacia() {
  return {
    nombre: "",
    seccion: "Otros",
    productoVinculadoId: "",
    rendimiento: 1,
    componentes: [],
    empaque: "",
    manoObra: "",
    tiempoElaboracionMinutos: "",
    otros: "",
    gastosGenerales: "",
    precioVenta: "",
    margenObjetivo: 70,
    iva: 10,
    redondear: true,
    alergenosExtra: [],
    elaboracion: "",
    tiempoElaboracion: "",
    conservacion: ""
  };
}
function FichasCosto({ fichasCosto, productos, addFichaCosto, updateFichaCosto, deleteFichaCosto, updateProducto, addProducto, ordenesProduccion = [], nominas = [], fichajes = [] }) {
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [filtroSeccion, setFiltroSeccion] = (0, import_react.useState)("todas");
  const [form, setForm] = (0, import_react.useState)(fichaVacia());
  const [error, setError] = (0, import_react.useState)("");
  const [editingId, setEditingId] = (0, import_react.useState)(null);
  const [avisoCreado, setAvisoCreado] = (0, import_react.useState)("");
  const mesActualFichas = todayISO().slice(0, 7);
  const costePorHoraEquipo = costePorHoraEquipoEnMes(nominas, fichajes, mesActualFichas);
  const manoObraSugerida = costePorHoraEquipo !== null && Number(form.tiempoElaboracionMinutos) > 0 ? Number(form.tiempoElaboracionMinutos) / 60 * costePorHoraEquipo : null;
  function addComponente() {
    setForm((f) => ({
      ...f,
      componentes: [...f.componentes, { productoId: "", nombre: "", unidad: "unidad", cantidad: 1, costoUnitario: 0, unidadEntrada: "nativa" }]
    }));
  }
  function updateComponente(idx, campo, valor) {
    setForm((f) => ({
      ...f,
      componentes: f.componentes.map((c, i) => {
        if (i !== idx) return c;
        if (campo === "productoId") {
          const p = productos.find((pr) => pr.id === valor);
          if (p) return { ...c, productoId: valor, nombre: p.nombre, unidad: p.unidad, costoUnitario: p.costo, unidadEntrada: "nativa" };
          return { ...c, productoId: "" };
        }
        return { ...c, [campo]: valor };
      })
    }));
  }
  function removeComponente(idx) {
    setForm((f) => ({ ...f, componentes: f.componentes.filter((_, i) => i !== idx) }));
  }
  function costoDe(c) {
    if (c.productoId) {
      const p = productos.find((pr) => pr.id === c.productoId);
      if (p) return Number(p.costo) || 0;
    }
    return Number(c.costoUnitario) || 0;
  }
  function estaEnlazado(c) {
    return !!(c.productoId && productos.some((pr) => pr.id === c.productoId));
  }
  function productoDe(c) {
    return c.productoId ? productos.find((pr) => pr.id === c.productoId) : null;
  }
  function tieneConversion(c) {
    const p = productoDe(c);
    return !!(p && Number(p.contenido) > 0 && p.unidadContenido);
  }
  function cantidadMostrada(c) {
    const p = productoDe(c);
    if (c.unidadEntrada === "contenido" && p) return (Number(c.cantidad) || 0) * Number(p.contenido);
    return c.cantidad;
  }
  function handleCantidadInput(idx, valorTecleado, c) {
    const p = productoDe(c);
    if (c.unidadEntrada === "contenido" && p && Number(p.contenido) > 0) {
      const enNativa = valorTecleado === "" ? "" : Number(valorTecleado) / Number(p.contenido);
      updateComponente(idx, "cantidad", enNativa);
    } else {
      updateComponente(idx, "cantidad", valorTecleado);
    }
  }
  const enlaceRoto = !!form.productoVinculadoId && !productos.some((p) => p.id === form.productoVinculadoId);
  const costoComponentes = form.componentes.reduce((a, c) => a + (Number(c.cantidad) || 0) * costoDe(c), 0);
  const costoAdicionales = (Number(form.empaque) || 0) + (Number(form.manoObra) || 0) + (Number(form.otros) || 0) + (Number(form.gastosGenerales) || 0);
  const costoTotal = costoComponentes + costoAdicionales;
  const rendimiento = Number(form.rendimiento) || 1;
  const costoPorUnidad = costoTotal / rendimiento;
  const margenObjetivo = Math.min(95, Math.max(0, Number(form.margenObjetivo) || 0));
  const ivaPct = Number(form.iva) || 0;
  const netoSugerido = margenObjetivo < 100 ? costoPorUnidad / (1 - margenObjetivo / 100) : 0;
  const pvpBruto = netoSugerido * (1 + ivaPct / 100);
  const pvpSugerido = form.redondear ? Math.ceil(pvpBruto * 10) / 10 : pvpBruto;
  const pvpFinal = Number(form.precioVenta) || pvpSugerido;
  const netoFinal = ivaPct > 0 ? pvpFinal / (1 + ivaPct / 100) : pvpFinal;
  const precioVentaNum = netoFinal;
  const margen = netoFinal > 0 ? (netoFinal - costoPorUnidad) / netoFinal * 100 : null;
  function resetFicha() {
    setForm(fichaVacia());
    setEditingId(null);
    setError("");
    setAvisoCreado("");
  }
  function openEdit(f) {
    setEditingId(f.id);
    setForm({
      nombre: f.nombre || "",
      seccion: f.seccion || "Otros",
      productoVinculadoId: f.productoVinculadoId || "",
      rendimiento: f.rendimiento || 1,
      componentes: (f.componentes || []).map((c) => ({ ...c })),
      empaque: f.empaque ?? "",
      manoObra: f.manoObra ?? "",
      tiempoElaboracionMinutos: f.tiempoElaboracionMinutos ?? "",
      otros: f.otros ?? "",
      gastosGenerales: f.gastosGenerales ?? "",
      precioVenta: f.precioVenta ?? "",
      margenObjetivo: f.margenObjetivo ?? 70,
      iva: f.iva ?? 10,
      redondear: f.redondear ?? true,
      alergenosExtra: f.alergenosExtra || [],
      elaboracion: f.elaboracion || "",
      tiempoElaboracion: f.tiempoElaboracion || "",
      conservacion: f.conservacion || ""
    });
    setError("");
    setShowForm(true);
  }
  function crearProductoDeVenta() {
    if (!form.nombre.trim()) {
      setError("Escribe primero el nombre de la receta.");
      return;
    }
    const nuevo = {
      codigo: "",
      nombre: form.nombre.trim(),
      categoria: "Elaborados / venta",
      tipo: "elaborado",
      unidad: "unidad",
      costo: Number(costoPorUnidad.toFixed(4)),
      precioVenta: Number(pvpFinal.toFixed(2)),
      ivaVenta: ivaPct,
      stock: 0,
      stockMinimo: 0,
      ubicacion: "",
      proveedorId: "",
      esElaborado: true
    };
    const creado = addProducto(nuevo);
    if (creado && creado.id) {
      setForm((f) => ({ ...f, productoVinculadoId: creado.id }));
    }
    setError("");
    setAvisoCreado(form.nombre.trim());
  }
  function submit() {
    if (!form.nombre.trim()) {
      setError("Escribe el nombre del producto o receta.");
      return;
    }
    if (form.componentes.length === 0) {
      setError("A\xF1ade al menos un ingrediente o insumo.");
      return;
    }
    setError("");
    const datos = { ...form, rendimiento, precioCalculado: netoFinal, pvpConIva: pvpFinal };
    if (editingId) {
      updateFichaCosto(editingId, datos);
    } else {
      addFichaCosto(datos);
    }
    if (form.productoVinculadoId && pvpFinal > 0) {
      updateProducto(form.productoVinculadoId, {
        precioVenta: Number(pvpFinal.toFixed(2)),
        ivaVenta: ivaPct,
        costo: Number(costoPorUnidad.toFixed(4))
      });
    }
    resetFicha();
    setShowForm(false);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
    if (showForm) resetFicha();
    setShowForm((s) => !s);
  } }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nueva ficha de costo") }, "Fichas de costo"), !showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]", style: { color: C.ink } }, "Una ficha de costo desglosa todos los ingredientes o insumos de un producto que vendes, para saber exactamente cu\xE1nto cuesta producirlo y qu\xE9 margen real deja frente a tu precio de venta.")), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, editingId ? "Editando ficha de costo" : "Nueva ficha de costo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre del producto / receta" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.nombre, onChange: (e) => setForm({ ...form, nombre: e.target.value }), placeholder: "Ej. Caja de bombones x12" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Secci\xF3n de la carta" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.seccion, onChange: (e) => setForm({ ...form, seccion: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, SECCIONES_FICHAS.map((s) => /* @__PURE__ */ import_react.default.createElement("option", { key: s, value: s }, s)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "\xBFA qu\xE9 producto del cat\xE1logo corresponde?" }, /* @__PURE__ */ import_react.default.createElement("select", { value: enlaceRoto ? "" : form.productoVinculadoId, onChange: (e) => setForm({ ...form, productoVinculadoId: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${form.productoVinculadoId && !enlaceRoto ? C.line : C.amber}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, enlaceRoto ? "El producto enlazado ya no existe \u2014 elige otro" : "Sin vincular \u2014 no se podr\xE1 producir"), productos.slice().sort((a, b) => {
    const aEs = a.tipo === "elaborado" ? 0 : 1;
    const bEs = b.tipo === "elaborado" ? 0 : 1;
    if (aEs !== bEs) return aEs - bEs;
    return a.nombre.localeCompare(b.nombre, "es");
  }).map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre, p.tipo !== "elaborado" ? " (no es elaborado)" : "")))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Rendimiento (unidades que produce esta receta)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.rendimiento, onChange: (e) => setForm({ ...form, rendimiento: e.target.value }), placeholder: "1" }))), enlaceRoto && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("b", null, "Esta ficha apunta a un producto que ya no existe."), " Puede que se borrara. Elige otro producto en el desplegable de arriba, o usa el bot\xF3n ", /* @__PURE__ */ import_react.default.createElement("b", null, '"Crear producto de venta"'), " para generar uno nuevo a partir de esta receta. Mientras tanto no se puede producir.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "mb-2 text-[12px] font-medium", style: { color: C.inkSoft } }, "Ingredientes / insumos"), form.componentes.map((c, idx) => {
    const enlazado = estaEnlazado(c);
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: idx, className: "mb-3", style: { background: C.bg } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12px] font-semibold" }, "Ingrediente ", idx + 1), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, enlazado && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.accent }, "enlazado al cat\xE1logo"), /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: () => removeComponente(idx), "aria-label": "Quitar componente" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 16, color: C.inkSoft })))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Elegir del cat\xE1logo (recomendado)" }, /* @__PURE__ */ import_react.default.createElement(
      "select",
      {
        value: c.productoId,
        onChange: (e) => updateComponente(idx, "productoId", e.target.value),
        className: "w-full rounded-lg px-3 py-2 text-[13px]",
        style: { border: `1px solid ${C.line}`, background: C.surface }
      },
      /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "\u2014 Escribirlo a mano \u2014"),
      productos.filter((p) => (p.tipo || "materia_prima") === "materia_prima").slice().sort((a, b) => a.nombre.localeCompare(b.nombre, "es")).map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre))
    )), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mb-2", style: { color: C.inkSoft } }, 'Solo salen materias primas. Si aqu\xED ves algo que no es un ingrediente (limpieza, vasos\u2026), ed\xEDtalo en Productos y c\xE1mbiale el tipo \u2014 desaparecer\xE1 de esta lista. El envase va en "Empaque", m\xE1s abajo.'), !enlazado && /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre del ingrediente" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: c.nombre, onChange: (e) => updateComponente(idx, "nombre", e.target.value), placeholder: "Ej. cacao en polvo" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-x-2" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Unidad" }, /* @__PURE__ */ import_react.default.createElement(
      Input,
      {
        value: tieneConversion(c) && c.unidadEntrada === "contenido" ? productoDe(c).unidadContenido : c.unidad,
        onChange: (e) => updateComponente(idx, "unidad", e.target.value),
        readOnly: enlazado,
        style: enlazado ? { background: C.accentSoft } : {}
      }
    )), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Cantidad" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.001", value: cantidadMostrada(c), onChange: (e) => handleCantidadInput(idx, e.target.value, c) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Costo unidad" }, /* @__PURE__ */ import_react.default.createElement(
      Input,
      {
        type: "number",
        step: "0.01",
        value: enlazado ? costoDe(c) : c.costoUnitario,
        onChange: (e) => updateComponente(idx, "costoUnitario", e.target.value),
        readOnly: enlazado,
        style: enlazado ? { background: C.accentSoft, color: C.inkSoft } : {}
      }
    ))), tieneConversion(c) && /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        type: "button",
        onClick: () => updateComponente(idx, "unidadEntrada", c.unidadEntrada === "contenido" ? "nativa" : "contenido"),
        className: "text-[11px] font-medium mt-1.5",
        style: { color: C.accent }
      },
      c.unidadEntrada === "contenido" ? `Poner en ${c.unidad} en vez de ${productoDe(c).unidadContenido}` : `Poner en ${productoDe(c).unidadContenido} en vez de ${c.unidad} (cada ${c.unidad} trae ${productoDe(c).contenido} ${productoDe(c).unidadContenido})`
    ), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between pt-2", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12px]", style: { color: C.inkSoft } }, "Subtotal de este ingrediente"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold text-[13px]" }, "\u20AC", fmt((Number(c.cantidad) || 0) * costoDe(c)))));
  }), /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: addComponente, className: "text-[12.5px] font-medium mb-2", style: { color: C.accent } }, "+ A\xF1adir ingrediente / insumo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-4", style: { color: C.inkSoft } }, "Si eliges el ingrediente del cat\xE1logo, su costo se toma autom\xE1ticamente y se actualiza solo cuando cambies el precio en Productos."), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-4 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Empaque (\u20AC, opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.empaque, onChange: (e) => setForm({ ...form, empaque: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Mano de obra (\u20AC, opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.manoObra, onChange: (e) => setForm({ ...form, manoObra: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tiempo de elaboraci\xF3n (minutos, opcional)" }, /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      type: "number",
      step: "1",
      value: form.tiempoElaboracionMinutos,
      onChange: (e) => setForm({ ...form, tiempoElaboracionMinutos: e.target.value })
    }
  )), manoObraSugerida !== null && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3 -mt-2 flex items-center gap-2", style: { color: C.inkSoft } }, /* @__PURE__ */ import_react.default.createElement("span", null, "A \u20AC", fmt(costePorHoraEquipo), "/h del equipo este mes \u2192 mano de obra sugerida: \u20AC", fmt(manoObraSugerida)), /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      type: "button",
      onClick: () => setForm((f) => ({ ...f, manoObra: manoObraSugerida.toFixed(2) })),
      className: "underline",
      style: { color: C.accent }
    },
    "Usar este valor"
  )), form.tiempoElaboracionMinutos && costePorHoraEquipo === null && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3 -mt-2", style: { color: C.inkSoft } }, "No se puede sugerir mano de obra todav\xEDa: falta registrar n\xF3minas o fichajes de este mes en Coste de Personal.")), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Otros costos (\u20AC, opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.otros, onChange: (e) => setForm({ ...form, otros: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Gastos generales de esta producci\xF3n (\u20AC, opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.gastosGenerales, onChange: (e) => setForm({ ...form, gastosGenerales: e.target.value }), placeholder: "Parte de luz, horno\u2026" }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2 pt-2", style: { borderTop: `1px solid ${C.line}` } }, "Al\xE9rgenos"), /* @__PURE__ */ import_react.default.createElement("div", { className: "rounded-lg p-3 mb-3", style: { background: C.bg, border: `1px solid ${C.line}` } }, (() => {
    const heredados = /* @__PURE__ */ new Set();
    (form.componentes || []).forEach((c) => {
      if (!c.productoId) return;
      const p = productos.find((x) => x.id === c.productoId);
      (p?.alergenos || []).forEach((a) => heredados.add(a));
    });
    const lista = [...heredados];
    return /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2", style: { color: C.inkSoft } }, "Heredados de los ingredientes del cat\xE1logo:"), lista.length === 0 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.inkSoft } }, "Ninguno. Si alg\xFAn ingrediente lleva al\xE9rgenos, m\xE1rcalos en su ficha de producto y aparecer\xE1n aqu\xED solos.") : /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-1.5 mb-3" }, lista.map((id) => {
      const a = alergenoPorId(id);
      return a ? /* @__PURE__ */ import_react.default.createElement("span", { key: id, className: "text-[11.5px] px-2 py-1 rounded-lg", style: { background: C.amberSoft, border: `1px solid ${C.amber}` } }, a.icono, " ", a.nombre) : null;
    })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2 pt-2", style: { color: C.inkSoft, borderTop: `1px solid ${C.line}` } }, "A\xF1adir a mano (ingredientes libres, trazas del obrador\u2026):"), /* @__PURE__ */ import_react.default.createElement(
      SelectorAlergenos,
      {
        seleccionados: form.alergenosExtra || [],
        onCambio: (l) => setForm({ ...form, alergenosExtra: l }),
        nombreParaSugerir: form.nombre
      }
    ));
  })()), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2 pt-2", style: { borderTop: `1px solid ${C.line}` } }, "Ficha t\xE9cnica de elaboraci\xF3n (opcional)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tiempo de elaboraci\xF3n" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.tiempoElaboracion, onChange: (e) => setForm({ ...form, tiempoElaboracion: e.target.value }), placeholder: "20 min" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Conservaci\xF3n" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.conservacion, onChange: (e) => setForm({ ...form, conservacion: e.target.value }), placeholder: "Refrigerado 2 d\xEDas" }))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Pasos de elaboraci\xF3n" }, /* @__PURE__ */ import_react.default.createElement(
    "textarea",
    {
      value: form.elaboracion,
      onChange: (e) => setForm({ ...form, elaboracion: e.target.value }),
      placeholder: "1. Tostar la chapata 2 min\n2. Frotar con tomate rallado\n3. Ali\xF1ar con AOVE y sal",
      className: "w-full rounded-lg p-2 text-[12.5px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink, height: 100, resize: "vertical" }
    }
  )), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3", style: { color: C.inkSoft } }, "Sirve para que cualquiera del equipo elabore el producto igual. Se imprime junto con la ficha."), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2 pt-2", style: { borderTop: `1px solid ${C.line}` } }, "C\xE1lculo del precio de venta"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Margen objetivo (%)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "1", value: form.margenObjetivo, onChange: (e) => setForm({ ...form, margenObjetivo: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "IVA (%)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "1", value: form.iva, onChange: (e) => setForm({ ...form, iva: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Redondeo" }, /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      type: "button",
      onClick: () => setForm({ ...form, redondear: !form.redondear }),
      className: "w-full rounded-lg px-3 py-2 text-[12.5px] font-medium",
      style: {
        border: `1px solid ${form.redondear ? C.accent : C.line}`,
        background: form.redondear ? C.accentSoft : C.surface,
        color: C.ink
      }
    },
    form.redondear ? "A m\xFAltiplos de 0,10" : "Sin redondear"
  ))), /* @__PURE__ */ import_react.default.createElement(Card, { style: { background: C.accentSoft, border: "none" }, className: "mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 md:grid-cols-4 gap-3 text-center mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Costo total"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold text-[15px]" }, "\u20AC", fmt(costoTotal))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Costo por unidad"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold text-[15px]" }, "\u20AC", fmt(costoPorUnidad))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Precio sin IVA"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold text-[15px]" }, "\u20AC", fmt(netoFinal))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Margen real"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold text-[15px]", style: { color: margen == null ? C.ink : margen < 15 ? C.red : C.accent } }, margen == null ? "\u2014" : `${fmt(margen)}%`))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-center pt-3", style: { borderTop: `1px solid rgba(0,0,0,0.08)` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Precio de carta (IVA ", ivaPct, "% incluido)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-bold text-[26px]", style: { color: C.accent } }, "\u20AC", fmt(pvpFinal)))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-1", style: { color: C.inkSoft } }, "Fijar el precio a mano (opcional). Escribe en la casilla que prefieras: la otra se calcula sola."), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio NETO sin IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(
    InputCalc,
    {
      step: "0.01",
      decimales: 2,
      valor: form.precioVenta === "" ? "" : Number(form.precioVenta) / (1 + ivaPct / 100),
      onEscribir: (neto) => {
        if (neto === "") {
          setForm({ ...form, precioVenta: "" });
          return;
        }
        const n = Number(neto);
        if (isNaN(n)) return;
        setForm({ ...form, precioVenta: String(n * (1 + ivaPct / 100)) });
      },
      placeholder: `Sugerido: ${fmt(pvpSugerido / (1 + ivaPct / 100))}`
    }
  )), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio de CARTA con IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      type: "number",
      step: "0.01",
      value: form.precioVenta,
      onChange: (e) => setForm({ ...form, precioVenta: e.target.value }),
      placeholder: `Sugerido: ${fmt(pvpSugerido)}`
    }
  ))), form.precioVenta !== "" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, "Precio fijado a mano: se ignora el margen objetivo y se recalcula el margen real sobre este precio."), form.productoVinculadoId && !enlaceRoto ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.accent } }, "Al guardar, ", /* @__PURE__ */ import_react.default.createElement("b", null, (productos.find((p) => p.id === form.productoVinculadoId) || {}).nombre), " quedar\xE1 con precio de venta \u20AC", fmt(pvpFinal), " (IVA ", ivaPct, "% incluido) y costo \u20AC", fmt(costoPorUnidad), ".") : /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2" }, enlaceRoto ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, "El producto de venta que ten\xEDa esta ficha ", /* @__PURE__ */ import_react.default.createElement("b", null, "ya no existe en el cat\xE1logo"), ". Elige otro en el desplegable de arriba, o crea uno nuevo aqu\xED mismo a partir de esta receta.") : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, "Esta ficha no est\xE1 asociada a ning\xFAn producto del cat\xE1logo, as\xED que el precio no se guardar\xE1 en ninguna parte. Los ingredientes de arriba no llevan precio de venta: quien lo lleva es el ", /* @__PURE__ */ import_react.default.createElement("b", null, "producto terminado"), " que vendes al cliente.")), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: crearProductoDeVenta, variant: "primary" }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 13 }), ' Crear "', form.nombre || "producto", '" como producto de venta')), avisoCreado && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2 p-2 rounded-lg", style: { background: C.accentSoft, color: C.ink } }, 'Producto "', avisoCreado, '" creado en el cat\xE1logo con su costo y precio de venta, y ya enlazado con esta ficha. ', /* @__PURE__ */ import_react.default.createElement("b", null, "Guarda la ficha ahora"), " para que el enlace quede fijo \u2014 si sales sin guardar, se pierde."), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, editingId ? "Guardar cambios" : "Guardar ficha de costo"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowForm(false);
    resetFicha();
  } }, "Cancelar"))), fichasCosto.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has creado ninguna ficha de costo." }) : (() => {
    const seccionesConFichas = SECCIONES_FICHAS.filter((s) => fichasCosto.some((f) => (f.seccion || "Otros") === s));
    const fichasFiltradas = filtroSeccion === "todas" ? fichasCosto : fichasCosto.filter((f) => (f.seccion || "Otros") === filtroSeccion);
    const grupos = filtroSeccion !== "todas" ? [{ nombre: filtroSeccion, fichas: fichasFiltradas }] : seccionesConFichas.map((s) => ({ nombre: s, fichas: fichasCosto.filter((f) => (f.seccion || "Otros") === s) }));
    return /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-3 overflow-x-auto" }, /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        onClick: () => setFiltroSeccion("todas"),
        className: "text-[12px] font-medium px-3 py-1.5 rounded-full whitespace-nowrap",
        style: {
          background: filtroSeccion === "todas" ? C.chrome : C.surface,
          color: filtroSeccion === "todas" ? "#fff" : C.inkSoft,
          border: `1px solid ${filtroSeccion === "todas" ? C.chrome : C.line}`
        }
      },
      "Todas (",
      fichasCosto.length,
      ")"
    ), seccionesConFichas.map((s) => /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: s,
        onClick: () => setFiltroSeccion(s),
        className: "text-[12px] font-medium px-3 py-1.5 rounded-full whitespace-nowrap",
        style: {
          background: filtroSeccion === s ? C.accent : C.surface,
          color: filtroSeccion === s ? "#fff" : C.inkSoft,
          border: `1px solid ${filtroSeccion === s ? C.accent : C.line}`
        }
      },
      s,
      " (",
      fichasCosto.filter((f) => (f.seccion || "Otros") === s).length,
      ")"
    ))), grupos.map((grupo) => /* @__PURE__ */ import_react.default.createElement("div", { key: grupo.nombre, className: "mb-5" }, filtroSeccion === "todas" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2 uppercase tracking-wide", style: { color: C.inkSoft } }, grupo.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-3" }, grupo.fichas.map((f) => {
      const costoComp = f.componentes.reduce((a, c) => a + (Number(c.cantidad) || 0) * costoDe(c), 0);
      const costoAdi = (Number(f.empaque) || 0) + (Number(f.manoObra) || 0) + (Number(f.otros) || 0) + (Number(f.gastosGenerales) || 0);
      const total = costoComp + costoAdi;
      const rend = Number(f.rendimiento) || 1;
      const porUnidad = total / rend;
      const pv = Number(f.precioVenta) || Number(f.precioCalculado) || 0;
      const marg = pv > 0 ? (pv - porUnidad) / pv * 100 : null;
      const ordenesDeFicha = ordenesProduccion.filter((o) => o.fichaId === f.id);
      let costoRealMedio = null;
      let diferenciaPct = null;
      if (ordenesDeFicha.length > 0) {
        const totalUnidades = ordenesDeFicha.reduce((a, o) => a + (Number(o.unidadesBuenas) || 0), 0);
        const totalCoste = ordenesDeFicha.reduce((a, o) => a + (Number(o.costoUnitarioLote) || 0) * (Number(o.unidadesBuenas) || 0), 0);
        if (totalUnidades > 0) {
          costoRealMedio = totalCoste / totalUnidades;
          diferenciaPct = porUnidad > 0 ? (costoRealMedio - porUnidad) / porUnidad * 100 : null;
        }
      }
      return /* @__PURE__ */ import_react.default.createElement(Card, { key: f.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold" }, f.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Rinde ", rend, " unidad(es)"), f.productoVinculadoId && !productos.some((p) => p.id === f.productoVinculadoId) && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: C.amber } }, "\u26A0 El producto enlazado ya no existe \u2014 no se puede producir hasta arreglarlo"), !f.productoVinculadoId && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: C.amber } }, "\u26A0 Sin producto de venta enlazado \u2014 no se puede producir")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => openEdit(f) }, "Editar"), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => deleteFichaCosto(f.id), "aria-label": "Eliminar ficha de costo" }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 15, color: C.inkSoft })))), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1 mb-2" }, f.componentes.map((c, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, c.nombre, " ", /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, "(", fmt(cantidadMostrada(c)), " ", tieneConversion(c) && c.unidadEntrada === "contenido" ? productoDe(c).unidadContenido : c.unidad, ")"), estaEnlazado(c) && /* @__PURE__ */ import_react.default.createElement("span", { className: "ml-1", style: { color: C.accent }, title: "Costo enlazado al cat\xE1logo" }, "\u2022")), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, "\u20AC", fmt((Number(c.cantidad) || 0) * costoDe(c))))), costoAdi > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, "Costos adicionales"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, "\u20AC", fmt(costoAdi)))), /* @__PURE__ */ import_react.default.createElement("div", { className: "pt-2 flex items-center justify-between text-[13px]", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("span", { className: "font-medium" }, "Costo por unidad"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold" }, "\u20AC", fmt(porUnidad))), costoRealMedio !== null && /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-1.5 p-2 rounded-lg text-[11.5px]", style: { background: Math.abs(diferenciaPct) > 15 ? C.amberSoft : C.accentSoft } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, "Coste real medio (", ordenesDeFicha.length, " producci\xF3n/es)"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold", style: { color: Math.abs(diferenciaPct) > 15 ? C.amber : C.accent } }, "\u20AC", fmt(costoRealMedio))), Math.abs(diferenciaPct) > 15 && /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-0.5", style: { color: C.inkSoft } }, diferenciaPct > 0 ? `Te est\xE1 saliendo un ${fmt(diferenciaPct)}% m\xE1s caro de lo que dice la ficha \u2014 revisa las mermas o actualiza la receta.` : `Te est\xE1 saliendo un ${fmt(Math.abs(diferenciaPct))}% m\xE1s barato de lo que dice la ficha.`)), pv > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[13px] mt-1" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "font-medium" }, "Margen sobre \u20AC", fmt(pv)), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold", style: { color: marg < 15 ? C.red : C.accent } }, fmt(marg), "%")), f.pvpConIva > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[13px] mt-2 pt-2", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("span", { className: "font-medium" }, "Precio de carta (IVA incl.)"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-bold text-[15px]", style: { color: C.accent } }, "\u20AC", fmt(f.pvpConIva))), (() => {
        const set = new Set(f.alergenosExtra || []);
        (f.componentes || []).forEach((c) => {
          if (!c.productoId) return;
          const p = productos.find((x) => x.id === c.productoId);
          (p?.alergenos || []).forEach((a) => set.add(a));
        });
        const lista = [...set];
        if (lista.length === 0) return null;
        return /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 pt-2", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-1", style: { color: C.inkSoft } }, "Al\xE9rgenos"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-1" }, lista.map((id) => {
          const a = alergenoPorId(id);
          return a ? /* @__PURE__ */ import_react.default.createElement("span", { key: id, className: "text-[11px]", title: a.nombre }, a.icono) : null;
        })));
      })(), (f.tiempoElaboracion || f.conservacion) && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-2", style: { color: C.inkSoft } }, f.tiempoElaboracion && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, "\u23F1 ", f.tiempoElaboracion), f.tiempoElaboracion && f.conservacion && " \xB7 ", f.conservacion && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, "\u2744 ", f.conservacion)));
    })))));
  })());
}
function Respaldos({ historial, crearPuntoDeGuardado, restaurarDesdeHistorial, abrirRespaldo, abrirRestaurar, exportarExcelGeneral, pinPropietario, establecerPin, activarModoEmpleado }) {
  const [nuevoPin, setNuevoPin] = (0, import_react.useState)("");
  const [confirmarPin, setConfirmarPin] = (0, import_react.useState)("");
  const [pinError, setPinError] = (0, import_react.useState)("");
  const [pinGuardado, setPinGuardado] = (0, import_react.useState)(false);
  async function guardarPin() {
    if (!/^\d{4,6}$/.test(nuevoPin)) {
      setPinError("El PIN debe tener entre 4 y 6 d\xEDgitos.");
      return;
    }
    if (nuevoPin !== confirmarPin) {
      setPinError("Los dos PIN no coinciden.");
      return;
    }
    setPinError("");
    await establecerPin(nuevoPin);
    setNuevoPin("");
    setConfirmarPin("");
    setPinGuardado(true);
    setTimeout(() => setPinGuardado(false), 3e3);
  }
  const [guardado, setGuardado] = (0, import_react.useState)(false);
  function guardarAhora() {
    crearPuntoDeGuardado();
    setGuardado(true);
    setTimeout(() => setGuardado(false), 2e3);
  }
  function contar(data) {
    return (data.proveedores?.length || 0) + (data.productos?.length || 0) + (data.pedidos?.length || 0);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Respaldos"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]", style: { color: C.ink } }, "Todo lo que capturas en este programa ya se guarda autom\xE1ticamente en tu cuenta, no solo en este tel\xE9fono \u2014 si abres esta misma conversaci\xF3n desde otro dispositivo, ver\xE1s la misma informaci\xF3n. Aqu\xED adem\xE1s puedes crear copias de seguridad puntuales, para poder volver atr\xE1s si algo se borra o se modifica por error.")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold text-[14px]" }, "Crear copia de seguridad ahora"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, "Guarda el estado actual de todo el programa como un punto al que podr\xE1s volver.")), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: guardarAhora }, /* @__PURE__ */ import_react.default.createElement(RotateCcwClock, { size: 14 }), " ", guardado ? "\xA1Guardada!" : "Guardar copia"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Historial de copias guardadas"), historial.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no hay copias de seguridad guardadas." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2 mb-6" }, historial.map((h) => /* @__PURE__ */ import_react.default.createElement(Card, { key: h.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-medium" }, new Date(h.fecha).toLocaleString("es-ES", { dateStyle: "medium", timeStyle: "short" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, h.automatica ? "Copia autom\xE1tica de inicio de sesi\xF3n" : "Copia manual", " \xB7 ", contar(h.data), " registros principales")), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => restaurarDesdeHistorial(h) }, "Restaurar esta copia"))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Copia port\xE1til (para guardar fuera del programa)"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3", style: { color: C.inkSoft } }, "Si quieres una copia que puedas guardar t\xFA mismo fuera de aqu\xED (en tus notas, correo, etc.), puedes verla como texto y copiarla, o pegar una que hayas guardado antes."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: abrirRespaldo }, /* @__PURE__ */ import_react.default.createElement(Download, { size: 14 }), " Ver / copiar respaldo"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: abrirRestaurar }, /* @__PURE__ */ import_react.default.createElement(Upload, { size: 14 }), " Restaurar desde texto"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Para tu gestor\xEDa"), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3", style: { color: C.inkSoft } }, "Un Excel con productos, movimientos y albaranes, listo para mandar. Para el resumen de IVA trimestral concreto, mejor usa el bot\xF3n de exportar dentro de ", /* @__PURE__ */ import_react.default.createElement("b", null, "Libro de IVA"), ", que ya viene calculado."), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: exportarExcelGeneral }, /* @__PURE__ */ import_react.default.createElement(Download, { size: 14 }), " Exportar datos a Excel")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2 mt-4", style: { color: C.inkSoft } }, "Acceso y modo empleado"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Esto son c\xF3digos guardados en este tel\xE9fono, no cuentas de usuario reales: sirven para que un empleado no vea m\xE1rgenes, sueldos ni resultados si coge el m\xF3vil, y para que la Auditor\xEDa diga qui\xE9n hizo qu\xE9 \u2014 pero no protegen los datos frente a alguien con acceso t\xE9cnico al dispositivo. En modo empleado solo quedan visibles: Panel general, Venta r\xE1pida, Registro horario, Recepci\xF3n, Inventario ciego, Control sanitario, Mapa de almac\xE9n y Traspasos.")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-2" }, pinPropietario ? "Cambiar tu PIN de propietario" : "Crear tu PIN de propietario"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.inkSoft } }, "Este es el tuyo, el del due\xF1o \u2014 hace falta para volver a modo propietario cuando alguien est\xE9 en modo empleado."), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "PIN (4 a 6 d\xEDgitos)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "password", inputMode: "numeric", value: nuevoPin, onChange: (e) => setNuevoPin(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Repite el PIN" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "password", inputMode: "numeric", value: confirmarPin, onChange: (e) => setConfirmarPin(e.target.value) }))), pinError && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, pinError), pinGuardado && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.accent } }, "PIN guardado."), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: guardarPin }, pinPropietario ? "Cambiar PIN" : "Guardar PIN")), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-2" }, "C\xF3mo entra cada empleado"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, "Ponle a cada empleado su propio c\xF3digo en ", /* @__PURE__ */ import_react.default.createElement("b", null, "Personal"), " (al editar su ficha). Con eso, cualquiera puede tocar el bot\xF3n ", /* @__PURE__ */ import_react.default.createElement("b", null, '"\u{1F513} Modo empleado"'), " junto al men\xFA, elegir su nombre y entrar con su c\xF3digo \u2014 sin que t\xFA tengas que hacerlo por ellos.")));
}
function SaldoAlmacen({ productos, proveedores, valorInventario, valorUtillaje = 0, proveedorPorId, clasificacionABC, analisisABC }) {
  const [vista, setVista] = (0, import_react.useState)("saldo");
  const [ubicacion, setUbicacion] = (0, import_react.useState)("todo");
  const [copiado, setCopiado] = (0, import_react.useState)(false);
  const fecha = (/* @__PURE__ */ new Date()).toLocaleDateString("es-ES", { day: "2-digit", month: "2-digit", year: "numeric" });
  const esVendible = (p) => p.tipo === "elaborado" || Number(p.precioVenta) > 0;
  const productosVista = (0, import_react.useMemo)(() => {
    if (ubicacion === "piso") {
      return productos.filter(esVendible).map((p) => ({ ...p, stock: Number(p.stockPisoVenta) || 0 }));
    }
    if (ubicacion === "almacen") {
      return productos.filter((p) => p.tipo !== "elaborado").map(
        (p) => esVendible(p) ? { ...p, stock: Math.max(0, (Number(p.stock) || 0) - (Number(p.stockPisoVenta) || 0)) } : p
      );
    }
    return productos;
  }, [productos, ubicacion]);
  const valorVista = productosVista.reduce((a, p) => a + (Number(p.stock) || 0) * (Number(p.costo) || 0), 0);
  const grupos = agruparPorProveedor(productosVista, proveedores || []);
  const totalUnidades = productosVista.reduce((a, p) => a + (Number(p.stock) || 0), 0);
  const tituloUbicacion = ubicacion === "piso" ? "Piso de venta" : ubicacion === "almacen" ? "Almac\xE9n (trastienda)" : "Total";
  function textoSaldo() {
    const bloques = grupos.map((g) => {
      const sub = g.items.reduce((a, p) => a + (Number(p.stock) || 0) * (Number(p.costo) || 0), 0);
      const lineas = g.items.map((p) => {
        const importe = (Number(p.stock) || 0) * (Number(p.costo) || 0);
        return `${p.nombre} | ${p.unidad} | ${p.stock} | \u20AC${fmt(importe)}`;
      });
      return `--- ${g.nombre.toUpperCase()} ---
${lineas.join("\n")}
Subtotal: \u20AC${fmt(sub)}`;
    });
    return `SALDO DE ALMAC\xC9N (${tituloUbicacion}) \u2014 ${fecha}

${bloques.join("\n\n")}

TOTAL: \u20AC${fmt(valorVista)}`;
  }
  function textoConteo() {
    return textoHojaConteo(productosVista, proveedores);
  }
  function copiar() {
    const texto = vista === "saldo" ? textoSaldo() : textoConteo();
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(texto).then(() => setCopiado(true)).catch(() => setCopiado(false));
    }
  }
  const whatsappUrl = `https://wa.me/?text=${encodeURIComponent(vista === "saldo" ? textoSaldo() : textoConteo())}`;
  const correoUrl = `mailto:?subject=${encodeURIComponent(vista === "saldo" ? `Saldo de almac\xE9n ${fecha}` : `Hoja de conteo ${fecha}`)}&body=${encodeURIComponent(vista === "saldo" ? textoSaldo() : textoConteo())}`;
  const thStyle = { color: "#fff", background: C.chrome, fontWeight: 600 };
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Saldo de almac\xE9n"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: vista === "saldo" ? "primary" : "ghost", onClick: () => setVista("saldo") }, "Saldo valorado"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: vista === "conteo" ? "primary" : "ghost", onClick: () => setVista("conteo") }, "Hoja de conteo")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-4" }, [
    { id: "todo", label: "Total" },
    { id: "piso", label: "Piso de venta" },
    { id: "almacen", label: "Almac\xE9n (trastienda)" }
  ].map((o) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: o.id,
      onClick: () => setUbicacion(o.id),
      className: "text-[12px] font-medium px-3 py-1.5 rounded-full whitespace-nowrap",
      style: {
        background: ubicacion === o.id ? C.chrome : C.surface,
        color: ubicacion === o.id ? "#fff" : C.inkSoft,
        border: `1px solid ${ubicacion === o.id ? C.chrome : C.line}`
      }
    },
    o.label
  ))), vista === "saldo" && /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Referencias"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, productosVista.length)), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Unidades totales"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, fmt(totalUnidades))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Valor \u2014 ", tituloUbicacion), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1", style: { color: C.accent } }, "\u20AC", fmt(valorVista)), ubicacion === "todo" && valorUtillaje > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-0.5", style: { color: C.inkSoft } }, "+ \u20AC", fmt(valorUtillaje), " en utillaje y limpieza"))), vista === "saldo" && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-1" }, "Clasificaci\xF3n ABC y frecuencia de conteo sugerida"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] space-y-0.5", style: { color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("div", null, "Se calcula por ", /* @__PURE__ */ import_react.default.createElement("b", null, "valor de consumo"), ": lo que ha salido en los \xFAltimos 12 meses \xD7 su costo. Un producto barato que rota mucho pesa m\xE1s que uno caro parado."), /* @__PURE__ */ import_react.default.createElement("div", { className: "pt-1" }, /* @__PURE__ */ import_react.default.createElement("b", null, "A"), " \u2014 mueven el 80% del dinero. Cu\xE9ntalos ", /* @__PURE__ */ import_react.default.createElement("b", null, "cada mes"), "."), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("b", null, "B"), " \u2014 hasta el 95%. Cu\xE9ntalos ", /* @__PURE__ */ import_react.default.createElement("b", null, "cada trimestre"), "."), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("b", null, "C"), " \u2014 el resto. Cu\xE9ntalos ", /* @__PURE__ */ import_react.default.createElement("b", null, "una o dos veces al a\xF1o"), ".")), analisisABC && !analisisABC.baseConsumo && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mt-2 pt-2", style: { color: C.amber, borderTop: `1px solid rgba(0,0,0,0.08)` } }, "Todav\xEDa no hay salidas registradas, as\xED que de momento se clasifica por valor en stock. En cuanto empieces a registrar salidas, el ABC pasar\xE1 a medir consumo real.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-2 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => window.print() }, "Imprimir / Guardar PDF"), /* @__PURE__ */ import_react.default.createElement(LinkBtn, { href: whatsappUrl }, /* @__PURE__ */ import_react.default.createElement(MessageCircle, { size: 14 }), " WhatsApp"), /* @__PURE__ */ import_react.default.createElement(LinkBtn, { href: correoUrl, newTab: false }, /* @__PURE__ */ import_react.default.createElement(Mail, { size: 14 }), " Correo"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: copiar }, copiado ? "\xA1Copiado!" : "Copiar texto"))), productosVista.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No hay productos en esta ubicaci\xF3n todav\xEDa." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end justify-between mb-3 pb-3", style: { borderBottom: `2px solid ${C.accent}` } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[17px] font-semibold" }, vista === "saldo" ? "Saldo de almac\xE9n" : "Hoja de conteo de inventario", " \u2014 ", tituloUbicacion), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, "Fecha: ", fecha)), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 62, width: "auto" } })), vista === "conteo" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3", style: { color: C.inkSoft } }, 'Cuenta f\xEDsicamente cada producto y anota la cantidad en la columna "Conteo". No se muestra la existencia del sistema.'), /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[11.5px]", style: { borderCollapse: "collapse" } }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { ...thStyle, width: 28 } }, "N\xBA"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: thStyle }, "Descripci\xF3n del producto"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { ...thStyle, width: 52 } }, "U/M"), vista === "saldo" ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-right", style: { ...thStyle, width: 70 } }, "Existencia"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-right", style: { ...thStyle, width: 70 } }, "Costo"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-right", style: { ...thStyle, width: 80 } }, "Importe"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-right", style: { ...thStyle, width: 66 } }, "Salidas 12m"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { ...thStyle, width: 34 } }, "ABC")) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { ...thStyle, width: 90 } }, "Conteo")))), /* @__PURE__ */ import_react.default.createElement("tbody", null, (() => {
    let n = 0;
    const cols = vista === "saldo" ? 8 : 4;
    return grupos.map((g) => {
      const sub = g.items.reduce((a, p) => a + (Number(p.stock) || 0) * (Number(p.costo) || 0), 0);
      return /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, { key: g.id }, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("td", { colSpan: cols, className: "py-1.5 px-2 font-semibold text-[11.5px]", style: { background: C.accentSoft, border: `1px solid ${C.line}` } }, g.nombre, " \xB7 ", g.items.length, " referencia(s)", vista === "saldo" && /* @__PURE__ */ import_react.default.createElement("span", { className: "mono float-right" }, "\u20AC", fmt(sub)))), g.items.map((p) => {
        const importe = (Number(p.stock) || 0) * (Number(p.costo) || 0);
        n += 1;
        const i = n;
        return /* @__PURE__ */ import_react.default.createElement("tr", { key: p.id, style: { background: i % 2 ? C.bg : "#fff" } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 mono", style: { border: `1px solid ${C.line}` } }, i), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2", style: { border: `1px solid ${C.line}` } }, p.nombre, p.codigo && /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: C.inkSoft } }, " \xB7 ", p.codigo)), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-center", style: { border: `1px solid ${C.line}` } }, p.unidad), vista === "saldo" ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-right mono", style: { border: `1px solid ${C.line}` } }, p.stock), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-right mono", style: { border: `1px solid ${C.line}` } }, "\u20AC", fmt(p.costo)), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-right mono font-semibold", style: { border: `1px solid ${C.line}` } }, "\u20AC", fmt(importe)), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-right mono", style: { border: `1px solid ${C.line}` } }, analisisABC && analisisABC.detalle[p.id] ? analisisABC.detalle[p.id].unidades : 0), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-center mono font-semibold", style: { border: `1px solid ${C.line}` } }, esNoMercancia(p) ? "\u2014" : clasificacionABC[p.id] || "C")) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("td", { style: { border: `1px solid ${C.line}`, height: 32, background: "#F0F4F1" } })));
      }));
    });
  })(), vista === "saldo" && /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("td", { colSpan: 5, className: "py-2 px-2 text-right font-semibold", style: { border: `1px solid ${C.line}`, background: C.accentSoft } }, "TOTAL VALOR \u2014 ", tituloUbicacion.toUpperCase()), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 text-right mono font-semibold", style: { border: `1px solid ${C.line}`, background: C.accentSoft } }, "\u20AC", fmt(valorVista)), /* @__PURE__ */ import_react.default.createElement("td", { style: { border: `1px solid ${C.line}`, background: C.accentSoft } }), /* @__PURE__ */ import_react.default.createElement("td", { style: { border: `1px solid ${C.line}`, background: C.accentSoft } })))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-6 text-[11px]", style: { color: C.inkSoft } }, vista === "conteo" ? /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-6" }, /* @__PURE__ */ import_react.default.createElement("div", null, "Contado por: _________________________"), /* @__PURE__ */ import_react.default.createElement("div", null, "Fecha y firma: _________________________")) : /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-6" }, /* @__PURE__ */ import_react.default.createElement("div", null, "Responsable de almac\xE9n: _________________________"), /* @__PURE__ */ import_react.default.createElement("div", null, "Fecha y firma: _________________________")))));
}
function numeroEs(txt) {
  if (txt == null) return null;
  const limpio = String(txt).trim().replace(/\s/g, "").replace(/\./g, "").replace(",", ".");
  if (!/^-?\d+(\.\d+)?$/.test(limpio)) return null;
  return parseFloat(limpio);
}
function analizarTextoAlbaran(texto) {
  const lineas = String(texto || "").split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  const resultado = { lineas: [], numero: "", fecha: "", avisos: [] };
  for (const l of lineas.slice(0, 12)) {
    if (!resultado.numero) {
      const m = l.match(/(?:albar[áa]n|n[ºo°]\.?|numero|número)\s*:?\s*([A-Z0-9\-\/]{3,})/i);
      if (m) resultado.numero = m[1];
    }
    if (!resultado.fecha) {
      const m = l.match(/(\d{1,2})[\/\-.](\d{1,2})[\/\-.](\d{2,4})/);
      if (m) {
        let [, d, mes, a] = m;
        if (a.length === 2) a = "20" + a;
        resultado.fecha = `${a}-${String(mes).padStart(2, "0")}-${String(d).padStart(2, "0")}`;
      }
    }
  }
  for (const l of lineas) {
    if (/^(total|subtotal|base|iva|suma|importe total|descripci[óo]n|c[óo]digo)/i.test(l)) continue;
    let campos = l.split(/\t|;|\s{2,}/).map((c) => c.trim()).filter(Boolean);
    if (campos.length < 2) {
      campos = l.split(/\s+/).filter(Boolean);
      if (campos.length < 3) continue;
    }
    const numeros = [];
    let i = campos.length - 1;
    while (i >= 0 && numeros.length < 4) {
      const n = numeroEs(campos[i].replace(/[€%]/g, ""));
      if (n === null) break;
      numeros.unshift({ valor: n, pos: i });
      i--;
    }
    if (numeros.length < 2) continue;
    const descripcionCampos = campos.slice(0, numeros[0].pos);
    if (descripcionCampos.length === 0) continue;
    let codigo = "";
    let descripcion = descripcionCampos.join(" ");
    if (descripcionCampos.length > 1 && /^[A-Z0-9][A-Z0-9\-\.\/]{2,}$/i.test(descripcionCampos[0]) && /\d/.test(descripcionCampos[0])) {
      codigo = descripcionCampos[0];
      descripcion = descripcionCampos.slice(1).join(" ");
    }
    if (!descripcion || descripcion.length < 2) continue;
    let cantidad = null, precio = null, importe = null;
    const vals = numeros.map((n) => n.valor);
    if (vals.length >= 3) {
      cantidad = vals[0];
      precio = vals[1];
      importe = vals[vals.length - 1];
      if (importe > 0 && Math.abs(cantidad * precio - importe) / importe > 0.15) {
        resultado.avisos.push(`"${descripcion.slice(0, 30)}": los n\xFAmeros no cuadran, rev\xEDsala`);
      }
    } else {
      cantidad = vals[0];
      precio = vals[1];
    }
    resultado.lineas.push({ codigo, descripcion, cantidad, precio, importe });
  }
  return resultado;
}
function lineaVacia() {
  return {
    codigoProveedor: "",
    descripcion: "",
    unidad: "unidad",
    tipoUnidad: "unidad",
    // "unidad" | "peso"
    cantidad: "",
    udsPorCaja: 1,
    precioPor: "bulto",
    // "bulto" = precio de la caja | "unidad" = precio de cada pieza
    precioBruto: "",
    dtoPct: "",
    importe: "",
    canon: "",
    // Punto Verde, SIG, canon de envases: suma a la base de ESTA línea
    ivaPct: 10,
    lote: "",
    caducidad: "",
    contenido: "",
    unidadContenido: "",
    productoId: ""
  };
}
function Albaranes({
  albaranes,
  proveedores,
  productos,
  proveedorPorId,
  buscarEnCatalogo,
  guardarAlbaran,
  eliminarAlbaran,
  confirmarAlbaran,
  anularAlbaran,
  marcarPagada,
  duplicadosDe,
  desviacionesDePrecio,
  prefill,
  limpiarPrefill,
  pedidoParaFotoIA,
  limpiarPedidoParaFotoIA,
  pedidos = []
}) {
  const [modo, setModo] = (0, import_react.useState)("lista");
  const [alb, setAlb] = (0, import_react.useState)(null);
  const [error, setError] = (0, import_react.useState)("");
  const [avisos, setAvisos] = (0, import_react.useState)(null);
  const [filtroMes, setFiltroMes] = (0, import_react.useState)("");
  const [mostrarPegar, setMostrarPegar] = (0, import_react.useState)(false);
  const [textoPegado, setTextoPegado] = (0, import_react.useState)("");
  const [proveedorTexto, setProveedorTexto] = (0, import_react.useState)("");
  const [errorPegado, setErrorPegado] = (0, import_react.useState)("");
  const [avisosPegado, setAvisosPegado] = (0, import_react.useState)([]);
  const [mostrarIA, setMostrarIA] = (0, import_react.useState)(false);
  const [fotosIA, setFotosIA] = (0, import_react.useState)([]);
  const [proveedorIA, setProveedorIA] = (0, import_react.useState)("");
  const [cargandoIA, setCargandoIA] = (0, import_react.useState)(false);
  const [errorIA, setErrorIA] = (0, import_react.useState)("");
  const [pedidoIALigado, setPedidoIALigado] = (0, import_react.useState)(null);
  const [confianzaIA, setConfianzaIA] = (0, import_react.useState)(null);
  const [avisosIA, setAvisosIA] = (0, import_react.useState)([]);
  const [fotoRevisionIA, setFotoRevisionIA] = (0, import_react.useState)("");
  const duplicados = (0, import_react.useMemo)(
    () => alb && duplicadosDe ? duplicadosDe(alb) : { albaran: null, factura: null },
    [alb, duplicadosDe]
  );
  const desviaciones = (0, import_react.useMemo)(
    () => alb && desviacionesDePrecio ? desviacionesDePrecio(alb) : [],
    [alb, desviacionesDePrecio]
  );
  const [confirmarId, setConfirmarId] = (0, import_react.useState)(null);
  const [anularId, setAnularId] = (0, import_react.useState)(null);
  const [procesandoEntrada, setProcesandoEntrada] = (0, import_react.useState)(false);
  (0, import_react.useEffect)(() => {
    if (prefill) {
      setAlb(prefill);
      setModo("editor");
      setError("");
      limpiarPrefill();
    }
  }, [prefill]);
  (0, import_react.useEffect)(() => {
    if (pedidoParaFotoIA) {
      setModo("lista");
      setPedidoIALigado(pedidoParaFotoIA);
      setProveedorIA(pedidoParaFotoIA.proveedorId);
      setFotosIA([]);
      setErrorIA("");
      setAvisosIA([]);
      setMostrarIA(true);
      limpiarPedidoParaFotoIA();
    }
  }, [pedidoParaFotoIA]);
  function crearDesdeTextoPegado() {
    const prov = proveedorTexto || (proveedores[0] ? proveedores[0].id : "");
    if (!prov) {
      setErrorPegado("Primero da de alta un proveedor.");
      return;
    }
    const r = analizarTextoAlbaran(textoPegado);
    if (r.lineas.length === 0) {
      setErrorPegado("No he reconocido ninguna l\xEDnea de producto. Comprueba que has copiado la tabla del albar\xE1n, no solo la cabecera.");
      return;
    }
    const lineas = r.lineas.map((l) => {
      const base = lineaVacia();
      const aprendido = buscarEnCatalogo(prov, l.codigo, l.descripcion);
      return {
        ...base,
        codigoProveedor: l.codigo || "",
        descripcion: l.descripcion,
        cantidad: l.cantidad != null ? l.cantidad : "",
        precioBruto: l.precio != null ? l.precio : "",
        importe: l.importe != null ? l.importe : "",
        // Si ya se enlazó antes este código con un producto, se recupera todo
        // lo aprendido: producto, unidad, uds. por caja, IVA…
        ...aprendido || {},
        productoId: aprendido && aprendido.productoId ? aprendido.productoId : ""
      };
    });
    setAlb({
      id: uid(),
      proveedorId: prov,
      numero: r.numero || "",
      fecha: r.fecha || todayISO(),
      totalPapel: "",
      cargos: "",
      cargosConcepto: "",
      cargosIva: 21,
      esFactura: true,
      numeroFactura: "",
      fechaFactura: r.fecha || todayISO(),
      pagada: false,
      fechaPago: "",
      lineas,
      estado: "borrador"
    });
    setAvisosPegado(r.avisos);
    setMostrarPegar(false);
    setTextoPegado("");
    setErrorPegado("");
    setError("");
    setModo("editor");
  }
  async function agregarFotosIA(archivos) {
    setErrorIA("");
    const validos = Array.from(archivos).filter((f) => f.type.startsWith("image/"));
    if (validos.length === 0) {
      setErrorIA("Solo se admiten fotos (jpg, png). Los PDF, hazles una foto de pantalla o de la hoja impresa.");
      return;
    }
    try {
      const nuevas = await Promise.all(validos.map(archivoABase64));
      setFotosIA((s) => [...s, ...nuevas]);
    } catch {
      setErrorIA("No se ha podido leer ese archivo. Prueba con otra foto.");
    }
  }
  function quitarFotoIA(idx) {
    setFotosIA((s) => s.filter((_, i) => i !== idx));
  }
  async function importarConIA() {
    if (fotosIA.length === 0) {
      setErrorIA("Sube al menos una foto del albar\xE1n.");
      return;
    }
    if (!proveedorIA) {
      setErrorIA("Elige a qu\xE9 proveedor pertenece.");
      return;
    }
    setCargandoIA(true);
    setErrorIA("");
    setAvisosIA([]);
    try {
      let udsPorCajaFiable = function(l) {
        const leido = Number(l.udsPorCaja) || 0;
        const cajas = Number(l.cantidad) || 0;
        if (leido <= 0) return null;
        const porNombre = String(l.descripcion || "").match(/\bC(\d{1,3})\b/);
        if (porNombre) {
          const n = Number(porNombre[1]);
          if (n > 0 && n <= 200) return n;
        }
        if (cajas > 1 && leido % cajas === 0) {
          const porCaja = leido / cajas;
          if (porCaja >= 1) return porCaja;
        }
        return leido;
      };
      const resp = await fetch(
        "https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/importar-albaran",
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ imagenes: fotosIA.map((f) => ({ base64: f.base64, mediaType: f.mediaType })) })
        }
      );
      const r = await resp.json();
      if (!r.ok) {
        setErrorIA(r.error || "No se ha podido leer correctamente esta informaci\xF3n. Rev\xEDsala antes de continuar.");
        setCargandoIA(false);
        return;
      }
      const d = r.datos || {};
      if (!d.lineas || d.lineas.length === 0) {
        setErrorIA("No he podido reconocer ninguna l\xEDnea de producto en la foto. Puede que est\xE9 borrosa, cortada o inclinada \u2014 prueba con otra foto, o usa 'Pegar texto' si tienes el PDF.");
        setCargandoIA(false);
        return;
      }
      const lineas = d.lineas.map((l) => {
        const base = lineaVacia();
        const aprendido = buscarEnCatalogo(proveedorIA, l.codigo, l.descripcion);
        const uds = udsPorCajaFiable(l);
        const productoId = aprendido && aprendido.productoId ? aprendido.productoId : "";
        const itemPedido = pedidoIALigado && productoId ? pedidoIALigado.items.find((it) => it.productoId === productoId) : null;
        return {
          ...base,
          codigoProveedor: l.codigo || "",
          descripcion: l.descripcion || "",
          cantidad: l.cantidad != null ? l.cantidad : "",
          precioBruto: l.precioUnitario != null ? l.precioUnitario : "",
          dtoPct: l.descuentoPct || "",
          iva: l.iva != null ? l.iva : 10,
          importe: l.importeLinea != null ? l.importeLinea : "",
          // Datos que la IA sí extrae del albarán y antes se perdían al
          // construir la línea: el canon (Punto Verde) sumaba mal el total,
          // y las uds. por caja obligaban a teclearlas a mano cada vez.
          udsPorCaja: uds != null ? uds : base.udsPorCaja || 1,
          canon: l.canon != null && Number(l.canon) > 0 ? Number(l.canon) : "",
          lote: l.lote || "",
          caducidad: l.caducidad || "",
          unidad: l.unidad || base.unidad,
          // Lo aprendido del catálogo va después: si ya enlazaste este código
          // antes, tus datos mandan sobre lo que lea la IA hoy.
          ...aprendido || {},
          productoId,
          cantidadPedida: itemPedido ? Math.max(0, (Number(itemPedido.cantidad) || 0) - (Number(itemPedido.cantidadRecibida) || 0)) : void 0
        };
      });
      setAlb({
        id: uid(),
        proveedorId: proveedorIA,
        numero: d.numeroAlbaran || "",
        fecha: d.fecha || todayISO(),
        totalPapel: d.totalAlbaran != null ? d.totalAlbaran : "",
        cargos: "",
        cargosConcepto: "",
        cargosIva: 21,
        esFactura: true,
        numeroFactura: "",
        fechaFactura: d.fecha || todayISO(),
        pagada: false,
        fechaPago: "",
        // Si venía de un pedido, el albarán queda enlazado a él: al confirmar,
        // el pedido se marcará recibido (o parcial) solo, igual que si se
        // hubiera rellenado a mano desde Recepción.
        pedidoId: pedidoIALigado ? pedidoIALigado.id : void 0,
        lineas,
        estado: "borrador"
      });
      setFotoRevisionIA(fotosIA[0].previsualizacion);
      setConfianzaIA(d.confianza || "media");
      setAvisosIA(d.avisos || []);
      setMostrarIA(false);
      setFotosIA([]);
      setPedidoIALigado(null);
      setModo("editor");
    } catch {
      setErrorIA("No se ha podido conectar con la IA. Comprueba tu conexi\xF3n e int\xE9ntalo otra vez.");
    }
    setCargandoIA(false);
  }
  function nuevoAlbaran() {
    setAlb({
      id: uid(),
      proveedorId: proveedores[0] ? proveedores[0].id : "",
      numero: "",
      fecha: todayISO(),
      totalPapel: "",
      cargos: "",
      cargosConcepto: "",
      cargosIva: 21,
      esFactura: true,
      numeroFactura: "",
      fechaFactura: todayISO(),
      pagada: false,
      fechaPago: "",
      lineas: [lineaVacia()],
      estado: "borrador"
    });
    setError("");
    setFotoRevisionIA("");
    setConfianzaIA(null);
    setAvisosIA([]);
    setModo("editor");
  }
  function abrir(a) {
    setAlb(JSON.parse(JSON.stringify(a)));
    setError("");
    setFotoRevisionIA("");
    setConfianzaIA(null);
    setAvisosIA([]);
    setModo("editor");
  }
  function setLinea(idx, campo, valor) {
    setAlb((a) => ({
      ...a,
      lineas: a.lineas.map((ln, i) => i === idx ? { ...ln, [campo]: valor } : ln)
    }));
  }
  function aplicarCodigo(idx, codigo) {
    setAlb((a) => {
      const lineaActual = a.lineas[idx];
      const cat = buscarEnCatalogo(a.proveedorId, codigo, lineaActual ? lineaActual.descripcion : "");
      return {
        ...a,
        lineas: a.lineas.map((ln, i) => {
          if (i !== idx) return ln;
          if (!cat) return { ...ln, codigoProveedor: codigo };
          return {
            ...ln,
            codigoProveedor: codigo,
            descripcion: cat.descripcion || ln.descripcion,
            unidad: cat.unidad || ln.unidad,
            udsPorCaja: cat.udsPorCaja || 1,
            ivaPct: cat.ivaPct ?? ln.ivaPct,
            tipoUnidad: cat.tipoUnidad || "unidad",
            precioPor: cat.precioPor || "bulto",
            contenido: cat.contenido || "",
            unidadContenido: cat.unidadContenido || "",
            productoId: cat.productoId || "",
            precioBruto: ln.precioBruto === "" && cat.ultimoPrecio ? cat.ultimoPrecio : ln.precioBruto,
            dtoPct: ln.dtoPct === "" && cat.ultimoDto ? cat.ultimoDto : ln.dtoPct
          };
        })
      };
    });
  }
  function unidadesFacturadas(ln) {
    const c = Number(ln.cantidad) || 0;
    if (ln.tipoUnidad === "peso") return c;
    const uds = Number(ln.udsPorCaja) > 0 ? Number(ln.udsPorCaja) : 1;
    return ln.precioPor === "unidad" ? c * uds : c;
  }
  function importeCalculado(ln) {
    const p = Number(ln.precioBruto) || 0;
    const d = Number(ln.dtoPct) || 0;
    const envase = Number(ln.envase) || 0;
    return unidadesFacturadas(ln) * p * (1 - d / 100) + envase;
  }
  function baseLinea(ln) {
    const imp = ln.importe !== "" ? Number(ln.importe) : importeCalculado(ln);
    return (Number(imp) || 0) + (Number(ln.canon) || 0);
  }
  function addLinea() {
    setAlb((a) => ({ ...a, lineas: [...a.lineas, lineaVacia()] }));
  }
  function quitarLinea(idx) {
    setAlb((a) => ({ ...a, lineas: a.lineas.filter((_, i) => i !== idx) }));
  }
  const baseTotal = !alb ? 0 : alb.lineas.reduce((acc, ln) => acc + baseLinea(ln), 0);
  const ivaTotal = !alb ? 0 : alb.lineas.reduce((acc, ln) => acc + baseLinea(ln) * ((Number(ln.ivaPct) || 0) / 100), 0);
  const cargosBase = Number(alb?.cargos) || 0;
  const cargosIvaImp = cargosBase * ((Number(alb?.cargosIva) || 0) / 100);
  const totalCalculado = baseTotal + ivaTotal + cargosBase + cargosIvaImp;
  const totalPapel = Number(alb?.totalPapel) || 0;
  const descuadre = totalPapel > 0 ? totalCalculado - totalPapel : 0;
  const cuadra = totalPapel > 0 && Math.abs(descuadre) < 0.02;
  function guardarBorrador() {
    if (!alb.proveedorId) {
      setError("Selecciona el proveedor.");
      return;
    }
    setError("");
    const limpio = {
      ...alb,
      lineas: alb.lineas.map((ln) => ({ ...ln, importe: ln.importe !== "" ? Number(ln.importe) : Number(importeCalculado(ln).toFixed(4)) }))
    };
    guardarAlbaran(limpio);
    setAlb(limpio);
  }
  function darEntrada() {
    if (procesandoEntrada) return;
    if (!alb.proveedorId) {
      setError("Selecciona el proveedor.");
      return;
    }
    const validas = alb.lineas.filter((ln) => (ln.descripcion || ln.codigoProveedor) && Number(ln.cantidad) > 0);
    if (!validas.length) {
      setError("A\xF1ade al menos una l\xEDnea con descripci\xF3n y cantidad.");
      return;
    }
    setError("");
    setProcesandoEntrada(true);
    try {
      const limpio = {
        ...alb,
        lineas: validas.map((ln) => ({ ...ln, importe: ln.importe !== "" ? Number(ln.importe) : Number(importeCalculado(ln).toFixed(4)) }))
      };
      const av = confirmarAlbaran(limpio);
      setAvisos(av && av.length ? av : []);
      setModo("lista");
      setAlb(null);
      setFotoRevisionIA("");
    } finally {
      setProcesandoEntrada(false);
    }
  }
  if (modo === "lista") {
    return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
      setMostrarPegar(true);
      setTextoPegado("");
      setErrorPegado("");
      setProveedorTexto(proveedores[0] ? proveedores[0].id : "");
    } }, /* @__PURE__ */ import_react.default.createElement(ClipboardList, { size: 15 }), " Pegar texto"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
      setMostrarIA(true);
      setFotosIA([]);
      setErrorIA("");
      setAvisosIA([]);
      setPedidoIALigado(null);
      setProveedorIA(proveedores[0] ? proveedores[0].id : "");
    } }, /* @__PURE__ */ import_react.default.createElement(Camera, { size: 15 }), " Foto con IA"), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: nuevoAlbaran }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nuevo albar\xE1n")) }, "Entrada de albaranes"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3 flex items-center gap-3 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Filtrar por mes" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "month", value: filtroMes, onChange: (e) => setFiltroMes(e.target.value) })), filtroMes && /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setFiltroMes(""), className: "mt-1" }, "Ver todos")), mostrarPegar && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setMostrarPegar(false), title: "Pegar el texto del albar\xE1n", ancho: "max-w-2xl" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.inkSoft } }, "Abre el PDF del proveedor, selecciona la tabla de productos, c\xF3piala y p\xE9gala aqu\xED. El programa rellenar\xE1 las l\xEDneas solo. Si ya enlazaste antes alg\xFAn c\xF3digo de este proveedor, lo reconocer\xE1 autom\xE1ticamente."), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Proveedor" }, /* @__PURE__ */ import_react.default.createElement(
      "select",
      {
        value: proveedorTexto,
        onChange: (e) => setProveedorTexto(e.target.value),
        className: "w-full rounded-lg px-3 py-2 text-[13px]",
        style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
      },
      proveedores.map((pv) => /* @__PURE__ */ import_react.default.createElement("option", { key: pv.id, value: pv.id }, pv.nombre))
    )), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Texto del albar\xE1n" }, /* @__PURE__ */ import_react.default.createElement(
      "textarea",
      {
        value: textoPegado,
        onChange: (e) => setTextoPegado(e.target.value),
        rows: 10,
        placeholder: "1234    COCA COLA 33CL LATA    24    0,45    10,80\n5678    AGUA MINERAL 1,5L      12    0,30     3,60",
        className: "w-full rounded-lg px-3 py-2 text-[12px] mono",
        style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
      }
    )), errorPegado && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, errorPegado), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3", style: { color: C.inkSoft } }, "Nada se guarda todav\xEDa: se prepara un borrador que podr\xE1s revisar y corregir antes de confirmar."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: crearDesdeTextoPegado }, "Leer y preparar borrador"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setMostrarPegar(false) }, "Cancelar"))), mostrarIA && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => {
      if (!cargandoIA) {
        setMostrarIA(false);
        setPedidoIALigado(null);
      }
    }, title: "Importar albar\xE1n con foto (IA)", ancho: "max-w-2xl" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.inkSoft } }, "Sube una o varias fotos del albar\xE1n (una por p\xE1gina si tiene varias). La IA lee los datos y prepara un borrador \u2014 nada se guarda ni afecta al stock hasta que lo revises y confirmes."), pedidoIALigado && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3 px-3 py-2 rounded-lg", style: { background: C.accentSoft } }, "Esta lectura quedar\xE1 enlazada al pedido de ", /* @__PURE__ */ import_react.default.createElement("b", null, proveedorPorId(pedidoIALigado.proveedorId)?.nombre || "\u2014"), ". Al confirmar el albar\xE1n, el pedido se marcar\xE1 como recibido o parcial seg\xFAn lo que reconozca la foto."), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Proveedor" }, /* @__PURE__ */ import_react.default.createElement(
      "select",
      {
        value: proveedorIA,
        onChange: (e) => setProveedorIA(e.target.value),
        disabled: cargandoIA,
        className: "w-full rounded-lg px-3 py-2 text-[13px]",
        style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
      },
      proveedores.map((pv) => /* @__PURE__ */ import_react.default.createElement("option", { key: pv.id, value: pv.id }, pv.nombre))
    )), /* @__PURE__ */ import_react.default.createElement(
      "label",
      {
        className: "flex flex-col items-center justify-center gap-2 rounded-lg py-6 mb-3 cursor-pointer",
        style: { border: `2px dashed ${C.line}`, background: C.surface }
      },
      /* @__PURE__ */ import_react.default.createElement(Camera, { size: 22, color: C.inkSoft }),
      /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px]", style: { color: C.inkSoft } }, "Toca para elegir: hacer foto ahora o subir de la galer\xEDa"),
      /* @__PURE__ */ import_react.default.createElement(
        "input",
        {
          type: "file",
          accept: "image/*",
          multiple: true,
          disabled: cargandoIA,
          onChange: (e) => {
            agregarFotosIA(e.target.files);
            e.target.value = "";
          },
          className: "hidden"
        }
      )
    ), fotosIA.length > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-2 mb-3" }, fotosIA.map((f, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "relative" }, /* @__PURE__ */ import_react.default.createElement("img", { src: f.previsualizacion, alt: "", className: "w-16 h-16 object-cover rounded-lg", style: { border: `1px solid ${C.line}` } }), !cargandoIA && /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        onClick: () => quitarFotoIA(i),
        "aria-label": "Quitar esta foto",
        className: "absolute -top-1.5 -right-1.5 rounded-full flex items-center justify-center",
        style: { width: 18, height: 18, background: C.red, color: "#fff" }
      },
      /* @__PURE__ */ import_react.default.createElement(X, { size: 12 })
    )))), errorIA && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, errorIA), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3", style: { color: C.inkSoft } }, "Si la foto sale borrosa o no se lee bien, la IA lo deja en blanco en vez de inventar \u2014 luego lo rellenas t\xFA en la revisi\xF3n."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: importarConIA, disabled: cargandoIA }, cargandoIA ? "Leyendo el albar\xE1n\u2026" : "Leer con IA"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
      setMostrarIA(false);
      setPedidoIALigado(null);
    }, disabled: cargandoIA }, "Cancelar"))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Teclea el albar\xE1n tal y como viene en el papel. La primera vez que introduzcas un c\xF3digo, el programa aprende su descripci\xF3n, unidad y unidades por caja: a partir de ah\xED solo tendr\xE1s que escribir el c\xF3digo y la cantidad. Al dar entrada se crean o actualizan los productos, sube el stock y se recalculan los costes.")), avisos && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: avisos.length ? C.amberSoft : C.accentSoft, border: "none" } }, avisos.length === 0 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Entrada registrada. Ning\xFAn precio ha variado m\xE1s de un 3%.") : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-2" }, "Atenci\xF3n: el precio de esta entrega se aleja del coste promedio que ten\xEDas"), avisos.map((a, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "text-[12px] flex items-center justify-between py-0.5" }, /* @__PURE__ */ import_react.default.createElement("span", null, a.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: a.variacion > 0 ? C.red : C.accent } }, "\u20AC", fmt(a.anterior), " \u2192 \u20AC", fmt(a.nuevo), " (", a.variacion > 0 ? "+" : "", fmt(a.variacion), "%)"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-2", style: { color: C.inkSoft } }, "El coste final del producto ya no es exactamente \u20AC", avisos[0] ? fmt(avisos[0].nuevo) : "", ": se ha promediado con el stock que ya ten\xEDas, para no revalorizar de golpe lo que compraste m\xE1s barato o m\xE1s caro antes.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setAvisos(null) }, "Entendido"))), albaranes.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has registrado ning\xFAn albar\xE1n." }) : (() => {
      const albaranesFiltrados = filtroMes ? albaranes.filter((a) => (a.fecha || "").startsWith(filtroMes)) : albaranes;
      if (albaranesFiltrados.length === 0) {
        return /* @__PURE__ */ import_react.default.createElement(Empty, { text: `No hay albaranes en ese mes.` });
      }
      const totalFiltrado = albaranesFiltrados.reduce((acc, a) => {
        const baseConCanon = a.lineas.reduce((x, ln) => x + (Number(ln.importe) || 0) + (Number(ln.canon) || 0), 0);
        const ivaLineas = a.lineas.reduce((x, ln) => x + ((Number(ln.importe) || 0) + (Number(ln.canon) || 0)) * ((Number(ln.ivaPct) || 0) / 100), 0);
        const cargos = Number(a.cargos) || 0;
        const ivaCargos = cargos * ((Number(a.cargosIva) || 0) / 100);
        return acc + baseConCanon + ivaLineas + cargos + ivaCargos;
      }, 0);
      return /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, filtroMes && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-1", style: { color: C.inkSoft } }, albaranesFiltrados.length, " albar\xE1n(es) este mes \xB7 total ", /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, "\u20AC", fmt(totalFiltrado))), albaranesFiltrados.map((a) => {
        const prov = proveedorPorId(a.proveedorId);
        const baseConCanon = a.lineas.reduce((x, ln) => x + (Number(ln.importe) || 0) + (Number(ln.canon) || 0), 0);
        const ivaLineas = a.lineas.reduce((x, ln) => x + ((Number(ln.importe) || 0) + (Number(ln.canon) || 0)) * ((Number(ln.ivaPct) || 0) / 100), 0);
        const cargos = Number(a.cargos) || 0;
        const ivaCargos = cargos * ((Number(a.cargosIva) || 0) / 100);
        const base = baseConCanon;
        const totalConIva = baseConCanon + ivaLineas + cargos + ivaCargos;
        return /* @__PURE__ */ import_react.default.createElement(Card, { key: a.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold" }, prov ? prov.nombre : "Sin proveedor"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, "Albar\xE1n ", a.numero || "s/n", " \xB7 ", a.fecha, " \xB7 ", a.lineas.length, " l\xEDnea(s)")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement(Pill, { color: a.estado === "confirmado" ? C.accent : C.amber }, a.estado === "confirmado" ? "Dado de entrada" : "Borrador"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold mt-1" }, "\u20AC", fmt(totalConIva)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mono", style: { color: C.inkSoft } }, "base \u20AC", fmt(base), " \xB7 IVA \u20AC", fmt(totalConIva - base)), a.estado === "confirmado" && a.esFactura !== false && /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-0.5" }, /* @__PURE__ */ import_react.default.createElement(Pill, { color: a.pagada ? C.inkSoft : C.amber }, a.pagada ? "Pagada" : "Sin pagar")))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 flex gap-2 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => abrir(a) }, a.estado === "confirmado" ? "Ver" : "Continuar editando"), a.estado === "confirmado" && /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setAnularId(a.id) }, "Anular para editar"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "danger", onClick: () => setConfirmarId(a.id) }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 13 }), " Eliminar")));
      }));
    })(), anularId && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setAnularId(null), title: "Anular entrada para poder editar" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-4" }, "Se restar\xE1 del stock lo que entr\xF3 con este albar\xE1n y se borrar\xE1n sus movimientos. El albar\xE1n volver\xE1 a estado borrador para que puedas corregirlo y darle entrada otra vez.", /* @__PURE__ */ import_react.default.createElement("br", null), /* @__PURE__ */ import_react.default.createElement("br", null), "Los costes de los productos ", /* @__PURE__ */ import_react.default.createElement("b", null, "no se revierten"), ": quedar\xE1n con el \xFAltimo valor introducido hasta que vuelvas a confirmar el albar\xE1n corregido."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
      const a = albaranes.find((x) => x.id === anularId);
      if (a) {
        anularAlbaran(a);
      }
      setAnularId(null);
    } }, "S\xED, anular y editar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setAnularId(null) }, "Cancelar"))), confirmarId && (() => {
      const a = albaranes.find((x) => x.id === confirmarId);
      const yaEntrado = a && a.estado === "confirmado";
      return /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmarId(null), title: "Eliminar albar\xE1n" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-4" }, yaEntrado ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, "Este albar\xE1n ya dio entrada. Al eliminarlo se ", /* @__PURE__ */ import_react.default.createElement("b", null, "restar\xE1 del stock"), " lo que trajo, se borrar\xE1n sus movimientos, y si ven\xEDa de un pedido, ese pedido volver\xE1 a quedar pendiente de lo que falte.") : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, "Se borrar\xE1 este borrador. No lleg\xF3 a afectar al stock, as\xED que no hay nada que revertir.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: () => {
        eliminarAlbaran(confirmarId);
        setConfirmarId(null);
      } }, "S\xED, eliminar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmarId(null) }, "Cancelar")));
    })());
  }
  const bloqueado = alb.estado === "confirmado";
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setModo("lista");
    setAlb(null);
    setFotoRevisionIA("");
  } }, "Volver") }, bloqueado ? "Albar\xE1n registrado" : "Nuevo albar\xE1n"), bloqueado && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.bg, border: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2" }, "Este albar\xE1n ya se dio de entrada, por eso est\xE1 en solo lectura. Si necesitas corregir algo, anula la entrada: se devolver\xE1 el stock y podr\xE1s editarlo."), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => {
    anularAlbaran(alb);
    setAlb({ ...alb, estado: "borrador" });
  } }, "Anular entrada y editar")), alb.pedidoId && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Este albar\xE1n est\xE1 enlazado a un pedido. Las l\xEDneas vienen con lo que pediste: corrige las cantidades para reflejar ", /* @__PURE__ */ import_react.default.createElement("b", null, "lo que ha llegado de verdad"), ". Al dar entrada, el pedido se marcar\xE1 como recibido o parcial seg\xFAn corresponda.")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-4 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Proveedor" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: alb.proveedorId,
      onChange: (e) => setAlb({ ...alb, proveedorId: e.target.value }),
      disabled: bloqueado,
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Selecciona\u2026"),
    proveedores.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre))
  )), /* @__PURE__ */ import_react.default.createElement(Field, { label: "N\xBA de albar\xE1n" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: alb.numero, onChange: (e) => setAlb({ ...alb, numero: e.target.value }), disabled: bloqueado, placeholder: "ALENDUO26027571" }), duplicados.albaran && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mt-1", style: { color: C.amber } }, "\u26A0 Ya hay un albar\xE1n con ese n\xFAmero de este proveedor (del ", duplicados.albaran.fecha, "). Si no es una correcci\xF3n, puede que lo est\xE9s registrando dos veces.")), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Fecha" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: alb.fecha, onChange: (e) => setAlb({ ...alb, fecha: e.target.value }), disabled: bloqueado })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Total del papel (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: alb.totalPapel, onChange: (e) => setAlb({ ...alb, totalPapel: e.target.value }), disabled: bloqueado, placeholder: "116,41" }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] font-semibold mb-2 pt-2", style: { borderTop: `1px solid ${C.line}`, color: C.inkSoft } }, "Cargos aparte de la mercanc\xEDa (opcional)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Concepto" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: alb.cargosConcepto || "", onChange: (e) => setAlb({ ...alb, cargosConcepto: e.target.value }), disabled: bloqueado, placeholder: "Portes, Punto Verde, envases\u2026" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Importe (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: alb.cargos || "", onChange: (e) => setAlb({ ...alb, cargos: e.target.value }), disabled: bloqueado, placeholder: "0,47" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "IVA del cargo (%)" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: alb.cargosIva ?? 21,
      onChange: (e) => setAlb({ ...alb, cargosIva: e.target.value }),
      disabled: bloqueado,
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface }
    },
    TIPOS_IVA.map((t) => /* @__PURE__ */ import_react.default.createElement("option", { key: t, value: t }, t, "%"))
  ))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Estos cargos no entran en el coste de los productos: solo sirven para que el total cuadre con el papel."), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] font-semibold mb-2 pt-3 mt-1", style: { borderTop: `1px solid ${C.line}`, color: C.inkSoft } }, "Facturaci\xF3n y pago"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2 mb-2" }, /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      type: "button",
      disabled: bloqueado,
      onClick: () => setAlb({ ...alb, esFactura: !(alb.esFactura !== false) }),
      className: "text-[12px] font-medium px-3 py-1.5 rounded-lg",
      style: {
        background: alb.esFactura !== false ? C.accentSoft : C.bg,
        border: `1px solid ${alb.esFactura !== false ? C.accent : C.line}`,
        color: C.ink
      }
    },
    alb.esFactura !== false ? "\u2713 Este albar\xE1n es tambi\xE9n la factura" : "Este albar\xE1n NO es la factura (a\xFAn no ha llegado)"
  )), alb.esFactura !== false && /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "N\xBA de factura (si es distinto del albar\xE1n)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: alb.numeroFactura || "", onChange: (e) => setAlb({ ...alb, numeroFactura: e.target.value }), disabled: bloqueado, placeholder: alb.numero || "s/n" }), duplicados.factura && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mt-1", style: { color: C.amber } }, "\u26A0 Ya hay una factura con ese n\xFAmero de este proveedor (del ", duplicados.factura.fechaFactura || duplicados.factura.fecha, ").")), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Fecha de factura" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: alb.fechaFactura || alb.fecha, onChange: (e) => setAlb({ ...alb, fechaFactura: e.target.value }), disabled: bloqueado })), /* @__PURE__ */ import_react.default.createElement(Field, { label: `Vencimiento (${proveedorPorId(alb.proveedorId)?.diasPago ? proveedorPorId(alb.proveedorId).diasPago + " d\xEDas" : "sin d\xEDas de pago"})` }, /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      readOnly: true,
      value: (() => {
        const prov = proveedorPorId(alb.proveedorId);
        if (!prov || !prov.diasPago) return "A\xF1ade los d\xEDas de pago en el proveedor";
        const f = new Date(alb.fechaFactura || alb.fecha);
        f.setDate(f.getDate() + Number(prov.diasPago));
        return f.toLocaleDateString("es-ES");
      })(),
      style: { background: C.bg, color: C.inkSoft }
    }
  ))), bloqueado && alb.esFactura !== false && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between pt-2 mt-1", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px]" }, alb.pagada ? `Pagada el ${alb.fechaPago}` : "Pendiente de pago"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: alb.pagada ? "ghost" : "primary", onClick: () => {
    marcarPagada(alb.id, !alb.pagada);
    setAlb({ ...alb, pagada: !alb.pagada });
  } }, alb.pagada ? "Marcar como pendiente" : "Marcar como pagada"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "L\xEDneas del albar\xE1n"), alb.lineas.map((ln, idx) => {
    const cat = buscarEnCatalogo(alb.proveedorId, ln.codigoProveedor, ln.descripcion);
    const conocido = !!cat;
    const uds = Number(ln.udsPorCaja) > 0 ? Number(ln.udsPorCaja) : 1;
    const imp = ln.importe !== "" ? Number(ln.importe) : importeCalculado(ln);
    const totalUds = ln.tipoUnidad === "peso" ? Number(ln.cantidad) || 0 : (Number(ln.cantidad) || 0) * uds;
    const costoUd = totalUds > 0 ? imp / totalUds : 0;
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: idx, className: "mb-3", style: { background: C.bg } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12px] font-semibold" }, "L\xEDnea ", idx + 1), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, conocido && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.accent }, "referencia conocida"), !bloqueado && /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: () => quitarLinea(idx), "aria-label": "Quitar l\xEDnea" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 16, color: C.inkSoft })))), !conocido && !bloqueado && (() => {
      const sugerencias = ln.descripcion ? productosSimilares(ln.descripcion, productos) : [];
      return /* @__PURE__ */ import_react.default.createElement(Field, { label: "\xBFEs un producto que ya tienes?" }, /* @__PURE__ */ import_react.default.createElement(
        "select",
        {
          value: ln.productoId || "",
          onChange: (e) => setLinea(idx, "productoId", e.target.value),
          className: "w-full rounded-lg px-3 py-2 text-[13px]",
          style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
        },
        /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "\u2014 Es un producto nuevo, dar de alta \u2014"),
        sugerencias.length > 0 && /* @__PURE__ */ import_react.default.createElement("optgroup", { label: "Se parece a..." }, sugerencias.map(({ producto }) => /* @__PURE__ */ import_react.default.createElement("option", { key: producto.id, value: producto.id }, producto.nombre))),
        /* @__PURE__ */ import_react.default.createElement("optgroup", { label: "Todo el cat\xE1logo" }, productos.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre)))
      ));
    })(), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "C\xF3digo del proveedor" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: ln.codigoProveedor, onChange: (e) => aplicarCodigo(idx, e.target.value), disabled: bloqueado, placeholder: "01201" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "\xBFC\xF3mo te lo cobran?" }, /* @__PURE__ */ import_react.default.createElement(
      "select",
      {
        value: ln.tipoUnidad,
        onChange: (e) => setLinea(idx, "tipoUnidad", e.target.value),
        disabled: bloqueado,
        className: "w-full rounded-lg px-3 py-2 text-[13px]",
        style: { border: `1px solid ${C.line}`, background: C.surface }
      },
      /* @__PURE__ */ import_react.default.createElement("option", { value: "unidad" }, "Precio por bulto (saco, caja, bote\u2026)"),
      /* @__PURE__ */ import_react.default.createElement("option", { value: "peso" }, "Precio por kilo (peso variable)")
    ))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3 rounded-lg p-2", style: { background: C.surface, color: C.inkSoft, border: `1px solid ${C.line}` } }, ln.tipoUnidad === "peso" ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, "Solo si el albar\xE1n pone ", /* @__PURE__ */ import_react.default.createElement("b", null, "\u20AC/kg"), ' y el peso cambia en cada entrega: jam\xF3n 1,02 kg \xD7 7,95 \u20AC/kg, naranjas 30 kg \xD7 1,20 \u20AC/kg. En "Kilos" van los kilos servidos.') : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, "Lo habitual. Un saco de harina de 25 kg a 18 \u20AC es ", /* @__PURE__ */ import_react.default.createElement("b", null, "1 bulto a 18 \u20AC"), ', no 25 kilos: los 25 kg son el formato, van en el nombre. En "Cantidad" van los sacos o cajas que llegaron.')), /* @__PURE__ */ import_react.default.createElement("div", { className: "hidden" }), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Descripci\xF3n" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: ln.descripcion, onChange: (e) => setLinea(idx, "descripcion", e.target.value), disabled: bloqueado, placeholder: "HARINA DE TRIGO FLOJA CIFUENTES S 25 KG" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Unidad" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: ln.unidad, onChange: (e) => setLinea(idx, "unidad", e.target.value), disabled: bloqueado, placeholder: "saco, kg, bote" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: ln.tipoUnidad === "peso" ? "Kilos servidos" : "Cantidad de bultos" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: ln.cantidad, onChange: (e) => setLinea(idx, "cantidad", e.target.value), disabled: bloqueado })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Uds por caja" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "1", value: ln.udsPorCaja, onChange: (e) => setLinea(idx, "udsPorCaja", e.target.value), disabled: bloqueado }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Contenido de cada bulto (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.001", value: ln.contenido, onChange: (e) => setLinea(idx, "contenido", e.target.value), disabled: bloqueado, placeholder: "25" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Unidad del contenido" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: ln.unidadContenido, onChange: (e) => setLinea(idx, "unidadContenido", e.target.value), disabled: bloqueado, placeholder: "kg, L, uds" }))), ln.tipoUnidad !== "peso" && /* @__PURE__ */ import_react.default.createElement(Field, { label: "El precio del albar\xE1n es\u2026" }, /* @__PURE__ */ import_react.default.createElement(
      "select",
      {
        value: ln.precioPor || "bulto",
        onChange: (e) => setLinea(idx, "precioPor", e.target.value),
        disabled: bloqueado,
        className: "w-full rounded-lg px-3 py-2 text-[13px]",
        style: { border: `1px solid ${C.line}`, background: C.surface }
      },
      /* @__PURE__ */ import_react.default.createElement("option", { value: "bulto" }, "Por bulto entero (1 caja = X \u20AC)"),
      /* @__PURE__ */ import_react.default.createElement("option", { value: "unidad" }, "Por pieza suelta (1 croissant = X \u20AC)")
    )), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.0001", value: ln.precioBruto, onChange: (e) => setLinea(idx, "precioBruto", e.target.value), disabled: bloqueado })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "% Dto." }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: ln.dtoPct, onChange: (e) => setLinea(idx, "dtoPct", e.target.value), disabled: bloqueado })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Punto Verde / envase (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.001", value: ln.envase, onChange: (e) => setLinea(idx, "envase", e.target.value), disabled: bloqueado, placeholder: "0,02" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Importe" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: ln.importe, onChange: (e) => setLinea(idx, "importe", e.target.value), disabled: bloqueado, placeholder: fmt(importeCalculado(ln)) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Punto Verde / envases" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: ln.canon, onChange: (e) => setLinea(idx, "canon", e.target.value), disabled: bloqueado, placeholder: "0,02" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "IVA %" }, /* @__PURE__ */ import_react.default.createElement(
      "select",
      {
        value: ln.ivaPct,
        onChange: (e) => setLinea(idx, "ivaPct", e.target.value),
        disabled: bloqueado,
        className: "w-full rounded-lg px-3 py-2 text-[13px]",
        style: { border: `1px solid ${C.line}`, background: C.surface }
      },
      TIPOS_IVA.map((t) => /* @__PURE__ */ import_react.default.createElement("option", { key: t, value: t }, t, "%"))
    ))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Lote (trazabilidad)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: ln.lote, onChange: (e) => setLinea(idx, "lote", e.target.value), disabled: bloqueado, placeholder: "1/52451" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Caducidad" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: ln.caducidad, onChange: (e) => setLinea(idx, "caducidad", e.target.value), disabled: bloqueado }))), ln.cantidadPedida != null && ln.cantidadPedida !== "" && (() => {
      const pedida = Number(ln.cantidadPedida) || 0;
      const dif = totalUds - pedida;
      if (Math.abs(dif) < 1e-3) {
        return /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2", style: { color: C.accent } }, "Coincide con lo pedido (", fmt(pedida), " ", ln.unidad, ").");
      }
      return /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2", style: { color: C.amber } }, "Pediste ", fmt(pedida), " ", ln.unidad, " y llegan ", fmt(totalUds), " ", "(", dif > 0 ? "+" : "", fmt(dif), "). Quedar\xE1 como recibido parcial.");
    })(), totalUds > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "pt-2", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mono", style: { color: C.inkSoft } }, ln.precioPor === "unidad" && ln.tipoUnidad !== "peso" ? `${fmt(Number(ln.cantidad) || 0)} bultos \xD7 ${fmt(uds)} uds \xD7 \u20AC${fmt(Number(ln.precioBruto) || 0)}` : `${fmt(Number(ln.cantidad) || 0)} \xD7 \u20AC${fmt(Number(ln.precioBruto) || 0)}`, Number(ln.dtoPct) ? ` \u2212 ${fmt(Number(ln.dtoPct))}%` : "", Number(ln.envase) ? ` + \u20AC${fmt(Number(ln.envase))} envase` : "", " = \u20AC", fmt(imp)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mono", style: { color: C.accent } }, ln.tipoUnidad === "peso" ? `Entran ${fmt(totalUds)} kg \xB7 \u20AC${fmt(costoUd)} el kg` : uds > 1 ? `Entran ${fmt(Number(ln.cantidad) || 0)} ${ln.unidad || "bultos"} = ${fmt(totalUds)} unidades \xB7 \u20AC${fmt(costoUd)} cada unidad` : `Entran ${fmt(totalUds)} unidades \xB7 \u20AC${fmt(costoUd)} cada unidad`), Number(ln.contenido) > 0 && ln.tipoUnidad !== "peso" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mono", style: { color: C.accent } }, "Contenido total ", fmt(totalUds * Number(ln.contenido)), " ", ln.unidadContenido || "", " \xB7 \u20AC", fmt(costoUd / Number(ln.contenido)), " por ", ln.unidadContenido || "unidad de contenido")));
  }), !bloqueado && /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: addLinea, className: "text-[12.5px] font-medium mb-4", style: { color: C.accent } }, "+ A\xF1adir l\xEDnea"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: totalPapel > 0 ? cuadra ? C.accentSoft : C.amberSoft : C.bg, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-3 text-center mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Base imponible"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, "\u20AC", fmt(baseTotal)), cargosBase > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mono", style: { color: C.inkSoft } }, "+ cargos \u20AC", fmt(cargosBase))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "IVA"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, "\u20AC", fmt(ivaTotal))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Total calculado"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-bold text-[16px]" }, "\u20AC", fmt(totalCalculado)))), totalPapel > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] text-center pt-2", style: { borderTop: "1px solid rgba(0,0,0,0.08)" } }, cuadra ? /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.accent } }, "Cuadra con el total del papel (\u20AC", fmt(totalPapel), ").") : /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.amber } }, "No cuadra: el papel dice \u20AC", fmt(totalPapel), " y sale \u20AC", fmt(totalCalculado), " ", "(diferencia ", descuadre > 0 ? "+" : "", "\u20AC", fmt(descuadre), "). Revisa descuentos, portes o IVA."))), fotoRevisionIA && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2" }, "Foto original, para comparar con lo le\xEDdo"), /* @__PURE__ */ import_react.default.createElement("img", { src: fotoRevisionIA, alt: "Albar\xE1n original", className: "w-full max-h-80 object-contain rounded-lg", style: { border: `1px solid ${C.line}` } }), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-2", style: { color: C.inkSoft } }, "La foto se ve mientras revisas. No se guarda con el albar\xE1n: ocupa miles de veces m\xE1s que el propio albar\xE1n y llenar\xEDa el almacenamiento del programa.")), confianzaIA && confianzaIA !== "alta" && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold" }, "\u26A0 Confianza ", confianzaIA === "baja" ? "baja" : "media", " en esta lectura \u2014 revisa cada l\xEDnea con m\xE1s cuidado antes de confirmar.")), avisosIA.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-1" }, "\u26A0 La IA marc\xF3 estas dudas"), avisosIA.map((a, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "text-[11.5px]" }, "\xB7 ", a))), avisosPegado.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-1" }, "\u26A0 Revisa estas l\xEDneas del texto pegado"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2" }, "En estas los n\xFAmeros no cuadraban entre s\xED. No he inventado nada: compru\xE9balas contra el albar\xE1n."), avisosPegado.map((a, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "text-[11.5px]" }, "\xB7 ", a))), desviaciones.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-1" }, "\u26A0 El precio ha cambiado respecto a la \xFAltima compra"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2" }, "Compru\xE9balo antes de confirmar: una subida que pasa desapercibida se lleva el margen sin que se note."), desviaciones.map((d) => /* @__PURE__ */ import_react.default.createElement("div", { key: d.productoId, className: "text-[11.5px] mono" }, d.nombre, ": \u20AC", fmt(d.antes), " \u2192 \u20AC", fmt(d.ahora), " ", /* @__PURE__ */ import_react.default.createElement("b", { style: { color: d.variacion > 0 ? C.red : C.accent } }, "(", d.variacion > 0 ? "+" : "", d.variacion, "%)")))), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), !bloqueado && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: darEntrada, disabled: procesandoEntrada }, /* @__PURE__ */ import_react.default.createElement(CircleCheck, { size: 14 }), " ", procesandoEntrada ? "Dando entrada\u2026" : "Dar entrada al almac\xE9n"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: guardarBorrador }, "Guardar como borrador"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setModo("lista");
    setAlb(null);
    setFotoRevisionIA("");
  } }, "Cancelar")));
}
function Resultados({ movimientos, productos, productoPorId, gastosGenerales, addGasto, deleteGasto, empleados = [] }) {
  const [desde, setDesde] = (0, import_react.useState)(primerDiaMes(/* @__PURE__ */ new Date()));
  const [hasta, setHasta] = (0, import_react.useState)(ultimoDiaMes(/* @__PURE__ */ new Date()));
  const [showGasto, setShowGasto] = (0, import_react.useState)(false);
  const [gastoForm, setGastoForm] = (0, import_react.useState)({ concepto: "", importe: "", categoria: "" });
  const [gastoError, setGastoError] = (0, import_react.useState)("");
  function irMesActual() {
    setDesde(primerDiaMes(/* @__PURE__ */ new Date()));
    setHasta(ultimoDiaMes(/* @__PURE__ */ new Date()));
  }
  function irMesAnterior() {
    const d = /* @__PURE__ */ new Date();
    d.setMonth(d.getMonth() - 1);
    setDesde(primerDiaMes(d));
    setHasta(ultimoDiaMes(d));
  }
  const salidasPeriodo = (0, import_react.useMemo)(
    () => movimientos.filter((m) => esSalida(m) && m.motivo && m.fecha >= desde && m.fecha <= hasta),
    [movimientos, desde, hasta]
  );
  const ventas = salidasPeriodo.filter((m) => esVenta(m));
  const autoconsumo = salidasPeriodo.filter((m) => m.motivo === "Autoconsumo");
  const roturas = salidasPeriodo.filter((m) => m.motivo === "Rotura");
  const mermas = salidasPeriodo.filter((m) => esMerma(m) && m.motivo !== "Autoconsumo" && m.motivo !== "Rotura");
  const aceite = salidasPeriodo.filter((m) => m.motivo === "Cambio de aceite" || m.motivo === "Relleno de aceite");
  const ingresos = ventas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.ingresoUnitario) || 0), 0);
  const costeVentas = ventas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0), 0);
  const margenBruto = ingresos - costeVentas;
  const margenPct = ingresos > 0 ? margenBruto / ingresos * 100 : null;
  const valorMermas = mermas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0), 0);
  const valorAutoconsumo = autoconsumo.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0), 0);
  const valorRoturas = roturas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0), 0);
  const valorAceite = aceite.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0), 0);
  const mermasPorProducto = (0, import_react.useMemo)(() => {
    const mapa = {};
    mermas.forEach((m) => {
      mapa[m.productoId] = (mapa[m.productoId] || 0) + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0);
    });
    return Object.entries(mapa).map(([id, valor]) => ({ producto: productoPorId(id), valor })).filter((x) => x.producto && x.valor > 0).sort((a, b) => b.valor - a.valor).slice(0, 5);
  }, [mermas, productoPorId]);
  const dias = Math.max(1, Math.round((new Date(hasta) - new Date(desde)) / 864e5) + 1);
  const gastosFijos = gastosGenerales.filter((g) => !g.fecha);
  const gastosPuntuales = gastosGenerales.filter((g) => g.fecha);
  const gastosGeneralesMensual = gastosFijos.reduce((a, g) => a + (Number(g.importe) || 0), 0);
  const gastosPuntualesPeriodo = gastosPuntuales.filter((g) => g.fecha >= desde && g.fecha <= hasta).reduce((a, g) => a + (Number(g.importe) || 0), 0);
  const desgloseGastosPorCategoria = (() => {
    const mapa = {};
    const suma = (cat, importe) => {
      const clave = cat && cat.trim() ? cat : "Sin categor\xEDa";
      mapa[clave] = (mapa[clave] || 0) + importe;
    };
    gastosFijos.forEach((g) => suma(g.categoria, (Number(g.importe) || 0) * (dias / 30)));
    gastosPuntuales.filter((g) => g.fecha >= desde && g.fecha <= hasta).forEach((g) => suma(g.categoria, Number(g.importe) || 0));
    return Object.entries(mapa).map(([categoria, importe]) => ({ categoria, importe })).sort((a, b) => b.importe - a.importe);
  })();
  const costePersonalMensual = empleados.filter((e) => e.activo !== false).reduce((a, e) => {
    const bruto = Number(e.salarioBrutoMensual) || 0;
    const coste = e.costeEmpresaMensual !== "" && e.costeEmpresaMensual != null ? Number(e.costeEmpresaMensual) : bruto * 1.32;
    return a + coste;
  }, 0);
  const personalEsEstimado = empleados.some((e) => e.activo !== false && (e.costeEmpresaMensual === "" || e.costeEmpresaMensual == null) && Number(e.salarioBrutoMensual) > 0);
  const gastosTotalMensual = gastosGeneralesMensual + costePersonalMensual;
  const gastosPeriodo = gastosTotalMensual * (dias / 30) + gastosPuntualesPeriodo;
  const costeInventarioSinVenta = valorMermas + valorAutoconsumo + valorRoturas + valorAceite;
  const resultadoNeto = margenBruto - gastosPeriodo - costeInventarioSinVenta;
  const huboLegacy = movimientos.some((m) => m.tipo === "salida" && !m.motivo);
  const margenTeorico = (0, import_react.useMemo)(() => {
    const conPrecio = productos.filter((p) => Number(p.precioVenta) > 0);
    if (!conPrecio.length) return null;
    const total = conPrecio.reduce((a, p) => a + (margenDe(p) || 0), 0);
    return total / conPrecio.length;
  }, [productos]);
  const margenParaPE = ingresos > 0 ? margenPct : margenTeorico;
  const esTeorico = !(ingresos > 0);
  const peMensual = margenParaPE > 0 ? gastosTotalMensual / (margenParaPE / 100) : null;
  const pePeriodo = margenParaPE > 0 ? gastosPeriodo / (margenParaPE / 100) : null;
  function submitGasto() {
    if (!gastoForm.concepto.trim()) {
      setGastoError("Escribe el concepto del gasto.");
      return;
    }
    if (!Number(gastoForm.importe) || Number(gastoForm.importe) <= 0) {
      setGastoError("Escribe un importe mensual mayor que cero.");
      return;
    }
    setGastoError("");
    addGasto({ concepto: gastoForm.concepto.trim(), importe: Number(gastoForm.importe), categoria: gastoForm.categoria });
    setGastoForm({ concepto: "", importe: "", categoria: "" });
    setShowGasto(false);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Resultados"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => window.print(), className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(FileText, { size: 13 }), " Imprimir / Guardar PDF")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4 no-imprimir" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-2 mb-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: irMesActual }, "Este mes"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: irMesAnterior }, "Mes anterior")), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Desde" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: desde, onChange: (e) => setDesde(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hasta" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: hasta, onChange: (e) => setHasta(e.target.value) })))), /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end justify-between mb-4 pb-3", style: { borderBottom: "2px solid #9C7A34" } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[17px] font-semibold" }, "Resultados"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: "#6B7A6E" } }, "Del ", desde, " al ", hasta, " \xB7 generado el ", todayISO())), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 56, width: "auto" } })), huboLegacy && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4 no-imprimir", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, "Algunas salidas registradas antes de esta actualizaci\xF3n no tienen motivo ni precio de venta, as\xED que no entran en este c\xE1lculo. A partir de ahora, cada salida que registres desde Productos s\xED alimentar\xE1 estos resultados.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Ingresos por ventas"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1", style: { color: C.accent } }, "\u20AC", fmt(ingresos))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Coste de lo vendido"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, "\u20AC", fmt(costeVentas)))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Margen bruto"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-2xl font-bold mono" }, "\u20AC", fmt(margenBruto))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Sobre ventas"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono", style: { color: margenPct == null ? C.ink : margenPct < 15 ? C.red : C.accent } }, margenPct == null ? "\u2014" : `${fmt(margenPct)}%`)))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Lo que se ha perdido (a coste)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 md:grid-cols-4 gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Mermas (caducidad, deterioro, roturas de proceso...)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-lg font-semibold mono mt-1", style: { color: valorMermas > 0 ? C.red : C.ink } }, "\u20AC", fmt(valorMermas))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Autoconsumo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-lg font-semibold mono mt-1" }, "\u20AC", fmt(valorAutoconsumo))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Roturas"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-lg font-semibold mono mt-1", style: { color: valorRoturas > 0 ? C.red : C.ink } }, "\u20AC", fmt(valorRoturas))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Aceite de freidoras"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-lg font-semibold mono mt-1", style: { color: valorAceite > 0 ? C.red : C.ink } }, "\u20AC", fmt(valorAceite)))), mermasPorProducto.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-2" }, "D\xF3nde m\xE1s se pierde por mermas"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, mermasPorProducto.map((x, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, x.producto.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold", style: { color: C.red } }, "\u20AC", fmt(x.valor)))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2 no-imprimir" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium", style: { color: C.inkSoft } }, "Gastos generales del negocio (mensuales)"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setShowGasto(true) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 13 }), " A\xF1adir gasto")), gastosFijos.length === 0 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No has a\xF1adido gastos fijos (alquiler, luz, seguros\u2026)." })) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5 mb-4 no-imprimir" }, gastosFijos.map((g) => /* @__PURE__ */ import_react.default.createElement(Card, { key: g.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px]" }, g.concepto), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-3" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "mono text-[12.5px]" }, "\u20AC", fmt(g.importe), "/mes"), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => deleteGasto(g.id), "aria-label": "Eliminar gasto" }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 14, color: C.inkSoft }))))))), gastosPuntuales.filter((g) => g.fecha >= desde && g.fecha <= hasta).length > 0 && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2 no-imprimir", style: { color: C.inkSoft } }, "Facturas pagadas y ajustes de este periodo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5 mb-4 no-imprimir" }, gastosPuntuales.filter((g) => g.fecha >= desde && g.fecha <= hasta).map((g) => /* @__PURE__ */ import_react.default.createElement(Card, { key: g.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px]" }, g.concepto), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mono", style: { color: C.inkSoft } }, g.fecha)), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono text-[12.5px]" }, "\u20AC", fmt(g.importe))))))), desgloseGastosPorCategoria.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Desglose de gastos por categor\xEDa"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, desgloseGastosPorCategoria.map(({ categoria, importe }) => {
    const totalDesglose = desgloseGastosPorCategoria.reduce((a, x) => a + x.importe, 0);
    const pct = totalDesglose > 0 ? importe / totalDesglose * 100 : 0;
    return /* @__PURE__ */ import_react.default.createElement("div", { key: categoria }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12.5px] mb-0.5" }, /* @__PURE__ */ import_react.default.createElement("span", null, categoria), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, "\u20AC", fmt(importe), " ", /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, "(", fmt(pct), "%)"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "w-full rounded-full overflow-hidden", style: { height: 5, background: C.line } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "h-full rounded-full", style: { width: `${Math.min(100, pct)}%`, background: C.accent } })));
  }))), /* @__PURE__ */ import_react.default.createElement(Card, { style: { background: C.bg, border: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12px]", style: { color: C.inkSoft } }, "Gastos generales del periodo"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, "\u20AC", fmt(gastosGeneralesMensual * (dias / 30) + gastosPuntualesPeriodo))), costePersonalMensual > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12px]", style: { color: C.inkSoft } }, "Coste de personal del periodo", personalEsEstimado ? " (parte estimado)" : ""), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, "\u20AC", fmt(costePersonalMensual * (dias / 30)))), costeInventarioSinVenta > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12px]", style: { color: C.inkSoft } }, "Mercanc\xEDa perdida sin venta (mermas, roturas, autoconsumo, aceite)"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: C.red } }, "\u2212\u20AC", fmt(costeInventarioSinVenta))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between pt-2", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("span", { className: "font-semibold" }, "Resultado neto aproximado"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-bold text-[17px]", style: { color: resultadoNeto >= 0 ? C.accent : C.red } }, resultadoNeto >= 0 ? "" : "\u2212", "\u20AC", fmt(Math.abs(resultadoNeto)))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-1", style: { color: C.inkSoft } }, "Aproximado: los gastos fijos y el personal se reparten a prorrata de los d\xEDas del periodo, no d\xEDa a d\xEDa real", personalEsEstimado ? ", y el coste de personal sin cifra exacta usa bruto \xD7 1,32 como aproximaci\xF3n." : ".")), gastosTotalMensual > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mt-4", style: { background: C.chrome, color: "#fff" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] tracking-wide uppercase", style: { color: "#9CB6A9" } }, "Punto de equilibrio"), margenParaPE > 0 ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mt-1 mb-2" }, "Con un margen ", esTeorico ? "medio del cat\xE1logo" : "real de este periodo", " del ", /* @__PURE__ */ import_react.default.createElement("b", null, fmt(margenParaPE), "%"), ", necesitas facturar esto solo para cubrir tus gastos fijos", costePersonalMensual > 0 ? " y el personal" : "", ", sin ganar ni perder:"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: "#9CB6A9" } }, "Al mes"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-bold mono" }, "\u20AC", fmt(peMensual))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: "#9CB6A9" } }, "En este periodo (", dias, " d\xEDas)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-bold mono" }, "\u20AC", fmt(pePeriodo)))), esTeorico && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-2", style: { color: "#9CB6A9" } }, "Todav\xEDa no hay ventas registradas en este periodo, as\xED que se usa el margen medio de los productos que ya tienen precio en el cat\xE1logo. En cuanto registres ventas reales, se calcular\xE1 con datos de verdad.")) : /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mt-1" }, "Ponle precio de venta a tus productos (o registra alguna venta) para poder calcular esto."))), showGasto && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowGasto(false), title: "A\xF1adir gasto general" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Concepto" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: gastoForm.concepto, onChange: (e) => setGastoForm({ ...gastoForm, concepto: e.target.value }), placeholder: "Alquiler, luz, agua, seguros\u2026" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Importe mensual (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: gastoForm.importe, onChange: (e) => setGastoForm({ ...gastoForm, importe: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Categor\xEDa" }, /* @__PURE__ */ import_react.default.createElement(CampoCategoria, { value: gastoForm.categoria, onChange: (v) => setGastoForm({ ...gastoForm, categoria: v }), opciones: CATEGORIAS_GASTO })), gastoError && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, gastoError), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submitGasto }, "Guardar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowGasto(false) }, "Cancelar"))));
}
function CuentasPorPagar({ facturasPorPagar, totalPendientePago, marcarPagada, marcarPagadaFacturaDirecta, addFacturaDirecta, deleteFacturaDirecta, proveedores, resaltadaId, limpiarResaltada }) {
  const [verPagadas, setVerPagadas] = (0, import_react.useState)(false);
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [form, setForm] = (0, import_react.useState)({ proveedorId: "", concepto: "", numeroFactura: "", fecha: todayISO(), importeBase: "", ivaPct: 21, vencimiento: "", categoria: "" });
  const [error, setError] = (0, import_react.useState)("");
  const pendientes = facturasPorPagar.filter((f) => !f.pagada);
  const pagadas = facturasPorPagar.filter((f) => f.pagada);
  const vencidas = pendientes.filter((f) => f.dias !== null && f.dias < 0);
  const proximas = pendientes.filter((f) => f.dias !== null && f.dias >= 0 && f.dias <= 7);
  const sinDatos = pendientes.filter((f) => f.dias === null);
  (0, import_react.useEffect)(() => {
    if (resaltadaId && pagadas.some((f) => f.id === resaltadaId)) setVerPagadas(true);
  }, [resaltadaId]);
  (0, import_react.useEffect)(() => () => {
    if (limpiarResaltada) limpiarResaltada();
  }, []);
  const colorPara = (f) => {
    if (f.dias === null) return C.inkSoft;
    if (f.dias < 0) return C.red;
    if (f.dias <= 7) return C.amber;
    return C.ink;
  };
  const textoPara = (f) => {
    if (f.dias === null) return "Sin d\xEDas de pago";
    if (f.dias < 0) return `Vencida hace ${Math.abs(f.dias)} d`;
    if (f.dias === 0) return "Vence hoy";
    return `Vence en ${f.dias} d`;
  };
  function marcar(f, pagada) {
    if (f.origen === "directa") marcarPagadaFacturaDirecta(f.id, pagada);
    else marcarPagada(f.id, pagada);
  }
  function submit() {
    if (!form.concepto.trim()) {
      setError('Escribe de qu\xE9 es el gasto (ej. "Electricidad julio").');
      return;
    }
    if (!(Number(form.importeBase) > 0)) {
      setError("Pon un importe mayor que cero.");
      return;
    }
    setError("");
    addFacturaDirecta(form);
    setForm({ proveedorId: "", concepto: "", numeroFactura: "", fecha: todayISO(), importeBase: "", ivaPct: 21, vencimiento: "", categoria: "" });
    setShowForm(false);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => setShowForm((s) => !s) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Factura directa") }, "Cuentas por pagar"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Junta dos or\xEDgenes: las facturas dadas de entrada por Albaranes (mercanc\xEDa recibida) y las ", /* @__PURE__ */ import_react.default.createElement("b", null, "facturas directas"), " que registres aqu\xED a mano \u2014 luz, alquiler, gestor\xEDa, cualquier gasto sin productos de por medio. El vencimiento sale de la fecha de factura m\xE1s los d\xEDas de pago del proveedor, o de la fecha que pongas t\xFA a mano si no hay proveedor con ese dato.")), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, "Nueva factura directa"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "\xBFDe qu\xE9 es? (concepto)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.concepto, onChange: (e) => setForm({ ...form, concepto: e.target.value }), placeholder: "Electricidad julio, alquiler local\u2026" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Proveedor (opcional)" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.proveedorId, onChange: (e) => setForm({ ...form, proveedorId: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Sin asignar"), proveedores.map((pv) => /* @__PURE__ */ import_react.default.createElement("option", { key: pv.id, value: pv.id }, pv.nombre)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "N\xFAmero de factura (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.numeroFactura, onChange: (e) => setForm({ ...form, numeroFactura: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Fecha de la factura" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: form.fecha, onChange: (e) => setForm({ ...form, fecha: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Importe sin IVA (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.importeBase, onChange: (e) => setForm({ ...form, importeBase: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "IVA %" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.ivaPct, onChange: (e) => setForm({ ...form, ivaPct: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, TIPOS_IVA.map((v) => /* @__PURE__ */ import_react.default.createElement("option", { key: v, value: v }, v, "%")))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Vencimiento manual (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: form.vencimiento, onChange: (e) => setForm({ ...form, vencimiento: e.target.value }), placeholder: "Si no, se calcula por d\xEDas de pago" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Categor\xEDa" }, /* @__PURE__ */ import_react.default.createElement(CampoCategoria, { value: form.categoria, onChange: (v) => setForm({ ...form, categoria: v }), opciones: CATEGORIAS_GASTO }))), Number(form.importeBase) > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.inkSoft } }, "Total con IVA: ", /* @__PURE__ */ import_react.default.createElement("b", { className: "mono" }, "\u20AC", fmt(Number(form.importeBase) * (1 + Number(form.ivaPct) / 100)))), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, "Guardar factura"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowForm(false) }, "Cancelar"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Pendiente de pagar"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, "\u20AC", fmt(totalPendientePago))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Vencidas / pr\xF3ximos 7 d\xEDas"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1", style: { color: vencidas.length ? C.red : proximas.length ? C.amber : C.ink } }, vencidas.length + proximas.length))), pendientes.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No hay facturas pendientes de pago." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2 mb-4" }, pendientes.map((f) => /* @__PURE__ */ import_react.default.createElement(Card, { key: f.id, style: f.id === resaltadaId ? { border: `2px solid ${C.accent}` } : void 0 }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "font-semibold" }, f.proveedor ? f.proveedor.nombre : f.origen === "directa" ? "Sin proveedor" : "Proveedor eliminado"), /* @__PURE__ */ import_react.default.createElement(Pill, { color: f.origen === "directa" ? C.amber : C.inkSoft }, f.origen === "directa" ? "Directa" : "Mercanc\xEDa")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, f.concepto), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, "Factura ", f.numeroFactura, " \xB7 ", f.fechaFactura, f.vencimiento && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, " \xB7 vence ", new Date(f.vencimiento).toLocaleDateString("es-ES")))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, "\u20AC", fmt(f.total)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] font-medium mt-0.5", style: { color: colorPara(f) } }, textoPara(f)))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => marcar(f, true) }, /* @__PURE__ */ import_react.default.createElement(CircleCheck, { size: 13 }), " Marcar como pagada"), f.origen === "directa" && /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => deleteFacturaDirecta(f.id) }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 13 }), " Eliminar"))))), sinDatos.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, sinDatos.length, ' factura(s) sin poder calcular vencimiento porque falta el proveedor con "d\xEDas de pago" puestos, o el vencimiento manual. A\xF1\xE1delo en Proveedores, o ed\xEDtalo a mano si es una factura directa.')), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setVerPagadas((s) => !s), className: "text-[12.5px] font-medium mb-2", style: { color: C.accent } }, verPagadas ? "Ocultar" : "Ver", " facturas ya pagadas (", pagadas.length, ")"), verPagadas && /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, pagadas.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has marcado ninguna como pagada." }) : pagadas.map((f) => /* @__PURE__ */ import_react.default.createElement(Card, { key: f.id, style: f.id === resaltadaId ? { background: C.bg, border: `2px solid ${C.accent}` } : { background: C.bg } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] flex items-center gap-1.5" }, f.proveedor ? f.proveedor.nombre : f.origen === "directa" ? "Sin proveedor" : "Proveedor eliminado", /* @__PURE__ */ import_react.default.createElement(Pill, { color: f.origen === "directa" ? C.amber : C.inkSoft }, f.origen === "directa" ? "Directa" : "Mercanc\xEDa")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, f.concepto, " \xB7 Factura ", f.numeroFactura)), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "mono text-[12.5px]", style: { color: C.inkSoft } }, "\u20AC", fmt(f.total)), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => marcar(f, false) }, "Deshacer")))))));
}
var TIPOS_CONTRATO = [
  "Indefinido",
  "Indefinido fijo-discontinuo",
  "Temporal \u2014 circunstancias de la producci\xF3n",
  "Temporal \u2014 sustituci\xF3n",
  "Formaci\xF3n en alternancia",
  "Obtenci\xF3n de pr\xE1ctica profesional"
];
function blankEmpleado() {
  return {
    nombre: "",
    dni: "",
    puesto: "",
    rol: "Est\xE1ndar",
    diasDisponibles: [],
    tipoContrato: "Indefinido",
    fechaAlta: todayISO(),
    fechaFinContrato: "",
    horasSemanales: 40,
    pagas: 14,
    salarioBrutoMensual: "",
    costeEmpresaMensual: "",
    diasVacacionesAnuales: 30,
    pin: "",
    documentos: [],
    activo: true
  };
}
var NIVELES_RECOMENDACION = {
  contratar: { texto: "Contratar", color: "#16a34a" },
  contratar_con_formacion: { texto: "Contratar con formaci\xF3n inicial", color: "#16a34a" },
  segunda_entrevista: { texto: "Realizar segunda entrevista", color: "#d97706" },
  prueba_practica: { texto: "Realizar prueba pr\xE1ctica", color: "#d97706" },
  no_recomendado: { texto: "No recomendado actualmente", color: "#dc2626" }
};
function colorPuntuacion(p) {
  if (p >= 80) return "#16a34a";
  if (p >= 60) return "#d97706";
  return "#dc2626";
}
function SeleccionPersonal({ entrevistas, crearEntrevista, actualizarEntrevista, finalizarEntrevista, eliminarEntrevista }) {
  const [activaId, setActivaId] = (0, import_react.useState)(null);
  const [verInformeId, setVerInformeId] = (0, import_react.useState)(null);
  const [nombreNuevo, setNombreNuevo] = (0, import_react.useState)("");
  const [showNuevo, setShowNuevo] = (0, import_react.useState)(false);
  const [respuestaActual, setRespuestaActual] = (0, import_react.useState)("");
  const [cargando, setCargando] = (0, import_react.useState)(false);
  const [error, setError] = (0, import_react.useState)("");
  const [confirmarEliminar, setConfirmarEliminar] = (0, import_react.useState)(null);
  const activa = entrevistas.find((e) => e.id === activaId);
  const verInforme = entrevistas.find((e) => e.id === verInformeId);
  async function llamarIA(entrevistaId, historial, finalizar) {
    setCargando(true);
    setError("");
    try {
      const resp = await fetch(
        "https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/entrevista-personal",
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ historial, finalizar: !!finalizar })
        }
      );
      const r = await resp.json();
      if (!r.ok) {
        setError(r.error || "No se ha podido conectar con la IA. Int\xE9ntalo otra vez.");
        return;
      }
      if (r.terminado) {
        finalizarEntrevista(entrevistaId, r.informe);
        setActivaId(null);
        setVerInformeId(entrevistaId);
      } else {
        actualizarEntrevista(entrevistaId, { siguientePregunta: r.siguiente_pregunta });
      }
    } catch {
      setError("No se ha podido conectar con la IA. Comprueba tu conexi\xF3n e int\xE9ntalo otra vez.");
    } finally {
      setCargando(false);
    }
  }
  function empezar() {
    if (!nombreNuevo.trim()) return;
    const nueva = crearEntrevista(nombreNuevo.trim());
    setNombreNuevo("");
    setShowNuevo(false);
    setActivaId(nueva.id);
    llamarIA(nueva.id, []);
  }
  function enviarRespuesta() {
    if (!respuestaActual.trim() || !activa) return;
    const nuevoHistorial = [...activa.historial, { pregunta: activa.siguientePregunta, respuesta: respuestaActual.trim() }];
    actualizarEntrevista(activa.id, { historial: nuevoHistorial, siguientePregunta: null });
    setRespuestaActual("");
    llamarIA(activa.id, nuevoHistorial);
  }
  function finalizarAhora() {
    if (!activa || activa.historial.length === 0) return;
    llamarIA(activa.id, activa.historial, true);
  }
  if (activa) {
    return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[15px] font-semibold" }, "Entrevista \xB7 ", activa.candidatoNombre), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setActivaId(null) }, "Pausar (seguir luego)")), activa.historial.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] font-medium mb-2", style: { color: C.inkSoft } }, "Ya respondido"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2 max-h-[30vh] overflow-y-auto" }, activa.historial.map((h, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft } }, h.pregunta), /* @__PURE__ */ import_react.default.createElement("div", { className: "font-medium" }, h.respuesta))))), cargando ? /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4 text-center py-6" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px]", style: { color: C.inkSoft } }, "La IA est\xE1 pensando la siguiente pregunta\u2026")) : activa.siguientePregunta ? /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, activa.siguientePregunta), /* @__PURE__ */ import_react.default.createElement(
      "textarea",
      {
        value: respuestaActual,
        onChange: (e) => setRespuestaActual(e.target.value),
        placeholder: "Escribe aqu\xED lo que responde el candidato\u2026",
        className: "w-full rounded-lg px-3 py-2 text-[13px] mb-3",
        style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink, minHeight: 90 }
      }
    ), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: enviarRespuesta, disabled: !respuestaActual.trim() }, "Siguiente pregunta"), activa.historial.length > 0 && /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: finalizarAhora }, "Finalizar entrevista ahora"))) : null, error && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.redSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, error)));
  }
  if (verInforme && verInforme.informe) {
    const inf = verInforme.informe;
    const rec = NIVELES_RECOMENDACION[inf.recomendacion_final] || { texto: inf.recomendacion_final, color: C.ink };
    return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[15px] font-semibold" }, verInforme.candidatoNombre, " \xB7 ", verInforme.fecha), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setVerInformeId(null) }, "Volver")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: rec.color + "18", border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-1", style: { color: C.inkSoft } }, "Recomendaci\xF3n final"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[15px] font-semibold", style: { color: rec.color } }, rec.texto), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mt-1" }, inf.recomendacion_explicacion)), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-2 mb-4" }, ["camarero", "cajero", "churrero"].map((p) => {
      const d = inf.puntuaciones?.[p];
      if (!d) return null;
      const etiqueta = p === "camarero" ? "Camarero/a" : p === "cajero" ? "Cajero/a" : "Churrero/a";
      return /* @__PURE__ */ import_react.default.createElement(Card, { key: p, className: "text-center py-2.5" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, etiqueta), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[18px] font-semibold mono", style: { color: colorPuntuacion(d.puntuacion) } }, d.puntuacion), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10px]", style: { color: C.inkSoft } }, d.nivel), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10px] mt-1", style: { color: C.inkSoft } }, "Confianza ", d.confianza, "%"));
    })), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-1" }, /* @__PURE__ */ import_react.default.createElement("b", null, "Puesto principal:"), " ", inf.puesto_principal), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2", style: { color: C.inkSoft } }, inf.puesto_principal_razon), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-1" }, /* @__PURE__ */ import_react.default.createElement("b", null, "Segunda opci\xF3n:"), " ", inf.segunda_opcion), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, inf.segunda_opcion_razon)), inf.pruebas_practicas_recomendadas?.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-1" }, "Se recomienda prueba pr\xE1ctica para: ", inf.pruebas_practicas_recomendadas.join(", ")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]" }, "La puntuaci\xF3n es orientativa pero la confianza es baja o moderada en ese puesto \u2014 conviene comprobarlo en la pr\xE1ctica antes de decidir del todo.")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2" }, "Fortalezas"), /* @__PURE__ */ import_react.default.createElement("ul", { className: "text-[12px] space-y-1 list-disc pl-4" }, (inf.fortalezas || []).map((f, i) => /* @__PURE__ */ import_react.default.createElement("li", { key: i }, f)))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2" }, "Aspectos a mejorar"), /* @__PURE__ */ import_react.default.createElement("ul", { className: "text-[12px] space-y-1 list-disc pl-4" }, (inf.aspectos_a_mejorar || []).map((f, i) => /* @__PURE__ */ import_react.default.createElement("li", { key: i }, f)))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: inf.senales_de_riesgo?.length && inf.senales_de_riesgo[0] !== "Ninguna relevante detectada" ? C.redSoft : void 0, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2" }, "Se\xF1ales de riesgo"), /* @__PURE__ */ import_react.default.createElement("ul", { className: "text-[12px] space-y-1 list-disc pl-4" }, (inf.senales_de_riesgo || ["Ninguna relevante detectada"]).map((f, i) => /* @__PURE__ */ import_react.default.createElement("li", { key: i }, f)))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2" }, "Perfil profesional"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, inf.perfil_profesional)), inf.competencias_generales && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2" }, "Competencias generales"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, Object.entries(inf.competencias_generales).map(([k, v]) => /* @__PURE__ */ import_react.default.createElement("div", { key: k, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, k.replace(/_/g, " ")), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: colorPuntuacion(v) } }, v, "/100"))))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2" }, "Por qu\xE9 esta confianza"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, inf.justificacion_confianza)), /* @__PURE__ */ import_react.default.createElement("details", { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("summary", { className: "text-[12px] cursor-pointer", style: { color: C.inkSoft } }, "Ver transcripci\xF3n completa de la entrevista"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 space-y-2" }, verInforme.historial.map((h, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "text-[12px] p-2 rounded-lg", style: { background: C.surfaceSoft } }, /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft } }, h.pregunta), /* @__PURE__ */ import_react.default.createElement("div", { className: "font-medium" }, h.respuesta))))));
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Entrevista al candidato en persona o por tel\xE9fono, y ve escribiendo aqu\xED sus respuestas \u2014 la IA decide la siguiente pregunta y, al final, genera un informe puntuado para Camarero/a, Cajero/a y Churrero/a. La evaluaci\xF3n nunca tiene en cuenta nacionalidad, sexo, edad ni ninguna caracter\xEDstica ajena al desempe\xF1o.")), !showNuevo ? /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => setShowNuevo(true), className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nueva entrevista") : /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre del candidato" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: nombreNuevo, onChange: (e) => setNombreNuevo(e.target.value), autoFocus: true, placeholder: "Nombre y apellido" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: empezar, disabled: !nombreNuevo.trim() }, "Empezar entrevista"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowNuevo(false);
    setNombreNuevo("");
  } }, "Cancelar"))), entrevistas.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin entrevistas todav\xEDa." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, entrevistas.map((e) => /* @__PURE__ */ import_react.default.createElement(Card, { key: e.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-medium" }, e.candidatoNombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, e.fecha, " \xB7 ", e.estado === "completada" ? `Recomendado: ${e.informe?.puesto_principal || "\u2014"}` : "En curso")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, e.estado === "completada" ? /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setVerInformeId(e.id) }, "Ver informe") : /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setActivaId(e.id) }, "Continuar"), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setConfirmarEliminar(e), "aria-label": "Eliminar entrevista" }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 14, color: C.inkSoft }))))))), confirmarEliminar && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmarEliminar(null), title: "Eliminar entrevista" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] mb-4" }, "\xBFEliminar la entrevista de ", /* @__PURE__ */ import_react.default.createElement("b", null, confirmarEliminar.candidatoNombre), "? Se pierde la transcripci\xF3n y el informe."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmarEliminar(null) }, "Cancelar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: () => {
    eliminarEntrevista(confirmarEliminar.id);
    setConfirmarEliminar(null);
  } }, "Eliminar"))));
}
function Personal({ empleados, addEmpleado, updateEmpleado, deleteEmpleado, anonimizarEmpleado, registrarAusencia, eliminarAusencia, registrarEpi, eliminarEpi, documentosPersonalCaducan, fichajes = [], nominas = [], entrevistas = [], crearEntrevista, actualizarEntrevista, finalizarEntrevista, eliminarEntrevista }) {
  const [vista, setVista] = (0, import_react.useState)("empleados");
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [form, setForm] = (0, import_react.useState)(blankEmpleado());
  const [error, setError] = (0, import_react.useState)("");
  const [editingId, setEditingId] = (0, import_react.useState)(null);
  const [confirmDeleteId, setConfirmDeleteId] = (0, import_react.useState)(null);
  const [ausenciaFor, setAusenciaFor] = (0, import_react.useState)(null);
  const [ausenciaForm, setAusenciaForm] = (0, import_react.useState)({ tipo: "Vacaciones", fechaInicio: todayISO(), fechaFin: todayISO() });
  const [epiFor, setEpiFor] = (0, import_react.useState)(null);
  const [epiForm, setEpiForm] = (0, import_react.useState)({ nombre: "", firmado: false });
  const [detalleId, setDetalleId] = (0, import_react.useState)(null);
  function resetForm() {
    setForm(blankEmpleado());
    setEditingId(null);
    setError("");
  }
  function openEdit(e) {
    setEditingId(e.id);
    setForm({
      nombre: e.nombre || "",
      dni: e.dni || "",
      puesto: e.puesto || "",
      rol: e.rol && ROLES_EMPLEADO[e.rol] ? e.rol : "Est\xE1ndar",
      diasDisponibles: e.diasDisponibles || [],
      tipoContrato: e.tipoContrato || "Indefinido",
      fechaAlta: e.fechaAlta || todayISO(),
      fechaFinContrato: e.fechaFinContrato || "",
      horasSemanales: e.horasSemanales ?? 40,
      pagas: e.pagas ?? 14,
      salarioBrutoMensual: e.salarioBrutoMensual ?? "",
      costeEmpresaMensual: e.costeEmpresaMensual ?? "",
      diasVacacionesAnuales: e.diasVacacionesAnuales ?? 30,
      pin: "",
      documentos: (e.documentos || []).map((d) => ({ ...d })),
      activo: e.activo !== false
    });
    setError("");
    setShowForm(true);
  }
  function addDocumento() {
    setForm((f) => ({ ...f, documentos: [...f.documentos, { id: uid(), nombre: "", fechaCaducidad: "" }] }));
  }
  function updateDocumento(idx, campo, valor) {
    setForm((f) => ({ ...f, documentos: f.documentos.map((d, i) => i === idx ? { ...d, [campo]: valor } : d) }));
  }
  function quitarDocumento(idx) {
    setForm((f) => ({ ...f, documentos: f.documentos.filter((_, i) => i !== idx) }));
  }
  async function submit() {
    if (!form.nombre.trim()) {
      setError("Escribe el nombre del empleado.");
      return;
    }
    if (form.pin && !/^\d{4,6}$/.test(form.pin)) {
      setError("El c\xF3digo de acceso debe tener entre 4 y 6 d\xEDgitos.");
      return;
    }
    let hashNuevoPin = null;
    if (form.pin) {
      hashNuevoPin = await hashPin(form.pin);
      if (empleados.some((e) => e.id !== editingId && e.pin === hashNuevoPin)) {
        setError("Ese c\xF3digo ya lo usa otro empleado. Ponle uno distinto.");
        return;
      }
    }
    setError("");
    const datos = {
      ...form,
      horasSemanales: Number(form.horasSemanales) || 0,
      pagas: Number(form.pagas) || 14,
      salarioBrutoMensual: Number(form.salarioBrutoMensual) || 0,
      costeEmpresaMensual: form.costeEmpresaMensual === "" ? "" : Number(form.costeEmpresaMensual),
      diasVacacionesAnuales: Number(form.diasVacacionesAnuales) || 0
    };
    if (hashNuevoPin) {
      datos.pin = hashNuevoPin;
    } else {
      delete datos.pin;
    }
    if (editingId) updateEmpleado(editingId, datos);
    else addEmpleado(datos);
    resetForm();
    setShowForm(false);
  }
  function diasAusencia(ini, fin) {
    const a = new Date(ini), b = new Date(fin);
    if (isNaN(a) || isNaN(b)) return 0;
    return Math.max(1, Math.round((b - a) / 864e5) + 1);
  }
  function submitAusencia() {
    registrarAusencia(ausenciaFor, {
      tipo: ausenciaForm.tipo,
      fechaInicio: ausenciaForm.fechaInicio,
      fechaFin: ausenciaForm.fechaFin,
      dias: diasAusencia(ausenciaForm.fechaInicio, ausenciaForm.fechaFin)
    });
    setAusenciaFor(null);
    setAusenciaForm({ tipo: "Vacaciones", fechaInicio: todayISO(), fechaFin: todayISO() });
  }
  const anioActual = (/* @__PURE__ */ new Date()).getFullYear();
  function descargarComoArchivo(nombreArchivo, texto) {
    const blob = new Blob([texto], { type: "text/plain;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = nombreArchivo;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  }
  function exportarDatosEmpleado(e) {
    const fichajesDelEmpleado = fichajes.filter((f) => f.empleadoId === e.id);
    const nominasDelEmpleado = nominas.filter((n) => n.empleadoId === e.id);
    const lineas = [
      `DATOS GUARDADOS SOBRE: ${e.nombre}`,
      `Generado el ${todayISO()}`,
      "",
      "=== FICHA ===",
      `Nombre: ${e.nombre}`,
      `DNI: ${e.dni || "\u2014"}`,
      `Puesto: ${e.puesto || "\u2014"}`,
      `Tipo de contrato: ${e.tipoContrato || "\u2014"}`,
      `Fecha de alta: ${e.fechaAlta || "\u2014"}`,
      `Fecha de fin de contrato: ${e.fechaFinContrato || "\u2014"}`,
      `Horas semanales: ${e.horasSemanales ?? "\u2014"}`,
      `Estado: ${e.activo === false ? "de baja" : "activo"}`,
      "",
      "=== AUSENCIAS ===",
      ...(e.ausencias || []).length === 0 ? ["Ninguna registrada."] : (e.ausencias || []).map((a) => `${a.tipo}: ${a.fechaInicio} a ${a.fechaFin} (${a.dias} d\xEDa/s)`),
      "",
      "=== DOCUMENTOS ===",
      ...(e.documentos || []).length === 0 ? ["Ninguno registrado."] : (e.documentos || []).map((d) => `${d.nombre}${d.fechaCaducidad ? ` \u2014 caduca ${d.fechaCaducidad}` : ""}`),
      "",
      `=== FICHAJES (${fichajesDelEmpleado.length} registrados) ===`,
      ...fichajesDelEmpleado.length === 0 ? ["Ninguno registrado."] : fichajesDelEmpleado.slice(0, 200).map((f) => `${f.fecha} ${f.hora} \u2014 ${f.tipo}`),
      fichajesDelEmpleado.length > 200 ? `\u2026 y ${fichajesDelEmpleado.length - 200} m\xE1s (se han omitido por espacio).` : "",
      "",
      `=== N\xD3MINAS REGISTRADAS (${nominasDelEmpleado.length}) ===`,
      ...nominasDelEmpleado.length === 0 ? ["Ninguna registrada."] : nominasDelEmpleado.map((n) => `${n.mes}: bruto \u20AC${fmt(n.brutoTotal)} + SS empresa \u20AC${fmt(n.seguridadSocialEmpresa)}`)
    ];
    descargarComoArchivo(`datos-${e.nombre.replace(/\s+/g, "-").toLowerCase()}.txt`, lineas.filter((l) => l !== "").join("\n"));
  }
  function vacacionesUsadas(e) {
    return (e.ausencias || []).filter((a) => a.tipo === "Vacaciones" && new Date(a.fechaInicio).getFullYear() === anioActual).reduce((acc, a) => acc + (Number(a.dias) || 0), 0);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: vista === "empleados" ? /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
    if (showForm) resetForm();
    setShowForm((s) => !s);
  } }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nuevo empleado") : null }, "Personal"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-1.5 mb-4" }, /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      onClick: () => setVista("empleados"),
      className: "text-[12px] font-medium px-3 py-1.5 rounded-full",
      style: vista === "empleados" ? { background: C.accent, color: "#fff" } : { background: C.surface, border: `1px solid ${C.line}`, color: C.inkSoft }
    },
    "Empleados"
  ), /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      onClick: () => setVista("seleccion"),
      className: "text-[12px] font-medium px-3 py-1.5 rounded-full",
      style: vista === "seleccion" ? { background: C.accent, color: "#fff" } : { background: C.surface, border: `1px solid ${C.line}`, color: C.inkSoft }
    },
    "Selecci\xF3n de personal"
  )), vista === "seleccion" ? /* @__PURE__ */ import_react.default.createElement(
    SeleccionPersonal,
    {
      entrevistas,
      crearEntrevista,
      actualizarEntrevista,
      finalizarEntrevista,
      eliminarEntrevista
    }
  ) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Aqu\xED llevas la ficha de cada empleado, sus documentos y sus vacaciones. Este programa ", /* @__PURE__ */ import_react.default.createElement("b", null, "no calcula n\xF3minas"), "(retenciones de IRPF, cotizaciones a la Seguridad Social): eso debe hacerlo tu gestor\xEDa o un programa de n\xF3minas certificado, porque las cifras cambian cada a\xF1o y un error tiene consecuencias legales reales.")), documentosPersonalCaducan.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.redSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-2 flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement(TriangleAlert, { size: 15, color: C.red }), " Documentos y contratos que necesitan atenci\xF3n"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1" }, documentosPersonalCaducan.slice(0, 8).map((d) => /* @__PURE__ */ import_react.default.createElement("div", { key: d.id, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, d.empleado, " \xB7 ", d.concepto), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold", style: { color: d.dias < 0 ? C.red : d.dias <= 7 ? C.amber : C.inkSoft } }, d.dias < 0 ? `venci\xF3 hace ${Math.abs(d.dias)} d` : d.dias === 0 ? "hoy" : `en ${d.dias} d`))))), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, editingId ? "Editando empleado" : "Nuevo empleado"), /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "1 \xB7 Identificaci\xF3n" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre completo" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.nombre, onChange: (e) => setForm({ ...form, nombre: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "DNI / NIE (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.dni, onChange: (e) => setForm({ ...form, dni: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Puesto / categor\xEDa" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.puesto, onChange: (e) => setForm({ ...form, puesto: e.target.value }), placeholder: "Dependienta, obrador\u2026" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nivel de acceso en modo empleado" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.rol, onChange: (e) => setForm({ ...form, rol: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, NOMBRES_ROLES.map((r) => /* @__PURE__ */ import_react.default.createElement("option", { key: r, value: r }, r))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: C.inkSoft } }, form.rol === "B\xE1sico" && "Solo panel, venta y fichaje \u2014 para alguien que empieza.", form.rol === "Est\xE1ndar" && "A\xF1ade recepci\xF3n, conteo, APPCC, mapa y traspasos.", form.rol === "Encargado" && "A\xF1ade adem\xE1s pedidos, productos y proveedores.")), /* @__PURE__ */ import_react.default.createElement(Field, { label: "D\xEDas que suele poder trabajar (opcional)" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-1.5 flex-wrap" }, DIAS_REPARTO.map((d) => {
    const activo = (form.diasDisponibles || []).includes(d.valor);
    return /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: d.valor,
        type: "button",
        onClick: () => setForm((f) => ({
          ...f,
          diasDisponibles: (f.diasDisponibles || []).includes(d.valor) ? f.diasDisponibles.filter((x) => x !== d.valor) : [...f.diasDisponibles || [], d.valor]
        })),
        className: "w-9 h-9 rounded-full text-[12.5px] font-semibold",
        style: {
          background: activo ? C.accent : C.surface,
          color: activo ? "#fff" : C.inkSoft,
          border: `1px solid ${activo ? C.accent : C.line}`
        }
      },
      d.corta
    );
  })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: C.inkSoft } }, "Sin marcar ninguno, Turnos no avisa de nada \u2014 solo avisa si marcas algo y luego se le asigna un turno un d\xEDa que no est\xE1 entre los marcados.")))), /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "2 \xB7 Contrato" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tipo de contrato" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.tipoContrato, onChange: (e) => setForm({ ...form, tipoContrato: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, TIPOS_CONTRATO.map((t) => /* @__PURE__ */ import_react.default.createElement("option", { key: t, value: t }, t)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Fecha de alta" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: form.fechaAlta, onChange: (e) => setForm({ ...form, fechaAlta: e.target.value }) })), form.tipoContrato.startsWith("Temporal") || form.tipoContrato.startsWith("Formaci\xF3n") || form.tipoContrato.startsWith("Obtenci\xF3n") ? /* @__PURE__ */ import_react.default.createElement(Field, { label: "Fecha de fin de contrato" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: form.fechaFinContrato, onChange: (e) => setForm({ ...form, fechaFinContrato: e.target.value }) })) : /* @__PURE__ */ import_react.default.createElement(Field, { label: "Horas semanales" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.horasSemanales, onChange: (e) => setForm({ ...form, horasSemanales: e.target.value }) }))), (form.tipoContrato.startsWith("Temporal") || form.tipoContrato.startsWith("Formaci\xF3n") || form.tipoContrato.startsWith("Obtenci\xF3n")) && /* @__PURE__ */ import_react.default.createElement(Field, { label: "Horas semanales" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.horasSemanales, onChange: (e) => setForm({ ...form, horasSemanales: e.target.value }) }))), /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "3 \xB7 Retribuci\xF3n" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Salario bruto mensual (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.salarioBrutoMensual, onChange: (e) => setForm({ ...form, salarioBrutoMensual: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "N\xBA de pagas al a\xF1o" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.pagas, onChange: (e) => setForm({ ...form, pagas: e.target.value }), placeholder: "12 o 14" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Coste total mensual para la empresa (\u20AC, opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.costeEmpresaMensual, onChange: (e) => setForm({ ...form, costeEmpresaMensual: e.target.value }), placeholder: "Bruto + Seg. Social, seg\xFAn tu gestor\xEDa" }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Si dejas el coste para la empresa en blanco, se usar\xE1 una estimaci\xF3n aproximada (bruto \xD7 1,32) para calcular resultados \u2014 p\xEDdele la cifra exacta a tu gestor\xEDa cuando la tengas.")), /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "4 \xB7 Vacaciones" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "D\xEDas de vacaciones al a\xF1o" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.diasVacacionesAnuales, onChange: (e) => setForm({ ...form, diasVacacionesAnuales: e.target.value }) }))), /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "5 \xB7 Acceso al programa (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "C\xF3digo de acceso (4 a 6 d\xEDgitos)" }, /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      type: "password",
      inputMode: "numeric",
      value: form.pin,
      onChange: (e) => setForm({ ...form, pin: e.target.value }),
      placeholder: editingId && empleados.find((e) => e.id === editingId)?.pin ? "Ya tiene un c\xF3digo \u2014 d\xE9jalo en blanco para no cambiarlo" : "Para que entre en modo empleado con su propio c\xF3digo"
    }
  )), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-1", style: { color: C.inkSoft } }, 'Si le pones un c\xF3digo, podr\xE1 entrar en modo empleado identific\xE1ndose con su nombre \u2014 y quedar\xE1 su nombre, no solo "Empleado", en la Auditor\xEDa. Es opcional: sin c\xF3digo, no podr\xE1 entrar por su cuenta.')), /* @__PURE__ */ import_react.default.createElement(Bloque, { titulo: "6 \xB7 Documentos con caducidad (opcional)" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-2", style: { color: C.inkSoft } }, "Certificado m\xE9dico, formaci\xF3n en manipulaci\xF3n de alimentos, permiso de trabajo\u2026"), form.documentos.map((d, idx) => /* @__PURE__ */ import_react.default.createElement("div", { key: d.id, className: "grid grid-cols-5 gap-2 items-center mb-2" }, /* @__PURE__ */ import_react.default.createElement(Input, { className: "col-span-3", value: d.nombre, onChange: (e) => updateDocumento(idx, "nombre", e.target.value), placeholder: "Formaci\xF3n manipulador de alimentos" }), /* @__PURE__ */ import_react.default.createElement(Input, { className: "col-span-1", type: "date", value: d.fechaCaducidad, onChange: (e) => updateDocumento(idx, "fechaCaducidad", e.target.value) }), /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: () => quitarDocumento(idx), "aria-label": "Quitar documento" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 16, color: C.inkSoft })))), /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: addDocumento, className: "text-[12.5px] font-medium", style: { color: C.accent } }, "+ A\xF1adir documento"), " \xB7 ", /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      type: "button",
      onClick: () => setForm((f) => ({ ...f, documentos: [...f.documentos, { id: uid(), nombre: "Formaci\xF3n PRL", fechaCaducidad: "" }] })),
      className: "text-[12.5px] font-medium",
      style: { color: C.amber }
    },
    "+ Formaci\xF3n PRL (obligatoria)"
  )), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, editingId ? "Guardar cambios" : "Guardar empleado"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowForm(false);
    resetForm();
  } }, "Cancelar"))), empleados.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has a\xF1adido a nadie." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-3" }, empleados.map((e) => {
    const usados = vacacionesUsadas(e);
    const total = Number(e.diasVacacionesAnuales) || 0;
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: e.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold flex items-center gap-1.5" }, e.nombre, e.activo === false && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.inkSoft }, "baja"), e.pin && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.accent }, "con acceso"), e.pin && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.inkSoft }, e.rol && ROLES_EMPLEADO[e.rol] ? e.rol : "Est\xE1ndar")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mt-0.5", style: { color: C.inkSoft } }, e.puesto, " \xB7 ", e.tipoContrato)), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => exportarDatosEmpleado(e), title: "Exportar sus datos" }, /* @__PURE__ */ import_react.default.createElement(Download, { size: 15, color: C.inkSoft })), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setConfirmDeleteId(e.id), "aria-label": "Eliminar empleado" }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 15, color: C.inkSoft })))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-3 text-[12px] space-y-1", style: { color: C.inkSoft } }, /* @__PURE__ */ import_react.default.createElement("div", null, "Alta: ", e.fechaAlta, e.fechaFinContrato && ` \xB7 Fin de contrato: ${e.fechaFinContrato}`), e.salarioBrutoMensual > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "mono" }, "Bruto: \u20AC", fmt(e.salarioBrutoMensual), "/mes (", e.pagas, " pagas)"), /* @__PURE__ */ import_react.default.createElement("div", null, "Vacaciones: ", usados, " / ", total, " d\xEDas usados este a\xF1o"), (e.documentos || []).length > 0 && /* @__PURE__ */ import_react.default.createElement("div", null, e.documentos.map((d) => d.nombre).filter(Boolean).join(" \xB7 "))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-3 flex gap-2 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => openEdit(e) }, "Editar"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setAusenciaFor(e.id) }, "Registrar ausencia"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setEpiFor(e.id) }, "Entregar EPI"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setDetalleId(detalleId === e.id ? null : e.id) }, detalleId === e.id ? "Ocultar historial" : "Ver historial")), detalleId === e.id && /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-3 pt-3", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] font-semibold uppercase tracking-wide mb-1", style: { color: C.inkSoft } }, "Ausencias"), (e.ausencias || []).length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin ausencias registradas." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1 mb-3" }, [...e.ausencias || []].reverse().map((a) => /* @__PURE__ */ import_react.default.createElement("div", { key: a.id, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, a.tipo, ": ", a.fechaInicio, " \u2192 ", a.fechaFin, " (", a.dias, " d)"), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => eliminarAusencia(e.id, a.id), "aria-label": "Eliminar ausencia" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 14, color: C.inkSoft }))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] font-semibold uppercase tracking-wide mb-1 pt-2", style: { color: C.inkSoft, borderTop: `1px solid ${C.line}` } }, "EPIs entregados"), (e.epis || []).length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin EPIs registrados." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1" }, [...e.epis || []].reverse().map((epi) => /* @__PURE__ */ import_react.default.createElement("div", { key: epi.id, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, epi.nombre, " \xB7 ", epi.fecha, " ", epi.firmado ? "\xB7 firmado" : "\xB7 sin firmar"), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => eliminarEpi(e.id, epi.id), "aria-label": "Eliminar EPI" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 14, color: C.inkSoft })))))));
  })), ausenciaFor && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setAusenciaFor(null), title: "Registrar ausencia" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tipo" }, /* @__PURE__ */ import_react.default.createElement("select", { value: ausenciaForm.tipo, onChange: (e) => setAusenciaForm({ ...ausenciaForm, tipo: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "Vacaciones" }, "Vacaciones"), /* @__PURE__ */ import_react.default.createElement("option", { value: "Baja m\xE9dica" }, "Baja m\xE9dica"), /* @__PURE__ */ import_react.default.createElement("option", { value: "Otro" }, "Otro permiso"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Desde" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: ausenciaForm.fechaInicio, onChange: (e) => setAusenciaForm({ ...ausenciaForm, fechaInicio: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hasta" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: ausenciaForm.fechaFin, onChange: (e) => setAusenciaForm({ ...ausenciaForm, fechaFin: e.target.value }) }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, diasAusencia(ausenciaForm.fechaInicio, ausenciaForm.fechaFin), " d\xEDa(s) naturales, contando ambos extremos."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submitAusencia }, "Guardar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setAusenciaFor(null) }, "Cancelar"))), epiFor && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setEpiFor(null), title: "Entregar EPI" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3", style: { color: C.inkSoft } }, "Deja constancia de qu\xE9 equipo de protecci\xF3n le has dado. Es parte de la obligaci\xF3n de PRL."), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Equipo entregado" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: epiForm.nombre, onChange: (e) => setEpiForm({ ...epiForm, nombre: e.target.value }), placeholder: "Guantes t\xE9rmicos, calzado antideslizante\u2026" })), /* @__PURE__ */ import_react.default.createElement("label", { className: "flex items-center gap-2 mb-3 text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("input", { type: "checkbox", checked: epiForm.firmado, onChange: (e) => setEpiForm({ ...epiForm, firmado: e.target.checked }) }), "El empleado ha firmado el recib\xED"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
    if (epiForm.nombre.trim()) {
      registrarEpi(epiFor, epiForm);
      setEpiFor(null);
      setEpiForm({ nombre: "", firmado: false });
    }
  } }, "Guardar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setEpiFor(null) }, "Cancelar"))), confirmDeleteId && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmDeleteId(null), title: "Eliminar empleado" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3" }, "Se borra la ficha completa, incluidas sus ausencias y documentos. Si solo ha causado baja, mejor ed\xEDtalo y desm\xE1rcalo como activo en vez de borrarlo, para conservar el historial."), nominas.some((n) => n.empleadoId === confirmDeleteId) && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, "Este empleado tiene n\xF3minas registradas. La legislaci\xF3n laboral obliga a conservar esos documentos varios a\xF1os. Mejor usa ", /* @__PURE__ */ import_react.default.createElement("b", null, '"Anonimizar"'), " \u2014 quita su nombre y datos personales, pero conserva fichajes y n\xF3minas con a qui\xE9n pertenecen, sin decir qui\xE9n era.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 flex-wrap" }, anonimizarEmpleado && /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
    anonimizarEmpleado(confirmDeleteId);
    setConfirmDeleteId(null);
  } }, "Anonimizar (recomendado)"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: () => {
    deleteEmpleado(confirmDeleteId);
    setConfirmDeleteId(null);
  } }, "Eliminar del todo"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmDeleteId(null) }, "Cancelar")))));
}
function inicioSemana(fecha) {
  const d = new Date(fecha);
  const dia = d.getDay();
  const diff = dia === 0 ? -6 : 1 - dia;
  const l = new Date(d);
  l.setDate(d.getDate() + diff);
  return aISO(l);
}
function RegistroHorario({ empleados, fichajes, fichar, addFichajeManual, updateFichaje, eliminarFichaje, fichajesAbiertos }) {
  const activos = empleados.filter((e) => e.activo !== false);
  const [desde, setDesde] = (0, import_react.useState)(inicioSemana(/* @__PURE__ */ new Date()));
  const [hasta, setHasta] = (0, import_react.useState)(todayISO());
  const [showManual, setShowManual] = (0, import_react.useState)(false);
  const [manual, setManual] = (0, import_react.useState)({ empleadoId: activos[0]?.id || "", fecha: todayISO(), tipo: "entrada", hora: "09:00" });
  const [manualError, setManualError] = (0, import_react.useState)("");
  const [showExportar, setShowExportar] = (0, import_react.useState)(false);
  const [copiado, setCopiado] = (0, import_react.useState)(false);
  function irEstaSemana() {
    setDesde(inicioSemana(/* @__PURE__ */ new Date()));
    setHasta(todayISO());
  }
  function irSemanaAnterior() {
    const d = /* @__PURE__ */ new Date();
    d.setDate(d.getDate() - 7);
    const ini = inicioSemana(d);
    const fin = new Date(ini);
    fin.setDate(fin.getDate() + 6);
    setDesde(ini);
    setHasta(aISO(fin));
  }
  const estadoHoy = activos.map((e) => {
    const deHoy = fichajes.filter((f) => f.empleadoId === e.id && f.fecha === todayISO()).sort((a, b) => (a.hora || "").localeCompare(b.hora || ""));
    const entradas = deHoy.filter((f) => f.tipo === "entrada");
    const salidas = deHoy.filter((f) => f.tipo === "salida");
    const abierto = entradas.length > salidas.length;
    return { empleado: e, ultimaEntrada: entradas[entradas.length - 1], abierto, totalFichajesHoy: deHoy.length };
  });
  const filasHistorial = (0, import_react.useMemo)(() => {
    const grupos = {};
    fichajes.filter((f) => f.fecha >= desde && f.fecha <= hasta).forEach((f) => {
      const key = `${f.empleadoId}-${f.fecha}`;
      if (!grupos[key]) grupos[key] = { empleadoId: f.empleadoId, fecha: f.fecha, entradas: [], salidas: [] };
      if (f.tipo === "entrada") grupos[key].entradas.push(f);
      else grupos[key].salidas.push(f);
    });
    return Object.values(grupos).map((g) => {
      const emp = empleados.find((e) => e.id === g.empleadoId);
      const entrada = g.entradas.sort((a, b) => a.hora.localeCompare(b.hora))[0];
      const salida = g.salidas.sort((a, b) => a.hora.localeCompare(b.hora)).slice(-1)[0];
      let horas = null;
      if (entrada && salida) {
        const [h1, m1] = entrada.hora.split(":").map(Number);
        const [h2, m2] = salida.hora.split(":").map(Number);
        horas = (h2 * 60 + m2 - (h1 * 60 + m1)) / 60;
      }
      return { ...g, nombre: emp ? emp.nombre : "\u2014", entrada, salida, horas };
    });
  }, [fichajes, desde, hasta, empleados]);
  function textoRegistro() {
    const lineas = filasHistorial.sort((a, b) => a.fecha.localeCompare(b.fecha) || a.nombre.localeCompare(b.nombre)).map((f) => `${f.fecha} \xB7 ${f.nombre} \xB7 Entrada ${f.entrada ? f.entrada.hora : "\u2014"} \xB7 Salida ${f.salida ? f.salida.hora : "sin fichar"} \xB7 ${f.horas !== null ? f.horas.toFixed(2) + " h" : ""}`);
    return `REGISTRO HORARIO
Del ${desde} al ${hasta}

${lineas.join("\n")}

Generado el ${(/* @__PURE__ */ new Date()).toLocaleString("es-ES")}`;
  }
  function copiarRegistro() {
    const texto = textoRegistro();
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(texto).then(() => setCopiado(true)).catch(() => setCopiado(false));
    }
  }
  function submitManual() {
    if (!manual.empleadoId) {
      setManualError("Selecciona un empleado.");
      return;
    }
    setManualError("");
    addFichajeManual({
      empleadoId: manual.empleadoId,
      fecha: manual.fecha,
      tipo: manual.tipo,
      hora: manual.hora,
      timestamp: (/* @__PURE__ */ new Date(`${manual.fecha}T${manual.hora}:00`)).toISOString(),
      manual: true
    });
    setShowManual(false);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Registro horario"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Desde 2019, todas las empresas en Espa\xF1a est\xE1n obligadas a llevar un registro diario de la jornada de cada persona trabajadora, sea cual sea su tama\xF1o. Este registro se debe conservar 4 a\xF1os y estar disponible para la Inspecci\xF3n de Trabajo si lo pide.")), fichajesAbiertos.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-1" }, "Fichajes sin cerrar"), fichajesAbiertos.map((f, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "text-[12px]" }, f.nombre, " \u2014 entr\xF3 el ", f.fecha, " y no consta salida"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Hoy"), activos.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "A\xF1ade empleados activos en Personal para poder ficharles." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-3 mb-4" }, estadoHoy.map(({ empleado, ultimaEntrada, abierto }) => /* @__PURE__ */ import_react.default.createElement(Card, { key: empleado.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-medium text-[13.5px]" }, empleado.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, abierto ? `Entr\xF3 a las ${ultimaEntrada.hora}` : ultimaEntrada ? "Jornada cerrada" : "Sin fichar hoy")), /* @__PURE__ */ import_react.default.createElement(Pill, { color: abierto ? C.accent : C.inkSoft }, abierto ? "Trabajando" : "Fuera")), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, disabled: abierto, onClick: () => fichar(empleado.id, "entrada") }, /* @__PURE__ */ import_react.default.createElement(LogIn, { size: 13 }), " Entrada"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", disabled: !abierto, onClick: () => fichar(empleado.id, "salida") }, /* @__PURE__ */ import_react.default.createElement(LogOut, { size: 13 }), " Salida"))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium", style: { color: C.inkSoft } }, "Historial"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setShowManual(true) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 13 }), " Correcci\xF3n manual"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setShowExportar(true) }, /* @__PURE__ */ import_react.default.createElement(Download, { size: 13 }), " Exportar"))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-2 mb-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: irEstaSemana }, "Esta semana"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: irSemanaAnterior }, "Semana anterior")), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Desde" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: desde, onChange: (e) => setDesde(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hasta" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: hasta, onChange: (e) => setHasta(e.target.value) })))), filasHistorial.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin fichajes en este periodo." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, filasHistorial.sort((a, b) => b.fecha.localeCompare(a.fecha) || a.nombre.localeCompare(b.nombre)).map((f, i) => /* @__PURE__ */ import_react.default.createElement(Card, { key: i }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-medium" }, f.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, f.fecha)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "mono" }, f.entrada ? f.entrada.hora : "\u2014", " \u2192 ", f.salida ? f.salida.hora : "sin salida"), f.horas !== null && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mono", style: { color: C.accent } }, f.horas.toFixed(2), " h")))))), showManual && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowManual(false), title: "Correcci\xF3n manual de fichaje" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Empleado" }, /* @__PURE__ */ import_react.default.createElement("select", { value: manual.empleadoId, onChange: (e) => setManual({ ...manual, empleadoId: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, activos.map((e) => /* @__PURE__ */ import_react.default.createElement("option", { key: e.id, value: e.id }, e.nombre)))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Fecha" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: manual.fecha, onChange: (e) => setManual({ ...manual, fecha: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tipo" }, /* @__PURE__ */ import_react.default.createElement("select", { value: manual.tipo, onChange: (e) => setManual({ ...manual, tipo: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "entrada" }, "Entrada"), /* @__PURE__ */ import_react.default.createElement("option", { value: "salida" }, "Salida"))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hora" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "time", value: manual.hora, onChange: (e) => setManual({ ...manual, hora: e.target.value }) }))), manualError && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, manualError), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submitManual }, "Guardar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowManual(false) }, "Cancelar"))), showExportar && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowExportar(false), title: "Exportar registro horario" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2" }, "Copia este texto y gu\xE1rdalo (o p\xE9galo en un documento). Es tu justificante del registro horario del periodo seleccionado, por si te lo pide la Inspecci\xF3n de Trabajo."), /* @__PURE__ */ import_react.default.createElement(
    "textarea",
    {
      readOnly: true,
      value: textoRegistro(),
      onFocus: (e) => e.target.select(),
      className: "w-full rounded-lg p-2 text-[11px] mono mb-3",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink, height: 180, resize: "none" }
    }
  ), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: copiarRegistro }, copiado ? "\xA1Copiado!" : "Copiar texto"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowExportar(false) }, "Cerrar"))));
}
function SelectorAlergenos({ seleccionados = [], onCambio, soloLectura = false, nombreParaSugerir = "" }) {
  function alternar(id) {
    if (soloLectura) return;
    onCambio(seleccionados.includes(id) ? seleccionados.filter((x) => x !== id) : [...seleccionados, id]);
  }
  const sugeridos = soloLectura ? [] : sugerirAlergenosDeTexto(nombreParaSugerir).filter((id) => !seleccionados.includes(id));
  return /* @__PURE__ */ import_react.default.createElement("div", null, sugeridos.length > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "rounded-lg p-2.5 mb-2.5", style: { background: C.amberSoft, border: `1px solid ${C.amber}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-1.5", style: { color: C.ink } }, "Por el nombre, podr\xEDa llevar (rev\xEDsalo, no se marca solo):"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-1.5" }, sugeridos.map((id) => {
    const a = alergenoPorId(id);
    if (!a) return null;
    return /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: id,
        type: "button",
        onClick: () => alternar(id),
        className: "text-[11.5px] font-medium px-2.5 py-1.5 rounded-lg flex items-center gap-1",
        style: { background: C.surface, border: `1px dashed ${C.amber}`, color: C.ink }
      },
      /* @__PURE__ */ import_react.default.createElement("span", null, a.icono),
      /* @__PURE__ */ import_react.default.createElement("span", null, a.nombre),
      /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.accent } }, "+")
    );
  }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-1.5" }, ALERGENOS.map((a) => {
    const activo = seleccionados.includes(a.id);
    return /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: a.id,
        type: "button",
        onClick: () => alternar(a.id),
        className: "text-[11.5px] font-medium px-2.5 py-1.5 rounded-lg flex items-center gap-1",
        style: {
          background: activo ? C.amberSoft : C.surface,
          border: `1px solid ${activo ? C.amber : C.line}`,
          color: activo ? C.ink : C.inkSoft,
          cursor: soloLectura ? "default" : "pointer"
        }
      },
      /* @__PURE__ */ import_react.default.createElement("span", null, a.icono),
      /* @__PURE__ */ import_react.default.createElement("span", null, a.nombre)
    );
  })), !soloLectura && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-2", style: { color: C.inkSoft } }, "Marca los que contenga este producto. Son los 14 de declaraci\xF3n obligatoria (Reglamento UE 1169/2011)."));
}
var TIPOS_PUNTO = [
  { id: "temperatura", label: "Temperatura (c\xE1mara, nevera, congelador)", unidad: "\xB0C" },
  { id: "limpieza", label: "Limpieza y desinfecci\xF3n", unidad: "" },
  { id: "recepcion", label: "Control en recepci\xF3n de mercanc\xEDa", unidad: "\xB0C" },
  { id: "otro", label: "Otro control", unidad: "" }
];
function Appcc({
  puntosControl,
  registrosAppcc,
  addPuntoControl,
  updatePuntoControl,
  deletePuntoControl,
  registrarAppcc,
  eliminarRegistroAppcc,
  appccPendientesHoy,
  productos,
  fichasCosto,
  alergenosDeFicha
}) {
  const [vista, setVista] = (0, import_react.useState)("hoy");
  const [showPunto, setShowPunto] = (0, import_react.useState)(false);
  const [puntoForm, setPuntoForm] = (0, import_react.useState)({ nombre: "", tipo: "temperatura", tempMin: "", tempMax: "", frecuencia: "Diaria" });
  const [puntoError, setPuntoError] = (0, import_react.useState)("");
  const [registroPara, setRegistroPara] = (0, import_react.useState)(null);
  const [registroForm, setRegistroForm] = (0, import_react.useState)({ valor: "", conforme: true, observaciones: "", responsable: "" });
  const [desde, setDesde] = (0, import_react.useState)(primerDiaMes(/* @__PURE__ */ new Date()));
  const [hasta, setHasta] = (0, import_react.useState)(todayISO());
  const [copiado, setCopiado] = (0, import_react.useState)(false);
  function submitPunto() {
    if (!puntoForm.nombre.trim()) {
      setPuntoError("Ponle nombre al punto de control.");
      return;
    }
    setPuntoError("");
    addPuntoControl({
      nombre: puntoForm.nombre.trim(),
      tipo: puntoForm.tipo,
      tempMin: puntoForm.tempMin === "" ? null : Number(puntoForm.tempMin),
      tempMax: puntoForm.tempMax === "" ? null : Number(puntoForm.tempMax),
      frecuencia: puntoForm.frecuencia
    });
    setPuntoForm({ nombre: "", tipo: "temperatura", tempMin: "", tempMax: "", frecuencia: "Diaria" });
    setShowPunto(false);
  }
  function abrirRegistro(punto) {
    setRegistroPara(punto);
    setRegistroForm({ valor: "", conforme: true, observaciones: "", responsable: "" });
  }
  function evaluarConformidad(punto, valor) {
    if (punto.tipo !== "temperatura" && punto.tipo !== "recepcion") return null;
    const v = Number(valor);
    if (valor === "" || isNaN(v)) return null;
    if (punto.tempMin != null && v < punto.tempMin) return false;
    if (punto.tempMax != null && v > punto.tempMax) return false;
    return true;
  }
  function submitRegistro() {
    const auto = evaluarConformidad(registroPara, registroForm.valor);
    registrarAppcc({
      puntoId: registroPara.id,
      puntoNombre: registroPara.nombre,
      tipo: registroPara.tipo,
      valor: registroForm.valor,
      conforme: auto === null ? registroForm.conforme : auto,
      observaciones: registroForm.observaciones,
      responsable: registroForm.responsable
    });
    setRegistroPara(null);
  }
  const registrosPeriodo = registrosAppcc.filter((r) => r.fecha >= desde && r.fecha <= hasta).sort((a, b) => (b.fecha + b.hora).localeCompare(a.fecha + a.hora));
  const noConformes = registrosPeriodo.filter((r) => r.conforme === false);
  const cartaAlergenos = (0, import_react.useMemo)(() => {
    const filas = [];
    (fichasCosto || []).forEach((f) => {
      filas.push({ id: "f-" + f.id, nombre: f.nombre, alergenos: alergenosDeFicha(f), origen: "ficha" });
    });
    (productos || []).filter((p) => p.tipo === "elaborado" && !(fichasCosto || []).some((f) => f.productoVinculadoId === p.id)).forEach((p) => {
      filas.push({ id: "p-" + p.id, nombre: p.nombre, alergenos: p.alergenos || [], origen: "producto" });
    });
    return filas.sort((a, b) => a.nombre.localeCompare(b.nombre));
  }, [fichasCosto, productos, alergenosDeFicha]);
  function textoRegistroAppcc() {
    const lineas = registrosPeriodo.map(
      (r) => `${r.fecha} ${r.hora} \xB7 ${r.puntoNombre} \xB7 ${r.valor || "\u2014"} \xB7 ${r.conforme ? "CONFORME" : "NO CONFORME"}${r.observaciones ? " \xB7 " + r.observaciones : ""}${r.responsable ? " \xB7 " + r.responsable : ""}`
    );
    return `REGISTRO APPCC
Del ${desde} al ${hasta}

${lineas.join("\n")}

No conformidades: ${noConformes.length}
Generado el ${(/* @__PURE__ */ new Date()).toLocaleString("es-ES")}`;
  }
  function copiar() {
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(textoRegistroAppcc()).then(() => setCopiado(true)).catch(() => setCopiado(false));
    }
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Control sanitario (APPCC)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-4 overflow-x-auto" }, [
    { id: "hoy", label: "Hoy" },
    { id: "historial", label: "Historial" },
    { id: "puntos", label: "Puntos de control" },
    { id: "alergenos", label: "Cartel de al\xE9rgenos" }
  ].map((v) => /* @__PURE__ */ import_react.default.createElement(Btn, { key: v.id, small: true, variant: vista === v.id ? "primary" : "ghost", onClick: () => setVista(v.id) }, v.label)))), vista === "hoy" && /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "El sistema APPCC es obligatorio en cualquier negocio que manipule alimentos. Estos registros deben conservarse y estar disponibles si Sanidad los pide. Registra cada control el d\xEDa que lo haces: rellenar una semana de golpe al final no vale.")), puntosControl.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has definido qu\xE9 controlas. Ve a \u201CPuntos de control\u201D para empezar." }) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, appccPendientesHoy.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold" }, "Te faltan ", appccPendientesHoy.length, " control(es) por registrar hoy")), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, puntosControl.filter((p) => p.activo !== false).map((p) => {
    const hechoHoy = registrosAppcc.find((r) => r.fecha === todayISO() && r.puntoId === p.id);
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: p.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-medium text-[13.5px]" }, p.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, TIPOS_PUNTO.find((t) => t.id === p.tipo)?.label, p.tempMin != null && p.tempMax != null && ` \xB7 rango ${p.tempMin} a ${p.tempMax} \xB0C`)), hechoHoy ? /* @__PURE__ */ import_react.default.createElement(Pill, { color: hechoHoy.conforme ? C.accent : C.red }, hechoHoy.conforme ? `OK ${hechoHoy.valor}` : `Desviaci\xF3n ${hechoHoy.valor}`) : /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.amber }, "pendiente")), !hechoHoy && /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => abrirRegistro(p) }, "Registrar ahora")));
  })))), vista === "puntos" && /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex justify-end mb-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => setShowPunto(true) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nuevo punto de control")), puntosControl.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Define qu\xE9 vas a controlar: c\xE1mara de refrigerado, congelador, limpieza del obrador\u2026" }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, puntosControl.map((p) => /* @__PURE__ */ import_react.default.createElement(Card, { key: p.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-medium" }, p.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, TIPOS_PUNTO.find((t) => t.id === p.tipo)?.label, " \xB7 ", p.frecuencia, p.tempMin != null && p.tempMax != null && ` \xB7 ${p.tempMin} a ${p.tempMax} \xB0C`)), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => updatePuntoControl(p.id, { activo: p.activo === false }) }, p.activo === false ? "Activar" : "Desactivar"), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => deletePuntoControl(p.id), "aria-label": "Eliminar punto de control" }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 15, color: C.inkSoft })))))))), vista === "historial" && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Desde" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: desde, onChange: (e) => setDesde(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hasta" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: hasta, onChange: (e) => setHasta(e.target.value) }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => window.print() }, "Imprimir / Guardar PDF"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: copiar }, copiado ? "\xA1Copiado!" : "Copiar texto"))), noConformes.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.redSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold" }, noConformes.length, " desviaci\xF3n(es) en el periodo. Revisa que quede anotada la medida correctora."))), /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end justify-between mb-3 pb-3", style: { borderBottom: `2px solid ${C.accent}` } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[17px] font-semibold" }, "Registro de control sanitario (APPCC)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, "Del ", desde, " al ", hasta)), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 52, width: "auto" } })), registrosPeriodo.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin registros en este periodo." }) : /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[11.5px]", style: { borderCollapse: "collapse" } }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Fecha"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Hora"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Punto de control"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { background: C.chrome, color: "#fff" } }, "Valor"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-center", style: { background: C.chrome, color: "#fff" } }, "Conforme"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Observaciones"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Responsable"))), /* @__PURE__ */ import_react.default.createElement("tbody", null, registrosPeriodo.map((r, i) => /* @__PURE__ */ import_react.default.createElement("tr", { key: r.id, style: { background: i % 2 ? C.bg : "#fff" } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2 mono", style: { border: `1px solid ${C.line}` } }, r.fecha), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2 mono", style: { border: `1px solid ${C.line}` } }, r.hora), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2", style: { border: `1px solid ${C.line}` } }, r.puntoNombre), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2 text-center mono", style: { border: `1px solid ${C.line}` } }, r.valor || "\u2014"), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2 text-center font-semibold", style: { border: `1px solid ${C.line}`, color: r.conforme ? C.accent : C.red } }, r.conforme ? "S\xED" : "NO"), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2", style: { border: `1px solid ${C.line}` } }, r.observaciones || ""), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-1.5 px-2", style: { border: `1px solid ${C.line}` } }, r.responsable || ""))))))), vista === "alergenos" && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir mb-3" }, /* @__PURE__ */ import_react.default.createElement(Card, { style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Este cartel se genera solo desde tus fichas de costo: hereda los al\xE9rgenos de cada ingrediente enlazado al cat\xE1logo. Impr\xEDmelo y t\xE9nlo visible o a disposici\xF3n del cliente, como exige la ley.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mt-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => window.print() }, "Imprimir cartel"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end justify-between mb-3 pb-3", style: { borderBottom: `2px solid ${C.accent}` } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[17px] font-semibold" }, "Informaci\xF3n de al\xE9rgenos"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, "Reglamento (UE) 1169/2011 \xB7 Actualizado el ", (/* @__PURE__ */ new Date()).toLocaleDateString("es-ES"))), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 52, width: "auto" } })), cartaAlergenos.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Crea fichas de costo o marca productos como elaborados para generar el cartel." }) : /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[11.5px]", style: { borderCollapse: "collapse" } }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", null, /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Producto"), /* @__PURE__ */ import_react.default.createElement("th", { className: "py-2 px-2 text-left", style: { background: C.chrome, color: "#fff" } }, "Contiene"))), /* @__PURE__ */ import_react.default.createElement("tbody", null, cartaAlergenos.map((f, i) => /* @__PURE__ */ import_react.default.createElement("tr", { key: f.id, style: { background: i % 2 ? C.bg : "#fff" } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2 font-medium", style: { border: `1px solid ${C.line}` } }, f.nombre), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-2", style: { border: `1px solid ${C.line}` } }, f.alergenos.length === 0 ? /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, "Sin al\xE9rgenos declarados") : f.alergenos.map((id) => {
    const a = alergenoPorId(id);
    return a ? `${a.icono} ${a.nombre}` : id;
  }).join(" \xB7 ")))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-4", style: { color: C.inkSoft } }, "Puede haber trazas de otros al\xE9rgenos por manipulaci\xF3n en el mismo obrador. Consulte al personal si tiene alguna alergia o intolerancia."))), showPunto && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowPunto(false), title: "Nuevo punto de control" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: puntoForm.nombre, onChange: (e) => setPuntoForm({ ...puntoForm, nombre: e.target.value }), placeholder: "C\xE1mara de refrigerado, Congelador obrador\u2026" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tipo" }, /* @__PURE__ */ import_react.default.createElement("select", { value: puntoForm.tipo, onChange: (e) => setPuntoForm({ ...puntoForm, tipo: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, TIPOS_PUNTO.map((t) => /* @__PURE__ */ import_react.default.createElement("option", { key: t.id, value: t.id }, t.label)))), (puntoForm.tipo === "temperatura" || puntoForm.tipo === "recepcion") && /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Temperatura m\xEDnima (\xB0C)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.1", value: puntoForm.tempMin, onChange: (e) => setPuntoForm({ ...puntoForm, tempMin: e.target.value }), placeholder: "0" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Temperatura m\xE1xima (\xB0C)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.1", value: puntoForm.tempMax, onChange: (e) => setPuntoForm({ ...puntoForm, tempMax: e.target.value }), placeholder: "4" }))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Frecuencia" }, /* @__PURE__ */ import_react.default.createElement("select", { value: puntoForm.frecuencia, onChange: (e) => setPuntoForm({ ...puntoForm, frecuencia: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("option", null, "Diaria"), /* @__PURE__ */ import_react.default.createElement("option", null, "Dos veces al d\xEDa"), /* @__PURE__ */ import_react.default.createElement("option", null, "Semanal"), /* @__PURE__ */ import_react.default.createElement("option", null, "En cada recepci\xF3n"))), puntoError && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, puntoError), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submitPunto }, "Guardar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowPunto(false) }, "Cancelar"))), registroPara && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setRegistroPara(null), title: registroPara.nombre }, registroPara.tipo === "temperatura" || registroPara.tipo === "recepcion" ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Temperatura medida (\xB0C)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.1", value: registroForm.valor, onChange: (e) => setRegistroForm({ ...registroForm, valor: e.target.value }), autoFocus: true })), (() => {
    const ev = evaluarConformidad(registroPara, registroForm.valor);
    if (ev === null) return null;
    return /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: ev ? C.accent : C.red } }, ev ? "Dentro del rango correcto." : `Fuera del rango (${registroPara.tempMin} a ${registroPara.tempMax} \xB0C). Se registrar\xE1 como desviaci\xF3n: anota abajo qu\xE9 medida correctora has tomado.`);
  })()) : /* @__PURE__ */ import_react.default.createElement(Field, { label: "Resultado" }, /* @__PURE__ */ import_react.default.createElement("select", { value: registroForm.conforme ? "si" : "no", onChange: (e) => setRegistroForm({ ...registroForm, conforme: e.target.value === "si" }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "si" }, "Conforme \u2014 realizado correctamente"), /* @__PURE__ */ import_react.default.createElement("option", { value: "no" }, "No conforme \u2014 hubo un problema"))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Observaciones / medida correctora" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: registroForm.observaciones, onChange: (e) => setRegistroForm({ ...registroForm, observaciones: e.target.value }), placeholder: "Opcional, salvo si hay desviaci\xF3n" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Responsable" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: registroForm.responsable, onChange: (e) => setRegistroForm({ ...registroForm, responsable: e.target.value }), placeholder: "Qui\xE9n lo comprueba" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submitRegistro }, "Guardar registro"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setRegistroPara(null) }, "Cancelar"))));
}
function lineaEncargo() {
  return { productoId: "", descripcion: "", cantidad: 1, precioUnitario: "" };
}
function Encargos({ encargosPendientes, encargos, clientes, productos, addEncargo, updateEncargo, deleteEncargo, entregarEncargo, addCliente }) {
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [editingId, setEditingId] = (0, import_react.useState)(null);
  const [form, setForm] = (0, import_react.useState)(null);
  const [error, setError] = (0, import_react.useState)("");
  const [confirmDeleteId, setConfirmDeleteId] = (0, import_react.useState)(null);
  const [entregarId, setEntregarId] = (0, import_react.useState)(null);
  const [medioPagoEntrega, setMedioPagoEntrega] = (0, import_react.useState)("Efectivo");
  const [verEntregados, setVerEntregados] = (0, import_react.useState)(false);
  const [clienteNuevo, setClienteNuevo] = (0, import_react.useState)("");
  const vendibles = productos.filter((p) => p.tipo === "elaborado" || Number(p.precioVenta) > 0);
  function nuevo() {
    setForm({
      clienteId: clientes[0]?.id || "",
      numero: "",
      fechaEntrega: todayISO(),
      horaEntrega: "",
      notas: "",
      se\u00F1al: "",
      se\u00F1alMedioPago: "Efectivo",
      lineas: [lineaEncargo()]
    });
    setEditingId(null);
    setError("");
    setShowForm(true);
  }
  function abrirEdicion(e) {
    setForm({
      clienteId: e.clienteId || "",
      numero: e.numero || "",
      fechaEntrega: e.fechaEntrega || todayISO(),
      horaEntrega: e.horaEntrega || "",
      notas: e.notas || "",
      se\u00F1al: e.se\u00F1al ?? "",
      se\u00F1alMedioPago: e.se\u00F1alMedioPago || "Efectivo",
      lineas: (e.lineas || []).map((l) => ({ ...l }))
    });
    setEditingId(e.id);
    setError("");
    setShowForm(true);
  }
  function setLinea(idx, campo, valor) {
    setForm((f) => ({
      ...f,
      lineas: f.lineas.map((l, i) => {
        if (i !== idx) return l;
        if (campo === "productoId") {
          const p = productos.find((x) => x.id === valor);
          return { ...l, productoId: valor, descripcion: p ? p.nombre : l.descripcion, precioUnitario: p ? precioNeto(p).toFixed(2) : l.precioUnitario };
        }
        return { ...l, [campo]: valor };
      })
    }));
  }
  const addLinea = () => setForm((f) => ({ ...f, lineas: [...f.lineas, lineaEncargo()] }));
  const quitarLinea = (idx) => setForm((f) => ({ ...f, lineas: f.lineas.filter((_, i) => i !== idx) }));
  const totalForm = !form ? 0 : form.lineas.reduce((a, l) => a + (Number(l.cantidad) || 0) * (Number(l.precioUnitario) || 0), 0);
  function submit() {
    if (!form.clienteId) {
      setError("Selecciona o crea un cliente.");
      return;
    }
    const validas = form.lineas.filter((l) => (l.productoId || l.descripcion) && Number(l.cantidad) > 0);
    if (!validas.length) {
      setError("A\xF1ade al menos un producto con cantidad.");
      return;
    }
    setError("");
    const datos = { ...form, lineas: validas, se\u00F1al: form.se\u00F1al === "" ? 0 : Number(form.se\u00F1al) };
    if (editingId) updateEncargo(editingId, datos);
    else addEncargo(datos);
    setShowForm(false);
    setForm(null);
    setEditingId(null);
  }
  function crearClienteRapido() {
    if (!clienteNuevo.trim()) return;
    const c = addCliente({ nombre: clienteNuevo.trim() });
    setForm((f) => ({ ...f, clienteId: c.id }));
    setClienteNuevo("");
  }
  const entregados = encargos.filter((e) => e.estado === "Entregado");
  function colorDias(d) {
    if (d === null) return C.inkSoft;
    if (d < 0) return C.red;
    if (d <= 2) return C.amber;
    return C.ink;
  }
  function textoDias(d) {
    if (d === null) return "Sin fecha";
    if (d < 0) return `Se pas\xF3 hace ${Math.abs(d)} d`;
    if (d === 0) return "\xA1Hoy!";
    if (d === 1) return "Ma\xF1ana";
    return `En ${d} d\xEDas`;
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: nuevo }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nuevo encargo") }, "Encargos"), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, editingId ? "Editando encargo" : "Nuevo encargo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Cliente" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.clienteId, onChange: (e) => setForm({ ...form, clienteId: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Selecciona\u2026"), clientes.map((c) => /* @__PURE__ */ import_react.default.createElement("option", { key: c.id, value: c.id }, c.nombre)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Referencia del encargo (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.numero, onChange: (e) => setForm({ ...form, numero: e.target.value }), placeholder: "E-001" }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 items-end mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex-1" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-1", style: { color: C.inkSoft } }, "\xBFCliente nuevo? Cr\xE9alo aqu\xED mismo"), /* @__PURE__ */ import_react.default.createElement(Input, { value: clienteNuevo, onChange: (e) => setClienteNuevo(e.target.value), placeholder: "Nombre del cliente" })), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: crearClienteRapido }, "Crear")), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Fecha de entrega" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: form.fechaEntrega, onChange: (e) => setForm({ ...form, fechaEntrega: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hora (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "time", value: form.horaEntrega, onChange: (e) => setForm({ ...form, horaEntrega: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Se\xF1al / anticipo (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.se\u00F1al, onChange: (e) => setForm({ ...form, se\u00F1al: e.target.value }) }))), Number(form.se\u00F1al) > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "C\xF3mo se ha cobrado la se\xF1al" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: form.se\u00F1alMedioPago || "Efectivo",
      onChange: (e) => setForm({ ...form, se\u00F1alMedioPago: e.target.value }),
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    },
    /* @__PURE__ */ import_react.default.createElement("option", null, "Efectivo"),
    /* @__PURE__ */ import_react.default.createElement("option", null, "Tarjeta"),
    /* @__PURE__ */ import_react.default.createElement("option", null, "Transferencia"),
    /* @__PURE__ */ import_react.default.createElement("option", null, "Otro")
  ))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Productos del encargo"), form.lineas.map((l, idx) => /* @__PURE__ */ import_react.default.createElement(Card, { key: idx, className: "mb-2", style: { background: C.bg } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12px] font-semibold" }, "L\xEDnea ", idx + 1), /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: () => quitarLinea(idx), "aria-label": "Quitar l\xEDnea" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 16, color: C.inkSoft }))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Producto del cat\xE1logo" }, /* @__PURE__ */ import_react.default.createElement("select", { value: l.productoId, onChange: (e) => setLinea(idx, "productoId", e.target.value), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "\u2014 Escribirlo a mano \u2014"), vendibles.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre)))), !l.productoId && /* @__PURE__ */ import_react.default.createElement(Field, { label: "Descripci\xF3n" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: l.descripcion, onChange: (e) => setLinea(idx, "descripcion", e.target.value), placeholder: "Tarta personalizada 6 personas" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-x-2" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Cantidad" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: l.cantidad, onChange: (e) => setLinea(idx, "cantidad", e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Precio ud. (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: l.precioUnitario, onChange: (e) => setLinea(idx, "precioUnitario", e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Subtotal" }, /* @__PURE__ */ import_react.default.createElement(Input, { readOnly: true, value: fmt((Number(l.cantidad) || 0) * (Number(l.precioUnitario) || 0)), style: { background: C.bg } }))))), /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: addLinea, className: "text-[12.5px] font-medium mb-3", style: { color: C.accent } }, "+ A\xF1adir producto"), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Notas para el obrador" }, /* @__PURE__ */ import_react.default.createElement("textarea", { value: form.notas, onChange: (e) => setForm({ ...form, notas: e.target.value }), className: "w-full rounded-lg p-2 text-[12.5px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink, height: 70, resize: "vertical" }, placeholder: "Sin gluten, felicitaci\xF3n 'Feliz cumplea\xF1os Ana'\u2026" })), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px] font-medium" }, "Total del encargo"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-bold text-[17px]" }, "\u20AC", fmt(totalForm))), Number(form.se\u00F1al) > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12px] mt-1", style: { color: C.inkSoft } }, /* @__PURE__ */ import_react.default.createElement("span", null, "Pendiente de cobrar al entregar"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, "\u20AC", fmt(totalForm - Number(form.se\u00F1al))))), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, editingId ? "Guardar cambios" : "Guardar encargo"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowForm(false);
    setForm(null);
    setEditingId(null);
  } }, "Cancelar"))), encargosPendientes.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No hay encargos pendientes." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, encargosPendientes.map((e) => /* @__PURE__ */ import_react.default.createElement(Card, { key: e.id, style: { borderLeft: `3px solid ${colorDias(e.dias)}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold" }, e.cliente ? e.cliente.nombre : "Sin cliente"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, e.fechaEntrega, e.horaEntrega && ` \xB7 ${e.horaEntrega}`, e.numero && ` \xB7 ${e.numero}`)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold", style: { color: colorDias(e.dias) } }, textoDias(e.dias)), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold mt-0.5" }, "\u20AC", fmt(e.total)))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 text-[12px] space-y-0.5" }, (e.lineas || []).map((l, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i }, "\xB7 ", l.cantidad, " \xD7 ", l.descripcion || "\u2014"))), e.notas && /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 text-[11.5px] p-2 rounded-lg", style: { background: C.amberSoft } }, e.notas), Number(e.se\u00F1al) > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mt-2", style: { color: C.inkSoft } }, "Se\xF1al cobrada \u20AC", fmt(e.se\u00F1al), " \xB7 pendiente \u20AC", fmt(e.total - Number(e.se\u00F1al))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 flex gap-2 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => {
    setEntregarId(e.id);
    setMedioPagoEntrega("Efectivo");
  } }, /* @__PURE__ */ import_react.default.createElement(CircleCheck, { size: 13 }), " Entregar"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => abrirEdicion(e) }, "Editar"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "danger", onClick: () => setConfirmDeleteId(e.id) }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 13 }), " Eliminar"))))), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setVerEntregados((s) => !s), className: "text-[12.5px] font-medium mt-4 mb-2", style: { color: C.accent } }, verEntregados ? "Ocultar" : "Ver", " encargos entregados (", entregados.length, ")"), verEntregados && /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, entregados.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has entregado ninguno." }) : entregados.map((e) => {
    const cliente = clientes.find((c) => c.id === e.clienteId);
    const total = (e.lineas || []).reduce((a, l) => a + (Number(l.cantidad) || 0) * (Number(l.precioUnitario) || 0), 0);
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: e.id, style: { background: C.bg } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, cliente ? cliente.nombre : "\u2014", " \xB7 ", e.fechaEntregaReal || e.fechaEntrega), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, "\u20AC", fmt(total))));
  })), entregarId && (() => {
    const e = encargos.find((x) => x.id === entregarId);
    if (!e) return null;
    const total = (e.lineas || []).reduce((a, l) => a + (Number(l.cantidad) || 0) * (Number(l.precioUnitario) || 0), 0);
    const resto = Math.max(0, total - (Number(e.se\u00F1al) || 0));
    return /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setEntregarId(null), title: "Entregar encargo" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-4" }, "Al entregar, se descuenta el stock de los productos del cat\xE1logo y se registra la venta, as\xED que entrar\xE1 en tus Resultados. Los productos escritos a mano no descuentan stock."), resto > 9e-3 && /* @__PURE__ */ import_react.default.createElement(Field, { label: `C\xF3mo se cobra el resto (\u20AC${fmt(resto)})` }, /* @__PURE__ */ import_react.default.createElement(
      "select",
      {
        value: medioPagoEntrega,
        onChange: (ev) => setMedioPagoEntrega(ev.target.value),
        className: "w-full rounded-lg px-3 py-2 text-[13px]",
        style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
      },
      /* @__PURE__ */ import_react.default.createElement("option", null, "Efectivo"),
      /* @__PURE__ */ import_react.default.createElement("option", null, "Tarjeta"),
      /* @__PURE__ */ import_react.default.createElement("option", null, "Transferencia"),
      /* @__PURE__ */ import_react.default.createElement("option", null, "Otro")
    )), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
      entregarEncargo(e, medioPagoEntrega);
      setEntregarId(null);
    } }, "Confirmar entrega"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setEntregarId(null) }, "Cancelar")));
  })(), confirmDeleteId && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmDeleteId(null), title: "Eliminar encargo" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-4" }, "Se borra el encargo. Esta acci\xF3n no se puede deshacer."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: () => {
    deleteEncargo(confirmDeleteId);
    setConfirmDeleteId(null);
  } }, "S\xED, eliminar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmDeleteId(null) }, "Cancelar"))));
}
function Clientes({ analisisClientes, clientesDormidos, ventaCruzada, addCliente, updateCliente, deleteCliente }) {
  const blank = { nombre: "", telefono: "", email: "", notas: "" };
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [form, setForm] = (0, import_react.useState)(blank);
  const [editingId, setEditingId] = (0, import_react.useState)(null);
  const [error, setError] = (0, import_react.useState)("");
  const [confirmDeleteId, setConfirmDeleteId] = (0, import_react.useState)(null);
  function submit() {
    if (!form.nombre.trim()) {
      setError("Escribe el nombre del cliente.");
      return;
    }
    setError("");
    if (editingId) updateCliente(editingId, form);
    else addCliente(form);
    setForm(blank);
    setEditingId(null);
    setShowForm(false);
  }
  function abrirEdicion(c) {
    setForm({ nombre: c.nombre || "", telefono: c.telefono || "", email: c.email || "", notas: c.notas || "" });
    setEditingId(c.id);
    setError("");
    setShowForm(true);
  }
  function exportarDatosCliente(c) {
    const lineas = [
      `DATOS GUARDADOS SOBRE: ${c.nombre}`,
      `Generado el ${todayISO()}`,
      "",
      "=== FICHA ===",
      `Nombre: ${c.nombre}`,
      `Tel\xE9fono: ${c.telefono || "\u2014"}`,
      `Email: ${c.email || "\u2014"}`,
      `Notas: ${c.notas || "\u2014"}`,
      "",
      "=== ACTIVIDAD ===",
      `Total facturado: \u20AC${fmt(c.facturado || 0)}`,
      `Encargos entregados: ${c.nEncargos || 0}`,
      `\xDAltima compra: ${c.ultima || "sin compras registradas"}`
    ];
    const blob = new Blob([lineas.join("\n")], { type: "text/plain;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = `datos-${c.nombre.replace(/\s+/g, "-").toLowerCase()}.txt`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
    setForm(blank);
    setEditingId(null);
    setShowForm(true);
  } }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Nuevo cliente") }, "Clientes"), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-3 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.nombre, onChange: (e) => setForm({ ...form, nombre: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tel\xE9fono" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.telefono, onChange: (e) => setForm({ ...form, telefono: e.target.value }), placeholder: "+34 600 000 000" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Correo (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "email", value: form.email, onChange: (e) => setForm({ ...form, email: e.target.value }) }))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Notas (gustos, alergias, empresa\u2026)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.notas, onChange: (e) => setForm({ ...form, notas: e.target.value }) })), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, editingId ? "Guardar cambios" : "Guardar cliente"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowForm(false);
    setForm(blank);
    setEditingId(null);
  } }, "Cancelar"))), clientesDormidos.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-2" }, "Clientes que hace tiempo que no vuelven"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1" }, clientesDormidos.slice(0, 6).map((c) => /* @__PURE__ */ import_react.default.createElement("div", { key: c.id, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, c.nombre, c.telefono && ` \xB7 ${c.telefono}`), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, c.diasSinComprar, " d\xEDas")))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-2", style: { color: C.inkSoft } }, "Te compraron antes y llevan m\xE1s de 2 meses sin encargar nada. Una llamada o un WhatsApp puede recuperarlos.")), analisisClientes.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no tienes clientes registrados." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2 mb-4" }, analisisClientes.map((c) => /* @__PURE__ */ import_react.default.createElement(Card, { key: c.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold" }, c.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, [c.telefono, c.email].filter(Boolean).join(" \xB7 ")), c.notas && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mt-1", style: { color: C.inkSoft } }, c.notas)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, "\u20AC", fmt(c.facturado)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, c.nEncargos, " encargo(s)"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11.5px]", style: { color: C.inkSoft } }, c.ultima ? `\xDAltima compra: ${c.ultima}` : "Sin compras registradas"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, c.telefono && /* @__PURE__ */ import_react.default.createElement(LinkBtn, { small: true, href: `https://wa.me/${c.telefono.replace(/[^0-9]/g, "")}` }, /* @__PURE__ */ import_react.default.createElement(MessageCircle, { size: 13 }), " WhatsApp"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => exportarDatosCliente(c) }, /* @__PURE__ */ import_react.default.createElement(Download, { size: 13 })), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => abrirEdicion(c) }, "Editar"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "danger", onClick: () => setConfirmDeleteId(c.id) }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 13 }))))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Productos que se piden juntos"), ventaCruzada.base < 3 ? /* @__PURE__ */ import_react.default.createElement(Card, { style: { background: C.bg } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]", style: { color: C.inkSoft } }, "Con ", ventaCruzada.base, " encargo(s) entregado(s) todav\xEDa no se puede sacar ninguna conclusi\xF3n fiable. A partir de 3 empezar\xE1s a ver qu\xE9 se pide junto; con 20 o 30 el dato ya sirve para decidir qu\xE9 poner cerca en el mostrador.")) : ventaCruzada.filas.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no hay dos productos distintos en un mismo encargo." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, ventaCruzada.filas.map((f, i) => /* @__PURE__ */ import_react.default.createElement(Card, { key: i }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, f.a.nombre, " ", /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, "+"), " ", f.b.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold" }, f.veces, "\xD7")))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: C.inkSoft } }, "Basado en ", ventaCruzada.base, " encargos entregados. Cuantos m\xE1s acumules, m\xE1s fiable ser\xE1.")), confirmDeleteId && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmDeleteId(null), title: "Eliminar cliente" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-4" }, "Se borra la ficha del cliente. Los encargos que ya le hiciste se conservan, pero quedar\xE1n sin nombre asociado."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: () => {
    deleteCliente(confirmDeleteId);
    setConfirmDeleteId(null);
  } }, "S\xED, eliminar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmDeleteId(null) }, "Cancelar"))));
}
function rangoTrimestre(anio, trimestre) {
  const mesIni = (trimestre - 1) * 3;
  const desde = aISO(new Date(anio, mesIni, 1));
  const hasta = aISO(new Date(anio, mesIni + 3, 0));
  return { desde, hasta };
}
function Facturas({ albaranes, facturasDirectas, proveedorPorId, irAAlbaran, irAFacturaDirecta }) {
  const [mes, setMes] = (0, import_react.useState)(aISO(/* @__PURE__ */ new Date()).slice(0, 7));
  const todas = (0, import_react.useMemo)(() => {
    const deMercancia = albaranes.filter((a) => a.estado === "confirmado" && a.esFactura !== false).map((a) => {
      const baseConCanon = a.lineas.reduce((x, ln) => x + (Number(ln.importe) || 0) + (Number(ln.canon) || 0), 0);
      const ivaLineas = a.lineas.reduce((x, ln) => x + ((Number(ln.importe) || 0) + (Number(ln.canon) || 0)) * ((Number(ln.ivaPct) || 0) / 100), 0);
      const cargos = Number(a.cargos) || 0;
      const ivaCargos = cargos * ((Number(a.cargosIva) || 0) / 100);
      const base = baseConCanon + cargos;
      const iva = ivaLineas + ivaCargos;
      return {
        id: "alb-" + a.id,
        idReal: a.id,
        tipoOrigen: "albaran",
        origen: "Mercanc\xEDa",
        fecha: a.fechaFactura || a.fecha,
        proveedorNombre: proveedorPorId(a.proveedorId)?.nombre || "Sin proveedor",
        numero: a.numeroFactura || a.numero || "s/n",
        categoria: null,
        base,
        iva,
        total: base + iva,
        pagada: !!a.pagada
      };
    });
    const directas = (facturasDirectas || []).map((f) => ({
      id: "fd-" + f.id,
      idReal: f.id,
      tipoOrigen: "directa",
      origen: "Directa",
      fecha: f.fecha,
      proveedorNombre: f.proveedorId ? proveedorPorId(f.proveedorId)?.nombre || "Sin proveedor" : f.concepto || "Sin proveedor",
      numero: f.numeroFactura || f.concepto || "s/n",
      categoria: f.categoria || null,
      base: Number(f.importeBase) || 0,
      iva: Number(f.importeIva) || 0,
      total: Number(f.importeTotal) || 0,
      pagada: !!f.pagada
    }));
    return [...deMercancia, ...directas];
  }, [albaranes, facturasDirectas, proveedorPorId]);
  const delMes = (0, import_react.useMemo)(
    () => todas.filter((f) => (f.fecha || "").startsWith(mes)).sort((a, b) => (b.fecha || "").localeCompare(a.fecha || "")),
    [todas, mes]
  );
  const totalMes = delMes.reduce(
    (acc, f) => ({ base: acc.base + f.base, iva: acc.iva + f.iva, total: acc.total + f.total }),
    { base: 0, iva: 0, total: 0 }
  );
  const porProveedor = (0, import_react.useMemo)(() => {
    const mapa = {};
    delMes.forEach((f) => {
      const clave = f.proveedorNombre;
      if (!mapa[clave]) mapa[clave] = { proveedor: clave, facturas: [], total: 0 };
      mapa[clave].facturas.push(f);
      mapa[clave].total += f.total;
    });
    return Object.values(mapa).sort((a, b) => b.total - a.total);
  }, [delMes]);
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Facturas"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Mes" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "month", value: mes, onChange: (e) => setMes(e.target.value) }))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.chrome, color: "#fff" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] tracking-wide uppercase", style: { color: "#9CB6A9" } }, "Total del mes"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-2 mt-2" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: "#9CB6A9" } }, "Base"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono text-[16px] font-semibold" }, "\u20AC", fmt(totalMes.base))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: "#9CB6A9" } }, "IVA"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono text-[16px] font-semibold" }, "\u20AC", fmt(totalMes.iva))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: "#9CB6A9" } }, "Total"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono text-[18px] font-bold" }, "\u20AC", fmt(totalMes.total)))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-2", style: { color: "#9CB6A9" } }, delMes.length, " factura(s) este mes")), delMes.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No hay facturas en este mes." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-3" }, porProveedor.map((grupo) => /* @__PURE__ */ import_react.default.createElement(Card, { key: grupo.proveedor }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold text-[13.5px]" }, grupo.proveedor), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono text-[13px] font-semibold" }, "\u20AC", fmt(grupo.total))), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, grupo.facturas.map((f) => {
    const irAOrigen = f.tipoOrigen === "albaran" ? irAAlbaran : irAFacturaDirecta;
    return /* @__PURE__ */ import_react.default.createElement(
      "div",
      {
        key: f.id,
        onClick: irAOrigen ? () => irAOrigen(f.idReal) : void 0,
        className: "flex items-center justify-between text-[12px] py-1",
        style: { borderTop: `1px solid ${C.line}`, cursor: irAOrigen ? "pointer" : "default" }
      },
      /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", null, f.fecha, " \xB7 N\xBA ", f.numero, /* @__PURE__ */ import_react.default.createElement("span", { className: "ml-1.5", style: { color: C.inkSoft } }, "(", f.origen, f.categoria ? ` \xB7 ${f.categoria}` : "", ")")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "base \u20AC", fmt(f.base), " \xB7 IVA \u20AC", fmt(f.iva))),
      /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, "\u20AC", fmt(f.total)), /* @__PURE__ */ import_react.default.createElement(Pill, { color: f.pagada ? C.inkSoft : C.amber }, f.pagada ? "Pagada" : "Sin pagar")), irAOrigen && /* @__PURE__ */ import_react.default.createElement(ChevronRight, { size: 15, color: C.inkSoft }))
    );
  }))))));
}
function LibroIva({ movimientos, productos, albaranes, proveedorPorId, facturasDirectas = [] }) {
  const hoy = /* @__PURE__ */ new Date();
  const [anio, setAnio] = (0, import_react.useState)(hoy.getFullYear());
  const [trimestre, setTrimestre] = (0, import_react.useState)(Math.floor(hoy.getMonth() / 3) + 1);
  const { desde, hasta } = rangoTrimestre(anio, trimestre);
  const repercutido = (0, import_react.useMemo)(() => {
    const porTipo = {};
    movimientos.filter((m) => esVenta(m) && m.fecha >= desde && m.fecha <= hasta).forEach((m) => {
      const base = Math.abs(Number(m.cantidad) || 0) * (Number(m.ingresoUnitario) || 0);
      const tipo = m.ivaVentaAplicado != null ? m.ivaVentaAplicado : productos.find((p) => p.id === m.productoId)?.ivaVenta ?? 10;
      if (!porTipo[tipo]) porTipo[tipo] = { base: 0, cuota: 0 };
      porTipo[tipo].base += base;
      porTipo[tipo].cuota += base * (tipo / 100);
    });
    return porTipo;
  }, [movimientos, productos, desde, hasta]);
  const soportado = (0, import_react.useMemo)(() => {
    const porTipo = {};
    albaranes.filter((a) => a.estado === "confirmado" && (a.fechaFactura || a.fecha) >= desde && (a.fechaFactura || a.fecha) <= hasta).forEach((a) => {
      (a.lineas || []).forEach((ln) => {
        const base = Number(ln.importe) || 0;
        const tipo = Number(ln.ivaPct) || 0;
        if (!porTipo[tipo]) porTipo[tipo] = { base: 0, cuota: 0 };
        porTipo[tipo].base += base;
        porTipo[tipo].cuota += base * (tipo / 100);
      });
      const cargoBase = Number(a.cargos) || 0;
      if (cargoBase > 0) {
        const tipo = Number(a.cargosIva) || 21;
        if (!porTipo[tipo]) porTipo[tipo] = { base: 0, cuota: 0 };
        porTipo[tipo].base += cargoBase;
        porTipo[tipo].cuota += cargoBase * (tipo / 100);
      }
    });
    facturasDirectas.filter((f) => f.fecha >= desde && f.fecha <= hasta).forEach((f) => {
      const base = Number(f.importeBase) || 0;
      const tipo = Number(f.ivaPct) || 0;
      if (!porTipo[tipo]) porTipo[tipo] = { base: 0, cuota: 0 };
      porTipo[tipo].base += base;
      porTipo[tipo].cuota += base * (tipo / 100);
    });
    return porTipo;
  }, [albaranes, facturasDirectas, desde, hasta]);
  const sumar = (obj) => Object.values(obj).reduce((a, x) => ({ base: a.base + x.base, cuota: a.cuota + x.cuota }), { base: 0, cuota: 0 });
  const totalRep = sumar(repercutido);
  const totalSop = sumar(soportado);
  const resultado = totalRep.cuota - totalSop.cuota;
  function exportarExcel() {
    const wb = utils.book_new();
    const filasRep = Object.entries(repercutido).map(([tipo, v]) => ({ "Tipo IVA": `${tipo}%`, "Base imponible": Number(v.base.toFixed(2)), "Cuota IVA": Number(v.cuota.toFixed(2)) }));
    filasRep.push({ "Tipo IVA": "TOTAL", "Base imponible": Number(totalRep.base.toFixed(2)), "Cuota IVA": Number(totalRep.cuota.toFixed(2)) });
    const wsRep = utils.json_to_sheet(filasRep);
    utils.book_append_sheet(wb, wsRep, "IVA Repercutido");
    const filasSop = Object.entries(soportado).map(([tipo, v]) => ({ "Tipo IVA": `${tipo}%`, "Base imponible": Number(v.base.toFixed(2)), "Cuota IVA": Number(v.cuota.toFixed(2)) }));
    filasSop.push({ "Tipo IVA": "TOTAL", "Base imponible": Number(totalSop.base.toFixed(2)), "Cuota IVA": Number(totalSop.cuota.toFixed(2)) });
    const wsSop = utils.json_to_sheet(filasSop);
    utils.book_append_sheet(wb, wsSop, "IVA Soportado");
    const wsResumen = utils.json_to_sheet([
      { Concepto: "Periodo", Valor: `T${trimestre} ${anio} (${desde} a ${hasta})` },
      { Concepto: "Base repercutida", Valor: Number(totalRep.base.toFixed(2)) },
      { Concepto: "IVA repercutido", Valor: Number(totalRep.cuota.toFixed(2)) },
      { Concepto: "Base soportada", Valor: Number(totalSop.base.toFixed(2)) },
      { Concepto: "IVA soportado", Valor: Number(totalSop.cuota.toFixed(2)) },
      { Concepto: resultado >= 0 ? "A ingresar" : "A compensar", Valor: Number(Math.abs(resultado).toFixed(2)) }
    ]);
    utils.book_append_sheet(wb, wsResumen, "Resumen");
    const detalleCompras = albaranes.filter((a) => a.estado === "confirmado" && (a.fechaFactura || a.fecha) >= desde && (a.fechaFactura || a.fecha) <= hasta).map((a) => {
      const prov = proveedorPorId(a.proveedorId);
      const base = (a.lineas || []).reduce((x, ln) => x + (Number(ln.importe) || 0), 0) + (Number(a.cargos) || 0);
      const iva = (a.lineas || []).reduce((x, ln) => x + (Number(ln.importe) || 0) * ((Number(ln.ivaPct) || 0) / 100), 0) + (Number(a.cargos) || 0) * ((Number(a.cargosIva) || 0) / 100);
      return {
        Fecha: a.fechaFactura || a.fecha,
        Proveedor: prov ? prov.nombre : "\u2014",
        "N\xBA factura": a.numeroFactura || a.numero || "",
        Base: Number(base.toFixed(2)),
        IVA: Number(iva.toFixed(2)),
        Total: Number((base + iva).toFixed(2))
      };
    });
    if (detalleCompras.length) {
      utils.book_append_sheet(wb, utils.json_to_sheet(detalleCompras), "Detalle compras");
    }
    writeFileSync(wb, `libro_iva_T${trimestre}_${anio}.xlsx`);
  }
  const filas = [.../* @__PURE__ */ new Set([...Object.keys(repercutido), ...Object.keys(soportado)])].sort((a, b) => Number(a) - Number(b));
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Libro de IVA"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Resumen trimestral para tu declaraci\xF3n (modelo 303): IVA repercutido de tus ventas e IVA soportado de tus compras, por tipo. Este programa no presenta el modelo por ti \u2014 te da los n\xFAmeros listos para que tu gestor\xEDa los meta, o para revisarlos t\xFA antes de d\xE1rselos.")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Trimestre" }, /* @__PURE__ */ import_react.default.createElement("select", { value: trimestre, onChange: (e) => setTrimestre(Number(e.target.value)), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("option", { value: 1 }, "1T \u2014 Enero a Marzo"), /* @__PURE__ */ import_react.default.createElement("option", { value: 2 }, "2T \u2014 Abril a Junio"), /* @__PURE__ */ import_react.default.createElement("option", { value: 3 }, "3T \u2014 Julio a Septiembre"), /* @__PURE__ */ import_react.default.createElement("option", { value: 4 }, "4T \u2014 Octubre a Diciembre"))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "A\xF1o" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: anio, onChange: (e) => setAnio(Number(e.target.value) || hoy.getFullYear()) }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Del ", desde, " al ", hasta)), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "IVA repercutido (ventas)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1", style: { color: C.accent } }, "\u20AC", fmt(totalRep.cuota))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "IVA soportado (compras)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, "\u20AC", fmt(totalSop.cuota)))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.chrome, color: "#fff" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[13px] font-medium" }, resultado >= 0 ? "A ingresar en Hacienda" : "A compensar / devolver"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-bold text-[19px]" }, "\u20AC", fmt(Math.abs(resultado))))), filas.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No hay ventas ni compras registradas en este trimestre." }) : /* @__PURE__ */ import_react.default.createElement(Card, { style: { padding: 0 }, className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("table", { className: "w-full text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("thead", null, /* @__PURE__ */ import_react.default.createElement("tr", { style: { color: C.inkSoft, borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("th", { className: "text-left font-medium py-2 px-3" }, "Tipo"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-right font-medium py-2 px-3" }, "Base repercutida"), /* @__PURE__ */ import_react.default.createElement("th", { className: "text-right font-medium py-2 px-3" }, "Base soportada"))), /* @__PURE__ */ import_react.default.createElement("tbody", null, filas.map((tipo) => /* @__PURE__ */ import_react.default.createElement("tr", { key: tipo, style: { borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-3 mono" }, tipo, "%"), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-3 mono text-right" }, repercutido[tipo] ? `\u20AC${fmt(repercutido[tipo].base)}` : "\u2014"), /* @__PURE__ */ import_react.default.createElement("td", { className: "py-2 px-3 mono text-right" }, soportado[tipo] ? `\u20AC${fmt(soportado[tipo].base)}` : "\u2014")))))), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: exportarExcel }, /* @__PURE__ */ import_react.default.createElement(Download, { size: 14 }), " Exportar a Excel"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-2", style: { color: C.inkSoft } }, 'Solo cuentan las ventas registradas con motivo "Venta", los albaranes ya dados de entrada y las facturas directas registradas en Cuentas por pagar. Si falta algo por registrar, este libro tampoco lo ver\xE1.'));
}
function ArqueoCaja({ movimientos, arqueos, addArqueo, deleteArqueo, encargos = [] }) {
  const [fecha, setFecha] = (0, import_react.useState)(todayISO());
  const [contado, setContado] = (0, import_react.useState)("");
  const [notas, setNotas] = (0, import_react.useState)("");
  const [error, setError] = (0, import_react.useState)("");
  const ventasDelDia = movimientos.filter((m) => esVenta(m) && m.fecha === fecha && !m.encargoId);
  const porMedio = { Efectivo: 0, Tarjeta: 0, Transferencia: 0, Otro: 0 };
  ventasDelDia.forEach((m) => {
    const medio = m.medioPago || "Efectivo";
    const importe = Math.abs(Number(m.cantidad) || 0) * (Number(m.ingresoUnitario) || 0) * (1 + (Number(m.ivaVentaAplicado) || 0) / 100);
    if (medio === "Mixto" && m.detallePago) {
      porMedio.Efectivo += Number(m.detallePago.efectivo) || 0;
      porMedio.Tarjeta += Number(m.detallePago.tarjeta) || 0;
    } else {
      porMedio[medio] = (porMedio[medio] || 0) + importe;
    }
  });
  encargos.forEach((e) => {
    (e.cobros || []).forEach((c) => {
      if (c.fecha !== fecha) return;
      const medio = c.medioPago || "Efectivo";
      porMedio[medio] = (porMedio[medio] || 0) + (Number(c.importe) || 0);
    });
  });
  const efectivoEsperado = porMedio.Efectivo || 0;
  const diferencia = contado === "" ? null : Number(contado) - efectivoEsperado;
  const yaArqueado = arqueos.find((a) => a.fecha === fecha);
  function submit() {
    if (contado === "") {
      setError("Escribe cu\xE1nto efectivo hay en caja.");
      return;
    }
    setError("");
    addArqueo({ fecha, efectivoEsperado, efectivoContado: Number(contado), diferencia, notas });
    setContado("");
    setNotas("");
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Arqueo de caja"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Compara el efectivo que hay f\xEDsicamente en el caj\xF3n con lo que el programa dice que se ha vendido en efectivo ese d\xEDa. Las ventas con tarjeta o transferencia no se cuadran aqu\xED, solo se muestran como referencia.")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Fecha" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: fecha, onChange: (e) => setFecha(e.target.value) })), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3 my-3" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Efectivo esperado"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono" }, "\u20AC", fmt(efectivoEsperado))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Otras ventas (tarjeta, transf.)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono", style: { color: C.inkSoft } }, "\u20AC", fmt((porMedio.Tarjeta || 0) + (porMedio.Transferencia || 0) + (porMedio.Otro || 0))))), yaArqueado ? /* @__PURE__ */ import_react.default.createElement("div", { className: "p-2 rounded-lg text-[12.5px]", style: { background: C.bg } }, "Ya arqueaste este d\xEDa: contaste \u20AC", fmt(yaArqueado.efectivoContado), " ", "(", yaArqueado.diferencia === 0 ? "cuadra" : yaArqueado.diferencia > 0 ? `sobran \u20AC${fmt(yaArqueado.diferencia)}` : `faltan \u20AC${fmt(Math.abs(yaArqueado.diferencia))}`, ").", /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "danger", onClick: () => deleteArqueo(yaArqueado.id) }, "Borrar y repetir"))) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Efectivo contado en caja (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: contado, onChange: (e) => setContado(e.target.value), autoFocus: true })), diferencia !== null && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2", style: { color: diferencia === 0 ? C.accent : Math.abs(diferencia) < 1 ? C.amber : C.red } }, diferencia === 0 ? "Cuadra exacto." : diferencia > 0 ? `Sobran \u20AC${fmt(diferencia)}` : `Faltan \u20AC${fmt(Math.abs(diferencia))}`), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Notas (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: notas, onChange: (e) => setNotas(e.target.value), placeholder: "Motivo del descuadre, si lo sabes" })), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, "Guardar arqueo"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Historial de arqueos"), arqueos.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has hecho ning\xFAn arqueo." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, arqueos.slice(0, 30).map((a) => /* @__PURE__ */ import_react.default.createElement(Card, { key: a.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, a.fecha), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold", style: { color: a.diferencia === 0 ? C.accent : Math.abs(a.diferencia) < 1 ? C.amber : C.red } }, a.diferencia === 0 ? "Cuadra" : a.diferencia > 0 ? `+\u20AC${fmt(a.diferencia)}` : `\u2212\u20AC${fmt(Math.abs(a.diferencia))}`))))));
}
var PLANTILLAS_TURNO = [
  { id: "manana", label: "Ma\xF1ana", horaInicio: "08:00", horaFin: "14:00" },
  { id: "tarde", label: "Tarde", horaInicio: "16:00", horaFin: "20:00" },
  { id: "cierre", label: "Cierre", horaInicio: "14:00", horaFin: "21:00" },
  { id: "libre", label: "Libre / descanso", horaInicio: "", horaFin: "" },
  { id: "personalizado", label: "Personalizado", horaInicio: "", horaFin: "" }
];
var DIAS_SEMANA = ["Lunes", "Martes", "Mi\xE9rcoles", "Jueves", "Viernes", "S\xE1bado", "Domingo"];
function fechasSemana(inicioIso) {
  const out = [];
  const d = new Date(inicioIso);
  for (let i = 0; i < 7; i++) {
    const f = new Date(d);
    f.setDate(d.getDate() + i);
    out.push(aISO(f));
  }
  return out;
}
function horasTurno(t) {
  if (!t.horaInicio || !t.horaFin) return 0;
  const [h1, m1] = t.horaInicio.split(":").map(Number);
  const [h2, m2] = t.horaFin.split(":").map(Number);
  return Math.max(0, (h2 * 60 + m2 - (h1 * 60 + m1)) / 60);
}
function Turnos({ empleados, turnos, addTurno, updateTurno, deleteTurno, copiarSemana }) {
  const activos = empleados.filter((e) => e.activo !== false);
  const [inicio, setInicio] = (0, import_react.useState)(inicioSemana(/* @__PURE__ */ new Date()));
  const dias = fechasSemana(inicio);
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [form, setForm] = (0, import_react.useState)(null);
  const [error, setError] = (0, import_react.useState)("");
  const [copiadoMsg, setCopiadoMsg] = (0, import_react.useState)("");
  function irSemana(delta) {
    const d = new Date(inicio);
    d.setDate(d.getDate() + delta * 7);
    setInicio(inicioSemana(d));
  }
  function abrirNuevo(fecha) {
    setForm({ empleadoId: activos[0]?.id || "", fecha: fecha || dias[0], plantilla: "manana", horaInicio: "08:00", horaFin: "14:00", notas: "" });
    setError("");
    setShowForm(true);
  }
  function elegirPlantilla(id) {
    const p = PLANTILLAS_TURNO.find((x) => x.id === id);
    setForm((f) => ({ ...f, plantilla: id, horaInicio: p.horaInicio, horaFin: p.horaFin }));
  }
  function submit() {
    if (!form.empleadoId) {
      setError("Selecciona un empleado.");
      return;
    }
    setError("");
    const plantilla = PLANTILLAS_TURNO.find((p) => p.id === form.plantilla);
    addTurno({
      empleadoId: form.empleadoId,
      fecha: form.fecha,
      tipo: plantilla ? plantilla.label : "Personalizado",
      horaInicio: form.horaInicio,
      horaFin: form.horaFin,
      notas: form.notas
    });
    setShowForm(false);
    setForm(null);
  }
  const empleadoElegido = form ? activos.find((e) => e.id === form.empleadoId) : null;
  const avisoDisponibilidad = (0, import_react.useMemo)(() => {
    if (!form || !empleadoElegido) return null;
    const dias2 = empleadoElegido.diasDisponibles;
    if (!dias2 || dias2.length === 0) return null;
    const diaSemana = new Date(form.fecha).getDay();
    if (dias2.includes(diaSemana)) return null;
    return `${empleadoElegido.nombre} no suele estar disponible los ${DIAS_SEMANA[diaSemana === 0 ? 6 : diaSemana - 1]}.`;
  }, [form, empleadoElegido]);
  function hacerCopiarSemana() {
    const anteriorInicio = new Date(inicio);
    anteriorInicio.setDate(anteriorInicio.getDate() - 7);
    const desdeFechas = fechasSemana(aISO(anteriorInicio));
    const n = copiarSemana(desdeFechas, dias);
    setCopiadoMsg(n > 0 ? `Copiados ${n} turno(s) de la semana anterior.` : "La semana anterior no ten\xEDa turnos que copiar.");
    setTimeout(() => setCopiadoMsg(""), 4e3);
  }
  const horasPorEmpleado = (0, import_react.useMemo)(() => {
    const mapa = {};
    activos.forEach((e) => {
      mapa[e.id] = 0;
    });
    turnos.filter((t) => dias.includes(t.fecha)).forEach((t) => {
      mapa[t.empleadoId] = (mapa[t.empleadoId] || 0) + horasTurno(t);
    });
    return mapa;
  }, [turnos, dias, activos]);
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(
    SectionTitle,
    {
      action: /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: hacerCopiarSemana }, "Copiar semana anterior"), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => abrirNuevo(dias[0]) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " A\xF1adir turno"))
    },
    "Cuadrante de turnos"
  ), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => irSemana(-1) }, "\u2190 Semana anterior"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-medium" }, dias[0], " a ", dias[6]), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => irSemana(1) }, "Siguiente \u2192")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex justify-center mt-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setInicio(inicioSemana(/* @__PURE__ */ new Date())) }, "Esta semana"))), copiadoMsg && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, copiadoMsg)), activos.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "A\xF1ade empleados activos en Personal para poder asignarles turno." }) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-3 mb-4" }, dias.map((fecha, i) => {
    const delDia = turnos.filter((t) => t.fecha === fecha);
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: fecha }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold" }, DIAS_SEMANA[i], " ", /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11px] font-normal", style: { color: C.inkSoft } }, fecha)), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => abrirNuevo(fecha) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 13 }))), delDia.length === 0 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, "Sin turnos asignados.") : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, delDia.map((t) => {
      const emp = empleados.find((e) => e.id === t.empleadoId);
      return /* @__PURE__ */ import_react.default.createElement("div", { key: t.id, className: "flex items-center justify-between text-[12.5px] py-1", style: { borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("span", null, /* @__PURE__ */ import_react.default.createElement("b", null, emp ? emp.nombre : "\u2014"), " \xB7 ", t.tipo, t.horaInicio && ` (${t.horaInicio}\u2013${t.horaFin})`, t.notas && /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, " \xB7 ", t.notas)), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => deleteTurno(t.id), "aria-label": "Eliminar turno" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 14, color: C.inkSoft })));
    })));
  })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Horas asignadas esta semana"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, activos.map((e) => {
    const asignadas = horasPorEmpleado[e.id] || 0;
    const contrato = Number(e.horasSemanales) || 0;
    const diferencia = asignadas - contrato;
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: e.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, e.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, fmt(asignadas), " h ", contrato > 0 && /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, "/ ", fmt(contrato), " h contrato"))), contrato > 0 && Math.abs(diferencia) >= 1 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-0.5", style: { color: diferencia > 0 ? C.amber : C.red } }, diferencia > 0 ? `${fmt(diferencia)} h por encima del contrato` : `${fmt(Math.abs(diferencia))} h por debajo del contrato`));
  }))), showForm && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowForm(false), title: "A\xF1adir turno" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Empleado" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.empleadoId, onChange: (e) => setForm({ ...form, empleadoId: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, activos.map((e) => /* @__PURE__ */ import_react.default.createElement("option", { key: e.id, value: e.id }, e.nombre)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "D\xEDa" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.fecha, onChange: (e) => setForm({ ...form, fecha: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, dias.map((f, i) => /* @__PURE__ */ import_react.default.createElement("option", { key: f, value: f }, DIAS_SEMANA[i], " (", f, ")")))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tipo de turno" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.plantilla, onChange: (e) => elegirPlantilla(e.target.value), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, PLANTILLAS_TURNO.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.label)))), form.plantilla !== "libre" && /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hora inicio" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "time", value: form.horaInicio, onChange: (e) => setForm({ ...form, horaInicio: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hora fin" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "time", value: form.horaFin, onChange: (e) => setForm({ ...form, horaFin: e.target.value }) }))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Notas (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.notas, onChange: (e) => setForm({ ...form, notas: e.target.value }) })), avisoDisponibilidad && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3 p-2 rounded-lg", style: { background: C.amberSoft, color: C.ink } }, "\u26A0 ", avisoDisponibilidad, " Puedes asignarlo igual si es una excepci\xF3n puntual."), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, "Guardar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowForm(false) }, "Cancelar"))));
}
function MapaAlmacen({ productos, proveedorPorId }) {
  const zonas = (0, import_react.useMemo)(() => {
    const mapa = {};
    ZONAS_ALMACEN.forEach((z) => {
      mapa[z] = [];
    });
    productos.forEach((p) => {
      const z = p.zona || "Sin asignar";
      if (!mapa[z]) mapa[z] = [];
      mapa[z].push(p);
    });
    return ZONAS_ALMACEN.map((z) => ({ zona: z, items: mapa[z] || [] })).filter((g) => g.items.length > 0);
  }, [productos]);
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Mapa de almac\xE9n"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Cada producto se asigna a una zona desde su ficha (Productos \u2192 Editar \u2192 Zona del almac\xE9n). Aqu\xED ves de un vistazo qu\xE9 hay en cada rinc\xF3n del local, sin tener que buscar producto a producto.")), zonas.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has asignado ninguna zona a tus productos." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "grid md:grid-cols-2 gap-3" }, zonas.map((g) => {
    const valor = g.items.reduce((a, p) => a + (Number(p.stock) || 0) * (Number(p.costo) || 0), 0);
    const bajos = g.items.filter((p) => (Number(p.stock) || 0) <= (Number(p.stockMinimo) || 0)).length;
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: g.zona }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold text-[14px]" }, g.zona), /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.accent }, g.items.length, " ref.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2", style: { color: C.inkSoft } }, "Valor: ", /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, "\u20AC", fmt(valor)), bajos > 0 && /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.amber } }, " \xB7 ", bajos, " en stock bajo")), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1 max-h-48 overflow-y-auto" }, g.items.sort((a, b) => (a.ubicacion || "").localeCompare(b.ubicacion || "") || a.nombre.localeCompare(b.nombre)).map((p) => {
      const bajo = (Number(p.stock) || 0) <= (Number(p.stockMinimo) || 0);
      return /* @__PURE__ */ import_react.default.createElement("div", { key: p.id, className: "flex items-center justify-between text-[12px] py-0.5" }, /* @__PURE__ */ import_react.default.createElement("span", null, p.nombre, p.ubicacion && /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, " \xB7 ", p.ubicacion)), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: bajo ? C.red : C.inkSoft } }, p.stock, " ", p.unidad));
    })));
  })));
}
function EscanerCodigoBarras({ onDetectado, onClose }) {
  const videoRef = import_react.default.useRef(null);
  const streamRef = import_react.default.useRef(null);
  const [estado, setEstado] = (0, import_react.useState)("iniciando");
  const [manual, setManual] = (0, import_react.useState)("");
  const [intento, setIntento] = (0, import_react.useState)(0);
  (0, import_react.useEffect)(() => {
    let detenido = false;
    async function arrancar() {
      if (!("BarcodeDetector" in window)) {
        setEstado("sin-soporte");
        return;
      }
      if (!window.isSecureContext) {
        setEstado("sin-https");
        return;
      }
      try {
        const stream = await navigator.mediaDevices.getUserMedia({ video: { facingMode: { ideal: "environment" } } });
        if (detenido) {
          stream.getTracks().forEach((t) => t.stop());
          return;
        }
        streamRef.current = stream;
        if (videoRef.current) {
          videoRef.current.srcObject = stream;
          try {
            await videoRef.current.play();
          } catch (e) {
            videoRef.current.onloadedmetadata = () => {
              videoRef.current.play().catch(() => {
              });
            };
          }
        }
        setEstado("activo");
        const detector = new window.BarcodeDetector({
          formats: ["ean_13", "ean_8", "upc_a", "upc_e", "code_128", "code_39", "qr_code"]
        });
        const loop = async () => {
          if (detenido || !videoRef.current) return;
          try {
            if (videoRef.current.readyState >= 2) {
              const codigos = await detector.detect(videoRef.current);
              if (codigos.length > 0) {
                onDetectado(codigos[0].rawValue);
                return;
              }
            }
          } catch (e) {
          }
          if (!detenido) requestAnimationFrame(loop);
        };
        loop();
      } catch (e) {
        setEstado("sin-permiso");
      }
    }
    arrancar();
    return () => {
      detenido = true;
      if (streamRef.current) streamRef.current.getTracks().forEach((t) => t.stop());
    };
  }, [intento]);
  return /* @__PURE__ */ import_react.default.createElement(Modal, { onClose, title: "Escanear c\xF3digo de barras" }, /* @__PURE__ */ import_react.default.createElement(
    "div",
    {
      className: "rounded-lg overflow-hidden mb-3",
      style: { background: "#000", height: 240, display: "flex", alignItems: "center", justifyContent: "center" }
    },
    /* @__PURE__ */ import_react.default.createElement(
      "video",
      {
        ref: videoRef,
        autoPlay: true,
        muted: true,
        playsInline: true,
        style: { width: "100%", height: "100%", objectFit: "cover", display: estado === "activo" ? "block" : "none" }
      }
    ),
    estado === "iniciando" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]", style: { color: "#fff" } }, "Abriendo la c\xE1mara\u2026")
  ), estado === "sin-soporte" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3", style: { color: C.amber } }, "Tu navegador no permite leer c\xF3digos de barras autom\xE1ticamente (esto solo funciona en Chrome para Android, y en tu web real, no aqu\xED dentro de Claude). Escribe el c\xF3digo a mano abajo."), estado === "sin-https" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2", style: { color: C.red } }, "La c\xE1mara no funciona as\xED \u2014 est\xE1s abriendo el archivo descargado directamente, y por seguridad del navegador, la c\xE1mara solo se puede usar entrando desde tu web real (la que empieza por ", /* @__PURE__ */ import_react.default.createElement("b", null, "https://"), "). Entra desde ah\xED para escanear, o escribe el c\xF3digo a mano abajo."), estado === "sin-permiso" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2", style: { color: C.red } }, "No se pudo acceder a la c\xE1mara \u2014 puede que no hayas dado permiso, o que est\xE9s en un sitio sin conexi\xF3n segura (https). Escribe el c\xF3digo a mano abajo."), estado === "activo" && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, 'Si ves negro m\xE1s de unos segundos, toca "Reintentar".', /* @__PURE__ */ import_react.default.createElement("button", { type: "button", onClick: () => {
    setEstado("iniciando");
    setIntento((i) => i + 1);
  }, className: "ml-1 font-medium", style: { color: C.accent } }, "Reintentar")), /* @__PURE__ */ import_react.default.createElement(Field, { label: "O escribe el c\xF3digo a mano" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: manual, onChange: (e) => setManual(e.target.value), placeholder: "EAN o c\xF3digo", autoFocus: estado !== "activo" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => manual.trim() && onDetectado(manual.trim()) }, "Usar este c\xF3digo"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: onClose }, "Cancelar")));
}
function Auditoria({ auditoria }) {
  const [desde, setDesde] = (0, import_react.useState)(primerDiaMes(/* @__PURE__ */ new Date()));
  const [hasta, setHasta] = (0, import_react.useState)(todayISO());
  const [copiado, setCopiado] = (0, import_react.useState)(false);
  const filtrada = auditoria.filter((a) => a.fecha >= desde && a.fecha <= hasta);
  function textoAuditoria() {
    const lineas = filtrada.map((a) => `${a.fecha} ${a.hora} \xB7 [${a.usuario || a.modo}] ${a.accion} \u2014 ${a.detalle}`);
    return `AUDITOR\xCDA DE CAMBIOS
Del ${desde} al ${hasta}

${lineas.join("\n")}

Generado el ${(/* @__PURE__ */ new Date()).toLocaleString("es-ES")}`;
  }
  function copiar() {
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(textoAuditoria()).then(() => setCopiado(true)).catch(() => setCopiado(false));
    }
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Auditor\xEDa"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Registro de las acciones sensibles: borrados, pagos marcados, ajustes de inventario, anulaciones y variaciones de precio al confirmar un albar\xE1n. No registra cada clic \u2014 solo lo que de verdad importa revisar si algo sale mal.")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Desde" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: desde, onChange: (e) => setDesde(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hasta" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: hasta, onChange: (e) => setHasta(e.target.value) }))), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: copiar }, copiado ? "\xA1Copiado!" : "Copiar como texto")), filtrada.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin acciones registradas en este periodo." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, filtrada.map((a) => /* @__PURE__ */ import_react.default.createElement(Card, { key: a.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-medium" }, a.accion), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, a.detalle)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right shrink-0 ml-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mono", style: { color: C.inkSoft } }, a.fecha, " ", a.hora), /* @__PURE__ */ import_react.default.createElement(Pill, { color: (a.usuario || a.modo) !== "Propietario/a" ? C.amber : C.inkSoft }, a.usuario || a.modo)))))));
}
var ESTADO_RECONCILIACION = {
  OK: { texto: "OK", color: "#16a34a" },
  DEFICIT: { texto: "D\xE9ficit", color: "#d97706" },
  DISCREPANCIA: { texto: "Discrepancia", color: "#dc2626" },
  DEFICIT_Y_DISCREPANCIA: { texto: "D\xE9ficit + discrepancia", color: "#dc2626" }
};
function DiagnosticoStock({ diagnostico, corregirProducto, movimientosParaReconciliar }) {
  const [filtro, setFiltro] = (0, import_react.useState)("discrepancias");
  const [verMovimientosDe, setVerMovimientosDe] = (0, import_react.useState)(null);
  const [confirmarCorreccion, setConfirmarCorreccion] = (0, import_react.useState)(null);
  const [ultimaCorreccion, setUltimaCorreccion] = (0, import_react.useState)("");
  const [procesandoCorreccion, setProcesandoCorreccion] = (0, import_react.useState)(false);
  const resumen = (0, import_react.useMemo)(() => {
    const r = { revisados: diagnostico.length, ok: 0, deficit: 0, discrepancia: 0, diferenciaTotal: 0 };
    diagnostico.forEach((d) => {
      if (d.estado === "OK") r.ok++;
      else if (d.estado === "DEFICIT") r.deficit++;
      else r.discrepancia++;
      if (!d.coincide) r.diferenciaTotal += d.diferencia;
    });
    return r;
  }, [diagnostico]);
  const visibles = (0, import_react.useMemo)(() => {
    if (filtro === "todos") return diagnostico;
    if (filtro === "correctos") return diagnostico.filter((d) => d.estado === "OK");
    if (filtro === "deficit") return diagnostico.filter((d) => d.estado === "DEFICIT" || d.estado === "DEFICIT_Y_DISCREPANCIA");
    return diagnostico.filter((d) => !d.coincide);
  }, [diagnostico, filtro]);
  function pedirConfirmacion(d) {
    setConfirmarCorreccion(d);
  }
  function confirmarYCorregir() {
    if (procesandoCorreccion) return;
    setProcesandoCorreccion(true);
    const d = confirmarCorreccion;
    const r = corregirProducto(d.productoId);
    setConfirmarCorreccion(null);
    setUltimaCorreccion(
      r.sinCambios ? `${d.nombre}: ya estaba corregido (alguien m\xE1s lo dej\xF3 bien mientras tanto).` : `${d.nombre}: corregido de ${fmt(r.antes)} a ${fmt(r.despues)}.`
    );
    setProcesandoCorreccion(false);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Reconciliaci\xF3n de stock"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Compara, producto a producto, lo que dice la pantalla contra lo que dicen de verdad todos sus movimientos sumados desde cero. Si no coinciden, algo desincroniz\xF3 la cach\xE9 \u2014 normalmente, dos dispositivos tocando el mismo producto casi a la vez. Un d\xE9ficit por s\xED solo no es un descuadre: se resuelve solo con la siguiente entrada, sin que hagas nada aqu\xED.")), ultimaCorreccion && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, ultimaCorreccion)), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-4 gap-2 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, { className: "text-center py-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[16px] font-semibold" }, resumen.revisados), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "Revisados")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "text-center py-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[16px] font-semibold", style: { color: "#16a34a" } }, resumen.ok), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "Correctos")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "text-center py-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[16px] font-semibold", style: { color: "#d97706" } }, resumen.deficit), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "Con d\xE9ficit")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "text-center py-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[16px] font-semibold", style: { color: "#dc2626" } }, resumen.discrepancia), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "Discrepancias"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-1.5 mb-3 flex-wrap" }, [
    ["discrepancias", "Discrepancias"],
    ["deficit", "Con d\xE9ficit"],
    ["correctos", "Correctos"],
    ["todos", "Todos"]
  ].map(([id, label]) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: id,
      onClick: () => setFiltro(id),
      className: "text-[11.5px] font-medium px-2.5 py-1 rounded-full",
      style: filtro === id ? { background: C.accent, color: "#fff" } : { background: C.surface, border: `1px solid ${C.line}`, color: C.inkSoft }
    },
    label
  ))), visibles.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] text-center py-4", style: { color: C.inkSoft } }, "Nada que mostrar con este filtro.")) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, visibles.map((d) => /* @__PURE__ */ import_react.default.createElement(Card, { key: d.productoId }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between gap-2" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "min-w-0" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-medium truncate" }, d.nombre), /* @__PURE__ */ import_react.default.createElement(
    "span",
    {
      className: "text-[10px] font-semibold px-1.5 py-0.5 rounded-full shrink-0",
      style: { background: ESTADO_RECONCILIACION[d.estado].color + "22", color: ESTADO_RECONCILIACION[d.estado].color }
    },
    ESTADO_RECONCILIACION[d.estado].texto
  )), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-0.5", style: { color: C.inkSoft } }, "Stock ", fmt(d.stockCache), " \xB7 D\xE9ficit ", fmt(d.deficitCache), " \xB7 Cach\xE9 ", fmt(d.teoricoCache), " \xB7 Hist\xF3rico ", fmt(d.teoricoReal), !d.coincide && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, " \xB7 dif. ", d.diferencia > 0 ? "+" : "", fmt(d.diferencia))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-0.5", style: { color: C.inkSoft } }, d.numMovimientos, " movimiento(s) \xB7 \xFAltimo ", d.ultimoMovimiento || "\u2014")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-col gap-1 shrink-0" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setVerMovimientosDe(d) }, "Ver movimientos"), !d.coincide && /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => pedirConfirmacion(d) }, "Corregir cach\xE9")))))), verMovimientosDe && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setVerMovimientosDe(null), title: `Movimientos \xB7 ${verMovimientosDe.nombre}` }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, "Estos son los movimientos que se han sumado para calcular el hist\xF3rico de este producto \u2014 el m\xE1s reciente primero. Nada de esto se puede editar desde aqu\xED."), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2 max-h-[60vh] overflow-y-auto" }, movimientosParaReconciliar(verMovimientosDe.productoId).map((m) => /* @__PURE__ */ import_react.default.createElement("div", { key: m.id, className: "text-[11px] p-2 rounded-lg", style: { background: C.surfaceSoft, border: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex justify-between font-medium" }, /* @__PURE__ */ import_react.default.createElement("span", null, m.fecha || "\u2014", " \xB7 ", m.tipo || "SIN_CLASIFICAR"), /* @__PURE__ */ import_react.default.createElement("span", { style: { color: cantidadConSigno(m) < 0 ? "#dc2626" : "#16a34a" } }, cantidadConSigno(m) > 0 ? "+" : "", fmt(cantidadConSigno(m)))), /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft } }, "origen: ", m.origen || "\u2014", " \xB7 afectaStockTotal: ", String(!!m.afectaStockTotal)), /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft } }, "operationId: ", m.operationId || "\u2014", " \xB7 movimientoId: ", m.id), m.documentoOrigenId && /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft } }, "documento: ", m.documentoOrigenId), m.revierteMovimientoId && /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft } }, "revierte a: ", m.revierteMovimientoId), /* @__PURE__ */ import_react.default.createElement("div", { style: { color: C.inkSoft } }, "usuario: ", m.usuario || "\u2014", " \xB7 dispositivo: ", m.dispositivo || "\u2014"))))), confirmarCorreccion && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmarCorreccion(null), title: "Confirmar correcci\xF3n" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] mb-3" }, "Vas a corregir ", /* @__PURE__ */ import_react.default.createElement("b", null, confirmarCorreccion.nombre), "."), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] space-y-1 mb-4 p-3 rounded-lg", style: { background: C.surfaceSoft } }, /* @__PURE__ */ import_react.default.createElement("div", null, "Valor actual de la cach\xE9: ", /* @__PURE__ */ import_react.default.createElement("b", null, fmt(confirmarCorreccion.teoricoCache))), /* @__PURE__ */ import_react.default.createElement("div", null, "Valor calculado desde el hist\xF3rico: ", /* @__PURE__ */ import_react.default.createElement("b", null, fmt(confirmarCorreccion.teoricoReal))), /* @__PURE__ */ import_react.default.createElement("div", null, "Diferencia: ", /* @__PURE__ */ import_react.default.createElement("b", null, confirmarCorreccion.diferencia > 0 ? "+" : "", fmt(confirmarCorreccion.diferencia))), /* @__PURE__ */ import_react.default.createElement("div", { className: "pt-1", style: { color: C.inkSoft } }, "Se va a generar un movimiento nuevo de tipo INVENTARIO por ", confirmarCorreccion.diferencia > 0 ? "+" : "", fmt(confirmarCorreccion.diferencia), " \u2014 ning\xFAn movimiento anterior se modifica ni se borra."), /* @__PURE__ */ import_react.default.createElement("div", { className: "pt-1" }, "Resultado esperado: el stock quedar\xE1 en ", /* @__PURE__ */ import_react.default.createElement("b", null, fmt(Math.max(0, confirmarCorreccion.teoricoReal))), confirmarCorreccion.teoricoReal < 0 && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, " con un d\xE9ficit de ", /* @__PURE__ */ import_react.default.createElement("b", null, fmt(-confirmarCorreccion.teoricoReal))), ".")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmarCorreccion(null), disabled: procesandoCorreccion }, "Cancelar"), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: confirmarYCorregir, disabled: procesandoCorreccion }, procesandoCorreccion ? "Corrigiendo\u2026" : "Confirmar correcci\xF3n"))));
}
function Produccion({ fichasCosto, productos, ordenesProduccion, producir, anularProduccion, traspasarStock }) {
  const [fichaId, setFichaId] = (0, import_react.useState)(fichasCosto[0]?.id || "");
  const [unidadesDeseadas, setUnidadesDeseadas] = (0, import_react.useState)("");
  const [ingredientesReales, setIngredientesReales] = (0, import_react.useState)([]);
  const [empaqueReal, setEmpaqueReal] = (0, import_react.useState)("");
  const [manoObraReal, setManoObraReal] = (0, import_react.useState)("");
  const [gastosGeneralesReal, setGastosGeneralesReal] = (0, import_react.useState)("");
  const [unidadesBuenas, setUnidadesBuenas] = (0, import_react.useState)("");
  const [notas, setNotas] = (0, import_react.useState)("");
  const [error, setError] = (0, import_react.useState)("");
  const [confirmacionFaltantes, setConfirmacionFaltantes] = (0, import_react.useState)("");
  const [resultado, setResultado] = (0, import_react.useState)(null);
  const [verHistorial, setVerHistorial] = (0, import_react.useState)(false);
  const [anulando, setAnulando] = (0, import_react.useState)(null);
  const [procesandoProduccion, setProcesandoProduccion] = (0, import_react.useState)(false);
  const [enviadoAPiso, setEnviadoAPiso] = (0, import_react.useState)(false);
  const [errorEnvio, setErrorEnvio] = (0, import_react.useState)("");
  function enviarAPisoDeVenta(orden) {
    const productoId = orden.productoVinculadoId || (fichasCosto.find((f) => f.id === orden.fichaId) || {}).productoVinculadoId;
    if (!productoId) {
      setErrorEnvio("Esta ficha no tiene un producto de venta enlazado.");
      return;
    }
    const res = traspasarStock(productoId, Number(orden.unidadesBuenas) || 0, "a_piso");
    if (!res.ok) {
      setErrorEnvio(res.error);
      return;
    }
    setErrorEnvio("");
    setEnviadoAPiso(true);
  }
  function rehacer(orden) {
    anularProduccion(orden);
    setFichaId(orden.fichaId);
    setUnidadesDeseadas(String(orden.unidadesDeseadas || ""));
    setUnidadesBuenas(String(orden.unidadesBuenas || ""));
    setIngredientesReales(
      (orden.ingredientes || []).map((i) => {
        const p = productos.find((x) => x.id === i.productoId);
        return {
          ...i,
          nombre: p ? p.nombre : i.nombre || "\u2014",
          unidad: p ? p.unidad : i.unidad || "",
          stockDisponible: p ? Number(p.stock) || 0 : 0
        };
      })
    );
    setEmpaqueReal(orden.empaqueReal ?? "");
    setManoObraReal(orden.manoObraReal ?? "");
    setGastosGeneralesReal(orden.gastosGeneralesReal ?? "");
    setNotas(orden.notas || "");
    setError("");
    setVerHistorial(false);
    if (typeof window !== "undefined" && window.scrollTo) window.scrollTo({ top: 0, behavior: "smooth" });
  }
  const ficha = fichasCosto.find((f) => f.id === fichaId);
  const rendimiento = Number(ficha?.rendimiento) || 1;
  const factor = ficha && Number(unidadesDeseadas) > 0 ? Number(unidadesDeseadas) / rendimiento : 1;
  function recalcular(fichaElegida, deseadas) {
    if (!fichaElegida) {
      setIngredientesReales([]);
      return;
    }
    const f = Number(deseadas) > 0 ? Number(deseadas) / (Number(fichaElegida.rendimiento) || 1) : 1;
    const lista = (fichaElegida.componentes || []).filter((c) => c.productoId).map((c) => {
      const p = productos.find((x) => x.id === c.productoId);
      const teorica = (Number(c.cantidad) || 0) * f;
      return {
        productoId: c.productoId,
        nombre: p ? p.nombre : c.nombre || "\u2014",
        unidad: p ? p.unidad : "",
        stockDisponible: p ? Number(p.stock) || 0 : 0,
        cantidadTeorica: teorica,
        cantidadReal: Number(teorica.toFixed(3))
      };
    });
    setIngredientesReales(lista);
    setEmpaqueReal(Number(((Number(fichaElegida.empaque) || 0) * f).toFixed(2)));
    setManoObraReal(Number(((Number(fichaElegida.manoObra) || 0) * f).toFixed(2)));
    setGastosGeneralesReal(Number(((Number(fichaElegida.gastosGenerales) || 0) * f).toFixed(2)));
  }
  function elegirFicha(id) {
    setFichaId(id);
    setError("");
    setResultado(null);
    setConfirmacionFaltantes("");
    const f = fichasCosto.find((x) => x.id === id);
    setUnidadesDeseadas(f ? String(f.rendimiento || 1) : "");
    setUnidadesBuenas(f ? String(f.rendimiento || 1) : "");
    recalcular(f, f ? f.rendimiento : 1);
  }
  function cambiarDeseadas(v) {
    setUnidadesDeseadas(v);
    setUnidadesBuenas(v);
    setConfirmacionFaltantes("");
    recalcular(ficha, v);
  }
  function actualizarIngrediente(idx, valor) {
    setIngredientesReales((s) => s.map((ing, i) => i === idx ? { ...ing, cantidadReal: valor } : ing));
    setConfirmacionFaltantes("");
  }
  const costoIngredientes = ingredientesReales.reduce((a, ing) => {
    const p = productos.find((x) => x.id === ing.productoId);
    return a + (Number(ing.cantidadReal) || 0) * (p ? Number(p.costo) || 0 : 0);
  }, 0);
  const costoTotalPrevisto = costoIngredientes + (Number(empaqueReal) || 0) + (Number(manoObraReal) || 0) + (Number(gastosGeneralesReal) || 0);
  const costoPorUnidadPrevisto = Number(unidadesBuenas) > 0 ? costoTotalPrevisto / Number(unidadesBuenas) : 0;
  const faltantes = ingredientesReales.filter((ing) => Number(ing.cantidadReal) > ing.stockDisponible);
  const FRASE_CONFIRMACION_FALTANTES = "CONFIRMO";
  function submit() {
    if (procesandoProduccion) return;
    if (!ficha) {
      setError("Elige una ficha de costo.");
      return;
    }
    if (!ficha.productoVinculadoId) {
      setError('Esta ficha no tiene un producto de venta enlazado. Ve a Fichas de costo y usa "Crear producto de venta" primero.');
      return;
    }
    if (!(Number(unidadesBuenas) > 0)) {
      setError("Indica cu\xE1ntas unidades buenas han salido.");
      return;
    }
    if (faltantes.length > 0 && confirmacionFaltantes.trim().toUpperCase() !== FRASE_CONFIRMACION_FALTANTES) {
      setError(
        `Te falta stock de: ${faltantes.map((f) => f.nombre).join(", ")}. Si quieres producir igual, escribe "${FRASE_CONFIRMACION_FALTANTES}" en la casilla de abajo.`
      );
      return;
    }
    setError("");
    setProcesandoProduccion(true);
    try {
      const res = producir({
        fichaId,
        unidadesDeseadas,
        unidadesBuenas,
        ingredientesReales,
        empaqueReal,
        manoObraReal,
        gastosGeneralesReal,
        notas
      });
      if (!res.ok) {
        setError(res.error);
        return;
      }
      setResultado(res.orden);
      setNotas("");
      setConfirmacionFaltantes("");
    } finally {
      setProcesandoProduccion(false);
    }
  }
  function cancelarFormulario() {
    setFichaId("");
    setUnidadesDeseadas("");
    setUnidadesBuenas("");
    setIngredientesReales([]);
    setEmpaqueReal("");
    setManoObraReal("");
    setGastosGeneralesReal("");
    setNotas("");
    setConfirmacionFaltantes("");
    setError("");
    setResultado(null);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Producci\xF3n"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Convierte una ficha de costo en una fabricaci\xF3n real: descuenta los ingredientes que de verdad se gastaron (no solo la teor\xEDa de la receta) y da entrada al producto elaborado con el coste real de esta tanda. Ajusta las cantidades si hubo mermas \u2014 un huevo roto, un poco m\xE1s de mantequilla, lo que sea.")), fichasCosto.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Crea antes una ficha de costo en 'Fichas de costo' para poder producir algo." }) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "\xBFQu\xE9 vas a producir?" }, /* @__PURE__ */ import_react.default.createElement("select", { value: fichaId, onChange: (e) => elegirFicha(e.target.value), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Selecciona una ficha\u2026"), fichasCosto.map((f) => /* @__PURE__ */ import_react.default.createElement("option", { key: f.id, value: f.id }, f.nombre)))), ficha && !ficha.productoVinculadoId && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.amber } }, 'Esta ficha no tiene un producto de venta enlazado \u2014 ve a Fichas de costo y usa "Crear producto de venta" antes de producir.'), ficha && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2", style: { color: C.inkSoft } }, "La receta rinde ", rendimiento, " unidad(es) de base."), /* @__PURE__ */ import_react.default.createElement(Field, { label: "\xBFCu\xE1ntas unidades quieres producir?" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: unidadesDeseadas, onChange: (e) => cambiarDeseadas(e.target.value) })))), ficha && ingredientesReales.length > 0 && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Ingredientes \u2014 ajusta si hubo mermas"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2 mb-4" }, ingredientesReales.map((ing, idx) => {
    const faltaStock = Number(ing.cantidadReal) > ing.stockDisponible;
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: idx }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px] font-medium" }, ing.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11px]", style: { color: C.inkSoft } }, "Receta: ", fmt(ing.cantidadTeorica), " ", ing.unidad)), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.001", value: ing.cantidadReal, onChange: (e) => actualizarIngrediente(idx, e.target.value) }), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12px] shrink-0", style: { color: C.inkSoft } }, ing.unidad)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: faltaStock ? C.red : C.inkSoft } }, "Stock disponible: ", fmt(ing.stockDisponible), " ", ing.unidad, faltaStock && " \u2014 no hay suficiente"));
  })), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Otros costes de esta tanda"), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-x-2" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Empaque (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: empaqueReal, onChange: (e) => setEmpaqueReal(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Mano de obra (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: manoObraReal, onChange: (e) => setManoObraReal(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Gastos generales (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: gastosGeneralesReal, onChange: (e) => setGastosGeneralesReal(e.target.value) })))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "\xBFCu\xE1ntas unidades buenas han salido realmente?" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: unidadesBuenas, onChange: (e) => setUnidadesBuenas(e.target.value) })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-2", style: { color: C.inkSoft } }, "Si se te quem\xF3 parte de la tanda, pon aqu\xED solo lo aprovechable \u2014 el coste se reparte entre estas unidades."), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Notas (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: notas, onChange: (e) => setNotas(e.target.value) }))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.chrome, color: "#fff" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px]" }, "Coste previsto de esta tanda"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-bold" }, "\u20AC", fmt(costoTotalPrevisto))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mt-1" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px]" }, "Coste por unidad"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-bold" }, "\u20AC", fmt(costoPorUnidadPrevisto)))), faltantes.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.redSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2" }, "Te falta stock de: ", faltantes.map((f) => f.nombre).join(", "), ". Si produces igual, ese ingrediente se quedar\xE1 en 0 aunque hayas gastado m\xE1s de lo que ten\xEDas \u2014 revisa si de verdad lo tienes o si falta darle entrada por Albaranes."), /* @__PURE__ */ import_react.default.createElement(Field, { label: `Para producir igual, escribe "${FRASE_CONFIRMACION_FALTANTES}"` }, /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      value: confirmacionFaltantes,
      onChange: (e) => setConfirmacionFaltantes(e.target.value),
      placeholder: FRASE_CONFIRMACION_FALTANTES
    }
  ))), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit, disabled: procesandoProduccion }, procesandoProduccion ? "Registrando\u2026" : "Confirmar producci\xF3n"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: cancelarFormulario, disabled: procesandoProduccion }, "Cancelar")))), resultado && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => {
    setResultado(null);
    setEnviadoAPiso(false);
  }, title: "Producci\xF3n registrada" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3" }, resultado.fichaNombre, ": ", fmt(resultado.unidadesBuenas), " unidad(es) a \u20AC", fmt(resultado.costoUnitarioLote), "/ud. Ya se ha descontado el stock de ingredientes y dado entrada al producto elaborado."), enviadoAPiso ? /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Enviadas al piso de venta. Ya aparecen en ", /* @__PURE__ */ import_react.default.createElement("b", null, "Venta r\xE1pida"), " para cobrarlas.")) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Est\xE1n en el almac\xE9n, todav\xEDa no en el mostrador. Para poder venderlas en Venta r\xE1pida hay que pasarlas al piso de venta.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => enviarAPisoDeVenta(resultado) }, "Enviar las ", fmt(resultado.unidadesBuenas), " al piso de venta")), errorEnvio && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, errorEnvio)), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setResultado(null);
    setEnviadoAPiso(false);
  } }, "Cerrar")), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setVerHistorial((s) => !s), className: "text-[12.5px] font-medium mt-4 mb-2", style: { color: C.accent } }, verHistorial ? "Ocultar" : "Ver", " historial de producci\xF3n (", ordenesProduccion.length, ")"), verHistorial && /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, ordenesProduccion.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has registrado ninguna producci\xF3n." }) : ordenesProduccion.map((o) => /* @__PURE__ */ import_react.default.createElement(Card, { key: o.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, o.fechaHora || `${o.fecha} ${o.hora}`, " \xB7 ", o.fichaNombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, fmt(o.unidadesBuenas), " ud \xB7 \u20AC", fmt(o.costoUnitarioLote), "/ud")), o.notas && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mt-1", style: { color: C.inkSoft } }, o.notas), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setAnulando(o) }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 13 }), " Anular"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => rehacer(o) }, "Corregir"))))), anulando && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setAnulando(null), title: "\xBFAnular esta producci\xF3n?" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3" }, /* @__PURE__ */ import_react.default.createElement("b", null, anulando.fichaNombre), " \xB7 ", fmt(anulando.unidadesBuenas), " unidad(es) del ", anulando.fecha), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3" }, "Se devolver\xE1n los ingredientes al almac\xE9n y se quitar\xE1n las unidades elaboradas. Se borran tambi\xE9n sus movimientos, para que no cuenten dos veces en los informes."), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, "El coste del producto elaborado vuelve al que ten\xEDa antes de esta tanda. Ese c\xE1lculo es exacto si no has producido ni recibido m\xE1s de ese producto desde entonces; si s\xED, quedar\xE1 aproximado \u2014 conviene revisar su coste en Productos despu\xE9s de anular.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: () => {
    anularProduccion(anulando);
    setAnulando(null);
  } }, "S\xED, anular"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setAnulando(null) }, "Cancelar"))));
}
var MOTIVOS_MERMA_FORMULARIO = [
  "Merma",
  "Caducidad",
  "Deterioro",
  "Rotura",
  "Producci\xF3n",
  "Robo o p\xE9rdida",
  "Error de registro",
  "Devoluci\xF3n no registrada",
  "Consumo interno",
  "Autoconsumo",
  "Degustaci\xF3n",
  "Producto defectuoso",
  "Otro"
];
function Mermas({ productos, movimientos, registrarSalida, almacenCongelado }) {
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [productoId, setProductoId] = (0, import_react.useState)("");
  const [cantidad, setCantidad] = (0, import_react.useState)("");
  const [motivo, setMotivo] = (0, import_react.useState)("Merma");
  const [observaciones, setObservaciones] = (0, import_react.useState)("");
  const [error, setError] = (0, import_react.useState)("");
  const [filtroMotivo, setFiltroMotivo] = (0, import_react.useState)("todos");
  const [desde, setDesde] = (0, import_react.useState)(primerDiaMes(/* @__PURE__ */ new Date()));
  const [hasta, setHasta] = (0, import_react.useState)(ultimoDiaMes(/* @__PURE__ */ new Date()));
  const productoPorId = (id) => productos.find((p) => p.id === id);
  const registros = (0, import_react.useMemo)(
    () => movimientos.filter((m) => esMerma(m) && m.fecha >= desde && m.fecha <= hasta).filter((m) => filtroMotivo === "todos" || m.motivo === filtroMotivo).sort((a, b) => (b.fecha || "").localeCompare(a.fecha || "")),
    [movimientos, desde, hasta, filtroMotivo]
  );
  const valorTotal = registros.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0), 0);
  const porMotivo = (0, import_react.useMemo)(() => {
    const mapa = {};
    registros.forEach((m) => {
      const clave = m.motivo || "Sin especificar";
      const valor = Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0);
      mapa[clave] = (mapa[clave] || 0) + valor;
    });
    return Object.entries(mapa).sort((a, b) => b[1] - a[1]);
  }, [registros]);
  function submit() {
    if (!productoId) {
      setError("Elige un producto.");
      return;
    }
    if (!cantidad || Number(cantidad) <= 0) {
      setError("Indica una cantidad mayor que cero.");
      return;
    }
    const ok = registrarSalida(productoId, cantidad, { motivo, referencia: observaciones ? `${motivo} \xB7 ${observaciones}` : motivo });
    if (!ok) {
      setError("No se ha podido registrar \u2014 comprueba que el almac\xE9n no est\xE9 congelado por un conteo en curso.");
      return;
    }
    setShowForm(false);
    setProductoId("");
    setCantidad("");
    setMotivo("Merma");
    setObservaciones("");
    setError("");
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => setShowForm((s) => !s) }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Registrar merma") }, "Mermas"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, 'Todo lo que se ha perdido \u2014caducidad, roturas, deterioro, autoconsumo, degustaciones, errores...\u2014 en un solo sitio, en vez de enterrado dentro de "Salida manual" de cada producto.')), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, "Registrar merma"), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Producto" }, /* @__PURE__ */ import_react.default.createElement("select", { value: productoId, onChange: (e) => setProductoId(e.target.value), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "Selecciona un producto"), productos.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Cantidad" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: cantidad, onChange: (e) => setCantidad(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Motivo" }, /* @__PURE__ */ import_react.default.createElement("select", { value: motivo, onChange: (e) => setMotivo(e.target.value), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, MOTIVOS_MERMA_FORMULARIO.map((m) => /* @__PURE__ */ import_react.default.createElement("option", { key: m, value: m }, m)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Observaciones (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: observaciones, onChange: (e) => setObservaciones(e.target.value), placeholder: "Ej. se qued\xF3 fuera de la c\xE1mara toda la noche" })), almacenCongelado && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2 p-2 rounded-lg", style: { background: C.amberSoft, color: C.ink } }, "El almac\xE9n est\xE1 congelado por un conteo en curso \u2014 no se puede registrar hasta que termine."), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowForm(false) }, "Cancelar"), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit, disabled: almacenCongelado }, "Registrar"))), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-2 mb-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Desde" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: desde, onChange: (e) => setDesde(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Hasta" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "date", value: hasta, onChange: (e) => setHasta(e.target.value) }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-1.5 flex-wrap" }, /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      onClick: () => setFiltroMotivo("todos"),
      className: "text-[11.5px] font-medium px-2.5 py-1 rounded-full",
      style: filtroMotivo === "todos" ? { background: C.accent, color: "#fff" } : { background: C.surface, border: `1px solid ${C.line}`, color: C.inkSoft }
    },
    "Todos"
  ), porMotivo.map(([m]) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: m,
      onClick: () => setFiltroMotivo(m),
      className: "text-[11.5px] font-medium px-2.5 py-1 rounded-full",
      style: filtroMotivo === m ? { background: C.accent, color: "#fff" } : { background: C.surface, border: `1px solid ${C.line}`, color: C.inkSoft }
    },
    m
  )))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-2 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, { className: "text-center py-2.5" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[16px] font-semibold mono" }, registros.length), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "Registros en el periodo")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "text-center py-2.5" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[16px] font-semibold mono", style: { color: C.red } }, "\u20AC", fmt(valorTotal)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "Valor total (a coste)"))), porMotivo.length > 1 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Por motivo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, porMotivo.map(([m, valor]) => /* @__PURE__ */ import_react.default.createElement("div", { key: m, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, m), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: C.red } }, "\u20AC", fmt(valor)))))), registros.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin mermas registradas en este periodo." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, registros.map((m) => {
    const prod = productoPorId(m.productoId);
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: m.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-medium" }, prod ? prod.nombre : "Producto eliminado"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, m.fecha, " \xB7 ", m.motivo || "Sin especificar")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "mono text-[13px]", style: { color: C.red } }, "-", fmt(Math.abs(cantidadConSigno(m))), " ", prod ? prod.unidad : ""), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "\u20AC", fmt(Math.abs(cantidadConSigno(m)) * (Number(m.costoUnitario) || 0))))));
  })));
}
function EtiquetasCatalogo({ productos, fichasCosto, alergenosDeFicha }) {
  const [vista, setVista] = (0, import_react.useState)("etiquetas");
  const [seleccionados, setSeleccionados] = (0, import_react.useState)([]);
  const [copias, setCopias] = (0, import_react.useState)(1);
  const vendibles = (0, import_react.useMemo)(() => {
    const conFicha = new Map(fichasCosto.filter((f) => f.productoVinculadoId).map((f) => [f.productoVinculadoId, f]));
    return productos.filter((p) => p.tipo === "elaborado" || Number(p.precioVenta) > 0).map((p) => {
      const ficha = conFicha.get(p.id);
      const alergenos = ficha ? alergenosDeFicha(ficha) : p.alergenos || [];
      return { producto: p, alergenos };
    }).sort((a, b) => (a.producto.categoria || "").localeCompare(b.producto.categoria || "") || a.producto.nombre.localeCompare(b.producto.nombre));
  }, [productos, fichasCosto, alergenosDeFicha]);
  function alternar(id) {
    setSeleccionados((s) => s.includes(id) ? s.filter((x) => x !== id) : [...s, id]);
  }
  function seleccionarTodos() {
    setSeleccionados(vendibles.map((v) => v.producto.id));
  }
  function quitarTodos() {
    setSeleccionados([]);
  }
  const etiquetasAImprimir = (0, import_react.useMemo)(() => {
    const elegidos = vendibles.filter((v) => seleccionados.includes(v.producto.id));
    const n = Math.max(1, Math.min(20, Number(copias) || 1));
    const out = [];
    elegidos.forEach((v) => {
      for (let i = 0; i < n; i++) out.push(v);
    });
    return out;
  }, [vendibles, seleccionados, copias]);
  const catalogoPorCategoria = (0, import_react.useMemo)(() => {
    const mapa = {};
    vendibles.forEach((v) => {
      const cat = v.producto.categoria || "Sin categor\xEDa";
      if (!mapa[cat]) mapa[cat] = [];
      mapa[cat].push(v);
    });
    return Object.entries(mapa).sort((a, b) => a[0].localeCompare(b[0]));
  }, [vendibles]);
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Etiquetas y cat\xE1logo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: vista === "etiquetas" ? "primary" : "ghost", onClick: () => setVista("etiquetas") }, "Etiquetas de vitrina"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: vista === "catalogo" ? "primary" : "ghost", onClick: () => setVista("catalogo") }, "Cat\xE1logo"))), vendibles.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no tienes productos de venta con precio puesto. Ponles precio en Productos, o crea uno desde una ficha de costo." }) : vista === "etiquetas" ? /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir" }, /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Marca los productos que quieres etiquetar. Cada uno saca su nombre, precio y al\xE9rgenos, listos para recortar y poner en la vitrina.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12px] font-medium", style: { color: C.inkSoft } }, seleccionados.length, " seleccionado(s)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: seleccionarTodos }, "Seleccionar todos"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: quitarTodos }, "Ninguno"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1 mb-4", style: { maxHeight: 280, overflowY: "auto" } }, vendibles.map((v) => /* @__PURE__ */ import_react.default.createElement("label", { key: v.producto.id, className: "flex items-center gap-2 px-3 py-2 rounded-lg", style: { background: C.surface, border: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("input", { type: "checkbox", checked: seleccionados.includes(v.producto.id), onChange: () => alternar(v.producto.id) }), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px] flex-1" }, v.producto.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11.5px] mono", style: { color: C.inkSoft } }, "\u20AC", fmt(precioNeto(v.producto) * (1 + ivaDe(v.producto) / 100)))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Copias por producto (por si pones varias en la vitrina)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", min: "1", max: "20", value: copias, onChange: (e) => setCopias(e.target.value) })), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => window.print(), disabled: seleccionados.length === 0 }, "Imprimir etiquetas"))), etiquetasAImprimir.length > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-3" }, etiquetasAImprimir.map((v, i) => /* @__PURE__ */ import_react.default.createElement(
    "div",
    {
      key: i,
      className: "rounded-lg p-3 flex flex-col justify-between",
      style: { border: "1px dashed #999", minHeight: 130 }
    },
    /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold leading-tight mb-1" }, v.producto.nombre), v.producto.categoria && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10px]", style: { color: "#666" } }, v.producto.categoria)),
    /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[20px] font-bold mono", style: { color: "#8C6D2A" } }, "\u20AC", fmt(precioNeto(v.producto) * (1 + ivaDe(v.producto) / 100))), v.alergenos.length > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] mt-1" }, v.alergenos.map((id) => {
      const a = alergenoPorId(id);
      return a ? a.icono : "";
    }).join(" ")))
  ))))) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "no-imprimir mb-3" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => window.print() }, "Imprimir cat\xE1logo")), /* @__PURE__ */ import_react.default.createElement("div", { className: "zona-impresion", style: ESTILO_IMPRESION_CLARO }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-end justify-between mb-3 pb-3", style: { borderBottom: "2px solid #8C6D2A" } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[17px] font-semibold" }, "Cat\xE1logo de productos"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: "#6B7A6E" } }, "Actualizado el ", (/* @__PURE__ */ new Date()).toLocaleDateString("es-ES"))), /* @__PURE__ */ import_react.default.createElement("img", { src: LOGO, alt: "", style: { height: 52, width: "auto" } })), catalogoPorCategoria.map(([categoria, items]) => /* @__PURE__ */ import_react.default.createElement("div", { key: categoria, className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-1.5 px-2 py-1", style: { background: "#F1E7CE" } }, categoria), items.map((v) => /* @__PURE__ */ import_react.default.createElement("div", { key: v.producto.id, className: "flex items-center justify-between py-1.5", style: { borderBottom: "1px solid #E2DBC7" } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, v.producto.nombre), v.alergenos.length > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: "#6B7A6E" } }, v.alergenos.map((id) => {
    const a = alergenoPorId(id);
    return a ? `${a.icono} ${a.nombre}` : "";
  }).join(" \xB7 "))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold text-[13px]" }, "\u20AC", fmt(precioNeto(v.producto) * (1 + ivaDe(v.producto) / 100))))))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10px] mt-4", style: { color: "#6B7A6E" } }, "Precios con IVA incluido. Puede haber trazas de otros al\xE9rgenos por manipulaci\xF3n en el mismo obrador."))));
}
function VentaRapida({ productos, venderCarrito, anularVenta, movimientos = [], registrarAuditoria }) {
  const [carrito, setCarrito] = (0, import_react.useState)([]);
  const [categoria, setCategoria] = (0, import_react.useState)("Todos");
  const [busqueda, setBusqueda] = (0, import_react.useState)("");
  const [avisoEscaneo, setAvisoEscaneo] = (0, import_react.useState)("");
  const [confirmAnular, setConfirmAnular] = (0, import_react.useState)(null);
  const [motivoAnular, setMotivoAnular] = (0, import_react.useState)("");
  const [errorAnular, setErrorAnular] = (0, import_react.useState)("");
  const [procesandoAnulacion, setProcesandoAnulacion] = (0, import_react.useState)(false);
  const ventasDeHoy = (0, import_react.useMemo)(() => {
    const hoy = todayISO();
    const anuladas = new Set(
      (movimientos || []).filter((m) => m.anulaVentaId).map((m) => m.anulaVentaId)
    );
    const porVenta = {};
    (movimientos || []).filter((m) => m.fecha === hoy && m.ventaId && m.tipo === "salida").forEach((m) => {
      if (!porVenta[m.ventaId]) porVenta[m.ventaId] = [];
      porVenta[m.ventaId].push(m);
    });
    return Object.entries(porVenta).map(([ventaId, lineas]) => {
      const importe = lineas.reduce(
        (a, l) => a + (Number(l.cantidad) || 0) * (Number(l.ingresoUnitario) || 0) * (1 + (Number(l.ivaVentaAplicado) || 0) / 100),
        0
      );
      const nombres = lineas.map((l) => {
        const p = productos.find((x) => x.id === l.productoId);
        return `${fmt(l.cantidad)}\xD7 ${p ? p.nombre : "\u2014"}`;
      });
      return {
        ventaId,
        resumen: nombres.slice(0, 3).join(", ") + (nombres.length > 3 ? ` y ${nombres.length - 3} m\xE1s` : ""),
        importe,
        medioPago: lineas[0].medioPago || "\u2014",
        usuario: lineas[0].usuario || "",
        anulada: anuladas.has(ventaId)
      };
    });
  }, [movimientos, productos]);
  function confirmarAnulacion() {
    if (procesandoAnulacion) return;
    if (!confirmAnular || !anularVenta) return;
    setProcesandoAnulacion(true);
    try {
      const r = anularVenta(confirmAnular.ventaId, movimientos, motivoAnular.trim());
      if (!r.ok) {
        setErrorAnular(r.error || "No se ha podido anular la venta.");
        return;
      }
      if (registrarAuditoria) {
        registrarAuditoria(
          "Anular venta",
          `${confirmAnular.resumen} \xB7 \u20AC${fmt(confirmAnular.importe)}${motivoAnular.trim() ? " \xB7 " + motivoAnular.trim() : ""}`
        );
      }
      setConfirmAnular(null);
      setMotivoAnular("");
      setErrorAnular("");
    } finally {
      setProcesandoAnulacion(false);
    }
  }
  const inputEscaneoRef = (0, import_react.useRef)(null);
  const [showCobro, setShowCobro] = (0, import_react.useState)(false);
  const [medioPago, setMedioPago] = (0, import_react.useState)("Efectivo");
  const [efectivoRecibido, setEfectivoRecibido] = (0, import_react.useState)("");
  const [importeTarjetaMixto, setImporteTarjetaMixto] = (0, import_react.useState)("");
  const [confirmacion, setConfirmacion] = (0, import_react.useState)(null);
  const [enviandoVenta, setEnviandoVenta] = (0, import_react.useState)(false);
  const [errorVenta, setErrorVenta] = (0, import_react.useState)("");
  const vendibles = (0, import_react.useMemo)(
    () => productos.filter((p) => p.activo !== false && (p.tipo === "elaborado" || Number(p.precioVenta) > 0) && (Number(p.stockPisoVenta) || 0) > 0),
    [productos]
  );
  const categorias = (0, import_react.useMemo)(() => {
    const set = new Set(vendibles.map((p) => p.categoria).filter(Boolean));
    return ["Todos", ...[...set].sort()];
  }, [vendibles]);
  const visibles = (0, import_react.useMemo)(() => {
    const base = categoria === "Todos" ? vendibles : vendibles.filter((p) => p.categoria === categoria);
    const q = busqueda.trim().toLowerCase();
    if (!q) return base;
    return base.filter((p) => p.nombre.toLowerCase().includes(q) || (p.codigoBarras || "").toLowerCase().includes(q));
  }, [vendibles, categoria, busqueda]);
  function manejarEnterEscaneo(e) {
    if (e.key !== "Enter") return;
    const q = busqueda.trim().toLowerCase();
    if (!q) return;
    const exacto = vendibles.find((p) => (p.codigoBarras || "").toLowerCase() === q);
    if (exacto) {
      agregar(exacto);
      setBusqueda("");
      setAvisoEscaneo(`\u2713 ${exacto.nombre}`);
      setTimeout(() => setAvisoEscaneo(""), 1500);
    } else if (visibles.length === 1) {
      agregar(visibles[0]);
      setBusqueda("");
      setAvisoEscaneo(`\u2713 ${visibles[0].nombre}`);
      setTimeout(() => setAvisoEscaneo(""), 1500);
    }
  }
  function agregar(producto) {
    setCarrito((s) => {
      const existe = s.find((l) => l.productoId === producto.id);
      if (existe) return s.map((l) => l.productoId === producto.id ? { ...l, cantidad: (Number(l.cantidad) || 0) + 1 } : l);
      return [...s, { productoId: producto.id, cantidad: 1 }];
    });
  }
  function cambiarCantidad(productoId, delta) {
    setCarrito(
      (s) => s.map((l) => l.productoId === productoId ? { ...l, cantidad: Math.max(1, (Number(l.cantidad) || 0) + delta) } : l).filter((l) => l.cantidad > 0)
    );
  }
  function fijarCantidad(productoId, valor) {
    setCarrito((s) => s.map((l) => l.productoId === productoId ? { ...l, cantidad: valor } : l));
  }
  function normalizarCantidad(productoId) {
    setCarrito(
      (s) => s.map((l) => {
        if (l.productoId !== productoId) return l;
        const n = Number(l.cantidad);
        return { ...l, cantidad: !n || n <= 0 ? 1 : n };
      })
    );
  }
  function quitar(productoId) {
    setCarrito((s) => s.filter((l) => l.productoId !== productoId));
  }
  function vaciarCarrito() {
    setCarrito([]);
  }
  const lineasCarrito = carrito.map((l) => {
    const p = productos.find((x) => x.id === l.productoId);
    if (!p) return null;
    const precioConIva = precioNeto(p) * (1 + ivaDe(p) / 100);
    const cant = Number(l.cantidad) || 0;
    return { ...l, cantidadNum: cant, producto: p, precioUnitario: precioConIva, subtotal: precioConIva * cant };
  }).filter(Boolean);
  const total = lineasCarrito.reduce((a, l) => a + l.subtotal, 0);
  const faltaStock = lineasCarrito.filter((l) => l.cantidadNum > (Number(l.producto.stockPisoVenta) || 0));
  const restoEfectivoMixto = medioPago === "Mixto" ? Math.max(0, total - (Number(importeTarjetaMixto) || 0)) : null;
  const baseParaCambio = medioPago === "Mixto" ? restoEfectivoMixto : total;
  const cambio = (medioPago === "Efectivo" || medioPago === "Mixto") && efectivoRecibido !== "" ? Number(efectivoRecibido) - baseParaCambio : null;
  function abrirCobro() {
    if (lineasCarrito.length === 0) return;
    setMedioPago("Efectivo");
    setEfectivoRecibido("");
    setImporteTarjetaMixto("");
    setErrorVenta("");
    setShowCobro(true);
  }
  async function confirmarCobro() {
    const detallePago = medioPago === "Mixto" ? { tarjeta: Number(importeTarjetaMixto) || 0, efectivo: restoEfectivoMixto } : null;
    setErrorVenta("");
    setEnviandoVenta(true);
    const resultado = await venderCarrito(
      carrito.map((l) => ({ productoId: l.productoId, cantidad: l.cantidad })),
      medioPago,
      detallePago
    );
    setEnviandoVenta(false);
    if (!resultado || resultado.ok === false) {
      const prod = resultado && resultado.productoId ? productos.find((p) => p.id === resultado.productoId) : null;
      setErrorVenta(
        resultado && resultado.error === "Stock insuficiente" ? `Sin stock suficiente de "${prod ? prod.nombre : "ese producto"}" \u2014 quedan ${fmt(resultado.disponible)}. No se ha cobrado nada, ajusta la cantidad.` : resultado && resultado.error || "No se pudo completar la venta. Int\xE9ntalo de nuevo."
      );
      return;
    }
    setConfirmacion({ total, n: resultado.n, medioPago, cambio, detallePago });
    setCarrito([]);
    setShowCobro(false);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-3" }, /* @__PURE__ */ import_react.default.createElement("h2", { className: "text-[16px] font-semibold" }, "Venta r\xE1pida"), carrito.length > 0 && /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: vaciarCarrito }, "Vaciar carrito")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, "Esto registra la venta y descuenta el stock para tu control interno \u2014 ", /* @__PURE__ */ import_react.default.createElement("b", null, "no emite ning\xFAn tique fiscal"), ". Para lo que le das al cliente, sigue usando tu caja o TPV habitual.")), vendibles.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "No hay nada en el piso de venta ahora mismo. Ponle precio a un producto en Productos, o haz un traspaso desde el almac\xE9n en la pesta\xF1a Traspasos." }) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "relative mb-3" }, /* @__PURE__ */ import_react.default.createElement(
    "input",
    {
      ref: inputEscaneoRef,
      type: "text",
      inputMode: "search",
      value: busqueda,
      onChange: (e) => setBusqueda(e.target.value),
      onKeyDown: manejarEnterEscaneo,
      placeholder: "Escanea un c\xF3digo de barras o busca por nombre\u2026",
      autoFocus: true,
      className: "w-full rounded-xl px-4 py-3 text-[14px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    }
  ), avisoEscaneo && /* @__PURE__ */ import_react.default.createElement(
    "div",
    {
      className: "absolute right-3 top-1/2 -translate-y-1/2 text-[12.5px] font-semibold px-2 py-1 rounded-md",
      style: { background: C.accentSoft, color: C.accent }
    },
    avisoEscaneo
  ), busqueda && !avisoEscaneo && /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      onClick: () => {
        setBusqueda("");
        inputEscaneoRef.current?.focus();
      },
      "aria-label": "Borrar b\xFAsqueda",
      className: "absolute right-3 top-1/2 -translate-y-1/2"
    },
    /* @__PURE__ */ import_react.default.createElement(X, { size: 16, color: C.inkSoft })
  )), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-3 overflow-x-auto" }, categorias.map((c) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: c,
      onClick: () => setCategoria(c),
      className: "text-[12px] font-medium px-3 py-1.5 rounded-full whitespace-nowrap shrink-0",
      style: {
        background: categoria === c ? C.chrome : C.surface,
        color: categoria === c ? "#fff" : C.inkSoft,
        border: `1px solid ${categoria === c ? C.chrome : C.line}`
      }
    },
    c
  ))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 sm:grid-cols-3 gap-2 mb-4" }, visibles.map((p) => {
    const precioConIva = precioNeto(p) * (1 + ivaDe(p) / 100);
    const enCarrito = carrito.find((l) => l.productoId === p.id);
    const sinStock = (Number(p.stockPisoVenta) || 0) <= 0;
    return /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: p.id,
        onClick: () => agregar(p),
        className: "rounded-xl p-3 text-left relative",
        style: { background: C.surface, border: `1px solid ${enCarrito ? C.accent : C.line}` }
      },
      /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-medium leading-tight mb-1" }, p.nombre),
      /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[14px] font-bold mono", style: { color: C.accent } }, "\u20AC", fmt(precioConIva)),
      sinStock && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10px] mt-0.5", style: { color: C.red } }, "sin stock"),
      enCarrito && /* @__PURE__ */ import_react.default.createElement(
        "span",
        {
          className: "absolute -top-2 -right-2 rounded-full text-[11px] font-bold flex items-center justify-center",
          style: { background: C.accent, color: "#fff", width: 22, height: 22 }
        },
        fmt(Number(enCarrito.cantidad) || 0)
      )
    );
  }))), lineasCarrito.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-2" }, "Carrito"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5 mb-3" }, lineasCarrito.map((l) => /* @__PURE__ */ import_react.default.createElement("div", { key: l.productoId, className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "flex-1" }, l.producto.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1.5" }, /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => cambiarCantidad(l.productoId, -1), className: "rounded-md p-1", style: { border: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement(Minus, { size: 12 })), /* @__PURE__ */ import_react.default.createElement(
    "input",
    {
      type: "number",
      inputMode: "decimal",
      step: "any",
      min: "0",
      value: l.cantidad,
      onChange: (e) => fijarCantidad(l.productoId, e.target.value),
      onBlur: () => normalizarCantidad(l.productoId),
      onFocus: (e) => e.target.select(),
      className: "mono text-center rounded-md py-1",
      style: { width: 54, border: `1px solid ${C.line}`, background: C.surface, color: C.ink, fontSize: 13 }
    }
  ), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => cambiarCantidad(l.productoId, 1), className: "rounded-md p-1", style: { border: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 12 })), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold w-16 text-right" }, "\u20AC", fmt(l.subtotal)), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => quitar(l.productoId), "aria-label": "Quitar producto" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 14, color: C.inkSoft })))))), faltaStock.length > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-2", style: { color: C.red } }, "Vas a dejar en negativo: ", faltaStock.map((l) => l.producto.nombre).join(", "), "."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between pt-2", style: { borderTop: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("span", { className: "font-semibold" }, "Total"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-bold text-[19px]", style: { color: C.accent } }, "\u20AC", fmt(total)))), lineasCarrito.length > 0 && /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: abrirCobro }, "Cobrar \u20AC", fmt(total)), showCobro && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowCobro(false), title: "Cobrar" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-center mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, "Total"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[28px] font-bold mono", style: { color: C.accent } }, "\u20AC", fmt(total))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Medio de pago" }, /* @__PURE__ */ import_react.default.createElement("select", { value: medioPago, onChange: (e) => setMedioPago(e.target.value), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", null, "Efectivo"), /* @__PURE__ */ import_react.default.createElement("option", null, "Tarjeta"), /* @__PURE__ */ import_react.default.createElement("option", null, "Mixto"), /* @__PURE__ */ import_react.default.createElement("option", null, "Transferencia"), /* @__PURE__ */ import_react.default.createElement("option", null, "Otro"))), medioPago === "Efectivo" && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Efectivo entregado por el cliente (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: efectivoRecibido, onChange: (e) => setEfectivoRecibido(e.target.value), autoFocus: true })), cambio !== null && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] mb-3", style: { color: cambio < 0 ? C.red : C.accent } }, cambio < 0 ? `Faltan \u20AC${fmt(Math.abs(cambio))}` : `Cambio a devolver: \u20AC${fmt(cambio)}`)), medioPago === "Mixto" && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Paga con tarjeta (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", min: "0", max: total, value: importeTarjetaMixto, onChange: (e) => setImporteTarjetaMixto(e.target.value), autoFocus: true })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3", style: { color: C.inkSoft } }, "Resto en efectivo: ", /* @__PURE__ */ import_react.default.createElement("b", { className: "mono", style: { color: C.ink } }, "\u20AC", fmt(restoEfectivoMixto))), restoEfectivoMixto > 0 && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Efectivo entregado por el cliente (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: efectivoRecibido, onChange: (e) => setEfectivoRecibido(e.target.value) })), cambio !== null && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] mb-3", style: { color: cambio < 0 ? C.red : C.accent } }, cambio < 0 ? `Faltan \u20AC${fmt(Math.abs(cambio))}` : `Cambio a devolver: \u20AC${fmt(cambio)}`))), errorVenta && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3 p-2 rounded-lg", style: { background: "#FCE8E6", color: C.red } }, "\u26A0 ", errorVenta), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(
    Btn,
    {
      onClick: confirmarCobro,
      disabled: enviandoVenta || medioPago === "Efectivo" && cambio !== null && cambio < 0 || medioPago === "Mixto" && (Number(importeTarjetaMixto) < 0 || Number(importeTarjetaMixto) > total || restoEfectivoMixto > 0 && cambio !== null && cambio < 0)
    },
    enviandoVenta ? "Cobrando\u2026" : "Confirmar venta"
  ), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowCobro(false), disabled: enviandoVenta }, "Cancelar"))), confirmacion && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmacion(null), title: "Venta registrada" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] mb-2" }, "\u20AC", fmt(confirmacion.total), " \xB7 ", confirmacion.medioPago, " \xB7 ", confirmacion.n, " l\xEDnea(s)"), confirmacion.detallePago && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2", style: { color: C.inkSoft } }, "Tarjeta \u20AC", fmt(confirmacion.detallePago.tarjeta), " + Efectivo \u20AC", fmt(confirmacion.detallePago.efectivo)), confirmacion.cambio > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] mb-3", style: { color: C.accent } }, "Entrega \u20AC", fmt(confirmacion.cambio), " de cambio."), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, "Ya se ha descontado el stock y entra en Resultados. Recuerda: esto no sustituye el tique que le des al cliente."), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => setConfirmacion(null) }, "Aceptar")), ventasDeHoy.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mt-5" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-1" }, "Ventas de hoy"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-3", style: { color: C.inkSoft } }, "Si te has equivocado al cobrar, puedes anular una venta. El stock vuelve y queda constancia de la anulaci\xF3n \u2014 el hist\xF3rico no se borra."), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, ventasDeHoy.map((v) => /* @__PURE__ */ import_react.default.createElement("div", { key: v.ventaId, className: "flex items-center justify-between py-1.5", style: { borderBottom: `1px solid ${C.line}` } }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, v.anulada ? /* @__PURE__ */ import_react.default.createElement("s", null, v.resumen) : v.resumen, v.anulada && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.red }, "anulada")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, v.medioPago, v.usuario ? ` \xB7 ${v.usuario}` : "")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "mono text-[12.5px]" }, "\u20AC", fmt(v.importe)), !v.anulada && /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => setConfirmAnular(v) }, "Anular")))))), confirmAnular && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmAnular(null), title: "Anular esta venta" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3" }, /* @__PURE__ */ import_react.default.createElement("b", null, confirmAnular.resumen), " \xB7 \u20AC", fmt(confirmAnular.importe)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.inkSoft } }, "El stock de esos productos volver\xE1 al piso de venta, y quedar\xE1 registrada la anulaci\xF3n. La venta original no se borra: se ve que existi\xF3 y que se anul\xF3."), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Motivo (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: motivoAnular, onChange: (e) => setMotivoAnular(e.target.value), placeholder: "Cobro duplicado, importe incorrecto\u2026" })), errorAnular && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, errorAnular), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mt-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: confirmarAnulacion, disabled: procesandoAnulacion }, procesandoAnulacion ? "Anulando\u2026" : "S\xED, anular la venta"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmAnular(null), disabled: procesandoAnulacion }, "Cancelar"))));
}
function Traspasos({ productos, traspasos, traspasarStock, pisoVentaBajo, fichasCosto = [] }) {
  const [verTodos, setVerTodos] = (0, import_react.useState)(false);
  const vendibles = (0, import_react.useMemo)(() => {
    if (verTodos) return productos;
    const idsDeFichas = new Set(fichasCosto.map((f) => f.productoVinculadoId).filter(Boolean));
    return productos.filter(
      (p) => p.tipo === "elaborado" || Number(p.precioVenta) > 0 || idsDeFichas.has(p.id) || Number(p.stockPisoVenta) > 0
    );
  }, [productos, fichasCosto, verTodos]);
  const [productoId, setProductoId] = (0, import_react.useState)(vendibles[0]?.id || "");
  const [cantidad, setCantidad] = (0, import_react.useState)("");
  const [direccion, setDireccion] = (0, import_react.useState)("a_piso");
  const [error, setError] = (0, import_react.useState)("");
  const [ok, setOk] = (0, import_react.useState)("");
  (0, import_react.useEffect)(() => {
    if (vendibles.length > 0 && !vendibles.some((p) => p.id === productoId)) {
      setProductoId(vendibles[0].id);
    }
  }, [vendibles, productoId]);
  const producto = productos.find((p) => p.id === productoId);
  const enPiso = producto ? Number(producto.stockPisoVenta) || 0 : 0;
  const enAlmacen = producto ? (Number(producto.stock) || 0) - enPiso : 0;
  function submit() {
    const res = traspasarStock(productoId, cantidad, direccion);
    if (!res.ok) {
      setError(res.error);
      setOk("");
      return;
    }
    setError("");
    setOk(`Traspasado ${fmt(Number(cantidad))} ${producto.unidad} ${direccion === "a_piso" ? "al piso de venta" : "al almac\xE9n"}.`);
    setCantidad("");
    setTimeout(() => setOk(""), 3e3);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Traspasos"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Mueve stock entre el almac\xE9n de atr\xE1s y el piso de venta. Esto no cambia cu\xE1nto tienes en total \u2014 solo d\xF3nde est\xE1. Lo que llega por albar\xE1n o sale de producci\xF3n entra al almac\xE9n; desde aqu\xED lo pasas al mostrador cuando lo repones.")), pisoVentaBajo.length > 0 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold mb-2" }, "Bajo m\xEDnimo en el piso de venta"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1" }, pisoVentaBajo.map((p) => /* @__PURE__ */ import_react.default.createElement("div", { key: p.id, className: "flex items-center justify-between text-[12px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, p.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono" }, fmt(p.stockPisoVenta || 0), " / m\xEDn. ", fmt(p.stockMinimoPisoVenta)))))), vendibles.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no tienes productos de venta. M\xE1rcalos como 'Elaborado' o ponles precio en Productos." }) : /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Producto" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: productoId,
      onChange: (e) => {
        setProductoId(e.target.value);
        setError("");
        setOk("");
      },
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    },
    vendibles.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre))
  )), /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      type: "button",
      onClick: () => setVerTodos((s) => !s),
      className: "text-[11.5px] font-medium mb-3",
      style: { color: C.accent }
    },
    verTodos ? "Ver solo los productos de venta" : "No encuentro el producto, ver todos"
  ), producto && /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3 mb-3" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "En almac\xE9n"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono" }, fmt(enAlmacen), " ", producto.unidad)), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "En piso de venta"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono" }, fmt(enPiso), " ", producto.unidad))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Direcci\xF3n" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: direccion,
      onChange: (e) => setDireccion(e.target.value),
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "a_piso" }, "Almac\xE9n \u2192 Piso de venta"),
    /* @__PURE__ */ import_react.default.createElement("option", { value: "a_almacen" }, "Piso de venta \u2192 Almac\xE9n")
  )), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Cantidad" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: cantidad, onChange: (e) => setCantidad(e.target.value) })), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), ok && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.accent } }, ok), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, "Traspasar")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "Historial de traspasos"), traspasos.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Todav\xEDa no has hecho ning\xFAn traspaso." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, traspasos.slice(0, 50).map((t) => /* @__PURE__ */ import_react.default.createElement(Card, { key: t.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, t.nombre), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono", style: { color: t.direccion === "a_piso" ? C.accent : C.inkSoft } }, t.direccion === "a_piso" ? "\u2192 Piso" : "\u2192 Almac\xE9n", " \xB7 ", fmt(t.cantidad))), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-0.5", style: { color: C.inkSoft } }, t.fecha, " ", t.hora)))));
}
function PanelDireccion({ movimientos, gastosGenerales, nominas, empleados, promedioDiarioVentas, proyeccionTesoreria, stockBajo = [], descuadresStock = [] }) {
  const hoy = todayISO();
  const mesActual = hoy.slice(0, 7);
  const [anio, mes] = mesActual.split("-").map(Number);
  const diaDelMes = (/* @__PURE__ */ new Date()).getDate();
  const diasEnMes = new Date(anio, mes, 0).getDate();
  function resumenDe(movsDelPeriodo) {
    const ventas = movsDelPeriodo.filter((m) => esVenta(m));
    const ingresos = ventas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.ingresoUnitario) || 0) * (1 + (Number(m.ivaVentaAplicado) || 0) / 100), 0);
    const coste = ventas.reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0), 0);
    const tickets = new Set(ventas.map((m) => m.operationId || m.ventaId || m.id)).size;
    const compras = movsDelPeriodo.filter((m) => m.tipo === "COMPRA" && Number(m.cantidad) > 0).reduce((a, m) => a + Number(m.cantidad) * (Number(m.costoUnitario) || 0), 0);
    const merma = movsDelPeriodo.filter((m) => esMerma(m)).reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.costoUnitario) || 0), 0);
    return { ingresos, coste, margen: ingresos - coste, tickets, compras, merma };
  }
  const movHoy = movimientos.filter((m) => m.fecha === hoy);
  const movMes = movimientos.filter((m) => (m.fecha || "").slice(0, 7) === mesActual);
  const rHoy = resumenDe(movHoy);
  const rMes = resumenDe(movMes);
  const nominasDelMes = nominas.filter((n) => n.mes === mesActual);
  const costePersonalMes = nominasDelMes.reduce((a, n) => a + (Number(n.costeTotalEmpresa) || 0), 0);
  const porcentajePersonal = rMes.ingresos > 0 ? costePersonalMes / rMes.ingresos * 100 : null;
  const gastosFijosMes = gastosGenerales.filter((g) => !g.fecha).reduce((a, g) => a + (Number(g.importe) || 0), 0) * (diasEnMes / 30);
  const gastosPuntualesMes = gastosGenerales.filter((g) => g.fecha && (g.fecha || "").slice(0, 7) === mesActual).reduce((a, g) => a + (Number(g.importe) || 0), 0);
  const beneficioMes = rMes.margen - costePersonalMes - gastosFijosMes - gastosPuntualesMes;
  const factorExtrapolacion = diaDelMes > 0 ? diasEnMes / diaDelMes : 1;
  const ventasFinMes = rMes.ingresos * factorExtrapolacion;
  const resultadoEstimadoFinMes = beneficioMes * factorExtrapolacion;
  const cajaFutura30d = (proyeccionTesoreria || []).reduce((a, d) => a + d.netoDia, 0);
  const Bloque2 = ({ titulo, children }) => /* @__PURE__ */ import_react.default.createElement("div", { className: "mb-5" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-semibold mb-2", style: { color: C.inkSoft } }, titulo), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-2" }, children));
  const Dato = ({ label, valor, color, nota }) => /* @__PURE__ */ import_react.default.createElement(Card, { className: "py-2.5" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, label), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[16px] font-semibold mono mt-0.5", style: { color: color || C.ink } }, valor), nota && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10px] mt-0.5", style: { color: C.inkSoft } }, nota));
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Panel de direcci\xF3n"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "C\xF3mo va el negocio, sin entrar en diez pantallas distintas \u2014 reutiliza los mismos n\xFAmeros que ya ves en Resultados, Tesorer\xEDa y Coste de Personal.")), /* @__PURE__ */ import_react.default.createElement(Bloque2, { titulo: "HOY" }, /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Ventas", valor: `\u20AC${fmt(rHoy.ingresos)}` }), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Tickets", valor: rHoy.tickets, nota: rHoy.tickets > 0 ? `ticket medio \u20AC${fmt(rHoy.ingresos / rHoy.tickets)}` : null }), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Margen", valor: `\u20AC${fmt(rHoy.margen)}`, color: C.accent }), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Stock cr\xEDtico", valor: stockBajo.length, color: stockBajo.length ? C.red : C.ink })), /* @__PURE__ */ import_react.default.createElement(Bloque2, { titulo: "ESTE MES" }, /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Ventas del mes", valor: `\u20AC${fmt(rMes.ingresos)}` }), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Margen del mes", valor: `\u20AC${fmt(rMes.margen)}`, color: C.accent }), /* @__PURE__ */ import_react.default.createElement(
    Dato,
    {
      label: "Personal sobre ventas",
      valor: porcentajePersonal === null ? "\u2014" : `${fmt(porcentajePersonal)}%`,
      color: porcentajePersonal === null ? C.ink : porcentajePersonal <= 35 ? C.accent : porcentajePersonal <= 40 ? C.amber : C.red
    }
  ), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Compras del mes", valor: `\u20AC${fmt(rMes.compras)}` }), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Merma del mes", valor: `\u20AC${fmt(rMes.merma)}`, color: rMes.merma > 0 ? C.amber : C.ink }), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Beneficio estimado", valor: `\u20AC${fmt(beneficioMes)}`, color: beneficioMes >= 0 ? C.accent : C.red, nota: "ventas \u2212 coste \u2212 personal \u2212 gastos" })), /* @__PURE__ */ import_react.default.createElement(Bloque2, { titulo: "PREVISI\xD3N (estimaci\xF3n, no un hecho)" }, /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Ventas fin de mes", valor: `\u20AC${fmt(ventasFinMes)}`, nota: `llevas ${diaDelMes} de ${diasEnMes} d\xEDas` }), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Resultado estimado fin de mes", valor: `\u20AC${fmt(resultadoEstimadoFinMes)}`, color: resultadoEstimadoFinMes >= 0 ? C.accent : C.red }), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Caja a 30 d\xEDas", valor: `${cajaFutura30d >= 0 ? "+" : ""}\u20AC${fmt(cajaFutura30d)}`, color: cajaFutura30d >= 0 ? C.accent : C.red, nota: `\u20AC${fmt(promedioDiarioVentas)}/d\xEDa de media` }), /* @__PURE__ */ import_react.default.createElement(Dato, { label: "Descuadres de stock sin resolver", valor: descuadresStock.length, color: descuadresStock.length ? C.red : C.ink })));
}
function Tesoreria({ proyeccionTesoreria, promedioDiarioVentas }) {
  const [saldoActual, setSaldoActual] = (0, import_react.useState)("");
  const saldoInicial = Number(saldoActual) || 0;
  let acumulado = saldoInicial;
  const filas = proyeccionTesoreria.map((d) => {
    acumulado += d.netoDia;
    return { ...d, saldoProyectado: acumulado };
  });
  const primerNegativo = filas.find((f) => f.saldoProyectado < 0);
  const totalPagos30d = proyeccionTesoreria.reduce((a, d) => a + d.pagosDia, 0);
  const totalIngresos30d = proyeccionTesoreria.reduce((a, d) => a + d.ingresosDia, 0);
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Tesorer\xEDa"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Proyecci\xF3n a 30 d\xEDas. Los pagos son reales (facturas con vencimiento conocido) y los encargos con fecha de entrega fijada tambi\xE9n. La venta del d\xEDa a d\xEDa se estima con tu promedio de los \xFAltimos 30 d\xEDas (\u20AC", fmt(promedioDiarioVentas), "/d\xEDa) \u2014 es una estimaci\xF3n, no una promesa, y mejora cuantas m\xE1s ventas registres.")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Saldo disponible hoy (caja + banco)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: saldoActual, onChange: (e) => setSaldoActual(e.target.value), placeholder: "Lo que tienes ahora mismo" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "El programa no sabe tu saldo bancario \u2014 lo pones t\xFA aqu\xED para poder proyectar hacia delante.")), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Ingresos previstos (30 d\xEDas)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1", style: { color: C.accent } }, "\u20AC", fmt(totalIngresos30d))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Pagos comprometidos (30 d\xEDas)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, "\u20AC", fmt(totalPagos30d)))), saldoActual !== "" && primerNegativo && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.redSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] font-semibold" }, "Posible falta de caja el ", new Date(primerNegativo.fecha).toLocaleDateString("es-ES")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mt-1", style: { color: C.inkSoft } }, "Al ritmo actual, ese d\xEDa el saldo proyectado bajar\xEDa de cero. Revisa si puedes retrasar alg\xFAn pago o adelantar alg\xFAn cobro antes de esa fecha.")), saldoActual === "" ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Pon tu saldo actual arriba para ver la proyecci\xF3n d\xEDa a d\xEDa." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1" }, filas.map((f) => /* @__PURE__ */ import_react.default.createElement(Card, { key: f.fecha, style: { padding: "0.6rem 1rem" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("span", null, new Date(f.fecha).toLocaleDateString("es-ES", { weekday: "short", day: "2-digit", month: "2-digit" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-3" }, f.pagosDia > 0 && /* @__PURE__ */ import_react.default.createElement("span", { className: "mono text-[11.5px]", style: { color: C.red } }, "\u2212\u20AC", fmt(f.pagosDia)), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono font-semibold", style: { color: f.saldoProyectado < 0 ? C.red : C.ink } }, "\u20AC", fmt(f.saldoProyectado))))))));
}
var MESES_NOMBRE = ["enero", "febrero", "marzo", "abril", "mayo", "junio", "julio", "agosto", "septiembre", "octubre", "noviembre", "diciembre"];
function Estacionalidad({ ingresosPorMes }) {
  const hoy = /* @__PURE__ */ new Date();
  const filas = [];
  for (let i = 0; i < 12; i++) {
    const d = new Date(hoy.getFullYear(), hoy.getMonth() - i, 1);
    const key = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}`;
    const keyAnterior = `${d.getFullYear() - 1}-${String(d.getMonth() + 1).padStart(2, "0")}`;
    const actual = ingresosPorMes[key] || 0;
    const anterior = ingresosPorMes[keyAnterior];
    const variacion = anterior ? (actual - anterior) / anterior * 100 : null;
    filas.push({
      key,
      etiqueta: `${MESES_NOMBRE[d.getMonth()]} ${d.getFullYear()}`,
      actual,
      anterior: anterior ?? null,
      variacion
    });
  }
  filas.reverse();
  const hayComparacion = filas.some((f) => f.anterior !== null);
  const mesesConDatos = filas.filter((f) => f.actual > 0).length;
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Estacionalidad"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.accentSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Compara cada mes contra el mismo mes del a\xF1o anterior \u2014 para ver si San Valent\xEDn, Navidad o el verano se comportan distinto de un a\xF1o a otro, en vez de comparar solo contra el mes justo antes.")), !hayComparacion && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "Todav\xEDa no tienes un a\xF1o completo de ventas registradas, as\xED que no hay ning\xFAn mes con el que comparar. En cuanto pase un a\xF1o desde que empezaste a registrar ventas, aqu\xED aparecer\xE1 la comparaci\xF3n real.", mesesConDatos > 0 && ` Por ahora tienes ${mesesConDatos} mes(es) con datos.`)), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, filas.map((f) => /* @__PURE__ */ import_react.default.createElement(Card, { key: f.key }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[13px] font-medium capitalize" }, f.etiqueta), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, "\u20AC", fmt(f.actual)), f.anterior !== null ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mono", style: { color: f.variacion >= 0 ? C.accent : C.red } }, f.variacion >= 0 ? "+" : "", fmt(f.variacion), "% vs. \u20AC", fmt(f.anterior), " el a\xF1o pasado") : /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Sin dato del a\xF1o anterior")))))));
}
function CostePersonal({ empleados, nominas, addNomina, updateNomina, deleteNomina, fichajes, movimientos }) {
  const mesActual = aISO(/* @__PURE__ */ new Date()).slice(0, 7);
  const [mes, setMes] = (0, import_react.useState)(mesActual);
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [editingId, setEditingId] = (0, import_react.useState)(null);
  const [form, setForm] = (0, import_react.useState)({ empleadoId: "", mes: mesActual, brutoTotal: "", seguridadSocialEmpresa: "", notas: "" });
  const [error, setError] = (0, import_react.useState)("");
  const [mostrarIA, setMostrarIA] = (0, import_react.useState)(false);
  const [fotosIA, setFotosIA] = (0, import_react.useState)([]);
  const [cargandoIA, setCargandoIA] = (0, import_react.useState)(false);
  const [errorIA, setErrorIA] = (0, import_react.useState)("");
  const [avisosIA, setAvisosIA] = (0, import_react.useState)([]);
  const activos = empleados.filter((e) => e.activo !== false);
  const nominasDelMes = nominas.filter((n) => n.mes === mes);
  const costeTotalMes = nominasDelMes.reduce((a, n) => a + (Number(n.costeTotalEmpresa) || 0), 0);
  const ingresosDelMes = movimientos.filter((m) => esVenta(m) && (m.fecha || "").slice(0, 7) === mes).reduce((a, m) => a + Math.abs(Number(m.cantidad) || 0) * (Number(m.ingresoUnitario) || 0), 0);
  const porcentajeSobreVentas = ingresosDelMes > 0 ? costeTotalMes / ingresosDelMes * 100 : null;
  function horasDelEmpleadoEnMes(empleadoId) {
    return horasDeEmpleadoEnMes(fichajes, empleadoId, mes);
  }
  const filas = nominasDelMes.map((n) => {
    const emp = empleados.find((e) => e.id === n.empleadoId);
    const horas = horasDelEmpleadoEnMes(n.empleadoId);
    const costePorHora = horas > 0 ? (Number(n.costeTotalEmpresa) || 0) / horas : null;
    return { ...n, empleado: emp, horas, costePorHora };
  }).sort((a, b) => (b.costeTotalEmpresa || 0) - (a.costeTotalEmpresa || 0));
  const LIMITE_COSTE_PERSONAL = 0.35;
  const ventaObjetivo35 = costeTotalMes > 0 ? costeTotalMes / LIMITE_COSTE_PERSONAL : null;
  const diferenciaConObjetivo = ventaObjetivo35 !== null ? ingresosDelMes - ventaObjetivo35 : null;
  const horasTotalesMes = filas.reduce((a, f) => a + (f.horas || 0), 0);
  const costePorHoraEquipo = horasTotalesMes > 0 ? costeTotalMes / horasTotalesMes : null;
  const ventasPorHoraEquipo = horasTotalesMes > 0 ? ingresosDelMes / horasTotalesMes : null;
  async function agregarFotosIA(archivos) {
    setErrorIA("");
    const validos = Array.from(archivos).filter((f) => f.type.startsWith("image/"));
    if (validos.length === 0) {
      setErrorIA("Solo se admiten fotos (jpg, png). Si es un PDF, hazle una foto de pantalla o de la hoja impresa.");
      return;
    }
    try {
      const nuevas = await Promise.all(validos.map(archivoABase64));
      setFotosIA((s) => [...s, ...nuevas]);
    } catch {
      setErrorIA("No se ha podido leer ese archivo. Prueba con otra foto.");
    }
  }
  function quitarFotoIA(idx) {
    setFotosIA((s) => s.filter((_, i) => i !== idx));
  }
  function empleadoPorNombreLeido(nombre) {
    if (!nombre) return null;
    const normaliza = (t) => t.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().trim();
    const buscado = normaliza(nombre);
    return activos.find((e) => normaliza(e.nombre) === buscado) || activos.find((e) => normaliza(e.nombre).includes(buscado) || buscado.includes(normaliza(e.nombre))) || null;
  }
  function numeroLimpio(v) {
    if (v === null || v === void 0 || v === "") return "";
    if (typeof v === "number" && !isNaN(v)) return v;
    let s = String(v).trim().replace(/[€\s]/g, "");
    if (/,/.test(s) && /\./.test(s)) s = s.replace(/\./g, "").replace(",", ".");
    else if (/,/.test(s)) s = s.replace(",", ".");
    const n = parseFloat(s);
    return isNaN(n) ? "" : n;
  }
  function mesLimpio(v) {
    if (!v) return null;
    const m = String(v).match(/^(\d{4})-(\d{2})/);
    return m ? `${m[1]}-${m[2]}` : null;
  }
  async function importarNominaConIA() {
    if (fotosIA.length === 0) {
      setErrorIA("Sube al menos una foto de la n\xF3mina.");
      return;
    }
    setCargandoIA(true);
    setErrorIA("");
    setAvisosIA([]);
    try {
      const resp = await fetch(
        "https://flqercbgpgmmfaakrwkc.supabase.co/functions/v1/importar-nomina",
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ imagenes: fotosIA.map((f) => ({ base64: f.base64, mediaType: f.mediaType })) })
        }
      );
      const r = await resp.json();
      if (!r.ok) {
        setErrorIA(r.error || "No se ha podido leer correctamente esta n\xF3mina. Rev\xEDsala antes de continuar.");
        setCargandoIA(false);
        return;
      }
      const d = r.datos || {};
      const empMatch = empleadoPorNombreLeido(d.empleado);
      const mesSaneado = mesLimpio(d.mes);
      const avisos = [];
      if (!empMatch) avisos.push(`No se ha podido emparejar el nombre le\xEDdo ("${d.empleado || "\u2014"}") con ning\xFAn empleado dado de alta. El\xEDgelo a mano abajo.`);
      if (!d.mes) avisos.push("No se ha podido leer el mes de la n\xF3mina. Rev\xEDsalo antes de guardar.");
      else if (!mesSaneado) avisos.push(`El mes le\xEDdo ("${d.mes}") no tiene un formato reconocible. Se ha dejado el mes actual \u2014 rev\xEDsalo antes de guardar.`);
      setForm({
        empleadoId: empMatch ? empMatch.id : "",
        mes: mesSaneado || mes,
        brutoTotal: numeroLimpio(d.brutoTotal),
        seguridadSocialEmpresa: numeroLimpio(d.seguridadSocialEmpresa),
        notas: d.notas || ""
      });
      setEditingId(null);
      setAvisosIA(avisos);
      setMostrarIA(false);
      setFotosIA([]);
      setShowForm(true);
    } catch {
      setErrorIA("No se ha podido conectar con la IA. Comprueba tu conexi\xF3n e int\xE9ntalo otra vez.");
    } finally {
      setCargandoIA(false);
    }
  }
  function submit() {
    if (!form.empleadoId) {
      setError("Elige de qu\xE9 empleado es la n\xF3mina.");
      return;
    }
    if (!(Number(form.brutoTotal) > 0)) {
      setError("Pon el bruto total del mes.");
      return;
    }
    setError("");
    if (editingId) updateNomina(editingId, form);
    else addNomina(form);
    if (form.mes && form.mes !== mes) setMes(form.mes);
    setForm({ empleadoId: "", mes: form.mes || mes, brutoTotal: "", seguridadSocialEmpresa: "", notas: "" });
    setEditingId(null);
    setAvisosIA([]);
    setShowForm(false);
  }
  function abrirEdicion(n) {
    setForm({
      empleadoId: n.empleadoId || "",
      mes: n.mes || mes,
      brutoTotal: n.brutoTotal ?? "",
      seguridadSocialEmpresa: n.seguridadSocialEmpresa ?? "",
      notas: n.notas || ""
    });
    setEditingId(n.id);
    setError("");
    setAvisosIA([]);
    setShowForm(true);
  }
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setMostrarIA(true);
    setFotosIA([]);
    setErrorIA("");
  } }, /* @__PURE__ */ import_react.default.createElement(Camera, { size: 15 }), " Foto con IA"), /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: () => {
    setForm({ empleadoId: "", mes, brutoTotal: "", seguridadSocialEmpresa: "", notas: "" });
    setEditingId(null);
    setError("");
    setShowForm((s) => !s);
  } }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " Registrar n\xF3mina")) }, "Coste de personal"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("b", null, "Esto no calcula n\xF3minas"), " \u2014 eso lo hace tu gestor\xEDa, que es quien tiene la autorizaci\xF3n ante la Seguridad Social y responde legalmente. Aqu\xED solo apuntas lo que la gestor\xEDa ya te ha pasado, para cruzarlo con tus ventas reales y saber cu\xE1nto te cuesta de verdad el personal, en vez de una estimaci\xF3n.")), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Mes que est\xE1s viendo" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "month", value: mes, onChange: (e) => setMes(e.target.value) }))), mostrarIA && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => {
    if (!cargandoIA) setMostrarIA(false);
  }, title: "Leer n\xF3mina con foto (IA)", ancho: "max-w-2xl" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-3", style: { color: C.inkSoft } }, "Sube una foto de la n\xF3mina (o de la hoja de cotizaci\xF3n, si quieres que lea tambi\xE9n la Seguridad Social). La IA prepara el formulario para que lo revises \u2014 nada se guarda hasta que t\xFA confirmes."), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.amberSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, "Una n\xF3mina lleva datos personales y econ\xF3micos sensibles del trabajador. Revisa bien cada cifra antes de guardar \u2014 la IA puede confundir n\xFAmeros, sobre todo si la foto sale torcida o con poca luz.")), /* @__PURE__ */ import_react.default.createElement(
    "label",
    {
      className: "flex flex-col items-center justify-center gap-2 rounded-lg py-6 mb-3 cursor-pointer",
      style: { border: `2px dashed ${C.line}`, background: C.surface }
    },
    /* @__PURE__ */ import_react.default.createElement(Camera, { size: 22, color: C.inkSoft }),
    /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px]", style: { color: C.inkSoft } }, "Toca para elegir: hacer foto ahora o subir de la galer\xEDa"),
    /* @__PURE__ */ import_react.default.createElement(
      "input",
      {
        type: "file",
        accept: "image/*",
        multiple: true,
        disabled: cargandoIA,
        onChange: (e) => {
          agregarFotosIA(e.target.files);
          e.target.value = "";
        },
        className: "hidden"
      }
    )
  ), fotosIA.length > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "flex flex-wrap gap-2 mb-3" }, fotosIA.map((f, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i, className: "relative" }, /* @__PURE__ */ import_react.default.createElement("img", { src: f.previsualizacion, alt: "", className: "w-16 h-16 object-cover rounded-lg", style: { border: `1px solid ${C.line}` } }), !cargandoIA && /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      onClick: () => quitarFotoIA(i),
      "aria-label": "Quitar esta foto",
      className: "absolute -top-1.5 -right-1.5 rounded-full flex items-center justify-center",
      style: { width: 18, height: 18, background: C.red, color: "#fff" }
    },
    /* @__PURE__ */ import_react.default.createElement(X, { size: 12 })
  )))), errorIA && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, errorIA), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mb-3", style: { color: C.inkSoft } }, "Si alg\xFAn dato sale borroso o no se lee bien, la IA lo deja en blanco en vez de inventarlo \u2014 luego lo rellenas t\xFA en la revisi\xF3n."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: importarNominaConIA, disabled: cargandoIA }, cargandoIA ? "Leyendo la n\xF3mina\u2026" : "Leer con IA"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setMostrarIA(false), disabled: cargandoIA }, "Cancelar"))), showForm && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-semibold mb-3" }, editingId ? "Editar n\xF3mina" : "Registrar n\xF3mina del mes"), avisosIA.length > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "mb-3 px-3 py-2 rounded-lg text-[12px]", style: { background: C.amberSoft } }, avisosIA.map((a, i) => /* @__PURE__ */ import_react.default.createElement("div", { key: i }, a))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Empleado" }, /* @__PURE__ */ import_react.default.createElement("select", { value: form.empleadoId, onChange: (e) => setForm({ ...form, empleadoId: e.target.value }), className: "w-full rounded-lg px-3 py-2 text-[13px]", style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink } }, /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "\u2014 Elige un empleado \u2014"), activos.map((e) => /* @__PURE__ */ import_react.default.createElement("option", { key: e.id, value: e.id }, e.nombre)))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Mes" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "month", value: form.mes, onChange: (e) => setForm({ ...form, mes: e.target.value }) })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Bruto del mes (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.brutoTotal, onChange: (e) => setForm({ ...form, brutoTotal: e.target.value }), placeholder: "Lo que pone la n\xF3mina" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Seguridad Social a cargo de la empresa (\u20AC)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", step: "0.01", value: form.seguridadSocialEmpresa, onChange: (e) => setForm({ ...form, seguridadSocialEmpresa: e.target.value }), placeholder: "Del bolet\xEDn de cotizaci\xF3n" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Notas (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.notas, onChange: (e) => setForm({ ...form, notas: e.target.value }), placeholder: "Incluye paga extra, finiquito\u2026" }))), Number(form.brutoTotal) > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.inkSoft } }, "Coste total para la empresa: ", /* @__PURE__ */ import_react.default.createElement("b", { className: "mono" }, "\u20AC", fmt(Number(form.brutoTotal) + (Number(form.seguridadSocialEmpresa) || 0)))), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, editingId ? "Guardar cambios" : "Guardar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => {
    setShowForm(false);
    setEditingId(null);
    setAvisosIA([]);
  } }, "Cancelar"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Coste total del mes"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, "\u20AC", fmt(costeTotalMes))), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "% sobre ventas del mes"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1", style: { color: porcentajeSobreVentas === null ? C.inkSoft : porcentajeSobreVentas > 35 ? C.red : porcentajeSobreVentas > 30 ? C.amber : C.accent } }, porcentajeSobreVentas === null ? "\u2014" : `${fmt(porcentajeSobreVentas)}%`), porcentajeSobreVentas !== null && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-0.5", style: { color: C.inkSoft } }, "sobre \u20AC", fmt(ingresosDelMes), " vendidos"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-3 gap-3 mb-4" }, /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Venta objetivo (no pasar del 35%)"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, ventaObjetivo35 === null ? "\u2014" : `\u20AC${fmt(ventaObjetivo35)}`), diferenciaConObjetivo !== null && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-0.5", style: { color: diferenciaConObjetivo >= 0 ? C.accent : C.red } }, diferenciaConObjetivo >= 0 ? `ya llevas \u20AC${fmt(diferenciaConObjetivo)} de margen` : `te faltan \u20AC${fmt(Math.abs(diferenciaConObjetivo))} por vender`)), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Coste de personal por hora"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, costePorHoraEquipo === null ? "\u2014" : `\u20AC${fmt(costePorHoraEquipo)}`), horasTotalesMes > 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px] mt-0.5", style: { color: C.inkSoft } }, "sobre ", fmt(horasTotalesMes), " h fichadas")), /* @__PURE__ */ import_react.default.createElement(Card, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px]", style: { color: C.inkSoft } }, "Ventas por hora trabajada"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-xl font-semibold mono mt-1" }, ventasPorHoraEquipo === null ? "\u2014" : `\u20AC${fmt(ventasPorHoraEquipo)}`))), porcentajeSobreVentas !== null && porcentajeSobreVentas > 35 && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.redSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, "El personal se est\xE1 llevando m\xE1s del 35% de lo que vendes. En hosteler\xEDa suele considerarse una se\xF1al de alerta \u2014 puede ser un mes flojo de ventas, o que haga falta revisar turnos o plantilla.")), filas.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: `No has registrado n\xF3minas de ${mes} todav\xEDa.` }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-2" }, filas.map((n) => /* @__PURE__ */ import_react.default.createElement(Card, { key: n.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[13px] font-medium" }, n.empleado ? n.empleado.nombre : "Empleado eliminado"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, "Bruto \u20AC", fmt(n.brutoTotal), " + SS empresa \u20AC", fmt(n.seguridadSocialEmpresa)), n.notas && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-0.5", style: { color: C.inkSoft } }, n.notas)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-right" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, "\u20AC", fmt(n.costeTotalEmpresa)), n.costePorHora !== null && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mono", style: { color: C.inkSoft } }, "\u20AC", fmt(n.costePorHora), "/h \xB7 ", fmt(n.horas), " h fichadas"))), /* @__PURE__ */ import_react.default.createElement("div", { className: "mt-2 flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => abrirEdicion(n) }, /* @__PURE__ */ import_react.default.createElement(Pencil, { size: 13 }), " Editar"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => deleteNomina(n.id) }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 13 }), " Eliminar"))))));
}
function HistorialProducto({ productos, movimientos, pedidos, albaranes, traspasos, proveedorPorId }) {
  const [busqueda, setBusqueda] = (0, import_react.useState)("");
  const [productoId, setProductoId] = (0, import_react.useState)("");
  const [seccion, setSeccion] = (0, import_react.useState)("movimientos");
  const coincidencias = (0, import_react.useMemo)(() => {
    const q = busqueda.trim().toLowerCase();
    if (!q) return [];
    return productos.filter((p) => p.nombre.toLowerCase().includes(q) || (p.codigoBarras || "").toLowerCase().includes(q)).slice(0, 8);
  }, [productos, busqueda]);
  const producto = productos.find((p) => p.id === productoId);
  const movimientosDelProducto = (0, import_react.useMemo)(() => {
    if (!producto) return [];
    return movimientos.filter((m) => m.productoId === producto.id).sort((a, b) => (b.fecha || "").localeCompare(a.fecha || ""));
  }, [movimientos, producto]);
  const pedidosDelProducto = (0, import_react.useMemo)(() => {
    if (!producto) return [];
    return pedidos.filter((pe) => (pe.items || []).some((it) => it.productoId === producto.id)).map((pe) => ({ pedido: pe, item: pe.items.find((it) => it.productoId === producto.id) })).sort((a, b) => (b.pedido.fecha || "").localeCompare(a.pedido.fecha || ""));
  }, [pedidos, producto]);
  const comprasDelProducto = (0, import_react.useMemo)(() => {
    if (!producto) return [];
    return albaranes.filter((a) => a.estado === "confirmado" && (a.lineas || []).some((ln) => ln.productoId === producto.id)).map((a) => {
      const ln = a.lineas.find((x) => x.productoId === producto.id);
      const unidades = Number(ln.unidadesEntradas) || 0;
      const importe = Number(ln.importe) || 0;
      return { albaran: a, linea: ln, unidades, precioUnitario: unidades > 0 ? importe / unidades : 0 };
    }).sort((a, b) => (b.albaran.fecha || "").localeCompare(a.albaran.fecha || ""));
  }, [albaranes, producto]);
  const traspasosDelProducto = (0, import_react.useMemo)(() => {
    if (!producto) return [];
    return traspasos.filter((t) => t.productoId === producto.id).sort((a, b) => (b.fecha || "").localeCompare(a.fecha || ""));
  }, [traspasos, producto]);
  const SECCIONES = [
    { id: "movimientos", label: "Movimientos", n: movimientosDelProducto.length },
    { id: "pedidos", label: "Pedidos", n: pedidosDelProducto.length },
    { id: "compras", label: "Compras", n: comprasDelProducto.length },
    { id: "traspasos", label: "Traspasos", n: traspasosDelProducto.length }
  ];
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Historial de producto"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Busca un producto por nombre o c\xF3digo de barras" }, /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      value: busqueda,
      onChange: (e) => {
        setBusqueda(e.target.value);
        setProductoId("");
      },
      placeholder: "Ej. Harina, Bomb\xF3n surtido\u2026",
      autoFocus: true
    }
  )), busqueda && !producto && /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1 mt-1" }, coincidencias.length === 0 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]", style: { color: C.inkSoft } }, "Sin resultados.") : coincidencias.map((p) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: p.id,
      onClick: () => {
        setProductoId(p.id);
        setBusqueda(p.nombre);
      },
      className: "w-full text-left px-3 py-2 rounded-lg text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    },
    p.nombre,
    " ",
    /* @__PURE__ */ import_react.default.createElement("span", { style: { color: C.inkSoft } }, "\xB7 ", p.unidad)
  )))), !producto ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Busca y elige un producto para ver todo su historial." }) : /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold text-[15px]" }, producto.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, producto.categoria || "Sin categor\xEDa", " \xB7 ", producto.unidad)), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => {
    setProductoId("");
    setBusqueda("");
  }, "aria-label": "Quitar selecci\xF3n" }, /* @__PURE__ */ import_react.default.createElement(X, { size: 16, color: C.inkSoft }))), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-3 mt-2" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "Stock total"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, fmt(producto.stock), " ", producto.unidad)), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "En piso de venta"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, fmt(producto.stockPisoVenta || 0), " ", producto.unidad)), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "Costo"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, "\u20AC", fmt(producto.costo))), /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, "Precio de venta"), /* @__PURE__ */ import_react.default.createElement("div", { className: "mono font-semibold" }, producto.precioVenta ? `\u20AC${fmt(producto.precioVenta)}` : "\u2014")))), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mb-3 overflow-x-auto" }, SECCIONES.map((s) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: s.id,
      onClick: () => setSeccion(s.id),
      className: "text-[12px] font-medium px-3 py-1.5 rounded-full whitespace-nowrap",
      style: {
        background: seccion === s.id ? C.accent : C.surface,
        color: seccion === s.id ? "#fff" : C.inkSoft,
        border: `1px solid ${seccion === s.id ? C.accent : C.line}`
      }
    },
    s.label,
    " (",
    s.n,
    ")"
  ))), seccion === "movimientos" && (movimientosDelProducto.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin movimientos registrados para este producto." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, movimientosDelProducto.map((m) => {
    const cantidadNum = Number(m.cantidad) || 0;
    const efecto = m.tipo === "salida" ? -Math.abs(cantidadNum) : cantidadNum;
    const esPositivo = efecto >= 0;
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: m.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, /* @__PURE__ */ import_react.default.createElement("b", { style: { color: esPositivo ? C.accent : C.red } }, esPositivo ? "+" : "\u2212", fmt(Math.abs(efecto))), " ", m.motivo || m.referencia || "\u2014", m.tipo === "ajuste" && /* @__PURE__ */ import_react.default.createElement("span", { className: "ml-1 text-[10.5px]", style: { color: C.inkSoft } }, "(ajuste de inventario)")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, m.fecha, m.usuario ? ` \xB7 ${m.usuario}` : "", m.stockAnterior != null && m.stockPosterior != null ? ` \xB7 saldo ${fmt(m.stockAnterior)} \u2192 ${fmt(m.stockPosterior)}` : "")), (m.costoUnitario || m.ingresoUnitario) && /* @__PURE__ */ import_react.default.createElement("span", { className: "mono text-[11.5px]", style: { color: C.inkSoft } }, "\u20AC", fmt(m.tipo === "entrada" ? m.costoUnitario : m.ingresoUnitario), "/", producto.unidad)));
  }))), seccion === "pedidos" && (pedidosDelProducto.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Este producto no est\xE1 en ning\xFAn pedido a proveedor." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, pedidosDelProducto.map(({ pedido, item }) => /* @__PURE__ */ import_react.default.createElement(Card, { key: pedido.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px] font-medium" }, (proveedorPorId(pedido.proveedorId) || {}).nombre || "Proveedor eliminado"), /* @__PURE__ */ import_react.default.createElement(Pill, { color: pedido.estado === "Recibido" ? C.inkSoft : pedido.estado === "Parcial" ? C.amber : C.accent }, pedido.estado)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, pedido.fecha, " \xB7 pedidas ", fmt(item.cantidad), " ", producto.unidad, " \xB7 recibidas ", fmt(item.cantidadRecibida || 0)))))), seccion === "compras" && (comprasDelProducto.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin compras confirmadas de este producto." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, comprasDelProducto.map(({ albaran, linea, unidades, precioUnitario }) => /* @__PURE__ */ import_react.default.createElement(Card, { key: albaran.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between mb-1" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px] font-medium" }, (proveedorPorId(albaran.proveedorId) || {}).nombre || "Proveedor eliminado"), /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[11px]", style: { color: C.inkSoft } }, "Albar\xE1n ", albaran.numero || "s/n")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]", style: { color: C.inkSoft } }, albaran.fecha, " \xB7 ", fmt(unidades), " ", producto.unidad, " a \u20AC", fmt(precioUnitario), "/", producto.unidad, linea.lote && ` \xB7 lote ${linea.lote}`, linea.caducidad && ` \xB7 caduca ${linea.caducidad}`))))), seccion === "traspasos" && (traspasosDelProducto.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Sin traspasos entre almac\xE9n y piso de venta para este producto." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, traspasosDelProducto.map((t) => /* @__PURE__ */ import_react.default.createElement(Card, { key: t.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[12.5px]" }, t.direccion === "a_piso" ? "Almac\xE9n \u2192 Piso de venta" : "Piso de venta \u2192 Almac\xE9n"), /* @__PURE__ */ import_react.default.createElement("span", { className: "mono text-[12.5px] font-semibold" }, fmt(t.cantidad), " ", producto.unidad)), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, t.fecha, " ", t.hora)))))));
}
function BusquedaGlobal({ productos, proveedores, clientes, fichasCosto, empleados, setTab }) {
  const [q, setQ] = (0, import_react.useState)("");
  const resultados = (0, import_react.useMemo)(() => {
    const texto = q.trim().toLowerCase();
    if (texto.length < 2) return null;
    return {
      productos: productos.filter((p) => p.nombre.toLowerCase().includes(texto) || (p.codigoBarras || "").toLowerCase().includes(texto)).slice(0, 8),
      proveedores: proveedores.filter((p) => p.nombre.toLowerCase().includes(texto)).slice(0, 8),
      clientes: clientes.filter((c) => c.nombre.toLowerCase().includes(texto)).slice(0, 8),
      fichasCosto: fichasCosto.filter((f) => f.nombre.toLowerCase().includes(texto)).slice(0, 8),
      empleados: empleados.filter((e) => e.nombre.toLowerCase().includes(texto)).slice(0, 8)
    };
  }, [q, productos, proveedores, clientes, fichasCosto, empleados]);
  const totalResultados = resultados ? Object.values(resultados).reduce((a, lista) => a + lista.length, 0) : 0;
  const GRUPOS = [
    { clave: "productos", titulo: "Productos", tab: "productos", icon: Package },
    { clave: "proveedores", titulo: "Proveedores", tab: "proveedores", icon: Truck },
    { clave: "clientes", titulo: "Clientes", tab: "clientes", icon: Users },
    { clave: "fichasCosto", titulo: "Fichas de costo", tab: "fichas", icon: Calculator },
    { clave: "empleados", titulo: "Personal", tab: "personal", icon: UserRound }
  ];
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, null, "Buscar en todo el programa"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement(
    Input,
    {
      value: q,
      onChange: (e) => setQ(e.target.value),
      placeholder: "Escribe un nombre\u2026 (productos, proveedores, clientes, fichas, personal)",
      autoFocus: true
    }
  )), q.trim().length > 0 && q.trim().length < 2 ? /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]", style: { color: C.inkSoft } }, "Sigue escribiendo, al menos 2 letras.") : resultados === null ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "Escribe algo para buscar en productos, proveedores, clientes, fichas de costo y personal." }) : totalResultados === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: `Sin resultados para "${q}".` }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-4" }, GRUPOS.map((g) => {
    const lista = resultados[g.clave];
    if (!lista || lista.length === 0) return null;
    const Icono = g.icon;
    return /* @__PURE__ */ import_react.default.createElement("div", { key: g.clave }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-1.5 text-[12px] font-semibold mb-2", style: { color: C.inkSoft } }, /* @__PURE__ */ import_react.default.createElement(Icono, { size: 14 }), " ", g.titulo, " (", lista.length, ")"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, lista.map((item) => /* @__PURE__ */ import_react.default.createElement(
      "button",
      {
        key: item.id,
        onClick: () => setTab(g.tab),
        className: "w-full text-left flex items-center justify-between px-3 py-2.5 rounded-lg",
        style: { border: `1px solid ${C.line}`, background: C.surface }
      },
      /* @__PURE__ */ import_react.default.createElement("span", { className: "text-[13px]", style: { color: C.ink } }, item.nombre),
      /* @__PURE__ */ import_react.default.createElement(ChevronRight, { size: 15, color: C.inkSoft })
    ))));
  })));
}
function AceiteFreidoras({ freidoras = [], registrosAceite = [], productos, addFreidora, updateFreidora, deleteFreidora, registrarCambio, registrarRelleno, eliminarRegistroAceite, consumoPorCiclo }) {
  const [showForm, setShowForm] = (0, import_react.useState)(false);
  const [editingId, setEditingId] = (0, import_react.useState)(null);
  const [form, setForm] = (0, import_react.useState)({ nombre: "", litrosCarga: "", rellenoHabitual: "5", productoAceiteId: "" });
  const [error, setError] = (0, import_react.useState)("");
  const [accion, setAccion] = (0, import_react.useState)(null);
  const [litros, setLitros] = (0, import_react.useState)("");
  const [tipoCambio, setTipoCambio] = (0, import_react.useState)("Voluntario");
  const [observaciones, setObservaciones] = (0, import_react.useState)("");
  const [responsable, setResponsable] = (0, import_react.useState)("");
  const [errorAccion, setErrorAccion] = (0, import_react.useState)("");
  const [confirmacionFaltanteAceite, setConfirmacionFaltanteAceite] = (0, import_react.useState)("");
  const [confirmDeleteId, setConfirmDeleteId] = (0, import_react.useState)(null);
  const [confirmEliminarRegistro, setConfirmEliminarRegistro] = (0, import_react.useState)(null);
  const [avisoStockNoDevuelto, setAvisoStockNoDevuelto] = (0, import_react.useState)("");
  const aceitesPosibles = (0, import_react.useMemo)(
    () => productos.filter((p) => /aceite|oliva|girasol/i.test(p.nombre)).sort((a, b) => a.nombre.localeCompare(b.nombre, "es")),
    [productos]
  );
  function abrirNueva() {
    setEditingId(null);
    setForm({ nombre: "", litrosCarga: "", rellenoHabitual: "5", productoAceiteId: aceitesPosibles[0]?.id || "" });
    setError("");
    setShowForm(true);
  }
  function abrirEdicion(f) {
    setEditingId(f.id);
    setForm({
      nombre: f.nombre,
      litrosCarga: String(f.litrosCarga || ""),
      rellenoHabitual: String(f.rellenoHabitual || 5),
      productoAceiteId: f.productoAceiteId || ""
    });
    setError("");
    setShowForm(true);
  }
  function submit() {
    if (!form.nombre.trim()) {
      setError("Ponle un nombre a la freidora.");
      return;
    }
    if (!(Number(form.litrosCarga) > 0)) {
      setError("Indica cu\xE1ntos litros lleva un cambio completo.");
      return;
    }
    if (!form.productoAceiteId) {
      setError("Elige de qu\xE9 aceite del almac\xE9n se descuenta.");
      return;
    }
    if (editingId) updateFreidora(editingId, { ...form, litrosCarga: Number(form.litrosCarga), rellenoHabitual: Number(form.rellenoHabitual) });
    else addFreidora(form);
    setShowForm(false);
    setError("");
  }
  function abrirAccion(freidora, tipo) {
    setAccion({ freidora, tipo });
    setLitros(String(tipo === "cambio" ? freidora.litrosCarga || "" : freidora.rellenoHabitual || 5));
    setTipoCambio("Voluntario");
    setObservaciones("");
    setResponsable("");
    setErrorAccion("");
    setConfirmacionFaltanteAceite("");
  }
  const FRASE_CONFIRMACION_ACEITE = "CONFIRMO";
  function confirmarAccion() {
    const litrosNumero = Number(litros);
    const aceiteEnlazado = productos.find((p) => p.id === accion.freidora.productoAceiteId);
    const stockDisponible = litrosDisponibles(aceiteEnlazado);
    if (aceiteEnlazado && litrosNumero > stockDisponible) {
      if (confirmacionFaltanteAceite.trim().toUpperCase() !== FRASE_CONFIRMACION_ACEITE) {
        setErrorAccion(
          `Solo quedan ${fmt(stockDisponible)} L de ${aceiteEnlazado.nombre}, y est\xE1s pidiendo ${fmt(litrosNumero)} L. Si quieres registrarlo igual, escribe "${FRASE_CONFIRMACION_ACEITE}" en la casilla de abajo.`
        );
        return;
      }
    }
    const datos = { freidoraId: accion.freidora.id, litros: Number(litros), responsable };
    const r = accion.tipo === "cambio" ? registrarCambio({ ...datos, tipoCambio, observaciones }) : registrarRelleno(datos);
    if (!r.ok) {
      setErrorAccion(r.error);
      return;
    }
    setConfirmacionFaltanteAceite("");
    setAccion(null);
  }
  const registrosRecientes = (0, import_react.useMemo)(
    () => (registrosAceite || []).slice(0, 25),
    [registrosAceite]
  );
  return /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement(SectionTitle, { action: /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: abrirNueva }, /* @__PURE__ */ import_react.default.createElement(Plus, { size: 15 }), " A\xF1adir freidora") }, "Aceite de freidoras"), /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px]" }, "Registra los cambios de aceite y los rellenos del d\xEDa a d\xEDa. Cada uno descuenta del almac\xE9n los litros que lleve, as\xED que el consumo real queda repartido en el tiempo tal como ocurre \u2014 no todo de golpe el d\xEDa del cambio.")), avisoStockNoDevuelto && /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-4", style: { background: C.redSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, avisoStockNoDevuelto)), freidoras.length === 0 ? /* @__PURE__ */ import_react.default.createElement(Empty, { text: "A\xF1ade tu primera freidora para empezar a registrar los cambios de aceite." }) : /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-3 mb-5" }, freidoras.map((f) => {
    const ciclo = consumoPorCiclo ? consumoPorCiclo(f.id, registrosAceite) : null;
    const aceite = productos.find((p) => p.id === f.productoAceiteId);
    return /* @__PURE__ */ import_react.default.createElement(Card, { key: f.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-start justify-between mb-2" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "font-semibold text-[14px]" }, f.nombre), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: C.inkSoft } }, "Cambio de ", fmt(f.litrosCarga), " L \xB7 relleno habitual ", fmt(f.rellenoHabitual), " L"), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px]", style: { color: aceite ? C.inkSoft : C.red } }, aceite ? `Descuenta de: ${aceite.nombre} (quedan ${fmt(litrosDisponibles(aceite))} L)` : "\u26A0 El aceite enlazado ya no existe")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => abrirEdicion(f) }, /* @__PURE__ */ import_react.default.createElement(Pencil, { size: 14, color: C.inkSoft })), /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setConfirmDeleteId(f.id), "aria-label": "Eliminar freidora" }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 14, color: C.inkSoft })))), ciclo && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11.5px] mb-2 p-2 rounded-lg", style: { background: C.bg } }, "Desde el \xFAltimo cambio (", ciclo.desde, ", hace ", ciclo.dias, " d\xEDa", ciclo.dias > 1 ? "s" : "", "):", " ", /* @__PURE__ */ import_react.default.createElement("b", { className: "mono" }, fmt(ciclo.litrosTotal), " L"), " en total", ciclo.numRellenos > 0 && ` (${fmt(ciclo.litrosCambio)} del cambio + ${fmt(ciclo.litrosRelleno)} en ${ciclo.numRellenos} relleno${ciclo.numRellenos > 1 ? "s" : ""})`, ciclo.coste > 0 && ` \xB7 \u20AC${fmt(ciclo.coste)}`), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, onClick: () => abrirAccion(f, "cambio") }, "Cambio de aceite"), /* @__PURE__ */ import_react.default.createElement(Btn, { small: true, variant: "ghost", onClick: () => abrirAccion(f, "relleno") }, "Relleno")));
  })), registrosRecientes.length > 0 && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] font-medium mb-2", style: { color: C.inkSoft } }, "\xDAltimos registros"), /* @__PURE__ */ import_react.default.createElement("div", { className: "space-y-1.5" }, registrosRecientes.map((r) => /* @__PURE__ */ import_react.default.createElement(Card, { key: r.id }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ import_react.default.createElement("div", null, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px]" }, r.tipo === "cambio" ? "Cambio de aceite" : "Relleno", " \xB7 ", r.freidoraNombre, r.tipo === "cambio" && r.tipoCambio === "Obligatorio" && /* @__PURE__ */ import_react.default.createElement(Pill, { color: C.amber }, "obligatorio")), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[10.5px]", style: { color: C.inkSoft } }, r.fecha, " ", r.hora, r.responsable ? ` \xB7 ${r.responsable}` : "", r.observaciones ? ` \xB7 ${r.observaciones}` : "")), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ import_react.default.createElement("span", { className: "mono text-[12.5px]" }, fmt(r.litros), " L"), eliminarRegistroAceite && /* @__PURE__ */ import_react.default.createElement("button", { onClick: () => setConfirmEliminarRegistro(r), "aria-label": "Eliminar registro" }, /* @__PURE__ */ import_react.default.createElement(Trash2, { size: 14, color: C.inkSoft })))))))), showForm && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setShowForm(false), title: editingId ? "Editar freidora" : "Nueva freidora" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Nombre" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: form.nombre, onChange: (e) => setForm({ ...form, nombre: e.target.value }), placeholder: "Freidora 1 (churros)" })), /* @__PURE__ */ import_react.default.createElement("div", { className: "grid grid-cols-2 gap-x-3" }, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Litros de un cambio completo" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.litrosCarga, onChange: (e) => setForm({ ...form, litrosCarga: e.target.value }), placeholder: "50" })), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Relleno habitual (litros)" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: form.rellenoHabitual, onChange: (e) => setForm({ ...form, rellenoHabitual: e.target.value }), placeholder: "5" }))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "\xBFDe qu\xE9 aceite del almac\xE9n se descuenta?" }, /* @__PURE__ */ import_react.default.createElement(
    "select",
    {
      value: form.productoAceiteId,
      onChange: (e) => setForm({ ...form, productoAceiteId: e.target.value }),
      className: "w-full rounded-lg px-3 py-2 text-[13px]",
      style: { border: `1px solid ${C.line}`, background: C.surface, color: C.ink }
    },
    /* @__PURE__ */ import_react.default.createElement("option", { value: "" }, "\u2014 Elige el aceite \u2014"),
    aceitesPosibles.map((p) => /* @__PURE__ */ import_react.default.createElement("option", { key: p.id, value: p.id }, p.nombre))
  ), aceitesPosibles.length === 0 && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: C.amber } }, 'No encuentro ning\xFAn producto con "aceite" en el nombre. Cr\xE9alo primero en Productos.')), error && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, error), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mt-1" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: submit }, editingId ? "Guardar cambios" : "A\xF1adir"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setShowForm(false) }, "Cancelar"))), accion && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setAccion(null), title: accion.tipo === "cambio" ? "Cambio de aceite" : "Relleno de aceite" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-3" }, accion.freidora.nombre), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Litros" }, /* @__PURE__ */ import_react.default.createElement(Input, { type: "number", value: litros, onChange: (e) => {
    setLitros(e.target.value);
    setConfirmacionFaltanteAceite("");
  } }), /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[11px] mt-1", style: { color: C.inkSoft } }, accion.tipo === "cambio" ? "Se propone la carga completa. Aj\xFAstalo si has echado m\xE1s o menos." : "Se propone el relleno habitual. Aj\xFAstalo a ojo, no hace falta medirlo exacto.")), accion.tipo === "cambio" && /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, null, /* @__PURE__ */ import_react.default.createElement(Field, { label: "Tipo de cambio" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, ["Voluntario", "Obligatorio"].map((t) => /* @__PURE__ */ import_react.default.createElement(
    "button",
    {
      key: t,
      onClick: () => setTipoCambio(t),
      className: "text-[12px] font-medium px-3 py-1.5 rounded-full",
      style: {
        background: tipoCambio === t ? C.accent : C.surface,
        color: tipoCambio === t ? "#fff" : C.inkSoft,
        border: `1px solid ${tipoCambio === t ? C.accent : C.line}`
      }
    },
    t
  )))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Observaciones (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: observaciones, onChange: (e) => setObservaciones(e.target.value), placeholder: "Aceite muy oscuro, olor fuerte\u2026" }))), /* @__PURE__ */ import_react.default.createElement(Field, { label: "Qui\xE9n lo hace (opcional)" }, /* @__PURE__ */ import_react.default.createElement(Input, { value: responsable, onChange: (e) => setResponsable(e.target.value) })), (() => {
    const aceiteEnlazado = accion && productos.find((p) => p.id === accion.freidora.productoAceiteId);
    const stockDisponible = litrosDisponibles(aceiteEnlazado);
    const faltaAceite = aceiteEnlazado && Number(litros) > stockDisponible;
    if (!faltaAceite) return null;
    return /* @__PURE__ */ import_react.default.createElement(Card, { className: "mb-3", style: { background: C.redSoft, border: "none" } }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-2" }, "Solo quedan ", fmt(stockDisponible), " L de ", aceiteEnlazado.nombre, ", y est\xE1s pidiendo ", fmt(Number(litros)), " L. Si lo registras igual, se quedar\xE1 en 0 aunque hayas gastado m\xE1s de lo que ten\xEDas."), /* @__PURE__ */ import_react.default.createElement(Field, { label: `Para registrarlo igual, escribe "${FRASE_CONFIRMACION_ACEITE}"` }, /* @__PURE__ */ import_react.default.createElement(
      Input,
      {
        value: confirmacionFaltanteAceite,
        onChange: (e) => setConfirmacionFaltanteAceite(e.target.value),
        placeholder: FRASE_CONFIRMACION_ACEITE
      }
    )));
  })(), errorAccion && /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12px] mb-2", style: { color: C.red } }, errorAccion), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2 mt-1" }, /* @__PURE__ */ import_react.default.createElement(Btn, { onClick: confirmarAccion }, "Registrar y descontar del almac\xE9n"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setAccion(null) }, "Cancelar"))), confirmDeleteId && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmDeleteId(null), title: "Eliminar freidora" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-4" }, "Se borra la freidora. Los registros de cambios y rellenos ya hechos se conservan en el hist\xF3rico."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "danger", onClick: () => {
    deleteFreidora(confirmDeleteId);
    setConfirmDeleteId(null);
  } }, "S\xED, eliminar"), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmDeleteId(null) }, "Cancelar"))), confirmEliminarRegistro && /* @__PURE__ */ import_react.default.createElement(Modal, { onClose: () => setConfirmEliminarRegistro(null), title: "Eliminar registro de aceite" }, /* @__PURE__ */ import_react.default.createElement("div", { className: "text-[12.5px] mb-4" }, "Se borra este ", confirmEliminarRegistro.tipo === "cambio" ? "cambio" : "relleno", " de", " ", /* @__PURE__ */ import_react.default.createElement("b", null, fmt(confirmEliminarRegistro.litros), " L"), " (", confirmEliminarRegistro.freidoraNombre, "), y esos litros", " ", /* @__PURE__ */ import_react.default.createElement("b", null, "vuelven a sumarse"), " al stock del aceite del almac\xE9n \u2014 como si nunca se hubiera registrado."), /* @__PURE__ */ import_react.default.createElement("div", { className: "flex gap-2" }, /* @__PURE__ */ import_react.default.createElement(
    Btn,
    {
      variant: "danger",
      onClick: () => {
        const r = eliminarRegistroAceite(confirmEliminarRegistro);
        setAvisoStockNoDevuelto(
          r && r.stockDevuelto === false ? "Se ha borrado el registro, pero no se ha podido devolver el stock: ni el registro ni la freidora ya existente sab\xEDan a qu\xE9 aceite pertenec\xEDa. Rev\xEDsalo a mano en Productos." : ""
        );
        setConfirmEliminarRegistro(null);
      }
    },
    "S\xED, eliminar y devolver el aceite"
  ), /* @__PURE__ */ import_react.default.createElement(Btn, { variant: "ghost", onClick: () => setConfirmEliminarRegistro(null) }, "Cancelar"))));
}
function AppConSesion() {
  const [fase, setFase] = (0, import_react.useState)("comprobando");
  const [cliente, setCliente] = (0, import_react.useState)(null);
  const [email, setEmail] = (0, import_react.useState)("");
  const [password, setPassword] = (0, import_react.useState)("");
  const [error, setError] = (0, import_react.useState)("");
  const [cargando, setCargando] = (0, import_react.useState)(false);
  (0, import_react.useEffect)(() => {
    let activo = true;
    (async () => {
      const hayNube = await window.conectarNube();
      if (!activo) return;
      if (!hayNube) {
        window.actualizarIndicador();
        setFase("dentro");
        return;
      }
      const supabase = window.__nubeCliente;
      setCliente(supabase);
      try {
        const { data } = await supabase.auth.getSession();
        if (!activo) return;
        if (data.session) {
          await window.subirPendientes();
          setFase("dentro");
        } else {
          setFase("login");
        }
        supabase.auth.onAuthStateChange(async (_evento, s) => {
          if (!activo) return;
          if (s) {
            await window.subirPendientes();
            setFase("dentro");
          } else setFase("login");
        });
      } catch (e) {
        window.__nubeActiva = false;
        window.actualizarIndicador();
        setFase("dentro");
      }
    })();
    return () => {
      activo = false;
    };
  }, []);
  async function entrar(e) {
    e.preventDefault();
    if (!cliente) return;
    setError("");
    setCargando(true);
    const { error: error2 } = await cliente.auth.signInWithPassword({ email, password });
    setCargando(false);
    if (error2) setError("Correo o contrase\xF1a incorrectos.");
  }
  function entrarSinNube() {
    window.__nubeActiva = false;
    window.actualizarIndicador();
    setFase("dentro");
  }
  const estiloBase = { minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", background: "#F7F3E9", fontFamily: "'IBM Plex Sans', ui-sans-serif, system-ui", padding: 20 };
  if (fase === "comprobando") {
    return /* @__PURE__ */ import_react.default.createElement("div", { style: { ...estiloBase, color: "#6B7A6E", fontSize: 13.5 } }, "Comprobando conexi\xF3n\u2026");
  }
  if (fase === "login") {
    return /* @__PURE__ */ import_react.default.createElement("div", { style: estiloBase }, /* @__PURE__ */ import_react.default.createElement("form", { onSubmit: entrar, style: { background: "#fff", padding: 28, borderRadius: 14, width: "100%", maxWidth: 340, border: "1px solid #E2DBC7", boxShadow: "0 4px 20px rgba(23,36,28,0.06)" } }, /* @__PURE__ */ import_react.default.createElement("div", { style: { fontSize: 18, fontWeight: 700, marginBottom: 4, color: "#17241C" } }, "Chocoloyos"), /* @__PURE__ */ import_react.default.createElement("div", { style: { fontSize: 12.5, color: "#6B7A6E", marginBottom: 22 } }, "Inicia sesi\xF3n para sincronizar entre dispositivos"), /* @__PURE__ */ import_react.default.createElement(
      "input",
      {
        type: "email",
        required: true,
        autoFocus: true,
        placeholder: "Correo",
        value: email,
        onChange: (e) => setEmail(e.target.value),
        style: { width: "100%", padding: "10px 12px", marginBottom: 10, border: "1px solid #E2DBC7", borderRadius: 8, fontSize: 14, boxSizing: "border-box", fontFamily: "inherit" }
      }
    ), /* @__PURE__ */ import_react.default.createElement(
      "input",
      {
        type: "password",
        required: true,
        placeholder: "Contrase\xF1a",
        value: password,
        onChange: (e) => setPassword(e.target.value),
        style: { width: "100%", padding: "10px 12px", marginBottom: 16, border: "1px solid #E2DBC7", borderRadius: 8, fontSize: 14, boxSizing: "border-box", fontFamily: "inherit" }
      }
    ), error && /* @__PURE__ */ import_react.default.createElement("div", { style: { color: "#B5473A", fontSize: 12.5, marginBottom: 12 } }, error), /* @__PURE__ */ import_react.default.createElement("button", { type: "submit", disabled: cargando, style: { width: "100%", padding: "11px", background: "#8C6D2A", color: "#fff", border: "none", borderRadius: 8, fontSize: 14, fontWeight: 700, cursor: "pointer" } }, cargando ? "Entrando\u2026" : "Entrar"), /* @__PURE__ */ import_react.default.createElement("div", { onClick: entrarSinNube, style: { marginTop: 16, textAlign: "center", fontSize: 12, color: "#6B7A6E", textDecoration: "underline", cursor: "pointer" } }, "Trabajar solo en este equipo, sin sincronizar")));
  }
  return /* @__PURE__ */ import_react.default.createElement(GestionAlmacen, null);
}
(0, import_client.createRoot)(document.getElementById("root")).render(/* @__PURE__ */ import_react.default.createElement(AppConSesion, null));
